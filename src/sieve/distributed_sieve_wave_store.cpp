#include "../relation/ooc_private_cleanup_union_internal.hpp"
#include "../relation/ooc_private_handoff_adoption_internal.hpp"
#include "../relation/ooc_private_handoff_cleanup_authorization_internal.hpp"
#include "../relation/ooc_private_lease_recovery_internal.hpp"
#include "../relation/ooc_private_lease_recovery_protocol_internal.hpp"
#include "../relation/ooc_private_lease_reservation_protocol_internal.hpp"
#include "distributed_sieve_bound_work_internal.hpp"
#include "distributed_sieve_wave_store_internal.hpp"
#include "distributed_sieve_worker_cleanup_codec_internal.hpp"
#include "distributed_sieve_worker_launcher_internal.hpp"
#include "distributed_sieve_worker_work_package_file_internal.hpp"

#include <gnfs/relation/ooc_cleanup_transaction.hpp>
#include <gnfs/relation/ooc_relation_store.hpp>
#include <gnfs/relation/relation_corpus_sha256.hpp>
#include <gnfs/relation/relation_sequence_receipt.hpp>
#include <gnfs/util/process.hpp>

#include <algorithm>
#include <array>
#include <atomic>
#include <cerrno>
#include <cstddef>
#include <cstdint>
#include <filesystem>
#include <iterator>
#include <limits>
#include <memory>
#include <new>
#include <optional>
#include <span>
#include <stdexcept>
#include <string>
#include <string_view>
#include <system_error>
#include <utility>
#include <variant>
#include <vector>

#if !defined(_WIN32)
#include <dirent.h>
#include <fcntl.h>
#include <sys/file.h>
#include <sys/stat.h>
#if defined(__APPLE__)
#include <sys/acl.h>
#elif defined(__linux__)
#include <sys/syscall.h>
#include <sys/xattr.h>
#endif
#include <unistd.h>
#endif

namespace gnfs::sieve::distributed_sieve_resume_detail {

class WorkerHandoffTypedValidatorAuthorityV1 final {
public:
    [[nodiscard]] static gnfs::relation::ooc_cleanup_detail::
        PrivateHandoffPublicationTypedValidatorV1
        bind(gnfs::relation::ooc_cleanup_detail::PrivateHandoffPublicationTypedValidatorV1::Validate
                 validate,
             void* context) noexcept {
        return gnfs::relation::ooc_cleanup_detail::PrivateHandoffPublicationTypedValidatorV1(
            validate, context);
    }
};

namespace {

namespace durable_record = gnfs::util::durable_immutable_record;
namespace private_lease = gnfs::relation::ooc_cleanup_detail;
namespace work_package_file = gnfs::sieve::distributed_sieve_worker_work_package_file_detail;
namespace worker_cleanup_codec = gnfs::sieve::distributed_sieve_worker_cleanup_codec_detail;

static_assert(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVATION_BOUNDARIES.size() ==
              private_lease::PRIVATE_LEASE_RESERVATION_BOUNDARIES.size());
static_assert(
    static_cast<std::size_t>(DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) ==
    static_cast<std::size_t>(private_lease::PrivateLeaseReservationBoundary::PermitAcquired));
static_assert(static_cast<std::size_t>(
                  DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable) ==
              static_cast<std::size_t>(
                  private_lease::PrivateLeaseReservationBoundary::ReservedPendingDurable));
static_assert(static_cast<std::size_t>(
                  DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable) ==
              static_cast<std::size_t>(
                  private_lease::PrivateLeaseReservationBoundary::ReservedCanonicalDurable));
static_assert(static_cast<std::size_t>(
                  DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable) ==
              static_cast<std::size_t>(
                  private_lease::PrivateLeaseReservationBoundary::StagingDirectoryDurable));
static_assert(
    static_cast<std::size_t>(
        DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable) ==
    static_cast<std::size_t>(private_lease::PrivateLeaseReservationBoundary::OwnerPendingDurable));
static_assert(static_cast<std::size_t>(
                  DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable) ==
              static_cast<std::size_t>(
                  private_lease::PrivateLeaseReservationBoundary::OwnerCanonicalDurable));
static_assert(
    static_cast<std::size_t>(
        DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable) ==
    static_cast<std::size_t>(private_lease::PrivateLeaseReservationBoundary::OwnedPendingDurable));
static_assert(static_cast<std::size_t>(
                  DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable) ==
              static_cast<std::size_t>(
                  private_lease::PrivateLeaseReservationBoundary::OwnedCanonicalDurable));
static_assert(static_cast<std::size_t>(
                  DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) ==
              static_cast<std::size_t>(
                  private_lease::PrivateLeaseReservationBoundary::FinalDirectoryDurable));
static_assert([] {
    for (std::size_t index = 0;
         index < DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVATION_BOUNDARIES.size(); ++index) {
        if (static_cast<std::size_t>(
                DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVATION_BOUNDARIES[index]) !=
            static_cast<std::size_t>(private_lease::PRIVATE_LEASE_RESERVATION_BOUNDARIES[index])) {
            return false;
        }
    }
    return true;
}());
static_assert(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RECOVERY_EDGES.size() ==
              private_lease::PRIVATE_LEASE_RECOVERY_TRANSITIONS.size());
static_assert([] {
    for (std::size_t index = 0; index < DISTRIBUTED_SIEVE_PRIVATE_LEASE_RECOVERY_EDGES.size();
         ++index) {
        const auto& edge = DISTRIBUTED_SIEVE_PRIVATE_LEASE_RECOVERY_EDGES[index];
        const auto& transition = private_lease::PRIVATE_LEASE_RECOVERY_TRANSITIONS[index];
        if (static_cast<std::size_t>(edge.source) != static_cast<std::size_t>(transition.source) ||
            static_cast<std::size_t>(edge.successor) !=
                static_cast<std::size_t>(transition.successor)) {
            return false;
        }
    }
    return true;
}());
static_assert(
    static_cast<std::size_t>(
        DistributedSieveWorkerHandoffResumeObservationPointV1::AfterExpectedPrefixValidated) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterExpectedPrefixValidated));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 BeforePendingRollbackSourceDirectorySync) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 BeforePendingRollbackSourceDirectorySync));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackSourceDirectoryDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackSourceDirectoryDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 BeforePendingRollbackDestinationDirectorySync) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 BeforePendingRollbackDestinationDirectorySync));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackDestinationDirectoryDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackDestinationDirectoryDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackPreactiveDirectoryQuarantinedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackPreactiveDirectoryQuarantinedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackPreactiveDataRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackPreactiveDataRemovedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackPreactiveIndexRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackPreactiveIndexRemovedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackOwnerRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackOwnerRemovedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackLeaseDirectoryRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackLeaseDirectoryRemovedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackReservedRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackReservedRemovedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackOwnedRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackOwnedRemovedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 BeforePendingRollbackTombstoneRemovalValidated) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 BeforePendingRollbackTombstoneRemovalValidated));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::
                                 AfterPendingRollbackTombstoneRemovedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterPendingRollbackTombstoneRemovedDurable));
static_assert(
    static_cast<std::size_t>(
        DistributedSieveWorkerHandoffResumeObservationPointV1::AfterCanonicalConfirmedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterCanonicalConfirmedDurable));
static_assert(
    static_cast<std::size_t>(
        DistributedSieveWorkerHandoffResumeObservationPointV1::AfterReservedRevokedDurable) ==
    static_cast<std::size_t>(private_lease::PrivateHandoffPublicationResumeObservationPointV1::
                                 AfterReservedRevokedDurable));
static_assert(
    static_cast<std::size_t>(DistributedSieveWorkerHandoffResumeObservationPointV1::Count) ==
    static_cast<std::size_t>(
        private_lease::PrivateHandoffPublicationResumeObservationPointV1::Count));
static_assert([] {
    using MergePoint = DistributedSieveMergePreparedResumeObservationPointV1;
    using RelationPoint = private_lease::PrivateHandoffPublicationResumeObservationPointV1;
    constexpr std::array wave{
        MergePoint::AfterExpectedPrefixValidated,
        MergePoint::BeforePendingRollbackSourceDirectorySync,
        MergePoint::AfterPendingRollbackSourceDirectoryDurable,
        MergePoint::BeforePendingRollbackDestinationDirectorySync,
        MergePoint::AfterPendingRollbackDestinationDirectoryDurable,
        MergePoint::AfterPendingRollbackPreactiveDirectoryQuarantinedDurable,
        MergePoint::AfterPendingRollbackPreactiveDataRemovedDurable,
        MergePoint::AfterPendingRollbackPreactiveIndexRemovedDurable,
        MergePoint::AfterPendingRollbackOwnerRemovedDurable,
        MergePoint::AfterPendingRollbackLeaseDirectoryRemovedDurable,
        MergePoint::AfterPendingRollbackReservedRemovedDurable,
        MergePoint::AfterPendingRollbackOwnedRemovedDurable,
        MergePoint::BeforePendingRollbackTombstoneRemovalValidated,
        MergePoint::AfterPendingRollbackTombstoneRemovedDurable,
        MergePoint::AfterCanonicalConfirmedDurable,
        MergePoint::AfterReservedRevokedDurable,
        MergePoint::Count,
    };
    constexpr std::array relation{
        RelationPoint::AfterExpectedPrefixValidated,
        RelationPoint::BeforePendingRollbackSourceDirectorySync,
        RelationPoint::AfterPendingRollbackSourceDirectoryDurable,
        RelationPoint::BeforePendingRollbackDestinationDirectorySync,
        RelationPoint::AfterPendingRollbackDestinationDirectoryDurable,
        RelationPoint::AfterPendingRollbackPreactiveDirectoryQuarantinedDurable,
        RelationPoint::AfterPendingRollbackPreactiveDataRemovedDurable,
        RelationPoint::AfterPendingRollbackPreactiveIndexRemovedDurable,
        RelationPoint::AfterPendingRollbackOwnerRemovedDurable,
        RelationPoint::AfterPendingRollbackLeaseDirectoryRemovedDurable,
        RelationPoint::AfterPendingRollbackReservedRemovedDurable,
        RelationPoint::AfterPendingRollbackOwnedRemovedDurable,
        RelationPoint::BeforePendingRollbackTombstoneRemovalValidated,
        RelationPoint::AfterPendingRollbackTombstoneRemovedDurable,
        RelationPoint::AfterCanonicalConfirmedDurable,
        RelationPoint::AfterReservedRevokedDurable,
        RelationPoint::Count,
    };
    for (std::size_t index = 0; index < wave.size(); ++index) {
        if (static_cast<std::size_t>(wave[index]) != static_cast<std::size_t>(relation[index])) {
            return false;
        }
    }
    return true;
}());

inline constexpr char LOCK_LEAF[] = ".gnfs-wave-v1.lock";
inline constexpr char MANIFEST_LEAF[] = ".gnfs-wave-v1.manifest";
inline constexpr char MANIFEST_PENDING_LEAF[] = ".gnfs-wave-v1.manifest.pending";
inline constexpr std::string_view WORKER_CORPUS_INDEX_LEAF = "corpus.relidx";
inline constexpr std::string_view WORKER_CORPUS_DATA_LEAF = "corpus.reldata";
inline constexpr std::string_view WORKER_HANDOFF_LEAF = "corpus.gnfs-ooc-private-handoff-v1";
inline constexpr std::string_view WORKER_HANDOFF_PENDING_LEAF =
    "corpus.gnfs-ooc-private-handoff-v1.pending";

static_assert(std::string_view(LOCK_LEAF) == DISTRIBUTED_SIEVE_WAVE_LOCK_LEAF);
static_assert(std::string_view(MANIFEST_LEAF) == DISTRIBUTED_SIEVE_WAVE_MANIFEST_LEAF);
static_assert(std::string_view(MANIFEST_PENDING_LEAF) ==
              DISTRIBUTED_SIEVE_WAVE_MANIFEST_PENDING_LEAF);

[[nodiscard]] std::error_code invalid_argument_error() noexcept {
    return std::make_error_code(std::errc::invalid_argument);
}

[[nodiscard]] std::error_code protocol_error() noexcept {
    return std::make_error_code(std::errc::protocol_error);
}

[[nodiscard, maybe_unused]] std::error_code unsupported_error() noexcept {
    return std::make_error_code(std::errc::operation_not_supported);
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
diagnostic(DistributedSieveWaveStoreStatus status, std::error_code native_error = {}) noexcept {
    DistributedSieveWaveStoreDiagnostic result;
    result.status = status;
    result.native_error = native_error;
    return result;
}

[[nodiscard]] DistributedSieveWaveStoreOpenResult
open_failure(DistributedSieveWaveStoreDiagnostic failure) noexcept {
    return {nullptr, std::nullopt, std::move(failure)};
}

[[nodiscard]] constexpr char decimal_digit(std::uint32_t value) noexcept {
    return static_cast<char>('0' + value);
}

[[nodiscard]] constexpr bool nil_identity(const NativeIdentityV1& identity) noexcept {
    return identity.volume == 0 && identity.object == 0 && identity.generation == 0;
}

[[nodiscard]] constexpr bool nil_digest(const util::Sha256Digest& digest) noexcept {
    return digest == util::Sha256Digest{};
}

struct FrozenRoot final {
    std::filesystem::path absolute;
    std::filesystem::path leaf;
    std::vector<std::string> parent_components;
};

[[nodiscard]] std::optional<FrozenRoot>
freeze_absolute_root(const std::filesystem::path& requested) {
    if (requested.empty() || !requested.is_absolute()) {
        return std::nullopt;
    }

    const auto& native = requested.native();
    if (native.empty() ||
        std::find(native.begin(), native.end(),
                  static_cast<std::filesystem::path::value_type>(0)) != native.end()) {
        return std::nullopt;
    }

#if defined(_WIN32)
    const auto is_separator = [](std::filesystem::path::value_type value) noexcept {
        return value == static_cast<std::filesystem::path::value_type>('/') ||
               value == static_cast<std::filesystem::path::value_type>('\\');
    };
    if (is_separator(native.back())) {
        return std::nullopt;
    }
    for (std::size_t index = 1; index < native.size(); ++index) {
        if (is_separator(native[index]) && is_separator(native[index - 1])) {
            return std::nullopt;
        }
    }
    const std::filesystem::path normalized = requested.lexically_normal();
    if (normalized != requested) {
        return std::nullopt;
    }
    for (const auto& component : requested.relative_path()) {
        if (component.empty() || component == "." || component == "..") {
            return std::nullopt;
        }
    }
    const std::filesystem::path leaf = requested.filename();
    if (leaf.empty() || leaf == "." || leaf == "..") {
        return std::nullopt;
    }
    return FrozenRoot{requested, leaf, {}};
#else
    if (native.front() != '/' || native.size() == 1 || native.back() == '/') {
        return std::nullopt;
    }

    std::vector<std::string> components;
    std::size_t begin = 1;
    while (begin < native.size()) {
        const std::size_t end = native.find('/', begin);
        const std::size_t component_end = end == std::string::npos ? native.size() : end;
        if (component_end == begin) {
            return std::nullopt;
        }
        std::string component = native.substr(begin, component_end - begin);
        if (component == "." || component == "..") {
            return std::nullopt;
        }
        components.push_back(std::move(component));
        if (end == std::string::npos) {
            break;
        }
        begin = end + 1;
    }
    if (components.empty()) {
        return std::nullopt;
    }

    std::filesystem::path leaf(components.back());
    components.pop_back();
    return FrozenRoot{requested, std::move(leaf), std::move(components)};
#endif
}

[[nodiscard]] bool valid_manifest_draft(const WaveManifestV1& draft) noexcept {
    return nil_identity(draft.wave_root_identity) && nil_identity(draft.permanent_lock_identity) &&
           draft.lock_semantics_version == 0 && nil_digest(draft.self_digest);
}

[[nodiscard]] DistributedSieveProtocolStatus
validate_manifest_draft_semantics(const WaveManifestV1& draft) {
    WaveManifestV1 candidate = draft;
    candidate.wave_root_identity = NativeIdentityV1{.object = 1};
    candidate.permanent_lock_identity = NativeIdentityV1{.object = 2};
    candidate.lock_semantics_version = DISTRIBUTED_SIEVE_WAVE_LOCK_SEMANTICS_VERSION_V1;
    candidate.self_digest = {};
    const DistributedSieveProtocolRecordV1 record(std::move(candidate));
    return validate_distributed_sieve_record(record, false);
}

[[nodiscard]] std::uint64_t current_process_id() noexcept {
    const int process_id = util::process_id();
    return process_id > 0 ? static_cast<std::uint64_t>(process_id) : 0;
}

[[nodiscard]] bool process_matches(std::uint64_t expected_process_id) noexcept {
    return expected_process_id != 0 && current_process_id() == expected_process_id;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic process_mismatch() noexcept {
    return diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error());
}

#if !defined(_WIN32)

[[nodiscard]] std::error_code posix_error(int value) noexcept {
    return {value, std::generic_category()};
}

class UniqueFd final {
public:
    UniqueFd() noexcept = default;
    explicit UniqueFd(int descriptor) noexcept : descriptor_(descriptor) {}

    ~UniqueFd() {
        reset();
    }

    UniqueFd(const UniqueFd&) = delete;
    UniqueFd& operator=(const UniqueFd&) = delete;

    UniqueFd(UniqueFd&& other) noexcept : descriptor_(std::exchange(other.descriptor_, -1)) {}

    UniqueFd& operator=(UniqueFd&& other) noexcept {
        if (this != &other) {
            reset();
            descriptor_ = std::exchange(other.descriptor_, -1);
        }
        return *this;
    }

    [[nodiscard]] int get() const noexcept {
        return descriptor_;
    }

    [[nodiscard]] explicit operator bool() const noexcept {
        return descriptor_ >= 0;
    }

    [[nodiscard]] int release() noexcept {
        return std::exchange(descriptor_, -1);
    }

    void reset(int replacement = -1) noexcept {
        if (descriptor_ >= 0) {
            // POSIX leaves descriptor ownership unspecified after EINTR.
            // Retrying close could close an unrelated reused descriptor.
            (void)::close(descriptor_);
        }
        descriptor_ = replacement;
    }

private:
    int descriptor_ = -1;
};

class UniqueDirectory final {
public:
    explicit UniqueDirectory(DIR* directory) noexcept : directory_(directory) {}

    ~UniqueDirectory() {
        if (directory_ != nullptr) {
            (void)::closedir(directory_);
        }
    }

    UniqueDirectory(const UniqueDirectory&) = delete;
    UniqueDirectory& operator=(const UniqueDirectory&) = delete;

private:
    DIR* directory_ = nullptr;
};

[[nodiscard]] int open_retrying_eintr(const char* path, int flags) noexcept {
    int descriptor = -1;
    do {
        descriptor = ::open(path, flags);
    } while (descriptor < 0 && errno == EINTR);
    return descriptor;
}

[[nodiscard]] int openat_retrying_eintr(int parent, const char* leaf, int flags,
                                        mode_t mode = 0) noexcept {
    int descriptor = -1;
    do {
        descriptor = (flags & O_CREAT) != 0 ? ::openat(parent, leaf, flags, mode)
                                            : ::openat(parent, leaf, flags);
    } while (descriptor < 0 && errno == EINTR);
    return descriptor;
}

struct ParentWalkResult final {
    UniqueFd parent;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return static_cast<bool>(parent) &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ParentWalkResult
walk_parent_no_follow(const std::vector<std::string>& parent_components,
                      std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return {{}, process_mismatch()};
    }
    const int root_fd = open_retrying_eintr("/", O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (root_fd < 0) {
        return {{}, diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno))};
    }
    UniqueFd current(root_fd);
    for (const auto& component : parent_components) {
        if (!process_matches(creator_process_id)) {
            return {{}, process_mismatch()};
        }
        const int next_fd = openat_retrying_eintr(current.get(), component.c_str(),
                                                  O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
        if (next_fd < 0) {
            return {{},
                    diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno))};
        }
        current.reset(next_fd);
    }
    if (!process_matches(creator_process_id)) {
        return {{}, process_mismatch()};
    }
    return {std::move(current), {}};
}

[[nodiscard]] durable_record::NativeIdentity record_identity(const struct stat& metadata) noexcept {
    return {
        .first = static_cast<std::uint64_t>(metadata.st_dev),
        .second = static_cast<std::uint64_t>(metadata.st_ino),
        .third = 0,
    };
}

[[nodiscard]] NativeIdentityV1 protocol_identity(const struct stat& metadata) noexcept {
    return {
        .volume = static_cast<std::uint64_t>(metadata.st_dev),
        .object = static_cast<std::uint64_t>(metadata.st_ino),
        .generation = 0,
    };
}

[[nodiscard]] bool exact_mode(const struct stat& metadata, mode_t expected) noexcept {
    return (metadata.st_mode & static_cast<mode_t>(07777)) == expected;
}

[[nodiscard]] bool stable_metadata(const struct stat& before, const struct stat& after) noexcept {
    if (before.st_dev != after.st_dev || before.st_ino != after.st_ino ||
        before.st_uid != after.st_uid || before.st_gid != after.st_gid ||
        before.st_mode != after.st_mode || before.st_nlink != after.st_nlink ||
        before.st_size != after.st_size) {
        return false;
    }
#if defined(__APPLE__)
    return before.st_mtimespec.tv_sec == after.st_mtimespec.tv_sec &&
           before.st_mtimespec.tv_nsec == after.st_mtimespec.tv_nsec &&
           before.st_ctimespec.tv_sec == after.st_ctimespec.tv_sec &&
           before.st_ctimespec.tv_nsec == after.st_ctimespec.tv_nsec;
#else
    return before.st_mtim.tv_sec == after.st_mtim.tv_sec &&
           before.st_mtim.tv_nsec == after.st_mtim.tv_nsec &&
           before.st_ctim.tv_sec == after.st_ctim.tv_sec &&
           before.st_ctim.tv_nsec == after.st_ctim.tv_nsec;
#endif
}

enum class AclInspectionState : std::uint8_t {
    absent,
    present,
    unsupported,
    failed,
};

struct AclInspection final {
    AclInspectionState state = AclInspectionState::absent;
    std::error_code error;
};

[[nodiscard]] bool unsupported_acl_error(int value) noexcept {
    return value == ENOTSUP || value == EOPNOTSUPP || value == ENOSYS;
}

#if defined(__linux__)
[[nodiscard]] AclInspection inspect_linux_acl_name(int descriptor, const char* name) noexcept {
    ssize_t size = -1;
    do {
        size = ::fgetxattr(descriptor, name, nullptr, 0);
    } while (size < 0 && errno == EINTR);
    if (size >= 0) {
        return {AclInspectionState::present, protocol_error()};
    }
    const int saved_errno = errno;
    if (saved_errno == ENODATA) {
        return {};
    }
    if (unsupported_acl_error(saved_errno)) {
        return {AclInspectionState::unsupported, posix_error(saved_errno)};
    }
    return {AclInspectionState::failed, posix_error(saved_errno)};
}
#endif

[[nodiscard]] AclInspection inspect_acl(int descriptor, bool directory) noexcept {
#if defined(__APPLE__)
    (void)directory;
    errno = 0;
    acl_t acl = ::acl_get_fd_np(descriptor, ACL_TYPE_EXTENDED);
    if (acl == nullptr) {
        const int saved_errno = errno;
        if (saved_errno == ENOENT) {
            return {};
        }
        if (unsupported_acl_error(saved_errno)) {
            return {AclInspectionState::unsupported, posix_error(saved_errno)};
        }
        return {AclInspectionState::failed, posix_error(saved_errno)};
    }
    (void)::acl_free(acl);
    return {AclInspectionState::present, protocol_error()};
#elif defined(__linux__)
    const auto access = inspect_linux_acl_name(descriptor, "system.posix_acl_access");
    if (access.state != AclInspectionState::absent || !directory) {
        return access;
    }
    return inspect_linux_acl_name(descriptor, "system.posix_acl_default");
#else
    (void)descriptor;
    (void)directory;
    return {AclInspectionState::unsupported, unsupported_error()};
#endif
}

[[nodiscard]] std::optional<DistributedSieveWaveStoreDiagnostic>
acl_rejection(int descriptor, bool directory,
              DistributedSieveWaveStoreStatus rejected_status) noexcept {
    const auto inspected = inspect_acl(descriptor, directory);
    switch (inspected.state) {
    case AclInspectionState::absent:
        return std::nullopt;
    case AclInspectionState::present:
        return diagnostic(rejected_status, inspected.error ? inspected.error : protocol_error());
    case AclInspectionState::unsupported:
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          inspected.error ? inspected.error : unsupported_error());
    case AclInspectionState::failed:
        return diagnostic(rejected_status, inspected.error ? inspected.error : protocol_error());
    }
    return diagnostic(rejected_status, protocol_error());
}

[[nodiscard]] bool sync_descriptor(int descriptor, std::error_code& error) noexcept {
    int result = -1;
    do {
#if defined(__APPLE__)
        result = ::fcntl(descriptor, F_FULLFSYNC);
#else
        result = ::fsync(descriptor);
#endif
    } while (result != 0 && errno == EINTR);
    if (result == 0) {
        return true;
    }
    error = posix_error(errno);
    return false;
}

[[nodiscard]] int fstat_retrying_eintr(int descriptor, struct stat& metadata) noexcept {
    int result = -1;
    do {
        result = ::fstat(descriptor, &metadata);
    } while (result != 0 && errno == EINTR);
    return result;
}

[[nodiscard]] int fstatat_retrying_eintr(int parent, const char* leaf,
                                         struct stat& metadata) noexcept {
    int result = -1;
    do {
        result = ::fstatat(parent, leaf, &metadata, AT_SYMLINK_NOFOLLOW);
    } while (result != 0 && errno == EINTR);
    return result;
}

[[nodiscard]] bool valid_root_metadata(const struct stat& metadata) noexcept {
    return S_ISDIR(metadata.st_mode) &&
           static_cast<std::uint64_t>(metadata.st_uid) == static_cast<std::uint64_t>(::geteuid()) &&
           exact_mode(metadata, 0700);
}

[[nodiscard]] bool valid_parent_metadata(const struct stat& metadata) noexcept {
    return S_ISDIR(metadata.st_mode) &&
           static_cast<std::uint64_t>(metadata.st_uid) == static_cast<std::uint64_t>(::geteuid()) &&
           (metadata.st_mode & static_cast<mode_t>(S_IWGRP | S_IWOTH)) == 0;
}

[[nodiscard]] bool valid_lock_metadata(const struct stat& metadata) noexcept {
    return S_ISREG(metadata.st_mode) && metadata.st_nlink == 1 && metadata.st_size == 0 &&
           static_cast<std::uint64_t>(metadata.st_uid) == static_cast<std::uint64_t>(::geteuid()) &&
           exact_mode(metadata, 0600);
}

[[nodiscard]] bool valid_manifest_metadata(const struct stat& metadata) noexcept {
    return S_ISREG(metadata.st_mode) && metadata.st_nlink == 1 && metadata.st_size > 0 &&
           static_cast<std::uint64_t>(metadata.st_size) <=
               DISTRIBUTED_SIEVE_PROTOCOL_MAX_RECORD_BYTES &&
           static_cast<std::uint64_t>(metadata.st_uid) == static_cast<std::uint64_t>(::geteuid()) &&
           exact_mode(metadata, 0600);
}

struct ImmutableProtocolRecordReadResult final {
    std::optional<std::vector<std::byte>> bytes;
    std::optional<durable_record::RecordSnapshot> snapshot;
    bool missing = false;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return bytes.has_value() && snapshot.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ImmutableProtocolRecordReadResult
read_immutable_protocol_record_leaf(int root_fd, const char* leaf, std::uint64_t creator_process_id,
                                    DistributedSieveWaveStoreStatus missing_status,
                                    DistributedSieveWaveStoreStatus invalid_status) noexcept {
    ImmutableProtocolRecordReadResult result;
    if (root_fd < 0 || leaf == nullptr || *leaf == '\0') {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error());
        return result;
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    const int descriptor =
        openat_retrying_eintr(root_fd, leaf, O_RDONLY | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        const int saved_errno = errno;
        if (saved_errno == ENOENT) {
            result.missing = true;
            result.diagnostic = diagnostic(missing_status, posix_error(saved_errno));
        } else {
            result.diagnostic = diagnostic(invalid_status, posix_error(saved_errno));
        }
        return result;
    }
    UniqueFd held(descriptor);
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    struct stat held_before{};
    struct stat named_before{};
    if (fstat_retrying_eintr(held.get(), held_before) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        return result;
    }
    if (fstatat_retrying_eintr(root_fd, leaf, named_before) != 0) {
        result.diagnostic = diagnostic(invalid_status, posix_error(errno));
        return result;
    }
    if (!valid_manifest_metadata(held_before) || !valid_manifest_metadata(named_before) ||
        !stable_metadata(held_before, named_before)) {
        result.diagnostic = diagnostic(invalid_status, protocol_error());
        return result;
    }
    if (auto acl = acl_rejection(held.get(), false, invalid_status); acl.has_value()) {
        result.diagnostic = *acl;
        return result;
    }

    const auto exact_size = static_cast<std::size_t>(held_before.st_size);
    try {
        result.bytes.emplace(exact_size);
    } catch (const std::bad_alloc&) {
        result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                       std::make_error_code(std::errc::not_enough_memory));
        return result;
    } catch (...) {
        result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                       std::make_error_code(std::errc::io_error));
        return result;
    }

    std::size_t offset = 0;
    while (offset < result.bytes->size()) {
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        const std::size_t request =
            std::min(result.bytes->size() - offset,
                     static_cast<std::size_t>(std::numeric_limits<ssize_t>::max()));
        const ssize_t count =
            ::pread(held.get(), result.bytes->data() + offset, request, static_cast<off_t>(offset));
        if (count < 0) {
            const int saved_errno = errno;
            if (saved_errno == EINTR) {
                continue;
            }
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
            return result;
        }
        if (count == 0) {
            result.diagnostic = diagnostic(invalid_status, protocol_error());
            return result;
        }
        offset += static_cast<std::size_t>(count);
    }

    std::byte trailing{};
    ssize_t trailing_count = -1;
    do {
        trailing_count =
            ::pread(held.get(), &trailing, 1, static_cast<off_t>(result.bytes->size()));
    } while (trailing_count < 0 && errno == EINTR);
    if (trailing_count < 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        return result;
    }
    if (trailing_count != 0) {
        result.diagnostic = diagnostic(invalid_status, protocol_error());
        return result;
    }

    struct stat held_after{};
    struct stat named_after{};
    if (fstat_retrying_eintr(held.get(), held_after) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        return result;
    }
    if (fstatat_retrying_eintr(root_fd, leaf, named_after) != 0) {
        result.diagnostic = diagnostic(invalid_status, posix_error(errno));
        return result;
    }
    if (!valid_manifest_metadata(held_after) || !valid_manifest_metadata(named_after) ||
        !stable_metadata(held_before, held_after) || !stable_metadata(held_after, named_after)) {
        result.diagnostic = diagnostic(invalid_status, protocol_error());
        return result;
    }
    if (auto acl = acl_rejection(held.get(), false, invalid_status); acl.has_value()) {
        result.diagnostic = *acl;
        return result;
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    result.snapshot = durable_record::RecordSnapshot{
        .identity = record_identity(held_after),
        .size = static_cast<std::uint64_t>(held_after.st_size),
    };
    return result;
}

[[nodiscard]] bool valid_private_lease_marker_metadata(const struct stat& metadata) noexcept {
    return S_ISREG(metadata.st_mode) && metadata.st_nlink == 1 &&
           static_cast<std::uint64_t>(metadata.st_size) ==
               private_lease::PRIVATE_LEASE_MARKER_BYTES &&
           static_cast<std::uint64_t>(metadata.st_uid) == static_cast<std::uint64_t>(::geteuid()) &&
           exact_mode(metadata, 0600);
}

[[nodiscard]] bool valid_private_lease_directory_metadata(const struct stat& metadata) noexcept {
    return S_ISDIR(metadata.st_mode) &&
           static_cast<std::uint64_t>(metadata.st_uid) == static_cast<std::uint64_t>(::geteuid()) &&
           exact_mode(metadata, 0700);
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
validate_parent_binding(int parent_fd, const std::vector<std::string>& parent_components,
                        std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    struct stat held_before{};
    if (fstat_retrying_eintr(parent_fd, held_before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
    }
    if (auto acl = acl_rejection(parent_fd, true, DistributedSieveWaveStoreStatus::root_invalid);
        acl.has_value()) {
        return *acl;
    }
    auto named_parent = walk_parent_no_follow(parent_components, creator_process_id);
    if (!named_parent) {
        return named_parent.diagnostic;
    }
    struct stat named{};
    if (fstat_retrying_eintr(named_parent.parent.get(), named) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
    }
    if (auto acl = acl_rejection(named_parent.parent.get(), true,
                                 DistributedSieveWaveStoreStatus::root_invalid);
        acl.has_value()) {
        return *acl;
    }
    struct stat held_after{};
    if (fstat_retrying_eintr(parent_fd, held_after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    if (auto acl = acl_rejection(parent_fd, true, DistributedSieveWaveStoreStatus::root_invalid);
        acl.has_value()) {
        return *acl;
    }
    if (!valid_parent_metadata(held_before) || !valid_parent_metadata(named) ||
        !valid_parent_metadata(held_after) || !stable_metadata(held_before, held_after) ||
        !stable_metadata(held_after, named)) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, protocol_error());
    }
    return {};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
validate_root_binding(int parent_fd, const std::vector<std::string>& parent_components, int root_fd,
                      const std::filesystem::path& root_leaf, std::uint64_t creator_process_id,
                      std::optional<NativeIdentityV1> expected = std::nullopt) noexcept {
    if (const auto parent =
            validate_parent_binding(parent_fd, parent_components, creator_process_id);
        parent.status != DistributedSieveWaveStoreStatus::ready) {
        return parent;
    }

    struct stat held_before{};
    if (fstat_retrying_eintr(root_fd, held_before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
    }
    if (auto acl = acl_rejection(root_fd, true, DistributedSieveWaveStoreStatus::root_invalid);
        acl.has_value()) {
        return *acl;
    }
    struct stat named{};
    if (fstatat_retrying_eintr(parent_fd, root_leaf.c_str(), named) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
    }
    struct stat held_after{};
    if (fstat_retrying_eintr(root_fd, held_after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    if (auto acl = acl_rejection(root_fd, true, DistributedSieveWaveStoreStatus::root_invalid);
        acl.has_value()) {
        return *acl;
    }
    if (!valid_root_metadata(held_before) || !valid_root_metadata(named) ||
        !valid_root_metadata(held_after) || !stable_metadata(held_before, held_after) ||
        !stable_metadata(held_after, named)) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, protocol_error());
    }
    if (expected.has_value() && protocol_identity(held_after) != *expected) {
        return diagnostic(DistributedSieveWaveStoreStatus::root_invalid, protocol_error());
    }
    return {};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
validate_lock_binding(int root_fd, int lock_fd, std::uint64_t creator_process_id,
                      std::optional<NativeIdentityV1> expected = std::nullopt) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    struct stat held_before{};
    if (fstat_retrying_eintr(lock_fd, held_before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, posix_error(errno));
    }
    if (auto acl = acl_rejection(lock_fd, false, DistributedSieveWaveStoreStatus::lock_invalid);
        acl.has_value()) {
        return *acl;
    }
    struct stat named{};
    if (fstatat_retrying_eintr(root_fd, LOCK_LEAF, named) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, posix_error(errno));
    }
    struct stat held_after{};
    if (fstat_retrying_eintr(lock_fd, held_after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, posix_error(errno));
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    if (auto acl = acl_rejection(lock_fd, false, DistributedSieveWaveStoreStatus::lock_invalid);
        acl.has_value()) {
        return *acl;
    }
    if (!valid_lock_metadata(held_before) || !valid_lock_metadata(named) ||
        !valid_lock_metadata(held_after) || !stable_metadata(held_before, held_after) ||
        !stable_metadata(held_after, named)) {
        return diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, protocol_error());
    }
    if (expected.has_value() && protocol_identity(held_after) != *expected) {
        return diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, protocol_error());
    }
    return {};
}

struct NamespaceInventory final {
    bool lock = false;
    bool manifest = false;
    bool pending = false;
    std::vector<std::string> private_lease_base_lock_leaves;
    std::vector<std::string> private_lease_protocol_leaves;
    std::vector<std::string> worker_attempt_record_leaves;
    std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempt_records;
    std::vector<std::string> merge_started_record_leaves;
    std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1> merge_started_records;
    std::vector<std::string> chunk_terminal_failure_record_leaves;
    std::vector<DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1>
        chunk_terminal_failure_records;
    std::vector<std::string> wave_merge_commit_record_leaves;
    std::vector<DistributedSieveWaveMergeCommitRecordInventoryWitnessV1> wave_merge_commit_records;
    std::vector<std::string> cleanup_record_leaves;

    [[nodiscard]] friend constexpr bool operator==(const NamespaceInventory&,
                                                   const NamespaceInventory&) noexcept = default;
};

[[nodiscard]] bool
has_pre_manifest_private_lease_candidate(const NamespaceInventory& inventory) noexcept {
    return !inventory.manifest && (!inventory.private_lease_protocol_leaves.empty() ||
                                   !inventory.worker_attempt_record_leaves.empty() ||
                                   !inventory.merge_started_record_leaves.empty() ||
                                   !inventory.chunk_terminal_failure_record_leaves.empty() ||
                                   !inventory.wave_merge_commit_record_leaves.empty() ||
                                   !inventory.cleanup_record_leaves.empty());
}

struct NamespaceInventoryResult final {
    std::optional<NamespaceInventory> inventory;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return inventory.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] NamespaceInventoryResult inspect_namespace(int root_fd) noexcept {
    const int scan_fd =
        openat_retrying_eintr(root_fd, ".", O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (scan_fd < 0) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
    }
    DIR* raw_directory = ::fdopendir(scan_fd);
    if (raw_directory == nullptr) {
        const int saved_errno = errno;
        (void)::close(scan_fd);
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno))};
    }
    UniqueDirectory directory(raw_directory);

    NamespaceInventory inventory;
    errno = 0;
    for (;;) {
        dirent* entry = ::readdir(raw_directory);
        if (entry == nullptr) {
            if (errno != 0) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
            }
            break;
        }
        const std::string_view leaf(entry->d_name);
        if (leaf == "." || leaf == "..") {
            continue;
        }
        if (leaf == LOCK_LEAF) {
            if (inventory.lock) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            inventory.lock = true;
            continue;
        }
        if (leaf == MANIFEST_LEAF) {
            if (inventory.manifest) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            inventory.manifest = true;
            continue;
        }
        if (leaf == MANIFEST_PENDING_LEAF) {
            if (inventory.pending) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            inventory.pending = true;
            continue;
        }
        if (leaf.ends_with(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
            const std::size_t stem_size =
                leaf.size() - DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size();
            constexpr std::size_t max_base_lock_count =
                static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) *
                    DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS +
                DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS;
            if (stem_size == 0 || stem_size > DISTRIBUTED_SIEVE_PROTOCOL_MAX_ARTIFACT_STEM_BYTES ||
                inventory.private_lease_base_lock_leaves.size() >= max_base_lock_count) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            try {
                inventory.private_lease_base_lock_leaves.emplace_back(leaf);
            } catch (const std::bad_alloc&) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory))};
            } catch (...) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error))};
            }
            continue;
        }
        if (parse_distributed_sieve_worker_attempt_leaf_v1(leaf).has_value()) {
            constexpr std::size_t max_attempt_record_leaf_count =
                static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) *
                DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS * 2U;
            if (inventory.worker_attempt_record_leaves.size() >= max_attempt_record_leaf_count) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            try {
                inventory.worker_attempt_record_leaves.emplace_back(leaf);
            } catch (const std::bad_alloc&) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory))};
            } catch (...) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error))};
            }
            continue;
        }
        if (parse_distributed_sieve_merge_started_leaf_v1(leaf).has_value()) {
            constexpr std::size_t max_merge_started_record_leaf_count =
                static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS) * 2U;
            if (inventory.merge_started_record_leaves.size() >=
                max_merge_started_record_leaf_count) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            try {
                inventory.merge_started_record_leaves.emplace_back(leaf);
            } catch (const std::bad_alloc&) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory))};
            } catch (...) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error))};
            }
            continue;
        }
        if (parse_distributed_sieve_chunk_terminal_failure_leaf_v1(leaf).has_value()) {
            constexpr std::size_t max_terminal_record_leaf_count =
                static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) * 2U;
            if (inventory.chunk_terminal_failure_record_leaves.size() >=
                max_terminal_record_leaf_count) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            try {
                inventory.chunk_terminal_failure_record_leaves.emplace_back(leaf);
            } catch (const std::bad_alloc&) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory))};
            } catch (...) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error))};
            }
            continue;
        }
        if (parse_distributed_sieve_wave_merge_commit_leaf_v1(leaf).has_value()) {
            if (inventory.wave_merge_commit_record_leaves.size() >= 2U) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            try {
                inventory.wave_merge_commit_record_leaves.emplace_back(leaf);
            } catch (const std::bad_alloc&) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory))};
            } catch (...) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error))};
            }
            continue;
        }
        if (parse_distributed_sieve_cleanup_record_leaf_v1(leaf).has_value()) {
            constexpr std::size_t max_cleanup_record_leaf_count =
                static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) * 4U + 4U;
            if (inventory.cleanup_record_leaves.size() >= max_cleanup_record_leaf_count) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            try {
                inventory.cleanup_record_leaves.emplace_back(leaf);
            } catch (const std::bad_alloc&) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory))};
            } catch (...) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error))};
            }
            continue;
        }
        constexpr std::size_t max_protocol_leaf_count =
            static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) *
            DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS * 7U;
        if (inventory.private_lease_protocol_leaves.size() >= max_protocol_leaf_count) {
            return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                             protocol_error())};
        }
        try {
            inventory.private_lease_protocol_leaves.emplace_back(leaf);
        } catch (const std::bad_alloc&) {
            return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                             std::make_error_code(std::errc::not_enough_memory))};
        } catch (...) {
            return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                             std::make_error_code(std::errc::io_error))};
        }
    }
    std::sort(inventory.private_lease_base_lock_leaves.begin(),
              inventory.private_lease_base_lock_leaves.end());
    std::sort(inventory.private_lease_protocol_leaves.begin(),
              inventory.private_lease_protocol_leaves.end());
    std::sort(inventory.worker_attempt_record_leaves.begin(),
              inventory.worker_attempt_record_leaves.end());
    std::sort(inventory.merge_started_record_leaves.begin(),
              inventory.merge_started_record_leaves.end());
    std::sort(inventory.chunk_terminal_failure_record_leaves.begin(),
              inventory.chunk_terminal_failure_record_leaves.end());
    std::sort(inventory.wave_merge_commit_record_leaves.begin(),
              inventory.wave_merge_commit_record_leaves.end());
    std::sort(inventory.cleanup_record_leaves.begin(), inventory.cleanup_record_leaves.end());
    return {std::move(inventory), {}};
}

struct ManifestWorkerAttemptCoordinate final {
    std::uint32_t chunk_id = 0;
    std::uint32_t attempt_ordinal = 0;

    [[nodiscard]] friend constexpr bool
    operator==(const ManifestWorkerAttemptCoordinate&,
               const ManifestWorkerAttemptCoordinate&) noexcept = default;
};

[[nodiscard]] std::optional<ManifestWorkerAttemptCoordinate>
manifest_worker_attempt_coordinate_from_private_lease_base_lock(const WaveManifestV1& manifest,
                                                                std::string_view leaf) noexcept {
    if (!leaf.ends_with(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
        return std::nullopt;
    }
    leaf.remove_suffix(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size());
    constexpr std::size_t attempt_suffix_size =
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
    if (leaf.size() <= attempt_suffix_size) {
        return std::nullopt;
    }
    const std::size_t attempt_tag_offset = leaf.size() - attempt_suffix_size;
    if (leaf.substr(attempt_tag_offset, DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size()) !=
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1) {
        return std::nullopt;
    }
    const std::size_t digits_offset =
        attempt_tag_offset + DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size();
    if (leaf[digits_offset] < '0' || leaf[digits_offset] > '9' || leaf[digits_offset + 1U] < '0' ||
        leaf[digits_offset + 1U] > '9') {
        return std::nullopt;
    }
    const auto attempt_ordinal = static_cast<std::uint32_t>(leaf[digits_offset] - '0') * 10U +
                                 static_cast<std::uint32_t>(leaf[digits_offset + 1U] - '0');
    if (attempt_ordinal >= manifest.max_worker_attempts) {
        return std::nullopt;
    }

    const std::string_view chunk_stem = leaf.substr(0, attempt_tag_offset);
    const ChunkPlanV1* matched_chunk = nullptr;
    for (const auto& chunk : manifest.chunks) {
        if (chunk.relative_artifact_stem == chunk_stem) {
            if (matched_chunk != nullptr || chunk.sq_begin >= chunk.sq_end) {
                return std::nullopt;
            }
            matched_chunk = &chunk;
        }
    }
    if (matched_chunk == nullptr ||
        !distributed_sieve_worker_attempt_relative_stem_matches(
            matched_chunk->relative_artifact_stem, attempt_ordinal, leaf)) {
        return std::nullopt;
    }
    return ManifestWorkerAttemptCoordinate{
        .chunk_id = matched_chunk->chunk_id,
        .attempt_ordinal = attempt_ordinal,
    };
}

[[nodiscard]] bool
manifest_attempt_matches_private_lease_base_lock(const WaveManifestV1& manifest,
                                                 std::string_view leaf) noexcept {
    return manifest_worker_attempt_coordinate_from_private_lease_base_lock(manifest, leaf)
        .has_value();
}

[[nodiscard]] std::optional<std::uint32_t>
manifest_merge_generation_from_private_lease_base_lock(const WaveManifestV1& manifest,
                                                       std::string_view leaf) noexcept {
    if (!leaf.ends_with(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
        return std::nullopt;
    }
    leaf.remove_suffix(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size());
    if (!leaf.starts_with(DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1) ||
        leaf.size() != DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1.size() +
                           DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1) {
        return std::nullopt;
    }
    const std::size_t offset = DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1.size();
    if (leaf[offset] < '0' || leaf[offset] > '9' || leaf[offset + 1U] < '0' ||
        leaf[offset + 1U] > '9') {
        return std::nullopt;
    }
    const auto ordinal = static_cast<std::uint32_t>(leaf[offset] - '0') * 10U +
                         static_cast<std::uint32_t>(leaf[offset + 1U] - '0');
    if (ordinal >= manifest.max_merge_build_attempts) {
        return std::nullopt;
    }
    const auto names = distributed_sieve_merge_generation_names_v1(ordinal);
    if (!names.has_value() || names->relative_lease_stem != leaf) {
        return std::nullopt;
    }
    return ordinal;
}

[[nodiscard]] bool manifest_private_lease_matches_base_lock(const WaveManifestV1& manifest,
                                                            std::string_view leaf) noexcept {
    return manifest_attempt_matches_private_lease_base_lock(manifest, leaf) ||
           manifest_merge_generation_from_private_lease_base_lock(manifest, leaf).has_value();
}

struct PrivateLeaseBaseLockLeafValidationResult final {
    std::optional<NativeIdentityV1> identity;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return identity.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] PrivateLeaseBaseLockLeafValidationResult validate_private_lease_base_lock_binding(
    int root_fd, int lock_fd, const std::string& leaf, std::uint64_t creator_process_id,
    std::optional<NativeIdentityV1> expected_identity = std::nullopt) noexcept {
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, process_mismatch()};
    }

    struct stat held_before{};
    if (fstat_retrying_eintr(lock_fd, held_before) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    if (auto acl =
            acl_rejection(lock_fd, false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, *acl};
    }
    struct stat named{};
    if (fstatat_retrying_eintr(root_fd, leaf.c_str(), named) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    struct stat held_after{};
    if (fstat_retrying_eintr(lock_fd, held_after) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, process_mismatch()};
    }
    if (auto acl =
            acl_rejection(lock_fd, false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, *acl};
    }
    if (!valid_lock_metadata(held_before) || !valid_lock_metadata(named) ||
        !valid_lock_metadata(held_after) || !stable_metadata(held_before, held_after) ||
        !stable_metadata(held_after, named)) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    const NativeIdentityV1 identity = protocol_identity(held_after);
    if (expected_identity.has_value() && identity != *expected_identity) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    return {identity, {}};
}

[[nodiscard]] PrivateLeaseBaseLockLeafValidationResult
validate_private_lease_base_lock_leaf(int root_fd, const std::string& leaf,
                                      std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, process_mismatch()};
    }
    const int descriptor = openat_retrying_eintr(root_fd, leaf.c_str(),
                                                 O_RDONLY | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    UniqueFd held(descriptor);
    return validate_private_lease_base_lock_binding(root_fd, held.get(), leaf, creator_process_id);
}

struct PrivateLeaseBaseLockInventoryValidationResult final {
    std::optional<std::vector<NativeIdentityV1>> identities;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return identities.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] PrivateLeaseBaseLockInventoryValidationResult
validate_private_lease_base_lock_inventory(int root_fd, const NamespaceInventory& inventory,
                                           const WaveManifestV1& manifest,
                                           std::uint64_t creator_process_id) noexcept {
    const std::size_t maximum_base_locks =
        manifest.chunks.size() * static_cast<std::size_t>(manifest.max_worker_attempts) +
        static_cast<std::size_t>(manifest.max_merge_build_attempts);
    if (inventory.private_lease_base_lock_leaves.size() > maximum_base_locks) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    std::vector<NativeIdentityV1> identities;
    try {
        identities.resize(inventory.private_lease_base_lock_leaves.size());
    } catch (const std::bad_alloc&) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                         std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                         std::make_error_code(std::errc::io_error))};
    }
    for (std::size_t index = 0; index < inventory.private_lease_base_lock_leaves.size(); ++index) {
        const auto& leaf = inventory.private_lease_base_lock_leaves[index];
        if (!manifest_private_lease_matches_base_lock(manifest, leaf)) {
            return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                             protocol_error())};
        }
        const auto validated =
            validate_private_lease_base_lock_leaf(root_fd, leaf, creator_process_id);
        if (!validated) {
            return {std::nullopt, validated.diagnostic};
        }
        identities[index] = *validated.identity;
    }
    return {std::move(identities), {}};
}

enum class PrivateLeaseProtocolLeafRole : std::uint8_t {
    reserved,
    reserved_pending,
    owned,
    owned_pending,
    rollback_handoff,
    staging_directory,
    final_directory,
};

struct ParsedPrivateLeaseProtocolLeaf final {
    DistributedSievePrivateLeaseNamesV1 names;
    std::optional<DistributedSieveWorkerAttemptNamesV1> worker_attempt_names;
    std::optional<DistributedSieveMergeGenerationNamesV1> merge_generation_names;
    PrivateLeaseProtocolLeafRole role = PrivateLeaseProtocolLeafRole::reserved;
    std::optional<std::array<std::uint64_t, 2>> staging_lease_id;
};

[[nodiscard]] std::optional<DistributedSieveWorkerAttemptNamesV1>
manifest_attempt_names_from_relative_stem(const WaveManifestV1& manifest,
                                          std::string_view relative_stem) {
    constexpr std::size_t suffix_size = DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size() +
                                        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
    if (relative_stem.size() <= suffix_size) {
        return std::nullopt;
    }
    const std::size_t tag_offset = relative_stem.size() - suffix_size;
    if (relative_stem.substr(tag_offset, DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size()) !=
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1) {
        return std::nullopt;
    }
    const std::size_t digits_offset =
        tag_offset + DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size();
    if (relative_stem[digits_offset] < '0' || relative_stem[digits_offset] > '9' ||
        relative_stem[digits_offset + 1U] < '0' || relative_stem[digits_offset + 1U] > '9') {
        return std::nullopt;
    }
    const auto attempt_ordinal =
        static_cast<std::uint32_t>(relative_stem[digits_offset] - '0') * 10U +
        static_cast<std::uint32_t>(relative_stem[digits_offset + 1U] - '0');
    if (attempt_ordinal >= manifest.max_worker_attempts) {
        return std::nullopt;
    }

    const std::string_view chunk_stem = relative_stem.substr(0, tag_offset);
    const ChunkPlanV1* matched_chunk = nullptr;
    for (const auto& chunk : manifest.chunks) {
        if (chunk.relative_artifact_stem == chunk_stem) {
            if (matched_chunk != nullptr || chunk.sq_begin >= chunk.sq_end) {
                return std::nullopt;
            }
            matched_chunk = &chunk;
        }
    }
    if (matched_chunk == nullptr) {
        return std::nullopt;
    }
    return distributed_sieve_worker_attempt_names_v1(matched_chunk->relative_artifact_stem,
                                                     matched_chunk->chunk_id, attempt_ordinal);
}

struct ManifestPrivateLeaseNamesV1 final {
    DistributedSievePrivateLeaseNamesV1 names;
    std::optional<DistributedSieveWorkerAttemptNamesV1> worker_attempt_names;
    std::optional<ManifestWorkerAttemptCoordinate> worker_coordinate;
    std::optional<DistributedSieveMergeGenerationNamesV1> merge_generation_names;
    std::optional<std::uint32_t> merge_attempt_ordinal;
    std::size_t manifest_chunk_order = static_cast<std::size_t>(-1);
};

[[nodiscard]] std::optional<ManifestPrivateLeaseNamesV1>
manifest_private_lease_names_from_relative_stem(const WaveManifestV1& manifest,
                                                std::string_view relative_stem) {
    if (auto worker = manifest_attempt_names_from_relative_stem(manifest, relative_stem);
        worker.has_value()) {
        const auto coordinate = manifest_worker_attempt_coordinate_from_private_lease_base_lock(
            manifest, worker->base_lock_leaf);
        if (!coordinate.has_value()) {
            return std::nullopt;
        }
        const auto chunk = std::find_if(manifest.chunks.begin(), manifest.chunks.end(),
                                        [&](const ChunkPlanV1& candidate) {
                                            return candidate.chunk_id == coordinate->chunk_id;
                                        });
        if (chunk == manifest.chunks.end()) {
            return std::nullopt;
        }
        return ManifestPrivateLeaseNamesV1{
            .names = static_cast<const DistributedSievePrivateLeaseNamesV1&>(*worker),
            .worker_attempt_names = std::move(*worker),
            .worker_coordinate = coordinate,
            .manifest_chunk_order =
                static_cast<std::size_t>(std::distance(manifest.chunks.begin(), chunk)),
        };
    }

    if (!relative_stem.starts_with(DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1) ||
        relative_stem.size() != DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1.size() +
                                    DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1) {
        return std::nullopt;
    }
    const std::size_t offset = DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1.size();
    if (relative_stem[offset] < '0' || relative_stem[offset] > '9' ||
        relative_stem[offset + 1U] < '0' || relative_stem[offset + 1U] > '9') {
        return std::nullopt;
    }
    const auto ordinal = static_cast<std::uint32_t>(relative_stem[offset] - '0') * 10U +
                         static_cast<std::uint32_t>(relative_stem[offset + 1U] - '0');
    if (ordinal >= manifest.max_merge_build_attempts) {
        return std::nullopt;
    }
    auto merge = distributed_sieve_merge_generation_names_v1(ordinal);
    if (!merge.has_value() || merge->relative_lease_stem != relative_stem) {
        return std::nullopt;
    }
    return ManifestPrivateLeaseNamesV1{
        .names = static_cast<const DistributedSievePrivateLeaseNamesV1&>(*merge),
        .merge_generation_names = std::move(*merge),
        .merge_attempt_ordinal = ordinal,
    };
}

[[nodiscard]] std::optional<std::uint8_t> hexadecimal_nibble(char value) noexcept {
    if (value >= '0' && value <= '9') {
        return static_cast<std::uint8_t>(value - '0');
    }
    if (value >= 'a' && value <= 'f') {
        return static_cast<std::uint8_t>(value - 'a' + 10);
    }
    return std::nullopt;
}

[[nodiscard]] std::optional<std::array<std::uint64_t, 2>>
parse_private_lease_id_hex(std::string_view encoded) noexcept {
    if (encoded.size() != 32U) {
        return std::nullopt;
    }
    std::array<std::uint64_t, 2> lease_id{};
    for (std::size_t word = 0; word < lease_id.size(); ++word) {
        for (std::size_t digit = 0; digit < 16U; ++digit) {
            const auto nibble = hexadecimal_nibble(encoded[word * 16U + digit]);
            if (!nibble.has_value()) {
                return std::nullopt;
            }
            lease_id[word] = (lease_id[word] << 4U) | *nibble;
        }
    }
    if (lease_id[0] == 0 && lease_id[1] == 0) {
        return std::nullopt;
    }
    return lease_id;
}

[[nodiscard]] std::optional<ParsedPrivateLeaseProtocolLeaf>
parse_manifest_bound_private_lease_protocol_leaf(const WaveManifestV1& manifest,
                                                 std::string_view leaf) {
    const auto parse_fixed = [&](std::string_view suffix, PrivateLeaseProtocolLeafRole role,
                                 const std::string DistributedSievePrivateLeaseNamesV1::* member)
        -> std::optional<ParsedPrivateLeaseProtocolLeaf> {
        if (!leaf.ends_with(suffix)) {
            return std::nullopt;
        }
        auto names = manifest_private_lease_names_from_relative_stem(
            manifest, leaf.substr(0, leaf.size() - suffix.size()));
        if (!names.has_value() || names->names.*member != leaf) {
            return std::nullopt;
        }
        return ParsedPrivateLeaseProtocolLeaf{
            .names = std::move(names->names),
            .worker_attempt_names = std::move(names->worker_attempt_names),
            .merge_generation_names = std::move(names->merge_generation_names),
            .role = role,
        };
    };

    if (auto parsed = parse_fixed(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVED_PENDING_SUFFIX,
                                  PrivateLeaseProtocolLeafRole::reserved_pending,
                                  &DistributedSievePrivateLeaseNamesV1::reserved_pending_leaf)) {
        return parsed;
    }
    if (auto parsed = parse_fixed(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVED_SUFFIX,
                                  PrivateLeaseProtocolLeafRole::reserved,
                                  &DistributedSievePrivateLeaseNamesV1::reserved_leaf)) {
        return parsed;
    }
    if (auto parsed = parse_fixed(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNED_PENDING_SUFFIX,
                                  PrivateLeaseProtocolLeafRole::owned_pending,
                                  &DistributedSievePrivateLeaseNamesV1::owned_pending_leaf)) {
        return parsed;
    }
    if (auto parsed = parse_fixed(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNED_SUFFIX,
                                  PrivateLeaseProtocolLeafRole::owned,
                                  &DistributedSievePrivateLeaseNamesV1::owned_leaf)) {
        return parsed;
    }
    if (auto parsed = parse_fixed(DISTRIBUTED_SIEVE_PRIVATE_HANDOFF_ROLLBACK_SUFFIX,
                                  PrivateLeaseProtocolLeafRole::rollback_handoff,
                                  &DistributedSievePrivateLeaseNamesV1::rollback_handoff_leaf)) {
        return parsed;
    }
    if (auto parsed = parse_fixed(DISTRIBUTED_SIEVE_PRIVATE_LEASE_DIRECTORY_SUFFIX,
                                  PrivateLeaseProtocolLeafRole::final_directory,
                                  &DistributedSievePrivateLeaseNamesV1::private_directory_leaf)) {
        return parsed;
    }

    constexpr std::size_t lease_id_hex_size = 32U;
    if (leaf.size() <= DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG.size() + lease_id_hex_size) {
        return std::nullopt;
    }
    const std::size_t staging_tag_offset =
        leaf.size() - DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG.size() - lease_id_hex_size;
    if (leaf.substr(staging_tag_offset, DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG.size()) !=
        DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG) {
        return std::nullopt;
    }
    auto names = manifest_private_lease_names_from_relative_stem(
        manifest, leaf.substr(0, staging_tag_offset));
    const auto lease_id = parse_private_lease_id_hex(leaf.substr(leaf.size() - lease_id_hex_size));
    if (!names.has_value() || !lease_id.has_value()) {
        return std::nullopt;
    }
    std::string expected = names->names.relative_lease_stem;
    expected.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG);
    expected.append(private_lease::private_lease_id_hex(*lease_id));
    if (expected != leaf) {
        return std::nullopt;
    }
    return ParsedPrivateLeaseProtocolLeaf{
        .names = std::move(names->names),
        .worker_attempt_names = std::move(names->worker_attempt_names),
        .merge_generation_names = std::move(names->merge_generation_names),
        .role = PrivateLeaseProtocolLeafRole::staging_directory,
        .staging_lease_id = lease_id,
    };
}

struct PrivateLeaseMarkerAtResult final {
    std::optional<private_lease::PrivateLeaseRecord> record;
    std::optional<NativeIdentityV1> identity;
    DistributedSieveWaveStoreDiagnostic diagnostic;
    UniqueFd marker{};

    [[nodiscard]] explicit operator bool() const noexcept {
        return record.has_value() && identity.has_value() && static_cast<bool>(marker) &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] PrivateLeaseMarkerAtResult
read_private_lease_marker_at(int parent_fd, const std::string& leaf,
                             std::uint64_t creator_process_id) noexcept {
    if (parent_fd < 0 || !process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt,
                parent_fd < 0 ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                           invalid_argument_error())
                              : process_mismatch()};
    }
    const int descriptor = openat_retrying_eintr(parent_fd, leaf.c_str(),
                                                 O_RDONLY | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        return {
            std::nullopt, std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    UniqueFd held(descriptor);

    struct stat held_before{};
    struct stat named_before{};
    if (fstat_retrying_eintr(held.get(), held_before) != 0) {
        return {
            std::nullopt, std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    if (fstatat_retrying_eintr(parent_fd, leaf.c_str(), named_before) != 0) {
        return {
            std::nullopt, std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    if (!valid_private_lease_marker_metadata(held_before) ||
        !valid_private_lease_marker_metadata(named_before) ||
        !stable_metadata(held_before, named_before)) {
        return {std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (auto acl =
            acl_rejection(held.get(), false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, std::nullopt, *acl};
    }

    std::array<std::byte, private_lease::PRIVATE_LEASE_MARKER_BYTES> bytes{};
    std::size_t offset = 0;
    while (offset < bytes.size()) {
        if (!process_matches(creator_process_id)) {
            return {std::nullopt, std::nullopt, process_mismatch()};
        }
        const ssize_t count = ::pread(held.get(), bytes.data() + offset, bytes.size() - offset,
                                      static_cast<off_t>(offset));
        if (count < 0) {
            if (errno == EINTR) {
                continue;
            }
            return {std::nullopt, std::nullopt,
                    diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
        }
        if (count == 0) {
            return {
                std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
        }
        offset += static_cast<std::size_t>(count);
    }

    struct stat held_after{};
    struct stat named_after{};
    if (fstat_retrying_eintr(held.get(), held_after) != 0) {
        return {
            std::nullopt, std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    if (fstatat_retrying_eintr(parent_fd, leaf.c_str(), named_after) != 0) {
        return {
            std::nullopt, std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    if (!valid_private_lease_marker_metadata(held_after) ||
        !valid_private_lease_marker_metadata(named_after) ||
        !stable_metadata(held_before, held_after) || !stable_metadata(held_after, named_after)) {
        return {std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (auto acl =
            acl_rejection(held.get(), false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, std::nullopt, *acl};
    }
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt, process_mismatch()};
    }

    try {
        return {
            private_lease::parse_private_lease_marker(bytes),
            protocol_identity(held_after),
            {},
            std::move(held),
        };
    } catch (const std::bad_alloc&) {
        return {std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                           std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        return {std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_private_lease_directory_binding(
    int root_fd, int directory_fd, const std::string& leaf, std::uint64_t creator_process_id,
    std::optional<NativeIdentityV1> expected_identity = std::nullopt) noexcept {
    if (root_fd < 0 || directory_fd < 0 || !process_matches(creator_process_id)) {
        return process_matches(creator_process_id)
                   ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                invalid_argument_error())
                   : process_mismatch();
    }
    struct stat held_before{};
    struct stat named{};
    struct stat held_after{};
    if (fstat_retrying_eintr(directory_fd, held_before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (fstatat_retrying_eintr(root_fd, leaf.c_str(), named) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (fstat_retrying_eintr(directory_fd, held_after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (!valid_private_lease_directory_metadata(held_before) ||
        !valid_private_lease_directory_metadata(named) ||
        !valid_private_lease_directory_metadata(held_after) ||
        !stable_metadata(held_before, held_after) || !stable_metadata(held_after, named)) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (auto acl =
            acl_rejection(directory_fd, true, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }
    const NativeIdentityV1 identity = protocol_identity(held_after);
    if (expected_identity.has_value() && identity != *expected_identity) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return process_matches(creator_process_id) ? DistributedSieveWaveStoreDiagnostic{}
                                               : process_mismatch();
}

struct PrivateLeaseDirectoryInventory final {
    bool owner = false;
    bool owner_pending = false;
    bool work_package_residue = false;
    bool corpus_index = false;
    bool corpus_data = false;
    bool worker_handoff = false;
    bool worker_handoff_pending = false;

    [[nodiscard]] friend constexpr bool
    operator==(const PrivateLeaseDirectoryInventory&,
               const PrivateLeaseDirectoryInventory&) noexcept = default;
};

struct PrivateLeaseDirectoryAtResult final {
    UniqueFd directory;
    std::optional<NativeIdentityV1> identity;
    std::optional<PrivateLeaseDirectoryInventory> inventory;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return static_cast<bool>(directory) && identity.has_value() && inventory.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] PrivateLeaseDirectoryAtResult
inspect_private_lease_directory_at(int root_fd, const std::string& leaf,
                                   std::uint64_t creator_process_id) noexcept {
    const int descriptor = openat_retrying_eintr(root_fd, leaf.c_str(),
                                                 O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        return {
            {},
            std::nullopt,
            std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    UniqueFd directory(descriptor);
    if (const auto validated = validate_private_lease_directory_binding(root_fd, directory.get(),
                                                                        leaf, creator_process_id);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        return {{}, std::nullopt, std::nullopt, validated};
    }
    struct stat metadata{};
    if (fstat_retrying_eintr(directory.get(), metadata) != 0) {
        return {{},
                std::nullopt,
                std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
    }
    const NativeIdentityV1 identity = protocol_identity(metadata);

    const int scan_fd = openat_retrying_eintr(directory.get(), ".",
                                              O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (scan_fd < 0) {
        return {{},
                std::nullopt,
                std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
    }
    DIR* raw_directory = ::fdopendir(scan_fd);
    if (raw_directory == nullptr) {
        const int saved_errno = errno;
        (void)::close(scan_fd);
        return {{},
                std::nullopt,
                std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno))};
    }
    UniqueDirectory scan(raw_directory);
    PrivateLeaseDirectoryInventory inventory;
    errno = 0;
    for (;;) {
        dirent* entry = ::readdir(raw_directory);
        if (entry == nullptr) {
            if (errno != 0) {
                return {{},
                        std::nullopt,
                        std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
            }
            break;
        }
        const std::string_view child(entry->d_name);
        if (child == "." || child == "..") {
            continue;
        }
        if (child == DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF && !inventory.owner) {
            inventory.owner = true;
            continue;
        }
        if (child == DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_PENDING_LEAF &&
            !inventory.owner_pending) {
            inventory.owner_pending = true;
            continue;
        }
        if (child == work_package_file::DISTRIBUTED_SIEVE_WORKER_WORK_PACKAGE_FILE_LEAF_V1 &&
            !inventory.work_package_residue) {
            inventory.work_package_residue = true;
            continue;
        }
        if (child == WORKER_CORPUS_INDEX_LEAF && !inventory.corpus_index) {
            inventory.corpus_index = true;
            continue;
        }
        if (child == WORKER_CORPUS_DATA_LEAF && !inventory.corpus_data) {
            inventory.corpus_data = true;
            continue;
        }
        if (child == WORKER_HANDOFF_LEAF && !inventory.worker_handoff) {
            inventory.worker_handoff = true;
            continue;
        }
        if (child == WORKER_HANDOFF_PENDING_LEAF && !inventory.worker_handoff_pending) {
            inventory.worker_handoff_pending = true;
            continue;
        }
        return {{},
                std::nullopt,
                std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (const auto validated = validate_private_lease_directory_binding(
            root_fd, directory.get(), leaf, creator_process_id, identity);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        return {{}, std::nullopt, std::nullopt, validated};
    }
    return {std::move(directory), identity, inventory, {}};
}

struct PrivateLeaseDirectoryHandleInventoryResult final {
    std::optional<PrivateLeaseDirectoryInventory> inventory;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return inventory.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

struct MergeRawWriterLeafInspectionResult final {
    std::optional<DistributedSieveMergeRawWriterLeafInventoryWitnessV1> witness;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] MergeRawWriterLeafInspectionResult
inspect_merge_raw_writer_leaf_at(int directory_fd, const char* leaf,
                                 std::uint64_t creator_process_id) noexcept {
    if (directory_fd < 0 || leaf == nullptr || *leaf == '\0') {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                         invalid_argument_error())};
    }
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, process_mismatch()};
    }
    const int descriptor =
        openat_retrying_eintr(directory_fd, leaf, O_RDONLY | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    UniqueFd held(descriptor);
    struct stat held_before{};
    struct stat named_before{};
    if (fstat_retrying_eintr(held.get(), held_before) != 0 ||
        fstatat_retrying_eintr(directory_fd, leaf, named_before) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    const auto valid = [](const struct stat& metadata) noexcept {
        return S_ISREG(metadata.st_mode) && metadata.st_nlink == 1 && metadata.st_size >= 0 &&
               static_cast<std::uint64_t>(metadata.st_uid) ==
                   static_cast<std::uint64_t>(::geteuid()) &&
               exact_mode(metadata, 0600);
    };
    if (!valid(held_before) || !valid(named_before) ||
        !stable_metadata(held_before, named_before)) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (auto acl =
            acl_rejection(held.get(), false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, *acl};
    }
    struct stat held_after{};
    struct stat named_after{};
    if (fstat_retrying_eintr(held.get(), held_after) != 0 ||
        fstatat_retrying_eintr(directory_fd, leaf, named_after) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    if (!valid(held_after) || !valid(named_after) || !stable_metadata(held_before, held_after) ||
        !stable_metadata(held_after, named_after) || !process_matches(creator_process_id)) {
        return {std::nullopt, process_matches(creator_process_id)
                                  ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                               protocol_error())
                                  : process_mismatch()};
    }
    return {
        DistributedSieveMergeRawWriterLeafInventoryWitnessV1{
            .identity = protocol_identity(held_after),
            .extent = static_cast<std::uint64_t>(held_after.st_size),
        },
        {},
    };
}

[[nodiscard]] PrivateLeaseDirectoryHandleInventoryResult
inspect_private_lease_directory_handle(int directory_fd,
                                       std::uint64_t creator_process_id) noexcept {
    if (directory_fd < 0 || !process_matches(creator_process_id)) {
        return {
            std::nullopt,
            directory_fd < 0 ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                          invalid_argument_error())
                             : process_mismatch(),
        };
    }
    struct stat before{};
    if (fstat_retrying_eintr(directory_fd, before) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    if (!valid_private_lease_directory_metadata(before)) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (auto acl =
            acl_rejection(directory_fd, true, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, *acl};
    }

    const int scan_fd =
        openat_retrying_eintr(directory_fd, ".", O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (scan_fd < 0) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
    }
    DIR* raw_directory = ::fdopendir(scan_fd);
    if (raw_directory == nullptr) {
        const int saved_errno = errno;
        (void)::close(scan_fd);
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno))};
    }
    UniqueDirectory scan(raw_directory);
    PrivateLeaseDirectoryInventory inventory;
    errno = 0;
    for (;;) {
        dirent* entry = ::readdir(raw_directory);
        if (entry == nullptr) {
            if (errno != 0) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno))};
            }
            break;
        }
        const std::string_view child(entry->d_name);
        if (child == "." || child == "..") {
            continue;
        }
        if (child == DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF && !inventory.owner) {
            inventory.owner = true;
            continue;
        }
        if (child == DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_PENDING_LEAF &&
            !inventory.owner_pending) {
            inventory.owner_pending = true;
            continue;
        }
        if (child == work_package_file::DISTRIBUTED_SIEVE_WORKER_WORK_PACKAGE_FILE_LEAF_V1 &&
            !inventory.work_package_residue) {
            inventory.work_package_residue = true;
            continue;
        }
        if (child == WORKER_CORPUS_INDEX_LEAF && !inventory.corpus_index) {
            inventory.corpus_index = true;
            continue;
        }
        if (child == WORKER_CORPUS_DATA_LEAF && !inventory.corpus_data) {
            inventory.corpus_data = true;
            continue;
        }
        if (child == WORKER_HANDOFF_LEAF && !inventory.worker_handoff) {
            inventory.worker_handoff = true;
            continue;
        }
        if (child == WORKER_HANDOFF_PENDING_LEAF && !inventory.worker_handoff_pending) {
            inventory.worker_handoff_pending = true;
            continue;
        }
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }

    struct stat after{};
    if (fstat_retrying_eintr(directory_fd, after) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    if (!valid_private_lease_directory_metadata(after) || !stable_metadata(before, after)) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (auto acl =
            acl_rejection(directory_fd, true, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, *acl};
    }
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, process_mismatch()};
    }
    return {inventory, {}};
}

struct PrivateLeaseAttemptInventory final {
    DistributedSievePrivateLeaseNamesV1 names;
    std::optional<DistributedSieveWorkerAttemptNamesV1> worker_attempt_names;
    std::optional<ManifestWorkerAttemptCoordinate> worker_coordinate;
    std::optional<DistributedSieveMergeGenerationNamesV1> merge_generation_names;
    std::optional<std::uint32_t> merge_attempt_ordinal;
    std::size_t manifest_chunk_order = static_cast<std::size_t>(-1);
    NativeIdentityV1 base_lock_identity{};
    bool reserved = false;
    bool reserved_pending = false;
    bool owned = false;
    bool owned_pending = false;
    bool rollback_handoff = false;
    bool final_directory = false;
    std::optional<std::string> staging_directory_leaf;
    std::optional<std::array<std::uint64_t, 2>> staging_lease_id;
    bool merge_started_record_leaf_present = false;
};

[[nodiscard]] std::array<std::uint64_t, 3>
relation_identity(const NativeIdentityV1& identity) noexcept {
    return {identity.volume, identity.object, identity.generation};
}

[[nodiscard]] NativeIdentityV1
protocol_identity(const durable_record::NativeIdentity& identity) noexcept {
    return {
        .volume = identity.first,
        .object = identity.second,
        .generation = identity.third,
    };
}

using PrivateLeaseReservationWitness = DistributedSievePrivateLeaseReservationInventoryWitness;

struct PrivateLeaseAttemptValidationResult final {
    std::optional<PrivateLeaseReservationWitness> witness;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

struct WorkerHandoffInventoryValidationResult final {
    std::optional<DistributedSieveWorkerHandoffInventoryWitnessV1> witness;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

struct MergePreparedInventoryValidationResult final {
    std::optional<DistributedSieveMergePreparedInventoryWitnessV1> witness;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
worker_handoff_inspection_failure(const gnfs::relation::OOCCleanupResult& lower) noexcept {
    using Status = gnfs::relation::OOCCleanupStatus;
    const auto error = lower.native_error ? lower.native_error : protocol_error();
    switch (lower.status) {
    case Status::Interrupted:
        return diagnostic(DistributedSieveWaveStoreStatus::interrupted, error);
    case Status::Busy:
        return diagnostic(DistributedSieveWaveStoreStatus::private_lease_lock_busy, error);
    case Status::IoFailure:
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, error);
    case Status::DurabilityFailure:
        return diagnostic(DistributedSieveWaveStoreStatus::durability_failed, error);
    case Status::PlatformUnsupported:
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, error);
    case Status::UnexpectedFailure:
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, error);
    case Status::Completed:
    case Status::NoTransaction:
    case Status::InvalidRequest:
    case Status::SourcePairInvalid:
    case Status::IntentCorrupt:
    case Status::IntentConflict:
    case Status::ForeignReplacementPreserved:
    case Status::NamespaceConflict:
    case Status::RecoveryRequired:
    case Status::HandoffPresent:
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, error);
    }
    return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
}

[[nodiscard]] std::optional<DistributedSieveMergeRawWriterRecoveryFaultPointV1>
wave_merge_raw_recovery_fault_point(gnfs::relation::OOCPrivateLeaseFaultPoint point) noexcept {
    using Lower = gnfs::relation::OOCPrivateLeaseFaultPoint;
    using Upper = DistributedSieveMergeRawWriterRecoveryFaultPointV1;
    switch (point) {
    case Lower::RecoveryPermitAcquired:
        return Upper::RecoveryPermitAcquired;
    case Lower::PreactiveDirectoryQuarantinedDurable:
        return Upper::PreactiveDirectoryQuarantinedDurable;
    case Lower::PreactiveDataRemovedDurable:
        return Upper::PreactiveDataRemovedDurable;
    case Lower::PreactiveIndexRemovedDurable:
        return Upper::PreactiveIndexRemovedDurable;
    case Lower::OwnerRemovedDurable:
        return Upper::OwnerRemovedDurable;
    case Lower::FinalDirectoryRemovedDurable:
        return Upper::FinalDirectoryRemovedDurable;
    case Lower::ReservedRemovedDurable:
        return Upper::ReservedRemovedDurable;
    case Lower::OwnedRemovedDurable:
        return Upper::OwnedRemovedDurable;
    default:
        return std::nullopt;
    }
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic merge_raw_recovery_failure(
    const gnfs::relation::OOCCleanupResult& lower,
    std::optional<DistributedSieveMergeRawWriterRecoveryFaultPointV1> last_point) noexcept {
    using Status = gnfs::relation::OOCCleanupStatus;
    const auto error = lower.native_error ? lower.native_error : protocol_error();
    DistributedSieveWaveStoreStatus status = DistributedSieveWaveStoreStatus::namespace_conflict;
    switch (lower.status) {
    case Status::Interrupted:
        status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case Status::InvalidRequest:
        status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case Status::Busy:
        status = DistributedSieveWaveStoreStatus::private_lease_lock_busy;
        break;
    case Status::IoFailure:
        status = DistributedSieveWaveStoreStatus::io_failed;
        break;
    case Status::DurabilityFailure:
        status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case Status::PlatformUnsupported:
        status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case Status::UnexpectedFailure:
        status = DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    case Status::Completed:
    case Status::NoTransaction:
    case Status::SourcePairInvalid:
    case Status::IntentCorrupt:
    case Status::IntentConflict:
    case Status::ForeignReplacementPreserved:
    case Status::NamespaceConflict:
    case Status::RecoveryRequired:
    case Status::HandoffPresent:
        break;
    }
    auto outcome = diagnostic(status, error);
    outcome.last_merge_raw_writer_recovery_fault_point = last_point;
    return outcome;
}

[[nodiscard]] WorkerHandoffInventoryValidationResult
validate_worker_handoff_envelope(const PrivateLeaseAttemptInventory& attempt,
                                 const WaveManifestV1& manifest,
                                 const NativeIdentityV1& directory_identity,
                                 const gnfs::relation::OOCPrivateHandoffRecordV1& envelope,
                                 const durable_record::RecordSnapshot& handoff_snapshot,
                                 const durable_record::RecordSnapshot& index_snapshot,
                                 const durable_record::RecordSnapshot& data_snapshot,
                                 std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return WorkerHandoffInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }

    try {
        if (!attempt.worker_attempt_names.has_value() || !attempt.worker_coordinate.has_value()) {
            return conflict();
        }
        const auto& parsed = *attempt.worker_coordinate;
        const ChunkPlanV1* chunk = nullptr;
        for (const auto& candidate : manifest.chunks) {
            if (candidate.chunk_id == parsed.chunk_id) {
                chunk = &candidate;
                break;
            }
        }
        if (chunk == nullptr || chunk->sq_begin >= chunk->sq_end) {
            return conflict();
        }

        if (envelope.payload_kind !=
                static_cast<std::uint32_t>(DistributedSieveRecordKindV1::worker_handoff) ||
            envelope.payload_version != manifest.handoff_version ||
            protocol_identity(envelope.lock_identity) != attempt.base_lock_identity ||
            protocol_identity(envelope.directory_identity) != directory_identity) {
            return conflict();
        }

        auto decoded = decode_distributed_sieve_record(envelope.opaque_payload);
        if (!decoded || !decoded.value.has_value()) {
            auto failure =
                diagnostic(decoded.status.error == DistributedSieveProtocolError::resource_exhausted
                               ? DistributedSieveWaveStoreStatus::resource_exhausted
                               : DistributedSieveWaveStoreStatus::namespace_conflict,
                           protocol_error());
            failure.protocol_status = decoded.status;
            return fail_with(std::move(failure));
        }
        auto* handoff = std::get_if<WorkerHandoffV1>(&*decoded.value);
        if (handoff == nullptr || handoff->manifest_digest != manifest.self_digest ||
            handoff->work_digest != manifest.work_sha256 || handoff->wave_id != manifest.wave_id ||
            handoff->chunk_id != parsed.chunk_id ||
            handoff->attempt_ordinal != parsed.attempt_ordinal ||
            handoff->sq_begin != chunk->sq_begin || handoff->sq_end != chunk->sq_end ||
            handoff->lease.lease_id.limbs != envelope.lease_id ||
            handoff->lease.owner_marker != protocol_identity(envelope.owner_marker_identity) ||
            handoff->lease.directory != protocol_identity(envelope.directory_identity) ||
            handoff->lease.relative_stem != attempt.names.relative_lease_stem ||
            handoff->artifact.descriptor.format_version != envelope.pair.format_version ||
            handoff->artifact.descriptor.store_id != envelope.pair.store_id ||
            handoff->artifact.descriptor.generation != envelope.pair.generation ||
            handoff->artifact.descriptor.relation_count != envelope.pair.count ||
            handoff->artifact.descriptor.data_end != envelope.pair.data_extent ||
            handoff->artifact.index_file.identity != protocol_identity(envelope.index.identity) ||
            handoff->artifact.index_file.extent != envelope.index.extent ||
            handoff->artifact.data_file.identity != protocol_identity(envelope.data.identity) ||
            handoff->artifact.data_file.extent != envelope.data.extent ||
            !handoff->cleanup_intent_absent) {
            return conflict();
        }

        return {
            DistributedSieveWorkerHandoffInventoryWitnessV1{
                .handoff = std::move(*handoff),
                .envelope_digest = envelope.self_digest,
                .owned_marker_identity = protocol_identity(envelope.owned_marker_identity),
                .handoff_snapshot = handoff_snapshot,
                .index_snapshot = index_snapshot,
                .data_snapshot = data_snapshot,
            },
            {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return conflict();
    }
}

[[nodiscard]] WorkerHandoffInventoryValidationResult validate_worker_handoff_inventory(
    const std::filesystem::path& absolute_root, const PrivateLeaseAttemptInventory& attempt,
    const WaveManifestV1& manifest, const NativeIdentityV1& directory_identity,
    std::uint64_t creator_process_id,
    const DistributedSievePrivateLeaseBaseLockAt* held_base_lock = nullptr) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return WorkerHandoffInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }
    try {
        const std::filesystem::path base_path =
            absolute_root / attempt.names.private_directory_leaf / "corpus";
        auto adopted =
            held_base_lock != nullptr
                ? held_base_lock->adopt_exact_private_handoff(base_path)
                : gnfs::relation::OOCCleanupTransaction::adopt_private_handoff(base_path);
        if (!adopted.adopted() || !adopted.adoption.has_value()) {
            return fail_with(worker_handoff_inspection_failure(adopted.result));
        }
        const auto& receipt = *adopted.adoption;
        return validate_worker_handoff_envelope(
            attempt, manifest, directory_identity, receipt.record(), receipt.handoff_snapshot(),
            receipt.index_snapshot(), receipt.data_snapshot(), creator_process_id);
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
}

[[nodiscard]] MergePreparedInventoryValidationResult
validate_merge_prepared_envelope(const PrivateLeaseAttemptInventory& attempt,
                                 const WaveManifestV1& manifest,
                                 const NativeIdentityV1& directory_identity,
                                 const gnfs::relation::OOCPrivateHandoffRecordV1& envelope,
                                 const durable_record::RecordSnapshot& handoff_snapshot,
                                 const durable_record::RecordSnapshot& index_snapshot,
                                 const durable_record::RecordSnapshot& data_snapshot,
                                 std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return MergePreparedInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }

    try {
        if (!attempt.merge_generation_names.has_value() ||
            !attempt.merge_attempt_ordinal.has_value()) {
            return conflict();
        }
        if (envelope.payload_kind !=
                static_cast<std::uint32_t>(DistributedSieveRecordKindV1::merge_prepared) ||
            envelope.payload_version != manifest.handoff_version ||
            protocol_identity(envelope.lock_identity) != attempt.base_lock_identity ||
            protocol_identity(envelope.directory_identity) != directory_identity) {
            return conflict();
        }

        auto decoded = decode_distributed_sieve_record(envelope.opaque_payload);
        if (!decoded || !decoded.value.has_value()) {
            auto failure =
                diagnostic(decoded.status.error == DistributedSieveProtocolError::resource_exhausted
                               ? DistributedSieveWaveStoreStatus::resource_exhausted
                               : DistributedSieveWaveStoreStatus::namespace_conflict,
                           protocol_error());
            failure.protocol_status = decoded.status;
            return fail_with(std::move(failure));
        }
        auto* prepared = std::get_if<MergePreparedV1>(&*decoded.value);
        if (prepared == nullptr || prepared->manifest_digest != manifest.self_digest ||
            prepared->work_digest != manifest.work_sha256 ||
            prepared->merge_policy_version != manifest.merge_policy_version ||
            prepared->ordered_inputs.size() != manifest.chunks.size() ||
            prepared->merged_lease.lease_id.limbs != envelope.lease_id ||
            prepared->merged_lease.owner_marker !=
                protocol_identity(envelope.owner_marker_identity) ||
            prepared->merged_lease.directory != protocol_identity(envelope.directory_identity) ||
            prepared->merged_lease.relative_stem != attempt.names.relative_lease_stem ||
            prepared->merged_artifact.descriptor.format_version != envelope.pair.format_version ||
            prepared->merged_artifact.descriptor.store_id != envelope.pair.store_id ||
            prepared->merged_artifact.descriptor.generation != envelope.pair.generation ||
            prepared->merged_artifact.descriptor.relation_count != envelope.pair.count ||
            prepared->merged_artifact.descriptor.data_end != envelope.pair.data_extent ||
            prepared->merged_artifact.index_file.identity !=
                protocol_identity(envelope.index.identity) ||
            prepared->merged_artifact.index_file.extent != envelope.index.extent ||
            prepared->merged_artifact.data_file.identity !=
                protocol_identity(envelope.data.identity) ||
            prepared->merged_artifact.data_file.extent != envelope.data.extent ||
            prepared->output_relation_count != envelope.pair.count) {
            return conflict();
        }

        return {
            DistributedSieveMergePreparedInventoryWitnessV1{
                .prepared = std::move(*prepared),
                .envelope_digest = envelope.self_digest,
                .owned_marker_identity = protocol_identity(envelope.owned_marker_identity),
                .handoff_snapshot = handoff_snapshot,
                .index_snapshot = index_snapshot,
                .data_snapshot = data_snapshot,
            },
            {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return conflict();
    }
}

[[nodiscard]] MergePreparedInventoryValidationResult validate_merge_prepared_inventory(
    const std::filesystem::path& absolute_root, const PrivateLeaseAttemptInventory& attempt,
    const WaveManifestV1& manifest, const NativeIdentityV1& directory_identity,
    std::uint64_t creator_process_id,
    const DistributedSievePrivateLeaseBaseLockAt* held_base_lock = nullptr) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return MergePreparedInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }
    try {
        const std::filesystem::path base_path =
            absolute_root / attempt.names.private_directory_leaf / "corpus";
        auto adopted =
            held_base_lock != nullptr
                ? held_base_lock->adopt_exact_private_handoff(base_path)
                : gnfs::relation::OOCCleanupTransaction::adopt_private_handoff(base_path);
        if (!adopted.adopted() || !adopted.adoption.has_value()) {
            return fail_with(worker_handoff_inspection_failure(adopted.result));
        }
        const auto& receipt = *adopted.adoption;
        return validate_merge_prepared_envelope(
            attempt, manifest, directory_identity, receipt.record(), receipt.handoff_snapshot(),
            receipt.index_snapshot(), receipt.data_snapshot(), creator_process_id);
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic work_package_residue_inspection_failure(
    const work_package_file::DistributedSieveWorkerWorkPackageFileDiagnostic& lower) noexcept {
    const auto status =
        lower.status ==
                work_package_file::DistributedSieveWorkerWorkPackageFileStatus::resource_exhausted
            ? DistributedSieveWaveStoreStatus::resource_exhausted
            : DistributedSieveWaveStoreStatus::namespace_conflict;
    auto failure = diagnostic(status, lower.native_error != 0 ? posix_error(lower.native_error)
                                                              : protocol_error());
    failure.protocol_status = lower.protocol_status;
    return failure;
}

[[nodiscard]] DistributedSieveWorkerWorkPackageResidueInventoryWitnessV1
compact_work_package_residue_witness(
    const work_package_file::DistributedSieveWorkerWorkPackageResidueWitnessV1& witness) noexcept {
    return {
        .body_bytes = witness.package.body_bytes,
        .total_bytes = witness.package.total_bytes,
        .work_sha256 = witness.package.work_sha256,
        .package_sha256 = witness.package.package_sha256,
        .file_identity = witness.file_identity,
        .file_extent = witness.file_extent,
        .owner_user_id = witness.owner_user_id,
    };
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
work_package_residue_reconciliation_inspection_failure(
    const work_package_file::DistributedSieveWorkerWorkPackageFileDiagnostic& lower) noexcept {
    using FileStatus = work_package_file::DistributedSieveWorkerWorkPackageFileStatus;
    DistributedSieveWaveStoreStatus status = DistributedSieveWaveStoreStatus::unexpected_failure;
    switch (lower.status) {
    case FileStatus::ready:
        status = DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    case FileStatus::interrupted:
        status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case FileStatus::invalid_request:
        status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case FileStatus::platform_unavailable:
        status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case FileStatus::resource_exhausted:
        status = DistributedSieveWaveStoreStatus::resource_exhausted;
        break;
    case FileStatus::namespace_conflict:
    case FileStatus::decode_failed:
        status = DistributedSieveWaveStoreStatus::namespace_conflict;
        break;
    case FileStatus::publication_failed:
        status = DistributedSieveWaveStoreStatus::publication_failed;
        break;
    case FileStatus::durability_failed:
        status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case FileStatus::close_failed:
        status = DistributedSieveWaveStoreStatus::io_failed;
        break;
    case FileStatus::ops_contract_violation:
    case FileStatus::unexpected_failure:
        status = DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    }
    auto failure = diagnostic(status, lower.native_error != 0 ? posix_error(lower.native_error)
                                                              : protocol_error());
    if (lower.protocol_status.error != DistributedSieveProtocolError::none) {
        failure.protocol_status = lower.protocol_status;
    }
    return failure;
}

[[nodiscard]] std::optional<DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint>
wave_work_package_residue_reconciliation_fault_point(
    std::optional<
        work_package_file::DistributedSieveWorkerWorkPackageResidueReconciliationFaultPointV1>
        point) noexcept {
    if (!point.has_value()) {
        return std::nullopt;
    }
    switch (*point) {
    case work_package_file::DistributedSieveWorkerWorkPackageResidueReconciliationFaultPointV1::
        after_name_unlinked:
        return DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint::AfterNameUnlinked;
    case work_package_file::DistributedSieveWorkerWorkPackageResidueReconciliationFaultPointV1::
        after_directory_durable:
        return DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint::
            AfterDirectoryDurable;
    }
    return std::nullopt;
}

[[nodiscard]] std::optional<
    work_package_file::DistributedSieveWorkerWorkPackageResidueReconciliationFaultPointV1>
carrier_work_package_residue_reconciliation_fault_point(
    std::optional<DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint>
        point) noexcept {
    if (!point.has_value()) {
        return std::nullopt;
    }
    switch (*point) {
    case DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint::AfterNameUnlinked:
        return work_package_file::
            DistributedSieveWorkerWorkPackageResidueReconciliationFaultPointV1::after_name_unlinked;
    case DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint::AfterDirectoryDurable:
        return work_package_file::
            DistributedSieveWorkerWorkPackageResidueReconciliationFaultPointV1::
                after_directory_durable;
    case DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint::Count:
        return std::nullopt;
    }
    return std::nullopt;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic work_package_residue_reconciliation_failure(
    const work_package_file::DistributedSieveWorkerWorkPackageResidueReconciliationResultV1&
        lower) noexcept {
    using FileStatus = work_package_file::DistributedSieveWorkerWorkPackageFileStatus;
    DistributedSieveWaveStoreStatus status = DistributedSieveWaveStoreStatus::unexpected_failure;
    switch (lower.diagnostic.status) {
    case FileStatus::ready:
        status = DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    case FileStatus::interrupted:
        status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case FileStatus::invalid_request:
        status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case FileStatus::platform_unavailable:
        status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case FileStatus::resource_exhausted:
        status = DistributedSieveWaveStoreStatus::resource_exhausted;
        break;
    case FileStatus::namespace_conflict:
    case FileStatus::decode_failed:
        status = DistributedSieveWaveStoreStatus::namespace_conflict;
        break;
    case FileStatus::publication_failed:
        status = DistributedSieveWaveStoreStatus::publication_failed;
        break;
    case FileStatus::durability_failed:
        status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case FileStatus::close_failed:
        status = DistributedSieveWaveStoreStatus::io_failed;
        break;
    case FileStatus::ops_contract_violation:
    case FileStatus::unexpected_failure:
        status = DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    }
    auto failure = diagnostic(status, lower.diagnostic.native_error != 0
                                          ? posix_error(lower.diagnostic.native_error)
                                          : protocol_error());
    if (lower.diagnostic.protocol_status.error != DistributedSieveProtocolError::none) {
        failure.protocol_status = lower.diagnostic.protocol_status;
    }
    failure.last_worker_work_package_residue_reconciliation_fault_point =
        wave_work_package_residue_reconciliation_fault_point(lower.fault_point);
    return failure;
}

[[nodiscard]] bool preselected_work_package_directory_sync_failure(void* context) noexcept {
    return context != nullptr && *static_cast<const bool*>(context);
}

[[nodiscard]] PrivateLeaseAttemptValidationResult validate_private_lease_attempt_inventory(
    int root_fd, const std::filesystem::path& absolute_root,
    const PrivateLeaseAttemptInventory& attempt, const WaveManifestV1& manifest,
    std::uint64_t creator_process_id,
    const DistributedSievePrivateLeaseBaseLockAt* held_base_lock = nullptr) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return PrivateLeaseAttemptValidationResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    try {
        PrivateLeaseReservationWitness witness{
            .base_lock_leaf = attempt.names.base_lock_leaf,
        };
        const bool any_root_protocol = attempt.reserved || attempt.reserved_pending ||
                                       attempt.owned || attempt.owned_pending ||
                                       attempt.rollback_handoff || attempt.final_directory ||
                                       attempt.staging_directory_leaf.has_value();
        if (!any_root_protocol) {
            return {std::move(witness), {}};
        }

        const bool canonical_handoff_root_shape = !attempt.reserved && !attempt.reserved_pending &&
                                                  attempt.owned && !attempt.owned_pending &&
                                                  attempt.final_directory &&
                                                  !attempt.staging_directory_leaf.has_value();
        if (canonical_handoff_root_shape) {
            const bool worker_generation = attempt.worker_attempt_names.has_value() &&
                                           attempt.worker_coordinate.has_value() &&
                                           !attempt.merge_generation_names.has_value() &&
                                           !attempt.merge_attempt_ordinal.has_value();
            const bool merge_generation = !attempt.worker_attempt_names.has_value() &&
                                          !attempt.worker_coordinate.has_value() &&
                                          attempt.merge_generation_names.has_value() &&
                                          attempt.merge_attempt_ordinal.has_value();
            if (worker_generation == merge_generation) {
                return conflict();
            }
            auto directory = inspect_private_lease_directory_at(
                root_fd, attempt.names.private_directory_leaf, creator_process_id);
            if (!directory) {
                return fail_with(directory.diagnostic);
            }
            const auto& entries = *directory.inventory;
            const bool exact_directory_shape =
                entries.owner && !entries.owner_pending && !entries.work_package_residue &&
                entries.corpus_index && entries.corpus_data && entries.worker_handoff &&
                !entries.worker_handoff_pending;
            if (!exact_directory_shape) {
                return conflict();
            }

            if (worker_generation) {
                auto handoff = validate_worker_handoff_inventory(
                    absolute_root, attempt, manifest, *directory.identity, creator_process_id,
                    held_base_lock);
                if (!handoff) {
                    return fail_with(std::move(handoff.diagnostic));
                }
                witness.lease_id = handoff.witness->handoff.lease.lease_id.limbs;
                witness.owner_marker_identity = handoff.witness->handoff.lease.owner_marker;
                witness.owned_marker_identity = handoff.witness->owned_marker_identity;
                witness.worker_handoff = std::move(*handoff.witness);
            } else {
                auto prepared = validate_merge_prepared_inventory(
                    absolute_root, attempt, manifest, *directory.identity, creator_process_id,
                    held_base_lock);
                if (!prepared) {
                    return fail_with(std::move(prepared.diagnostic));
                }
                witness.lease_id = prepared.witness->prepared.merged_lease.lease_id.limbs;
                witness.owner_marker_identity =
                    prepared.witness->prepared.merged_lease.owner_marker;
                witness.owned_marker_identity = prepared.witness->owned_marker_identity;
                witness.merge_prepared = std::move(*prepared.witness);
            }
            witness.boundary =
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable;
            witness.directory_identity = *directory.identity;
            if (const auto rebound = validate_private_lease_directory_binding(
                    root_fd, directory.directory.get(), attempt.names.private_directory_leaf,
                    creator_process_id, *directory.identity);
                rebound.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(rebound);
            }
            return {std::move(witness), {}};
        }

        // A started merge may be interrupted while the relation writer owns
        // an unsealed corpus pair, or while relation-layer preactive rollback
        // is removing that pair and its marker chain.  These phases are
        // classified read-only here.  They are not cleanup authority; the
        // MergeStarted validator below must still bind the typed witness to
        // the unique latest canonical-only record before it is accepted.
        const bool merge_recovery_candidate =
            attempt.merge_generation_names.has_value() &&
            attempt.merge_started_record_leaf_present && attempt.owned && !attempt.owned_pending &&
            !attempt.reserved_pending && !attempt.rollback_handoff &&
            !attempt.worker_attempt_names.has_value();
        if (merge_recovery_candidate) {
            const auto owned =
                read_private_lease_marker_at(root_fd, attempt.names.owned_leaf, creator_process_id);
            if (!owned) {
                return fail_with(owned.diagnostic);
            }
            const auto& owned_record = *owned.record;
            const std::filesystem::path base_path =
                absolute_root / attempt.names.private_directory_leaf / "corpus";
            struct stat root_metadata{};
            if (fstat_retrying_eintr(root_fd, root_metadata) != 0) {
                return fail_with(
                    diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno)));
            }
            const private_lease::PrivateLeaseRecord expected_reserved{
                .platform_id = private_lease::PLATFORM_ID,
                .phase = private_lease::PrivateLeasePhase::Reserved,
                .capability = private_lease::PrivateLeaseCapability::RollbackStagingOnly,
                .lease_id = owned_record.lease_id,
                .base_path_digest = private_lease::frozen_path_digest(base_path),
                .parent_identity = relation_identity(protocol_identity(root_metadata)),
                .lock_identity = relation_identity(attempt.base_lock_identity),
            };
            const auto expected_owner = private_lease::make_private_lease_owner_record(
                expected_reserved, owned_record.directory_identity);
            const auto expected_owned = private_lease::make_private_lease_owned_record(
                expected_owner, owned_record.owner_identity);
            if (owned_record != expected_owned ||
                owned_record.lease_id == std::array<std::uint64_t, 2>{} ||
                owned_record.directory_identity == std::array<std::uint64_t, 3>{} ||
                owned_record.owner_identity == std::array<std::uint64_t, 3>{}) {
                return conflict();
            }

            std::optional<PrivateLeaseMarkerAtResult> reserved;
            if (attempt.reserved) {
                reserved.emplace(read_private_lease_marker_at(root_fd, attempt.names.reserved_leaf,
                                                              creator_process_id));
                if (!*reserved || *reserved->record != expected_reserved) {
                    return reserved->diagnostic.status == DistributedSieveWaveStoreStatus::ready
                               ? conflict()
                               : fail_with(reserved->diagnostic);
                }
            }
            if (attempt.final_directory && attempt.staging_directory_leaf.has_value()) {
                return conflict();
            }
            if (attempt.staging_directory_leaf.has_value() &&
                (!attempt.staging_lease_id.has_value() ||
                 *attempt.staging_lease_id != owned_record.lease_id)) {
                return conflict();
            }

            DistributedSieveMergeRawWriterRecoveryInventoryWitnessV1 recovery{
                .phase = DistributedSieveMergeRawWriterRecoveryPhaseV1::Count,
                .lease_id = owned_record.lease_id,
                .directory_identity =
                    NativeIdentityV1{
                        .volume = owned_record.directory_identity[0],
                        .object = owned_record.directory_identity[1],
                        .generation = owned_record.directory_identity[2],
                    },
                .owner_marker_identity =
                    NativeIdentityV1{
                        .volume = owned_record.owner_identity[0],
                        .object = owned_record.owner_identity[1],
                        .generation = owned_record.owner_identity[2],
                    },
                .owned_marker_identity = *owned.identity,
                .reserved_marker_identity =
                    reserved.has_value() ? reserved->identity : std::nullopt,
            };
            witness.lease_id = recovery.lease_id;
            witness.reserved_marker_identity = recovery.reserved_marker_identity;
            witness.directory_identity = recovery.directory_identity;
            witness.owner_marker_identity = recovery.owner_marker_identity;
            witness.owned_marker_identity = recovery.owned_marker_identity;

            const bool directory_present =
                attempt.final_directory || attempt.staging_directory_leaf.has_value();
            if (!directory_present) {
                if (attempt.reserved) {
                    recovery.phase = DistributedSieveMergeRawWriterRecoveryPhaseV1::
                        DirectoryAbsentReservedAndOwned;
                } else {
                    recovery.phase =
                        DistributedSieveMergeRawWriterRecoveryPhaseV1::DirectoryAbsentOwnedOnly;
                }
                witness.boundary = DistributedSievePrivateLeaseReservationBoundary::Count;
                witness.merge_raw_writer_recovery = std::move(recovery);
                return {std::move(witness), {}};
            }
            if (!attempt.reserved) {
                return conflict();
            }

            const std::string& directory_leaf = attempt.final_directory
                                                    ? attempt.names.private_directory_leaf
                                                    : *attempt.staging_directory_leaf;
            auto directory =
                inspect_private_lease_directory_at(root_fd, directory_leaf, creator_process_id);
            if (!directory) {
                return fail_with(directory.diagnostic);
            }
            if (relation_identity(*directory.identity) != owned_record.directory_identity) {
                return conflict();
            }
            const auto& entries = *directory.inventory;
            if (entries.owner_pending || entries.work_package_residue || entries.worker_handoff ||
                entries.worker_handoff_pending || (entries.corpus_data && !entries.corpus_index)) {
                return conflict();
            }

            if (entries.owner) {
                const auto owner = read_private_lease_marker_at(
                    directory.directory.get(),
                    std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF), creator_process_id);
                if (!owner || *owner.record != expected_owner ||
                    relation_identity(*owner.identity) != owned_record.owner_identity) {
                    return owner.diagnostic.status == DistributedSieveWaveStoreStatus::ready
                               ? conflict()
                               : fail_with(owner.diagnostic);
                }
            }
            if (entries.corpus_index) {
                if (!entries.owner) {
                    return conflict();
                }
                auto index = inspect_merge_raw_writer_leaf_at(
                    directory.directory.get(), WORKER_CORPUS_INDEX_LEAF.data(), creator_process_id);
                if (!index) {
                    return fail_with(index.diagnostic);
                }
                recovery.index = std::move(index.witness);
                if (entries.corpus_data) {
                    auto data = inspect_merge_raw_writer_leaf_at(directory.directory.get(),
                                                                 WORKER_CORPUS_DATA_LEAF.data(),
                                                                 creator_process_id);
                    if (!data) {
                        return fail_with(data.diagnostic);
                    }
                    recovery.data = std::move(data.witness);
                }
                recovery.phase =
                    attempt.final_directory
                        ? DistributedSieveMergeRawWriterRecoveryPhaseV1::FinalDirectoryRawPair
                        : DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryRawPair;
                witness.boundary =
                    attempt.final_directory
                        ? DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable
                        : DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable;
            } else if (!attempt.final_directory && entries.owner) {
                recovery.phase =
                    DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryOwnerOnly;
                witness.boundary =
                    DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable;
            } else if (!attempt.final_directory && !entries.owner) {
                recovery.phase =
                    DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryOwnerRemoved;
                witness.boundary = DistributedSievePrivateLeaseReservationBoundary::Count;
            } else {
                // A clean final P8 reservation is handled by the ordinary
                // P0-P8 classifier below.  A final directory without its
                // canonical owner is never a valid recovery prefix.
                if (!entries.owner) {
                    return conflict();
                }
            }
            if (recovery.phase != DistributedSieveMergeRawWriterRecoveryPhaseV1::Count) {
                if (const auto rebound = validate_private_lease_directory_binding(
                        root_fd, directory.directory.get(), directory_leaf, creator_process_id,
                        *directory.identity);
                    rebound.status != DistributedSieveWaveStoreStatus::ready) {
                    return fail_with(rebound);
                }
                witness.merge_raw_writer_recovery = std::move(recovery);
                return {std::move(witness), {}};
            }
        }

        if (attempt.reserved == attempt.reserved_pending) {
            return conflict();
        }

        const auto read_if_present = [&](bool present,
                                         const std::string& leaf) -> PrivateLeaseMarkerAtResult {
            if (!present) {
                return {std::nullopt, std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::ready)};
            }
            return read_private_lease_marker_at(root_fd, leaf, creator_process_id);
        };
        const auto reserved = read_if_present(attempt.reserved, attempt.names.reserved_leaf);
        if (attempt.reserved && !reserved) {
            return fail_with(reserved.diagnostic);
        }
        const auto reserved_pending =
            read_if_present(attempt.reserved_pending, attempt.names.reserved_pending_leaf);
        if (attempt.reserved_pending && !reserved_pending) {
            return fail_with(reserved_pending.diagnostic);
        }
        const private_lease::PrivateLeaseRecord& reserved_record =
            attempt.reserved ? *reserved.record : *reserved_pending.record;

        const std::filesystem::path base_path =
            absolute_root / attempt.names.private_directory_leaf / "corpus";
        struct stat root_metadata{};
        if (fstat_retrying_eintr(root_fd, root_metadata) != 0) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno)));
        }
        const private_lease::PrivateLeaseRecord exact_reserved{
            .platform_id = private_lease::PLATFORM_ID,
            .phase = private_lease::PrivateLeasePhase::Reserved,
            .capability = private_lease::PrivateLeaseCapability::RollbackStagingOnly,
            .lease_id = reserved_record.lease_id,
            .base_path_digest = private_lease::frozen_path_digest(base_path),
            .parent_identity = relation_identity(protocol_identity(root_metadata)),
            .lock_identity = relation_identity(attempt.base_lock_identity),
        };
        if (reserved_record != exact_reserved) {
            return conflict();
        }
        witness.lease_id = reserved_record.lease_id;
        witness.reserved_marker_identity =
            attempt.reserved ? reserved.identity : reserved_pending.identity;

        if (attempt.staging_directory_leaf.has_value() &&
            (!attempt.staging_lease_id.has_value() ||
             *attempt.staging_lease_id != reserved_record.lease_id)) {
            return conflict();
        }
        if (attempt.final_directory && attempt.staging_directory_leaf.has_value()) {
            return conflict();
        }
        if (attempt.reserved_pending &&
            (attempt.final_directory || attempt.staging_directory_leaf.has_value() ||
             attempt.owned || attempt.owned_pending)) {
            return conflict();
        }

        const bool directory_present =
            attempt.final_directory || attempt.staging_directory_leaf.has_value();
        if (!directory_present) {
            if (attempt.owned || attempt.owned_pending) {
                return conflict();
            }
            witness.boundary =
                attempt.reserved
                    ? DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable
                    : DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable;
            return {std::move(witness), {}};
        }

        const std::string& directory_leaf = attempt.final_directory
                                                ? attempt.names.private_directory_leaf
                                                : *attempt.staging_directory_leaf;
        auto directory =
            inspect_private_lease_directory_at(root_fd, directory_leaf, creator_process_id);
        if (!directory) {
            return fail_with(directory.diagnostic);
        }
        witness.directory_identity = directory.identity;
        const auto& directory_inventory = *directory.inventory;
        const bool any_handoff_artifact =
            directory_inventory.corpus_index || directory_inventory.corpus_data ||
            directory_inventory.worker_handoff || directory_inventory.worker_handoff_pending;
        if (any_handoff_artifact) {
            return conflict();
        }
        if (directory_inventory.work_package_residue &&
            (!attempt.final_directory || !attempt.worker_attempt_names.has_value())) {
            return conflict();
        }
        if (directory_inventory.owner && directory_inventory.owner_pending) {
            return conflict();
        }
        if (!directory_inventory.owner && !directory_inventory.owner_pending) {
            if (attempt.final_directory || attempt.owned || attempt.owned_pending) {
                return conflict();
            }
            witness.boundary =
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable;
            return {std::move(witness), {}};
        }

        const auto owner =
            directory_inventory.owner
                ? read_private_lease_marker_at(
                      directory.directory.get(),
                      std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF), creator_process_id)
                : PrivateLeaseMarkerAtResult{
                      std::nullopt,
                      std::nullopt,
                      diagnostic(DistributedSieveWaveStoreStatus::ready),
                  };
        if (directory_inventory.owner && !owner) {
            return fail_with(owner.diagnostic);
        }
        const auto owner_pending =
            directory_inventory.owner_pending
                ? read_private_lease_marker_at(
                      directory.directory.get(),
                      std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_PENDING_LEAF),
                      creator_process_id)
                : PrivateLeaseMarkerAtResult{
                      std::nullopt,
                      std::nullopt,
                      diagnostic(DistributedSieveWaveStoreStatus::ready),
                  };
        if (directory_inventory.owner_pending && !owner_pending) {
            return fail_with(owner_pending.diagnostic);
        }
        const auto expected_owner = private_lease::make_private_lease_owner_record(
            reserved_record, relation_identity(*directory.identity));
        if ((directory_inventory.owner && *owner.record != expected_owner) ||
            (directory_inventory.owner_pending && *owner_pending.record != expected_owner)) {
            return conflict();
        }
        witness.owner_marker_identity =
            directory_inventory.owner ? owner.identity : owner_pending.identity;

        if (!directory_inventory.owner) {
            if (attempt.final_directory || attempt.owned || attempt.owned_pending) {
                return conflict();
            }
            witness.boundary = DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable;
            return {std::move(witness), {}};
        }

        if (attempt.owned && attempt.owned_pending) {
            return conflict();
        }
        const auto owned = read_if_present(attempt.owned, attempt.names.owned_leaf);
        if (attempt.owned && !owned) {
            return fail_with(owned.diagnostic);
        }
        const auto owned_pending =
            read_if_present(attempt.owned_pending, attempt.names.owned_pending_leaf);
        if (attempt.owned_pending && !owned_pending) {
            return fail_with(owned_pending.diagnostic);
        }
        if (!attempt.owned && !attempt.owned_pending) {
            if (attempt.final_directory) {
                return conflict();
            }
            witness.boundary =
                DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable;
            return {std::move(witness), {}};
        }
        const auto expected_owned = private_lease::make_private_lease_owned_record(
            expected_owner, relation_identity(*owner.identity));
        if ((attempt.owned && *owned.record != expected_owned) ||
            (attempt.owned_pending && *owned_pending.record != expected_owned)) {
            return conflict();
        }
        witness.owned_marker_identity = attempt.owned ? owned.identity : owned_pending.identity;
        if (attempt.final_directory && !attempt.owned) {
            return conflict();
        }
        if (attempt.final_directory) {
            witness.boundary =
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable;
            if (directory_inventory.work_package_residue) {
                auto inspected =
                    work_package_file::inspect_distributed_sieve_worker_work_package_residue_v1({
                        .borrowed_attempt_directory_handle = static_cast<
                            work_package_file::DistributedSieveWorkerWorkPackageNativeHandle>(
                            directory.directory.get()),
                        .expected_directory_identity = *directory.identity,
                        .observer_process_id = creator_process_id,
                    });
                if (!inspected) {
                    return fail_with(work_package_residue_inspection_failure(inspected.diagnostic));
                }
                const auto work_digest = distributed_sieve_work_digest(inspected.witness->identity);
                const auto manifest_status =
                    validate_manifest_work_identity(manifest, inspected.witness->identity);
                if (!work_digest || *work_digest.digest != manifest.work_sha256 ||
                    !manifest_status ||
                    inspected.witness->package.work_sha256 != manifest.work_sha256 ||
                    inspected.witness->package.total_bytes != inspected.witness->file_extent) {
                    const auto protocol_status =
                        !work_digest
                            ? work_digest.status
                            : (!manifest_status
                                   ? manifest_status
                                   : DistributedSieveProtocolStatus{
                                         .error = DistributedSieveProtocolError::digest_mismatch,
                                     });
                    const auto status =
                        protocol_status.error == DistributedSieveProtocolError::resource_exhausted
                            ? DistributedSieveWaveStoreStatus::resource_exhausted
                            : DistributedSieveWaveStoreStatus::namespace_conflict;
                    auto failure = diagnostic(status, protocol_error());
                    failure.protocol_status = protocol_status;
                    return fail_with(std::move(failure));
                }
                witness.work_package_residue =
                    compact_work_package_residue_witness(*inspected.witness);
            }
        } else if (attempt.owned) {
            witness.boundary =
                DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable;
        } else {
            witness.boundary = DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable;
        }
        if (const auto rebound = validate_private_lease_directory_binding(
                root_fd, directory.directory.get(), directory_leaf, creator_process_id,
                *directory.identity);
            rebound.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(rebound);
        }
        return {std::move(witness), {}};
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return conflict();
    }
}

struct PrivateLeaseProtocolInventoryValidationResult final {
    std::optional<std::vector<PrivateLeaseReservationWitness>> witnesses;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witnesses.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

struct HeldPrivateLeaseBaseLockInventoryV1 final {
    std::span<const DistributedSievePrivateLeaseBaseLockAt* const> locks;
};

/// Read-only view of canonical publication locks that are already retained by
/// the relation layer. The view is used only while constructing one recovered
/// MergePrepared admission. It never exposes a descriptor or permits the
/// ordinary path-based adoption fallback.
struct HeldCanonicalPublicationInventoryEntryV1 final {
    std::string_view base_lock_leaf;
    NativeIdentityV1 base_lock_identity;
    const PrivateLeaseReservationWitness* expected_lease = nullptr;
    const private_lease::PrivateHandoffPublicationPrefixWitnessV1* expected_prefix = nullptr;
    const private_lease::PrivateHandoffPublicationValidatedPermitV1* permit = nullptr;
    const std::unique_ptr<gnfs::relation::OOCPrivateHandoffReader>* reader_slot = nullptr;
    const bool* adopting = nullptr;
};

struct HeldCanonicalPublicationInventoryV1 final {
    std::span<const HeldCanonicalPublicationInventoryEntryV1> entries;
    const HeldCanonicalPublicationInventoryEntryV1* current = nullptr;
    const gnfs::relation::OOCPrivateHandoffReader* current_reader = nullptr;
};

[[nodiscard]] bool worker_handoff_publication_prefix_marker_chain_matches(
    const private_lease::PrivateHandoffPublicationPrefixWitnessV1& prefix,
    const NativeIdentityV1& expected_wave_root_identity,
    const PrivateLeaseAttemptInventory& attempt,
    const NativeIdentityV1& expected_directory_identity) noexcept;

[[nodiscard]] bool held_publication_reader_matches(
    const gnfs::relation::OOCPrivateHandoffReader& reader,
    const private_lease::PrivateHandoffPublicationPrefixWitnessV1& expected_prefix,
    const PrivateLeaseReservationWitness& expected_lease) noexcept {
    try {
        if (!reader.valid() || reader.record() != expected_prefix.record ||
            !expected_prefix.canonical_snapshot.has_value() ||
            reader.handoff_snapshot() != *expected_prefix.canonical_snapshot ||
            reader.index_snapshot().identity != expected_prefix.record.index.identity ||
            reader.index_snapshot().size != expected_prefix.record.index.extent ||
            reader.data_snapshot().identity != expected_prefix.record.data.identity ||
            reader.data_snapshot().size != expected_prefix.record.data.extent) {
            return false;
        }
        if (expected_lease.worker_handoff.has_value() ==
                expected_lease.merge_prepared.has_value() ||
            expected_lease.merge_raw_writer_recovery.has_value()) {
            return false;
        }
        if (expected_lease.worker_handoff.has_value()) {
            const auto& expected = *expected_lease.worker_handoff;
            return reader.handoff_snapshot() == expected.handoff_snapshot &&
                   reader.index_snapshot() == expected.index_snapshot &&
                   reader.data_snapshot() == expected.data_snapshot &&
                   reader.reader().count() == expected.handoff.relation_count;
        }
        const auto& expected = *expected_lease.merge_prepared;
        return reader.handoff_snapshot() == expected.handoff_snapshot &&
               reader.index_snapshot() == expected.index_snapshot &&
               reader.data_snapshot() == expected.data_snapshot &&
               reader.reader().count() == expected.prepared.output_relation_count;
    } catch (...) {
        return false;
    }
}

struct PrivateLeaseAttemptInventoryParseResult final {
    std::optional<std::vector<PrivateLeaseAttemptInventory>> attempts;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return attempts.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] PrivateLeaseAttemptInventoryParseResult parse_private_lease_attempt_inventory(
    const NamespaceInventory& inventory, const WaveManifestV1& manifest,
    std::span<const NativeIdentityV1> base_lock_identities) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return PrivateLeaseAttemptInventoryParseResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (inventory.private_lease_base_lock_leaves.size() != base_lock_identities.size()) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
    }

    std::vector<PrivateLeaseAttemptInventory> attempts;
    try {
        attempts.reserve(inventory.private_lease_base_lock_leaves.size());
        for (std::size_t index = 0; index < inventory.private_lease_base_lock_leaves.size();
             ++index) {
            const auto& base_lock_leaf = inventory.private_lease_base_lock_leaves[index];
            if (!base_lock_leaf.ends_with(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
                return conflict();
            }
            const auto relative_stem =
                std::string_view(base_lock_leaf)
                    .substr(0, base_lock_leaf.size() -
                                   DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size());
            auto names = manifest_private_lease_names_from_relative_stem(manifest, relative_stem);
            if (!names.has_value() || names->names.base_lock_leaf != base_lock_leaf ||
                names->worker_attempt_names.has_value() ==
                    names->merge_generation_names.has_value() ||
                names->worker_coordinate.has_value() == names->merge_attempt_ordinal.has_value()) {
                return conflict();
            }
            const bool merge_started_record_leaf_present =
                names->merge_generation_names.has_value() &&
                (std::binary_search(inventory.merge_started_record_leaves.begin(),
                                    inventory.merge_started_record_leaves.end(),
                                    names->merge_generation_names->canonical_record_leaf) ||
                 std::binary_search(inventory.merge_started_record_leaves.begin(),
                                    inventory.merge_started_record_leaves.end(),
                                    names->merge_generation_names->pending_record_leaf));
            attempts.push_back(PrivateLeaseAttemptInventory{
                .names = std::move(names->names),
                .worker_attempt_names = std::move(names->worker_attempt_names),
                .worker_coordinate = names->worker_coordinate,
                .merge_generation_names = std::move(names->merge_generation_names),
                .merge_attempt_ordinal = names->merge_attempt_ordinal,
                .manifest_chunk_order = names->manifest_chunk_order,
                .base_lock_identity = base_lock_identities[index],
                .merge_started_record_leaf_present = merge_started_record_leaf_present,
            });
        }

        for (const auto& leaf : inventory.private_lease_protocol_leaves) {
            auto parsed = parse_manifest_bound_private_lease_protocol_leaf(manifest, leaf);
            if (!parsed.has_value()) {
                return conflict();
            }
            const auto position = std::lower_bound(
                attempts.begin(), attempts.end(), parsed->names.base_lock_leaf,
                [](const PrivateLeaseAttemptInventory& candidate, const std::string& base_lock) {
                    return candidate.names.base_lock_leaf < base_lock;
                });
            if (position == attempts.end() ||
                position->names.base_lock_leaf != parsed->names.base_lock_leaf ||
                position->worker_attempt_names.has_value() !=
                    parsed->worker_attempt_names.has_value() ||
                position->merge_generation_names.has_value() !=
                    parsed->merge_generation_names.has_value()) {
                return conflict();
            }
            switch (parsed->role) {
            case PrivateLeaseProtocolLeafRole::reserved:
                if (position->reserved) {
                    return conflict();
                }
                position->reserved = true;
                break;
            case PrivateLeaseProtocolLeafRole::reserved_pending:
                if (position->reserved_pending) {
                    return conflict();
                }
                position->reserved_pending = true;
                break;
            case PrivateLeaseProtocolLeafRole::owned:
                if (position->owned) {
                    return conflict();
                }
                position->owned = true;
                break;
            case PrivateLeaseProtocolLeafRole::owned_pending:
                if (position->owned_pending) {
                    return conflict();
                }
                position->owned_pending = true;
                break;
            case PrivateLeaseProtocolLeafRole::rollback_handoff:
                if (position->rollback_handoff) {
                    return conflict();
                }
                position->rollback_handoff = true;
                break;
            case PrivateLeaseProtocolLeafRole::staging_directory:
                if (position->staging_directory_leaf.has_value() ||
                    !parsed->staging_lease_id.has_value()) {
                    return conflict();
                }
                position->staging_directory_leaf = leaf;
                position->staging_lease_id = parsed->staging_lease_id;
                break;
            case PrivateLeaseProtocolLeafRole::final_directory:
                if (position->final_directory) {
                    return conflict();
                }
                position->final_directory = true;
                break;
            }
        }
        return {std::move(attempts), {}};
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

[[nodiscard]] PrivateLeaseProtocolInventoryValidationResult
validate_private_lease_protocol_inventory(
    int root_fd, const std::filesystem::path& absolute_root, const NamespaceInventory& inventory,
    const WaveManifestV1& manifest, std::span<const NativeIdentityV1> base_lock_identities,
    std::uint64_t creator_process_id, HeldPrivateLeaseBaseLockInventoryV1 held = {},
    HeldCanonicalPublicationInventoryV1 held_canonical = {}) noexcept {
    if (inventory.private_lease_base_lock_leaves.size() != base_lock_identities.size()) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())};
    }
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return PrivateLeaseProtocolInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    auto parsed = parse_private_lease_attempt_inventory(inventory, manifest, base_lock_identities);
    if (!parsed) {
        return fail_with(std::move(parsed.diagnostic));
    }

    std::vector<PrivateLeaseReservationWitness> witnesses;
    try {
        witnesses.reserve(parsed.attempts->size());
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
    std::vector<bool> held_matches;
    std::vector<bool> held_canonical_matches;
    try {
        held_matches.resize(held.locks.size(), false);
        held_canonical_matches.resize(held_canonical.entries.size(), false);
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
    for (const auto& attempt : *parsed.attempts) {
        const DistributedSievePrivateLeaseBaseLockAt* matching_held = nullptr;
        for (std::size_t index = 0; index < held.locks.size(); ++index) {
            const auto* candidate = held.locks[index];
            if (candidate == nullptr ||
                !candidate->matches_exact_binding(attempt.names.base_lock_leaf,
                                                  attempt.base_lock_identity)) {
                continue;
            }
            if (matching_held != nullptr || held_matches[index]) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
            matching_held = candidate;
            held_matches[index] = true;
        }
        const HeldCanonicalPublicationInventoryEntryV1* matching_canonical = nullptr;
        for (std::size_t index = 0; index < held_canonical.entries.size(); ++index) {
            const auto& candidate = held_canonical.entries[index];
            if (candidate.base_lock_leaf != attempt.names.base_lock_leaf ||
                candidate.base_lock_identity != attempt.base_lock_identity) {
                continue;
            }
            if (matching_canonical != nullptr || held_canonical_matches[index]) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
            matching_canonical = std::addressof(candidate);
            held_canonical_matches[index] = true;
        }
        if (matching_held != nullptr && matching_canonical != nullptr) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (matching_canonical != nullptr) {
            const auto& retained = *matching_canonical;
            const auto* expected = retained.expected_lease;
            const auto* expected_prefix = retained.expected_prefix;
            const bool canonical_root_shape =
                !attempt.reserved && !attempt.reserved_pending && attempt.owned &&
                !attempt.owned_pending && !attempt.rollback_handoff && attempt.final_directory &&
                !attempt.staging_directory_leaf.has_value();
            if (expected == nullptr || expected_prefix == nullptr || retained.permit == nullptr ||
                retained.reader_slot == nullptr || retained.adopting == nullptr ||
                !canonical_root_shape || expected->base_lock_leaf != attempt.names.base_lock_leaf ||
                expected->boundary !=
                    DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
                !expected->directory_identity.has_value() ||
                !expected->owner_marker_identity.has_value() ||
                !expected->owned_marker_identity.has_value() ||
                !expected_prefix->canonical_terminal() ||
                !worker_handoff_publication_prefix_marker_chain_matches(
                    *expected_prefix, manifest.wave_root_identity, attempt,
                    *expected->directory_identity)) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
            auto directory = inspect_private_lease_directory_at(
                root_fd, attempt.names.private_directory_leaf, creator_process_id);
            if (!directory || *directory.identity != *expected->directory_identity) {
                return fail_with(
                    directory ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                           protocol_error())
                              : std::move(directory.diagnostic));
            }
            const auto& directory_inventory = *directory.inventory;
            if (!directory_inventory.owner || directory_inventory.owner_pending ||
                directory_inventory.work_package_residue || !directory_inventory.corpus_index ||
                !directory_inventory.corpus_data || !directory_inventory.worker_handoff ||
                directory_inventory.worker_handoff_pending) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }

            const bool is_current = held_canonical.current == matching_canonical;
            const auto* reader = is_current && held_canonical.current_reader != nullptr
                                     ? held_canonical.current_reader
                                     : retained.reader_slot->get();
            if (is_current) {
                if (!*retained.adopting || !retained.permit->held() ||
                    (reader != nullptr &&
                     !held_publication_reader_matches(*reader, *expected_prefix, *expected))) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
            } else if (reader != nullptr) {
                if (*retained.adopting || retained.permit->held() ||
                    !held_publication_reader_matches(*reader, *expected_prefix, *expected)) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
            } else {
                if (*retained.adopting || !retained.permit->valid() || !retained.permit->held()) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                const auto revalidated =
                    private_lease::revalidate_private_handoff_publication_resume_v1(
                        *retained.permit);
                if (!revalidated.revalidated() || !revalidated.witness.has_value()) {
                    return fail_with(worker_handoff_inspection_failure(revalidated.result));
                }
                if (*revalidated.witness != *expected_prefix) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
            }
            try {
                witnesses.push_back(*expected);
            } catch (const std::bad_alloc&) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                            std::make_error_code(std::errc::not_enough_memory)));
            } catch (...) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            std::make_error_code(std::errc::io_error)));
            }
            continue;
        }
        const bool any_root_protocol = attempt.reserved || attempt.reserved_pending ||
                                       attempt.owned || attempt.owned_pending ||
                                       attempt.rollback_handoff || attempt.final_directory ||
                                       attempt.staging_directory_leaf.has_value();
        if (!held_canonical.entries.empty() && any_root_protocol) {
            // No new logical action or lower-order BaseLock acquisition is
            // allowed after the final worker/target permit stack exists.
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        auto validated = validate_private_lease_attempt_inventory(
            root_fd, absolute_root, attempt, manifest, creator_process_id, matching_held);
        if (!validated) {
            return fail_with(std::move(validated.diagnostic));
        }
        try {
            witnesses.push_back(std::move(*validated.witness));
        } catch (const std::bad_alloc&) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                        std::make_error_code(std::errc::not_enough_memory)));
        } catch (...) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                        std::make_error_code(std::errc::io_error)));
        }
    }
    if (std::ranges::find(held_matches, false) != held_matches.end()) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (std::ranges::find(held_canonical_matches, false) != held_canonical_matches.end()) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    return {std::move(witnesses), {}};
}

struct WorkerAttemptRecordInventoryValidationResult final {
    std::optional<std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness>> witnesses;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witnesses.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] bool attempt_lease_identities_conflict(const LeaseIdentityV1& left,
                                                     const LeaseIdentityV1& right) noexcept {
    return left.lease_id == right.lease_id || left.owner_marker == right.owner_marker ||
           left.owner_marker == right.directory || left.directory == right.owner_marker ||
           left.directory == right.directory;
}

[[nodiscard]] bool attempt_record_matches_live_lease_projection(
    const AttemptStartedV1& record,
    const DistributedSievePrivateLeaseReservationInventoryWitness& lease) noexcept {
    if (lease.merge_prepared.has_value() || lease.merge_raw_writer_recovery.has_value()) {
        return false;
    }
    if (lease.worker_handoff.has_value()) {
        return lease.worker_handoff->handoff.attempt_started_digest == record.self_digest &&
               lease.worker_handoff->handoff.lease == record.lease &&
               lease.lease_id == record.lease.lease_id.limbs &&
               lease.directory_identity == record.lease.directory &&
               lease.owner_marker_identity == record.lease.owner_marker;
    }
    if (lease.boundary == DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
        return true;
    }
    if (lease.lease_id != record.lease.lease_id.limbs) {
        return false;
    }
    if (lease.directory_identity.has_value() &&
        *lease.directory_identity != record.lease.directory) {
        return false;
    }
    return !lease.owner_marker_identity.has_value() ||
           *lease.owner_marker_identity == record.lease.owner_marker;
}

[[nodiscard]] bool merge_started_record_matches_live_lease_projection(
    const MergeStartedV1& record,
    const DistributedSievePrivateLeaseReservationInventoryWitness& lease) noexcept {
    if (lease.worker_handoff.has_value() || lease.work_package_residue.has_value()) {
        return false;
    }
    if (lease.merge_raw_writer_recovery.has_value()) {
        const auto& recovery = *lease.merge_raw_writer_recovery;
        return lease.lease_id == record.merged_lease.lease_id.limbs &&
               lease.directory_identity == record.merged_lease.directory &&
               lease.owner_marker_identity == record.merged_lease.owner_marker &&
               recovery.lease_id == record.merged_lease.lease_id.limbs &&
               recovery.directory_identity == record.merged_lease.directory &&
               recovery.owner_marker_identity == record.merged_lease.owner_marker &&
               lease.directory_identity == recovery.directory_identity &&
               lease.owner_marker_identity == recovery.owner_marker_identity &&
               lease.owned_marker_identity == recovery.owned_marker_identity &&
               lease.reserved_marker_identity == recovery.reserved_marker_identity;
    }
    if (lease.merge_prepared.has_value()) {
        return lease.merge_prepared->prepared.merge_started_digest == record.self_digest &&
               lease.merge_prepared->prepared.merged_lease == record.merged_lease &&
               lease.lease_id == record.merged_lease.lease_id.limbs &&
               lease.directory_identity == record.merged_lease.directory &&
               lease.owner_marker_identity == record.merged_lease.owner_marker;
    }
    if (lease.boundary == DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
        return true;
    }
    if (lease.lease_id != record.merged_lease.lease_id.limbs) {
        return false;
    }
    if (lease.directory_identity.has_value() &&
        *lease.directory_identity != record.merged_lease.directory) {
        return false;
    }
    return !lease.owner_marker_identity.has_value() ||
           *lease.owner_marker_identity == record.merged_lease.owner_marker;
}

[[nodiscard]] WorkerAttemptRecordInventoryValidationResult validate_worker_attempt_record_inventory(
    int root_fd, const NamespaceInventory& inventory, const WaveManifestV1& manifest,
    std::span<const NativeIdentityV1> base_lock_identities,
    std::span<const DistributedSievePrivateLeaseReservationInventoryWitness> private_leases,
    std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return WorkerAttemptRecordInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = status;
        return fail_with(std::move(failure));
    };

    if (inventory.private_lease_base_lock_leaves.size() != base_lock_identities.size() ||
        inventory.private_lease_base_lock_leaves.size() != private_leases.size()) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
    }

    std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness> records;
    try {
        records.reserve(inventory.worker_attempt_record_leaves.size());
        for (const auto& leaf : inventory.worker_attempt_record_leaves) {
            const auto parsed = parse_distributed_sieve_worker_attempt_leaf_v1(leaf);
            if (!parsed.has_value() || parsed->attempt_ordinal >= manifest.max_worker_attempts) {
                return conflict();
            }
            const ChunkPlanV1* chunk = nullptr;
            for (const auto& candidate : manifest.chunks) {
                if (candidate.chunk_id == parsed->chunk_id) {
                    chunk = &candidate;
                    break;
                }
            }
            if (chunk == nullptr || chunk->sq_begin >= chunk->sq_end) {
                return conflict();
            }
            const auto names = distributed_sieve_worker_attempt_names_v1(
                chunk->relative_artifact_stem, parsed->chunk_id, parsed->attempt_ordinal);
            if (!names.has_value() || leaf != (parsed->pending ? names->pending_record_leaf
                                                               : names->canonical_record_leaf)) {
                return conflict();
            }

            auto loaded = read_immutable_protocol_record_leaf(
                root_fd, leaf.c_str(), creator_process_id,
                DistributedSieveWaveStoreStatus::namespace_conflict,
                DistributedSieveWaveStoreStatus::namespace_conflict);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            auto decoded = decode_distributed_sieve_record(*loaded.bytes);
            if (!decoded) {
                return protocol_conflict(decoded.status);
            }
            auto* attempt = std::get_if<AttemptStartedV1>(&*decoded.value);
            if (attempt == nullptr || attempt->chunk_id != parsed->chunk_id ||
                attempt->attempt_ordinal != parsed->attempt_ordinal ||
                attempt->manifest_digest != manifest.self_digest ||
                attempt->sq_begin != chunk->sq_begin || attempt->sq_end != chunk->sq_end ||
                attempt->retry_policy_version != manifest.retry_policy_version ||
                attempt->lease.relative_stem != names->relative_lease_stem) {
                return conflict();
            }

            const auto position = std::lower_bound(
                records.begin(), records.end(),
                std::pair{parsed->chunk_id, parsed->attempt_ordinal},
                [](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate,
                   const std::pair<std::uint32_t, std::uint32_t>& coordinate) {
                    return std::pair{candidate.chunk_id, candidate.attempt_ordinal} < coordinate;
                });
            if (position == records.end() || position->chunk_id != parsed->chunk_id ||
                position->attempt_ordinal != parsed->attempt_ordinal) {
                DistributedSieveWorkerAttemptRecordInventoryWitness witness{
                    .chunk_id = parsed->chunk_id,
                    .attempt_ordinal = parsed->attempt_ordinal,
                    .record = std::move(*attempt),
                    .bytes = std::move(*loaded.bytes),
                };
                if (parsed->pending) {
                    witness.pending_snapshot = *loaded.snapshot;
                } else {
                    witness.canonical_snapshot = *loaded.snapshot;
                }
                records.insert(position, std::move(witness));
                continue;
            }
            if (position->bytes != *loaded.bytes ||
                (parsed->pending ? position->pending_snapshot.has_value()
                                 : position->canonical_snapshot.has_value())) {
                return conflict();
            }
            if (parsed->pending) {
                position->pending_snapshot = *loaded.snapshot;
            } else {
                position->canonical_snapshot = *loaded.snapshot;
            }
        }
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }

    for (std::size_t left = 0; left < records.size(); ++left) {
        for (const auto& base_lock_identity : base_lock_identities) {
            if (records[left].record.lease.owner_marker == base_lock_identity ||
                records[left].record.lease.directory == base_lock_identity) {
                return conflict();
            }
        }
        for (std::size_t right = left + 1U; right < records.size(); ++right) {
            if (attempt_lease_identities_conflict(records[left].record.lease,
                                                  records[right].record.lease)) {
                return conflict();
            }
        }
    }

    for (const auto& chunk : manifest.chunks) {
        std::vector<AttemptStartedV1> canonical_chain;
        const DistributedSieveWorkerAttemptRecordInventoryWitness* pending = nullptr;
        try {
            canonical_chain.reserve(manifest.max_worker_attempts);
        } catch (const std::bad_alloc&) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                        std::make_error_code(std::errc::not_enough_memory)));
        } catch (...) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                        std::make_error_code(std::errc::io_error)));
        }

        for (const auto& record : records) {
            if (record.chunk_id != chunk.chunk_id) {
                continue;
            }
            if (record.canonical_snapshot.has_value()) {
                if (record.attempt_ordinal != canonical_chain.size()) {
                    return conflict();
                }
                try {
                    canonical_chain.push_back(record.record);
                } catch (const std::bad_alloc&) {
                    return fail_with(
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory)));
                } catch (...) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                                std::make_error_code(std::errc::io_error)));
                }
            }
            if (record.pending_snapshot.has_value()) {
                if (pending != nullptr) {
                    return conflict();
                }
                pending = &record;
            }
        }

        const WorkerHandoffV1* worker_handoff = nullptr;
        for (const auto& lease : private_leases) {
            if (!lease.worker_handoff.has_value() ||
                lease.worker_handoff->handoff.chunk_id != chunk.chunk_id) {
                continue;
            }
            if (worker_handoff != nullptr) {
                return conflict();
            }
            worker_handoff = &lease.worker_handoff->handoff;
        }
        if (worker_handoff != nullptr && pending != nullptr) {
            return conflict();
        }
        if (worker_handoff != nullptr) {
            for (const auto& lease : private_leases) {
                const auto coordinate =
                    manifest_worker_attempt_coordinate_from_private_lease_base_lock(
                        manifest, lease.base_lock_leaf);
                if (!coordinate.has_value()) {
                    if (manifest_merge_generation_from_private_lease_base_lock(manifest,
                                                                               lease.base_lock_leaf)
                            .has_value()) {
                        continue;
                    }
                    return conflict();
                }
                if (coordinate->chunk_id == chunk.chunk_id &&
                    coordinate->attempt_ordinal > worker_handoff->attempt_ordinal) {
                    return conflict();
                }
            }
        }

        if (!canonical_chain.empty() || worker_handoff != nullptr) {
            const auto status = validate_worker_attempt_chain(
                manifest, chunk.chunk_id, canonical_chain, worker_handoff, nullptr);
            if (!status) {
                return protocol_conflict(status);
            }
        }
        if (pending != nullptr) {
            if (pending->canonical_snapshot.has_value()) {
                if (canonical_chain.empty() ||
                    pending->attempt_ordinal + 1U != canonical_chain.size()) {
                    return conflict();
                }
            } else {
                if (pending->attempt_ordinal != canonical_chain.size()) {
                    return conflict();
                }
                try {
                    canonical_chain.push_back(pending->record);
                } catch (const std::bad_alloc&) {
                    return fail_with(
                        diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                   std::make_error_code(std::errc::not_enough_memory)));
                } catch (...) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                                std::make_error_code(std::errc::io_error)));
                }
                const auto status = validate_worker_attempt_chain(
                    manifest, chunk.chunk_id, canonical_chain, nullptr, nullptr);
                if (!status) {
                    return protocol_conflict(status);
                }
            }
        }

        const std::size_t canonical_count =
            canonical_chain.size() -
            static_cast<std::size_t>(pending != nullptr &&
                                     !pending->canonical_snapshot.has_value());
        const bool next_attempt_is_pending =
            pending != nullptr && !pending->canonical_snapshot.has_value();
        for (const auto& record : records) {
            if (record.chunk_id != chunk.chunk_id) {
                continue;
            }
            const auto names = distributed_sieve_worker_attempt_names_v1(
                chunk.relative_artifact_stem, chunk.chunk_id, record.attempt_ordinal);
            if (!names.has_value()) {
                return conflict();
            }
            const auto lock_position = std::lower_bound(
                inventory.private_lease_base_lock_leaves.begin(),
                inventory.private_lease_base_lock_leaves.end(), names->base_lock_leaf);
            if (lock_position == inventory.private_lease_base_lock_leaves.end() ||
                *lock_position != names->base_lock_leaf) {
                return conflict();
            }
            const auto lock_index = static_cast<std::size_t>(
                std::distance(inventory.private_lease_base_lock_leaves.begin(), lock_position));
            const auto& lease = private_leases[lock_index];
            if (!attempt_record_matches_live_lease_projection(record.record, lease)) {
                return conflict();
            }

            const bool pending_only =
                record.pending_snapshot.has_value() && !record.canonical_snapshot.has_value();
            if (pending_only &&
                lease.boundary !=
                    DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
                return conflict();
            }
            const bool historical_canonical =
                record.canonical_snapshot.has_value() &&
                (next_attempt_is_pending ? record.attempt_ordinal < canonical_count
                                         : record.attempt_ordinal + 1U < canonical_count);
            if (historical_canonical &&
                lease.boundary != DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
                return conflict();
            }
        }
    }

    for (const auto& lease : private_leases) {
        if (!lease.work_package_residue.has_value()) {
            continue;
        }
        if (lease.boundary !=
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
            !lease.directory_identity.has_value() || !lease.owner_marker_identity.has_value() ||
            !lease.owned_marker_identity.has_value() ||
            lease.work_package_residue->work_sha256 != manifest.work_sha256 ||
            !lease.base_lock_leaf.ends_with(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
            return conflict();
        }
        const auto relative_stem =
            std::string_view(lease.base_lock_leaf)
                .substr(0, lease.base_lock_leaf.size() -
                               DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size());
        const auto names = manifest_attempt_names_from_relative_stem(manifest, relative_stem);
        if (!names.has_value() || names->base_lock_leaf != lease.base_lock_leaf) {
            return conflict();
        }
        const auto parsed =
            parse_distributed_sieve_worker_attempt_leaf_v1(names->canonical_record_leaf);
        if (!parsed.has_value() || parsed->pending) {
            return conflict();
        }
        const auto record = std::lower_bound(
            records.begin(), records.end(), std::pair{parsed->chunk_id, parsed->attempt_ordinal},
            [](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate,
               const std::pair<std::uint32_t, std::uint32_t>& coordinate) {
                return std::pair{candidate.chunk_id, candidate.attempt_ordinal} < coordinate;
            });
        if (record == records.end() || record->chunk_id != parsed->chunk_id ||
            record->attempt_ordinal != parsed->attempt_ordinal ||
            !record->canonical_snapshot.has_value() || record->pending_snapshot.has_value() ||
            record->record.chunk_id != parsed->chunk_id ||
            record->record.attempt_ordinal != parsed->attempt_ordinal ||
            record->record.manifest_digest != manifest.self_digest ||
            record->record.lease.lease_id.limbs != lease.lease_id ||
            record->record.lease.owner_marker != *lease.owner_marker_identity ||
            record->record.lease.directory != *lease.directory_identity ||
            record->record.lease.relative_stem != names->relative_lease_stem) {
            return conflict();
        }
        const auto later =
            std::find_if(std::next(record), records.end(),
                         [&](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate) {
                             return candidate.chunk_id == parsed->chunk_id;
                         });
        if (later != records.end()) {
            return conflict();
        }
        for (const auto& candidate_lease : private_leases) {
            if (!candidate_lease.base_lock_leaf.ends_with(
                    DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
                return conflict();
            }
            const auto candidate_relative_stem =
                std::string_view(candidate_lease.base_lock_leaf)
                    .substr(0, candidate_lease.base_lock_leaf.size() -
                                   DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size());
            const auto candidate_names =
                manifest_attempt_names_from_relative_stem(manifest, candidate_relative_stem);
            if (!candidate_names.has_value()) {
                if (manifest_private_lease_names_from_relative_stem(manifest,
                                                                    candidate_relative_stem)
                        .has_value()) {
                    continue;
                }
                return conflict();
            }
            const auto candidate = parse_distributed_sieve_worker_attempt_leaf_v1(
                candidate_names->canonical_record_leaf);
            if (!candidate.has_value() || candidate->pending) {
                return conflict();
            }
            if (candidate->chunk_id == parsed->chunk_id &&
                candidate->attempt_ordinal > parsed->attempt_ordinal) {
                return conflict();
            }
        }
    }
    return {std::move(records), {}};
}

struct WaveMergeCommitRecordInventoryValidationResult final {
    std::optional<std::vector<DistributedSieveWaveMergeCommitRecordInventoryWitnessV1>> witnesses;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witnesses.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] WaveMergeCommitRecordInventoryValidationResult
validate_wave_merge_commit_record_inventory(int root_fd, const NamespaceInventory& inventory,
                                            const WaveManifestV1& manifest,
                                            std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return WaveMergeCommitRecordInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = status;
        return fail_with(std::move(failure));
    };

    std::vector<DistributedSieveWaveMergeCommitRecordInventoryWitnessV1> records;
    try {
        records.reserve(1U);
        const auto names = distributed_sieve_wave_merge_commit_names_v1();
        for (const auto& leaf : inventory.wave_merge_commit_record_leaves) {
            const auto parsed = parse_distributed_sieve_wave_merge_commit_leaf_v1(leaf);
            if (!parsed.has_value() || leaf != (parsed->pending ? names.pending_record_leaf
                                                                : names.canonical_record_leaf)) {
                return conflict();
            }
            auto loaded = read_immutable_protocol_record_leaf(
                root_fd, leaf.c_str(), creator_process_id,
                DistributedSieveWaveStoreStatus::namespace_conflict,
                DistributedSieveWaveStoreStatus::namespace_conflict);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            auto decoded = decode_distributed_sieve_record(*loaded.bytes);
            if (!decoded) {
                return protocol_conflict(decoded.status);
            }
            auto* commit = std::get_if<WaveMergeCommitV1>(&*decoded.value);
            if (commit == nullptr || commit->manifest_digest != manifest.self_digest ||
                commit->work_digest != manifest.work_sha256 ||
                commit->merge_policy_version != manifest.merge_policy_version) {
                return conflict();
            }
            if (records.empty()) {
                DistributedSieveWaveMergeCommitRecordInventoryWitnessV1 witness{
                    .record = std::move(*commit),
                    .bytes = std::move(*loaded.bytes),
                };
                if (parsed->pending) {
                    witness.pending_snapshot = *loaded.snapshot;
                } else {
                    witness.canonical_snapshot = *loaded.snapshot;
                }
                records.push_back(std::move(witness));
                continue;
            }
            auto& witness = records.front();
            if (witness.bytes != *loaded.bytes ||
                (parsed->pending ? witness.pending_snapshot.has_value()
                                 : witness.canonical_snapshot.has_value())) {
                return conflict();
            }
            if (parsed->pending) {
                witness.pending_snapshot = *loaded.snapshot;
            } else {
                witness.canonical_snapshot = *loaded.snapshot;
            }
        }
        if (records.size() > 1U ||
            records.size() !=
                static_cast<std::size_t>(!inventory.wave_merge_commit_record_leaves.empty())) {
            return conflict();
        }
        return {std::move(records), {}};
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

struct MergeStartedRecordInventoryValidationResult final {
    std::optional<std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1>> witnesses;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witnesses.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] MergeStartedRecordInventoryValidationResult validate_merge_started_record_inventory(
    int root_fd, const NamespaceInventory& inventory, const WaveManifestV1& manifest,
    std::span<const NativeIdentityV1> base_lock_identities,
    std::span<const DistributedSievePrivateLeaseReservationInventoryWitness> private_leases,
    std::span<const DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempts,
    std::uint64_t creator_process_id, const WaveMergeCommitV1* commit = nullptr) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return MergeStartedRecordInventoryValidationResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = status;
        return fail_with(std::move(failure));
    };
    if (inventory.private_lease_base_lock_leaves.size() != base_lock_identities.size() ||
        inventory.private_lease_base_lock_leaves.size() != private_leases.size()) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
    }

    std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1> records;
    try {
        records.reserve(inventory.merge_started_record_leaves.size());
        for (const auto& leaf : inventory.merge_started_record_leaves) {
            const auto parsed = parse_distributed_sieve_merge_started_leaf_v1(leaf);
            if (!parsed.has_value() ||
                parsed->merge_attempt_ordinal >= manifest.max_merge_build_attempts) {
                return conflict();
            }
            const auto names =
                distributed_sieve_merge_generation_names_v1(parsed->merge_attempt_ordinal);
            if (!names.has_value() || leaf != (parsed->pending ? names->pending_record_leaf
                                                               : names->canonical_record_leaf)) {
                return conflict();
            }

            auto loaded = read_immutable_protocol_record_leaf(
                root_fd, leaf.c_str(), creator_process_id,
                DistributedSieveWaveStoreStatus::namespace_conflict,
                DistributedSieveWaveStoreStatus::namespace_conflict);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            auto decoded = decode_distributed_sieve_record(*loaded.bytes);
            if (!decoded) {
                return protocol_conflict(decoded.status);
            }
            auto* start = std::get_if<MergeStartedV1>(&*decoded.value);
            if (start == nullptr || start->manifest_digest != manifest.self_digest ||
                start->work_digest != manifest.work_sha256 ||
                start->merge_policy_version != manifest.merge_policy_version ||
                start->merge_attempt_ordinal != parsed->merge_attempt_ordinal ||
                start->merged_lease.relative_stem != names->relative_lease_stem) {
                return conflict();
            }

            const auto position = std::lower_bound(
                records.begin(), records.end(), parsed->merge_attempt_ordinal,
                [](const DistributedSieveMergeStartedRecordInventoryWitnessV1& candidate,
                   std::uint32_t ordinal) { return candidate.merge_attempt_ordinal < ordinal; });
            if (position == records.end() ||
                position->merge_attempt_ordinal != parsed->merge_attempt_ordinal) {
                DistributedSieveMergeStartedRecordInventoryWitnessV1 witness{
                    .merge_attempt_ordinal = parsed->merge_attempt_ordinal,
                    .record = std::move(*start),
                    .bytes = std::move(*loaded.bytes),
                };
                if (parsed->pending) {
                    witness.pending_snapshot = *loaded.snapshot;
                } else {
                    witness.canonical_snapshot = *loaded.snapshot;
                }
                records.insert(position, std::move(witness));
                continue;
            }
            if (position->bytes != *loaded.bytes ||
                (parsed->pending ? position->pending_snapshot.has_value()
                                 : position->canonical_snapshot.has_value())) {
                return conflict();
            }
            if (parsed->pending) {
                position->pending_snapshot = *loaded.snapshot;
            } else {
                position->canonical_snapshot = *loaded.snapshot;
            }
        }

        std::vector<MergeStartedV1> observed_chain;
        observed_chain.reserve(manifest.max_merge_build_attempts);
        const DistributedSieveMergeStartedRecordInventoryWitnessV1* pending = nullptr;
        for (const auto& record : records) {
            if (record.canonical_snapshot.has_value()) {
                if (record.merge_attempt_ordinal != observed_chain.size()) {
                    return conflict();
                }
                observed_chain.push_back(record.record);
            }
            if (record.pending_snapshot.has_value()) {
                if (pending != nullptr) {
                    return conflict();
                }
                pending = &record;
            }
        }
        if (pending != nullptr) {
            if (pending->canonical_snapshot.has_value()) {
                if (observed_chain.empty() ||
                    pending->merge_attempt_ordinal + 1U != observed_chain.size()) {
                    return conflict();
                }
            } else {
                if (pending->merge_attempt_ordinal != observed_chain.size()) {
                    return conflict();
                }
                observed_chain.push_back(pending->record);
            }
        }

        std::vector<std::vector<AttemptStartedV1>> attempt_chains(manifest.chunks.size());
        std::vector<const WorkerHandoffV1*> handoffs(manifest.chunks.size(), nullptr);
        std::vector<ChunkTerminalEvidenceViewV1> evidence;
        evidence.reserve(manifest.chunks.size());
        for (std::size_t chunk_index = 0; chunk_index < manifest.chunks.size(); ++chunk_index) {
            const auto& chunk = manifest.chunks[chunk_index];
            auto& chain = attempt_chains[chunk_index];
            for (const auto& attempt : worker_attempts) {
                if (attempt.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (!attempt.canonical_snapshot.has_value() ||
                    attempt.pending_snapshot.has_value() ||
                    attempt.attempt_ordinal != chain.size()) {
                    if (!observed_chain.empty()) {
                        return conflict();
                    }
                    continue;
                }
                chain.push_back(attempt.record);
            }
            for (const auto& lease : private_leases) {
                if (!lease.worker_handoff.has_value() ||
                    lease.worker_handoff->handoff.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (handoffs[chunk_index] != nullptr) {
                    return conflict();
                }
                handoffs[chunk_index] = &lease.worker_handoff->handoff;
            }
            evidence.push_back(ChunkTerminalEvidenceViewV1{
                .attempts = chain,
                .handoff = handoffs[chunk_index],
            });
        }
        const DistributedSieveMergePreparedInventoryWitnessV1* prepared_witness = nullptr;
        std::optional<std::uint32_t> prepared_ordinal;
        for (const auto& lease : private_leases) {
            if (!lease.merge_prepared.has_value()) {
                continue;
            }
            const auto ordinal = manifest_merge_generation_from_private_lease_base_lock(
                manifest, lease.base_lock_leaf);
            if (prepared_witness != nullptr || !ordinal.has_value() ||
                lease.boundary !=
                    DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
                return conflict();
            }
            prepared_witness = std::addressof(*lease.merge_prepared);
            prepared_ordinal = *ordinal;
        }
        if (!observed_chain.empty() || prepared_witness != nullptr || commit != nullptr) {
            const auto dependency = validate_merge_dependency_chain(
                manifest, evidence, observed_chain,
                prepared_witness != nullptr ? std::addressof(prepared_witness->prepared) : nullptr,
                commit);
            if (!dependency) {
                return protocol_conflict(dependency);
            }
        }
        if (prepared_witness != nullptr) {
            if (observed_chain.empty() || !prepared_ordinal.has_value() ||
                *prepared_ordinal + 1U != observed_chain.size() ||
                records.size() != observed_chain.size()) {
                return conflict();
            }
            const auto& latest = records.back();
            if (latest.merge_attempt_ordinal != *prepared_ordinal ||
                !latest.canonical_snapshot.has_value() || latest.pending_snapshot.has_value() ||
                prepared_witness->prepared.merge_started_digest != latest.record.self_digest ||
                prepared_witness->prepared.merged_lease != latest.record.merged_lease) {
                return conflict();
            }
        }

        const DistributedSieveMergeRawWriterRecoveryInventoryWitnessV1* raw_recovery = nullptr;
        std::optional<std::uint32_t> raw_recovery_ordinal;
        for (const auto& lease : private_leases) {
            if (!lease.merge_raw_writer_recovery.has_value()) {
                continue;
            }
            const auto ordinal = manifest_merge_generation_from_private_lease_base_lock(
                manifest, lease.base_lock_leaf);
            if (raw_recovery != nullptr || prepared_witness != nullptr || !ordinal.has_value()) {
                return conflict();
            }
            raw_recovery = std::addressof(*lease.merge_raw_writer_recovery);
            raw_recovery_ordinal = *ordinal;
            const bool has_pair = raw_recovery->index.has_value();
            if (raw_recovery->data.has_value() && !has_pair) {
                return conflict();
            }
            switch (raw_recovery->phase) {
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::FinalDirectoryRawPair:
                if (!has_pair || !raw_recovery->reserved_marker_identity.has_value() ||
                    lease.boundary !=
                        DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
                    return conflict();
                }
                break;
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryRawPair:
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryOwnerOnly:
                if ((raw_recovery->phase ==
                     DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryRawPair) !=
                        has_pair ||
                    !raw_recovery->reserved_marker_identity.has_value() ||
                    lease.boundary !=
                        DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable) {
                    return conflict();
                }
                break;
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryOwnerRemoved:
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::DirectoryAbsentReservedAndOwned:
                if (has_pair || !raw_recovery->reserved_marker_identity.has_value() ||
                    lease.boundary != DistributedSievePrivateLeaseReservationBoundary::Count) {
                    return conflict();
                }
                break;
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::DirectoryAbsentOwnedOnly:
                if (has_pair || raw_recovery->reserved_marker_identity.has_value() ||
                    lease.boundary != DistributedSievePrivateLeaseReservationBoundary::Count) {
                    return conflict();
                }
                break;
            case DistributedSieveMergeRawWriterRecoveryPhaseV1::Count:
                return conflict();
            }
        }
        if (raw_recovery != nullptr) {
            if (!raw_recovery_ordinal.has_value() || records.empty() ||
                *raw_recovery_ordinal + 1U != records.size()) {
                return conflict();
            }
            const auto& latest = records.back();
            if (latest.merge_attempt_ordinal != *raw_recovery_ordinal ||
                !latest.canonical_snapshot.has_value() || latest.pending_snapshot.has_value()) {
                return conflict();
            }
        }

        for (const auto& record : records) {
            const auto names =
                distributed_sieve_merge_generation_names_v1(record.merge_attempt_ordinal);
            if (!names.has_value()) {
                return conflict();
            }
            const auto position = std::lower_bound(inventory.private_lease_base_lock_leaves.begin(),
                                                   inventory.private_lease_base_lock_leaves.end(),
                                                   names->base_lock_leaf);
            if (position == inventory.private_lease_base_lock_leaves.end() ||
                *position != names->base_lock_leaf) {
                return conflict();
            }
            const auto index = static_cast<std::size_t>(
                std::distance(inventory.private_lease_base_lock_leaves.begin(), position));
            if (!merge_started_record_matches_live_lease_projection(record.record,
                                                                    private_leases[index])) {
                return conflict();
            }
            // A pending name can survive only the durable publication
            // transaction, which is admitted after the reservation reaches
            // P8. Cleanup never begins until the record has normalized to its
            // canonical-only name. Canonical-only records may legitimately
            // accompany any P0-P8 boundary while crash-resumable cleanup is
            // rolling the lease back.
            if (record.pending_snapshot.has_value() &&
                private_leases[index].boundary !=
                    DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
                return conflict();
            }
            for (const auto& base_lock_identity : base_lock_identities) {
                if (record.record.merged_lease.owner_marker == base_lock_identity ||
                    record.record.merged_lease.directory == base_lock_identity) {
                    return conflict();
                }
            }
        }

        // The existence of a later durable start proves every earlier
        // generation completed its typed start cleanup and returned to P0.
        // The newest start may still retain P8 while its writer is active.
        for (std::uint32_t ordinal = 0; ordinal + 1U < records.size(); ++ordinal) {
            const auto names = distributed_sieve_merge_generation_names_v1(ordinal);
            if (!names.has_value()) {
                return conflict();
            }
            const auto position = std::lower_bound(inventory.private_lease_base_lock_leaves.begin(),
                                                   inventory.private_lease_base_lock_leaves.end(),
                                                   names->base_lock_leaf);
            if (position == inventory.private_lease_base_lock_leaves.end() ||
                *position != names->base_lock_leaf) {
                return conflict();
            }
            const auto index = static_cast<std::size_t>(
                std::distance(inventory.private_lease_base_lock_leaves.begin(), position));
            if (private_leases[index].boundary !=
                DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
                return conflict();
            }
        }

        for (std::uint32_t ordinal = 0; ordinal < manifest.max_merge_build_attempts; ++ordinal) {
            const auto names = distributed_sieve_merge_generation_names_v1(ordinal);
            if (!names.has_value()) {
                return conflict();
            }
            const auto position = std::lower_bound(inventory.private_lease_base_lock_leaves.begin(),
                                                   inventory.private_lease_base_lock_leaves.end(),
                                                   names->base_lock_leaf);
            if (position == inventory.private_lease_base_lock_leaves.end() ||
                *position != names->base_lock_leaf) {
                continue;
            }
            if (ordinal > records.size()) {
                return conflict();
            }
            if (ordinal == records.size()) {
                const auto index = static_cast<std::size_t>(
                    std::distance(inventory.private_lease_base_lock_leaves.begin(), position));
                const auto& reservation = private_leases[index];
                if (reservation.worker_handoff.has_value() ||
                    reservation.work_package_residue.has_value() ||
                    reservation.merge_prepared.has_value() ||
                    reservation.merge_raw_writer_recovery.has_value()) {
                    return conflict();
                }
                for (std::uint32_t predecessor = 0; predecessor < records.size(); ++predecessor) {
                    const auto predecessor_names =
                        distributed_sieve_merge_generation_names_v1(predecessor);
                    if (!predecessor_names.has_value()) {
                        return conflict();
                    }
                    const auto predecessor_position =
                        std::lower_bound(inventory.private_lease_base_lock_leaves.begin(),
                                         inventory.private_lease_base_lock_leaves.end(),
                                         predecessor_names->base_lock_leaf);
                    if (predecessor_position == inventory.private_lease_base_lock_leaves.end() ||
                        *predecessor_position != predecessor_names->base_lock_leaf) {
                        return conflict();
                    }
                    const auto predecessor_index = static_cast<std::size_t>(std::distance(
                        inventory.private_lease_base_lock_leaves.begin(), predecessor_position));
                    if (private_leases[predecessor_index].boundary !=
                        DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
                        return conflict();
                    }
                }
            }
        }
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
    return {std::move(records), {}};
}

struct ChunkTerminalFailureRecordInventoryValidationResult final {
    std::optional<std::vector<DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1>>
        witnesses;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witnesses.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ChunkTerminalFailureRecordInventoryValidationResult
validate_chunk_terminal_failure_record_inventory(
    int root_fd, const NamespaceInventory& inventory, const WaveManifestV1& manifest,
    std::span<const DistributedSievePrivateLeaseReservationInventoryWitness> private_leases,
    std::span<const DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempts,
    std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return ChunkTerminalFailureRecordInventoryValidationResult{std::nullopt,
                                                                   std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = status;
        return fail_with(std::move(failure));
    };

    std::vector<DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1> records;
    try {
        records.reserve(inventory.chunk_terminal_failure_record_leaves.size());
        for (const auto& leaf : inventory.chunk_terminal_failure_record_leaves) {
            const auto parsed = parse_distributed_sieve_chunk_terminal_failure_leaf_v1(leaf);
            if (!parsed.has_value()) {
                return conflict();
            }
            const auto names = distributed_sieve_chunk_terminal_failure_names_v1(parsed->chunk_id);
            if (!names.has_value() || leaf != (parsed->pending ? names->pending_record_leaf
                                                               : names->canonical_record_leaf)) {
                return conflict();
            }
            const auto chunk = std::find_if(manifest.chunks.begin(), manifest.chunks.end(),
                                            [&](const ChunkPlanV1& candidate) {
                                                return candidate.chunk_id == parsed->chunk_id;
                                            });
            if (chunk == manifest.chunks.end() || chunk->sq_begin >= chunk->sq_end) {
                return conflict();
            }

            auto loaded = read_immutable_protocol_record_leaf(
                root_fd, leaf.c_str(), creator_process_id,
                DistributedSieveWaveStoreStatus::namespace_conflict,
                DistributedSieveWaveStoreStatus::namespace_conflict);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            auto decoded = decode_distributed_sieve_record(*loaded.bytes);
            if (!decoded) {
                return protocol_conflict(decoded.status);
            }
            auto* terminal = std::get_if<ChunkTerminalFailureV1>(&*decoded.value);
            if (terminal == nullptr || terminal->chunk_id != parsed->chunk_id ||
                terminal->manifest_digest != manifest.self_digest ||
                terminal->sq_begin != chunk->sq_begin || terminal->sq_end != chunk->sq_end) {
                return conflict();
            }

            const auto position =
                std::lower_bound(records.begin(), records.end(), parsed->chunk_id,
                                 [](const auto& candidate, std::uint32_t chunk_id) {
                                     return candidate.chunk_id < chunk_id;
                                 });
            if (position == records.end() || position->chunk_id != parsed->chunk_id) {
                DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1 witness{
                    .chunk_id = parsed->chunk_id,
                    .record = std::move(*terminal),
                    .bytes = std::move(*loaded.bytes),
                };
                if (parsed->pending) {
                    witness.pending_snapshot = *loaded.snapshot;
                } else {
                    witness.canonical_snapshot = *loaded.snapshot;
                }
                records.insert(position, std::move(witness));
                continue;
            }
            if (position->bytes != *loaded.bytes ||
                (parsed->pending ? position->pending_snapshot.has_value()
                                 : position->canonical_snapshot.has_value())) {
                return conflict();
            }
            if (parsed->pending) {
                position->pending_snapshot = *loaded.snapshot;
            } else {
                position->canonical_snapshot = *loaded.snapshot;
            }
        }
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }

    // A terminal chunk is an absorbing wave outcome. V1 intentionally
    // canonicalizes only the first manifest-order exhausted chunk, so multiple
    // terminal coordinates are never a valid aggregate.
    if (records.size() > 1U) {
        return conflict();
    }

    for (const auto& terminal : records) {
        std::vector<AttemptStartedV1> chain;
        try {
            chain.reserve(manifest.max_worker_attempts);
            for (const auto& attempt : worker_attempts) {
                if (attempt.chunk_id != terminal.chunk_id) {
                    continue;
                }
                if (!attempt.canonical_snapshot.has_value() ||
                    attempt.pending_snapshot.has_value() ||
                    attempt.attempt_ordinal != chain.size()) {
                    return conflict();
                }
                chain.push_back(attempt.record);
            }
        } catch (const std::bad_alloc&) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                        std::make_error_code(std::errc::not_enough_memory)));
        } catch (...) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                        std::make_error_code(std::errc::io_error)));
        }
        if (chain.size() != manifest.max_worker_attempts || chain.empty()) {
            return conflict();
        }

        const auto chunk = std::find_if(
            manifest.chunks.begin(), manifest.chunks.end(),
            [&](const ChunkPlanV1& candidate) { return candidate.chunk_id == terminal.chunk_id; });
        if (chunk == manifest.chunks.end()) {
            return conflict();
        }
        const auto final_names = distributed_sieve_worker_attempt_names_v1(
            chunk->relative_artifact_stem, terminal.chunk_id,
            static_cast<std::uint32_t>(chain.size() - 1U));
        if (!final_names.has_value()) {
            return conflict();
        }
        const auto final_lease =
            std::find_if(private_leases.begin(), private_leases.end(), [&](const auto& lease) {
                return lease.base_lock_leaf == final_names->base_lock_leaf;
            });
        if (final_lease == private_leases.end() ||
            final_lease->boundary !=
                DistributedSievePrivateLeaseReservationBoundary::PermitAcquired ||
            final_lease->work_package_residue.has_value() ||
            final_lease->worker_handoff.has_value() || final_lease->merge_prepared.has_value() ||
            final_lease->merge_raw_writer_recovery.has_value()) {
            return conflict();
        }
        for (const auto& lease : private_leases) {
            if (lease.merge_prepared.has_value() || lease.merge_raw_writer_recovery.has_value()) {
                return conflict();
            }
            if (!lease.worker_handoff.has_value()) {
                continue;
            }
            const auto coordinate = manifest_worker_attempt_coordinate_from_private_lease_base_lock(
                manifest, lease.base_lock_leaf);
            if (!coordinate.has_value() || coordinate->chunk_id == terminal.chunk_id) {
                return conflict();
            }
        }
        const auto status = validate_worker_attempt_chain(manifest, terminal.chunk_id, chain,
                                                          nullptr, &terminal.record);
        if (!status) {
            return protocol_conflict(status);
        }
    }
    return {std::move(records), {}};
}

[[nodiscard]] NativeIdentityV1
protocol_identity_words(const std::array<std::uint64_t, 3>& identity) noexcept {
    return {
        .volume = identity[0],
        .object = identity[1],
        .generation = identity[2],
    };
}

struct ExactCanonicalWorkerAttemptResult final {
    std::optional<DistributedSieveWorkerAttemptRecordInventoryWitness> witness;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

struct ExactCanonicalMergeStartedResult final {
    std::optional<DistributedSieveMergeStartedRecordInventoryWitnessV1> witness;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_exact_canonical_worker_attempt(
    int root_fd, const DistributedSieveWorkerAttemptNamesV1& names,
    const DistributedSieveWorkerAttemptRecordInventoryWitness& expected,
    std::uint64_t creator_process_id) noexcept;

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_exact_canonical_merge_started(
    int root_fd, const DistributedSieveMergeGenerationNamesV1& names,
    const DistributedSieveMergeStartedRecordInventoryWitnessV1& expected,
    std::uint64_t creator_process_id) noexcept;

[[nodiscard]] ExactCanonicalWorkerAttemptResult
load_exact_canonical_worker_attempt(int root_fd, const PrivateLeaseAttemptInventory& attempt,
                                    const WaveManifestV1& manifest,
                                    std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return ExactCanonicalWorkerAttemptResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }
    try {
        if (!attempt.worker_attempt_names.has_value() || !attempt.worker_coordinate.has_value()) {
            return conflict();
        }
        const auto& coordinate = *attempt.worker_coordinate;
        const auto& names = *attempt.worker_attempt_names;
        const auto chunk = std::find_if(
            manifest.chunks.begin(), manifest.chunks.end(),
            [&](const auto& candidate) { return candidate.chunk_id == coordinate.chunk_id; });
        if (chunk == manifest.chunks.end() || chunk->sq_begin >= chunk->sq_end) {
            return conflict();
        }
        auto canonical = read_immutable_protocol_record_leaf(
            root_fd, names.canonical_record_leaf.c_str(), creator_process_id,
            DistributedSieveWaveStoreStatus::namespace_conflict,
            DistributedSieveWaveStoreStatus::namespace_conflict);
        if (!canonical) {
            return fail_with(std::move(canonical.diagnostic));
        }
        struct stat pending{};
        if (fstatat_retrying_eintr(root_fd, names.pending_record_leaf.c_str(), pending) == 0) {
            return conflict();
        }
        if (errno != ENOENT) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        posix_error(errno)));
        }
        auto decoded = decode_distributed_sieve_record(*canonical.bytes);
        if (!decoded) {
            auto failure =
                diagnostic(decoded.status.error == DistributedSieveProtocolError::resource_exhausted
                               ? DistributedSieveWaveStoreStatus::resource_exhausted
                               : DistributedSieveWaveStoreStatus::namespace_conflict,
                           protocol_error());
            failure.protocol_status = decoded.status;
            return fail_with(std::move(failure));
        }
        auto* record = std::get_if<AttemptStartedV1>(&*decoded.value);
        if (record == nullptr || record->manifest_digest != manifest.self_digest ||
            record->chunk_id != coordinate.chunk_id ||
            record->attempt_ordinal != coordinate.attempt_ordinal ||
            record->sq_begin != chunk->sq_begin || record->sq_end != chunk->sq_end ||
            record->retry_policy_version != manifest.retry_policy_version ||
            record->lease.relative_stem != names.relative_lease_stem) {
            return conflict();
        }
        return {
            DistributedSieveWorkerAttemptRecordInventoryWitness{
                .chunk_id = record->chunk_id,
                .attempt_ordinal = record->attempt_ordinal,
                .record = std::move(*record),
                .bytes = std::move(*canonical.bytes),
                .canonical_snapshot = *canonical.snapshot,
                .pending_snapshot = std::nullopt,
            },
            {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

[[nodiscard]] ExactCanonicalMergeStartedResult
load_exact_canonical_merge_started(int root_fd, const PrivateLeaseAttemptInventory& attempt,
                                   const WaveManifestV1& manifest,
                                   std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return ExactCanonicalMergeStartedResult{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }
    try {
        if (!attempt.merge_generation_names.has_value() ||
            !attempt.merge_attempt_ordinal.has_value()) {
            return conflict();
        }
        const auto& names = *attempt.merge_generation_names;
        const auto ordinal = *attempt.merge_attempt_ordinal;
        auto canonical = read_immutable_protocol_record_leaf(
            root_fd, names.canonical_record_leaf.c_str(), creator_process_id,
            DistributedSieveWaveStoreStatus::namespace_conflict,
            DistributedSieveWaveStoreStatus::namespace_conflict);
        if (!canonical) {
            return fail_with(std::move(canonical.diagnostic));
        }
        struct stat pending{};
        if (fstatat_retrying_eintr(root_fd, names.pending_record_leaf.c_str(), pending) == 0) {
            return conflict();
        }
        if (errno != ENOENT) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        posix_error(errno)));
        }
        auto decoded = decode_distributed_sieve_record(*canonical.bytes);
        if (!decoded || !decoded.value.has_value()) {
            auto failure =
                diagnostic(decoded.status.error == DistributedSieveProtocolError::resource_exhausted
                               ? DistributedSieveWaveStoreStatus::resource_exhausted
                               : DistributedSieveWaveStoreStatus::namespace_conflict,
                           protocol_error());
            failure.protocol_status = decoded.status;
            return fail_with(std::move(failure));
        }
        auto* record = std::get_if<MergeStartedV1>(&*decoded.value);
        if (record == nullptr || record->manifest_digest != manifest.self_digest ||
            record->work_digest != manifest.work_sha256 ||
            record->merge_policy_version != manifest.merge_policy_version ||
            record->merge_attempt_ordinal != ordinal ||
            record->merged_lease.relative_stem != names.relative_lease_stem) {
            return conflict();
        }
        return {
            DistributedSieveMergeStartedRecordInventoryWitnessV1{
                .merge_attempt_ordinal = ordinal,
                .record = std::move(*record),
                .bytes = std::move(*canonical.bytes),
                .canonical_snapshot = *canonical.snapshot,
                .pending_snapshot = std::nullopt,
            },
            {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

struct WorkerHandoffPublicationPrefixCandidate final {
    std::size_t attempt_index = 0;
    private_lease::PrivateHandoffPublicationPrefixStateV1 expected_state =
        private_lease::PrivateHandoffPublicationPrefixStateV1::Count;
    DistributedSieveWorkerAttemptRecordInventoryWitness attempt_record;
};

struct RetainedWorkerHandoffPublicationPrefix final {
    std::size_t attempt_index = 0;
    std::size_t manifest_slot = 0;
    DistributedSieveWorkerAttemptNamesV1 names;
    ManifestWorkerAttemptCoordinate coordinate;
    private_lease::PrivateHandoffPublicationPrefixWitnessV1 witness;
    DistributedSieveWorkerHandoffInventoryWitnessV1 typed_handoff;
    PrivateLeaseReservationWitness provisional_lease;
    DistributedSieveWorkerAttemptRecordInventoryWitness attempt_record;
    private_lease::PrivateHandoffPublicationValidatedPermitV1 permit;
    std::unique_ptr<gnfs::relation::OOCPrivateHandoffReader> reader;
    bool adopting = false;
    bool consumed = false;
};

/// LIFO destruction is part of the lock-order contract. Prefix BaseLocks are
/// acquired in manifest-chunk/attempt order and released or consumed in the
/// exact reverse order.
class RetainedWorkerHandoffPublicationPrefixStack final {
public:
    RetainedWorkerHandoffPublicationPrefixStack() = default;
    RetainedWorkerHandoffPublicationPrefixStack(
        const RetainedWorkerHandoffPublicationPrefixStack&) = delete;
    RetainedWorkerHandoffPublicationPrefixStack&
    operator=(const RetainedWorkerHandoffPublicationPrefixStack&) = delete;
    RetainedWorkerHandoffPublicationPrefixStack(
        RetainedWorkerHandoffPublicationPrefixStack&& other) noexcept
        : entries(std::move(other.entries)) {}
    RetainedWorkerHandoffPublicationPrefixStack&
    operator=(RetainedWorkerHandoffPublicationPrefixStack&&) = delete;

    ~RetainedWorkerHandoffPublicationPrefixStack() {
        while (!entries.empty()) {
            entries.pop_back();
        }
    }

    std::vector<RetainedWorkerHandoffPublicationPrefix> entries;
};

struct WorkerHandoffPublicationAggregateWitness final {
    std::size_t attempt_index = 0;
    DistributedSieveWorkerAttemptNamesV1 names;
    ManifestWorkerAttemptCoordinate coordinate;
    private_lease::PrivateHandoffPublicationPrefixWitnessV1 prefix;
    DistributedSieveWorkerHandoffInventoryWitnessV1 typed_handoff;
    DistributedSieveWorkerAttemptRecordInventoryWitness attempt_record;

    [[nodiscard]] friend bool operator==(const WorkerHandoffPublicationAggregateWitness&,
                                         const WorkerHandoffPublicationAggregateWitness&) = default;
};

struct RecoverableWorkerHandoffInventoryWitness final {
    NamespaceInventory inventory;
    std::vector<NativeIdentityV1> base_lock_identities;
    std::vector<PrivateLeaseReservationWitness> private_leases;
    std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempt_records;
    std::vector<WorkerHandoffPublicationAggregateWitness> prefixes;

    [[nodiscard]] friend bool operator==(const RecoverableWorkerHandoffInventoryWitness&,
                                         const RecoverableWorkerHandoffInventoryWitness&) = default;
};

struct RecoverableWorkerHandoffInventoryResult final {
    std::optional<RecoverableWorkerHandoffInventoryWitness> witness;
    RetainedWorkerHandoffPublicationPrefixStack retained;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return witness.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

struct WorkerHandoffTypedValidationContext final {
    const PrivateLeaseAttemptInventory* attempt = nullptr;
    const WaveManifestV1* manifest = nullptr;
    const DistributedSieveWorkerAttemptRecordInventoryWitness* attempt_record = nullptr;
    int root_fd = -1;
    NativeIdentityV1 expected_directory_identity;
    std::uint64_t creator_process_id = 0;
    std::optional<DistributedSieveWorkerHandoffInventoryWitnessV1> typed_handoff;
    DistributedSieveWaveStoreDiagnostic diagnostic;
};

[[nodiscard]] bool validate_worker_handoff_prefix_type(
    const private_lease::PrivateHandoffPublicationPrefixWitnessV1& prefix, void* opaque) noexcept {
    auto* context = static_cast<WorkerHandoffTypedValidationContext*>(opaque);
    if (context == nullptr || context->root_fd < 0 || context->attempt == nullptr ||
        context->manifest == nullptr || context->attempt_record == nullptr ||
        !context->attempt->worker_attempt_names.has_value() ||
        !context->attempt->worker_coordinate.has_value() ||
        !context->attempt_record->canonical_snapshot.has_value() ||
        context->attempt_record->pending_snapshot.has_value()) {
        return false;
    }
    if (const auto exact = revalidate_exact_canonical_worker_attempt(
            context->root_fd, *context->attempt->worker_attempt_names, *context->attempt_record,
            context->creator_process_id);
        exact.status != DistributedSieveWaveStoreStatus::ready) {
        context->diagnostic = exact;
        return false;
    }
    const durable_record::RecordSnapshot* handoff_snapshot = nullptr;
    switch (prefix.state) {
    case private_lease::PrivateHandoffPublicationPrefixStateV1::PendingOnly:
        if (prefix.canonical_snapshot.has_value() || !prefix.pending_snapshot.has_value() ||
            prefix.rollback_snapshot.has_value()) {
            return false;
        }
        handoff_snapshot = &*prefix.pending_snapshot;
        break;
    case private_lease::PrivateHandoffPublicationPrefixStateV1::PendingRollback:
        if (prefix.canonical_snapshot.has_value() || prefix.pending_snapshot.has_value() ||
            !prefix.rollback_snapshot.has_value()) {
            return false;
        }
        handoff_snapshot = &*prefix.rollback_snapshot;
        break;
    case private_lease::PrivateHandoffPublicationPrefixStateV1::Canonical:
        if (!prefix.canonical_snapshot.has_value() || prefix.pending_snapshot.has_value() ||
            prefix.rollback_snapshot.has_value()) {
            return false;
        }
        handoff_snapshot = &*prefix.canonical_snapshot;
        break;
    case private_lease::PrivateHandoffPublicationPrefixStateV1::IdenticalDual:
        if (!prefix.canonical_snapshot.has_value() || !prefix.pending_snapshot.has_value() ||
            prefix.rollback_snapshot.has_value()) {
            return false;
        }
        handoff_snapshot = &*prefix.canonical_snapshot;
        break;
    case private_lease::PrivateHandoffPublicationPrefixStateV1::Count:
        return false;
    }
    const durable_record::RecordSnapshot index_snapshot{
        .identity = prefix.record.index.identity,
        .size = prefix.record.index.extent,
    };
    const durable_record::RecordSnapshot data_snapshot{
        .identity = prefix.record.data.identity,
        .size = prefix.record.data.extent,
    };
    auto typed = validate_worker_handoff_envelope(
        *context->attempt, *context->manifest, context->expected_directory_identity, prefix.record,
        *handoff_snapshot, index_snapshot, data_snapshot, context->creator_process_id);
    if (!typed) {
        context->diagnostic = std::move(typed.diagnostic);
        return false;
    }
    const auto& handoff = typed.witness->handoff;
    const auto& started = context->attempt_record->record;
    if (handoff.attempt_started_digest != started.self_digest || handoff.lease != started.lease ||
        handoff.chunk_id != context->attempt->worker_coordinate->chunk_id ||
        handoff.attempt_ordinal != context->attempt->worker_coordinate->attempt_ordinal) {
        context->diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        return false;
    }
    context->typed_handoff = std::move(*typed.witness);
    return true;
}

[[nodiscard]] bool worker_handoff_publication_prefix_marker_chain_matches(
    const private_lease::PrivateHandoffPublicationPrefixWitnessV1& prefix,
    const NativeIdentityV1& expected_wave_root_identity,
    const PrivateLeaseAttemptInventory& attempt,
    const NativeIdentityV1& expected_directory_identity) noexcept {
    using Capability = private_lease::PrivateLeaseCapability;
    using Phase = private_lease::PrivateLeasePhase;
    const auto& record = prefix.record;
    if (prefix.parent_identity != relation_identity(expected_wave_root_identity) ||
        prefix.lock_identity != relation_identity(attempt.base_lock_identity) ||
        prefix.directory_identity != relation_identity(expected_directory_identity) ||
        protocol_identity(record.lock_identity) != attempt.base_lock_identity ||
        protocol_identity(record.directory_identity) != expected_directory_identity ||
        attempt.rollback_handoff != prefix.rollback_armed() ||
        attempt.reserved != prefix.reserved.has_value() ||
        attempt.owned != prefix.owned.has_value()) {
        return false;
    }
    if (prefix.owner.has_value()) {
        const auto& owner = *prefix.owner;
        if (protocol_identity(record.owner_marker_identity) !=
                protocol_identity_words(owner.identity) ||
            record.lease_id != owner.record.lease_id || owner.record.phase != Phase::Owner ||
            owner.record.capability != Capability::RemoveOwnerAndEmptyLease) {
            return false;
        }
    }
    if (prefix.owned.has_value()) {
        const auto& owned = *prefix.owned;
        if (protocol_identity(record.owned_marker_identity) !=
                protocol_identity_words(owned.identity) ||
            record.lease_id != owned.record.lease_id || owned.record.phase != Phase::Owned ||
            owned.record.capability != Capability::RollbackPreactivePairAndLease ||
            owned.record.parent_identity != prefix.parent_identity ||
            owned.record.lock_identity != prefix.lock_identity ||
            owned.record.directory_identity != prefix.directory_identity) {
            return false;
        }
    }
    if (prefix.owner.has_value() && prefix.owned.has_value() &&
        (prefix.owner->record != private_lease::owner_record_for(prefix.owned->record) ||
         prefix.owned->record.owner_identity != prefix.owner->identity)) {
        return false;
    }
    if (!prefix.rollback_armed() &&
        (!prefix.owner.has_value() || !prefix.owned.has_value() || !attempt.final_directory)) {
        return false;
    }
    if (!prefix.reserved.has_value()) {
        return true;
    }
    const auto& reserved = *prefix.reserved;
    return reserved.record.phase == Phase::Reserved &&
           reserved.record.capability == Capability::RollbackStagingOnly &&
           reserved.record.lease_id == record.lease_id &&
           reserved.record.parent_identity == prefix.parent_identity &&
           reserved.record.lock_identity == prefix.lock_identity &&
           reserved.record.directory_identity == std::array<std::uint64_t, 3>{} &&
           reserved.record.owner_identity == std::array<std::uint64_t, 3>{};
}

struct MergePreparedPublicationPrefixCandidate final {
    std::size_t attempt_index = 0;
    private_lease::PrivateHandoffPublicationPrefixStateV1 expected_state =
        private_lease::PrivateHandoffPublicationPrefixStateV1::Count;
    DistributedSieveMergeStartedRecordInventoryWitnessV1 start_record;
};

struct MergePreparedTypedValidationContext final {
    const PrivateLeaseAttemptInventory* attempt = nullptr;
    const WaveManifestV1* manifest = nullptr;
    const DistributedSieveMergeStartedRecordInventoryWitnessV1* start_record = nullptr;
    const std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness>* worker_attempts =
        nullptr;
    const std::vector<PrivateLeaseReservationWitness>* private_leases = nullptr;
    const std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1>* merge_starts = nullptr;
    int root_fd = -1;
    NativeIdentityV1 expected_directory_identity;
    std::uint64_t creator_process_id = 0;
    std::optional<DistributedSieveMergePreparedInventoryWitnessV1> typed_prepared;
    DistributedSieveWaveStoreDiagnostic diagnostic;
};

struct MergePreparedPublicationAggregateWitness final {
    NamespaceInventory inventory;
    std::vector<NativeIdentityV1> base_lock_identities;
    std::vector<PrivateLeaseReservationWitness> private_leases;
    std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempt_records;
    std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1> merge_started_records;
    std::vector<DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1>
        chunk_terminal_failure_records;
    std::vector<DistributedSieveWaveMergeCommitRecordInventoryWitnessV1> wave_merge_commit_records;
    std::size_t attempt_index = 0;
    DistributedSieveMergeGenerationNamesV1 names;
    private_lease::PrivateHandoffPublicationPrefixWitnessV1 prefix;
    DistributedSieveMergePreparedInventoryWitnessV1 typed_prepared;
    DistributedSieveMergeStartedRecordInventoryWitnessV1 start_record;
    RetainedWorkerHandoffPublicationPrefixStack retained_workers;
};

struct RetainedMergePreparedPublicationPrefix final {
    std::size_t attempt_index = 0;
    DistributedSieveMergeGenerationNamesV1 names;
    private_lease::PrivateHandoffPublicationPrefixWitnessV1 witness;
    DistributedSieveMergePreparedInventoryWitnessV1 typed_prepared;
    PrivateLeaseReservationWitness provisional_lease;
    DistributedSieveMergeStartedRecordInventoryWitnessV1 start_record;
    private_lease::PrivateHandoffPublicationValidatedPermitV1 permit;
    std::unique_ptr<gnfs::relation::OOCPrivateHandoffReader> reader;
    bool adopting = false;
    bool consumed = false;
};

struct MergePreparedPublicationPrefixCaptureResult final {
    // `retained` is declared after `witness`, so every implicit destruction
    // releases the target permit before `witness.retained_workers` performs
    // its reverse-order worker-lock release.
    std::optional<MergePreparedPublicationAggregateWitness> witness;
    std::optional<RetainedMergePreparedPublicationPrefix> retained;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }

    [[nodiscard]] bool prefix_present() const noexcept {
        // Either half is enough to enter the fail-closed prefix path. Cold
        // open then enforces exact witness/permit parity, while a live claim
        // conservatively reports reconciliation_required.
        return witness.has_value() || retained.has_value();
    }
};

/// Authority-free projection for one worker whose generic handoff may already
/// have been removed by an externally authorized cleanup prefix. The value is
/// reconstructed only from a sealed root authorization and WaveMergeCommit;
/// it carries no relation handle or BaseLock.
struct CleanupRecoveredWorkerPublicationV1 final {
    std::size_t manifest_slot = 0;
    NativeIdentityV1 base_lock_identity;
    DistributedSieveWorkerHandoffInventoryWitnessV1 typed_handoff;
    PrivateLeaseReservationWitness provisional_lease;

    [[nodiscard]] friend bool operator==(const CleanupRecoveredWorkerPublicationV1&,
                                         const CleanupRecoveredWorkerPublicationV1&) = default;
};

[[nodiscard]] DistributedSieveProtocolStatus validate_merge_prepared_dependency_projection(
    const WaveManifestV1& manifest,
    std::span<const DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempts,
    std::span<const PrivateLeaseReservationWitness> private_leases,
    std::span<const DistributedSieveMergeStartedRecordInventoryWitnessV1> merge_starts,
    const MergePreparedV1& prepared) noexcept {
    try {
        std::vector<std::vector<AttemptStartedV1>> attempt_chains(manifest.chunks.size());
        std::vector<const WorkerHandoffV1*> handoffs(manifest.chunks.size(), nullptr);
        std::vector<ChunkTerminalEvidenceViewV1> evidence;
        std::vector<MergeStartedV1> starts;
        evidence.reserve(manifest.chunks.size());
        starts.reserve(merge_starts.size());
        for (const auto& start : merge_starts) {
            if (!start.canonical_snapshot.has_value() || start.pending_snapshot.has_value() ||
                start.merge_attempt_ordinal != starts.size()) {
                return {
                    .error = DistributedSieveProtocolError::noncanonical_order,
                    .element_index = start.merge_attempt_ordinal,
                };
            }
            starts.push_back(start.record);
        }
        for (std::size_t chunk_index = 0; chunk_index < manifest.chunks.size(); ++chunk_index) {
            const auto& chunk = manifest.chunks[chunk_index];
            auto& chain = attempt_chains[chunk_index];
            for (const auto& attempt : worker_attempts) {
                if (attempt.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (!attempt.canonical_snapshot.has_value() ||
                    attempt.pending_snapshot.has_value() ||
                    attempt.attempt_ordinal != chain.size()) {
                    return {
                        .error = DistributedSieveProtocolError::noncanonical_order,
                        .element_index = static_cast<std::uint32_t>(chunk_index),
                    };
                }
                chain.push_back(attempt.record);
            }
            for (const auto& lease : private_leases) {
                if (!lease.worker_handoff.has_value() ||
                    lease.worker_handoff->handoff.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (handoffs[chunk_index] != nullptr) {
                    return {
                        .error = DistributedSieveProtocolError::duplicate_entry,
                        .element_index = static_cast<std::uint32_t>(chunk_index),
                    };
                }
                handoffs[chunk_index] = &lease.worker_handoff->handoff;
            }
            evidence.push_back({
                .attempts = chain,
                .handoff = handoffs[chunk_index],
            });
        }
        return validate_merge_dependency_chain(manifest, evidence, starts, &prepared, nullptr);
    } catch (const std::bad_alloc&) {
        return {
            .error = DistributedSieveProtocolError::resource_exhausted,
        };
    } catch (...) {
        return {
            .error = DistributedSieveProtocolError::invalid_value,
        };
    }
}

using MergePreparedStartedRevalidatorV1 = DistributedSieveWaveStoreDiagnostic (*)(
    int, const DistributedSieveMergeGenerationNamesV1&,
    const DistributedSieveMergeStartedRecordInventoryWitnessV1&, std::uint64_t) noexcept;
using MergePreparedEnvelopeValidatorV1 = MergePreparedInventoryValidationResult (*)(
    const PrivateLeaseAttemptInventory&, const WaveManifestV1&, const NativeIdentityV1&,
    const gnfs::relation::OOCPrivateHandoffRecordV1&, const durable_record::RecordSnapshot&,
    const durable_record::RecordSnapshot&, const durable_record::RecordSnapshot&,
    std::uint64_t) noexcept;
using MergePreparedDependencyValidatorV1 = DistributedSieveProtocolStatus (*)(
    const WaveManifestV1&, std::span<const DistributedSieveWorkerAttemptRecordInventoryWitness>,
    std::span<const PrivateLeaseReservationWitness>,
    std::span<const DistributedSieveMergeStartedRecordInventoryWitnessV1>,
    const MergePreparedV1&) noexcept;

constexpr MergePreparedStartedRevalidatorV1 closed_merge_started_revalidator_v1 =
    &revalidate_exact_canonical_merge_started;
constexpr MergePreparedEnvelopeValidatorV1 closed_merge_prepared_envelope_validator_v1 =
    &validate_merge_prepared_envelope;
constexpr MergePreparedDependencyValidatorV1 closed_merge_prepared_dependency_validator_v1 =
    &validate_merge_prepared_dependency_projection;

#if defined(classify_merge_prepared_publication_prefix_v1) ||                                      \
    defined(validate_merge_prepared_prefix_type) ||                                                \
    defined(revalidate_exact_canonical_merge_started) ||                                           \
    defined(validate_merge_prepared_envelope) ||                                                   \
    defined(validate_merge_prepared_dependency_projection) ||                                      \
    defined(closed_merge_started_revalidator_v1) ||                                                \
    defined(closed_merge_prepared_envelope_validator_v1) ||                                        \
    defined(closed_merge_prepared_dependency_validator_v1) ||                                      \
    defined(acquire_private_handoff_publication_resume_v1) ||                                      \
    defined(validate_private_handoff_publication_resume_v1) ||                                     \
    defined(revalidate_private_handoff_publication_resume_v1) ||                                   \
    defined(WorkerHandoffTypedValidatorAuthorityV1)
#error "MergePrepared authority identifiers must not be preprocessor macros"
#endif

[[nodiscard]] bool validate_merge_prepared_prefix_type(
    const private_lease::PrivateHandoffPublicationPrefixWitnessV1& prefix, void* opaque) noexcept {
    auto* context = static_cast<MergePreparedTypedValidationContext*>(opaque);
    if (context == nullptr || context->attempt == nullptr || context->manifest == nullptr ||
        context->start_record == nullptr || context->worker_attempts == nullptr ||
        context->private_leases == nullptr || context->merge_starts == nullptr ||
        context->root_fd < 0 || !context->attempt->merge_generation_names.has_value() ||
        !context->attempt->merge_attempt_ordinal.has_value() ||
        !context->start_record->canonical_snapshot.has_value() ||
        context->start_record->pending_snapshot.has_value()) {
        return false;
    }
    try {
        if (const auto exact = closed_merge_started_revalidator_v1(
                context->root_fd, *context->attempt->merge_generation_names, *context->start_record,
                context->creator_process_id);
            exact.status != DistributedSieveWaveStoreStatus::ready) {
            context->diagnostic = exact;
            return false;
        }
        const durable_record::RecordSnapshot* handoff_snapshot = nullptr;
        switch (prefix.state) {
        case private_lease::PrivateHandoffPublicationPrefixStateV1::PendingOnly:
            if (prefix.canonical_snapshot.has_value() || !prefix.pending_snapshot.has_value() ||
                prefix.rollback_snapshot.has_value()) {
                return false;
            }
            handoff_snapshot = &*prefix.pending_snapshot;
            break;
        case private_lease::PrivateHandoffPublicationPrefixStateV1::PendingRollback:
            if (prefix.canonical_snapshot.has_value() || prefix.pending_snapshot.has_value() ||
                !prefix.rollback_snapshot.has_value()) {
                return false;
            }
            handoff_snapshot = &*prefix.rollback_snapshot;
            break;
        case private_lease::PrivateHandoffPublicationPrefixStateV1::Canonical:
            if (!prefix.canonical_snapshot.has_value() || prefix.pending_snapshot.has_value() ||
                prefix.rollback_snapshot.has_value()) {
                return false;
            }
            handoff_snapshot = &*prefix.canonical_snapshot;
            break;
        case private_lease::PrivateHandoffPublicationPrefixStateV1::IdenticalDual:
            if (!prefix.canonical_snapshot.has_value() || !prefix.pending_snapshot.has_value() ||
                prefix.rollback_snapshot.has_value()) {
                return false;
            }
            handoff_snapshot = &*prefix.canonical_snapshot;
            break;
        case private_lease::PrivateHandoffPublicationPrefixStateV1::Count:
            return false;
        }
        const durable_record::RecordSnapshot index_snapshot{
            .identity = prefix.record.index.identity,
            .size = prefix.record.index.extent,
        };
        const durable_record::RecordSnapshot data_snapshot{
            .identity = prefix.record.data.identity,
            .size = prefix.record.data.extent,
        };
        auto typed = closed_merge_prepared_envelope_validator_v1(
            *context->attempt, *context->manifest, context->expected_directory_identity,
            prefix.record, *handoff_snapshot, index_snapshot, data_snapshot,
            context->creator_process_id);
        if (!typed) {
            context->diagnostic = std::move(typed.diagnostic);
            return false;
        }
        const auto& prepared = typed.witness->prepared;
        const auto& started = context->start_record->record;
        if (prepared.merge_started_digest != started.self_digest ||
            prepared.merged_lease != started.merged_lease ||
            prepared.ordered_inputs != started.ordered_inputs ||
            *context->attempt->merge_attempt_ordinal != started.merge_attempt_ordinal) {
            context->diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
            return false;
        }
        const auto dependency = closed_merge_prepared_dependency_validator_v1(
            *context->manifest, *context->worker_attempts, *context->private_leases,
            *context->merge_starts, prepared);
        if (!dependency) {
            context->diagnostic =
                diagnostic(dependency.error == DistributedSieveProtocolError::resource_exhausted
                               ? DistributedSieveWaveStoreStatus::resource_exhausted
                               : DistributedSieveWaveStoreStatus::namespace_conflict,
                           protocol_error());
            context->diagnostic.protocol_status = dependency;
            return false;
        }
        context->typed_prepared = std::move(*typed.witness);
        return true;
    } catch (const std::bad_alloc&) {
        context->diagnostic = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                         std::make_error_code(std::errc::not_enough_memory));
        return false;
    } catch (...) {
        context->diagnostic = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                         std::make_error_code(std::errc::io_error));
        return false;
    }
}

[[nodiscard]] bool private_lease_attempt_may_be_handoff_publication_prefix(
    int root_fd, const PrivateLeaseAttemptInventory& attempt, const WaveManifestV1& manifest,
    std::uint64_t creator_process_id,
    std::optional<WorkerHandoffPublicationPrefixCandidate>& candidate,
    DistributedSieveWaveStoreDiagnostic& failure) noexcept;

[[nodiscard]] MergePreparedPublicationPrefixCaptureResult
classify_merge_prepared_publication_prefix_v1(
    int root_fd, const std::filesystem::path& absolute_root, const WaveManifestV1& manifest,
    const NativeIdentityV1& expected_wave_root_identity, std::uint64_t creator_process_id,
    HeldPrivateLeaseBaseLockInventoryV1 held = {},
    std::span<const CleanupRecoveredWorkerPublicationV1> cleanup_recovered_workers = {},
    bool allow_worker_cleanup_records = false) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return MergePreparedPublicationPrefixCaptureResult{
            .witness = std::nullopt,
            .retained = std::nullopt,
            .diagnostic = std::move(failure),
        };
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }
    try {
        auto initial = inspect_namespace(root_fd);
        if (!initial) {
            return fail_with(std::move(initial.diagnostic));
        }
        if (!initial.inventory->cleanup_record_leaves.empty() && !allow_worker_cleanup_records) {
            return conflict();
        }
        auto base_locks = validate_private_lease_base_lock_inventory(root_fd, *initial.inventory,
                                                                     manifest, creator_process_id);
        if (!base_locks) {
            return fail_with(std::move(base_locks.diagnostic));
        }
        auto parsed = parse_private_lease_attempt_inventory(*initial.inventory, manifest,
                                                            *base_locks.identities);
        if (!parsed) {
            return fail_with(std::move(parsed.diagnostic));
        }

        std::vector<const CleanupRecoveredWorkerPublicationV1*> recovered_by_attempt(
            parsed.attempts->size(), nullptr);
        for (const auto& recovered : cleanup_recovered_workers) {
            if (recovered.manifest_slot >= manifest.chunks.size() ||
                !recovered.provisional_lease.worker_handoff.has_value() ||
                recovered.provisional_lease.worker_handoff != recovered.typed_handoff) {
                return conflict();
            }
            const auto& handoff = recovered.typed_handoff.handoff;
            bool matched = false;
            for (std::size_t index = 0; index < parsed.attempts->size(); ++index) {
                const auto& attempt = parsed.attempts->at(index);
                if (!attempt.worker_coordinate.has_value() ||
                    attempt.manifest_chunk_order != recovered.manifest_slot ||
                    attempt.worker_coordinate->chunk_id != handoff.chunk_id ||
                    attempt.worker_coordinate->attempt_ordinal != handoff.attempt_ordinal) {
                    continue;
                }
                if (matched || recovered_by_attempt[index] != nullptr ||
                    attempt.base_lock_identity != recovered.base_lock_identity) {
                    return conflict();
                }
                recovered_by_attempt[index] = std::addressof(recovered);
                matched = true;
            }
            if (!matched) {
                return conflict();
            }
        }

        std::optional<MergePreparedPublicationPrefixCandidate> candidate;
        for (std::size_t index = 0; index < parsed.attempts->size(); ++index) {
            const auto& attempt = (*parsed.attempts)[index];
            if (!attempt.merge_generation_names.has_value() ||
                !attempt.merge_attempt_ordinal.has_value()) {
                continue;
            }
            std::optional<private_lease::PrivateHandoffPublicationPrefixStateV1> state;
            if (attempt.rollback_handoff) {
                state = private_lease::PrivateHandoffPublicationPrefixStateV1::PendingRollback;
            } else if (attempt.final_directory) {
                auto directory = inspect_private_lease_directory_at(
                    root_fd, attempt.names.private_directory_leaf, creator_process_id);
                if (!directory) {
                    return fail_with(std::move(directory.diagnostic));
                }
                const auto& entries = *directory.inventory;
                if (!entries.worker_handoff && !entries.worker_handoff_pending) {
                    continue;
                }
                const bool possible_root_shape = !attempt.reserved_pending && attempt.owned &&
                                                 !attempt.owned_pending &&
                                                 !attempt.staging_directory_leaf.has_value();
                const bool exact_directory_shape = entries.owner && !entries.owner_pending &&
                                                   !entries.work_package_residue &&
                                                   entries.corpus_index && entries.corpus_data;
                if (!possible_root_shape || !exact_directory_shape) {
                    return conflict();
                }
                state = entries.worker_handoff && entries.worker_handoff_pending
                            ? private_lease::PrivateHandoffPublicationPrefixStateV1::IdenticalDual
                        : entries.worker_handoff
                            ? private_lease::PrivateHandoffPublicationPrefixStateV1::Canonical
                            : private_lease::PrivateHandoffPublicationPrefixStateV1::PendingOnly;
            }
            if (!state.has_value()) {
                continue;
            }
            if (candidate.has_value()) {
                return conflict();
            }
            auto start =
                load_exact_canonical_merge_started(root_fd, attempt, manifest, creator_process_id);
            if (!start) {
                return fail_with(std::move(start.diagnostic));
            }
            candidate = MergePreparedPublicationPrefixCandidate{
                .attempt_index = index,
                .expected_state = *state,
                .start_record = std::move(*start.witness),
            };
        }
        if (!candidate.has_value()) {
            return {
                .witness = std::nullopt,
                .retained = std::nullopt,
                .diagnostic = {},
            };
        }

        std::vector<std::optional<PrivateLeaseReservationWitness>> lease_slots(
            parsed.attempts->size());
        std::vector<WorkerHandoffPublicationPrefixCandidate> worker_candidates;
        std::vector<bool> held_matches(held.locks.size(), false);
        worker_candidates.reserve(parsed.attempts->size());
        for (std::size_t index = 0; index < parsed.attempts->size(); ++index) {
            const auto& attempt = (*parsed.attempts)[index];
            const DistributedSievePrivateLeaseBaseLockAt* matching_held = nullptr;
            for (std::size_t held_index = 0; held_index < held.locks.size(); ++held_index) {
                const auto* possible = held.locks[held_index];
                if (possible == nullptr ||
                    !possible->matches_exact_binding(attempt.names.base_lock_leaf,
                                                     attempt.base_lock_identity)) {
                    continue;
                }
                if (matching_held != nullptr || held_matches[held_index]) {
                    return conflict();
                }
                matching_held = possible;
                held_matches[held_index] = true;
            }
            if (index == candidate->attempt_index) {
                if (matching_held != nullptr) {
                    return conflict();
                }
                const auto& started = candidate->start_record.record;
                lease_slots[index] = PrivateLeaseReservationWitness{
                    .base_lock_leaf = attempt.names.base_lock_leaf,
                    .boundary =
                        DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable,
                    .lease_id = started.merged_lease.lease_id.limbs,
                    .directory_identity = started.merged_lease.directory,
                    .owner_marker_identity = started.merged_lease.owner_marker,
                };
                continue;
            }

            if (recovered_by_attempt[index] != nullptr) {
                if (matching_held != nullptr) {
                    return conflict();
                }
                lease_slots[index] = recovered_by_attempt[index]->provisional_lease;
                continue;
            }

            if (attempt.worker_attempt_names.has_value() && attempt.worker_coordinate.has_value()) {
                std::optional<WorkerHandoffPublicationPrefixCandidate> worker_candidate;
                DistributedSieveWaveStoreDiagnostic worker_candidate_failure;
                if (!private_lease_attempt_may_be_handoff_publication_prefix(
                        root_fd, attempt, manifest, creator_process_id, worker_candidate,
                        worker_candidate_failure)) {
                    return fail_with(std::move(worker_candidate_failure));
                }
                if (worker_candidate.has_value()) {
                    if (worker_candidate->expected_state !=
                        private_lease::PrivateHandoffPublicationPrefixStateV1::Canonical) {
                        return conflict();
                    }
                    worker_candidate->attempt_index = index;
                    if (matching_held == nullptr) {
                        worker_candidates.push_back(std::move(*worker_candidate));
                        continue;
                    }
                }
            }
            auto validated = validate_private_lease_attempt_inventory(
                root_fd, absolute_root, attempt, manifest, creator_process_id, matching_held);
            if (!validated) {
                return fail_with(std::move(validated.diagnostic));
            }
            lease_slots[index] = std::move(*validated.witness);
        }
        if (std::ranges::find(held_matches, false) != held_matches.end()) {
            return conflict();
        }

        std::sort(worker_candidates.begin(), worker_candidates.end(),
                  [&](const WorkerHandoffPublicationPrefixCandidate& left,
                      const WorkerHandoffPublicationPrefixCandidate& right) {
                      const auto& left_attempt = (*parsed.attempts)[left.attempt_index];
                      const auto& right_attempt = (*parsed.attempts)[right.attempt_index];
                      return std::pair{left_attempt.manifest_chunk_order,
                                       left_attempt.worker_coordinate->attempt_ordinal} <
                             std::pair{right_attempt.manifest_chunk_order,
                                       right_attempt.worker_coordinate->attempt_ordinal};
                  });

        RetainedWorkerHandoffPublicationPrefixStack retained_workers;
        retained_workers.entries.reserve(worker_candidates.size());
        for (const auto& worker_candidate : worker_candidates) {
            const auto& attempt = (*parsed.attempts)[worker_candidate.attempt_index];
            const auto expected_directory_identity =
                worker_candidate.attempt_record.record.lease.directory;
            const auto base_path = absolute_root / attempt.names.private_directory_leaf / "corpus";
            const auto paths = gnfs::relation::OOCCleanupTransaction::paths_for(base_path);
            auto admission = private_lease::acquire_private_handoff_publication_resume_v1(
                paths, relation_identity(expected_directory_identity));
            if (!admission.acquired() || !admission.observed.has_value()) {
                return fail_with(worker_handoff_inspection_failure(admission.result));
            }
            const auto* observed = admission.observed->witness();
            if (observed == nullptr || !observed->canonical_terminal() ||
                observed->state != worker_candidate.expected_state ||
                !worker_handoff_publication_prefix_marker_chain_matches(
                    *observed, expected_wave_root_identity, attempt, expected_directory_identity)) {
                return conflict();
            }
            const auto observed_prefix = *observed;
            if (const auto exact = revalidate_exact_canonical_worker_attempt(
                    root_fd, *attempt.worker_attempt_names, worker_candidate.attempt_record,
                    creator_process_id);
                exact.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(exact);
            }
            WorkerHandoffTypedValidationContext typed_context{
                .attempt = &attempt,
                .manifest = &manifest,
                .attempt_record = &worker_candidate.attempt_record,
                .root_fd = root_fd,
                .expected_directory_identity = expected_directory_identity,
                .creator_process_id = creator_process_id,
            };
            auto validation = private_lease::validate_private_handoff_publication_resume_v1(
                std::move(*admission.observed),
                WorkerHandoffTypedValidatorAuthorityV1::bind(validate_worker_handoff_prefix_type,
                                                             &typed_context));
            if (!validation.validated() || !validation.permit.has_value() ||
                !typed_context.typed_handoff.has_value()) {
                if (typed_context.diagnostic.status != DistributedSieveWaveStoreStatus::ready) {
                    return fail_with(std::move(typed_context.diagnostic));
                }
                return fail_with(worker_handoff_inspection_failure(validation.result));
            }
            const auto typed_handoff = *typed_context.typed_handoff;
            PrivateLeaseReservationWitness provisional{
                .base_lock_leaf = attempt.names.base_lock_leaf,
                .boundary = DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable,
                .lease_id = observed_prefix.record.lease_id,
                .directory_identity = expected_directory_identity,
                .owner_marker_identity =
                    protocol_identity(observed_prefix.record.owner_marker_identity),
                .owned_marker_identity =
                    protocol_identity(observed_prefix.record.owned_marker_identity),
                .worker_handoff = typed_handoff,
            };
            lease_slots[worker_candidate.attempt_index] = provisional;
            retained_workers.entries.push_back(RetainedWorkerHandoffPublicationPrefix{
                .attempt_index = worker_candidate.attempt_index,
                .manifest_slot = attempt.manifest_chunk_order,
                .names = *attempt.worker_attempt_names,
                .coordinate = *attempt.worker_coordinate,
                .witness = observed_prefix,
                .typed_handoff = typed_handoff,
                .provisional_lease = std::move(provisional),
                .attempt_record = worker_candidate.attempt_record,
                .permit = std::move(*validation.permit),
            });
        }

        std::vector<PrivateLeaseReservationWitness> private_leases;
        private_leases.reserve(lease_slots.size());
        for (auto& lease : lease_slots) {
            if (!lease.has_value()) {
                return conflict();
            }
            private_leases.push_back(std::move(*lease));
        }

        auto worker_attempts = validate_worker_attempt_record_inventory(
            root_fd, *initial.inventory, manifest, *base_locks.identities, private_leases,
            creator_process_id);
        if (!worker_attempts) {
            return fail_with(std::move(worker_attempts.diagnostic));
        }
        auto merge_commit = validate_wave_merge_commit_record_inventory(
            root_fd, *initial.inventory, manifest, creator_process_id);
        if (!merge_commit) {
            return fail_with(std::move(merge_commit.diagnostic));
        }
        auto merge_starts = validate_merge_started_record_inventory(
            root_fd, *initial.inventory, manifest, *base_locks.identities, private_leases,
            *worker_attempts.witnesses, creator_process_id);
        if (!merge_starts) {
            return fail_with(std::move(merge_starts.diagnostic));
        }
        auto terminal_failures = validate_chunk_terminal_failure_record_inventory(
            root_fd, *initial.inventory, manifest, private_leases, *worker_attempts.witnesses,
            creator_process_id);
        if (!terminal_failures) {
            return fail_with(std::move(terminal_failures.diagnostic));
        }
        if (!terminal_failures.witnesses->empty() || merge_starts.witnesses->empty() ||
            candidate->start_record.merge_attempt_ordinal + 1U != merge_starts.witnesses->size() ||
            merge_starts.witnesses->back() != candidate->start_record) {
            return conflict();
        }

        const auto& target_attempt = parsed.attempts->at(candidate->attempt_index);
        const auto expected_directory_identity =
            candidate->start_record.record.merged_lease.directory;
        const auto base_path =
            absolute_root / target_attempt.names.private_directory_leaf / "corpus";
        const auto paths = gnfs::relation::OOCCleanupTransaction::paths_for(base_path);
        auto admission = private_lease::acquire_private_handoff_publication_resume_v1(
            paths, relation_identity(expected_directory_identity));
        if (!admission.acquired() || !admission.observed.has_value()) {
            return fail_with(worker_handoff_inspection_failure(admission.result));
        }
        const auto* observed = admission.observed->witness();
        if (observed == nullptr || observed->state != candidate->expected_state ||
            !worker_handoff_publication_prefix_marker_chain_matches(
                *observed, expected_wave_root_identity, target_attempt,
                expected_directory_identity)) {
            return conflict();
        }
        const auto observed_prefix = *observed;
        auto& target_lease = private_leases[candidate->attempt_index];
        target_lease.lease_id = observed_prefix.record.lease_id;
        target_lease.reserved_marker_identity =
            observed_prefix.reserved.has_value()
                ? std::optional<NativeIdentityV1>(
                      protocol_identity_words(observed_prefix.reserved->identity))
                : std::nullopt;
        target_lease.directory_identity = expected_directory_identity;
        target_lease.owner_marker_identity =
            protocol_identity(observed_prefix.record.owner_marker_identity);
        target_lease.owned_marker_identity =
            protocol_identity(observed_prefix.record.owned_marker_identity);

        auto marker_bound_starts = validate_merge_started_record_inventory(
            root_fd, *initial.inventory, manifest, *base_locks.identities, private_leases,
            *worker_attempts.witnesses, creator_process_id);
        if (!marker_bound_starts) {
            return fail_with(std::move(marker_bound_starts.diagnostic));
        }
        if (*marker_bound_starts.witnesses != *merge_starts.witnesses) {
            return conflict();
        }

        MergePreparedTypedValidationContext typed_context{
            .attempt = &target_attempt,
            .manifest = &manifest,
            .start_record = &marker_bound_starts.witnesses->back(),
            .worker_attempts = &*worker_attempts.witnesses,
            .private_leases = &private_leases,
            .merge_starts = &*marker_bound_starts.witnesses,
            .root_fd = root_fd,
            .expected_directory_identity = expected_directory_identity,
            .creator_process_id = creator_process_id,
        };
        auto validation = private_lease::validate_private_handoff_publication_resume_v1(
            std::move(*admission.observed),
            WorkerHandoffTypedValidatorAuthorityV1::bind(validate_merge_prepared_prefix_type,
                                                         &typed_context));
        if (!validation.validated() || !validation.permit.has_value() ||
            !typed_context.typed_prepared.has_value()) {
            if (typed_context.diagnostic.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(std::move(typed_context.diagnostic));
            }
            return fail_with(worker_handoff_inspection_failure(validation.result));
        }
        target_lease.merge_prepared = *typed_context.typed_prepared;

        auto prepared_bound_starts = validate_merge_started_record_inventory(
            root_fd, *initial.inventory, manifest, *base_locks.identities, private_leases,
            *worker_attempts.witnesses, creator_process_id,
            merge_commit.witnesses->empty()
                ? nullptr
                : std::addressof(merge_commit.witnesses->front().record));
        if (!prepared_bound_starts) {
            return fail_with(std::move(prepared_bound_starts.diagnostic));
        }
        if (*prepared_bound_starts.witnesses != *marker_bound_starts.witnesses) {
            return conflict();
        }
        const auto revalidated =
            private_lease::revalidate_private_handoff_publication_resume_v1(*validation.permit);
        if (!revalidated.revalidated() || !revalidated.witness.has_value()) {
            return fail_with(worker_handoff_inspection_failure(revalidated.result));
        }
        if (*revalidated.witness != observed_prefix) {
            return conflict();
        }
        for (const auto& worker : retained_workers.entries) {
            if (worker.consumed || !worker.permit.valid() || !worker.permit.held() ||
                worker.attempt_index >= private_leases.size() ||
                private_leases[worker.attempt_index] != worker.provisional_lease) {
                return conflict();
            }
            const auto exact_worker =
                private_lease::revalidate_private_handoff_publication_resume_v1(worker.permit);
            if (!exact_worker.revalidated() || !exact_worker.witness.has_value()) {
                return fail_with(worker_handoff_inspection_failure(exact_worker.result));
            }
            if (*exact_worker.witness != worker.witness) {
                return conflict();
            }
            if (const auto attempt_record = revalidate_exact_canonical_worker_attempt(
                    root_fd, worker.names, worker.attempt_record, creator_process_id);
                attempt_record.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(attempt_record);
            }
        }
        if (const auto exact = revalidate_exact_canonical_merge_started(
                root_fd, *target_attempt.merge_generation_names,
                marker_bound_starts.witnesses->back(), creator_process_id);
            exact.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(exact);
        }
        const auto final_inventory = inspect_namespace(root_fd);
        if (!final_inventory) {
            return fail_with(std::move(final_inventory.diagnostic));
        }
        if (*final_inventory.inventory != *initial.inventory) {
            return conflict();
        }
        if (!process_matches(creator_process_id)) {
            return fail_with(process_mismatch());
        }
        auto witnessed_inventory = *initial.inventory;
        witnessed_inventory.worker_attempt_records = *worker_attempts.witnesses;
        witnessed_inventory.merge_started_records = *prepared_bound_starts.witnesses;
        witnessed_inventory.chunk_terminal_failure_records = *terminal_failures.witnesses;
        witnessed_inventory.wave_merge_commit_records = *merge_commit.witnesses;
        const auto typed_prepared = *typed_context.typed_prepared;
        const auto start_record = prepared_bound_starts.witnesses->back();
        const auto names = *target_attempt.merge_generation_names;
        const auto provisional_lease = target_lease;
        MergePreparedPublicationAggregateWitness aggregate{
            .inventory = std::move(witnessed_inventory),
            .base_lock_identities = *base_locks.identities,
            .private_leases = private_leases,
            .worker_attempt_records = *worker_attempts.witnesses,
            .merge_started_records = *prepared_bound_starts.witnesses,
            .chunk_terminal_failure_records = *terminal_failures.witnesses,
            .wave_merge_commit_records = *merge_commit.witnesses,
            .attempt_index = candidate->attempt_index,
            .names = names,
            .prefix = observed_prefix,
            .typed_prepared = typed_prepared,
            .start_record = start_record,
            .retained_workers = std::move(retained_workers),
        };
        RetainedMergePreparedPublicationPrefix retained{
            .attempt_index = candidate->attempt_index,
            .names = std::move(names),
            .witness = observed_prefix,
            .typed_prepared = std::move(typed_prepared),
            .provisional_lease = std::move(provisional_lease),
            .start_record = std::move(start_record),
            .permit = std::move(*validation.permit),
        };
        return {
            .witness = std::move(aggregate),
            .retained = std::move(retained),
            .diagnostic = {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

[[nodiscard]] bool private_lease_attempt_may_be_handoff_publication_prefix(
    int root_fd, const PrivateLeaseAttemptInventory& attempt, const WaveManifestV1& manifest,
    std::uint64_t creator_process_id,
    std::optional<WorkerHandoffPublicationPrefixCandidate>& candidate,
    DistributedSieveWaveStoreDiagnostic& failure) noexcept {
    candidate.reset();
    if (attempt.rollback_handoff) {
        auto record =
            load_exact_canonical_worker_attempt(root_fd, attempt, manifest, creator_process_id);
        if (!record) {
            failure = std::move(record.diagnostic);
            return false;
        }
        candidate = WorkerHandoffPublicationPrefixCandidate{
            .expected_state =
                private_lease::PrivateHandoffPublicationPrefixStateV1::PendingRollback,
            .attempt_record = std::move(*record.witness),
        };
        return true;
    }
    const bool possible_root_shape = !attempt.reserved_pending && attempt.owned &&
                                     !attempt.owned_pending && attempt.final_directory &&
                                     !attempt.staging_directory_leaf.has_value();
    if (!possible_root_shape) {
        return true;
    }
    auto directory = inspect_private_lease_directory_at(
        root_fd, attempt.names.private_directory_leaf, creator_process_id);
    if (!directory) {
        failure = std::move(directory.diagnostic);
        return false;
    }
    const auto& entries = *directory.inventory;
    if (!entries.worker_handoff && !entries.worker_handoff_pending) {
        return true;
    }
    const bool exact_handoff_shape = entries.owner && !entries.owner_pending &&
                                     !entries.work_package_residue && entries.corpus_index &&
                                     entries.corpus_data;
    if (!exact_handoff_shape) {
        return true;
    }
    auto record =
        load_exact_canonical_worker_attempt(root_fd, attempt, manifest, creator_process_id);
    if (!record) {
        failure = std::move(record.diagnostic);
        return false;
    }
    const auto state = entries.worker_handoff && entries.worker_handoff_pending
                           ? private_lease::PrivateHandoffPublicationPrefixStateV1::IdenticalDual
                       : entries.worker_handoff
                           ? private_lease::PrivateHandoffPublicationPrefixStateV1::Canonical
                           : private_lease::PrivateHandoffPublicationPrefixStateV1::PendingOnly;
    candidate = WorkerHandoffPublicationPrefixCandidate{
        .expected_state = state,
        .attempt_record = std::move(*record.witness),
    };
    return true;
}

[[nodiscard]] RecoverableWorkerHandoffInventoryResult capture_recoverable_worker_handoff_inventory(
    int root_fd, const std::filesystem::path& absolute_root, const NamespaceInventory& inventory,
    const WaveManifestV1& manifest, const NativeIdentityV1& expected_wave_root_identity,
    std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return RecoverableWorkerHandoffInventoryResult{
            .witness = std::nullopt,
            .retained = {},
            .diagnostic = std::move(failure),
        };
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    if (!process_matches(creator_process_id)) {
        return fail_with(process_mismatch());
    }
    if (!inventory.lock || !inventory.manifest || inventory.pending ||
        !inventory.chunk_terminal_failure_record_leaves.empty() ||
        !inventory.merge_started_record_leaves.empty() ||
        !inventory.wave_merge_commit_record_leaves.empty() ||
        !inventory.cleanup_record_leaves.empty()) {
        return conflict();
    }

    auto base_locks = validate_private_lease_base_lock_inventory(root_fd, inventory, manifest,
                                                                 creator_process_id);
    if (!base_locks) {
        return fail_with(std::move(base_locks.diagnostic));
    }
    auto parsed =
        parse_private_lease_attempt_inventory(inventory, manifest, *base_locks.identities);
    if (!parsed) {
        return fail_with(std::move(parsed.diagnostic));
    }

    std::vector<std::optional<PrivateLeaseReservationWitness>> lease_slots;
    std::vector<WorkerHandoffPublicationPrefixCandidate> candidates;
    try {
        lease_slots.resize(parsed.attempts->size());
        candidates.reserve(parsed.attempts->size());
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }

    for (std::size_t index = 0; index < parsed.attempts->size(); ++index) {
        const auto& attempt = (*parsed.attempts)[index];
        if (!attempt.worker_attempt_names.has_value() || !attempt.worker_coordinate.has_value()) {
            auto validated = validate_private_lease_attempt_inventory(
                root_fd, absolute_root, attempt, manifest, creator_process_id);
            if (!validated) {
                return fail_with(std::move(validated.diagnostic));
            }
            lease_slots[index] = std::move(*validated.witness);
            continue;
        }
        std::optional<WorkerHandoffPublicationPrefixCandidate> candidate;
        DistributedSieveWaveStoreDiagnostic candidate_failure;
        if (!private_lease_attempt_may_be_handoff_publication_prefix(
                root_fd, attempt, manifest, creator_process_id, candidate, candidate_failure)) {
            return fail_with(std::move(candidate_failure));
        }
        if (candidate.has_value()) {
            candidate->attempt_index = index;
            try {
                candidates.push_back(std::move(*candidate));
            } catch (const std::bad_alloc&) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                            std::make_error_code(std::errc::not_enough_memory)));
            } catch (...) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            std::make_error_code(std::errc::io_error)));
            }
            continue;
        }
        auto validated = validate_private_lease_attempt_inventory(root_fd, absolute_root, attempt,
                                                                  manifest, creator_process_id);
        if (!validated) {
            return fail_with(std::move(validated.diagnostic));
        }
        lease_slots[index] = std::move(*validated.witness);
    }

    std::sort(candidates.begin(), candidates.end(),
              [&](const WorkerHandoffPublicationPrefixCandidate& left,
                  const WorkerHandoffPublicationPrefixCandidate& right) {
                  const auto& left_attempt = (*parsed.attempts)[left.attempt_index];
                  const auto& right_attempt = (*parsed.attempts)[right.attempt_index];
                  return std::pair{left_attempt.manifest_chunk_order,
                                   left_attempt.worker_coordinate->attempt_ordinal} <
                         std::pair{right_attempt.manifest_chunk_order,
                                   right_attempt.worker_coordinate->attempt_ordinal};
              });

    RetainedWorkerHandoffPublicationPrefixStack retained;
    std::vector<WorkerHandoffPublicationAggregateWitness> prefix_witnesses;
    try {
        retained.entries.reserve(candidates.size());
        prefix_witnesses.reserve(candidates.size());
        for (const auto& candidate : candidates) {
            const auto& attempt = (*parsed.attempts)[candidate.attempt_index];
            const auto expected_directory_identity =
                candidate.attempt_record.record.lease.directory;
            const auto base_path = absolute_root / attempt.names.private_directory_leaf / "corpus";
            const auto paths = gnfs::relation::OOCCleanupTransaction::paths_for(base_path);
            auto admission = private_lease::acquire_private_handoff_publication_resume_v1(
                paths, relation_identity(expected_directory_identity));
            if (!admission.acquired() || !admission.observed.has_value()) {
                return fail_with(worker_handoff_inspection_failure(admission.result));
            }
            const auto* prefix = admission.observed->witness();
            if (prefix == nullptr || prefix->state != candidate.expected_state ||
                !worker_handoff_publication_prefix_marker_chain_matches(
                    *prefix, expected_wave_root_identity, attempt, expected_directory_identity)) {
                return conflict();
            }
            const auto observed_prefix = *prefix;
            if (const auto exact = revalidate_exact_canonical_worker_attempt(
                    root_fd, *attempt.worker_attempt_names, candidate.attempt_record,
                    creator_process_id);
                exact.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(exact);
            }
            WorkerHandoffTypedValidationContext typed_context{
                .attempt = &attempt,
                .manifest = &manifest,
                .attempt_record = &candidate.attempt_record,
                .root_fd = root_fd,
                .expected_directory_identity = expected_directory_identity,
                .creator_process_id = creator_process_id,
            };
            auto validation = private_lease::validate_private_handoff_publication_resume_v1(
                std::move(*admission.observed),
                WorkerHandoffTypedValidatorAuthorityV1::bind(validate_worker_handoff_prefix_type,
                                                             &typed_context));
            if (!validation.validated() || !validation.permit.has_value() ||
                !typed_context.typed_handoff.has_value()) {
                if (typed_context.diagnostic.status != DistributedSieveWaveStoreStatus::ready) {
                    return fail_with(std::move(typed_context.diagnostic));
                }
                return fail_with(worker_handoff_inspection_failure(validation.result));
            }
            const auto typed_handoff = *typed_context.typed_handoff;

            PrivateLeaseReservationWitness provisional{
                .base_lock_leaf = attempt.names.base_lock_leaf,
                .boundary = DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable,
                .lease_id = observed_prefix.record.lease_id,
                .reserved_marker_identity =
                    observed_prefix.reserved.has_value()
                        ? std::optional<NativeIdentityV1>(
                              protocol_identity_words(observed_prefix.reserved->identity))
                        : std::nullopt,
                .directory_identity = expected_directory_identity,
                .owner_marker_identity =
                    protocol_identity(observed_prefix.record.owner_marker_identity),
                .owned_marker_identity =
                    protocol_identity(observed_prefix.record.owned_marker_identity),
                .worker_handoff = typed_handoff,
            };
            lease_slots[candidate.attempt_index] = provisional;
            prefix_witnesses.push_back(WorkerHandoffPublicationAggregateWitness{
                .attempt_index = candidate.attempt_index,
                .names = *attempt.worker_attempt_names,
                .coordinate = *attempt.worker_coordinate,
                .prefix = observed_prefix,
                .typed_handoff = typed_handoff,
                .attempt_record = candidate.attempt_record,
            });
            retained.entries.push_back(RetainedWorkerHandoffPublicationPrefix{
                .attempt_index = candidate.attempt_index,
                .names = *attempt.worker_attempt_names,
                .coordinate = *attempt.worker_coordinate,
                .witness = observed_prefix,
                .typed_handoff = typed_handoff,
                .provisional_lease = std::move(provisional),
                .attempt_record = candidate.attempt_record,
                .permit = std::move(*validation.permit),
            });
        }
    } catch (const private_lease::Failure& failure) {
        return fail_with(worker_handoff_inspection_failure(gnfs::relation::OOCCleanupResult{
            .status = failure.status,
            .stage = failure.stage,
            .native_error = failure.error,
        }));
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }

    std::vector<PrivateLeaseReservationWitness> private_leases;
    try {
        private_leases.reserve(lease_slots.size());
        for (auto& lease : lease_slots) {
            if (!lease.has_value()) {
                return conflict();
            }
            private_leases.push_back(std::move(*lease));
        }
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }

    auto worker_attempts = validate_worker_attempt_record_inventory(
        root_fd, inventory, manifest, *base_locks.identities, private_leases, creator_process_id);
    if (!worker_attempts) {
        return fail_with(std::move(worker_attempts.diagnostic));
    }
    for (auto& prefix : retained.entries) {
        const auto record =
            std::find_if(worker_attempts.witnesses->begin(), worker_attempts.witnesses->end(),
                         [&](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate) {
                             return candidate.chunk_id == prefix.coordinate.chunk_id &&
                                    candidate.attempt_ordinal == prefix.coordinate.attempt_ordinal;
                         });
        if (record == worker_attempts.witnesses->end() || !record->canonical_snapshot.has_value() ||
            record->pending_snapshot.has_value() || *record != prefix.attempt_record ||
            prefix.typed_handoff.handoff.attempt_started_digest != record->record.self_digest ||
            prefix.typed_handoff.handoff.lease != record->record.lease ||
            prefix.typed_handoff.handoff.chunk_id != prefix.coordinate.chunk_id ||
            prefix.typed_handoff.handoff.attempt_ordinal != prefix.coordinate.attempt_ordinal) {
            return conflict();
        }
        for (const auto& lease : private_leases) {
            const auto coordinate = manifest_worker_attempt_coordinate_from_private_lease_base_lock(
                manifest, lease.base_lock_leaf);
            if (!coordinate.has_value()) {
                if (manifest_merge_generation_from_private_lease_base_lock(manifest,
                                                                           lease.base_lock_leaf)
                        .has_value()) {
                    continue;
                }
                return conflict();
            }
            if (coordinate->chunk_id == prefix.coordinate.chunk_id &&
                coordinate->attempt_ordinal > prefix.coordinate.attempt_ordinal) {
                return conflict();
            }
        }
    }

    auto witnessed_inventory = inventory;
    witnessed_inventory.worker_attempt_records = *worker_attempts.witnesses;
    RecoverableWorkerHandoffInventoryWitness witness{
        .inventory = std::move(witnessed_inventory),
        .base_lock_identities = *base_locks.identities,
        .private_leases = private_leases,
        .worker_attempt_records = *worker_attempts.witnesses,
        .prefixes = std::move(prefix_witnesses),
    };
    return {
        .witness = std::move(witness),
        .retained = std::move(retained),
        .diagnostic = {},
    };
}

struct ManifestBoundInventoryValidationResult final {
    std::optional<std::vector<NativeIdentityV1>> base_lock_identities;
    std::optional<std::vector<PrivateLeaseReservationWitness>> private_lease_witnesses;
    std::optional<std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness>>
        worker_attempt_records;
    std::optional<std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1>>
        merge_started_records;
    std::optional<std::vector<DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1>>
        chunk_terminal_failure_records;
    std::optional<std::vector<DistributedSieveWaveMergeCommitRecordInventoryWitnessV1>>
        wave_merge_commit_records;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return base_lock_identities.has_value() && private_lease_witnesses.has_value() &&
               worker_attempt_records.has_value() && merge_started_records.has_value() &&
               chunk_terminal_failure_records.has_value() &&
               wave_merge_commit_records.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ManifestBoundInventoryValidationResult validate_manifest_bound_inventory(
    int root_fd, const std::filesystem::path& absolute_root, const NamespaceInventory& inventory,
    const WaveManifestV1& manifest, std::uint64_t creator_process_id,
    HeldPrivateLeaseBaseLockInventoryV1 held = {}, bool require_canonical_manifest = true,
    HeldCanonicalPublicationInventoryV1 held_canonical = {}) noexcept {
    const bool manifest_shape_valid = require_canonical_manifest
                                          ? inventory.manifest && !inventory.pending
                                          : inventory.manifest || inventory.pending;
    if (!inventory.lock || !manifest_shape_valid) {
        return {.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         protocol_error())};
    }
    if (!inventory.cleanup_record_leaves.empty()) {
        return {.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         protocol_error())};
    }
    auto base_locks = validate_private_lease_base_lock_inventory(root_fd, inventory, manifest,
                                                                 creator_process_id);
    if (!base_locks) {
        return {.diagnostic = std::move(base_locks.diagnostic)};
    }
    auto private_leases = validate_private_lease_protocol_inventory(
        root_fd, absolute_root, inventory, manifest, *base_locks.identities, creator_process_id,
        held, held_canonical);
    if (!private_leases) {
        return {.diagnostic = std::move(private_leases.diagnostic)};
    }
    auto worker_attempts = validate_worker_attempt_record_inventory(
        root_fd, inventory, manifest, *base_locks.identities, *private_leases.witnesses,
        creator_process_id);
    if (!worker_attempts) {
        return {.diagnostic = std::move(worker_attempts.diagnostic)};
    }
    auto merge_commit = validate_wave_merge_commit_record_inventory(root_fd, inventory, manifest,
                                                                    creator_process_id);
    if (!merge_commit) {
        return {.diagnostic = std::move(merge_commit.diagnostic)};
    }
    const WaveMergeCommitV1* commit = merge_commit.witnesses->empty()
                                          ? nullptr
                                          : std::addressof(merge_commit.witnesses->front().record);
    auto merge_starts = validate_merge_started_record_inventory(
        root_fd, inventory, manifest, *base_locks.identities, *private_leases.witnesses,
        *worker_attempts.witnesses, creator_process_id, commit);
    if (!merge_starts) {
        return {.diagnostic = std::move(merge_starts.diagnostic)};
    }
    auto terminal_failures = validate_chunk_terminal_failure_record_inventory(
        root_fd, inventory, manifest, *private_leases.witnesses, *worker_attempts.witnesses,
        creator_process_id);
    if (!terminal_failures) {
        return {.diagnostic = std::move(terminal_failures.diagnostic)};
    }
    return {
        .base_lock_identities = std::move(base_locks.identities),
        .private_lease_witnesses = std::move(private_leases.witnesses),
        .worker_attempt_records = std::move(worker_attempts.witnesses),
        .merge_started_records = std::move(merge_starts.witnesses),
        .chunk_terminal_failure_records = std::move(terminal_failures.witnesses),
        .wave_merge_commit_records = std::move(merge_commit.witnesses),
        .diagnostic = {},
    };
}

struct ManifestBoundInventoryWitnessResult final {
    std::optional<NamespaceInventory> inventory;
    std::optional<std::vector<NativeIdentityV1>> base_lock_identities;
    std::optional<std::vector<PrivateLeaseReservationWitness>> private_lease_witnesses;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return inventory.has_value() && base_lock_identities.has_value() &&
               private_lease_witnesses.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ManifestBoundInventoryWitnessResult capture_manifest_bound_inventory_witness(
    int root_fd, const WaveManifestV1& manifest, const std::filesystem::path& absolute_root,
    std::uint64_t creator_process_id, HeldPrivateLeaseBaseLockInventoryV1 held = {}) noexcept {
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt, std::nullopt, process_mismatch()};
    }
    auto inventory = inspect_namespace(root_fd);
    if (!inventory) {
        return {std::nullopt, std::nullopt, std::nullopt, std::move(inventory.diagnostic)};
    }
    auto validated = validate_manifest_bound_inventory(root_fd, absolute_root, *inventory.inventory,
                                                       manifest, creator_process_id, held);
    if (!validated) {
        return {std::nullopt, std::nullopt, std::nullopt, std::move(validated.diagnostic)};
    }
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt, std::nullopt, process_mismatch()};
    }
    inventory.inventory->worker_attempt_records = std::move(*validated.worker_attempt_records);
    inventory.inventory->merge_started_records = std::move(*validated.merge_started_records);
    inventory.inventory->chunk_terminal_failure_records =
        std::move(*validated.chunk_terminal_failure_records);
    inventory.inventory->wave_merge_commit_records =
        std::move(*validated.wave_merge_commit_records);
    return {
        std::move(inventory.inventory),
        std::move(validated.base_lock_identities),
        std::move(validated.private_lease_witnesses),
        {},
    };
}

[[nodiscard]] bool exact_worker_attempt_terminal_transition(
    const ManifestBoundInventoryWitnessResult& before,
    const ManifestBoundInventoryWitnessResult& after, const WaveManifestV1& manifest,
    const DistributedSieveWorkerAttemptNamesV1& names, std::uint32_t chunk_id,
    std::uint32_t attempt_ordinal, std::size_t target_index) {
    if (!before || !after ||
        before.inventory->private_lease_base_lock_leaves !=
            after.inventory->private_lease_base_lock_leaves ||
        *before.base_lock_identities != *after.base_lock_identities ||
        before.inventory->worker_attempt_records != after.inventory->worker_attempt_records ||
        before.private_lease_witnesses->size() != after.private_lease_witnesses->size() ||
        target_index >= before.private_lease_witnesses->size()) {
        return false;
    }

    for (std::size_t index = 0; index < before.private_lease_witnesses->size(); ++index) {
        if (index != target_index &&
            before.private_lease_witnesses->at(index) != after.private_lease_witnesses->at(index)) {
            return false;
        }
    }

    const auto& initial_lease = before.private_lease_witnesses->at(target_index);
    const auto& terminal_lease = after.private_lease_witnesses->at(target_index);
    if (initial_lease.base_lock_leaf != names.base_lock_leaf ||
        terminal_lease.base_lock_leaf != names.base_lock_leaf ||
        initial_lease.worker_handoff.has_value() || initial_lease.merge_prepared.has_value() ||
        initial_lease.merge_raw_writer_recovery.has_value() ||
        !terminal_lease.worker_handoff.has_value() || terminal_lease.merge_prepared.has_value() ||
        terminal_lease.merge_raw_writer_recovery.has_value() ||
        terminal_lease.boundary !=
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
        return false;
    }

    const auto record = std::lower_bound(
        before.inventory->worker_attempt_records.begin(),
        before.inventory->worker_attempt_records.end(), std::pair{chunk_id, attempt_ordinal},
        [](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate,
           const std::pair<std::uint32_t, std::uint32_t>& coordinate) {
            return std::pair{candidate.chunk_id, candidate.attempt_ordinal} < coordinate;
        });
    if (record == before.inventory->worker_attempt_records.end() || record->chunk_id != chunk_id ||
        record->attempt_ordinal != attempt_ordinal || !record->canonical_snapshot.has_value() ||
        record->pending_snapshot.has_value() ||
        !attempt_record_matches_live_lease_projection(record->record, initial_lease) ||
        !attempt_record_matches_live_lease_projection(record->record, terminal_lease)) {
        return false;
    }

    const auto& terminal = terminal_lease.worker_handoff->handoff;
    if (terminal.chunk_id != chunk_id || terminal.attempt_ordinal != attempt_ordinal ||
        terminal.attempt_started_digest != record->record.self_digest ||
        terminal.lease != record->record.lease) {
        return false;
    }

    auto initial_projection = *before.inventory;
    auto terminal_projection = *after.inventory;
    const auto erase_target_protocol = [&](NamespaceInventory& inventory) {
        std::erase_if(inventory.private_lease_protocol_leaves, [&](const std::string& leaf) {
            const auto parsed = parse_manifest_bound_private_lease_protocol_leaf(manifest, leaf);
            return parsed.has_value() && parsed->names.base_lock_leaf == names.base_lock_leaf;
        });
    };
    erase_target_protocol(initial_projection);
    erase_target_protocol(terminal_projection);
    return initial_projection == terminal_projection;
}

struct RootOpenResult final {
    UniqueFd parent;
    UniqueFd root;
    NativeIdentityV1 root_identity;
    bool created = false;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return static_cast<bool>(parent) && static_cast<bool>(root) &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] RootOpenResult open_root(const FrozenRoot& frozen, bool create,
                                       std::uint64_t creator_process_id) noexcept {
    RootOpenResult result;
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    auto parent = walk_parent_no_follow(frozen.parent_components, creator_process_id);
    if (!parent) {
        result.diagnostic = parent.diagnostic;
        return result;
    }
    result.parent = std::move(parent.parent);
    if (const auto parent_validated = validate_parent_binding(
            result.parent.get(), frozen.parent_components, creator_process_id);
        parent_validated.status != DistributedSieveWaveStoreStatus::ready) {
        result.diagnostic = parent_validated;
        return result;
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    if (create) {
        // Parent trust, ACL support, and stable no-follow reachability have all
        // been proven before this first namespace mutation.
        int created = -1;
        do {
            created = ::mkdirat(result.parent.get(), frozen.leaf.c_str(), 0700);
        } while (created != 0 && errno == EINTR);
        if (created == 0) {
            result.created = true;
        } else if (errno != EEXIST) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
            return result;
        }
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
    }

    const int root_fd = openat_retrying_eintr(result.parent.get(), frozen.leaf.c_str(),
                                              O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (root_fd < 0) {
        const int saved_errno = errno;
        result.diagnostic =
            diagnostic(saved_errno == ENOENT ? DistributedSieveWaveStoreStatus::root_missing
                                             : DistributedSieveWaveStoreStatus::root_invalid,
                       posix_error(saved_errno));
        return result;
    }
    result.root = UniqueFd(root_fd);
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    if (result.created) {
        int mode_result = -1;
        do {
            mode_result = ::fchmod(result.root.get(), 0700);
        } while (mode_result != 0 && errno == EINTR);
        if (mode_result != 0) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
            return result;
        }
    }

    if (const auto validated =
            validate_root_binding(result.parent.get(), frozen.parent_components, result.root.get(),
                                  frozen.leaf, creator_process_id);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        result.diagnostic = validated;
        return result;
    }
    struct stat metadata{};
    if (fstat_retrying_eintr(result.root.get(), metadata) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::root_invalid, posix_error(errno));
        return result;
    }
    result.root_identity = protocol_identity(metadata);

    if (create) {
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        std::error_code sync_error;
        if (!sync_descriptor(result.root.get(), sync_error) ||
            !sync_descriptor(result.parent.get(), sync_error)) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::durability_failed, sync_error);
            return result;
        }
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        if (const auto validated = validate_root_binding(
                result.parent.get(), frozen.parent_components, result.root.get(), frozen.leaf,
                creator_process_id, result.root_identity);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            result.diagnostic = validated;
            return result;
        }
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    return result;
}

struct LockOpenResult final {
    UniqueFd lock;
    NativeIdentityV1 lock_identity;
    bool created = false;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return static_cast<bool>(lock) &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] LockOpenResult open_lock(int root_fd, const NamespaceInventory& before, bool create,
                                       std::uint64_t creator_process_id) noexcept {
    LockOpenResult result;
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    if (auto acl = acl_rejection(root_fd, true, DistributedSieveWaveStoreStatus::root_invalid);
        acl.has_value()) {
        result.diagnostic = *acl;
        return result;
    }
    if (!before.lock && !create) {
        result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::lock_missing);
        return result;
    }
    if (!before.lock &&
        (before.manifest || before.pending || !before.private_lease_base_lock_leaves.empty() ||
         !before.private_lease_protocol_leaves.empty() ||
         !before.worker_attempt_record_leaves.empty())) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        return result;
    }

    constexpr int common_flags = O_RDWR | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC;
    int lock_fd = -1;
    if (create && !before.lock) {
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        lock_fd = openat_retrying_eintr(root_fd, LOCK_LEAF, common_flags | O_CREAT | O_EXCL, 0600);
        if (lock_fd >= 0) {
            result.created = true;
        }
    }
    if (lock_fd < 0 && ((!create && before.lock) || (create && before.lock) ||
                        (create && !before.lock && errno == EEXIST))) {
        lock_fd = openat_retrying_eintr(root_fd, LOCK_LEAF, common_flags);
    }
    if (lock_fd < 0) {
        const int saved_errno = errno;
        result.diagnostic =
            diagnostic(saved_errno == ENOENT ? DistributedSieveWaveStoreStatus::lock_missing
                                             : DistributedSieveWaveStoreStatus::lock_invalid,
                       posix_error(saved_errno));
        return result;
    }
    result.lock = UniqueFd(lock_fd);
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    if (result.created) {
        int mode_result = -1;
        do {
            mode_result = ::fchmod(result.lock.get(), 0600);
        } while (mode_result != 0 && errno == EINTR);
        if (mode_result != 0) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, posix_error(errno));
            return result;
        }
    }

    if (const auto validated =
            validate_lock_binding(root_fd, result.lock.get(), creator_process_id);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        result.diagnostic = validated;
        return result;
    }

    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    int locked = -1;
    do {
        locked = ::flock(result.lock.get(), LOCK_EX | LOCK_NB);
    } while (locked != 0 && errno == EINTR);
    if (locked != 0) {
        const int saved_errno = errno;
        if (saved_errno == EWOULDBLOCK || saved_errno == EAGAIN) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::lock_busy, posix_error(saved_errno));
        } else if (saved_errno == ENOTSUP || saved_errno == EOPNOTSUPP || saved_errno == ENOSYS) {
            result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                                           posix_error(saved_errno));
        } else {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, posix_error(saved_errno));
        }
        return result;
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    if (const auto validated =
            validate_lock_binding(root_fd, result.lock.get(), creator_process_id);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        result.diagnostic = validated;
        return result;
    }
    struct stat metadata{};
    if (fstat_retrying_eintr(result.lock.get(), metadata) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::lock_invalid, posix_error(errno));
        return result;
    }
    result.lock_identity = protocol_identity(metadata);

    if (create) {
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        std::error_code sync_error;
        if (!sync_descriptor(result.lock.get(), sync_error) ||
            !sync_descriptor(root_fd, sync_error)) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::durability_failed, sync_error);
            return result;
        }
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        if (const auto validated = validate_lock_binding(root_fd, result.lock.get(),
                                                         creator_process_id, result.lock_identity);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            result.diagnostic = validated;
            return result;
        }
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    return result;
}

struct ManifestReadResult final {
    std::optional<std::vector<std::byte>> bytes;
    std::optional<durable_record::RecordSnapshot> snapshot;
    bool missing = false;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return bytes.has_value() && snapshot.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ManifestReadResult read_manifest_leaf(int root_fd, const char* leaf,
                                                    std::uint64_t creator_process_id) noexcept {
    ManifestReadResult result;
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    const int manifest_fd =
        openat_retrying_eintr(root_fd, leaf, O_RDONLY | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (manifest_fd < 0) {
        const int saved_errno = errno;
        if (saved_errno == ENOENT) {
            result.missing = true;
            result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::manifest_missing,
                                           posix_error(saved_errno));
        } else {
            result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid,
                                           posix_error(saved_errno));
        }
        return result;
    }
    UniqueFd manifest(manifest_fd);
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    struct stat held_before{};
    if (fstat_retrying_eintr(manifest.get(), held_before) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        return result;
    }
    struct stat named_before{};
    if (fstatat_retrying_eintr(root_fd, leaf, named_before) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, posix_error(errno));
        return result;
    }
    if (!valid_manifest_metadata(held_before) || !valid_manifest_metadata(named_before) ||
        !stable_metadata(held_before, named_before)) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, protocol_error());
        return result;
    }
    if (auto acl =
            acl_rejection(manifest.get(), false, DistributedSieveWaveStoreStatus::manifest_invalid);
        acl.has_value()) {
        result.diagnostic = *acl;
        return result;
    }

    const std::size_t exact_size = static_cast<std::size_t>(held_before.st_size);
    std::vector<std::byte> bytes;
    try {
        bytes.resize(exact_size);
    } catch (const std::bad_alloc&) {
        result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                       std::make_error_code(std::errc::not_enough_memory));
        return result;
    } catch (...) {
        result.diagnostic = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                       std::make_error_code(std::errc::io_error));
        return result;
    }
    std::size_t offset = 0;
    while (offset < bytes.size()) {
        if (!process_matches(creator_process_id)) {
            result.diagnostic = process_mismatch();
            return result;
        }
        const std::size_t request = std::min(
            bytes.size() - offset, static_cast<std::size_t>(std::numeric_limits<ssize_t>::max()));
        const ssize_t count =
            ::pread(manifest.get(), bytes.data() + offset, request, static_cast<off_t>(offset));
        if (count < 0) {
            const int saved_errno = errno;
            if (saved_errno == EINTR) {
                continue;
            }
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
            return result;
        }
        if (count == 0) {
            result.diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, protocol_error());
            return result;
        }
        offset += static_cast<std::size_t>(count);
    }

    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }
    std::byte trailing{};
    ssize_t trailing_count = -1;
    do {
        trailing_count = ::pread(manifest.get(), &trailing, 1, static_cast<off_t>(bytes.size()));
    } while (trailing_count < 0 && errno == EINTR);
    if (trailing_count < 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        return result;
    }
    if (trailing_count != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, protocol_error());
        return result;
    }

    struct stat held_after{};
    if (fstat_retrying_eintr(manifest.get(), held_after) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        return result;
    }
    struct stat named_after{};
    if (fstatat_retrying_eintr(root_fd, leaf, named_after) != 0) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, posix_error(errno));
        return result;
    }
    if (!valid_manifest_metadata(held_after) || !valid_manifest_metadata(named_after) ||
        !stable_metadata(held_before, held_after) || !stable_metadata(held_after, named_after)) {
        result.diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, protocol_error());
        return result;
    }
    if (auto acl =
            acl_rejection(manifest.get(), false, DistributedSieveWaveStoreStatus::manifest_invalid);
        acl.has_value()) {
        result.diagnostic = *acl;
        return result;
    }
    if (!process_matches(creator_process_id)) {
        result.diagnostic = process_mismatch();
        return result;
    }

    result.snapshot = durable_record::RecordSnapshot{
        .identity = record_identity(held_after),
        .size = static_cast<std::uint64_t>(held_after.st_size),
    };
    result.bytes = std::move(bytes);
    return result;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
require_manifest_pending_missing(int root_fd, std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    struct stat named{};
    if (fstatat_retrying_eintr(root_fd, MANIFEST_PENDING_LEAF, named) == 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    const int saved_errno = errno;
    if (saved_errno != ENOENT) {
        return diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid,
                          posix_error(saved_errno));
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    return {};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_held_wave_store_manifest_authority(
    int parent_fd, const std::vector<std::string>& parent_components, int root_fd,
    const std::filesystem::path& root_leaf, const NativeIdentityV1& root_identity, int lock_fd,
    const NativeIdentityV1& lock_identity, std::span<const std::byte> expected_manifest_bytes,
    const durable_record::RecordSnapshot& expected_manifest_snapshot,
    std::uint64_t creator_process_id) noexcept {
    if (const auto root_validated = validate_root_binding(
            parent_fd, parent_components, root_fd, root_leaf, creator_process_id, root_identity);
        root_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return root_validated;
    }
    if (const auto lock_validated =
            validate_lock_binding(root_fd, lock_fd, creator_process_id, lock_identity);
        lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return lock_validated;
    }
    if (const auto pending = require_manifest_pending_missing(root_fd, creator_process_id);
        pending.status != DistributedSieveWaveStoreStatus::ready) {
        return pending;
    }
    auto canonical = read_manifest_leaf(root_fd, MANIFEST_LEAF, creator_process_id);
    if (!canonical) {
        return canonical.diagnostic;
    }
    if (canonical.bytes->size() != expected_manifest_bytes.size() ||
        !std::equal(canonical.bytes->begin(), canonical.bytes->end(),
                    expected_manifest_bytes.begin()) ||
        *canonical.snapshot != expected_manifest_snapshot) {
        return diagnostic(DistributedSieveWaveStoreStatus::manifest_conflict, protocol_error());
    }
    if (const auto root_validated = validate_root_binding(
            parent_fd, parent_components, root_fd, root_leaf, creator_process_id, root_identity);
        root_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return root_validated;
    }
    if (const auto lock_validated =
            validate_lock_binding(root_fd, lock_fd, creator_process_id, lock_identity);
        lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return lock_validated;
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    return {};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_exact_canonical_worker_attempt(
    int root_fd, const DistributedSieveWorkerAttemptNamesV1& names,
    const DistributedSieveWorkerAttemptRecordInventoryWitness& expected,
    std::uint64_t creator_process_id) noexcept {
    auto canonical = read_immutable_protocol_record_leaf(
        root_fd, names.canonical_record_leaf.c_str(), creator_process_id,
        DistributedSieveWaveStoreStatus::namespace_conflict,
        DistributedSieveWaveStoreStatus::namespace_conflict);
    if (!canonical) {
        return canonical.diagnostic;
    }
    struct stat pending{};
    if (fstatat_retrying_eintr(root_fd, names.pending_record_leaf.c_str(), pending) == 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    const int saved_errno = errno;
    if (saved_errno != ENOENT) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                          posix_error(saved_errno));
    }
    if (!expected.canonical_snapshot.has_value() || expected.pending_snapshot.has_value() ||
        expected.bytes != *canonical.bytes || *expected.canonical_snapshot != *canonical.snapshot) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return {};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_exact_canonical_merge_started(
    int root_fd, const DistributedSieveMergeGenerationNamesV1& names,
    const DistributedSieveMergeStartedRecordInventoryWitnessV1& expected,
    std::uint64_t creator_process_id) noexcept {
    auto canonical = read_immutable_protocol_record_leaf(
        root_fd, names.canonical_record_leaf.c_str(), creator_process_id,
        DistributedSieveWaveStoreStatus::namespace_conflict,
        DistributedSieveWaveStoreStatus::namespace_conflict);
    if (!canonical) {
        return canonical.diagnostic;
    }
    struct stat pending{};
    if (fstatat_retrying_eintr(root_fd, names.pending_record_leaf.c_str(), pending) == 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    const int saved_errno = errno;
    if (saved_errno != ENOENT) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                          posix_error(saved_errno));
    }
    if (!expected.canonical_snapshot.has_value() || expected.pending_snapshot.has_value() ||
        expected.bytes != *canonical.bytes || *expected.canonical_snapshot != *canonical.snapshot) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return {};
}

[[nodiscard]] std::string
private_lease_staging_directory_leaf(const DistributedSievePrivateLeaseNamesV1& names,
                                     const std::array<std::uint64_t, 2>& lease_id);

void set_worker_handoff_protocol_leaf_presence(NamespaceInventory& inventory,
                                               const std::string& leaf, bool present) {
    auto& leaves = inventory.private_lease_protocol_leaves;
    const auto position = std::lower_bound(leaves.begin(), leaves.end(), leaf);
    const bool found = position != leaves.end() && *position == leaf;
    if (present && !found) {
        leaves.insert(position, leaf);
    } else if (!present && found) {
        leaves.erase(position);
    }
}

[[nodiscard]] bool project_selected_worker_handoff_protocol_state(
    NamespaceInventory& inventory, const RetainedWorkerHandoffPublicationPrefix& selected,
    DistributedSieveWorkerHandoffResumeObservationPointV1 point) {
    using Point = DistributedSieveWorkerHandoffResumeObservationPointV1;
    if (point == Point::AfterExpectedPrefixValidated ||
        point == Point::AfterCanonicalConfirmedDurable) {
        return true;
    }

    const auto staging_leaf =
        private_lease_staging_directory_leaf(selected.names, selected.witness.record.lease_id);
    bool reserved = false;
    bool owned = false;
    bool final_directory = false;
    bool staging_directory = false;
    bool rollback_handoff = false;
    switch (point) {
    case Point::BeforePendingRollbackSourceDirectorySync:
    case Point::AfterPendingRollbackSourceDirectoryDurable:
    case Point::BeforePendingRollbackDestinationDirectorySync:
    case Point::AfterPendingRollbackDestinationDirectoryDurable:
        reserved = true;
        owned = true;
        final_directory = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackPreactiveDirectoryQuarantinedDurable:
    case Point::AfterPendingRollbackPreactiveDataRemovedDurable:
    case Point::AfterPendingRollbackPreactiveIndexRemovedDurable:
    case Point::AfterPendingRollbackOwnerRemovedDurable:
        reserved = true;
        owned = true;
        staging_directory = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackLeaseDirectoryRemovedDurable:
        reserved = true;
        owned = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackReservedRemovedDurable:
        owned = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackOwnedRemovedDurable:
    case Point::BeforePendingRollbackTombstoneRemovalValidated:
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackTombstoneRemovedDurable:
        break;
    case Point::AfterReservedRevokedDurable:
        owned = true;
        final_directory = true;
        break;
    case Point::AfterExpectedPrefixValidated:
    case Point::AfterCanonicalConfirmedDurable:
        return true;
    case Point::Count:
        return false;
    }

    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.reserved_leaf, reserved);
    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.owned_leaf, owned);
    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.private_directory_leaf,
                                              final_directory);
    set_worker_handoff_protocol_leaf_presence(inventory, staging_leaf, staging_directory);
    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.rollback_handoff_leaf,
                                              rollback_handoff);
    return true;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_worker_handoff_aggregate_projection(
    int root_fd, const std::filesystem::path& absolute_root, const WaveManifestV1& manifest,
    const RecoverableWorkerHandoffInventoryWitness& expected,
    const RetainedWorkerHandoffPublicationPrefixStack& retained, std::size_t current_attempt_index,
    std::uint64_t creator_process_id,
    std::optional<DistributedSieveWorkerHandoffResumeObservationPointV1> observation_point =
        std::nullopt) noexcept {
    const auto current = inspect_namespace(root_fd);
    if (!current) {
        return current.diagnostic;
    }
    auto expected_inventory = expected.inventory;
    auto current_inventory = *current.inventory;
    const auto selected = std::find_if(retained.entries.begin(), retained.entries.end(),
                                       [&](const RetainedWorkerHandoffPublicationPrefix& prefix) {
                                           return prefix.attempt_index == current_attempt_index;
                                       });
    if (selected == retained.entries.end() || selected->consumed || !selected->permit.held()) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    try {
        if (observation_point.has_value() &&
            !project_selected_worker_handoff_protocol_state(expected_inventory, *selected,
                                                            *observation_point)) {
            return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                              protocol_error());
        }
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }
    expected_inventory.worker_attempt_records.clear();
    current_inventory.worker_attempt_records.clear();
    if (current_inventory != expected_inventory) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }

    auto base_locks = validate_private_lease_base_lock_inventory(root_fd, current_inventory,
                                                                 manifest, creator_process_id);
    if (!base_locks) {
        return base_locks.diagnostic;
    }
    if (*base_locks.identities != expected.base_lock_identities) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }

    auto parsed =
        parse_private_lease_attempt_inventory(current_inventory, manifest, *base_locks.identities);
    if (!parsed) {
        return parsed.diagnostic;
    }
    if (parsed.attempts->size() != expected.private_leases.size()) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    for (std::size_t index = 0; index < parsed.attempts->size(); ++index) {
        if (index == current_attempt_index) {
            continue;
        }
        const auto held = std::find_if(retained.entries.begin(), retained.entries.end(),
                                       [&](const RetainedWorkerHandoffPublicationPrefix& prefix) {
                                           return prefix.attempt_index == index;
                                       });
        if (held != retained.entries.end()) {
            if (held->consumed || !held->permit.valid() || !held->permit.held()) {
                return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                  protocol_error());
            }
            const auto exact =
                private_lease::revalidate_private_handoff_publication_resume_v1(held->permit);
            if (!exact.revalidated() || !exact.witness.has_value()) {
                return worker_handoff_inspection_failure(exact.result);
            }
            if (*exact.witness != held->witness ||
                expected.private_leases[index] != held->provisional_lease) {
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            continue;
        }
        auto lease = validate_private_lease_attempt_inventory(
            root_fd, absolute_root, (*parsed.attempts)[index], manifest, creator_process_id);
        if (!lease) {
            return lease.diagnostic;
        }
        if (*lease.witness != expected.private_leases[index]) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
    }

    auto attempts = validate_worker_attempt_record_inventory(
        root_fd, current_inventory, manifest, *base_locks.identities, expected.private_leases,
        creator_process_id);
    if (!attempts) {
        return attempts.diagnostic;
    }
    if (*attempts.witnesses != expected.worker_attempt_records) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return {};
}

struct WorkerHandoffResumeBridgeContext final {
    DistributedSieveWorkerHandoffResumeTestHooksV1 user_hooks;
    int parent_fd = -1;
    const std::vector<std::string>* parent_components = nullptr;
    int root_fd = -1;
    const std::filesystem::path* root_leaf = nullptr;
    NativeIdentityV1 root_identity;
    int lock_fd = -1;
    NativeIdentityV1 lock_identity;
    const std::vector<std::byte>* manifest_bytes = nullptr;
    durable_record::RecordSnapshot manifest_snapshot;
    const std::filesystem::path* absolute_root = nullptr;
    const WaveManifestV1* manifest = nullptr;
    const RecoverableWorkerHandoffInventoryWitness* aggregate = nullptr;
    const RetainedWorkerHandoffPublicationPrefixStack* retained = nullptr;
    std::size_t current_attempt_index = 0;
    const DistributedSieveWorkerAttemptNamesV1* attempt_names = nullptr;
    const DistributedSieveWorkerAttemptRecordInventoryWitness* attempt_record = nullptr;
    std::uint64_t creator_process_id = 0;
    bool revalidation_failed = false;
    DistributedSieveWaveStoreDiagnostic revalidation_diagnostic;
};

[[nodiscard]] bool bridge_worker_handoff_resume_observation(
    private_lease::PrivateHandoffPublicationResumeObservationPointV1 point, void* opaque) noexcept {
    auto* context = static_cast<WorkerHandoffResumeBridgeContext*>(opaque);
    if (context == nullptr) {
        return true;
    }
    const auto wave_point =
        static_cast<DistributedSieveWorkerHandoffResumeObservationPointV1>(point);
    const bool user_requested_stop =
        context->user_hooks.stop_after != nullptr &&
        context->user_hooks.stop_after(wave_point, context->user_hooks.context);
    if (context->parent_components == nullptr || context->root_leaf == nullptr ||
        context->manifest_bytes == nullptr || context->absolute_root == nullptr ||
        context->manifest == nullptr || context->aggregate == nullptr ||
        context->retained == nullptr || context->attempt_names == nullptr ||
        context->attempt_record == nullptr) {
        context->revalidation_failed = true;
        context->revalidation_diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        return true;
    }
    auto revalidated = validate_held_wave_store_manifest_authority(
        context->parent_fd, *context->parent_components, context->root_fd, *context->root_leaf,
        context->root_identity, context->lock_fd, context->lock_identity, *context->manifest_bytes,
        context->manifest_snapshot, context->creator_process_id);
    if (revalidated.status == DistributedSieveWaveStoreStatus::ready) {
        revalidated = revalidate_worker_handoff_aggregate_projection(
            context->root_fd, *context->absolute_root, *context->manifest, *context->aggregate,
            *context->retained, context->current_attempt_index, context->creator_process_id,
            wave_point);
    }
    if (revalidated.status == DistributedSieveWaveStoreStatus::ready) {
        revalidated = revalidate_exact_canonical_worker_attempt(
            context->root_fd, *context->attempt_names, *context->attempt_record,
            context->creator_process_id);
    }
    if (revalidated.status == DistributedSieveWaveStoreStatus::ready) {
        return user_requested_stop;
    }
    context->revalidation_failed = true;
    context->revalidation_diagnostic = std::move(revalidated);
    return true;
}

[[nodiscard]] private_lease::PrivateHandoffPublicationResumeTestHooksV1
relation_worker_handoff_resume_hooks(WorkerHandoffResumeBridgeContext& context) noexcept {
    return {
        .stop_after = bridge_worker_handoff_resume_observation,
        .fail_before = nullptr,
        .context = &context,
    };
}

[[nodiscard]] bool merge_prepared_resume_point_allowed(
    const RetainedMergePreparedPublicationPrefix& retained,
    DistributedSieveMergePreparedResumeObservationPointV1 point) noexcept {
    using Point = DistributedSieveMergePreparedResumeObservationPointV1;
    using State = private_lease::PrivateHandoffPublicationPrefixStateV1;
    const auto state = retained.witness.state;
    const bool pending = state == State::PendingOnly || state == State::PendingRollback;
    const bool canonical = state == State::Canonical || state == State::IdenticalDual;
    switch (point) {
    case Point::AfterExpectedPrefixValidated:
        return true;
    case Point::BeforePendingRollbackSourceDirectorySync:
    case Point::AfterPendingRollbackSourceDirectoryDurable:
    case Point::BeforePendingRollbackDestinationDirectorySync:
    case Point::AfterPendingRollbackDestinationDirectoryDurable:
        return state == State::PendingOnly;
    case Point::AfterPendingRollbackPreactiveDirectoryQuarantinedDurable:
    case Point::AfterPendingRollbackPreactiveDataRemovedDurable:
    case Point::AfterPendingRollbackPreactiveIndexRemovedDurable:
    case Point::AfterPendingRollbackOwnerRemovedDurable:
    case Point::AfterPendingRollbackLeaseDirectoryRemovedDurable:
    case Point::AfterPendingRollbackReservedRemovedDurable:
    case Point::AfterPendingRollbackOwnedRemovedDurable:
    case Point::BeforePendingRollbackTombstoneRemovalValidated:
    case Point::AfterPendingRollbackTombstoneRemovedDurable:
        return pending;
    case Point::AfterCanonicalConfirmedDurable:
        return canonical &&
               (state == State::IdenticalDual || retained.witness.reserved.has_value());
    case Point::AfterReservedRevokedDurable:
        return canonical && retained.witness.reserved.has_value();
    case Point::Count:
        return false;
    }
    return false;
}

[[nodiscard]] bool project_selected_merge_prepared_protocol_state(
    NamespaceInventory& inventory, const RetainedMergePreparedPublicationPrefix& selected,
    DistributedSieveMergePreparedResumeObservationPointV1 point) {
    using Point = DistributedSieveMergePreparedResumeObservationPointV1;
    if (point == Point::AfterExpectedPrefixValidated ||
        point == Point::AfterCanonicalConfirmedDurable) {
        return true;
    }

    const auto staging_leaf =
        private_lease_staging_directory_leaf(selected.names, selected.witness.record.lease_id);
    bool reserved = false;
    bool owned = false;
    bool final_directory = false;
    bool staging_directory = false;
    bool rollback_handoff = false;
    switch (point) {
    case Point::BeforePendingRollbackSourceDirectorySync:
    case Point::AfterPendingRollbackSourceDirectoryDurable:
    case Point::BeforePendingRollbackDestinationDirectorySync:
    case Point::AfterPendingRollbackDestinationDirectoryDurable:
        reserved = true;
        owned = true;
        final_directory = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackPreactiveDirectoryQuarantinedDurable:
    case Point::AfterPendingRollbackPreactiveDataRemovedDurable:
    case Point::AfterPendingRollbackPreactiveIndexRemovedDurable:
    case Point::AfterPendingRollbackOwnerRemovedDurable:
        reserved = true;
        owned = true;
        staging_directory = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackLeaseDirectoryRemovedDurable:
        reserved = true;
        owned = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackReservedRemovedDurable:
        owned = true;
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackOwnedRemovedDurable:
    case Point::BeforePendingRollbackTombstoneRemovalValidated:
        rollback_handoff = true;
        break;
    case Point::AfterPendingRollbackTombstoneRemovedDurable:
        break;
    case Point::AfterReservedRevokedDurable:
        owned = true;
        final_directory = true;
        break;
    case Point::AfterExpectedPrefixValidated:
    case Point::AfterCanonicalConfirmedDurable:
        return true;
    case Point::Count:
        return false;
    }

    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.reserved_leaf, reserved);
    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.owned_leaf, owned);
    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.private_directory_leaf,
                                              final_directory);
    set_worker_handoff_protocol_leaf_presence(inventory, staging_leaf, staging_directory);
    set_worker_handoff_protocol_leaf_presence(inventory, selected.names.rollback_handoff_leaf,
                                              rollback_handoff);
    return true;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_merge_prepared_aggregate_projection(
    int root_fd, const std::filesystem::path& absolute_root, const WaveManifestV1& manifest,
    const MergePreparedPublicationAggregateWitness& expected,
    const RetainedMergePreparedPublicationPrefix& retained, std::uint64_t creator_process_id,
    std::optional<DistributedSieveMergePreparedResumeObservationPointV1> observation_point =
        std::nullopt) noexcept {
    if (retained.consumed || !retained.permit.held()) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    if ((!observation_point.has_value() && !retained.permit.valid()) ||
        (observation_point.has_value() &&
         (!merge_prepared_resume_point_allowed(retained, *observation_point) ||
          retained.permit.valid()))) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (expected.attempt_index != retained.attempt_index || expected.names != retained.names ||
        expected.prefix != retained.witness || expected.typed_prepared != retained.typed_prepared ||
        expected.start_record != retained.start_record ||
        retained.attempt_index >= expected.private_leases.size() ||
        expected.private_leases[retained.attempt_index] != retained.provisional_lease ||
        expected.inventory.worker_attempt_records != expected.worker_attempt_records ||
        expected.inventory.merge_started_records != expected.merge_started_records ||
        expected.inventory.chunk_terminal_failure_records !=
            expected.chunk_terminal_failure_records) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }

    const auto current = inspect_namespace(root_fd);
    if (!current) {
        return current.diagnostic;
    }
    try {
        auto expected_inventory = expected.inventory;
        auto current_inventory = *current.inventory;
        if (observation_point.has_value() &&
            !project_selected_merge_prepared_protocol_state(expected_inventory, retained,
                                                            *observation_point)) {
            return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                              protocol_error());
        }
        expected_inventory.worker_attempt_records.clear();
        expected_inventory.merge_started_records.clear();
        expected_inventory.chunk_terminal_failure_records.clear();
        current_inventory.worker_attempt_records.clear();
        current_inventory.merge_started_records.clear();
        current_inventory.chunk_terminal_failure_records.clear();
        if (current_inventory != expected_inventory) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }

    auto base_locks = validate_private_lease_base_lock_inventory(root_fd, *current.inventory,
                                                                 manifest, creator_process_id);
    if (!base_locks) {
        return base_locks.diagnostic;
    }
    if (*base_locks.identities != expected.base_lock_identities) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    auto parsed =
        parse_private_lease_attempt_inventory(*current.inventory, manifest, *base_locks.identities);
    if (!parsed) {
        return parsed.diagnostic;
    }
    if (parsed.attempts->size() != expected.private_leases.size() ||
        retained.attempt_index != expected.attempt_index ||
        retained.attempt_index >= parsed.attempts->size()) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    std::size_t retained_worker_matches = 0;
    for (std::size_t index = 0; index < parsed.attempts->size(); ++index) {
        const auto& attempt = (*parsed.attempts)[index];
        if (index == retained.attempt_index) {
            if (!attempt.merge_generation_names.has_value() ||
                !attempt.merge_attempt_ordinal.has_value() ||
                *attempt.merge_generation_names != retained.names ||
                *attempt.merge_attempt_ordinal != retained.start_record.merge_attempt_ordinal) {
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            continue;
        }

        const auto retained_worker = std::find_if(
            expected.retained_workers.entries.begin(), expected.retained_workers.entries.end(),
            [&](const RetainedWorkerHandoffPublicationPrefix& worker) {
                return worker.attempt_index == index;
            });
        if (retained_worker != expected.retained_workers.entries.end()) {
            ++retained_worker_matches;
            const bool canonical_handoff_root_shape =
                !attempt.reserved && !attempt.reserved_pending && attempt.owned &&
                !attempt.owned_pending && attempt.final_directory &&
                !attempt.staging_directory_leaf.has_value();
            if (!canonical_handoff_root_shape || !attempt.worker_attempt_names.has_value() ||
                !attempt.worker_coordinate.has_value() ||
                *attempt.worker_attempt_names != retained_worker->names ||
                *attempt.worker_coordinate != retained_worker->coordinate ||
                retained_worker->consumed || !retained_worker->permit.valid() ||
                !retained_worker->permit.held() ||
                expected.private_leases[index] != retained_worker->provisional_lease ||
                !expected.private_leases[index].worker_handoff.has_value() ||
                *expected.private_leases[index].worker_handoff != retained_worker->typed_handoff ||
                std::ranges::find(expected.worker_attempt_records,
                                  retained_worker->attempt_record) ==
                    expected.worker_attempt_records.end() ||
                !worker_handoff_publication_prefix_marker_chain_matches(
                    retained_worker->witness, manifest.wave_root_identity, attempt,
                    retained_worker->typed_handoff.handoff.lease.directory)) {
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            const auto exact = private_lease::revalidate_private_handoff_publication_resume_v1(
                retained_worker->permit);
            if (!exact.revalidated() || !exact.witness.has_value()) {
                return worker_handoff_inspection_failure(exact.result);
            }
            if (*exact.witness != retained_worker->witness) {
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            if (const auto attempt_record = revalidate_exact_canonical_worker_attempt(
                    root_fd, retained_worker->names, retained_worker->attempt_record,
                    creator_process_id);
                attempt_record.status != DistributedSieveWaveStoreStatus::ready) {
                return attempt_record;
            }
            continue;
        }

        const bool canonical_handoff_root_shape = !attempt.reserved && !attempt.reserved_pending &&
                                                  attempt.owned && !attempt.owned_pending &&
                                                  attempt.final_directory &&
                                                  !attempt.staging_directory_leaf.has_value();
        if (canonical_handoff_root_shape ||
            expected.private_leases[index].worker_handoff.has_value() ||
            expected.private_leases[index].merge_prepared.has_value() ||
            expected.private_leases[index].merge_raw_writer_recovery.has_value()) {
            // Acquiring any canonical handoff BaseLock here would invert the
            // retained worker/predecessor -> target order. Every legal worker
            // handoff must already be represented by `retained_workers`, and
            // the classifier rejects a second MergePrepared prefix.
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
        auto lease = validate_private_lease_attempt_inventory(root_fd, absolute_root, attempt,
                                                              manifest, creator_process_id);
        if (!lease) {
            return lease.diagnostic;
        }
        if (*lease.witness != expected.private_leases[index]) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
    }
    if (retained_worker_matches != expected.retained_workers.entries.size()) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }

    auto worker_attempts = validate_worker_attempt_record_inventory(
        root_fd, *current.inventory, manifest, *base_locks.identities, expected.private_leases,
        creator_process_id);
    if (!worker_attempts) {
        return worker_attempts.diagnostic;
    }
    if (*worker_attempts.witnesses != expected.worker_attempt_records) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    for (const auto& start : expected.merge_started_records) {
        const auto names = distributed_sieve_merge_generation_names_v1(start.merge_attempt_ordinal);
        if (!names.has_value()) {
            return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                              protocol_error());
        }
        if (const auto exact = revalidate_exact_canonical_merge_started(root_fd, *names, start,
                                                                        creator_process_id);
            exact.status != DistributedSieveWaveStoreStatus::ready) {
            return exact;
        }
    }
    if (const auto dependency = validate_merge_prepared_dependency_projection(
            manifest, *worker_attempts.witnesses, expected.private_leases,
            expected.merge_started_records, expected.typed_prepared.prepared);
        !dependency) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = dependency;
        return failure;
    }
    auto terminal_failures = validate_chunk_terminal_failure_record_inventory(
        root_fd, *current.inventory, manifest, expected.private_leases, *worker_attempts.witnesses,
        creator_process_id);
    if (!terminal_failures) {
        return terminal_failures.diagnostic;
    }
    if (*terminal_failures.witnesses != expected.chunk_terminal_failure_records) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return {};
}

struct MergePreparedResumeBridgeContext final {
    DistributedSieveMergePreparedResumeTestHooksV1 user_hooks;
    int parent_fd = -1;
    const std::vector<std::string>* parent_components = nullptr;
    int root_fd = -1;
    const std::filesystem::path* root_leaf = nullptr;
    NativeIdentityV1 root_identity;
    int lock_fd = -1;
    NativeIdentityV1 lock_identity;
    const std::vector<std::byte>* manifest_bytes = nullptr;
    durable_record::RecordSnapshot manifest_snapshot;
    const std::filesystem::path* absolute_root = nullptr;
    const WaveManifestV1* manifest = nullptr;
    const MergePreparedPublicationAggregateWitness* aggregate = nullptr;
    const RetainedMergePreparedPublicationPrefix* retained = nullptr;
    std::uint64_t creator_process_id = 0;
    bool revalidation_failed = false;
    DistributedSieveWaveStoreDiagnostic revalidation_diagnostic;
};

[[nodiscard]] bool bridge_merge_prepared_resume_observation(
    private_lease::PrivateHandoffPublicationResumeObservationPointV1 point, void* opaque,
    bool fail_before) noexcept {
    auto* context = static_cast<MergePreparedResumeBridgeContext*>(opaque);
    if (context == nullptr) {
        return true;
    }
    const auto wave_point =
        static_cast<DistributedSieveMergePreparedResumeObservationPointV1>(point);
    const bool user_requested_stop =
        fail_before ? context->user_hooks.fail_before != nullptr &&
                          context->user_hooks.fail_before(wave_point, context->user_hooks.context)
                    : context->user_hooks.stop_after != nullptr &&
                          context->user_hooks.stop_after(wave_point, context->user_hooks.context);
    if (context->parent_components == nullptr || context->root_leaf == nullptr ||
        context->manifest_bytes == nullptr || context->absolute_root == nullptr ||
        context->manifest == nullptr || context->aggregate == nullptr ||
        context->retained == nullptr) {
        context->revalidation_failed = true;
        context->revalidation_diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        return true;
    }
    auto revalidated = validate_held_wave_store_manifest_authority(
        context->parent_fd, *context->parent_components, context->root_fd, *context->root_leaf,
        context->root_identity, context->lock_fd, context->lock_identity, *context->manifest_bytes,
        context->manifest_snapshot, context->creator_process_id);
    if (revalidated.status == DistributedSieveWaveStoreStatus::ready) {
        revalidated = revalidate_merge_prepared_aggregate_projection(
            context->root_fd, *context->absolute_root, *context->manifest, *context->aggregate,
            *context->retained, context->creator_process_id, wave_point);
    }
    if (revalidated.status == DistributedSieveWaveStoreStatus::ready) {
        revalidated = revalidate_exact_canonical_merge_started(
            context->root_fd, context->retained->names, context->retained->start_record,
            context->creator_process_id);
    }
    if (revalidated.status == DistributedSieveWaveStoreStatus::ready) {
        return user_requested_stop;
    }
    context->revalidation_failed = true;
    context->revalidation_diagnostic = std::move(revalidated);
    return true;
}

[[nodiscard]] bool bridge_merge_prepared_resume_stop_after(
    private_lease::PrivateHandoffPublicationResumeObservationPointV1 point, void* opaque) noexcept {
    return bridge_merge_prepared_resume_observation(point, opaque, false);
}

[[nodiscard]] bool bridge_merge_prepared_resume_fail_before(
    private_lease::PrivateHandoffPublicationResumeObservationPointV1 point, void* opaque) noexcept {
    return bridge_merge_prepared_resume_observation(point, opaque, true);
}

[[nodiscard]] private_lease::PrivateHandoffPublicationResumeTestHooksV1
relation_merge_prepared_resume_hooks(MergePreparedResumeBridgeContext& context) noexcept {
    return {
        .stop_after = bridge_merge_prepared_resume_stop_after,
        .fail_before = bridge_merge_prepared_resume_fail_before,
        .context = &context,
    };
}

struct DecodedManifestResult final {
    std::optional<WaveManifestV1> manifest;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return manifest.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] DecodedManifestResult decode_manifest(
    std::span<const std::byte> bytes, const util::Sha256Digest& expected_manifest_digest,
    const NativeIdentityV1& root_identity, const NativeIdentityV1& lock_identity) noexcept {
    auto decoded = decode_distributed_sieve_record(bytes);
    if (!decoded) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, protocol_error());
        failure.protocol_status = decoded.status;
        return {std::nullopt, std::move(failure)};
    }
    WaveManifestV1* manifest = std::get_if<WaveManifestV1>(&*decoded.value);
    if (manifest == nullptr) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::manifest_invalid, protocol_error());
        failure.protocol_status = DistributedSieveProtocolStatus{
            .error = DistributedSieveProtocolError::record_type_mismatch,
        };
        return {std::nullopt, std::move(failure)};
    }
    if (manifest->self_digest != expected_manifest_digest ||
        manifest->wave_root_identity != root_identity ||
        manifest->permanent_lock_identity != lock_identity ||
        manifest->lock_semantics_version != DISTRIBUTED_SIEVE_WAVE_LOCK_SEMANTICS_VERSION_V1) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::manifest_conflict, protocol_error())};
    }
    return {std::move(*manifest), {}};
}

struct ExistingManifestResult final {
    std::optional<std::vector<std::byte>> bytes;
    std::optional<WaveManifestV1> manifest;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return bytes.has_value() && manifest.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ExistingManifestResult
read_existing_manifest(int root_fd, const NamespaceInventory& inventory,
                       const util::Sha256Digest& expected_manifest_digest,
                       const NativeIdentityV1& root_identity, const NativeIdentityV1& lock_identity,
                       std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt, process_mismatch()};
    }
    if (!inventory.manifest && !inventory.pending) {
        return {std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::manifest_missing)};
    }

    std::optional<ManifestReadResult> canonical;
    std::optional<DecodedManifestResult> decoded_canonical;
    if (inventory.manifest) {
        canonical.emplace(read_manifest_leaf(root_fd, MANIFEST_LEAF, creator_process_id));
        if (!*canonical) {
            return {std::nullopt, std::nullopt, canonical->diagnostic};
        }
        decoded_canonical.emplace(decode_manifest(*canonical->bytes, expected_manifest_digest,
                                                  root_identity, lock_identity));
        if (!*decoded_canonical) {
            return {std::nullopt, std::nullopt, decoded_canonical->diagnostic};
        }
    }

    std::optional<ManifestReadResult> pending;
    std::optional<DecodedManifestResult> decoded_pending;
    if (inventory.pending) {
        pending.emplace(read_manifest_leaf(root_fd, MANIFEST_PENDING_LEAF, creator_process_id));
        if (!*pending) {
            return {std::nullopt, std::nullopt, pending->diagnostic};
        }
        decoded_pending.emplace(decode_manifest(*pending->bytes, expected_manifest_digest,
                                                root_identity, lock_identity));
        if (!*decoded_pending) {
            return {std::nullopt, std::nullopt, decoded_pending->diagnostic};
        }
    }

    if (canonical.has_value() && pending.has_value() && *canonical->bytes != *pending->bytes) {
        return {std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::manifest_conflict, protocol_error())};
    }
    if (canonical.has_value()) {
        return {std::move(canonical->bytes), std::move(decoded_canonical->manifest), {}};
    }
    return {std::move(pending->bytes), std::move(decoded_pending->manifest), {}};
}

struct ManifestHookBridge final {
    DistributedSieveWaveStoreTestHooks hooks;
    std::uint64_t creator_process_id = 0;
    bool process_changed = false;
    std::optional<DistributedSieveWaveStoreFaultPoint> last_durable_fault_point;
};

[[nodiscard]] bool bridge_manifest_hook(durable_record::RecordFaultPoint point,
                                        void* raw_context) noexcept {
    auto& context = *static_cast<ManifestHookBridge*>(raw_context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    DistributedSieveWaveStoreFaultPoint mapped = DistributedSieveWaveStoreFaultPoint::Count;
    switch (point) {
    case durable_record::RecordFaultPoint::PendingDurable:
        mapped = DistributedSieveWaveStoreFaultPoint::ManifestPendingDurable;
        break;
    case durable_record::RecordFaultPoint::CanonicalPromoted:
        mapped = DistributedSieveWaveStoreFaultPoint::ManifestCanonicalPromoted;
        break;
    case durable_record::RecordFaultPoint::CanonicalDurable:
        mapped = DistributedSieveWaveStoreFaultPoint::ManifestCanonicalDurable;
        break;
    }
    if (mapped == DistributedSieveWaveStoreFaultPoint::Count) {
        return true;
    }
    context.last_durable_fault_point = mapped;
    const bool interrupted = context.hooks.stop_after != nullptr &&
                             context.hooks.stop_after(mapped, context.hooks.context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    return interrupted;
}

struct WorkerAttemptStartHookBridge final {
    DistributedSieveWorkerAttemptStartTestHooks hooks;
    std::uint64_t creator_process_id = 0;
    bool process_changed = false;
    std::optional<DistributedSieveWorkerAttemptStartFaultPoint> last_fault_point;
};

struct MergeStartHookBridgeV1 final {
    DistributedSieveMergeStartTestHooksV1 hooks;
    std::uint64_t creator_process_id = 0;
    bool process_changed = false;
    std::optional<DistributedSieveMergeStartFaultPointV1> last_fault_point;
};

struct WaveMergeCommitHookBridgeV1 final {
    DistributedSieveWaveMergeCommitTestHooksV1 hooks;
    std::uint64_t creator_process_id = 0;
    bool process_changed = false;
    std::optional<DistributedSieveWaveMergeCommitFaultPointV1> last_fault_point;
};

struct WorkerAttemptReconcileHookBridge final {
    DistributedSieveWorkerAttemptReconcileTestHooks hooks;
    std::uint64_t creator_process_id = 0;
    bool process_changed = false;
    std::optional<DistributedSieveWorkerAttemptReconcileFaultPoint> last_fault_point;
};

struct ChunkTerminalFailureHookBridge final {
    DistributedSieveChunkTerminalFailureTestHooksV1 hooks;
    std::uint64_t creator_process_id = 0;
    bool process_changed = false;
    std::optional<DistributedSieveChunkTerminalFailureFaultPoint> last_fault_point;
};

[[nodiscard]] bool bridge_worker_attempt_start_hook(durable_record::RecordFaultPoint point,
                                                    void* raw_context) noexcept {
    auto& context = *static_cast<WorkerAttemptStartHookBridge*>(raw_context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    DistributedSieveWorkerAttemptStartFaultPoint mapped =
        DistributedSieveWorkerAttemptStartFaultPoint::Count;
    switch (point) {
    case durable_record::RecordFaultPoint::PendingDurable:
        mapped = DistributedSieveWorkerAttemptStartFaultPoint::PendingDurable;
        break;
    case durable_record::RecordFaultPoint::CanonicalPromoted:
        mapped = DistributedSieveWorkerAttemptStartFaultPoint::CanonicalPromoted;
        break;
    case durable_record::RecordFaultPoint::CanonicalDurable:
        mapped = DistributedSieveWorkerAttemptStartFaultPoint::CanonicalDurable;
        break;
    }
    if (mapped == DistributedSieveWorkerAttemptStartFaultPoint::Count) {
        return true;
    }
    context.last_fault_point = mapped;
    const bool interrupted = context.hooks.stop_after != nullptr &&
                             context.hooks.stop_after(mapped, context.hooks.context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    return interrupted;
}

[[nodiscard]] bool bridge_merge_start_hook_v1(durable_record::RecordFaultPoint point,
                                              void* raw_context) noexcept {
    auto& context = *static_cast<MergeStartHookBridgeV1*>(raw_context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    DistributedSieveMergeStartFaultPointV1 mapped = DistributedSieveMergeStartFaultPointV1::Count;
    switch (point) {
    case durable_record::RecordFaultPoint::PendingDurable:
        mapped = DistributedSieveMergeStartFaultPointV1::PendingDurable;
        break;
    case durable_record::RecordFaultPoint::CanonicalPromoted:
        mapped = DistributedSieveMergeStartFaultPointV1::CanonicalPromoted;
        break;
    case durable_record::RecordFaultPoint::CanonicalDurable:
        mapped = DistributedSieveMergeStartFaultPointV1::CanonicalDurable;
        break;
    }
    if (mapped == DistributedSieveMergeStartFaultPointV1::Count) {
        return true;
    }
    context.last_fault_point = mapped;
    const bool interrupted = context.hooks.stop_after != nullptr &&
                             context.hooks.stop_after(mapped, context.hooks.context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    return interrupted;
}

[[nodiscard]] bool bridge_wave_merge_commit_hook_v1(durable_record::RecordFaultPoint point,
                                                    void* raw_context) noexcept {
    auto& context = *static_cast<WaveMergeCommitHookBridgeV1*>(raw_context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    DistributedSieveWaveMergeCommitFaultPointV1 mapped =
        DistributedSieveWaveMergeCommitFaultPointV1::Count;
    switch (point) {
    case durable_record::RecordFaultPoint::PendingDurable:
        mapped = DistributedSieveWaveMergeCommitFaultPointV1::PendingDurable;
        break;
    case durable_record::RecordFaultPoint::CanonicalPromoted:
        mapped = DistributedSieveWaveMergeCommitFaultPointV1::CanonicalPromoted;
        break;
    case durable_record::RecordFaultPoint::CanonicalDurable:
        mapped = DistributedSieveWaveMergeCommitFaultPointV1::CanonicalDurable;
        break;
    }
    if (mapped == DistributedSieveWaveMergeCommitFaultPointV1::Count) {
        return true;
    }
    context.last_fault_point = mapped;
    const bool interrupted = context.hooks.stop_after != nullptr &&
                             context.hooks.stop_after(mapped, context.hooks.context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    return interrupted;
}

[[nodiscard]] bool bridge_worker_attempt_reconcile_hook(durable_record::RecordFaultPoint point,
                                                        void* raw_context) noexcept {
    auto& context = *static_cast<WorkerAttemptReconcileHookBridge*>(raw_context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    DistributedSieveWorkerAttemptReconcileFaultPoint mapped =
        DistributedSieveWorkerAttemptReconcileFaultPoint::Count;
    switch (point) {
    case durable_record::RecordFaultPoint::PendingDurable:
        mapped = DistributedSieveWorkerAttemptReconcileFaultPoint::PendingDurable;
        break;
    case durable_record::RecordFaultPoint::CanonicalPromoted:
        mapped = DistributedSieveWorkerAttemptReconcileFaultPoint::CanonicalPromoted;
        break;
    case durable_record::RecordFaultPoint::CanonicalDurable:
        mapped = DistributedSieveWorkerAttemptReconcileFaultPoint::CanonicalDurable;
        break;
    }
    if (mapped == DistributedSieveWorkerAttemptReconcileFaultPoint::Count) {
        return true;
    }
    context.last_fault_point = mapped;
    const bool interrupted = context.hooks.stop_after != nullptr &&
                             context.hooks.stop_after(mapped, context.hooks.context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    return interrupted;
}

[[nodiscard]] bool bridge_chunk_terminal_failure_hook(durable_record::RecordFaultPoint point,
                                                      void* raw_context) noexcept {
    auto& context = *static_cast<ChunkTerminalFailureHookBridge*>(raw_context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    DistributedSieveChunkTerminalFailureFaultPoint mapped =
        DistributedSieveChunkTerminalFailureFaultPoint::Count;
    switch (point) {
    case durable_record::RecordFaultPoint::PendingDurable:
        mapped = DistributedSieveChunkTerminalFailureFaultPoint::PendingDurable;
        break;
    case durable_record::RecordFaultPoint::CanonicalPromoted:
        mapped = DistributedSieveChunkTerminalFailureFaultPoint::CanonicalPromoted;
        break;
    case durable_record::RecordFaultPoint::CanonicalDurable:
        mapped = DistributedSieveChunkTerminalFailureFaultPoint::CanonicalDurable;
        break;
    }
    if (mapped == DistributedSieveChunkTerminalFailureFaultPoint::Count) {
        return true;
    }
    context.last_fault_point = mapped;
    const bool interrupted = context.hooks.stop_after != nullptr &&
                             context.hooks.stop_after(mapped, context.hooks.context);
    if (!process_matches(context.creator_process_id)) {
        context.process_changed = true;
        return true;
    }
    return interrupted;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
worker_attempt_start_publication_diagnostic(const durable_record::RecordPublishResult& published,
                                            const WorkerAttemptStartHookBridge& bridge) noexcept {
    DistributedSieveWaveStoreDiagnostic outcome;
    outcome.publication_status = published.status();
    outcome.publication_disposition = published.disposition();
    outcome.native_error = published.native_error();
    outcome.last_worker_attempt_start_fault_point = bridge.last_fault_point;
    if (bridge.process_changed || !process_matches(bridge.creator_process_id)) {
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        outcome.native_error = invalid_argument_error();
        return outcome;
    }
    switch (published.status()) {
    case durable_record::RecordPublishStatus::durable:
        outcome.status = DistributedSieveWaveStoreStatus::ready;
        break;
    case durable_record::RecordPublishStatus::interrupted:
        outcome.status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case durable_record::RecordPublishStatus::invalid_request:
    case durable_record::RecordPublishStatus::input_too_large:
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case durable_record::RecordPublishStatus::platform_unsupported:
        outcome.status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case durable_record::RecordPublishStatus::pending_conflict:
    case durable_record::RecordPublishStatus::canonical_conflict:
        outcome.status = DistributedSieveWaveStoreStatus::namespace_conflict;
        break;
    case durable_record::RecordPublishStatus::parent_sync_failed:
        outcome.status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case durable_record::RecordPublishStatus::pending_publish_failed:
    case durable_record::RecordPublishStatus::promotion_failed:
    case durable_record::RecordPublishStatus::canonical_confirm_failed:
    case durable_record::RecordPublishStatus::pending_cleanup_failed:
    case durable_record::RecordPublishStatus::ops_contract_violation:
        outcome.status = DistributedSieveWaveStoreStatus::publication_failed;
        break;
    case durable_record::RecordPublishStatus::unexpected_failure:
        outcome.status =
            published.native_error() == std::make_error_code(std::errc::not_enough_memory)
                ? DistributedSieveWaveStoreStatus::resource_exhausted
                : DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    }
    return outcome;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
merge_start_publication_diagnostic_v1(const durable_record::RecordPublishResult& published,
                                      const MergeStartHookBridgeV1& bridge) noexcept {
    auto outcome = worker_attempt_start_publication_diagnostic(
        published, WorkerAttemptStartHookBridge{
                       .creator_process_id = bridge.creator_process_id,
                       .process_changed = bridge.process_changed,
                   });
    outcome.last_worker_attempt_start_fault_point.reset();
    outcome.last_merge_start_fault_point = bridge.last_fault_point;
    return outcome;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
wave_merge_commit_publication_diagnostic_v1(const durable_record::RecordPublishResult& published,
                                            const WaveMergeCommitHookBridgeV1& bridge) noexcept {
    auto outcome = worker_attempt_start_publication_diagnostic(
        published, WorkerAttemptStartHookBridge{
                       .creator_process_id = bridge.creator_process_id,
                       .process_changed = bridge.process_changed,
                   });
    outcome.last_worker_attempt_start_fault_point.reset();
    outcome.last_wave_merge_commit_fault_point = bridge.last_fault_point;
    return outcome;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic worker_attempt_reconcile_publication_diagnostic(
    const durable_record::RecordPublishResult& published,
    const WorkerAttemptReconcileHookBridge& bridge) noexcept {
    DistributedSieveWaveStoreDiagnostic outcome;
    outcome.publication_status = published.status();
    outcome.publication_disposition = published.disposition();
    outcome.native_error = published.native_error();
    outcome.last_worker_attempt_reconcile_fault_point = bridge.last_fault_point;
    if (bridge.process_changed || !process_matches(bridge.creator_process_id)) {
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        outcome.native_error = invalid_argument_error();
        return outcome;
    }
    switch (published.status()) {
    case durable_record::RecordPublishStatus::durable:
        outcome.status = DistributedSieveWaveStoreStatus::ready;
        break;
    case durable_record::RecordPublishStatus::interrupted:
        outcome.status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case durable_record::RecordPublishStatus::invalid_request:
    case durable_record::RecordPublishStatus::input_too_large:
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case durable_record::RecordPublishStatus::platform_unsupported:
        outcome.status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case durable_record::RecordPublishStatus::pending_conflict:
    case durable_record::RecordPublishStatus::canonical_conflict:
        outcome.status = DistributedSieveWaveStoreStatus::namespace_conflict;
        break;
    case durable_record::RecordPublishStatus::parent_sync_failed:
        outcome.status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case durable_record::RecordPublishStatus::pending_publish_failed:
    case durable_record::RecordPublishStatus::promotion_failed:
    case durable_record::RecordPublishStatus::canonical_confirm_failed:
    case durable_record::RecordPublishStatus::pending_cleanup_failed:
    case durable_record::RecordPublishStatus::ops_contract_violation:
        outcome.status = DistributedSieveWaveStoreStatus::publication_failed;
        break;
    case durable_record::RecordPublishStatus::unexpected_failure:
        outcome.status =
            published.native_error() == std::make_error_code(std::errc::not_enough_memory)
                ? DistributedSieveWaveStoreStatus::resource_exhausted
                : DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    }
    return outcome;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic chunk_terminal_failure_publication_diagnostic(
    const durable_record::RecordPublishResult& published,
    const ChunkTerminalFailureHookBridge& bridge) noexcept {
    DistributedSieveWaveStoreDiagnostic outcome;
    outcome.publication_status = published.status();
    outcome.publication_disposition = published.disposition();
    outcome.native_error = published.native_error();
    outcome.last_chunk_terminal_failure_fault_point = bridge.last_fault_point;
    if (bridge.process_changed || !process_matches(bridge.creator_process_id)) {
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        outcome.native_error = invalid_argument_error();
        return outcome;
    }
    switch (published.status()) {
    case durable_record::RecordPublishStatus::durable:
        outcome.status = DistributedSieveWaveStoreStatus::ready;
        break;
    case durable_record::RecordPublishStatus::interrupted:
        outcome.status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case durable_record::RecordPublishStatus::invalid_request:
    case durable_record::RecordPublishStatus::input_too_large:
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case durable_record::RecordPublishStatus::platform_unsupported:
        outcome.status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case durable_record::RecordPublishStatus::pending_conflict:
    case durable_record::RecordPublishStatus::canonical_conflict:
        outcome.status = DistributedSieveWaveStoreStatus::namespace_conflict;
        break;
    case durable_record::RecordPublishStatus::parent_sync_failed:
        outcome.status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case durable_record::RecordPublishStatus::pending_publish_failed:
    case durable_record::RecordPublishStatus::promotion_failed:
    case durable_record::RecordPublishStatus::canonical_confirm_failed:
    case durable_record::RecordPublishStatus::pending_cleanup_failed:
    case durable_record::RecordPublishStatus::ops_contract_violation:
        outcome.status = DistributedSieveWaveStoreStatus::publication_failed;
        break;
    case durable_record::RecordPublishStatus::unexpected_failure:
        outcome.status =
            published.native_error() == std::make_error_code(std::errc::not_enough_memory)
                ? DistributedSieveWaveStoreStatus::resource_exhausted
                : DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    }
    return outcome;
}

struct ManifestPublishResult final {
    std::optional<durable_record::RecordSnapshot> snapshot;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return snapshot.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] ManifestPublishResult publish_manifest(int root_fd, std::span<const std::byte> bytes,
                                                     DistributedSieveWaveStoreTestHooks hooks,
                                                     std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, process_mismatch()};
    }
    ManifestHookBridge bridge{
        .hooks = hooks,
        .creator_process_id = creator_process_id,
        .process_changed = false,
        .last_durable_fault_point = std::nullopt,
    };
    const auto published =
        durable_record::publish_at(static_cast<durable_record::NativeHandle>(root_fd),
                                   MANIFEST_PENDING_LEAF, MANIFEST_LEAF, bytes,
                                   durable_record::RecordTestHooks{
                                       .stop_after = bridge_manifest_hook,
                                       .context = &bridge,
                                   });

    DistributedSieveWaveStoreDiagnostic outcome;
    outcome.publication_status = published.status();
    outcome.native_error = published.native_error();
    outcome.last_durable_fault_point = bridge.last_durable_fault_point;
    if (bridge.process_changed || !process_matches(creator_process_id)) {
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        outcome.native_error = invalid_argument_error();
        return {std::nullopt, std::move(outcome)};
    }

    switch (published.status()) {
    case durable_record::RecordPublishStatus::durable:
        if (!published.canonical_snapshot().has_value()) {
            outcome.status = DistributedSieveWaveStoreStatus::publication_failed;
            outcome.native_error = protocol_error();
            return {std::nullopt, std::move(outcome)};
        }
        outcome.status = DistributedSieveWaveStoreStatus::ready;
        return {*published.canonical_snapshot(), std::move(outcome)};
    case durable_record::RecordPublishStatus::interrupted:
        outcome.status = DistributedSieveWaveStoreStatus::interrupted;
        break;
    case durable_record::RecordPublishStatus::invalid_request:
    case durable_record::RecordPublishStatus::input_too_large:
        outcome.status = DistributedSieveWaveStoreStatus::invalid_request;
        break;
    case durable_record::RecordPublishStatus::platform_unsupported:
        outcome.status = DistributedSieveWaveStoreStatus::platform_unsupported;
        break;
    case durable_record::RecordPublishStatus::pending_conflict:
    case durable_record::RecordPublishStatus::canonical_conflict:
        outcome.status = DistributedSieveWaveStoreStatus::manifest_conflict;
        break;
    case durable_record::RecordPublishStatus::parent_sync_failed:
        outcome.status = DistributedSieveWaveStoreStatus::durability_failed;
        break;
    case durable_record::RecordPublishStatus::pending_publish_failed:
    case durable_record::RecordPublishStatus::promotion_failed:
    case durable_record::RecordPublishStatus::canonical_confirm_failed:
    case durable_record::RecordPublishStatus::pending_cleanup_failed:
    case durable_record::RecordPublishStatus::ops_contract_violation:
        outcome.status = DistributedSieveWaveStoreStatus::publication_failed;
        break;
    case durable_record::RecordPublishStatus::unexpected_failure:
        outcome.status =
            published.native_error() == std::make_error_code(std::errc::not_enough_memory)
                ? DistributedSieveWaveStoreStatus::resource_exhausted
                : DistributedSieveWaveStoreStatus::unexpected_failure;
        break;
    }
    return {std::nullopt, std::move(outcome)};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
invoke_store_hook(DistributedSieveWaveStoreTestHooks hooks,
                  DistributedSieveWaveStoreFaultPoint point,
                  std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    const bool interrupted = hooks.stop_after != nullptr && hooks.stop_after(point, hooks.context);
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    if (!interrupted) {
        return {};
    }
    auto outcome = diagnostic(DistributedSieveWaveStoreStatus::interrupted);
    outcome.last_durable_fault_point = point;
    return outcome;
}

struct FinalManifestResult final {
    std::optional<std::vector<std::byte>> bytes;
    std::optional<WaveManifestV1> manifest;
    std::optional<durable_record::RecordSnapshot> snapshot;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return bytes.has_value() && manifest.has_value() && snapshot.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] FinalManifestResult
confirm_final_manifest(int root_fd, std::span<const std::byte> expected_bytes,
                       const util::Sha256Digest& expected_manifest_digest,
                       const NativeIdentityV1& root_identity, const NativeIdentityV1& lock_identity,
                       const durable_record::RecordSnapshot& published_snapshot,
                       std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt, std::nullopt, process_mismatch()};
    }
    auto canonical = read_manifest_leaf(root_fd, MANIFEST_LEAF, creator_process_id);
    if (!canonical) {
        return {std::nullopt, std::nullopt, std::nullopt, canonical.diagnostic};
    }
    if (!std::equal(canonical.bytes->begin(), canonical.bytes->end(), expected_bytes.begin(),
                    expected_bytes.end()) ||
        *canonical.snapshot != published_snapshot) {
        return {std::nullopt, std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::manifest_conflict, protocol_error())};
    }
    auto decoded =
        decode_manifest(*canonical.bytes, expected_manifest_digest, root_identity, lock_identity);
    if (!decoded) {
        return {std::nullopt, std::nullopt, std::nullopt, decoded.diagnostic};
    }
    const auto inventory = inspect_namespace(root_fd);
    if (!inventory) {
        return {std::nullopt, std::nullopt, std::nullopt, inventory.diagnostic};
    }
    if (!inventory.inventory->lock || !inventory.inventory->manifest ||
        inventory.inventory->pending) {
        return {std::nullopt, std::nullopt, std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (!process_matches(creator_process_id)) {
        return {std::nullopt, std::nullopt, std::nullopt, process_mismatch()};
    }
    return {
        std::move(canonical.bytes), std::move(decoded.manifest), std::move(canonical.snapshot), {}};
}

[[nodiscard]] bool valid_private_lease_base_lock_leaf(std::string_view leaf) noexcept {
    return !leaf.empty() && leaf != "." && leaf != ".." &&
           leaf.find('/') == std::string_view::npos && leaf.find('\0') == std::string_view::npos &&
           leaf.ends_with(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX);
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
private_lease_base_lock_open_failure(int error) noexcept {
    switch (error) {
    case ENOMEM:
    case EMFILE:
    case ENFILE:
    case ENOSPC:
#if defined(EDQUOT)
    case EDQUOT:
#endif
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted, posix_error(error));
    case EACCES:
    case EEXIST:
    case EISDIR:
    case ELOOP:
    case ENOENT:
    case ENOTDIR:
    case EPERM:
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(error));
    default:
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(error));
    }
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
lock_private_lease_base_lock(int descriptor) noexcept {
    int result = -1;
    do {
        result = ::flock(descriptor, LOCK_EX | LOCK_NB);
    } while (result != 0 && errno == EINTR);
    if (result == 0) {
        return {};
    }
    const int saved_errno = errno;
    if (saved_errno == EWOULDBLOCK || saved_errno == EAGAIN) {
        return diagnostic(DistributedSieveWaveStoreStatus::private_lease_lock_busy,
                          posix_error(saved_errno));
    }
    if (saved_errno == ENOTSUP || saved_errno == EOPNOTSUPP || saved_errno == ENOSYS) {
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          posix_error(saved_errno));
    }
    return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
invoke_private_lease_base_lock_hook(DistributedSievePrivateLeaseBaseLockTestHooks::Boundary hook,
                                    void* context, std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    if (hook != nullptr) {
        hook(context);
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    return {};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
invoke_private_lease_base_lock_sync_hook(DistributedSievePrivateLeaseBaseLockTestHooks hooks,
                                         DistributedSievePrivateLeaseBaseLockSyncPoint point,
                                         std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    const bool fail =
        hooks.fail_before_sync != nullptr && hooks.fail_before_sync(point, hooks.context);
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    if (!fail) {
        return {};
    }
    auto outcome = diagnostic(DistributedSieveWaveStoreStatus::durability_failed,
                              std::make_error_code(std::errc::io_error));
    outcome.failed_private_lease_base_lock_sync_point = point;
    return outcome;
}

struct PrivateLeaseMarkerIdentityResult final {
    std::optional<NativeIdentityV1> identity;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return identity.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] PrivateLeaseMarkerIdentityResult
inspect_published_private_lease_marker(int parent_fd, const char* leaf, int marker_fd) noexcept {
    struct stat held{};
    struct stat named{};
    if (fstat_retrying_eintr(marker_fd, held) != 0 ||
        fstatat_retrying_eintr(parent_fd, leaf, named) != 0) {
        return {std::nullopt, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         posix_error(errno))};
    }
    if (!valid_private_lease_marker_metadata(held) || !valid_private_lease_marker_metadata(named) ||
        !stable_metadata(held, named)) {
        return {std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    if (auto acl =
            acl_rejection(marker_fd, false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return {std::nullopt, *acl};
    }
    return {protocol_identity(held), {}};
}

struct PrivateLeaseStrictMarkerPublishResult final {
    UniqueFd marker;
    std::optional<NativeIdentityV1> identity;
    DistributedSieveWaveStoreDiagnostic outcome;

    [[nodiscard]] explicit operator bool() const noexcept {
        return static_cast<bool>(marker) && identity.has_value() &&
               outcome.status == DistributedSieveWaveStoreStatus::ready;
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic close_checked() noexcept {
        const int descriptor = marker.release();
        if (descriptor < 0) {
            return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                              protocol_error());
        }
        if (::close(descriptor) == 0) {
            return {};
        }
        return diagnostic(DistributedSieveWaveStoreStatus::publication_failed, posix_error(errno));
    }
};

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
private_lease_sync_handle(int descriptor) noexcept;

[[nodiscard]] DistributedSieveWaveStoreDiagnostic private_lease_reservation_sync_handle(
    int descriptor, DistributedSievePrivateLeaseReservationBoundary boundary,
    DistributedSievePrivateLeaseReservationSyncPoint point,
    std::optional<DistributedSievePrivateLeaseReservationSyncPoint> injected_failure) noexcept {
    if (const auto synchronized = private_lease_sync_handle(descriptor);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        return synchronized;
    }
    if (!injected_failure.has_value() || *injected_failure != point) {
        return {};
    }
    auto outcome = diagnostic(DistributedSieveWaveStoreStatus::durability_failed,
                              std::make_error_code(std::errc::io_error));
    outcome.failed_private_lease_reservation_sync_site =
        DistributedSievePrivateLeaseReservationSyncFailureSite{
            .boundary = boundary,
            .point = point,
        };
    return outcome;
}

[[nodiscard]] PrivateLeaseStrictMarkerPublishResult publish_private_lease_marker_strict_at(
    int parent_fd, const std::string& leaf, std::span<const std::byte> bytes,
    DistributedSievePrivateLeaseReservationBoundary boundary,
    std::optional<DistributedSievePrivateLeaseReservationSyncPoint>
        injected_sync_failure) noexcept {
    if (parent_fd < 0 || leaf.empty() || leaf == "." || leaf == ".." ||
        leaf.find('/') != std::string::npos || leaf.find('\0') != std::string::npos ||
        bytes.size() != private_lease::PRIVATE_LEASE_MARKER_BYTES) {
        return {
            {},
            std::nullopt,
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()),
        };
    }

    UniqueFd marker(openat_retrying_eintr(
        parent_fd, leaf.c_str(), O_WRONLY | O_CREAT | O_EXCL | O_NOFOLLOW | O_CLOEXEC, 0600));
    if (!marker) {
        const int saved_errno = errno;
        const auto status =
            saved_errno == EEXIST
                ? DistributedSieveWaveStoreStatus::namespace_conflict
                : (saved_errno == EMFILE || saved_errno == ENFILE || saved_errno == ENOMEM
                       ? DistributedSieveWaveStoreStatus::resource_exhausted
                       : DistributedSieveWaveStoreStatus::publication_failed);
        return {{}, std::nullopt, diagnostic(status, posix_error(saved_errno))};
    }
    auto fail_with = [&](DistributedSieveWaveStoreDiagnostic failure,
                         std::optional<NativeIdentityV1> identity = std::nullopt) mutable {
        return PrivateLeaseStrictMarkerPublishResult{std::move(marker), identity,
                                                     std::move(failure)};
    };

    int chmod_result = -1;
    do {
        chmod_result = ::fchmod(marker.get(), 0600);
    } while (chmod_result != 0 && errno == EINTR);
    if (chmod_result != 0) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::publication_failed, posix_error(errno)));
    }
    struct stat initial{};
    if (fstat_retrying_eintr(marker.get(), initial) != 0) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::publication_failed, posix_error(errno)));
    }
    if (!S_ISREG(initial.st_mode) || initial.st_nlink != 1 ||
        static_cast<std::uint64_t>(initial.st_uid) != static_cast<std::uint64_t>(::geteuid()) ||
        !exact_mode(initial, 0600) || initial.st_size != 0) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (auto acl =
            acl_rejection(marker.get(), false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return fail_with(*acl);
    }
    const NativeIdentityV1 created_identity = protocol_identity(initial);

    std::size_t offset = 0;
    while (offset < bytes.size()) {
        const std::size_t request = std::min<std::size_t>(
            bytes.size() - offset, static_cast<std::size_t>(std::numeric_limits<ssize_t>::max()));
        ssize_t written = -1;
        do {
            written = ::write(marker.get(), bytes.data() + offset, request);
        } while (written < 0 && errno == EINTR);
        if (written < 0) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::publication_failed,
                                        posix_error(errno)));
        }
        if (written == 0) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::publication_failed,
                                        std::make_error_code(std::errc::io_error)));
        }
        offset += static_cast<std::size_t>(written);
    }

    auto inspected = inspect_published_private_lease_marker(parent_fd, leaf.c_str(), marker.get());
    if (!inspected || *inspected.identity != created_identity) {
        return fail_with(inspected ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error())
                                   : std::move(inspected.diagnostic));
    }
    if (const auto synchronized = private_lease_reservation_sync_handle(
            marker.get(), boundary,
            DistributedSievePrivateLeaseReservationSyncPoint::MarkerFileInitial,
            injected_sync_failure);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(synchronized, created_identity);
    }
    inspected = inspect_published_private_lease_marker(parent_fd, leaf.c_str(), marker.get());
    if (!inspected || *inspected.identity != created_identity) {
        return fail_with(inspected ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error())
                                   : std::move(inspected.diagnostic));
    }
    if (const auto synchronized = private_lease_reservation_sync_handle(
            parent_fd, boundary, DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory,
            injected_sync_failure);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(synchronized, created_identity);
    }
    if (const auto synchronized = private_lease_reservation_sync_handle(
            marker.get(), boundary,
            DistributedSievePrivateLeaseReservationSyncPoint::MarkerFileFinal,
            injected_sync_failure);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(synchronized, created_identity);
    }
    inspected = inspect_published_private_lease_marker(parent_fd, leaf.c_str(), marker.get());
    if (!inspected || *inspected.identity != created_identity) {
        return fail_with(inspected ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error())
                                   : std::move(inspected.diagnostic));
    }
    return {std::move(marker), created_identity, {}};
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
private_lease_rename_no_replace_at(int parent_fd, const char* source,
                                   const char* destination) noexcept {
    for (;;) {
#if defined(__APPLE__)
        if (::renameatx_np(parent_fd, source, parent_fd, destination, RENAME_EXCL) == 0) {
            return {};
        }
#elif defined(__linux__) && defined(SYS_renameat2)
        constexpr unsigned int rename_noreplace = 1U;
        if (::syscall(SYS_renameat2, parent_fd, source, parent_fd, destination, rename_noreplace) ==
            0) {
            return {};
        }
#else
        (void)parent_fd;
        (void)source;
        (void)destination;
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          unsupported_error());
#endif
        const int saved_errno = errno;
        if (saved_errno == EINTR) {
            continue;
        }
        switch (saved_errno) {
        case EEXIST:
        case ENOENT:
        case ENOTDIR:
        case EISDIR:
        case ENOTEMPTY:
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              posix_error(saved_errno));
        case ENOSYS:
        case EINVAL:
        case EOPNOTSUPP:
            return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                              posix_error(saved_errno));
        default:
            return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
        }
    }
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
private_lease_sync_handle(int descriptor) noexcept {
    std::error_code error;
    if (sync_descriptor(descriptor, error)) {
        return {};
    }
    return diagnostic(DistributedSieveWaveStoreStatus::durability_failed,
                      error ? error : std::make_error_code(std::errc::io_error));
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic private_lease_recovery_sync_handle(
    int descriptor, DistributedSievePrivateLeaseRecoveryEdge edge,
    const std::optional<DistributedSievePrivateLeaseRecoveryEdge>& injected_failure) noexcept {
    auto outcome = private_lease_sync_handle(descriptor);
    if (outcome.status != DistributedSieveWaveStoreStatus::ready) {
        outcome.failed_private_lease_recovery_sync_edge = edge;
        return outcome;
    }
    if (!injected_failure.has_value() || *injected_failure != edge) {
        return outcome;
    }
    outcome = diagnostic(DistributedSieveWaveStoreStatus::durability_failed,
                         std::make_error_code(std::errc::io_error));
    outcome.failed_private_lease_recovery_sync_edge = edge;
    return outcome;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
read_exact_private_lease_marker_handle(int marker_fd,
                                       const private_lease::PrivateLeaseRecord& expected,
                                       std::uint64_t creator_process_id) noexcept {
    if (marker_fd < 0 || !process_matches(creator_process_id)) {
        return marker_fd < 0 ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                          invalid_argument_error())
                             : process_mismatch();
    }
    std::array<std::byte, private_lease::PRIVATE_LEASE_MARKER_BYTES> bytes{};
    std::size_t offset = 0;
    while (offset < bytes.size()) {
        if (!process_matches(creator_process_id)) {
            return process_mismatch();
        }
        const ssize_t count = ::pread(marker_fd, bytes.data() + offset, bytes.size() - offset,
                                      static_cast<off_t>(offset));
        if (count < 0) {
            if (errno == EINTR) {
                continue;
            }
            return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
        }
        if (count == 0) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
        offset += static_cast<std::size_t>(count);
    }
    try {
        if (private_lease::parse_private_lease_marker(bytes) != expected) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return process_matches(creator_process_id) ? DistributedSieveWaveStoreDiagnostic{}
                                               : process_mismatch();
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
validate_named_private_lease_marker_handle(int parent_fd, const char* leaf, int marker_fd,
                                           const private_lease::PrivateLeaseRecord& expected_record,
                                           const NativeIdentityV1& expected_identity,
                                           std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    auto first = inspect_published_private_lease_marker(parent_fd, leaf, marker_fd);
    if (!first || *first.identity != expected_identity) {
        return first ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error())
                     : std::move(first.diagnostic);
    }
    if (const auto record =
            read_exact_private_lease_marker_handle(marker_fd, expected_record, creator_process_id);
        record.status != DistributedSieveWaveStoreStatus::ready) {
        return record;
    }
    auto confirmed = inspect_published_private_lease_marker(parent_fd, leaf, marker_fd);
    if (!confirmed || *confirmed.identity != expected_identity ||
        *confirmed.identity != *first.identity) {
        return confirmed ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                      protocol_error())
                         : std::move(confirmed.diagnostic);
    }
    return process_matches(creator_process_id) ? DistributedSieveWaveStoreDiagnostic{}
                                               : process_mismatch();
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_unlinked_private_lease_marker_handle(
    int parent_fd, const char* former_leaf, int marker_fd,
    const private_lease::PrivateLeaseRecord& expected_record,
    const NativeIdentityV1& expected_identity, std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    struct stat named{};
    if (fstatat_retrying_eintr(parent_fd, former_leaf, named) == 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (const int saved_errno = errno; saved_errno != ENOENT) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                          posix_error(saved_errno));
    }
    struct stat held{};
    if (fstat_retrying_eintr(marker_fd, held) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (!S_ISREG(held.st_mode) || held.st_nlink != 0 ||
        static_cast<std::uint64_t>(held.st_size) != private_lease::PRIVATE_LEASE_MARKER_BYTES ||
        static_cast<std::uint64_t>(held.st_uid) != static_cast<std::uint64_t>(::geteuid()) ||
        !exact_mode(held, 0600) || protocol_identity(held) != expected_identity) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (auto acl =
            acl_rejection(marker_fd, false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }
    return read_exact_private_lease_marker_handle(marker_fd, expected_record, creator_process_id);
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
validate_exact_empty_private_lease_directory_handle(int directory_fd,
                                                    std::uint64_t creator_process_id) noexcept {
    if (directory_fd < 0 || !process_matches(creator_process_id)) {
        return directory_fd < 0 ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                             invalid_argument_error())
                                : process_mismatch();
    }
    struct stat before{};
    if (fstat_retrying_eintr(directory_fd, before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (!valid_private_lease_directory_metadata(before)) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (auto acl =
            acl_rejection(directory_fd, true, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }

    const int scan_fd =
        openat_retrying_eintr(directory_fd, ".", O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (scan_fd < 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
    }
    DIR* raw_directory = ::fdopendir(scan_fd);
    if (raw_directory == nullptr) {
        const int saved_errno = errno;
        (void)::close(scan_fd);
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
    }
    UniqueDirectory scan(raw_directory);
    errno = 0;
    for (;;) {
        dirent* entry = ::readdir(raw_directory);
        if (entry == nullptr) {
            if (errno != 0) {
                return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
            }
            break;
        }
        const std::string_view child(entry->d_name);
        if (child != "." && child != "..") {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
    }
    struct stat after{};
    if (fstat_retrying_eintr(directory_fd, after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (!valid_private_lease_directory_metadata(after) || !stable_metadata(before, after)) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    return process_matches(creator_process_id) ? DistributedSieveWaveStoreDiagnostic{}
                                               : process_mismatch();
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_unlinked_private_lease_directory_handle(
    int root_fd, const char* former_leaf, int directory_fd,
    const NativeIdentityV1& expected_identity, std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }
    struct stat named{};
    if (fstatat_retrying_eintr(root_fd, former_leaf, named) == 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (const int saved_errno = errno; saved_errno != ENOENT) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                          posix_error(saved_errno));
    }
    struct stat held{};
    if (fstat_retrying_eintr(directory_fd, held) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (!valid_private_lease_directory_metadata(held) ||
        protocol_identity(held) != expected_identity) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
#if defined(__linux__)
    if (held.st_nlink != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
#elif defined(__APPLE__)
    // Darwin retains st_nlink == 2 for an open directory after rmdir. The
    // disconnected handle keeps its former ".." binding, so prove that it
    // still names the held WaveStore root. The surrounding exact closed-root
    // observation proves that no alternate name in that parent survived.
    const int parent_descriptor =
        openat_retrying_eintr(directory_fd, "..", O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
    if (parent_descriptor < 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    UniqueFd parent(parent_descriptor);
    struct stat parent_metadata{};
    struct stat root_metadata{};
    if (fstat_retrying_eintr(parent.get(), parent_metadata) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (fstat_retrying_eintr(root_fd, root_metadata) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    if (!valid_private_lease_directory_metadata(parent_metadata) ||
        !valid_private_lease_directory_metadata(root_metadata) ||
        !stable_metadata(parent_metadata, root_metadata)) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (auto acl =
            acl_rejection(parent.get(), true, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }
#else
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#endif
    if (auto acl =
            acl_rejection(directory_fd, true, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }
    return validate_exact_empty_private_lease_directory_handle(directory_fd, creator_process_id);
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
private_lease_unlink_at(int parent_fd, const char* leaf, int flags) noexcept {
    for (;;) {
        if (::unlinkat(parent_fd, leaf, flags) == 0) {
            return {};
        }
        const int saved_errno = errno;
        if (saved_errno == EINTR) {
            continue;
        }
        switch (saved_errno) {
        case EEXIST:
        case ENOENT:
        case ENOTDIR:
        case EISDIR:
        case ENOTEMPTY:
        case ELOOP:
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              posix_error(saved_errno));
        default:
            return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
        }
    }
}

#endif

} // namespace

std::optional<DistributedSieveWorkerAttemptNamesV1>
distributed_sieve_worker_attempt_names_v1(std::string_view chunk_relative_artifact_stem,
                                          std::uint32_t chunk_id, std::uint32_t attempt_ordinal) {
    if (chunk_id >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS ||
        attempt_ordinal >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS ||
        chunk_relative_artifact_stem.size() >
            DISTRIBUTED_SIEVE_PROTOCOL_MAX_ARTIFACT_STEM_BYTES -
                DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size() -
                DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1) {
        return std::nullopt;
    }

    DistributedSieveWorkerAttemptNamesV1 names;
    names.relative_lease_stem.reserve(chunk_relative_artifact_stem.size() +
                                      DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1.size() +
                                      DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1);
    names.relative_lease_stem.append(chunk_relative_artifact_stem);
    names.relative_lease_stem.append(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_STEM_TAG_V1);
    names.relative_lease_stem.push_back(decimal_digit(attempt_ordinal / 10U));
    names.relative_lease_stem.push_back(decimal_digit(attempt_ordinal % 10U));
    if (!distributed_sieve_worker_attempt_relative_stem_matches(
            chunk_relative_artifact_stem, attempt_ordinal, names.relative_lease_stem)) {
        return std::nullopt;
    }

    names.private_directory_leaf = names.relative_lease_stem;
    names.private_directory_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_DIRECTORY_SUFFIX);
    names.base_lock_leaf = names.relative_lease_stem;
    names.base_lock_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX);
    names.reserved_leaf = names.relative_lease_stem;
    names.reserved_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVED_SUFFIX);
    names.reserved_pending_leaf = names.relative_lease_stem;
    names.reserved_pending_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVED_PENDING_SUFFIX);
    names.owned_leaf = names.relative_lease_stem;
    names.owned_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNED_SUFFIX);
    names.owned_pending_leaf = names.relative_lease_stem;
    names.owned_pending_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNED_PENDING_SUFFIX);
    names.rollback_handoff_leaf = names.relative_lease_stem;
    names.rollback_handoff_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_HANDOFF_ROLLBACK_SUFFIX);

    names.canonical_record_leaf.reserve(
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PREFIX.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1 +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_ORDINAL_SEPARATOR.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1);
    names.canonical_record_leaf.append(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PREFIX);
    names.canonical_record_leaf.push_back(decimal_digit(chunk_id / 10U));
    names.canonical_record_leaf.push_back(decimal_digit(chunk_id % 10U));
    names.canonical_record_leaf.append(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_ORDINAL_SEPARATOR);
    names.canonical_record_leaf.push_back(decimal_digit(attempt_ordinal / 10U));
    names.canonical_record_leaf.push_back(decimal_digit(attempt_ordinal % 10U));
    names.pending_record_leaf = names.canonical_record_leaf;
    names.pending_record_leaf.append(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PENDING_SUFFIX);
    return names;
}

std::optional<DistributedSieveParsedWorkerAttemptLeafV1>
parse_distributed_sieve_worker_attempt_leaf_v1(std::string_view leaf) noexcept {
    bool pending = false;
    if (leaf.ends_with(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PENDING_SUFFIX)) {
        pending = true;
        leaf.remove_suffix(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PENDING_SUFFIX.size());
    }
    const std::size_t expected_size =
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PREFIX.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1 +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_ORDINAL_SEPARATOR.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
    if (leaf.size() != expected_size ||
        !leaf.starts_with(DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PREFIX)) {
        return std::nullopt;
    }
    std::size_t cursor = DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_PREFIX.size();
    const auto parse_two_digits = [&](std::uint32_t& output) noexcept {
        if (leaf[cursor] < '0' || leaf[cursor] > '9' || leaf[cursor + 1U] < '0' ||
            leaf[cursor + 1U] > '9') {
            return false;
        }
        output = static_cast<std::uint32_t>(leaf[cursor] - '0') * 10U +
                 static_cast<std::uint32_t>(leaf[cursor + 1U] - '0');
        cursor += DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
        return true;
    };

    DistributedSieveParsedWorkerAttemptLeafV1 parsed{.pending = pending};
    if (!parse_two_digits(parsed.chunk_id) ||
        leaf.substr(cursor, DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_ORDINAL_SEPARATOR.size()) !=
            DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_ORDINAL_SEPARATOR) {
        return std::nullopt;
    }
    cursor += DISTRIBUTED_SIEVE_WORKER_ATTEMPT_RECORD_ORDINAL_SEPARATOR.size();
    if (!parse_two_digits(parsed.attempt_ordinal) || cursor != leaf.size() ||
        parsed.chunk_id >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS ||
        parsed.attempt_ordinal >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS) {
        return std::nullopt;
    }
    return parsed;
}

std::optional<DistributedSieveMergeGenerationNamesV1>
distributed_sieve_merge_generation_names_v1(std::uint32_t merge_attempt_ordinal) {
    if (merge_attempt_ordinal >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS) {
        return std::nullopt;
    }

    DistributedSieveMergeGenerationNamesV1 names;
    names.relative_lease_stem.reserve(DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1.size() +
                                      DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1);
    names.relative_lease_stem.append(DISTRIBUTED_SIEVE_MERGE_GENERATION_STEM_PREFIX_V1);
    names.relative_lease_stem.push_back(decimal_digit(merge_attempt_ordinal / 10U));
    names.relative_lease_stem.push_back(decimal_digit(merge_attempt_ordinal % 10U));

    names.private_directory_leaf = names.relative_lease_stem;
    names.private_directory_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_DIRECTORY_SUFFIX);
    names.base_lock_leaf = names.relative_lease_stem;
    names.base_lock_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX);
    names.reserved_leaf = names.relative_lease_stem;
    names.reserved_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVED_SUFFIX);
    names.reserved_pending_leaf = names.relative_lease_stem;
    names.reserved_pending_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_RESERVED_PENDING_SUFFIX);
    names.owned_leaf = names.relative_lease_stem;
    names.owned_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNED_SUFFIX);
    names.owned_pending_leaf = names.relative_lease_stem;
    names.owned_pending_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNED_PENDING_SUFFIX);
    names.rollback_handoff_leaf = names.relative_lease_stem;
    names.rollback_handoff_leaf.append(DISTRIBUTED_SIEVE_PRIVATE_HANDOFF_ROLLBACK_SUFFIX);

    names.canonical_record_leaf.reserve(DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PREFIX.size() +
                                        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1);
    names.canonical_record_leaf.append(DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PREFIX);
    names.canonical_record_leaf.push_back(decimal_digit(merge_attempt_ordinal / 10U));
    names.canonical_record_leaf.push_back(decimal_digit(merge_attempt_ordinal % 10U));
    names.pending_record_leaf = names.canonical_record_leaf;
    names.pending_record_leaf.append(DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PENDING_SUFFIX);
    return names;
}

std::optional<DistributedSieveParsedMergeStartedLeafV1>
parse_distributed_sieve_merge_started_leaf_v1(std::string_view leaf) noexcept {
    bool pending = false;
    if (leaf.ends_with(DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PENDING_SUFFIX)) {
        pending = true;
        leaf.remove_suffix(DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PENDING_SUFFIX.size());
    }
    const std::size_t expected_size = DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PREFIX.size() +
                                      DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
    if (leaf.size() != expected_size ||
        !leaf.starts_with(DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PREFIX)) {
        return std::nullopt;
    }
    const std::size_t digits_offset = DISTRIBUTED_SIEVE_MERGE_STARTED_RECORD_PREFIX.size();
    if (leaf[digits_offset] < '0' || leaf[digits_offset] > '9' || leaf[digits_offset + 1U] < '0' ||
        leaf[digits_offset + 1U] > '9') {
        return std::nullopt;
    }
    const auto ordinal = static_cast<std::uint32_t>(leaf[digits_offset] - '0') * 10U +
                         static_cast<std::uint32_t>(leaf[digits_offset + 1U] - '0');
    if (ordinal >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_ATTEMPTS) {
        return std::nullopt;
    }
    return DistributedSieveParsedMergeStartedLeafV1{
        .merge_attempt_ordinal = ordinal,
        .pending = pending,
    };
}

std::optional<DistributedSieveChunkTerminalFailureNamesV1>
distributed_sieve_chunk_terminal_failure_names_v1(std::uint32_t chunk_id) {
    if (chunk_id >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) {
        return std::nullopt;
    }
    DistributedSieveChunkTerminalFailureNamesV1 names;
    names.canonical_record_leaf.reserve(
        DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PREFIX.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1);
    names.canonical_record_leaf.append(DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PREFIX);
    names.canonical_record_leaf.push_back(decimal_digit(chunk_id / 10U));
    names.canonical_record_leaf.push_back(decimal_digit(chunk_id % 10U));
    names.pending_record_leaf = names.canonical_record_leaf;
    names.pending_record_leaf.append(
        DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PENDING_SUFFIX);
    return names;
}

std::optional<DistributedSieveParsedChunkTerminalFailureLeafV1>
parse_distributed_sieve_chunk_terminal_failure_leaf_v1(std::string_view leaf) noexcept {
    bool pending = false;
    if (leaf.ends_with(DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PENDING_SUFFIX)) {
        pending = true;
        leaf.remove_suffix(DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PENDING_SUFFIX.size());
    }
    const std::size_t expected_size =
        DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PREFIX.size() +
        DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
    if (leaf.size() != expected_size ||
        !leaf.starts_with(DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PREFIX)) {
        return std::nullopt;
    }
    const std::size_t cursor = DISTRIBUTED_SIEVE_CHUNK_TERMINAL_FAILURE_RECORD_PREFIX.size();
    if (leaf[cursor] < '0' || leaf[cursor] > '9' || leaf[cursor + 1U] < '0' ||
        leaf[cursor + 1U] > '9') {
        return std::nullopt;
    }
    const std::uint32_t chunk_id = static_cast<std::uint32_t>(leaf[cursor] - '0') * 10U +
                                   static_cast<std::uint32_t>(leaf[cursor + 1U] - '0');
    if (chunk_id >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) {
        return std::nullopt;
    }
    return DistributedSieveParsedChunkTerminalFailureLeafV1{
        .chunk_id = chunk_id,
        .pending = pending,
    };
}

DistributedSieveWaveMergeCommitNamesV1 distributed_sieve_wave_merge_commit_names_v1() {
    return {
        .canonical_record_leaf = std::string(DISTRIBUTED_SIEVE_WAVE_MERGE_COMMIT_RECORD_LEAF),
        .pending_record_leaf = std::string(DISTRIBUTED_SIEVE_WAVE_MERGE_COMMIT_RECORD_PENDING_LEAF),
    };
}

std::optional<DistributedSieveParsedWaveMergeCommitLeafV1>
parse_distributed_sieve_wave_merge_commit_leaf_v1(std::string_view leaf) noexcept {
    if (leaf == DISTRIBUTED_SIEVE_WAVE_MERGE_COMMIT_RECORD_LEAF) {
        return DistributedSieveParsedWaveMergeCommitLeafV1{.pending = false};
    }
    if (leaf == DISTRIBUTED_SIEVE_WAVE_MERGE_COMMIT_RECORD_PENDING_LEAF) {
        return DistributedSieveParsedWaveMergeCommitLeafV1{.pending = true};
    }
    return std::nullopt;
}

std::optional<DistributedSieveParsedCleanupRecordLeafV1>
parse_distributed_sieve_cleanup_record_leaf_v1(std::string_view leaf) noexcept {
    bool pending = false;
    if (leaf.ends_with(DISTRIBUTED_SIEVE_ROOT_RECORD_PENDING_SUFFIX)) {
        pending = true;
        leaf.remove_suffix(DISTRIBUTED_SIEVE_ROOT_RECORD_PENDING_SUFFIX.size());
    }

    const auto parse_worker = [&](std::string_view prefix,
                                  DistributedSieveCleanupRecordCoordinateKindV1 kind)
        -> std::optional<DistributedSieveParsedCleanupRecordLeafV1> {
        const std::size_t expected_size =
            prefix.size() + DISTRIBUTED_SIEVE_WORKER_ATTEMPT_DECIMAL_WIDTH_V1;
        if (leaf.size() != expected_size || !leaf.starts_with(prefix)) {
            return std::nullopt;
        }
        const std::size_t cursor = prefix.size();
        if (leaf[cursor] < '0' || leaf[cursor] > '9' || leaf[cursor + 1U] < '0' ||
            leaf[cursor + 1U] > '9') {
            return std::nullopt;
        }
        const std::uint32_t ordinal = static_cast<std::uint32_t>(leaf[cursor] - '0') * 10U +
                                      static_cast<std::uint32_t>(leaf[cursor + 1U] - '0');
        if (ordinal >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) {
            return std::nullopt;
        }
        return DistributedSieveParsedCleanupRecordLeafV1{
            .kind = kind,
            .manifest_order_ordinal = ordinal,
            .pending = pending,
        };
    };

    if (auto parsed =
            parse_worker(DISTRIBUTED_SIEVE_CLEANUP_AUTHORIZED_WORKER_RECORD_PREFIX,
                         DistributedSieveCleanupRecordCoordinateKindV1::authorized_worker);
        parsed.has_value()) {
        return parsed;
    }
    if (auto parsed = parse_worker(DISTRIBUTED_SIEVE_CLEANUP_COMPLETED_WORKER_RECORD_PREFIX,
                                   DistributedSieveCleanupRecordCoordinateKindV1::completed_worker);
        parsed.has_value()) {
        return parsed;
    }
    if (leaf == DISTRIBUTED_SIEVE_CLEANUP_AUTHORIZED_MERGED_RECORD_LEAF) {
        return DistributedSieveParsedCleanupRecordLeafV1{
            .kind = DistributedSieveCleanupRecordCoordinateKindV1::authorized_merged,
            .manifest_order_ordinal = std::nullopt,
            .pending = pending,
        };
    }
    if (leaf == DISTRIBUTED_SIEVE_CLEANUP_COMPLETED_MERGED_RECORD_LEAF) {
        return DistributedSieveParsedCleanupRecordLeafV1{
            .kind = DistributedSieveCleanupRecordCoordinateKindV1::completed_merged,
            .manifest_order_ordinal = std::nullopt,
            .pending = pending,
        };
    }
    return std::nullopt;
}

std::optional<DistributedSieveWorkerCleanupRecordNamesV1>
distributed_sieve_worker_cleanup_record_names_v1(std::uint32_t manifest_order_ordinal) {
    if (manifest_order_ordinal >= DISTRIBUTED_SIEVE_PROTOCOL_MAX_CHUNKS) {
        return std::nullopt;
    }
    const std::array digits{
        decimal_digit(manifest_order_ordinal / 10U),
        decimal_digit(manifest_order_ordinal % 10U),
    };
    const auto make_canonical = [&](std::string_view prefix) {
        std::string leaf;
        leaf.reserve(prefix.size() + digits.size());
        leaf.append(prefix);
        leaf.append(digits.data(), digits.size());
        return leaf;
    };

    DistributedSieveWorkerCleanupRecordNamesV1 names;
    names.authorization_canonical_record_leaf =
        make_canonical(DISTRIBUTED_SIEVE_CLEANUP_AUTHORIZED_WORKER_RECORD_PREFIX);
    names.authorization_pending_record_leaf = names.authorization_canonical_record_leaf;
    names.authorization_pending_record_leaf.append(DISTRIBUTED_SIEVE_ROOT_RECORD_PENDING_SUFFIX);
    names.completion_canonical_record_leaf =
        make_canonical(DISTRIBUTED_SIEVE_CLEANUP_COMPLETED_WORKER_RECORD_PREFIX);
    names.completion_pending_record_leaf = names.completion_canonical_record_leaf;
    names.completion_pending_record_leaf.append(DISTRIBUTED_SIEVE_ROOT_RECORD_PENDING_SUFFIX);
    return names;
}

DistributedSieveWorkerCleanupRecordLeafLoadResultV1
load_distributed_sieve_worker_cleanup_record_leaf_v1(int wave_root_fd, std::string_view leaf,
                                                     std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return DistributedSieveWorkerCleanupRecordLeafLoadResultV1{std::nullopt,
                                                                   std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = status;
        return fail_with(std::move(failure));
    };

    try {
        const auto parsed = parse_distributed_sieve_cleanup_record_leaf_v1(leaf);
        if (!parsed.has_value() || !parsed->manifest_order_ordinal.has_value() ||
            (parsed->kind != DistributedSieveCleanupRecordCoordinateKindV1::authorized_worker &&
             parsed->kind != DistributedSieveCleanupRecordCoordinateKindV1::completed_worker)) {
            return conflict();
        }
        const auto names =
            distributed_sieve_worker_cleanup_record_names_v1(*parsed->manifest_order_ordinal);
        if (!names.has_value()) {
            return conflict();
        }
        const std::string* expected_leaf = nullptr;
        switch (parsed->kind) {
        case DistributedSieveCleanupRecordCoordinateKindV1::authorized_worker:
            expected_leaf = parsed->pending ? &names->authorization_pending_record_leaf
                                            : &names->authorization_canonical_record_leaf;
            break;
        case DistributedSieveCleanupRecordCoordinateKindV1::completed_worker:
            expected_leaf = parsed->pending ? &names->completion_pending_record_leaf
                                            : &names->completion_canonical_record_leaf;
            break;
        case DistributedSieveCleanupRecordCoordinateKindV1::authorized_merged:
        case DistributedSieveCleanupRecordCoordinateKindV1::completed_merged:
            return conflict();
        }
        if (expected_leaf == nullptr || leaf != *expected_leaf) {
            return conflict();
        }

#if defined(_WIN32)
        (void)wave_root_fd;
        (void)creator_process_id;
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        auto loaded = read_immutable_protocol_record_leaf(
            wave_root_fd, expected_leaf->c_str(), creator_process_id,
            DistributedSieveWaveStoreStatus::namespace_conflict,
            DistributedSieveWaveStoreStatus::namespace_conflict);
        if (!loaded) {
            return fail_with(std::move(loaded.diagnostic));
        }
        auto decoded = decode_distributed_sieve_record(*loaded.bytes);
        if (!decoded) {
            return protocol_conflict(decoded.status);
        }

        DistributedSieveWorkerCleanupRecordLeafWitnessV1 witness{
            .coordinate = *parsed,
            .bytes = std::move(*loaded.bytes),
            .snapshot = *loaded.snapshot,
        };
        switch (parsed->kind) {
        case DistributedSieveCleanupRecordCoordinateKindV1::authorized_worker: {
            auto* authorization = std::get_if<ArtifactCleanupAuthorizedV1>(&*decoded.value);
            if (authorization == nullptr ||
                authorization->authorizer != CleanupAuthorizerKindV1::merge_commit_worker ||
                authorization->artifact_kind != CleanupArtifactKindV1::worker ||
                authorization->manifest_order_ordinal != *parsed->manifest_order_ordinal) {
                return conflict();
            }
            witness.record = std::move(*authorization);
            break;
        }
        case DistributedSieveCleanupRecordCoordinateKindV1::completed_worker: {
            auto* completion = std::get_if<ArtifactCleanupCompletedV1>(&*decoded.value);
            if (completion == nullptr) {
                return conflict();
            }
            witness.record = std::move(*completion);
            break;
        }
        case DistributedSieveCleanupRecordCoordinateKindV1::authorized_merged:
        case DistributedSieveCleanupRecordCoordinateKindV1::completed_merged:
            return conflict();
        }
        return {std::move(witness), {}};
#endif
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

DistributedSieveWorkerCleanupPrefixClassificationResultV1
classify_distributed_sieve_worker_cleanup_prefix_v1(
    const WaveManifestV1& manifest, const WaveMergeCommitV1& commit,
    std::span<const WorkerHandoffV1* const> worker_handoffs,
    std::span<const DistributedSieveWorkerCleanupRecordLeafWitnessV1> records) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return DistributedSieveWorkerCleanupPrefixClassificationResultV1{std::nullopt,
                                                                         std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        failure.protocol_status = status;
        return fail_with(std::move(failure));
    };

    struct MutableCoordinate final {
        std::uint32_t ordinal = 0;
        std::optional<ArtifactCleanupAuthorizedV1> authorization;
        std::vector<std::byte> authorization_bytes;
        std::optional<durable_record::RecordSnapshot> authorization_canonical_snapshot;
        std::optional<durable_record::RecordSnapshot> authorization_pending_snapshot;
        std::optional<ArtifactCleanupCompletedV1> completion;
        std::vector<std::byte> completion_bytes;
        std::optional<durable_record::RecordSnapshot> completion_canonical_snapshot;
        std::optional<durable_record::RecordSnapshot> completion_pending_snapshot;
    };

    try {
        const auto manifest_status =
            validate_distributed_sieve_record(DistributedSieveProtocolRecordV1{manifest}, true);
        if (!manifest_status) {
            return protocol_conflict(manifest_status);
        }
        const auto commit_status =
            validate_distributed_sieve_record(DistributedSieveProtocolRecordV1{commit}, true);
        if (!commit_status) {
            return protocol_conflict(commit_status);
        }
        if (manifest.chunks.size() != commit.chunks.size() ||
            manifest.chunks.size() != worker_handoffs.size() ||
            commit.manifest_digest != manifest.self_digest ||
            commit.work_digest != manifest.work_sha256 ||
            commit.merge_policy_version != manifest.merge_policy_version) {
            return conflict();
        }

        for (std::size_t index = 0; index < manifest.chunks.size(); ++index) {
            const auto& chunk = manifest.chunks[index];
            const auto& summary = commit.chunks[index].input;
            const WorkerHandoffV1* handoff = worker_handoffs[index];
            if (chunk.chunk_id != index || summary.chunk_id != chunk.chunk_id ||
                summary.sq_begin != chunk.sq_begin || summary.sq_end != chunk.sq_end) {
                return conflict();
            }
            if (summary.disposition == ChunkDispositionV1::empty) {
                if (chunk.sq_begin != chunk.sq_end || handoff != nullptr) {
                    return conflict();
                }
                continue;
            }
            if (summary.disposition != ChunkDispositionV1::handoff ||
                chunk.sq_begin >= chunk.sq_end || handoff == nullptr) {
                return conflict();
            }
            const auto handoff_status =
                validate_distributed_sieve_record(DistributedSieveProtocolRecordV1{*handoff}, true);
            if (!handoff_status) {
                return protocol_conflict(handoff_status);
            }
            if (handoff->self_digest != summary.handoff_digest ||
                handoff->manifest_digest != manifest.self_digest ||
                handoff->work_digest != manifest.work_sha256 ||
                handoff->wave_id != manifest.wave_id || handoff->chunk_id != chunk.chunk_id ||
                handoff->sq_begin != chunk.sq_begin || handoff->sq_end != chunk.sq_end ||
                handoff->next_sq_index != summary.next_sq_index ||
                handoff->processed_sq_count != summary.processed_sq_count ||
                handoff->completion_reason != summary.completion_reason ||
                handoff->attempt_ordinal + 1U != summary.durable_attempt_count ||
                handoff->attempt_started_digest != summary.last_attempt_digest ||
                handoff->lease.lease_id != summary.lease_id ||
                handoff->artifact.descriptor.relation_count != summary.raw_relation_count ||
                handoff->artifact.sequence_receipt != summary.sequence_receipt ||
                handoff->artifact.corpus_sha256 != summary.corpus_sha256 ||
                handoff->relation_count != summary.raw_relation_count) {
                return conflict();
            }
        }

        std::vector<MutableCoordinate> grouped;
        grouped.reserve(records.size());
        for (const auto& leaf : records) {
            if (!leaf.coordinate.manifest_order_ordinal.has_value() ||
                *leaf.coordinate.manifest_order_ordinal >= manifest.chunks.size() ||
                (leaf.coordinate.kind !=
                     DistributedSieveCleanupRecordCoordinateKindV1::authorized_worker &&
                 leaf.coordinate.kind !=
                     DistributedSieveCleanupRecordCoordinateKindV1::completed_worker)) {
                return conflict();
            }
            const std::uint32_t ordinal = *leaf.coordinate.manifest_order_ordinal;
            auto position =
                std::lower_bound(grouped.begin(), grouped.end(), ordinal,
                                 [](const MutableCoordinate& candidate, std::uint32_t value) {
                                     return candidate.ordinal < value;
                                 });
            if (position == grouped.end() || position->ordinal != ordinal) {
                position = grouped.insert(position, MutableCoordinate{.ordinal = ordinal});
            }

            if (leaf.coordinate.kind ==
                DistributedSieveCleanupRecordCoordinateKindV1::authorized_worker) {
                const auto* authorization = std::get_if<ArtifactCleanupAuthorizedV1>(&leaf.record);
                auto& target_snapshot = leaf.coordinate.pending
                                            ? position->authorization_pending_snapshot
                                            : position->authorization_canonical_snapshot;
                if (authorization == nullptr || target_snapshot.has_value() ||
                    authorization->authorizer != CleanupAuthorizerKindV1::merge_commit_worker ||
                    authorization->artifact_kind != CleanupArtifactKindV1::worker ||
                    authorization->manifest_order_ordinal != ordinal) {
                    return conflict();
                }
                if (position->authorization.has_value() &&
                    position->authorization_bytes != leaf.bytes) {
                    return conflict();
                }
                if (!position->authorization.has_value()) {
                    position->authorization = *authorization;
                    position->authorization_bytes = leaf.bytes;
                }
                target_snapshot = leaf.snapshot;
                continue;
            }

            const auto* completion = std::get_if<ArtifactCleanupCompletedV1>(&leaf.record);
            auto& target_snapshot = leaf.coordinate.pending
                                        ? position->completion_pending_snapshot
                                        : position->completion_canonical_snapshot;
            if (completion == nullptr || target_snapshot.has_value()) {
                return conflict();
            }
            if (position->completion.has_value() && position->completion_bytes != leaf.bytes) {
                return conflict();
            }
            if (!position->completion.has_value()) {
                position->completion = *completion;
                position->completion_bytes = leaf.bytes;
            }
            target_snapshot = leaf.snapshot;
        }

        DistributedSieveWorkerCleanupPrefixWitnessV1 prefix;
        prefix.coordinates.reserve(grouped.size());
        for (auto& coordinate : grouped) {
            if (!coordinate.authorization.has_value()) {
                return conflict();
            }
            const std::size_t ordinal = coordinate.ordinal;
            if (commit.chunks[ordinal].input.disposition != ChunkDispositionV1::handoff ||
                worker_handoffs[ordinal] == nullptr) {
                return conflict();
            }
            const auto authorization_status = validate_artifact_cleanup_dependencies(
                manifest, commit, {}, nullptr, nullptr, *coordinate.authorization,
                worker_handoffs[ordinal], nullptr);
            if (!authorization_status) {
                return protocol_conflict(authorization_status);
            }

            const bool authorization_canonical =
                coordinate.authorization_canonical_snapshot.has_value();
            const bool authorization_pending =
                coordinate.authorization_pending_snapshot.has_value();
            const bool completion_canonical = coordinate.completion_canonical_snapshot.has_value();
            const bool completion_pending = coordinate.completion_pending_snapshot.has_value();
            if ((!authorization_canonical && !authorization_pending) ||
                (coordinate.completion.has_value() !=
                 (completion_canonical || completion_pending))) {
                return conflict();
            }

            DistributedSieveWorkerCleanupPrefixStateV1 state;
            if (!coordinate.completion.has_value()) {
                if (authorization_canonical && authorization_pending) {
                    state =
                        DistributedSieveWorkerCleanupPrefixStateV1::authorization_identical_dual;
                } else if (authorization_canonical) {
                    state =
                        DistributedSieveWorkerCleanupPrefixStateV1::authorization_canonical_only;
                } else {
                    state = DistributedSieveWorkerCleanupPrefixStateV1::authorization_pending_only;
                }
            } else {
                if (!authorization_canonical || authorization_pending) {
                    return conflict();
                }
                const auto completion_status = validate_artifact_cleanup_completion_dependency(
                    *coordinate.authorization, *coordinate.completion);
                if (!completion_status) {
                    return protocol_conflict(completion_status);
                }
                if (completion_canonical && completion_pending) {
                    state = DistributedSieveWorkerCleanupPrefixStateV1::completion_identical_dual;
                } else if (completion_canonical) {
                    state = DistributedSieveWorkerCleanupPrefixStateV1::completed;
                } else {
                    state = DistributedSieveWorkerCleanupPrefixStateV1::completion_pending_only;
                }
            }

            prefix.coordinates.push_back({
                .manifest_order_ordinal = coordinate.ordinal,
                .state = state,
                .authorization = std::move(*coordinate.authorization),
                .authorization_bytes = std::move(coordinate.authorization_bytes),
                .authorization_canonical_snapshot = coordinate.authorization_canonical_snapshot,
                .authorization_pending_snapshot = coordinate.authorization_pending_snapshot,
                .completion = std::move(coordinate.completion),
                .completion_bytes = std::move(coordinate.completion_bytes),
                .completion_canonical_snapshot = coordinate.completion_canonical_snapshot,
                .completion_pending_snapshot = coordinate.completion_pending_snapshot,
            });
        }

        std::size_t coordinate_index = 0;
        bool frontier_seen = false;
        for (std::size_t manifest_slot = 0; manifest_slot < manifest.chunks.size();
             ++manifest_slot) {
            const bool has_coordinate =
                coordinate_index < prefix.coordinates.size() &&
                prefix.coordinates[coordinate_index].manifest_order_ordinal == manifest_slot;
            const auto disposition = commit.chunks[manifest_slot].input.disposition;
            if (disposition == ChunkDispositionV1::empty) {
                if (has_coordinate) {
                    return conflict();
                }
                continue;
            }
            if (disposition != ChunkDispositionV1::handoff) {
                return conflict();
            }

            if (!has_coordinate) {
                if (!frontier_seen) {
                    prefix.frontier_manifest_order_ordinal =
                        static_cast<std::uint32_t>(manifest_slot);
                    frontier_seen = true;
                }
                continue;
            }
            const auto state = prefix.coordinates[coordinate_index].state;
            if (frontier_seen) {
                return conflict();
            }
            if (state == DistributedSieveWorkerCleanupPrefixStateV1::completed) {
                ++prefix.completed_worker_count;
                ++coordinate_index;
                continue;
            }
            prefix.frontier_manifest_order_ordinal = static_cast<std::uint32_t>(manifest_slot);
            prefix.active_manifest_order_ordinal = static_cast<std::uint32_t>(manifest_slot);
            frontier_seen = true;
            ++coordinate_index;
        }
        if (coordinate_index != prefix.coordinates.size()) {
            return conflict();
        }
        return {std::move(prefix), {}};
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

struct DistributedSieveWaveStore::State final {
    std::filesystem::path absolute_root;
    std::filesystem::path root_leaf;
    std::vector<std::string> parent_components;
    WaveManifestV1 manifest;
    util::durable_immutable_record::RecordSnapshot manifest_snapshot;
    NativeIdentityV1 root_identity;
    NativeIdentityV1 lock_identity;
    std::vector<std::byte> manifest_bytes;
    std::optional<ManifestBoundInventoryWitnessResult> merge_raw_recovery_open_anchor;
    std::uint64_t creator_process_id = 0;
    mutable std::atomic_bool merge_raw_recovery_completed = false;
    mutable std::atomic_flag worker_coordinator_claimed = ATOMIC_FLAG_INIT;
    mutable std::atomic_flag private_lease_root_action_claimed = ATOMIC_FLAG_INIT;
#if !defined(_WIN32)
    int parent_fd = -1;
    int root_fd = -1;
    int lock_fd = -1;
#endif

    ~State();
};

DistributedSieveWaveStore::State::~State() {
#if !defined(_WIN32)
    if (lock_fd >= 0) {
        (void)::close(lock_fd);
    }
    if (root_fd >= 0) {
        (void)::close(root_fd);
    }
    if (parent_fd >= 0) {
        (void)::close(parent_fd);
    }
#endif
}

class MergePreparedAdmissionRevalidatorAuthorityV1 final {
public:
    [[nodiscard]] static private_lease::PrivateHandoffPublicationAdoptionRevalidatorV1
    bind(private_lease::PrivateHandoffPublicationAdoptionRevalidatorV1::Validate validate,
         void* context) noexcept {
        return private_lease::PrivateHandoffPublicationAdoptionRevalidatorV1(validate, context);
    }

    [[nodiscard]] static DistributedSieveWaveStoreDiagnostic
    revalidate_wave_store_authority(const DistributedSieveWaveStore& store) noexcept {
        return store.revalidate_authority();
    }

    [[nodiscard]] static int root_fd(const DistributedSieveWaveStore& store) noexcept {
#if defined(_WIN32)
        (void)store;
        return -1;
#else
        return store.state_ != nullptr ? store.state_->root_fd : -1;
#endif
    }

    [[nodiscard]] static const std::filesystem::path&
    absolute_root(const DistributedSieveWaveStore& store) noexcept {
        return store.state_->absolute_root;
    }

    [[nodiscard]] static const WaveManifestV1&
    manifest(const DistributedSieveWaveStore& store) noexcept {
        return store.state_->manifest;
    }

    [[nodiscard]] static std::uint64_t
    creator_process_id(const DistributedSieveWaveStore& store) noexcept {
        return store.state_ != nullptr ? store.state_->creator_process_id : 0;
    }
};

/// Narrow source-private view used only by the worker-cleanup cold-open
/// admission. Keeping it separate prevents the merge-prepared adoption
/// revalidator from becoming a general WaveStore accessor.
class WorkerCleanupRootRevalidatorAuthorityV1 final {
public:
    [[nodiscard]] static DistributedSieveWaveStoreDiagnostic
    revalidate_authority(const DistributedSieveWaveStore& store) noexcept {
        return store.revalidate_authority();
    }

    [[nodiscard]] static int root_fd(const DistributedSieveWaveStore& store) noexcept {
#if defined(_WIN32)
        (void)store;
        return -1;
#else
        return store.state_ != nullptr ? store.state_->root_fd : -1;
#endif
    }

    [[nodiscard]] static const std::filesystem::path&
    absolute_root(const DistributedSieveWaveStore& store) noexcept {
        return store.state_->absolute_root;
    }

    [[nodiscard]] static const WaveManifestV1&
    manifest(const DistributedSieveWaveStore& store) noexcept {
        return store.state_->manifest;
    }
};

DistributedSievePrivateLeaseBaseLockAt::DistributedSievePrivateLeaseBaseLockAt(
    int root_fd, std::string leaf, std::uint64_t creator_process_id) noexcept
    : root_fd_(root_fd), leaf_(std::move(leaf)), creator_process_id_(creator_process_id) {}

DistributedSievePrivateLeaseBaseLockAt::~DistributedSievePrivateLeaseBaseLockAt() noexcept {
#if !defined(_WIN32)
    if (lock_fd_ >= 0) {
        // Never issue LOCK_UN: after fork the parent and child descriptors
        // refer to the same open-file description. Close-only preserves the
        // parent's flock until its final descriptor is gone.
        (void)::close(lock_fd_);
        lock_fd_ = -1;
    }
#endif
}

gnfs::relation::OOCPrivateHandoffAdoptionResult
DistributedSievePrivateLeaseBaseLockAt::adopt_exact_private_handoff(
    const std::filesystem::path& base_path) const noexcept {
    const bool owned = owned_by_current_process();
    return private_lease::adopt_private_handoff_with_borrowed_base_lock_v1(
        base_path, private_lease::OOCPrivateHandoffBorrowedBaseLockV1(
                       owned ? root_fd_ : -1, owned ? lock_fd_ : -1, leaf_,
                       relation_identity(identity_), owned ? creator_process_id_ : 0));
}

gnfs::relation::OOCCleanupResult
DistributedSievePrivateLeaseBaseLockAt::recover_exact_merge_raw_writer_private_lease(
    const std::filesystem::path& base_path,
    const DistributedSieveMergeRawWriterRecoveryInventoryWitnessV1& expected,
    gnfs::relation::OOCPrivateLeaseTestHooks hooks) const noexcept {
    const auto lower_phase = [](DistributedSieveMergeRawWriterRecoveryPhaseV1 phase) noexcept {
        using Upper = DistributedSieveMergeRawWriterRecoveryPhaseV1;
        using Lower = private_lease::OOCPreactiveLeaseRecoveryPhaseV1;
        switch (phase) {
        case Upper::FinalDirectoryRawPair:
            return Lower::FinalDirectoryRawPair;
        case Upper::StagingDirectoryRawPair:
            return Lower::StagingDirectoryRawPair;
        case Upper::StagingDirectoryOwnerOnly:
            return Lower::StagingDirectoryOwnerOnly;
        case Upper::StagingDirectoryOwnerRemoved:
            return Lower::StagingDirectoryOwnerRemoved;
        case Upper::DirectoryAbsentReservedAndOwned:
            return Lower::DirectoryAbsentReservedAndOwned;
        case Upper::DirectoryAbsentOwnedOnly:
            return Lower::DirectoryAbsentOwnedOnly;
        case Upper::Count:
            return Lower::Count;
        }
        return Lower::Count;
    };
    const auto lower_file = [](const DistributedSieveMergeRawWriterLeafInventoryWitnessV1& leaf) {
        return private_lease::OOCPreactiveLeaseRecoveryFileExpectationV1{
            .identity = relation_identity(leaf.identity),
            .extent = leaf.extent,
        };
    };
    private_lease::OOCPreactiveLeaseRecoveryExpectationV1 lower{
        .phase = lower_phase(expected.phase),
        .lease_id = expected.lease_id,
        .directory_identity = relation_identity(expected.directory_identity),
        .owner_marker_identity = relation_identity(expected.owner_marker_identity),
        .owned_marker_identity = relation_identity(expected.owned_marker_identity),
        .reserved_marker_identity =
            expected.reserved_marker_identity.has_value()
                ? std::optional{relation_identity(*expected.reserved_marker_identity)}
                : std::nullopt,
        .index =
            expected.index.has_value() ? std::optional{lower_file(*expected.index)} : std::nullopt,
        .data =
            expected.data.has_value() ? std::optional{lower_file(*expected.data)} : std::nullopt,
    };
    const bool owned = owned_by_current_process();
    return private_lease::recover_private_lease_with_borrowed_base_lock_v1(
        base_path,
        private_lease::OOCPrivateLeaseRecoveryBorrowedBaseLockV1(
            owned ? root_fd_ : -1, owned ? lock_fd_ : -1, leaf_, relation_identity(identity_),
            owned ? creator_process_id_ : 0),
        std::move(lower), hooks);
}

bool DistributedSievePrivateLeaseBaseLockAt::matches_exact_binding(
    std::string_view base_lock_leaf, const NativeIdentityV1& expected_identity) const noexcept {
    return owned_by_current_process() && base_lock_leaf == leaf_ && expected_identity == identity_;
}

std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>
DistributedSievePrivateLeaseBaseLockAt::create_new_locked(
    int root_fd, std::string leaf, std::uint64_t creator_process_id,
    DistributedSieveWaveStoreDiagnostic& outcome) noexcept {
    outcome = {};
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    (void)root_fd;
    (void)leaf;
    (void)creator_process_id;
    outcome =
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
    return nullptr;
#else
    if (root_fd < 0 || !process_matches(creator_process_id) ||
        !valid_private_lease_base_lock_leaf(leaf)) {
        outcome = process_matches(creator_process_id)
                      ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                   invalid_argument_error())
                      : process_mismatch();
        return nullptr;
    }

    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> lock;
    try {
        lock.reset(new DistributedSievePrivateLeaseBaseLockAt(root_fd, std::move(leaf),
                                                              creator_process_id));
    } catch (const std::bad_alloc&) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                             std::make_error_code(std::errc::not_enough_memory));
        return nullptr;
    } catch (...) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                             std::make_error_code(std::errc::io_error));
        return nullptr;
    }

    struct stat existing{};
    if (fstatat_retrying_eintr(root_fd, lock->leaf_.c_str(), existing) == 0) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
        return nullptr;
    }
    const int absence_errno = errno;
    if (absence_errno != ENOENT) {
        outcome = private_lease_base_lock_open_failure(absence_errno);
        return nullptr;
    }
    if (!process_matches(creator_process_id)) {
        outcome = process_mismatch();
        return nullptr;
    }

    const int descriptor = openat_retrying_eintr(
        root_fd, lock->leaf_.c_str(),
        O_RDWR | O_NONBLOCK | O_CREAT | O_EXCL | O_NOFOLLOW | O_CLOEXEC, 0600);
    if (descriptor < 0) {
        outcome = private_lease_base_lock_open_failure(errno);
        return nullptr;
    }
    UniqueFd held(descriptor);

    int chmod_result = -1;
    do {
        chmod_result = ::fchmod(held.get(), 0600);
    } while (chmod_result != 0 && errno == EINTR);
    if (chmod_result != 0) {
        outcome =
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
        return nullptr;
    }

    const auto initially_bound = validate_private_lease_base_lock_binding(
        root_fd, held.get(), lock->leaf_, creator_process_id);
    if (!initially_bound) {
        outcome = initially_bound.diagnostic;
        return nullptr;
    }
    if (const auto locked = lock_private_lease_base_lock(held.get());
        locked.status != DistributedSieveWaveStoreStatus::ready) {
        outcome = locked;
        return nullptr;
    }
    const auto finally_bound = validate_private_lease_base_lock_binding(
        root_fd, held.get(), lock->leaf_, creator_process_id, *initially_bound.identity);
    if (!finally_bound) {
        outcome = finally_bound.diagnostic;
        return nullptr;
    }

    lock->identity_ = *finally_bound.identity;
    lock->lock_fd_ = held.release();
    return lock;
#endif
}

std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>
DistributedSievePrivateLeaseBaseLockAt::open_existing_locked(
    int root_fd, std::string leaf, const NativeIdentityV1& expected_identity,
    std::uint64_t creator_process_id, DistributedSieveWaveStoreDiagnostic& outcome) noexcept {
    outcome = {};
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    (void)root_fd;
    (void)leaf;
    (void)expected_identity;
    (void)creator_process_id;
    outcome =
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
    return nullptr;
#else
    if (root_fd < 0 || !process_matches(creator_process_id) ||
        !valid_private_lease_base_lock_leaf(leaf)) {
        outcome = process_matches(creator_process_id)
                      ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                   invalid_argument_error())
                      : process_mismatch();
        return nullptr;
    }

    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> lock;
    try {
        lock.reset(new DistributedSievePrivateLeaseBaseLockAt(root_fd, std::move(leaf),
                                                              creator_process_id));
    } catch (const std::bad_alloc&) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                             std::make_error_code(std::errc::not_enough_memory));
        return nullptr;
    } catch (...) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                             std::make_error_code(std::errc::io_error));
        return nullptr;
    }

    if (!process_matches(creator_process_id)) {
        outcome = process_mismatch();
        return nullptr;
    }
    const int descriptor = openat_retrying_eintr(root_fd, lock->leaf_.c_str(),
                                                 O_RDWR | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        outcome = private_lease_base_lock_open_failure(errno);
        return nullptr;
    }
    UniqueFd held(descriptor);

    const auto initially_bound = validate_private_lease_base_lock_binding(
        root_fd, held.get(), lock->leaf_, creator_process_id, expected_identity);
    if (!initially_bound) {
        outcome = initially_bound.diagnostic;
        return nullptr;
    }
    if (const auto locked = lock_private_lease_base_lock(held.get());
        locked.status != DistributedSieveWaveStoreStatus::ready) {
        outcome = locked;
        return nullptr;
    }
    const auto finally_bound = validate_private_lease_base_lock_binding(
        root_fd, held.get(), lock->leaf_, creator_process_id, expected_identity);
    if (!finally_bound) {
        outcome = finally_bound.diagnostic;
        return nullptr;
    }

    lock->identity_ = *finally_bound.identity;
    lock->lock_fd_ = held.release();
    return lock;
#endif
}

std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>
DistributedSievePrivateLeaseBaseLockAt::duplicate_same_open_file_description(
    DistributedSieveWaveStoreDiagnostic& outcome) const noexcept {
    outcome = {};
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    outcome =
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
    return nullptr;
#else
    if (const auto original = revalidate();
        original.status != DistributedSieveWaveStoreStatus::ready) {
        outcome = original;
        return nullptr;
    }

    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> duplicate;
    try {
        duplicate.reset(
            new DistributedSievePrivateLeaseBaseLockAt(root_fd_, leaf_, creator_process_id_));
    } catch (const std::bad_alloc&) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                             std::make_error_code(std::errc::not_enough_memory));
        return nullptr;
    } catch (...) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                             std::make_error_code(std::errc::io_error));
        return nullptr;
    }

    int descriptor = -1;
    do {
        descriptor = ::fcntl(lock_fd_, F_DUPFD_CLOEXEC, 0);
    } while (descriptor < 0 && errno == EINTR);
    if (descriptor < 0) {
        outcome = private_lease_base_lock_open_failure(errno);
        return nullptr;
    }
    UniqueFd held(descriptor);

    const auto initially_bound = validate_private_lease_base_lock_binding(
        root_fd_, held.get(), leaf_, creator_process_id_, identity_);
    if (!initially_bound) {
        outcome = initially_bound.diagnostic;
        return nullptr;
    }
    duplicate->identity_ = *initially_bound.identity;
    duplicate->lock_fd_ = held.release();
    if (const auto original = revalidate();
        original.status != DistributedSieveWaveStoreStatus::ready) {
        outcome = original;
        return nullptr;
    }
    if (const auto retained = duplicate->revalidate();
        retained.status != DistributedSieveWaveStoreStatus::ready) {
        outcome = retained;
        return nullptr;
    }
    return duplicate;
#endif
}

DistributedSieveWaveStoreDiagnostic
DistributedSievePrivateLeaseBaseLockAt::revalidate() const noexcept {
    if (!owned_by_current_process()) {
        return invalidated_.load(std::memory_order_acquire)
                   ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                protocol_error())
                   : process_mismatch();
    }
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    const auto validated = validate_private_lease_base_lock_binding(root_fd_, lock_fd_, leaf_,
                                                                    creator_process_id_, identity_);
    if (!validated) {
        invalidated_.store(true, std::memory_order_release);
        return validated.diagnostic;
    }
    return {};
#endif
}

DistributedSieveWaveStoreDiagnostic DistributedSievePrivateLeaseBaseLockAt::synchronize(
    DistributedSievePrivateLeaseBaseLockTestHooks hooks) const noexcept {
    if (const auto before = revalidate(); before.status != DistributedSieveWaveStoreStatus::ready) {
        return before;
    }
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    std::error_code sync_error;
    if (const auto injected = invoke_private_lease_base_lock_sync_hook(
            hooks, DistributedSievePrivateLeaseBaseLockSyncPoint::TargetInitial,
            creator_process_id_);
        injected.status != DistributedSieveWaveStoreStatus::ready) {
        return injected;
    }
    if (!sync_descriptor(lock_fd_, sync_error)) {
        return diagnostic(DistributedSieveWaveStoreStatus::durability_failed, sync_error);
    }
    if (const auto after_lock_sync = revalidate();
        after_lock_sync.status != DistributedSieveWaveStoreStatus::ready) {
        return after_lock_sync;
    }
    if (const auto injected = invoke_private_lease_base_lock_sync_hook(
            hooks, DistributedSievePrivateLeaseBaseLockSyncPoint::RootDirectory,
            creator_process_id_);
        injected.status != DistributedSieveWaveStoreStatus::ready) {
        return injected;
    }
    if (!sync_descriptor(root_fd_, sync_error)) {
        return diagnostic(DistributedSieveWaveStoreStatus::durability_failed, sync_error);
    }
    if (const auto after_root_sync = revalidate();
        after_root_sync.status != DistributedSieveWaveStoreStatus::ready) {
        return after_root_sync;
    }
    if (const auto injected = invoke_private_lease_base_lock_sync_hook(
            hooks, DistributedSievePrivateLeaseBaseLockSyncPoint::TargetFinal, creator_process_id_);
        injected.status != DistributedSieveWaveStoreStatus::ready) {
        return injected;
    }
    if (!sync_descriptor(lock_fd_, sync_error)) {
        return diagnostic(DistributedSieveWaveStoreStatus::durability_failed, sync_error);
    }
    return revalidate();
#endif
}

bool DistributedSievePrivateLeaseBaseLockAt::owned_by_current_process() const noexcept {
    return root_fd_ >= 0 && lock_fd_ >= 0 && creator_process_id_ != 0 && !leaf_.empty() &&
           !invalidated_.load(std::memory_order_acquire) &&
           creator_process_id_ == static_cast<std::uint64_t>(gnfs::util::process_id());
}

const NativeIdentityV1& DistributedSievePrivateLeaseBaseLockAt::identity() const noexcept {
    return identity_;
}

void DistributedSievePrivateLeaseBaseLockAt::invalidate() const noexcept {
    invalidated_.store(true, std::memory_order_release);
}

DistributedSieveExternalCleanupAuthorizationState::
    DistributedSieveExternalCleanupAuthorizationState(
        std::shared_ptr<const DistributedSieveWaveStore::State> wave_store_state) noexcept
    : wave_store_state_(std::move(wave_store_state)),
      creator_process_id_(wave_store_state_ != nullptr ? wave_store_state_->creator_process_id
                                                       : 0) {}

bool distributed_sieve_external_cleanup_authorization_state_owned_by_current_process(
    const DistributedSieveExternalCleanupAuthorizationState& state) noexcept {
    return state.owned_by_current_process();
}

DistributedSieveWaveStore::DistributedSieveWaveStore(std::shared_ptr<const State> state) noexcept
    : state_(std::move(state)) {}

DistributedSieveWaveStore::~DistributedSieveWaveStore() = default;

DistributedSieveAdoptedWorkerChunkV1::DistributedSieveAdoptedWorkerChunkV1(
    std::shared_ptr<const void> wave_store_state_anchor, WorkerHandoffV1 handoff,
    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> retained_base_lock,
    std::unique_ptr<relation::OOCPrivateHandoffReader> reader,
    std::uint64_t creator_process_id) noexcept
    : wave_store_state_anchor_(std::move(wave_store_state_anchor)), handoff_(std::move(handoff)),
      retained_base_lock_(std::move(retained_base_lock)), reader_(std::move(reader)),
      creator_process_id_(creator_process_id) {}

DistributedSieveAdoptedWorkerChunkV1::DistributedSieveAdoptedWorkerChunkV1(
    DistributedSieveAdoptedWorkerChunkV1&& other) noexcept
    : wave_store_state_anchor_(std::move(other.wave_store_state_anchor_)),
      handoff_(std::move(other.handoff_)),
      retained_base_lock_(std::move(other.retained_base_lock_)), reader_(std::move(other.reader_)),
      creator_process_id_(std::exchange(other.creator_process_id_, 0)) {}

DistributedSieveAdoptedWorkerChunkV1::~DistributedSieveAdoptedWorkerChunkV1() = default;

bool DistributedSieveAdoptedWorkerChunkV1::valid() const noexcept {
    if (wave_store_state_anchor_ == nullptr || retained_base_lock_ == nullptr ||
        reader_ == nullptr || !reader_->valid() || creator_process_id_ == 0 ||
        !process_matches(creator_process_id_)) {
        return false;
    }
    return retained_base_lock_->owned_by_current_process() &&
           retained_base_lock_->identity() == protocol_identity(reader_->record().lock_identity);
}

const WorkerHandoffV1& DistributedSieveAdoptedWorkerChunkV1::handoff() const noexcept {
    return handoff_;
}

const relation::OOCRelationReader& DistributedSieveAdoptedWorkerChunkV1::reader() const {
    if (!valid()) {
        throw std::logic_error(
            "DistributedSieveAdoptedWorkerChunkV1: reader is moved-from or invalid");
    }
    return reader_->reader();
}

DistributedSieveWorkerCoordinatorClaimV1::DistributedSieveWorkerCoordinatorClaimV1(
    std::shared_ptr<const void> wave_store_state_anchor, std::atomic_flag* gate,
    std::uint64_t creator_process_id) noexcept
    : wave_store_state_anchor_(std::move(wave_store_state_anchor)), gate_(gate),
      creator_process_id_(creator_process_id) {}

DistributedSieveWorkerCoordinatorClaimV1::~DistributedSieveWorkerCoordinatorClaimV1() noexcept {
    if (wave_store_state_anchor_ != nullptr && gate_ != nullptr && creator_process_id_ != 0 &&
        process_matches(creator_process_id_)) {
        gate_->clear(std::memory_order_release);
    }
    gate_ = nullptr;
    creator_process_id_ = 0;
}

bool DistributedSieveWorkerCoordinatorClaimV1::owned_by_current_process() const noexcept {
    return wave_store_state_anchor_ != nullptr && gate_ != nullptr && creator_process_id_ != 0 &&
           process_matches(creator_process_id_) && gate_->test(std::memory_order_acquire);
}

DistributedSievePrivateLeaseRootClaim::DistributedSievePrivateLeaseRootClaim(
    std::shared_ptr<const DistributedSieveWaveStore::State> wave_store_state,
    std::vector<const DistributedSievePrivateLeaseBaseLockAt*> borrowed_worker_base_locks) noexcept
    : wave_store_state_(std::move(wave_store_state)),
      creator_process_id_(wave_store_state_ != nullptr ? wave_store_state_->creator_process_id : 0),
      borrowed_worker_base_locks_(std::move(borrowed_worker_base_locks)) {}

DistributedSievePrivateLeaseRootClaim::~DistributedSievePrivateLeaseRootClaim() noexcept {
    base_lock_at_.reset();
    predecessor_base_locks_.clear();
    if (wave_store_state_ != nullptr && creator_process_id_ != 0 &&
        creator_process_id_ == static_cast<std::uint64_t>(gnfs::util::process_id())) {
        wave_store_state_->private_lease_root_action_claimed.clear(std::memory_order_release);
    }
}

bool DistributedSievePrivateLeaseRootClaim::owned_by_current_process() const noexcept {
    return wave_store_state_ != nullptr && creator_process_id_ != 0 &&
           creator_process_id_ == static_cast<std::uint64_t>(gnfs::util::process_id()) &&
           wave_store_state_->private_lease_root_action_claimed.test(std::memory_order_acquire);
}

DistributedSieveWaveStoreDiagnostic DistributedSievePrivateLeaseRootClaim::revalidate(
    DistributedSievePrivateLeaseRootClaimTestHooks hooks) const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    DistributedSieveWaveStore view(wave_store_state_);
    if (base_lock_at_ == nullptr) {
        if (!borrowed_worker_base_locks_.empty()) {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
            (void)hooks;
            return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                              unsupported_error());
#else
            const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
                std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                    borrowed_worker_base_locks_.data(), borrowed_worker_base_locks_.size())};
            const auto revalidate_bindings = [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                for (const auto* borrowed : borrowed_worker_base_locks_) {
                    if (borrowed == nullptr) {
                        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                          protocol_error());
                    }
                    if (const auto validated = borrowed->revalidate();
                        validated.status != DistributedSieveWaveStoreStatus::ready) {
                        return validated;
                    }
                }
                return view.revalidate_authority();
            };
            if (const auto authority = view.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            if (hooks.after_first_authority_validation != nullptr) {
                hooks.after_first_authority_validation(hooks.context);
            }
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            auto first = capture_manifest_bound_inventory_witness(
                wave_store_state_->root_fd, wave_store_state_->manifest,
                wave_store_state_->absolute_root, creator_process_id_, held_inventory);
            if (!first) {
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return bindings;
                }
                return first.diagnostic;
            }
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            auto confirmed = capture_manifest_bound_inventory_witness(
                wave_store_state_->root_fd, wave_store_state_->manifest,
                wave_store_state_->absolute_root, creator_process_id_, held_inventory);
            if (!confirmed || *confirmed.inventory != *first.inventory ||
                *confirmed.base_lock_identities != *first.base_lock_identities ||
                *confirmed.private_lease_witnesses != *first.private_lease_witnesses) {
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return bindings;
                }
                return confirmed ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : confirmed.diagnostic;
            }
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{}
                                              : process_mismatch();
#endif
        }
        auto validated = view.revalidate();
        if (validated.status != DistributedSieveWaveStoreStatus::ready) {
            return validated;
        }
        return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{}
                                          : process_mismatch();
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    (void)hooks;
    base_lock_at_->invalidate();
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    const auto invalidate_with = [&](DistributedSieveWaveStoreDiagnostic outcome) noexcept
        -> DistributedSieveWaveStoreDiagnostic {
        base_lock_at_->invalidate();
        return outcome;
    };
    const bool worker_target = worker_attempt_names_.has_value();
    const bool merge_target = merge_generation_names_.has_value();
    if (worker_target == merge_target || !expected_private_lease_base_lock_leaves_.has_value() ||
        !expected_private_lease_base_lock_identities_.has_value() ||
        !expected_private_lease_reservation_witnesses_.has_value() ||
        !expected_worker_attempt_record_witnesses_.has_value() ||
        !expected_merge_started_record_witnesses_.has_value() ||
        !expected_chunk_terminal_failure_record_witnesses_.has_value() ||
        expected_private_lease_base_lock_leaves_->size() !=
            expected_private_lease_base_lock_identities_->size() ||
        expected_private_lease_base_lock_leaves_->size() !=
            expected_private_lease_reservation_witnesses_->size()) {
        return invalidate_with(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
    }
    const auto& private_lease_names =
        worker_target
            ? static_cast<const DistributedSievePrivateLeaseNamesV1&>(*worker_attempt_names_)
            : static_cast<const DistributedSievePrivateLeaseNamesV1&>(*merge_generation_names_);
    const auto target_position = std::lower_bound(expected_private_lease_base_lock_leaves_->begin(),
                                                  expected_private_lease_base_lock_leaves_->end(),
                                                  private_lease_names.base_lock_leaf);
    const auto target_index = static_cast<std::size_t>(
        std::distance(expected_private_lease_base_lock_leaves_->begin(), target_position));
    if (target_position == expected_private_lease_base_lock_leaves_->end() ||
        *target_position != private_lease_names.base_lock_leaf ||
        expected_private_lease_base_lock_identities_->at(target_index) !=
            base_lock_at_->identity()) {
        return invalidate_with(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
    }
    std::vector<const DistributedSievePrivateLeaseBaseLockAt*> held_base_locks;
    try {
        held_base_locks.reserve(borrowed_worker_base_locks_.size() +
                                predecessor_base_locks_.size() + 1U);
        for (const auto* borrowed : borrowed_worker_base_locks_) {
            if (borrowed == nullptr) {
                return invalidate_with(diagnostic(
                    DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
            }
            held_base_locks.push_back(borrowed);
        }
        for (const auto& predecessor : predecessor_base_locks_) {
            if (predecessor == nullptr) {
                return invalidate_with(diagnostic(
                    DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
            }
            held_base_locks.push_back(predecessor.get());
        }
        held_base_locks.push_back(base_lock_at_.get());
    } catch (const std::bad_alloc&) {
        return invalidate_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                          std::make_error_code(std::errc::not_enough_memory)));
    }
    const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
        std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(held_base_locks)};

    bool claim_boundary_offered = false;
    const auto revalidate_higher_priority_bindings =
        [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
        if (const auto authority = view.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        if (!claim_boundary_offered) {
            claim_boundary_offered = true;
            if (const auto hooked = invoke_private_lease_base_lock_hook(
                    hooks.after_first_authority_validation, hooks.context, creator_process_id_);
                hooked.status != DistributedSieveWaveStoreStatus::ready) {
                return hooked;
            }
        }
        for (const auto* borrowed : borrowed_worker_base_locks_) {
            if (borrowed == nullptr) {
                return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                  protocol_error());
            }
            if (const auto validated = borrowed->revalidate();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return validated;
            }
        }
        if (const auto target = base_lock_at_->revalidate();
            target.status != DistributedSieveWaveStoreStatus::ready) {
            if (const auto authority = view.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            return target;
        }
        for (const auto& predecessor : predecessor_base_locks_) {
            if (const auto validated = predecessor->revalidate();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return validated;
            }
        }
        return view.revalidate_authority();
    };
    const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic outcome) noexcept
        -> DistributedSieveWaveStoreDiagnostic {
        if (const auto bindings = revalidate_higher_priority_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return invalidate_with(bindings);
        }
        return invalidate_with(std::move(outcome));
    };

    if (const auto bindings = revalidate_higher_priority_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return invalidate_with(bindings);
    }
    auto first = capture_manifest_bound_inventory_witness(
        wave_store_state_->root_fd, wave_store_state_->manifest, wave_store_state_->absolute_root,
        creator_process_id_, held_inventory);
    if (!first) {
        return reject_lower_priority(std::move(first.diagnostic));
    }
    if (first.inventory->private_lease_base_lock_leaves !=
            *expected_private_lease_base_lock_leaves_ ||
        *first.base_lock_identities != *expected_private_lease_base_lock_identities_ ||
        *first.private_lease_witnesses != *expected_private_lease_reservation_witnesses_ ||
        first.inventory->worker_attempt_records != *expected_worker_attempt_record_witnesses_ ||
        first.inventory->merge_started_records != *expected_merge_started_record_witnesses_ ||
        first.inventory->chunk_terminal_failure_records !=
            *expected_chunk_terminal_failure_record_witnesses_) {
        return reject_lower_priority(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto bindings = revalidate_higher_priority_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return invalidate_with(bindings);
    }

    auto confirmed = capture_manifest_bound_inventory_witness(
        wave_store_state_->root_fd, wave_store_state_->manifest, wave_store_state_->absolute_root,
        creator_process_id_, held_inventory);
    if (!confirmed) {
        return reject_lower_priority(std::move(confirmed.diagnostic));
    }
    if (*confirmed.inventory != *first.inventory ||
        *confirmed.base_lock_identities != *first.base_lock_identities ||
        *confirmed.private_lease_witnesses != *first.private_lease_witnesses ||
        confirmed.inventory->private_lease_base_lock_leaves !=
            *expected_private_lease_base_lock_leaves_ ||
        *confirmed.base_lock_identities != *expected_private_lease_base_lock_identities_ ||
        *confirmed.private_lease_witnesses != *expected_private_lease_reservation_witnesses_ ||
        confirmed.inventory->worker_attempt_records != *expected_worker_attempt_record_witnesses_ ||
        confirmed.inventory->merge_started_records != *expected_merge_started_record_witnesses_ ||
        confirmed.inventory->chunk_terminal_failure_records !=
            *expected_chunk_terminal_failure_record_witnesses_) {
        return reject_lower_priority(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto bindings = revalidate_higher_priority_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return invalidate_with(bindings);
    }
    if (!owned_by_current_process()) {
        return invalidate_with(process_mismatch());
    }
    return {};
#endif
}

DistributedSieveWaveStoreDiagnostic
DistributedSievePrivateLeaseRootClaim::revalidate_authority() const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    DistributedSieveWaveStore view(wave_store_state_);
    auto validated = view.revalidate_authority();
    if (validated.status != DistributedSieveWaveStoreStatus::ready) {
        if (base_lock_at_ != nullptr) {
            base_lock_at_->invalidate();
        }
        return validated;
    }
    for (const auto* borrowed : borrowed_worker_base_locks_) {
        if (borrowed == nullptr) {
            if (base_lock_at_ != nullptr) {
                base_lock_at_->invalidate();
            }
            return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                              protocol_error());
        }
        if (const auto worker = borrowed->revalidate();
            worker.status != DistributedSieveWaveStoreStatus::ready) {
            if (base_lock_at_ != nullptr) {
                base_lock_at_->invalidate();
            }
            return worker;
        }
    }
    if (base_lock_at_ != nullptr) {
        if (const auto target = base_lock_at_->revalidate();
            target.status != DistributedSieveWaveStoreStatus::ready) {
            if (const auto authority = view.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                base_lock_at_->invalidate();
                return authority;
            }
            return target;
        }
        for (const auto& predecessor : predecessor_base_locks_) {
            if (predecessor == nullptr) {
                base_lock_at_->invalidate();
                return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                  protocol_error());
            }
            if (const auto prior = predecessor->revalidate();
                prior.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    base_lock_at_->invalidate();
                    return authority;
                }
                return prior;
            }
        }
    }
    validated = view.revalidate_authority();
    if (validated.status != DistributedSieveWaveStoreStatus::ready) {
        if (base_lock_at_ != nullptr) {
            base_lock_at_->invalidate();
        }
        return validated;
    }
    if (!owned_by_current_process()) {
        if (base_lock_at_ != nullptr) {
            base_lock_at_->invalidate();
        }
        return process_mismatch();
    }
    return {};
}

namespace {

#if !defined(_WIN32)

struct DistributedSievePrivateLeaseClosedSnapshot final {
    NamespaceInventory inventory;
    std::vector<NativeIdentityV1> base_lock_identities;
    std::vector<PrivateLeaseReservationWitness> reservation_witnesses;
};

[[nodiscard]] DistributedSievePrivateLeaseClosedSnapshot
private_lease_closed_snapshot(ManifestBoundInventoryWitnessResult observed) {
    return {
        .inventory = std::move(*observed.inventory),
        .base_lock_identities = std::move(*observed.base_lock_identities),
        .reservation_witnesses = std::move(*observed.private_lease_witnesses),
    };
}

[[nodiscard]] bool private_lease_boundary_has_reserved(
    DistributedSievePrivateLeaseReservationBoundary boundary) noexcept {
    return boundary != DistributedSievePrivateLeaseReservationBoundary::PermitAcquired &&
           boundary != DistributedSievePrivateLeaseReservationBoundary::Count;
}

[[nodiscard]] bool private_lease_boundary_has_directory(
    DistributedSievePrivateLeaseReservationBoundary boundary) noexcept {
    switch (boundary) {
    case DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable:
        return true;
    case DistributedSievePrivateLeaseReservationBoundary::PermitAcquired:
    case DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::Count:
        return false;
    }
    return false;
}

[[nodiscard]] bool private_lease_boundary_has_owner(
    DistributedSievePrivateLeaseReservationBoundary boundary) noexcept {
    switch (boundary) {
    case DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable:
        return true;
    case DistributedSievePrivateLeaseReservationBoundary::PermitAcquired:
    case DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable:
    case DistributedSievePrivateLeaseReservationBoundary::Count:
        return false;
    }
    return false;
}

[[nodiscard]] bool private_lease_boundary_has_owned(
    DistributedSievePrivateLeaseReservationBoundary boundary) noexcept {
    switch (boundary) {
    case DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable:
        return true;
    case DistributedSievePrivateLeaseReservationBoundary::PermitAcquired:
    case DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable:
    case DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable:
    case DistributedSievePrivateLeaseReservationBoundary::Count:
        return false;
    }
    return false;
}

[[nodiscard]] DistributedSievePrivateLeaseReservationBoundary
wave_private_lease_boundary(private_lease::PrivateLeaseReservationBoundary boundary) noexcept {
    return static_cast<DistributedSievePrivateLeaseReservationBoundary>(
        static_cast<std::uint8_t>(boundary));
}

[[nodiscard]] std::string
private_lease_staging_directory_leaf(const DistributedSievePrivateLeaseNamesV1& names,
                                     const std::array<std::uint64_t, 2>& lease_id) {
    std::string leaf;
    leaf.reserve(names.relative_lease_stem.size() +
                 DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG.size() + 32U);
    leaf.append(names.relative_lease_stem);
    leaf.append(DISTRIBUTED_SIEVE_PRIVATE_LEASE_STAGING_TAG);
    leaf.append(private_lease::private_lease_id_hex(lease_id));
    return leaf;
}

#endif

} // namespace

#if defined(_WIN32)

class DistributedSieveFdPrivateLeaseReservationTarget final {
public:
    struct Failure final {
        DistributedSieveWaveStoreDiagnostic diagnostic;
    };

    struct Completion final {
        NativeIdentityV1 base_lock_identity;
        DistributedSievePrivateLeaseReservationInventoryWitness final_witness;
    };

    DistributedSieveFdPrivateLeaseReservationTarget(DistributedSievePrivateLeaseRootClaim&,
                                                    DistributedSievePrivateLeaseProtocolTestHooks) {
        throw Failure{
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error())};
    }

    [[nodiscard]] bool checkpoint(private_lease::PrivateLeaseReservationBoundary) {
        return false;
    }

    [[nodiscard]] bool publish_marker(private_lease::PrivateLeaseReservationMarkerRole,
                                      private_lease::PrivateLeaseReservationBoundary) {
        return false;
    }

    void create_staging_directory() {}
    void promote_final_directory() {}
    void complete() {}

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic interruption_diagnostic() const noexcept {
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          unsupported_error());
    }

    [[nodiscard]] Completion take_completion() {
        throw Failure{
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error())};
    }
};

#else

class DistributedSieveFdPrivateLeaseReservationTarget final {
public:
    struct Failure final {
        DistributedSieveWaveStoreDiagnostic diagnostic;
    };

    struct Completion final {
        NativeIdentityV1 base_lock_identity;
        DistributedSievePrivateLeaseReservationInventoryWitness final_witness;
    };

    DistributedSieveFdPrivateLeaseReservationTarget(
        DistributedSievePrivateLeaseRootClaim& claim,
        DistributedSievePrivateLeaseProtocolTestHooks hooks)
        : claim_(claim), hooks_(hooks) {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        const bool worker_target = claim_.worker_attempt_names_.has_value();
        const bool merge_target = claim_.merge_generation_names_.has_value();
        if (!claim_.owned_by_current_process() || claim_.wave_store_state_ == nullptr ||
            claim_.base_lock_at_ == nullptr || worker_target == merge_target ||
            !claim_.expected_private_lease_base_lock_leaves_.has_value() ||
            !claim_.expected_private_lease_base_lock_identities_.has_value() ||
            !claim_.expected_private_lease_reservation_witnesses_.has_value() ||
            !claim_.expected_worker_attempt_record_witnesses_.has_value() ||
            !claim_.expected_merge_started_record_witnesses_.has_value() ||
            !claim_.expected_chunk_terminal_failure_record_witnesses_.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        private_lease_names_ = worker_target
                                   ? static_cast<const DistributedSievePrivateLeaseNamesV1&>(
                                         *claim_.worker_attempt_names_)
                                   : static_cast<const DistributedSievePrivateLeaseNamesV1&>(
                                         *claim_.merge_generation_names_);
        if (const auto validated = claim_.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            fail(validated);
        }

        const auto& state = *claim_.wave_store_state_;
        auto observed = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!observed) {
            fail(adjudicate_observation_failure(std::move(observed.diagnostic)));
        }
        if (observed.inventory->private_lease_base_lock_leaves !=
                *claim_.expected_private_lease_base_lock_leaves_ ||
            *observed.base_lock_identities !=
                *claim_.expected_private_lease_base_lock_identities_ ||
            *observed.private_lease_witnesses !=
                *claim_.expected_private_lease_reservation_witnesses_ ||
            observed.inventory->worker_attempt_records !=
                *claim_.expected_worker_attempt_record_witnesses_ ||
            observed.inventory->merge_started_records !=
                *claim_.expected_merge_started_record_witnesses_ ||
            observed.inventory->chunk_terminal_failure_records !=
                *claim_.expected_chunk_terminal_failure_record_witnesses_) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        current_ = private_lease_closed_snapshot(std::move(observed));
        const bool record_present =
            worker_target
                ? (std::binary_search(current_.inventory.worker_attempt_record_leaves.begin(),
                                      current_.inventory.worker_attempt_record_leaves.end(),
                                      claim_.worker_attempt_names_->canonical_record_leaf) ||
                   std::binary_search(current_.inventory.worker_attempt_record_leaves.begin(),
                                      current_.inventory.worker_attempt_record_leaves.end(),
                                      claim_.worker_attempt_names_->pending_record_leaf))
                : (std::binary_search(current_.inventory.merge_started_record_leaves.begin(),
                                      current_.inventory.merge_started_record_leaves.end(),
                                      claim_.merge_generation_names_->canonical_record_leaf) ||
                   std::binary_search(current_.inventory.merge_started_record_leaves.begin(),
                                      current_.inventory.merge_started_record_leaves.end(),
                                      claim_.merge_generation_names_->pending_record_leaf));
        if (record_present) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        const auto target =
            std::lower_bound(current_.inventory.private_lease_base_lock_leaves.begin(),
                             current_.inventory.private_lease_base_lock_leaves.end(),
                             private_lease_names_.base_lock_leaf);
        if (target == current_.inventory.private_lease_base_lock_leaves.end() ||
            *target != private_lease_names_.base_lock_leaf) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        target_index_ = static_cast<std::size_t>(
            std::distance(current_.inventory.private_lease_base_lock_leaves.begin(), target));
        if (target_index_ >= current_.base_lock_identities.size() ||
            target_index_ >= current_.reservation_witnesses.size() ||
            current_.base_lock_identities[target_index_] != claim_.base_lock_at_->identity()) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto& initial = current_.reservation_witnesses[target_index_];
        if (initial.base_lock_leaf != private_lease_names_.base_lock_leaf ||
            initial.boundary != DistributedSievePrivateLeaseReservationBoundary::PermitAcquired ||
            initial.lease_id != std::array<std::uint64_t, 2>{} ||
            initial.reserved_marker_identity.has_value() ||
            initial.directory_identity.has_value() || initial.owner_marker_identity.has_value() ||
            initial.owned_marker_identity.has_value() || initial.work_package_residue.has_value() ||
            initial.worker_handoff.has_value() || initial.merge_prepared.has_value() ||
            initial.merge_raw_writer_recovery.has_value()) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        lease_id_ = private_lease::allocate_private_lease_id();
        if (lease_id_ == std::array<std::uint64_t, 2>{}) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
        }
        staging_directory_leaf_ =
            private_lease_staging_directory_leaf(private_lease_names_, lease_id_);

        const std::filesystem::path base_path =
            state.absolute_root / private_lease_names_.private_directory_leaf / "corpus";
        reserved_record_ = private_lease::PrivateLeaseRecord{
            .platform_id = private_lease::PLATFORM_ID,
            .phase = private_lease::PrivateLeasePhase::Reserved,
            .capability = private_lease::PrivateLeaseCapability::RollbackStagingOnly,
            .lease_id = lease_id_,
            .base_path_digest = private_lease::frozen_path_digest(base_path),
            .parent_identity = relation_identity(state.root_identity),
            .lock_identity = relation_identity(claim_.base_lock_at_->identity()),
        };
        reserved_bytes_ = private_lease::serialize_private_lease_marker(reserved_record_);
        require_current();
#endif
    }

    [[nodiscard]] bool checkpoint(private_lease::PrivateLeaseReservationBoundary boundary) {
        const auto wave_boundary = wave_private_lease_boundary(boundary);
        if (wave_boundary == DistributedSievePrivateLeaseReservationBoundary::Count ||
            current_witness().boundary != wave_boundary) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        return offer_boundary(wave_boundary);
    }

    [[nodiscard]] bool
    publish_marker(private_lease::PrivateLeaseReservationMarkerRole role,
                   private_lease::PrivateLeaseReservationBoundary pending_boundary) {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)role;
        (void)pending_boundary;
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        switch (role) {
        case private_lease::PrivateLeaseReservationMarkerRole::Reserved:
            require_pending_boundary(
                pending_boundary,
                private_lease::PrivateLeaseReservationBoundary::ReservedPendingDurable);
            require_phase(DistributedSievePrivateLeaseReservationBoundary::PermitAcquired);
            return publish_marker_pair(
                claim_.wave_store_state_->root_fd, private_lease_names_.reserved_pending_leaf,
                private_lease_names_.reserved_leaf, reserved_bytes_,
                DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable,
                DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable, true);
        case private_lease::PrivateLeaseReservationMarkerRole::Owner: {
            require_pending_boundary(
                pending_boundary,
                private_lease::PrivateLeaseReservationBoundary::OwnerPendingDurable);
            require_phase(DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable);
            if (!current_witness().directory_identity.has_value()) {
                fail(adjudicate_observation_failure(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            const auto owner = private_lease::make_private_lease_owner_record(
                reserved_record_, relation_identity(*current_witness().directory_identity));
            auto bytes = private_lease::serialize_private_lease_marker(owner);
            return publish_marker_pair(
                staging_directory_.get(),
                std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_PENDING_LEAF),
                std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF), bytes,
                DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable,
                DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable, false);
        }
        case private_lease::PrivateLeaseReservationMarkerRole::Owned: {
            require_pending_boundary(
                pending_boundary,
                private_lease::PrivateLeaseReservationBoundary::OwnedPendingDurable);
            require_phase(DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable);
            if (!current_witness().directory_identity.has_value() ||
                !current_witness().owner_marker_identity.has_value()) {
                fail(adjudicate_observation_failure(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            const auto owner = private_lease::make_private_lease_owner_record(
                reserved_record_, relation_identity(*current_witness().directory_identity));
            const auto owned = private_lease::make_private_lease_owned_record(
                owner, relation_identity(*current_witness().owner_marker_identity));
            auto bytes = private_lease::serialize_private_lease_marker(owned);
            return publish_marker_pair(
                claim_.wave_store_state_->root_fd, private_lease_names_.owned_pending_leaf,
                private_lease_names_.owned_leaf, bytes,
                DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable,
                DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable, true);
        }
        case private_lease::PrivateLeaseReservationMarkerRole::Count:
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        fail(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
#endif
    }

    void create_staging_directory() {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        require_phase(DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable);
        NamespaceInventory successor_inventory = current_.inventory;
        insert_protocol_leaf(successor_inventory, staging_directory_leaf_);
        before_mutation(DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable);

        int result = -1;
        do {
            result =
                ::mkdirat(claim_.wave_store_state_->root_fd, staging_directory_leaf_.c_str(), 0700);
        } while (result != 0 && errno == EINTR);
        if (result != 0) {
            fail(adjudicate_operation_failure(
                diagnostic(errno == EEXIST ? DistributedSieveWaveStoreStatus::namespace_conflict
                                           : DistributedSieveWaveStoreStatus::io_failed,
                           posix_error(errno)),
                &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable));
        }

        UniqueFd created(openat_retrying_eintr(claim_.wave_store_state_->root_fd,
                                               staging_directory_leaf_.c_str(),
                                               O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC));
        if (!created) {
            fail(adjudicate_operation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno)),
                &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable));
        }
        int chmod_result = -1;
        do {
            chmod_result = ::fchmod(created.get(), 0700);
        } while (chmod_result != 0 && errno == EINTR);
        if (chmod_result != 0) {
            fail(adjudicate_operation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno)),
                &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable));
        }
        struct stat metadata{};
        if (fstat_retrying_eintr(created.get(), metadata) != 0) {
            fail(adjudicate_operation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno)),
                &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable));
        }
        if (!valid_private_lease_directory_metadata(metadata)) {
            fail(adjudicate_operation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
                &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable));
        }
        if (auto acl = acl_rejection(created.get(), true,
                                     DistributedSieveWaveStoreStatus::namespace_conflict);
            acl.has_value()) {
            fail(adjudicate_operation_failure(
                *acl, &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable));
        }
        const NativeIdentityV1 created_identity = protocol_identity(metadata);
        staging_directory_ = std::move(created);
        if (const auto synchronized = private_lease_reservation_sync_handle(
                staging_directory_.get(),
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable,
                DistributedSievePrivateLeaseReservationSyncPoint::StagingDirectory,
                injected_sync_failure_);
            synchronized.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(
                synchronized, &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable,
                created_identity));
        }
        if (const auto synchronized = private_lease_reservation_sync_handle(
                claim_.wave_store_state_->root_fd,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable,
                DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory,
                injected_sync_failure_);
            synchronized.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(
                synchronized, &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable,
                created_identity));
        }
        accept_successor(std::move(successor_inventory),
                         DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable,
                         created_identity);
#endif
    }

    void promote_final_directory() {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        require_phase(DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable);
        NamespaceInventory successor_inventory = current_.inventory;
        erase_protocol_leaf(successor_inventory, staging_directory_leaf_);
        insert_protocol_leaf(successor_inventory, private_lease_names_.private_directory_leaf);
        before_mutation(DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable);
        if (const auto renamed = private_lease_rename_no_replace_at(
                claim_.wave_store_state_->root_fd, staging_directory_leaf_.c_str(),
                private_lease_names_.private_directory_leaf.c_str());
            renamed.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(
                renamed, &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable));
        }
        if (const auto synchronized = private_lease_reservation_sync_handle(
                claim_.wave_store_state_->root_fd,
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable,
                DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory,
                injected_sync_failure_);
            synchronized.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(
                synchronized, &successor_inventory,
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable));
        }
        accept_successor(std::move(successor_inventory),
                         DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable);
#endif
    }

    void complete() {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable);
        require_current();
        completed_ = true;
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic interruption_diagnostic() const noexcept {
        auto outcome = diagnostic(DistributedSieveWaveStoreStatus::interrupted,
                                  std::make_error_code(std::errc::operation_canceled));
        outcome.last_private_lease_reservation_boundary = interrupted_boundary_;
        return outcome;
    }

    [[nodiscard]] Completion take_completion() {
        if (!completed_ ||
            current_witness().boundary !=
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
            !current_witness().directory_identity.has_value() ||
            !current_witness().owner_marker_identity.has_value() ||
            !current_witness().owned_marker_identity.has_value() ||
            current_witness().work_package_residue.has_value() ||
            current_witness().worker_handoff.has_value() ||
            current_witness().merge_prepared.has_value() ||
            current_witness().merge_raw_writer_recovery.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
        }
        require_current();
        return {
            .base_lock_identity = current_.base_lock_identities[target_index_],
            .final_witness = std::move(current_.reservation_witnesses[target_index_]),
        };
    }

private:
    [[noreturn]] static void fail(DistributedSieveWaveStoreDiagnostic outcome) {
        throw Failure{std::move(outcome)};
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic
    adjudicate_observation_failure(DistributedSieveWaveStoreDiagnostic lower) const noexcept {
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        return lower;
    }

    [[nodiscard]] const PrivateLeaseReservationWitness& current_witness() const {
        if (target_index_ >= current_.reservation_witnesses.size()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
        }
        return current_.reservation_witnesses[target_index_];
    }

    void require_phase(DistributedSievePrivateLeaseReservationBoundary expected) const {
        if (current_witness().boundary != expected) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
    }

    static void require_pending_boundary(private_lease::PrivateLeaseReservationBoundary actual,
                                         private_lease::PrivateLeaseReservationBoundary expected) {
        if (actual != expected) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
    }

    static void insert_protocol_leaf(NamespaceInventory& inventory, const std::string& leaf) {
        const auto position = std::lower_bound(inventory.private_lease_protocol_leaves.begin(),
                                               inventory.private_lease_protocol_leaves.end(), leaf);
        if (position != inventory.private_lease_protocol_leaves.end() && *position == leaf) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        inventory.private_lease_protocol_leaves.insert(position, leaf);
    }

    static void erase_protocol_leaf(NamespaceInventory& inventory, const std::string& leaf) {
        const auto position = std::lower_bound(inventory.private_lease_protocol_leaves.begin(),
                                               inventory.private_lease_protocol_leaves.end(), leaf);
        if (position == inventory.private_lease_protocol_leaves.end() || *position != leaf) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        inventory.private_lease_protocol_leaves.erase(position);
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic
    validate_staging_handle(const PrivateLeaseReservationWitness& witness) const noexcept {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)witness;
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          unsupported_error());
#else
        if (!private_lease_boundary_has_directory(witness.boundary)) {
            return staging_directory_
                       ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                    protocol_error())
                       : DistributedSieveWaveStoreDiagnostic{};
        }
        if (!staging_directory_ || !witness.directory_identity.has_value()) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
        struct stat metadata{};
        if (fstat_retrying_eintr(staging_directory_.get(), metadata) != 0) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              posix_error(errno));
        }
        if (!valid_private_lease_directory_metadata(metadata) ||
            protocol_identity(metadata) != *witness.directory_identity) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
        if (auto acl = acl_rejection(staging_directory_.get(), true,
                                     DistributedSieveWaveStoreStatus::namespace_conflict);
            acl.has_value()) {
            return *acl;
        }
        return {};
#endif
    }

    [[nodiscard]] bool successor_witness_valid(
        const PrivateLeaseReservationWitness& candidate,
        DistributedSievePrivateLeaseReservationBoundary boundary,
        std::optional<NativeIdentityV1> expected_introduced_identity) const noexcept {
        const auto& prior = current_witness();
        if (candidate.base_lock_leaf != prior.base_lock_leaf || candidate.boundary != boundary ||
            candidate.lease_id != lease_id_ || prior.work_package_residue.has_value() ||
            prior.worker_handoff.has_value() || prior.merge_prepared.has_value() ||
            prior.merge_raw_writer_recovery.has_value() ||
            candidate.work_package_residue.has_value() || candidate.worker_handoff.has_value() ||
            candidate.merge_prepared.has_value() ||
            candidate.merge_raw_writer_recovery.has_value()) {
            return false;
        }
        const auto optional_identity_valid = [](const std::optional<NativeIdentityV1>& previous,
                                                const std::optional<NativeIdentityV1>& next,
                                                bool required) noexcept {
            if (next.has_value() != required || (next.has_value() && nil_identity(*next))) {
                return false;
            }
            return !previous.has_value() || (next.has_value() && *previous == *next);
        };
        if (!optional_identity_valid(prior.reserved_marker_identity,
                                     candidate.reserved_marker_identity,
                                     private_lease_boundary_has_reserved(boundary)) ||
            !optional_identity_valid(prior.directory_identity, candidate.directory_identity,
                                     private_lease_boundary_has_directory(boundary)) ||
            !optional_identity_valid(prior.owner_marker_identity, candidate.owner_marker_identity,
                                     private_lease_boundary_has_owner(boundary)) ||
            !optional_identity_valid(prior.owned_marker_identity, candidate.owned_marker_identity,
                                     private_lease_boundary_has_owned(boundary))) {
            return false;
        }

        const std::optional<NativeIdentityV1>* introduced = nullptr;
        switch (boundary) {
        case DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable:
            introduced = &candidate.reserved_marker_identity;
            break;
        case DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable:
            introduced = &candidate.directory_identity;
            break;
        case DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable:
            introduced = &candidate.owner_marker_identity;
            break;
        case DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable:
            introduced = &candidate.owned_marker_identity;
            break;
        case DistributedSievePrivateLeaseReservationBoundary::PermitAcquired:
        case DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable:
        case DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable:
        case DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable:
        case DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable:
        case DistributedSievePrivateLeaseReservationBoundary::Count:
            break;
        }
        if (introduced == nullptr) {
            return !expected_introduced_identity.has_value();
        }
        return expected_introduced_identity.has_value() && introduced->has_value() &&
               **introduced == *expected_introduced_identity;
    }

    [[nodiscard]] bool
    semantic_successor_valid(const ManifestBoundInventoryWitnessResult& observed,
                             const NamespaceInventory& expected_inventory,
                             DistributedSievePrivateLeaseReservationBoundary boundary,
                             std::optional<NativeIdentityV1> expected_introduced_identity =
                                 std::nullopt) const noexcept {
        if (!observed || *observed.inventory != expected_inventory ||
            *observed.base_lock_identities != current_.base_lock_identities ||
            observed.private_lease_witnesses->size() != current_.reservation_witnesses.size() ||
            target_index_ >= observed.private_lease_witnesses->size()) {
            return false;
        }
        for (std::size_t index = 0; index < observed.private_lease_witnesses->size(); ++index) {
            if (index != target_index_ && observed.private_lease_witnesses->at(index) !=
                                              current_.reservation_witnesses[index]) {
                return false;
            }
        }
        return successor_witness_valid(observed.private_lease_witnesses->at(target_index_),
                                       boundary, expected_introduced_identity);
    }

    [[nodiscard]] HeldPrivateLeaseBaseLockInventoryV1 held_inventory() const noexcept {
        return {
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                claim_.borrowed_worker_base_locks_.data(),
                claim_.borrowed_worker_base_locks_.size()),
        };
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_exact_snapshot(
        const DistributedSievePrivateLeaseClosedSnapshot& expected) const noexcept {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)expected;
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          unsupported_error());
#else
        const auto& state = *claim_.wave_store_state_;
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        auto first = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!first) {
            return adjudicate_observation_failure(std::move(first.diagnostic));
        }
        if (*first.inventory != expected.inventory ||
            *first.base_lock_identities != expected.base_lock_identities ||
            *first.private_lease_witnesses != expected.reservation_witnesses) {
            return adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto staging =
                validate_staging_handle(expected.reservation_witnesses[target_index_]);
            staging.status != DistributedSieveWaveStoreStatus::ready) {
            return adjudicate_observation_failure(staging);
        }
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        auto confirmed = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!confirmed) {
            return adjudicate_observation_failure(std::move(confirmed.diagnostic));
        }
        if (*confirmed.inventory != *first.inventory ||
            *confirmed.base_lock_identities != *first.base_lock_identities ||
            *confirmed.private_lease_witnesses != *first.private_lease_witnesses ||
            *confirmed.inventory != expected.inventory ||
            *confirmed.base_lock_identities != expected.base_lock_identities ||
            *confirmed.private_lease_witnesses != expected.reservation_witnesses) {
            return adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto staging =
                validate_staging_handle(expected.reservation_witnesses[target_index_]);
            staging.status != DistributedSieveWaveStoreStatus::ready) {
            return adjudicate_observation_failure(staging);
        }
        return claim_.revalidate_authority();
#endif
    }

    void require_current() const {
        if (const auto validated = validate_exact_snapshot(current_);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            fail(validated);
        }
    }

    void select_injected_sync_failure(DistributedSievePrivateLeaseReservationBoundary successor) {
        injected_sync_failure_.reset();
        if (hooks_.fail_before_sync == nullptr) {
            return;
        }
        const auto offer = [&](DistributedSievePrivateLeaseReservationSyncPoint point) -> bool {
            if (!process_matches(claim_.creator_process_id_)) {
                fail(process_mismatch());
            }
            const bool selected = hooks_.fail_before_sync(successor, point, hooks_.context);
            if (!process_matches(claim_.creator_process_id_)) {
                fail(process_mismatch());
            }
            require_current();
            if (selected) {
                injected_sync_failure_ = point;
            }
            return selected;
        };

        switch (successor) {
        case DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable:
        case DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable:
        case DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable:
            if (offer(DistributedSievePrivateLeaseReservationSyncPoint::MarkerFileInitial) ||
                offer(DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory)) {
                return;
            }
            (void)offer(DistributedSievePrivateLeaseReservationSyncPoint::MarkerFileFinal);
            return;
        case DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable:
            if (offer(DistributedSievePrivateLeaseReservationSyncPoint::StagingDirectory)) {
                return;
            }
            (void)offer(DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory);
            return;
        case DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable:
        case DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable:
        case DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable:
        case DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable:
            (void)offer(DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory);
            return;
        case DistributedSievePrivateLeaseReservationBoundary::PermitAcquired:
        case DistributedSievePrivateLeaseReservationBoundary::Count:
            return;
        }
    }

    void before_mutation(DistributedSievePrivateLeaseReservationBoundary successor) {
        require_current();
        if (current_witness().work_package_residue.has_value() ||
            current_witness().worker_handoff.has_value() ||
            current_witness().merge_prepared.has_value() ||
            current_witness().merge_raw_writer_recovery.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        if (hooks_.after_predecessor_validation != nullptr) {
            hooks_.after_predecessor_validation(successor, hooks_.context);
        }
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        require_current();
        select_injected_sync_failure(successor);
    }

    [[nodiscard]] bool offer_boundary(DistributedSievePrivateLeaseReservationBoundary boundary) {
        require_current();
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        const bool stop =
            hooks_.stop_after != nullptr && hooks_.stop_after(boundary, hooks_.context);
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        require_current();
        if (stop) {
            interrupted_boundary_ = boundary;
        }
        return stop;
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic adjudicate_operation_failure(
        DistributedSieveWaveStoreDiagnostic lower,
        const NamespaceInventory* possible_successor_inventory,
        DistributedSievePrivateLeaseReservationBoundary possible_successor_boundary,
        std::optional<NativeIdentityV1> expected_introduced_identity =
            std::nullopt) const noexcept {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)possible_successor_inventory;
        (void)possible_successor_boundary;
        (void)expected_introduced_identity;
        return lower;
#else
        const auto& state = *claim_.wave_store_state_;
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        auto first = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!first) {
            return adjudicate_observation_failure(std::move(first.diagnostic));
        }
        const bool predecessor = *first.inventory == current_.inventory &&
                                 *first.base_lock_identities == current_.base_lock_identities &&
                                 *first.private_lease_witnesses == current_.reservation_witnesses;
        const bool successor =
            possible_successor_inventory != nullptr &&
            semantic_successor_valid(first, *possible_successor_inventory,
                                     possible_successor_boundary, expected_introduced_identity);
        if (!predecessor && !successor) {
            return adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto& first_witness = predecessor ? current_.reservation_witnesses[target_index_]
                                                : first.private_lease_witnesses->at(target_index_);
        if (const auto staging = validate_staging_handle(first_witness);
            staging.status != DistributedSieveWaveStoreStatus::ready) {
            return adjudicate_observation_failure(staging);
        }
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        if (lower.failed_private_lease_reservation_sync_site.has_value() &&
            hooks_.after_injected_sync_failure != nullptr) {
            if (!process_matches(claim_.creator_process_id_)) {
                return process_mismatch();
            }
            hooks_.after_injected_sync_failure(*lower.failed_private_lease_reservation_sync_site,
                                               hooks_.context);
            if (!process_matches(claim_.creator_process_id_)) {
                return process_mismatch();
            }
            if (const auto authority = claim_.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
        }
        auto confirmed = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!confirmed) {
            return adjudicate_observation_failure(std::move(confirmed.diagnostic));
        }
        if (*confirmed.inventory != *first.inventory ||
            *confirmed.base_lock_identities != *first.base_lock_identities ||
            *confirmed.private_lease_witnesses != *first.private_lease_witnesses) {
            return adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto& confirmed_witness = predecessor
                                            ? current_.reservation_witnesses[target_index_]
                                            : confirmed.private_lease_witnesses->at(target_index_);
        if (const auto staging = validate_staging_handle(confirmed_witness);
            staging.status != DistributedSieveWaveStoreStatus::ready) {
            return adjudicate_observation_failure(staging);
        }
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        return lower;
#endif
    }

    void
    accept_successor(NamespaceInventory expected_inventory,
                     DistributedSievePrivateLeaseReservationBoundary boundary,
                     std::optional<NativeIdentityV1> expected_introduced_identity = std::nullopt) {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)expected_inventory;
        (void)boundary;
        (void)expected_introduced_identity;
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        const auto& state = *claim_.wave_store_state_;
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            fail(authority);
        }
        auto first = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!first) {
            fail(adjudicate_observation_failure(std::move(first.diagnostic)));
        }
        if (!semantic_successor_valid(first, expected_inventory, boundary,
                                      expected_introduced_identity)) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        if (const auto staging =
                validate_staging_handle(first.private_lease_witnesses->at(target_index_));
            staging.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_observation_failure(staging));
        }
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            fail(authority);
        }
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        if (hooks_.after_first_successor_validation != nullptr) {
            hooks_.after_first_successor_validation(boundary, hooks_.context);
        }
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            fail(authority);
        }
        auto confirmed = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory());
        if (!confirmed) {
            fail(adjudicate_observation_failure(std::move(confirmed.diagnostic)));
        }
        if (*confirmed.inventory != *first.inventory ||
            *confirmed.base_lock_identities != *first.base_lock_identities ||
            *confirmed.private_lease_witnesses != *first.private_lease_witnesses ||
            !semantic_successor_valid(confirmed, expected_inventory, boundary,
                                      expected_introduced_identity)) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        if (const auto authority = claim_.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            fail(authority);
        }
        current_ = private_lease_closed_snapshot(std::move(first));
        if (const auto staging = validate_staging_handle(current_witness());
            staging.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_observation_failure(staging));
        }
#endif
    }

    [[nodiscard]] bool
    publish_marker_pair(int parent_fd, const std::string& pending_leaf,
                        const std::string& canonical_leaf, std::span<const std::byte> bytes,
                        DistributedSievePrivateLeaseReservationBoundary pending_boundary,
                        DistributedSievePrivateLeaseReservationBoundary canonical_boundary,
                        bool root_inventory_leaf) {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)parent_fd;
        (void)pending_leaf;
        (void)canonical_leaf;
        (void)bytes;
        (void)pending_boundary;
        (void)canonical_boundary;
        (void)root_inventory_leaf;
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        NamespaceInventory pending_inventory = current_.inventory;
        NamespaceInventory canonical_inventory = current_.inventory;
        if (root_inventory_leaf) {
            insert_protocol_leaf(pending_inventory, pending_leaf);
            insert_protocol_leaf(canonical_inventory, canonical_leaf);
        }
        before_mutation(pending_boundary);
        auto published = publish_private_lease_marker_strict_at(
            parent_fd, pending_leaf, bytes, pending_boundary, injected_sync_failure_);
        if (!published) {
            fail(adjudicate_operation_failure(std::move(published.outcome), &pending_inventory,
                                              pending_boundary, published.identity));
        }
        accept_successor(std::move(pending_inventory), pending_boundary, published.identity);
        if (const auto closed = published.close_checked();
            closed.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(
                closed, nullptr, DistributedSievePrivateLeaseReservationBoundary::Count));
        }
        if (offer_boundary(pending_boundary)) {
            return true;
        }

        before_mutation(canonical_boundary);
        if (const auto renamed = private_lease_rename_no_replace_at(parent_fd, pending_leaf.c_str(),
                                                                    canonical_leaf.c_str());
            renamed.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(renamed, &canonical_inventory, canonical_boundary));
        }
        if (const auto synchronized = private_lease_reservation_sync_handle(
                parent_fd, canonical_boundary,
                DistributedSievePrivateLeaseReservationSyncPoint::ParentDirectory,
                injected_sync_failure_);
            synchronized.status != DistributedSieveWaveStoreStatus::ready) {
            fail(adjudicate_operation_failure(synchronized, &canonical_inventory,
                                              canonical_boundary));
        }
        accept_successor(std::move(canonical_inventory), canonical_boundary);
        return false;
#endif
    }

    DistributedSievePrivateLeaseRootClaim& claim_;
    DistributedSievePrivateLeaseProtocolTestHooks hooks_;
    DistributedSievePrivateLeaseClosedSnapshot current_;
    DistributedSievePrivateLeaseNamesV1 private_lease_names_;
    std::size_t target_index_ = 0;
    std::array<std::uint64_t, 2> lease_id_{};
    std::string staging_directory_leaf_;
    private_lease::PrivateLeaseRecord reserved_record_;
    std::vector<std::byte> reserved_bytes_;
    std::optional<DistributedSievePrivateLeaseReservationSyncPoint> injected_sync_failure_;
#if !defined(_WIN32)
    UniqueFd staging_directory_;
#endif
    std::optional<DistributedSievePrivateLeaseReservationBoundary> interrupted_boundary_;
    bool completed_ = false;
};

#endif

class DistributedSieveStartedAttemptCleanupAdmission final {
public:
    DistributedSieveStartedAttemptCleanupAdmission() = delete;
    DistributedSieveStartedAttemptCleanupAdmission(
        const DistributedSieveStartedAttemptCleanupAdmission&) = delete;
    DistributedSieveStartedAttemptCleanupAdmission&
    operator=(const DistributedSieveStartedAttemptCleanupAdmission&) = delete;
    DistributedSieveStartedAttemptCleanupAdmission(
        DistributedSieveStartedAttemptCleanupAdmission&&) noexcept = default;
    DistributedSieveStartedAttemptCleanupAdmission&
    operator=(DistributedSieveStartedAttemptCleanupAdmission&&) = delete;
    ~DistributedSieveStartedAttemptCleanupAdmission() = default;

private:
#if !defined(_WIN32)
    DistributedSieveStartedAttemptCleanupAdmission(
        UniqueFd canonical_record, DistributedSieveWorkerAttemptNamesV1 worker_attempt_names,
        DistributedSieveWorkerAttemptRecordInventoryWitness record_witness,
        std::uint64_t creator_process_id) noexcept
        : canonical_record_(std::move(canonical_record)),
          worker_attempt_names_(std::move(worker_attempt_names)),
          record_witness_(std::move(record_witness)), creator_process_id_(creator_process_id) {}

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate(int root_fd) const noexcept;

    UniqueFd canonical_record_;
    DistributedSieveWorkerAttemptNamesV1 worker_attempt_names_;
    DistributedSieveWorkerAttemptRecordInventoryWitness record_witness_;
    std::uint64_t creator_process_id_ = 0;
#endif

    friend class DistributedSieveFdPrivateLeaseRecoveryTarget;
    friend DistributedSieveWorkerAttemptReconcileResult reconcile_worker_attempt_started(
        DistributedSievePrivateLeaseRootClaimResult&& claimed,
        DistributedSieveWorkerAttemptReconcileTestHooks hooks) noexcept;
    friend DistributedSieveChunkTerminalFailurePublicationResultV1
    publish_chunk_terminal_failure_v1(
        DistributedSieveChunkTerminalFailureAdmissionV1&& admission,
        DistributedSieveChunkTerminalFailureTestHooksV1 hooks) noexcept;
};

class DistributedSieveStartedMergeCleanupAdmissionV1 final {
public:
    DistributedSieveStartedMergeCleanupAdmissionV1() = delete;
    DistributedSieveStartedMergeCleanupAdmissionV1(
        const DistributedSieveStartedMergeCleanupAdmissionV1&) = delete;
    DistributedSieveStartedMergeCleanupAdmissionV1&
    operator=(const DistributedSieveStartedMergeCleanupAdmissionV1&) = delete;
    DistributedSieveStartedMergeCleanupAdmissionV1(
        DistributedSieveStartedMergeCleanupAdmissionV1&&) noexcept = default;
    DistributedSieveStartedMergeCleanupAdmissionV1&
    operator=(DistributedSieveStartedMergeCleanupAdmissionV1&&) = delete;
    ~DistributedSieveStartedMergeCleanupAdmissionV1() = default;

private:
#if !defined(_WIN32)
    DistributedSieveStartedMergeCleanupAdmissionV1(
        UniqueFd canonical_record, DistributedSieveMergeGenerationNamesV1 merge_generation_names,
        DistributedSieveMergeStartedRecordInventoryWitnessV1 record_witness,
        std::uint64_t creator_process_id) noexcept
        : canonical_record_(std::move(canonical_record)),
          merge_generation_names_(std::move(merge_generation_names)),
          record_witness_(std::move(record_witness)), creator_process_id_(creator_process_id) {}

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate(int root_fd) const noexcept;

    UniqueFd canonical_record_;
    DistributedSieveMergeGenerationNamesV1 merge_generation_names_;
    DistributedSieveMergeStartedRecordInventoryWitnessV1 record_witness_;
    std::uint64_t creator_process_id_ = 0;
#endif

    friend class DistributedSieveFdPrivateLeaseRecoveryTarget;
    friend DistributedSieveMergeStartedReconcileResultV1 reconcile_merge_started_generation_v1(
        DistributedSieveWaveStore& store, std::uint32_t merge_attempt_ordinal,
        DistributedSieveMergeStartedReconcileTestHooksV1 hooks,
        std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs) noexcept;
};

struct DistributedSieveChunkTerminalFailureAdmissionV1::State final {
    State(std::unique_ptr<DistributedSievePrivateLeaseRootClaim> retained_claim,
          DistributedSieveStartedAttemptCleanupAdmission retained_attempt,
          std::vector<AttemptStartedV1> retained_chain, ChunkPlanV1 retained_chunk,
          DistributedSieveChunkTerminalFailureNamesV1 retained_names,
          std::uint64_t retained_creator_process_id) noexcept
        : claim(std::move(retained_claim)), pinned_attempt(std::move(retained_attempt)),
          chain(std::move(retained_chain)), chunk(std::move(retained_chunk)),
          names(std::move(retained_names)), creator_process_id(retained_creator_process_id) {}

    std::unique_ptr<DistributedSievePrivateLeaseRootClaim> claim;
    DistributedSieveStartedAttemptCleanupAdmission pinned_attempt;
    std::vector<AttemptStartedV1> chain;
    ChunkPlanV1 chunk;
    DistributedSieveChunkTerminalFailureNamesV1 names;
    std::uint64_t creator_process_id = 0;
};

DistributedSieveChunkTerminalFailureAdmissionV1::DistributedSieveChunkTerminalFailureAdmissionV1(
    std::unique_ptr<State> state) noexcept
    : state_(std::move(state)) {}

DistributedSieveChunkTerminalFailureAdmissionV1::DistributedSieveChunkTerminalFailureAdmissionV1(
    DistributedSieveChunkTerminalFailureAdmissionV1&&) noexcept = default;

DistributedSieveChunkTerminalFailureAdmissionV1::
    ~DistributedSieveChunkTerminalFailureAdmissionV1() = default;

bool DistributedSieveChunkTerminalFailureAdmissionV1::owned_by_current_process() const noexcept {
    return state_ != nullptr && state_->claim != nullptr && state_->creator_process_id != 0 &&
           process_matches(state_->creator_process_id) && state_->claim->owned_by_current_process();
}

#if !defined(_WIN32)

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
validate_exact_worker_attempt_record_handle(int root_fd, int record_fd, const std::string& leaf,
                                            const durable_record::RecordSnapshot& expected_snapshot,
                                            std::span<const std::byte> expected_bytes,
                                            std::uint64_t creator_process_id) noexcept {
    if (root_fd < 0 || record_fd < 0 || leaf.empty() || expected_bytes.empty()) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }
    if (!process_matches(creator_process_id)) {
        return process_mismatch();
    }

    struct stat held_before{};
    struct stat named_before{};
    if (fstat_retrying_eintr(record_fd, held_before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
    }
    if (fstatat_retrying_eintr(root_fd, leaf.c_str(), named_before) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    const auto held_snapshot = durable_record::RecordSnapshot{
        .identity = record_identity(held_before),
        .size = static_cast<std::uint64_t>(held_before.st_size),
    };
    if (!valid_manifest_metadata(held_before) || !valid_manifest_metadata(named_before) ||
        !stable_metadata(held_before, named_before) || held_snapshot != expected_snapshot ||
        static_cast<std::uint64_t>(expected_bytes.size()) != expected_snapshot.size) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (auto acl =
            acl_rejection(record_fd, false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }

    std::size_t offset = 0;
    while (offset < expected_bytes.size()) {
        if (!process_matches(creator_process_id)) {
            return process_mismatch();
        }
        const std::size_t request =
            std::min(expected_bytes.size() - offset,
                     static_cast<std::size_t>(std::numeric_limits<ssize_t>::max()));
        std::array<std::byte, 4096> buffer{};
        const std::size_t bounded_request = std::min(request, buffer.size());
        const ssize_t count =
            ::pread(record_fd, buffer.data(), bounded_request, static_cast<off_t>(offset));
        if (count < 0) {
            const int saved_errno = errno;
            if (saved_errno == EINTR) {
                continue;
            }
            return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(saved_errno));
        }
        if (count == 0 ||
            !std::equal(buffer.begin(), buffer.begin() + static_cast<std::ptrdiff_t>(count),
                        expected_bytes.begin() + static_cast<std::ptrdiff_t>(offset))) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
        offset += static_cast<std::size_t>(count);
    }

    std::byte trailing{};
    ssize_t trailing_count = -1;
    do {
        trailing_count =
            ::pread(record_fd, &trailing, 1, static_cast<off_t>(expected_bytes.size()));
    } while (trailing_count < 0 && errno == EINTR);
    if (trailing_count < 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
    }
    if (trailing_count != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }

    struct stat held_after{};
    struct stat named_after{};
    if (fstat_retrying_eintr(record_fd, held_after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, posix_error(errno));
    }
    if (fstatat_retrying_eintr(root_fd, leaf.c_str(), named_after) != 0) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
    }
    const auto final_snapshot = durable_record::RecordSnapshot{
        .identity = record_identity(held_after),
        .size = static_cast<std::uint64_t>(held_after.st_size),
    };
    if (!valid_manifest_metadata(held_after) || !valid_manifest_metadata(named_after) ||
        !stable_metadata(held_before, held_after) || !stable_metadata(held_after, named_after) ||
        final_snapshot != expected_snapshot) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (auto acl =
            acl_rejection(record_fd, false, DistributedSieveWaveStoreStatus::namespace_conflict);
        acl.has_value()) {
        return *acl;
    }
    return process_matches(creator_process_id) ? DistributedSieveWaveStoreDiagnostic{}
                                               : process_mismatch();
}

struct WorkerAttemptCanonicalRecordOpenResult final {
    UniqueFd canonical_record;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return static_cast<bool>(canonical_record) &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] WorkerAttemptCanonicalRecordOpenResult open_worker_attempt_canonical_record(
    int root_fd, const std::string& leaf, const durable_record::RecordSnapshot& expected_snapshot,
    std::span<const std::byte> expected_bytes, std::uint64_t creator_process_id) noexcept {
    if (!process_matches(creator_process_id)) {
        return {{}, process_mismatch()};
    }
    const int descriptor = openat_retrying_eintr(root_fd, leaf.c_str(),
                                                 O_RDONLY | O_NONBLOCK | O_NOFOLLOW | O_CLOEXEC);
    if (descriptor < 0) {
        return {
            {},
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno))};
    }
    UniqueFd held(descriptor);
    auto validated = validate_exact_worker_attempt_record_handle(
        root_fd, held.get(), leaf, expected_snapshot, expected_bytes, creator_process_id);
    if (validated.status != DistributedSieveWaveStoreStatus::ready) {
        return {{}, std::move(validated)};
    }
    return {std::move(held), {}};
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveStartedAttemptCleanupAdmission::revalidate(int root_fd) const noexcept {
    if (!canonical_record_ || creator_process_id_ == 0 ||
        !record_witness_.canonical_snapshot.has_value() ||
        record_witness_.pending_snapshot.has_value()) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }
    return validate_exact_worker_attempt_record_handle(
        root_fd, canonical_record_.get(), worker_attempt_names_.canonical_record_leaf,
        *record_witness_.canonical_snapshot, record_witness_.bytes, creator_process_id_);
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveStartedMergeCleanupAdmissionV1::revalidate(int root_fd) const noexcept {
    if (!canonical_record_ || creator_process_id_ == 0 ||
        !record_witness_.canonical_snapshot.has_value() ||
        record_witness_.pending_snapshot.has_value()) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }
    return validate_exact_worker_attempt_record_handle(
        root_fd, canonical_record_.get(), merge_generation_names_.canonical_record_leaf,
        *record_witness_.canonical_snapshot, record_witness_.bytes, creator_process_id_);
}

#endif

#if defined(_WIN32)

class DistributedSieveFdPrivateLeaseRecoveryTarget final {
public:
    struct Failure final {
        DistributedSieveWaveStoreDiagnostic diagnostic;
    };

    DistributedSieveFdPrivateLeaseRecoveryTarget(DistributedSievePrivateLeaseRootClaim&,
                                                 DistributedSievePrivateLeaseRecoveryTestHooks) {
        throw Failure{
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error())};
    }

    [[nodiscard]] private_lease::PrivateLeaseReservationBoundary boundary() const {
        return private_lease::PrivateLeaseReservationBoundary::Count;
    }

    void apply(private_lease::PrivateLeaseRecoveryTransition) {}

    [[nodiscard]] bool checkpoint(private_lease::PrivateLeaseReservationBoundary) {
        return false;
    }

    void complete() {}
    void reject_invalid_boundary() {}
    [[nodiscard]] bool completed() const noexcept {
        return false;
    }

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic interruption_diagnostic() const noexcept {
        return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                          unsupported_error());
    }
};

#else

class DistributedSieveFdPrivateLeaseRecoveryTarget final {
public:
    struct Failure final {
        DistributedSieveWaveStoreDiagnostic diagnostic;
    };

    DistributedSieveFdPrivateLeaseRecoveryTarget(
        DistributedSievePrivateLeaseRootClaim& claim,
        DistributedSievePrivateLeaseRecoveryTestHooks hooks);
    DistributedSieveFdPrivateLeaseRecoveryTarget(
        DistributedSievePrivateLeaseRootClaim& claim,
        DistributedSieveStartedAttemptCleanupAdmission&& admission,
        DistributedSievePrivateLeaseRecoveryTestHooks hooks);
    DistributedSieveFdPrivateLeaseRecoveryTarget(
        DistributedSievePrivateLeaseRootClaim& claim,
        DistributedSieveStartedMergeCleanupAdmissionV1&& admission,
        DistributedSievePrivateLeaseRecoveryTestHooks hooks);

    [[nodiscard]] private_lease::PrivateLeaseReservationBoundary boundary() const;
    void apply(private_lease::PrivateLeaseRecoveryTransition transition);
    [[nodiscard]] bool checkpoint(private_lease::PrivateLeaseReservationBoundary boundary);
    void complete();
    void reject_invalid_boundary() noexcept;
    [[nodiscard]] bool completed() const noexcept;
    [[nodiscard]] std::optional<DistributedSieveReconciledWorkerAttemptV1>
    started_completion() const;
    [[nodiscard]] std::optional<DistributedSieveStartedAttemptCleanupAdmission>
    take_started_admission();

    [[nodiscard]] DistributedSieveWaveStoreDiagnostic interruption_diagnostic() const noexcept;

private:
    [[noreturn]] static void fail(DistributedSieveWaveStoreDiagnostic outcome);

    void initialize_common();
    void require_prestart_record_absent() const;
    void require_started_record_exact() const;
    [[nodiscard]] const PrivateLeaseReservationWitness& current_witness() const;
    void require_phase(DistributedSievePrivateLeaseReservationBoundary expected) const;
    void require_current() const;
    [[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_started_record_pin() const noexcept;
    void before_mutation();
    void select_injected_sync_failure(DistributedSievePrivateLeaseRecoveryEdge edge);

    [[nodiscard]] private_lease::PrivateLeaseRecord expected_reserved_record() const;
    [[nodiscard]] private_lease::PrivateLeaseRecord expected_owner_record() const;
    [[nodiscard]] private_lease::PrivateLeaseRecord expected_owned_record() const;

    [[nodiscard]] PrivateLeaseMarkerAtResult
    hold_exact_marker(int parent_fd, const std::string& leaf,
                      const private_lease::PrivateLeaseRecord& expected_record,
                      const NativeIdentityV1& expected_identity) const;

    [[nodiscard]] DistributedSievePrivateLeaseClosedSnapshot
    expected_successor(private_lease::PrivateLeaseRecoveryTransition transition) const;
    [[nodiscard]] HeldPrivateLeaseBaseLockInventoryV1 held_inventory() const noexcept {
        return {
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                claim_.borrowed_worker_base_locks_.data(),
                claim_.borrowed_worker_base_locks_.size()),
        };
    }
    [[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_exact_snapshot(
        const DistributedSievePrivateLeaseClosedSnapshot& expected) const noexcept;
    [[nodiscard]] DistributedSieveWaveStoreDiagnostic
    validate_directory_handle(const PrivateLeaseReservationWitness& witness) const noexcept;
    [[nodiscard]] DistributedSieveWaveStoreDiagnostic
    adjudicate_observation_failure(DistributedSieveWaveStoreDiagnostic lower) const noexcept;
    template <typename LocalValidator>
    [[nodiscard]] DistributedSieveWaveStoreDiagnostic adjudicate_operation_failure(
        DistributedSieveWaveStoreDiagnostic lower,
        const DistributedSievePrivateLeaseClosedSnapshot& possible_successor,
        DistributedSievePrivateLeaseRecoveryEdge edge,
        LocalValidator&& validate_local) const noexcept;
    template <typename LocalValidator>
    void accept_successor(const DistributedSievePrivateLeaseClosedSnapshot& expected,
                          DistributedSievePrivateLeaseRecoveryEdge edge,
                          LocalValidator&& validate_local);

    static void insert_protocol_leaf(NamespaceInventory& inventory, const std::string& leaf);
    static void erase_protocol_leaf(NamespaceInventory& inventory, const std::string& leaf);
    static void replace_protocol_leaf(NamespaceInventory& inventory, const std::string& source,
                                      const std::string& destination);

    void rename_marker_no_replace(int parent_fd, const std::string& source,
                                  const std::string& destination,
                                  const private_lease::PrivateLeaseRecord& expected_record,
                                  NativeIdentityV1 expected_identity,
                                  const DistributedSievePrivateLeaseClosedSnapshot& successor,
                                  DistributedSievePrivateLeaseRecoveryEdge edge);
    void unlink_exact_marker(int parent_fd, const std::string& leaf,
                             const private_lease::PrivateLeaseRecord& expected_record,
                             NativeIdentityV1 expected_identity,
                             const DistributedSievePrivateLeaseClosedSnapshot& successor,
                             DistributedSievePrivateLeaseRecoveryEdge edge);
    void
    rename_final_directory_to_staging(const DistributedSievePrivateLeaseClosedSnapshot& successor,
                                      DistributedSievePrivateLeaseRecoveryEdge edge);
    void remove_exact_empty_staging_directory(
        const DistributedSievePrivateLeaseClosedSnapshot& successor,
        DistributedSievePrivateLeaseRecoveryEdge edge);
    [[nodiscard]] bool offer_boundary(DistributedSievePrivateLeaseReservationBoundary boundary);

    DistributedSievePrivateLeaseRootClaim& claim_;
    DistributedSievePrivateLeaseRecoveryTestHooks hooks_;
    DistributedSievePrivateLeaseClosedSnapshot current_;
    DistributedSievePrivateLeaseNamesV1 private_lease_names_;
    std::optional<DistributedSieveWorkerAttemptNamesV1> worker_attempt_names_;
    std::size_t target_index_ = 0;
    std::string staging_directory_leaf_;
    std::optional<DistributedSieveStartedAttemptCleanupAdmission> started_admission_;
    std::optional<DistributedSieveStartedMergeCleanupAdmissionV1> merge_started_admission_;
    UniqueFd directory_;
    std::optional<DistributedSievePrivateLeaseRecoveryEdge> injected_sync_failure_;
    std::optional<DistributedSievePrivateLeaseReservationBoundary> interrupted_boundary_;
    bool completed_ = false;
    bool rejected_ = false;
};

#endif

#if !defined(_WIN32)

DistributedSieveFdPrivateLeaseRecoveryTarget::DistributedSieveFdPrivateLeaseRecoveryTarget(
    DistributedSievePrivateLeaseRootClaim& claim,
    DistributedSievePrivateLeaseRecoveryTestHooks hooks)
    : claim_(claim), hooks_(hooks) {
    initialize_common();
    require_prestart_record_absent();
    require_current();
}

DistributedSieveFdPrivateLeaseRecoveryTarget::DistributedSieveFdPrivateLeaseRecoveryTarget(
    DistributedSievePrivateLeaseRootClaim& claim,
    DistributedSieveStartedAttemptCleanupAdmission&& admission,
    DistributedSievePrivateLeaseRecoveryTestHooks hooks)
    : claim_(claim), hooks_(hooks), started_admission_(std::move(admission)) {
    initialize_common();
    require_started_record_exact();
    require_current();
}

DistributedSieveFdPrivateLeaseRecoveryTarget::DistributedSieveFdPrivateLeaseRecoveryTarget(
    DistributedSievePrivateLeaseRootClaim& claim,
    DistributedSieveStartedMergeCleanupAdmissionV1&& admission,
    DistributedSievePrivateLeaseRecoveryTestHooks hooks)
    : claim_(claim), hooks_(hooks), merge_started_admission_(std::move(admission)) {
    initialize_common();
    require_started_record_exact();
    require_current();
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::initialize_common() {
    const bool worker_target = claim_.worker_attempt_names_.has_value();
    const bool merge_target = claim_.merge_generation_names_.has_value();
    if (!claim_.owned_by_current_process() || claim_.wave_store_state_ == nullptr ||
        claim_.base_lock_at_ == nullptr || worker_target == merge_target ||
        !claim_.expected_private_lease_base_lock_leaves_.has_value() ||
        !claim_.expected_private_lease_base_lock_identities_.has_value() ||
        !claim_.expected_private_lease_reservation_witnesses_.has_value() ||
        !claim_.expected_worker_attempt_record_witnesses_.has_value() ||
        !claim_.expected_merge_started_record_witnesses_.has_value() ||
        !claim_.expected_chunk_terminal_failure_record_witnesses_.has_value() ||
        claim_.base_lock_acquisition_ !=
            DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    if (const auto validated = claim_.revalidate();
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(validated);
    }

    const auto& state = *claim_.wave_store_state_;
    auto observed =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!observed) {
        fail(adjudicate_observation_failure(std::move(observed.diagnostic)));
    }
    if (observed.inventory->private_lease_base_lock_leaves !=
            *claim_.expected_private_lease_base_lock_leaves_ ||
        *observed.base_lock_identities != *claim_.expected_private_lease_base_lock_identities_ ||
        *observed.private_lease_witnesses !=
            *claim_.expected_private_lease_reservation_witnesses_ ||
        observed.inventory->worker_attempt_records !=
            *claim_.expected_worker_attempt_record_witnesses_ ||
        observed.inventory->merge_started_records !=
            *claim_.expected_merge_started_record_witnesses_ ||
        observed.inventory->chunk_terminal_failure_records !=
            *claim_.expected_chunk_terminal_failure_record_witnesses_) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    current_ = private_lease_closed_snapshot(std::move(observed));
    private_lease_names_ =
        worker_target
            ? static_cast<const DistributedSievePrivateLeaseNamesV1&>(*claim_.worker_attempt_names_)
            : static_cast<const DistributedSievePrivateLeaseNamesV1&>(
                  *claim_.merge_generation_names_);
    worker_attempt_names_ = claim_.worker_attempt_names_;

    const auto target = std::lower_bound(current_.inventory.private_lease_base_lock_leaves.begin(),
                                         current_.inventory.private_lease_base_lock_leaves.end(),
                                         private_lease_names_.base_lock_leaf);
    if (target == current_.inventory.private_lease_base_lock_leaves.end() ||
        *target != private_lease_names_.base_lock_leaf) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    target_index_ = static_cast<std::size_t>(
        std::distance(current_.inventory.private_lease_base_lock_leaves.begin(), target));
    if (target_index_ >= current_.base_lock_identities.size() ||
        target_index_ >= current_.reservation_witnesses.size() ||
        current_.base_lock_identities[target_index_] != claim_.base_lock_at_->identity()) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }

    const auto& initial = current_witness();
    if (initial.base_lock_leaf != private_lease_names_.base_lock_leaf ||
        initial.boundary == DistributedSievePrivateLeaseReservationBoundary::Count ||
        initial.work_package_residue.has_value() || initial.merge_raw_writer_recovery.has_value() ||
        (worker_target && initial.merge_prepared.has_value()) ||
        (!worker_target &&
         (initial.worker_handoff.has_value() || initial.merge_prepared.has_value()))) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    if (initial.boundary == DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
        if (initial.lease_id != std::array<std::uint64_t, 2>{} ||
            initial.reserved_marker_identity.has_value() ||
            initial.directory_identity.has_value() || initial.owner_marker_identity.has_value() ||
            initial.owned_marker_identity.has_value()) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
    } else {
        if (initial.lease_id == std::array<std::uint64_t, 2>{}) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        staging_directory_leaf_ =
            private_lease_staging_directory_leaf(private_lease_names_, initial.lease_id);
    }

    if (private_lease_boundary_has_directory(initial.boundary)) {
        if (!initial.directory_identity.has_value()) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const std::string& named_leaf =
            initial.boundary ==
                    DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable
                ? private_lease_names_.private_directory_leaf
                : staging_directory_leaf_;
        auto inspected =
            inspect_private_lease_directory_at(state.root_fd, named_leaf, state.creator_process_id);
        if (!inspected || inspected.identity != initial.directory_identity) {
            fail(adjudicate_observation_failure(
                inspected ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                       protocol_error())
                          : std::move(inspected.diagnostic)));
        }
        directory_ = std::move(inspected.directory);
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::require_prestart_record_absent() const {
    const bool worker_target = worker_attempt_names_.has_value();
    const bool record_present =
        worker_target
            ? (std::binary_search(current_.inventory.worker_attempt_record_leaves.begin(),
                                  current_.inventory.worker_attempt_record_leaves.end(),
                                  worker_attempt_names_->canonical_record_leaf) ||
               std::binary_search(current_.inventory.worker_attempt_record_leaves.begin(),
                                  current_.inventory.worker_attempt_record_leaves.end(),
                                  worker_attempt_names_->pending_record_leaf))
            : (std::binary_search(current_.inventory.merge_started_record_leaves.begin(),
                                  current_.inventory.merge_started_record_leaves.end(),
                                  claim_.merge_generation_names_->canonical_record_leaf) ||
               std::binary_search(current_.inventory.merge_started_record_leaves.begin(),
                                  current_.inventory.merge_started_record_leaves.end(),
                                  claim_.merge_generation_names_->pending_record_leaf));
    if (started_admission_.has_value() || merge_started_admission_.has_value() || record_present) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::require_started_record_exact() const {
    if (started_admission_.has_value() == merge_started_admission_.has_value()) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    if (started_admission_.has_value()) {
        if (!worker_attempt_names_.has_value() ||
            started_admission_->worker_attempt_names_ != *worker_attempt_names_ ||
            started_admission_->creator_process_id_ != claim_.creator_process_id_ ||
            !started_admission_->record_witness_.canonical_snapshot.has_value() ||
            started_admission_->record_witness_.pending_snapshot.has_value() ||
            !std::binary_search(current_.inventory.worker_attempt_record_leaves.begin(),
                                current_.inventory.worker_attempt_record_leaves.end(),
                                worker_attempt_names_->canonical_record_leaf) ||
            std::binary_search(current_.inventory.worker_attempt_record_leaves.begin(),
                               current_.inventory.worker_attempt_record_leaves.end(),
                               worker_attempt_names_->pending_record_leaf)) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto& expected = started_admission_->record_witness_;
        const auto target = std::lower_bound(
            current_.inventory.worker_attempt_records.begin(),
            current_.inventory.worker_attempt_records.end(),
            std::pair{expected.chunk_id, expected.attempt_ordinal},
            [](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate,
               const std::pair<std::uint32_t, std::uint32_t>& coordinate) {
                return std::pair{candidate.chunk_id, candidate.attempt_ordinal} < coordinate;
            });
        if (target == current_.inventory.worker_attempt_records.end() || !(*target == expected)) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
    } else {
        const auto& admission = *merge_started_admission_;
        if (worker_attempt_names_.has_value() || !claim_.merge_generation_names_.has_value() ||
            admission.merge_generation_names_ != *claim_.merge_generation_names_ ||
            admission.creator_process_id_ != claim_.creator_process_id_ ||
            !admission.record_witness_.canonical_snapshot.has_value() ||
            admission.record_witness_.pending_snapshot.has_value() ||
            !std::binary_search(current_.inventory.merge_started_record_leaves.begin(),
                                current_.inventory.merge_started_record_leaves.end(),
                                admission.merge_generation_names_.canonical_record_leaf) ||
            std::binary_search(current_.inventory.merge_started_record_leaves.begin(),
                               current_.inventory.merge_started_record_leaves.end(),
                               admission.merge_generation_names_.pending_record_leaf)) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target = std::lower_bound(
            current_.inventory.merge_started_records.begin(),
            current_.inventory.merge_started_records.end(),
            admission.record_witness_.merge_attempt_ordinal,
            [](const DistributedSieveMergeStartedRecordInventoryWitnessV1& candidate,
               std::uint32_t ordinal) { return candidate.merge_attempt_ordinal < ordinal; });
        if (target == current_.inventory.merge_started_records.end() ||
            !(*target == admission.record_witness_)) {
            fail(adjudicate_observation_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(pinned));
    }
}

[[noreturn]] void
DistributedSieveFdPrivateLeaseRecoveryTarget::fail(DistributedSieveWaveStoreDiagnostic outcome) {
    throw Failure{std::move(outcome)};
}

const PrivateLeaseReservationWitness&
DistributedSieveFdPrivateLeaseRecoveryTarget::current_witness() const {
    if (target_index_ >= current_.reservation_witnesses.size()) {
        fail(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
    }
    return current_.reservation_witnesses[target_index_];
}

private_lease::PrivateLeaseReservationBoundary
DistributedSieveFdPrivateLeaseRecoveryTarget::boundary() const {
    return static_cast<private_lease::PrivateLeaseReservationBoundary>(
        static_cast<std::uint8_t>(current_witness().boundary));
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::require_phase(
    DistributedSievePrivateLeaseReservationBoundary expected) const {
    if (current_witness().boundary != expected) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::before_mutation() {
    require_current();
    const auto& current = current_witness();
    const bool carried_merge_owner_only_projection = [&] {
        if (!merge_started_admission_.has_value() ||
            current.boundary !=
                DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable ||
            !current.merge_raw_writer_recovery.has_value() ||
            !current.directory_identity.has_value() || !current.owner_marker_identity.has_value() ||
            !current.owned_marker_identity.has_value()) {
            return false;
        }
        const auto& recovery = *current.merge_raw_writer_recovery;
        return recovery.phase ==
                   DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryOwnerOnly &&
               recovery.lease_id == current.lease_id &&
               recovery.directory_identity == *current.directory_identity &&
               recovery.owner_marker_identity == *current.owner_marker_identity &&
               recovery.owned_marker_identity == *current.owned_marker_identity &&
               recovery.reserved_marker_identity == current.reserved_marker_identity &&
               !recovery.index.has_value() && !recovery.data.has_value();
    }();
    if (current.work_package_residue.has_value() || current.worker_handoff.has_value() ||
        current.merge_prepared.has_value() ||
        (current.merge_raw_writer_recovery.has_value() && !carried_merge_owner_only_projection)) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required, protocol_error()));
    }
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::select_injected_sync_failure(
    DistributedSievePrivateLeaseRecoveryEdge edge) {
    injected_sync_failure_.reset();
    require_current();
    if (hooks_.fail_before_sync == nullptr) {
        return;
    }
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
    const bool selected = hooks_.fail_before_sync(edge, hooks_.context);
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
    require_current();
    if (selected) {
        injected_sync_failure_ = edge;
    }
}

private_lease::PrivateLeaseRecord
DistributedSieveFdPrivateLeaseRecoveryTarget::expected_reserved_record() const {
    const auto& state = *claim_.wave_store_state_;
    const auto& witness = current_witness();
    if (witness.lease_id == std::array<std::uint64_t, 2>{}) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    const std::filesystem::path base_path =
        state.absolute_root / private_lease_names_.private_directory_leaf / "corpus";
    return {
        .platform_id = private_lease::PLATFORM_ID,
        .phase = private_lease::PrivateLeasePhase::Reserved,
        .capability = private_lease::PrivateLeaseCapability::RollbackStagingOnly,
        .lease_id = witness.lease_id,
        .base_path_digest = private_lease::frozen_path_digest(base_path),
        .parent_identity = relation_identity(state.root_identity),
        .lock_identity = relation_identity(claim_.base_lock_at_->identity()),
    };
}

private_lease::PrivateLeaseRecord
DistributedSieveFdPrivateLeaseRecoveryTarget::expected_owner_record() const {
    if (!current_witness().directory_identity.has_value()) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    return private_lease::make_private_lease_owner_record(
        expected_reserved_record(), relation_identity(*current_witness().directory_identity));
}

private_lease::PrivateLeaseRecord
DistributedSieveFdPrivateLeaseRecoveryTarget::expected_owned_record() const {
    if (!current_witness().owner_marker_identity.has_value()) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    return private_lease::make_private_lease_owned_record(
        expected_owner_record(), relation_identity(*current_witness().owner_marker_identity));
}

PrivateLeaseMarkerAtResult DistributedSieveFdPrivateLeaseRecoveryTarget::hold_exact_marker(
    int parent_fd, const std::string& leaf,
    const private_lease::PrivateLeaseRecord& expected_record,
    const NativeIdentityV1& expected_identity) const {
    auto held = read_private_lease_marker_at(parent_fd, leaf, claim_.creator_process_id_);
    if (!held) {
        fail(adjudicate_observation_failure(std::move(held.diagnostic)));
    }
    if (*held.record != expected_record || *held.identity != expected_identity) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    if (const auto validated = validate_named_private_lease_marker_handle(
            parent_fd, leaf.c_str(), held.marker.get(), expected_record, expected_identity,
            claim_.creator_process_id_);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
    return held;
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::insert_protocol_leaf(
    NamespaceInventory& inventory, const std::string& leaf) {
    const auto position = std::lower_bound(inventory.private_lease_protocol_leaves.begin(),
                                           inventory.private_lease_protocol_leaves.end(), leaf);
    if (position != inventory.private_lease_protocol_leaves.end() && *position == leaf) {
        fail(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    inventory.private_lease_protocol_leaves.insert(position, leaf);
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::erase_protocol_leaf(
    NamespaceInventory& inventory, const std::string& leaf) {
    const auto position = std::lower_bound(inventory.private_lease_protocol_leaves.begin(),
                                           inventory.private_lease_protocol_leaves.end(), leaf);
    if (position == inventory.private_lease_protocol_leaves.end() || *position != leaf) {
        fail(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    inventory.private_lease_protocol_leaves.erase(position);
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::replace_protocol_leaf(
    NamespaceInventory& inventory, const std::string& source, const std::string& destination) {
    erase_protocol_leaf(inventory, source);
    insert_protocol_leaf(inventory, destination);
}

DistributedSievePrivateLeaseClosedSnapshot
DistributedSieveFdPrivateLeaseRecoveryTarget::expected_successor(
    private_lease::PrivateLeaseRecoveryTransition transition) const {
    const auto source = wave_private_lease_boundary(transition.source);
    const auto successor = wave_private_lease_boundary(transition.successor);
    const auto transition_index =
        source == DistributedSievePrivateLeaseReservationBoundary::PermitAcquired
            ? private_lease::PRIVATE_LEASE_RECOVERY_TRANSITIONS.size()
            : static_cast<std::size_t>(source) - 1U;
    if (source == DistributedSievePrivateLeaseReservationBoundary::Count ||
        successor == DistributedSievePrivateLeaseReservationBoundary::Count ||
        source != current_witness().boundary ||
        static_cast<std::size_t>(successor) + 1U != static_cast<std::size_t>(source) ||
        transition_index >= private_lease::PRIVATE_LEASE_RECOVERY_TRANSITIONS.size() ||
        private_lease::PRIVATE_LEASE_RECOVERY_TRANSITIONS[transition_index] != transition) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }

    DistributedSievePrivateLeaseClosedSnapshot expected = current_;
    auto& witness = expected.reservation_witnesses[target_index_];
    witness.boundary = successor;
    if (successor == DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
        witness.lease_id = {};
    }
    if (!private_lease_boundary_has_reserved(successor)) {
        witness.reserved_marker_identity.reset();
    }
    if (!private_lease_boundary_has_directory(successor)) {
        witness.directory_identity.reset();
    }
    if (!private_lease_boundary_has_owner(successor)) {
        witness.owner_marker_identity.reset();
    }
    if (!private_lease_boundary_has_owned(successor)) {
        witness.owned_marker_identity.reset();
    }

    switch (transition.action) {
    case private_lease::PrivateLeaseRecoveryAction::UnlinkExactReservedPending:
        erase_protocol_leaf(expected.inventory, private_lease_names_.reserved_pending_leaf);
        break;
    case private_lease::PrivateLeaseRecoveryAction::RenameReservedCanonicalToPendingNoReplace:
        replace_protocol_leaf(expected.inventory, private_lease_names_.reserved_leaf,
                              private_lease_names_.reserved_pending_leaf);
        break;
    case private_lease::PrivateLeaseRecoveryAction::RemoveExactEmptyStagingDirectory:
        erase_protocol_leaf(expected.inventory, staging_directory_leaf_);
        break;
    case private_lease::PrivateLeaseRecoveryAction::UnlinkExactOwnerPending:
    case private_lease::PrivateLeaseRecoveryAction::RenameOwnerCanonicalToPendingNoReplace:
        break;
    case private_lease::PrivateLeaseRecoveryAction::UnlinkExactOwnedPending:
        erase_protocol_leaf(expected.inventory, private_lease_names_.owned_pending_leaf);
        break;
    case private_lease::PrivateLeaseRecoveryAction::RenameOwnedCanonicalToPendingNoReplace:
        replace_protocol_leaf(expected.inventory, private_lease_names_.owned_leaf,
                              private_lease_names_.owned_pending_leaf);
        // A canonical MergeStarted makes the otherwise ordinary P7 shape
        // indistinguishable from relation preactive recovery's owner-only
        // prefix.  The read-only classifier therefore carries that typed
        // witness at P7; the P7->P6 edge removes it together with canonical
        // OWNED.
        witness.merge_raw_writer_recovery.reset();
        break;
    case private_lease::PrivateLeaseRecoveryAction::RenameFinalDirectoryToStagingNoReplace:
        replace_protocol_leaf(expected.inventory, private_lease_names_.private_directory_leaf,
                              staging_directory_leaf_);
        if (merge_started_admission_.has_value() && witness.directory_identity.has_value() &&
            witness.owner_marker_identity.has_value() &&
            witness.owned_marker_identity.has_value()) {
            witness.merge_raw_writer_recovery =
                DistributedSieveMergeRawWriterRecoveryInventoryWitnessV1{
                    .phase =
                        DistributedSieveMergeRawWriterRecoveryPhaseV1::StagingDirectoryOwnerOnly,
                    .lease_id = witness.lease_id,
                    .directory_identity = *witness.directory_identity,
                    .owner_marker_identity = *witness.owner_marker_identity,
                    .owned_marker_identity = *witness.owned_marker_identity,
                    .reserved_marker_identity = witness.reserved_marker_identity,
                };
        }
        break;
    case private_lease::PrivateLeaseRecoveryAction::Count:
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    return expected;
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveFdPrivateLeaseRecoveryTarget::adjudicate_observation_failure(
    DistributedSieveWaveStoreDiagnostic lower) const noexcept {
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        return pinned;
    }
    return lower;
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveFdPrivateLeaseRecoveryTarget::validate_started_record_pin() const noexcept {
    if (!started_admission_.has_value() && !merge_started_admission_.has_value()) {
        return {};
    }
    if (claim_.wave_store_state_ == nullptr) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }
    return started_admission_.has_value()
               ? started_admission_->revalidate(claim_.wave_store_state_->root_fd)
               : merge_started_admission_->revalidate(claim_.wave_store_state_->root_fd);
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveFdPrivateLeaseRecoveryTarget::validate_directory_handle(
    const PrivateLeaseReservationWitness& witness) const noexcept {
    if (!private_lease_boundary_has_directory(witness.boundary)) {
        return directory_ ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                       protocol_error())
                          : DistributedSieveWaveStoreDiagnostic{};
    }
    if (!directory_ || !witness.directory_identity.has_value()) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    const std::string& named_leaf =
        witness.boundary == DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable
            ? private_lease_names_.private_directory_leaf
            : staging_directory_leaf_;
    return validate_private_lease_directory_binding(
        claim_.wave_store_state_->root_fd, directory_.get(), named_leaf, claim_.creator_process_id_,
        *witness.directory_identity);
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveFdPrivateLeaseRecoveryTarget::validate_exact_snapshot(
    const DistributedSievePrivateLeaseClosedSnapshot& expected) const noexcept {
    const auto& state = *claim_.wave_store_state_;
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    auto first =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!first) {
        return adjudicate_observation_failure(std::move(first.diagnostic));
    }
    if (*first.inventory != expected.inventory ||
        *first.base_lock_identities != expected.base_lock_identities ||
        *first.private_lease_witnesses != expected.reservation_witnesses) {
        return adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(pinned);
    }
    if (const auto directory =
            validate_directory_handle(expected.reservation_witnesses[target_index_]);
        directory.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(directory);
    }
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    auto confirmed =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!confirmed) {
        return adjudicate_observation_failure(std::move(confirmed.diagnostic));
    }
    if (*confirmed.inventory != *first.inventory ||
        *confirmed.base_lock_identities != *first.base_lock_identities ||
        *confirmed.private_lease_witnesses != *first.private_lease_witnesses ||
        *confirmed.inventory != expected.inventory ||
        *confirmed.base_lock_identities != expected.base_lock_identities ||
        *confirmed.private_lease_witnesses != expected.reservation_witnesses) {
        return adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(pinned);
    }
    if (const auto directory =
            validate_directory_handle(expected.reservation_witnesses[target_index_]);
        directory.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(directory);
    }
    return claim_.revalidate_authority();
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::require_current() const {
    if (const auto validated = validate_exact_snapshot(current_);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(validated);
    }
}

template <typename LocalValidator>
DistributedSieveWaveStoreDiagnostic
DistributedSieveFdPrivateLeaseRecoveryTarget::adjudicate_operation_failure(
    DistributedSieveWaveStoreDiagnostic lower,
    const DistributedSievePrivateLeaseClosedSnapshot& possible_successor,
    DistributedSievePrivateLeaseRecoveryEdge edge, LocalValidator&& validate_local) const noexcept {
    const auto& state = *claim_.wave_store_state_;
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    auto first =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!first) {
        return adjudicate_observation_failure(std::move(first.diagnostic));
    }
    const bool predecessor = *first.inventory == current_.inventory &&
                             *first.base_lock_identities == current_.base_lock_identities &&
                             *first.private_lease_witnesses == current_.reservation_witnesses;
    const bool successor =
        *first.inventory == possible_successor.inventory &&
        *first.base_lock_identities == possible_successor.base_lock_identities &&
        *first.private_lease_witnesses == possible_successor.reservation_witnesses;
    if (predecessor == successor) {
        return adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(pinned);
    }
    if (const auto local = validate_local(successor);
        local.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(local);
    }
    if (successor && hooks_.after_first_successor_validation != nullptr) {
        if (!process_matches(claim_.creator_process_id_)) {
            return process_mismatch();
        }
        hooks_.after_first_successor_validation(edge, hooks_.context);
        if (!process_matches(claim_.creator_process_id_)) {
            return process_mismatch();
        }
    }
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    auto confirmed =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!confirmed) {
        return adjudicate_observation_failure(std::move(confirmed.diagnostic));
    }
    if (*confirmed.inventory != *first.inventory ||
        *confirmed.base_lock_identities != *first.base_lock_identities ||
        *confirmed.private_lease_witnesses != *first.private_lease_witnesses) {
        return adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(pinned);
    }
    if (const auto local = validate_local(successor);
        local.status != DistributedSieveWaveStoreStatus::ready) {
        return adjudicate_observation_failure(local);
    }
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    return lower;
}

template <typename LocalValidator>
void DistributedSieveFdPrivateLeaseRecoveryTarget::accept_successor(
    const DistributedSievePrivateLeaseClosedSnapshot& expected,
    DistributedSievePrivateLeaseRecoveryEdge edge, LocalValidator&& validate_local) {
    const auto& state = *claim_.wave_store_state_;
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        fail(authority);
    }
    auto first =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!first) {
        fail(adjudicate_observation_failure(std::move(first.diagnostic)));
    }
    if (*first.inventory != expected.inventory ||
        *first.base_lock_identities != expected.base_lock_identities ||
        *first.private_lease_witnesses != expected.reservation_witnesses) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(pinned));
    }
    if (const auto directory =
            validate_directory_handle(expected.reservation_witnesses[target_index_]);
        directory.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(directory));
    }
    if (const auto local = validate_local(true);
        local.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(local));
    }
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
    if (hooks_.after_first_successor_validation != nullptr) {
        hooks_.after_first_successor_validation(edge, hooks_.context);
    }
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        fail(authority);
    }
    auto confirmed =
        capture_manifest_bound_inventory_witness(state.root_fd, state.manifest, state.absolute_root,
                                                 state.creator_process_id, held_inventory());
    if (!confirmed) {
        fail(adjudicate_observation_failure(std::move(confirmed.diagnostic)));
    }
    if (*confirmed.inventory != *first.inventory ||
        *confirmed.base_lock_identities != *first.base_lock_identities ||
        *confirmed.private_lease_witnesses != *first.private_lease_witnesses ||
        *confirmed.inventory != expected.inventory ||
        *confirmed.base_lock_identities != expected.base_lock_identities ||
        *confirmed.private_lease_witnesses != expected.reservation_witnesses) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    if (const auto pinned = validate_started_record_pin();
        pinned.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(pinned));
    }
    if (const auto directory =
            validate_directory_handle(expected.reservation_witnesses[target_index_]);
        directory.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(directory));
    }
    if (const auto local = validate_local(true);
        local.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(local));
    }
    if (const auto authority = claim_.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        fail(authority);
    }
    current_ = private_lease_closed_snapshot(std::move(first));
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::rename_marker_no_replace(
    int parent_fd, const std::string& source, const std::string& destination,
    const private_lease::PrivateLeaseRecord& expected_record, NativeIdentityV1 expected_identity,
    const DistributedSievePrivateLeaseClosedSnapshot& successor,
    DistributedSievePrivateLeaseRecoveryEdge edge) {
    before_mutation();
    auto held = hold_exact_marker(parent_fd, source, expected_record, expected_identity);
    const auto validate_local = [&](bool visible_successor) noexcept {
        const std::string& leaf = visible_successor ? destination : source;
        return validate_named_private_lease_marker_handle(
            parent_fd, leaf.c_str(), held.marker.get(), expected_record, expected_identity,
            claim_.creator_process_id_);
    };
    require_current();
    if (const auto renamed =
            private_lease_rename_no_replace_at(parent_fd, source.c_str(), destination.c_str());
        renamed.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(renamed, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    if (const auto synchronized =
            private_lease_recovery_sync_handle(parent_fd, edge, injected_sync_failure_);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(synchronized, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    accept_successor(successor, edge, validate_local);
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::unlink_exact_marker(
    int parent_fd, const std::string& leaf,
    const private_lease::PrivateLeaseRecord& expected_record, NativeIdentityV1 expected_identity,
    const DistributedSievePrivateLeaseClosedSnapshot& successor,
    DistributedSievePrivateLeaseRecoveryEdge edge) {
    before_mutation();
    auto held = hold_exact_marker(parent_fd, leaf, expected_record, expected_identity);
    const auto validate_local = [&](bool visible_successor) noexcept {
        return visible_successor ? validate_unlinked_private_lease_marker_handle(
                                       parent_fd, leaf.c_str(), held.marker.get(), expected_record,
                                       expected_identity, claim_.creator_process_id_)
                                 : validate_named_private_lease_marker_handle(
                                       parent_fd, leaf.c_str(), held.marker.get(), expected_record,
                                       expected_identity, claim_.creator_process_id_);
    };
    require_current();
    if (const auto removed = private_lease_unlink_at(parent_fd, leaf.c_str(), 0);
        removed.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(removed, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    if (const auto synchronized =
            private_lease_recovery_sync_handle(parent_fd, edge, injected_sync_failure_);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(synchronized, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    accept_successor(successor, edge, validate_local);
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::rename_final_directory_to_staging(
    const DistributedSievePrivateLeaseClosedSnapshot& successor,
    DistributedSievePrivateLeaseRecoveryEdge edge) {
    require_phase(DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable);
    before_mutation();
    if (!directory_ || !current_witness().directory_identity.has_value()) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    const auto identity = *current_witness().directory_identity;
    const auto& state = *claim_.wave_store_state_;
    const auto validate_local = [&](bool visible_successor) noexcept {
        const std::string& leaf = visible_successor ? staging_directory_leaf_
                                                    : private_lease_names_.private_directory_leaf;
        return validate_private_lease_directory_binding(state.root_fd, directory_.get(), leaf,
                                                        claim_.creator_process_id_, identity);
    };
    if (const auto validated = validate_local(false);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
    require_current();
    if (const auto renamed = private_lease_rename_no_replace_at(
            state.root_fd, private_lease_names_.private_directory_leaf.c_str(),
            staging_directory_leaf_.c_str());
        renamed.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(renamed, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    if (const auto synchronized =
            private_lease_recovery_sync_handle(state.root_fd, edge, injected_sync_failure_);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(synchronized, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    accept_successor(successor, edge, validate_local);
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::remove_exact_empty_staging_directory(
    const DistributedSievePrivateLeaseClosedSnapshot& successor,
    DistributedSievePrivateLeaseRecoveryEdge edge) {
    require_phase(DistributedSievePrivateLeaseReservationBoundary::StagingDirectoryDurable);
    before_mutation();
    if (!directory_ || !current_witness().directory_identity.has_value()) {
        fail(adjudicate_observation_failure(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
    }
    const auto identity = *current_witness().directory_identity;
    const auto& state = *claim_.wave_store_state_;
    if (const auto validated = validate_private_lease_directory_binding(
            state.root_fd, directory_.get(), staging_directory_leaf_, claim_.creator_process_id_,
            identity);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
    if (const auto empty = validate_exact_empty_private_lease_directory_handle(
            directory_.get(), claim_.creator_process_id_);
        empty.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(empty));
    }
    require_current();

    if (hooks_.before_staging_directory_remove != nullptr) {
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        hooks_.before_staging_directory_remove(edge, hooks_.context);
        if (!process_matches(claim_.creator_process_id_)) {
            fail(process_mismatch());
        }
        require_current();
    }

    UniqueFd removed_directory = std::move(directory_);
    const auto validate_local = [&](bool visible_successor) noexcept {
        if (visible_successor) {
            return validate_unlinked_private_lease_directory_handle(
                state.root_fd, staging_directory_leaf_.c_str(), removed_directory.get(), identity,
                claim_.creator_process_id_);
        }
        if (const auto bound = validate_private_lease_directory_binding(
                state.root_fd, removed_directory.get(), staging_directory_leaf_,
                claim_.creator_process_id_, identity);
            bound.status != DistributedSieveWaveStoreStatus::ready) {
            return bound;
        }
        return validate_exact_empty_private_lease_directory_handle(removed_directory.get(),
                                                                   claim_.creator_process_id_);
    };
    if (const auto validated = validate_local(false);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
    if (const auto removed =
            private_lease_unlink_at(state.root_fd, staging_directory_leaf_.c_str(), AT_REMOVEDIR);
        removed.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(removed, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    if (const auto synchronized =
            private_lease_recovery_sync_handle(state.root_fd, edge, injected_sync_failure_);
        synchronized.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(synchronized, successor, edge, validate_local));
    }
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_operation_failure(validated, successor, edge, validate_local));
    }
    accept_successor(successor, edge, validate_local);
    if (const auto validated = validate_local(true);
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(adjudicate_observation_failure(validated));
    }
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::apply(
    private_lease::PrivateLeaseRecoveryTransition transition) {
    const auto successor = expected_successor(transition);
    const DistributedSievePrivateLeaseRecoveryEdge edge{
        .source = wave_private_lease_boundary(transition.source),
        .successor = wave_private_lease_boundary(transition.successor),
    };
    select_injected_sync_failure(edge);
    const int root_fd = claim_.wave_store_state_->root_fd;

    switch (transition.action) {
    case private_lease::PrivateLeaseRecoveryAction::UnlinkExactReservedPending: {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::ReservedPendingDurable);
        if (!current_witness().reserved_marker_identity.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        const auto record = expected_reserved_record();
        unlink_exact_marker(root_fd, private_lease_names_.reserved_pending_leaf, record,
                            *current_witness().reserved_marker_identity, successor, edge);
        return;
    }
    case private_lease::PrivateLeaseRecoveryAction::RenameReservedCanonicalToPendingNoReplace: {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::ReservedCanonicalDurable);
        if (!current_witness().reserved_marker_identity.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        const auto record = expected_reserved_record();
        rename_marker_no_replace(root_fd, private_lease_names_.reserved_leaf,
                                 private_lease_names_.reserved_pending_leaf, record,
                                 *current_witness().reserved_marker_identity, successor, edge);
        return;
    }
    case private_lease::PrivateLeaseRecoveryAction::RemoveExactEmptyStagingDirectory:
        remove_exact_empty_staging_directory(successor, edge);
        return;
    case private_lease::PrivateLeaseRecoveryAction::UnlinkExactOwnerPending: {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::OwnerPendingDurable);
        if (!directory_ || !current_witness().owner_marker_identity.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        const auto record = expected_owner_record();
        unlink_exact_marker(directory_.get(),
                            std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_PENDING_LEAF), record,
                            *current_witness().owner_marker_identity, successor, edge);
        return;
    }
    case private_lease::PrivateLeaseRecoveryAction::RenameOwnerCanonicalToPendingNoReplace: {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::OwnerCanonicalDurable);
        if (!directory_ || !current_witness().owner_marker_identity.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        const auto record = expected_owner_record();
        rename_marker_no_replace(directory_.get(),
                                 std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF),
                                 std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_PENDING_LEAF),
                                 record, *current_witness().owner_marker_identity, successor, edge);
        return;
    }
    case private_lease::PrivateLeaseRecoveryAction::UnlinkExactOwnedPending: {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::OwnedPendingDurable);
        if (!current_witness().owned_marker_identity.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        const auto record = expected_owned_record();
        unlink_exact_marker(root_fd, private_lease_names_.owned_pending_leaf, record,
                            *current_witness().owned_marker_identity, successor, edge);
        return;
    }
    case private_lease::PrivateLeaseRecoveryAction::RenameOwnedCanonicalToPendingNoReplace: {
        require_phase(DistributedSievePrivateLeaseReservationBoundary::OwnedCanonicalDurable);
        if (!current_witness().owned_marker_identity.has_value()) {
            fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                            invalid_argument_error()));
        }
        const auto record = expected_owned_record();
        rename_marker_no_replace(root_fd, private_lease_names_.owned_leaf,
                                 private_lease_names_.owned_pending_leaf, record,
                                 *current_witness().owned_marker_identity, successor, edge);
        return;
    }
    case private_lease::PrivateLeaseRecoveryAction::RenameFinalDirectoryToStagingNoReplace:
        rename_final_directory_to_staging(successor, edge);
        return;
    case private_lease::PrivateLeaseRecoveryAction::Count:
        break;
    }
    fail(diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
}

bool DistributedSieveFdPrivateLeaseRecoveryTarget::offer_boundary(
    DistributedSievePrivateLeaseReservationBoundary boundary) {
    require_current();
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
    const bool stop = hooks_.stop_after != nullptr && hooks_.stop_after(boundary, hooks_.context);
    if (!process_matches(claim_.creator_process_id_)) {
        fail(process_mismatch());
    }
    require_current();
    if (stop) {
        interrupted_boundary_ = boundary;
    }
    return stop;
}

bool DistributedSieveFdPrivateLeaseRecoveryTarget::checkpoint(
    private_lease::PrivateLeaseReservationBoundary boundary) {
    const auto wave_boundary = wave_private_lease_boundary(boundary);
    if (wave_boundary == DistributedSievePrivateLeaseReservationBoundary::Count ||
        current_witness().boundary != wave_boundary) {
        fail(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    return offer_boundary(wave_boundary);
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::complete() {
    require_phase(DistributedSievePrivateLeaseReservationBoundary::PermitAcquired);
    require_current();
    *claim_.expected_private_lease_base_lock_leaves_ =
        current_.inventory.private_lease_base_lock_leaves;
    *claim_.expected_private_lease_base_lock_identities_ = current_.base_lock_identities;
    *claim_.expected_private_lease_reservation_witnesses_ = current_.reservation_witnesses;
    *claim_.expected_worker_attempt_record_witnesses_ = current_.inventory.worker_attempt_records;
    *claim_.expected_merge_started_record_witnesses_ = current_.inventory.merge_started_records;
    *claim_.expected_chunk_terminal_failure_record_witnesses_ =
        current_.inventory.chunk_terminal_failure_records;
    if (const auto validated = claim_.revalidate();
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        fail(validated);
    }
    completed_ = true;
}

void DistributedSieveFdPrivateLeaseRecoveryTarget::reject_invalid_boundary() noexcept {
    rejected_ = true;
}

bool DistributedSieveFdPrivateLeaseRecoveryTarget::completed() const noexcept {
    return completed_ && !rejected_ && target_index_ < current_.reservation_witnesses.size() &&
           current_.reservation_witnesses[target_index_].boundary ==
               DistributedSievePrivateLeaseReservationBoundary::PermitAcquired;
}

std::optional<DistributedSieveReconciledWorkerAttemptV1>
DistributedSieveFdPrivateLeaseRecoveryTarget::started_completion() const {
    if (!completed() || !started_admission_.has_value() || claim_.wave_store_state_ == nullptr) {
        return std::nullopt;
    }
    require_current();
    require_started_record_exact();
    const auto& witness = started_admission_->record_witness_;
    std::optional<std::uint32_t> next_attempt_ordinal;
    if (witness.attempt_ordinal + 1U < claim_.wave_store_state_->manifest.max_worker_attempts) {
        next_attempt_ordinal = witness.attempt_ordinal + 1U;
    }
    return DistributedSieveReconciledWorkerAttemptV1{
        .record = witness.record,
        .canonical_snapshot = *witness.canonical_snapshot,
        .next_attempt_ordinal = next_attempt_ordinal,
    };
}

std::optional<DistributedSieveStartedAttemptCleanupAdmission>
DistributedSieveFdPrivateLeaseRecoveryTarget::take_started_admission() {
    if (!completed() || !started_admission_.has_value()) {
        return std::nullopt;
    }
    return std::move(started_admission_);
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveFdPrivateLeaseRecoveryTarget::interruption_diagnostic() const noexcept {
    auto outcome = diagnostic(DistributedSieveWaveStoreStatus::interrupted,
                              std::make_error_code(std::errc::operation_canceled));
    outcome.last_private_lease_recovery_boundary = interrupted_boundary_;
    return outcome;
}

#endif

namespace {

[[nodiscard]] DistributedSieveWaveStoreDiagnostic validate_recovered_merge_prepared_projection_once(
    const DistributedSieveWaveStore& store,
    const MergePreparedPublicationAggregateWitness& expected,
    HeldCanonicalPublicationInventoryV1 held) noexcept {
    const int root_fd = MergePreparedAdmissionRevalidatorAuthorityV1::root_fd(store);
    const auto creator_process_id =
        MergePreparedAdmissionRevalidatorAuthorityV1::creator_process_id(store);
    if (root_fd < 0 || creator_process_id == 0 || !process_matches(creator_process_id)) {
        return process_mismatch();
    }
    auto observed = inspect_namespace(root_fd);
    if (!observed) {
        return std::move(observed.diagnostic);
    }
    auto validated = validate_manifest_bound_inventory(
        root_fd, MergePreparedAdmissionRevalidatorAuthorityV1::absolute_root(store),
        *observed.inventory, MergePreparedAdmissionRevalidatorAuthorityV1::manifest(store),
        creator_process_id, {}, true, held);
    if (!validated) {
        return std::move(validated.diagnostic);
    }
    try {
        observed.inventory->worker_attempt_records = *validated.worker_attempt_records;
        observed.inventory->merge_started_records = *validated.merge_started_records;
        observed.inventory->chunk_terminal_failure_records =
            *validated.chunk_terminal_failure_records;
        observed.inventory->wave_merge_commit_records = *validated.wave_merge_commit_records;
        if (*observed.inventory != expected.inventory ||
            *validated.base_lock_identities != expected.base_lock_identities ||
            *validated.private_lease_witnesses != expected.private_leases ||
            *validated.worker_attempt_records != expected.worker_attempt_records ||
            *validated.merge_started_records != expected.merge_started_records ||
            *validated.chunk_terminal_failure_records != expected.chunk_terminal_failure_records ||
            *validated.wave_merge_commit_records != expected.wave_merge_commit_records) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }
    return process_matches(creator_process_id) ? DistributedSieveWaveStoreDiagnostic{}
                                               : process_mismatch();
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic revalidate_recovered_merge_prepared_projection(
    const DistributedSieveWaveStore& store,
    const MergePreparedPublicationAggregateWitness& expected,
    HeldCanonicalPublicationInventoryV1 held) noexcept {
    if (const auto authority =
            MergePreparedAdmissionRevalidatorAuthorityV1::revalidate_wave_store_authority(store);
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    if (auto first = validate_recovered_merge_prepared_projection_once(store, expected, held);
        first.status != DistributedSieveWaveStoreStatus::ready) {
        return first;
    }
    if (const auto authority =
            MergePreparedAdmissionRevalidatorAuthorityV1::revalidate_wave_store_authority(store);
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    if (auto second = validate_recovered_merge_prepared_projection_once(store, expected, held);
        second.status != DistributedSieveWaveStoreStatus::ready) {
        return second;
    }
    return MergePreparedAdmissionRevalidatorAuthorityV1::revalidate_wave_store_authority(store);
}

struct MergePreparedAdmissionRevalidationContextV1 final {
    const DistributedSieveWaveStore* store = nullptr;
    const MergePreparedPublicationAggregateWitness* expected = nullptr;
    const DistributedSieveMergePreparedResumeTestHooksV1* hooks = nullptr;
    HeldCanonicalPublicationInventoryV1 held;
    DistributedSieveRecoveredPreparedPublicationSubjectV1 subject =
        DistributedSieveRecoveredPreparedPublicationSubjectV1::Count;
    std::size_t manifest_slot = static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_NO_INDEX);
    std::size_t aggregate_revalidation_count = 0;
    DistributedSieveWaveStoreDiagnostic diagnostic;
};

[[nodiscard]] bool revalidate_merge_prepared_admission_for_relation(
    const gnfs::relation::OOCPrivateHandoffReader* current_reader, void* opaque) noexcept {
    auto* context = static_cast<MergePreparedAdmissionRevalidationContextV1*>(opaque);
    if (context == nullptr || context->store == nullptr || context->expected == nullptr ||
        context->hooks == nullptr || context->held.current == nullptr ||
        context->subject == DistributedSieveRecoveredPreparedPublicationSubjectV1::Count ||
        context->aggregate_revalidation_count >=
            static_cast<std::size_t>(DistributedSieveRecoveredPreparedAggregatePhaseV1::Count)) {
        return false;
    }
    const auto phase = static_cast<DistributedSieveRecoveredPreparedAggregatePhaseV1>(
        context->aggregate_revalidation_count++);
    const bool live_reader_phase =
        phase == DistributedSieveRecoveredPreparedAggregatePhaseV1::LiveReaderFinal;
    if (live_reader_phase != (current_reader != nullptr)) {
        context->diagnostic =
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        return false;
    }
    if (context->hooks->stop_before_recovered_aggregate_revalidation != nullptr &&
        context->hooks->stop_before_recovered_aggregate_revalidation(
            context->subject, context->manifest_slot, phase, context->hooks->context)) {
        context->diagnostic = diagnostic(DistributedSieveWaveStoreStatus::interrupted,
                                         std::make_error_code(std::errc::operation_canceled));
        return false;
    }
    if (!process_matches(
            MergePreparedAdmissionRevalidatorAuthorityV1::creator_process_id(*context->store))) {
        context->diagnostic = process_mismatch();
        return false;
    }
    auto held = context->held;
    held.current_reader = current_reader;
    context->diagnostic =
        revalidate_recovered_merge_prepared_projection(*context->store, *context->expected, held);
    return context->diagnostic.status == DistributedSieveWaveStoreStatus::ready;
}

struct RecoveredMergePreparedWorkerReaderV1 final {
    std::uint64_t expected_relation_count = 0;
    std::unique_ptr<gnfs::relation::OOCPrivateHandoffReader> reader;
};

class RecoveredMergePreparedWorkerReaderStackV1 final {
public:
    RecoveredMergePreparedWorkerReaderStackV1() = default;
    RecoveredMergePreparedWorkerReaderStackV1(const RecoveredMergePreparedWorkerReaderStackV1&) =
        delete;
    RecoveredMergePreparedWorkerReaderStackV1&
    operator=(const RecoveredMergePreparedWorkerReaderStackV1&) = delete;
    ~RecoveredMergePreparedWorkerReaderStackV1() {
        while (!entries.empty()) {
            entries.pop_back();
        }
    }

    std::vector<RecoveredMergePreparedWorkerReaderV1> entries;
};

/// Declaration order is the recovered-admission lock-release contract.
/// Reverse destruction closes the merged reader, then worker readers in
/// reverse manifest order, then the coordinator gate, and finally WaveLock.
struct RecoveredMergePreparedAdmissionStateV1 final
    : distributed_sieve_merge_writer_authority_detail::DistributedSieveMergePreparedOriginV1 {
    std::unique_ptr<DistributedSieveWaveStore> store;
    std::unique_ptr<DistributedSieveWorkerCoordinatorClaimV1> coordinator_claim;
    RecoveredMergePreparedWorkerReaderStackV1 worker_readers;
    std::unique_ptr<gnfs::relation::OOCPrivateHandoffReader> merged_reader;
    std::vector<MergeStartedV1> merge_started_chain;
    std::vector<std::optional<WorkerHandoffV1>> worker_handoffs;
    std::optional<distributed_sieve_merge_writer_authority_detail::
                      DistributedSieveMergeCommitPredecessorSnapshotsV1>
        predecessor_snapshots;
    MergePreparedV1 prepared_record;
    std::uint64_t creator_process_id = 0;

private:
    [[nodiscard]] bool
    prepared_origin_valid(const MergePreparedV1* stable_record,
                          std::uint64_t expected_creator_process_id) const noexcept override;

    [[nodiscard]] DistributedSieveWaveStore* retained_wave_store() noexcept override {
        return store.get();
    }

    [[nodiscard]] std::span<const MergeStartedV1>
    retained_merge_started_chain() const noexcept override {
        return merge_started_chain;
    }

    [[nodiscard]] const distributed_sieve_merge_writer_authority_detail::
        DistributedSieveMergeCommitPredecessorSnapshotsV1*
        retained_predecessor_snapshots() const noexcept override {
        return predecessor_snapshots ? std::addressof(*predecessor_snapshots) : nullptr;
    }

    [[nodiscard]] std::size_t retained_manifest_slot_count() const noexcept override {
        return worker_handoffs.size();
    }

    [[nodiscard]] const WorkerHandoffV1*
    retained_worker_handoff(std::size_t manifest_slot) const noexcept override {
        if (manifest_slot >= worker_handoffs.size() ||
            !worker_handoffs[manifest_slot].has_value()) {
            return nullptr;
        }
        return std::addressof(*worker_handoffs[manifest_slot]);
    }
};

[[nodiscard]] bool
recovered_merge_prepared_admission_state_valid(const void* opaque,
                                               const MergePreparedV1* stable_record,
                                               std::uint64_t creator_process_id) noexcept {
    try {
        const auto* state = static_cast<const RecoveredMergePreparedAdmissionStateV1*>(opaque);
        if (state == nullptr || stable_record == nullptr ||
            stable_record != std::addressof(state->prepared_record) || creator_process_id == 0 ||
            state->creator_process_id != creator_process_id ||
            !process_matches(creator_process_id) || state->store == nullptr ||
            state->coordinator_claim == nullptr ||
            !state->coordinator_claim->owned_by_current_process() ||
            !state->predecessor_snapshots.has_value() || state->merged_reader == nullptr ||
            !state->merged_reader->valid() ||
            state->store->manifest_digest() != state->prepared_record.manifest_digest ||
            state->merged_reader->reader().count() !=
                state->prepared_record.output_relation_count) {
            return false;
        }
        for (const auto& worker : state->worker_readers.entries) {
            if (worker.reader == nullptr || !worker.reader->valid() ||
                worker.reader->reader().count() != worker.expected_relation_count) {
                return false;
            }
        }
        return true;
    } catch (...) {
        return false;
    }
}

bool RecoveredMergePreparedAdmissionStateV1::prepared_origin_valid(
    const MergePreparedV1* stable_record,
    std::uint64_t expected_creator_process_id) const noexcept {
    return recovered_merge_prepared_admission_state_valid(this, stable_record,
                                                          expected_creator_process_id);
}

using MergeCommitPredecessorSnapshots = distributed_sieve_merge_writer_authority_detail::
    DistributedSieveMergeCommitPredecessorSnapshotsV1;

struct WaveMergeCommitDependencySnapshotValidationResult final {
    std::optional<MergeCommitPredecessorSnapshots> snapshots;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return snapshots.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] WaveMergeCommitDependencySnapshotValidationResult
validate_wave_merge_commit_dependency_snapshot(
    int root_fd, const NamespaceInventory& inventory, const WaveManifestV1& manifest,
    const MergePreparedV1& prepared, std::span<const MergeStartedV1> retained_starts,
    std::span<const WorkerHandoffV1* const> retained_handoffs, const WaveMergeCommitV1* commit,
    std::uint64_t creator_process_id,
    const MergeCommitPredecessorSnapshots* expected_snapshots = nullptr) noexcept {
    const auto conflict = [] {
        return WaveMergeCommitDependencySnapshotValidationResult{
            .snapshots = std::nullopt,
            .diagnostic =
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
        };
    };
    const auto protocol_conflict = [&](DistributedSieveProtocolStatus status) {
        auto failure = conflict();
        failure.diagnostic.protocol_status = status;
        return failure;
    };
    if (!process_matches(creator_process_id)) {
        return {.snapshots = std::nullopt, .diagnostic = process_mismatch()};
    }
    if (prepared.ordered_inputs.size() != manifest.chunks.size() ||
        prepared.per_chunk_retained_counts.size() != manifest.chunks.size() ||
        retained_handoffs.size() != manifest.chunks.size() || retained_starts.empty() ||
        !inventory.chunk_terminal_failure_record_leaves.empty() ||
        !inventory.cleanup_record_leaves.empty()) {
        return conflict();
    }

    try {
        MergeCommitPredecessorSnapshots observed_snapshots;
        std::size_t worker_attempt_count = 0;
        for (const auto& input : prepared.ordered_inputs) {
            if (input.durable_attempt_count >
                std::numeric_limits<std::size_t>::max() - worker_attempt_count) {
                return protocol_conflict(DistributedSieveProtocolStatus{
                    .error = DistributedSieveProtocolError::integer_out_of_range,
                });
            }
            worker_attempt_count += input.durable_attempt_count;
        }
        observed_snapshots.worker_attempts.reserve(worker_attempt_count);
        observed_snapshots.merge_starts.reserve(retained_starts.size());
        std::vector<std::vector<AttemptStartedV1>> attempt_chains(manifest.chunks.size());
        std::vector<std::string> expected_attempt_leaves;
        for (std::size_t slot = 0; slot < manifest.chunks.size(); ++slot) {
            const auto& plan = manifest.chunks[slot];
            const auto& input = prepared.ordered_inputs[slot];
            if (input.chunk_id != plan.chunk_id || input.sq_begin != plan.sq_begin ||
                input.sq_end != plan.sq_end ||
                (input.disposition == ChunkDispositionV1::handoff) !=
                    (retained_handoffs[slot] != nullptr)) {
                return conflict();
            }
            auto& chain = attempt_chains[slot];
            chain.reserve(input.durable_attempt_count);
            for (std::uint32_t ordinal = 0; ordinal < input.durable_attempt_count; ++ordinal) {
                const auto names = distributed_sieve_worker_attempt_names_v1(
                    plan.relative_artifact_stem, plan.chunk_id, ordinal);
                if (!names.has_value()) {
                    return conflict();
                }
                expected_attempt_leaves.push_back(names->canonical_record_leaf);
                auto loaded = read_immutable_protocol_record_leaf(
                    root_fd, names->canonical_record_leaf.c_str(), creator_process_id,
                    DistributedSieveWaveStoreStatus::namespace_conflict,
                    DistributedSieveWaveStoreStatus::namespace_conflict);
                if (!loaded) {
                    return {.snapshots = std::nullopt, .diagnostic = std::move(loaded.diagnostic)};
                }
                auto decoded = decode_distributed_sieve_record(*loaded.bytes);
                if (!decoded) {
                    return protocol_conflict(decoded.status);
                }
                auto* attempt = std::get_if<AttemptStartedV1>(&*decoded.value);
                if (attempt == nullptr || attempt->manifest_digest != manifest.self_digest ||
                    attempt->chunk_id != plan.chunk_id || attempt->sq_begin != plan.sq_begin ||
                    attempt->sq_end != plan.sq_end || attempt->attempt_ordinal != ordinal) {
                    return conflict();
                }
                observed_snapshots.worker_attempts.push_back(*loaded.snapshot);
                chain.push_back(std::move(*attempt));
            }
        }
        std::sort(expected_attempt_leaves.begin(), expected_attempt_leaves.end());
        if (expected_attempt_leaves != inventory.worker_attempt_record_leaves) {
            return conflict();
        }

        std::vector<MergeStartedV1> observed_starts;
        std::vector<std::string> expected_start_leaves;
        observed_starts.reserve(retained_starts.size());
        expected_start_leaves.reserve(retained_starts.size());
        for (std::size_t ordinal = 0; ordinal < retained_starts.size(); ++ordinal) {
            const auto names =
                distributed_sieve_merge_generation_names_v1(static_cast<std::uint32_t>(ordinal));
            if (!names.has_value()) {
                return conflict();
            }
            expected_start_leaves.push_back(names->canonical_record_leaf);
            auto loaded = read_immutable_protocol_record_leaf(
                root_fd, names->canonical_record_leaf.c_str(), creator_process_id,
                DistributedSieveWaveStoreStatus::namespace_conflict,
                DistributedSieveWaveStoreStatus::namespace_conflict);
            if (!loaded) {
                return {.snapshots = std::nullopt, .diagnostic = std::move(loaded.diagnostic)};
            }
            auto decoded = decode_distributed_sieve_record(*loaded.bytes);
            if (!decoded) {
                return protocol_conflict(decoded.status);
            }
            auto* start = std::get_if<MergeStartedV1>(&*decoded.value);
            if (start == nullptr) {
                return conflict();
            }
            DistributedSieveProtocolRecordV1 retained_record(retained_starts[ordinal]);
            auto retained_encoded = encode_distributed_sieve_record(retained_record);
            if (!retained_encoded || !retained_encoded.bytes.has_value() ||
                *retained_encoded.bytes != *loaded.bytes) {
                return retained_encoded ? conflict() : protocol_conflict(retained_encoded.status);
            }
            observed_snapshots.merge_starts.push_back(*loaded.snapshot);
            observed_starts.push_back(std::move(*start));
        }
        std::sort(expected_start_leaves.begin(), expected_start_leaves.end());
        if (expected_start_leaves != inventory.merge_started_record_leaves) {
            return conflict();
        }

        std::vector<ChunkTerminalEvidenceViewV1> evidence;
        evidence.reserve(manifest.chunks.size());
        for (std::size_t slot = 0; slot < manifest.chunks.size(); ++slot) {
            evidence.push_back(ChunkTerminalEvidenceViewV1{
                .attempts = attempt_chains[slot],
                .handoff = retained_handoffs[slot],
            });
        }
        const auto dependency = validate_merge_dependency_chain(manifest, evidence, observed_starts,
                                                                std::addressof(prepared), commit);
        if (!dependency) {
            return protocol_conflict(dependency);
        }
        if (expected_snapshots != nullptr && observed_snapshots != *expected_snapshots) {
            return conflict();
        }
        return {.snapshots = std::move(observed_snapshots), .diagnostic = {}};
    } catch (const std::bad_alloc&) {
        return {.snapshots = std::nullopt,
                .diagnostic = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                         std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        return {.snapshots = std::nullopt,
                .diagnostic = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                         std::make_error_code(std::errc::io_error))};
    }
}

[[nodiscard]] durable_record::RecordSnapshot
cleanup_record_snapshot(const NativeFileExtentV1& file) noexcept {
    return {
        .identity =
            {
                .first = file.identity.volume,
                .second = file.identity.object,
                .third = file.identity.generation,
            },
        .size = file.extent,
    };
}

struct WorkerCleanupRootPreludeV1 final {
    NamespaceInventory inventory;
    DistributedSieveWaveMergeCommitRecordInventoryWitnessV1 commit;
    std::vector<DistributedSieveWorkerCleanupRecordLeafWitnessV1> cleanup_records;
    std::vector<CleanupRecoveredWorkerPublicationV1> recovered_workers;
};

struct WorkerCleanupRootPreludeResultV1 final {
    std::optional<WorkerCleanupRootPreludeV1> prelude;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return prelude.has_value() && diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] WorkerCleanupRootPreludeResultV1
load_worker_cleanup_root_prelude_v1(int root_fd, const WaveManifestV1& manifest,
                                    std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return WorkerCleanupRootPreludeResultV1{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    try {
        auto inspected = inspect_namespace(root_fd);
        if (!inspected) {
            return fail_with(std::move(inspected.diagnostic));
        }
        auto commit = validate_wave_merge_commit_record_inventory(root_fd, *inspected.inventory,
                                                                  manifest, creator_process_id);
        if (!commit) {
            return fail_with(std::move(commit.diagnostic));
        }
        if (commit.witnesses->size() != 1U ||
            !commit.witnesses->front().canonical_snapshot.has_value() ||
            commit.witnesses->front().pending_snapshot.has_value()) {
            return conflict();
        }

        std::vector<DistributedSieveWorkerCleanupRecordLeafWitnessV1> cleanup_records;
        cleanup_records.reserve(inspected.inventory->cleanup_record_leaves.size());
        for (const auto& leaf : inspected.inventory->cleanup_record_leaves) {
            auto loaded = load_distributed_sieve_worker_cleanup_record_leaf_v1(root_fd, leaf,
                                                                               creator_process_id);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            cleanup_records.push_back(std::move(*loaded.witness));
        }

        std::vector<const ArtifactCleanupAuthorizedV1*> authorizations(manifest.chunks.size(),
                                                                       nullptr);
        std::vector<bool> canonical_authorizations(manifest.chunks.size(), false);
        for (const auto& leaf : cleanup_records) {
            const auto* authorization = std::get_if<ArtifactCleanupAuthorizedV1>(&leaf.record);
            if (authorization == nullptr) {
                continue;
            }
            const auto ordinal = authorization->manifest_order_ordinal;
            if (ordinal >= authorizations.size() || authorizations[ordinal] != nullptr) {
                // Canonical+pending duplicates are represented by separate
                // leaves but must carry byte-identical records.
                if (ordinal >= authorizations.size() || authorizations[ordinal] == nullptr ||
                    authorizations[ordinal]->self_digest != authorization->self_digest) {
                    return conflict();
                }
                continue;
            }
            authorizations[ordinal] = authorization;
            canonical_authorizations[ordinal] = !leaf.coordinate.pending;
            continue;
        }
        for (const auto& leaf : cleanup_records) {
            const auto* authorization = std::get_if<ArtifactCleanupAuthorizedV1>(&leaf.record);
            if (authorization != nullptr && !leaf.coordinate.pending) {
                canonical_authorizations[authorization->manifest_order_ordinal] = true;
            }
        }

        const auto& commit_record = commit.witnesses->front().record;
        std::vector<CleanupRecoveredWorkerPublicationV1> recovered;
        recovered.reserve(cleanup_records.size());
        for (std::size_t slot = 0; slot < authorizations.size(); ++slot) {
            const auto* authorization = authorizations[slot];
            if (authorization == nullptr || !canonical_authorizations[slot]) {
                continue;
            }
            if (slot >= commit_record.chunks.size() || slot >= manifest.chunks.size()) {
                return conflict();
            }
            const auto& chunk = manifest.chunks[slot];
            const auto& summary = commit_record.chunks[slot].input;
            if (summary.disposition != ChunkDispositionV1::handoff ||
                summary.durable_attempt_count == 0U) {
                return conflict();
            }
            auto projected =
                worker_cleanup_codec::project_distributed_sieve_worker_cleanup_handoff_v1(
                    manifest, commit_record, *authorization);
            if (!projected) {
                auto failure = diagnostic(
                    projected.diagnostic.status ==
                            worker_cleanup_codec::DistributedSieveWorkerCleanupCodecStatusV1::
                                resource_exhausted
                        ? DistributedSieveWaveStoreStatus::resource_exhausted
                        : DistributedSieveWaveStoreStatus::namespace_conflict,
                    protocol_error());
                if (!projected.diagnostic.protocol) {
                    failure.protocol_status = projected.diagnostic.protocol;
                }
                return fail_with(std::move(failure));
            }
            WorkerHandoffV1 handoff = std::move(*projected.worker_handoff);
            const auto names = distributed_sieve_worker_attempt_names_v1(
                chunk.relative_artifact_stem, chunk.chunk_id, handoff.attempt_ordinal);
            if (!names.has_value()) {
                return conflict();
            }
            DistributedSieveWorkerHandoffInventoryWitnessV1 typed{
                .handoff = handoff,
                .envelope_digest = authorization->private_handoff_digest,
                .owned_marker_identity = authorization->owned_marker_identity,
                .handoff_snapshot = cleanup_record_snapshot(authorization->private_handoff_record),
                .index_snapshot = cleanup_record_snapshot(authorization->artifact.index_file),
                .data_snapshot = cleanup_record_snapshot(authorization->artifact.data_file),
            };
            recovered.push_back(CleanupRecoveredWorkerPublicationV1{
                .manifest_slot = slot,
                .base_lock_identity = authorization->base_lock_identity,
                .typed_handoff = typed,
                .provisional_lease =
                    PrivateLeaseReservationWitness{
                        .base_lock_leaf = names->base_lock_leaf,
                        .boundary =
                            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable,
                        .lease_id = authorization->lease.lease_id.limbs,
                        .directory_identity = authorization->lease.directory,
                        .owner_marker_identity = authorization->lease.owner_marker,
                        .owned_marker_identity = authorization->owned_marker_identity,
                        .worker_handoff = std::move(typed),
                    },
            });
        }
        return {
            WorkerCleanupRootPreludeV1{
                .inventory = std::move(*inspected.inventory),
                .commit = commit.witnesses->front(),
                .cleanup_records = std::move(cleanup_records),
                .recovered_workers = std::move(recovered),
            },
            {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

/// Immutable projection retained by the worker-cleanup cold-open admission.
/// Every temporary relation publication permit used to construct it is
/// destroyed before the value escapes `observe_cleanup_root_v1`.
struct WorkerCleanupRootCommittedObservationV1 final {
    NamespaceInventory inventory;
    std::vector<NativeIdentityV1> base_lock_identities;
    std::vector<PrivateLeaseReservationWitness> private_leases;
    std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempt_records;
    std::vector<DistributedSieveMergeStartedRecordInventoryWitnessV1> merge_started_records;
    std::vector<DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1>
        chunk_terminal_failure_records;
    DistributedSieveWaveMergeCommitRecordInventoryWitnessV1 commit_record;
    std::size_t merge_attempt_index = 0;
    DistributedSieveMergeGenerationNamesV1 merge_names;
    private_lease::PrivateHandoffPublicationPrefixWitnessV1 merged_prefix;
    DistributedSieveMergePreparedInventoryWitnessV1 typed_prepared;
    DistributedSieveMergeStartedRecordInventoryWitnessV1 final_merge_start;
    std::vector<WorkerHandoffPublicationAggregateWitness> worker_prefixes;
    std::vector<CleanupRecoveredWorkerPublicationV1> cleanup_recovered_workers;
    DistributedSieveWorkerCleanupPrefixWitnessV1 cleanup_prefix;

    [[nodiscard]] friend bool operator==(const WorkerCleanupRootCommittedObservationV1&,
                                         const WorkerCleanupRootCommittedObservationV1&) = default;
};

struct WorkerCleanupRootCommittedObservationResultV1 final {
    std::optional<WorkerCleanupRootCommittedObservationV1> observation;
    DistributedSieveWaveStoreDiagnostic diagnostic;

    [[nodiscard]] explicit operator bool() const noexcept {
        return observation.has_value() &&
               diagnostic.status == DistributedSieveWaveStoreStatus::ready;
    }
};

[[nodiscard]] WorkerCleanupRootCommittedObservationResultV1
observe_cleanup_root_v1(int root_fd, const std::filesystem::path& absolute_root,
                        const WaveManifestV1& manifest,
                        const NativeIdentityV1& expected_wave_root_identity,
                        std::uint64_t creator_process_id) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return WorkerCleanupRootCommittedObservationResultV1{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    try {
        auto prelude = load_worker_cleanup_root_prelude_v1(root_fd, manifest, creator_process_id);
        if (!prelude) {
            return fail_with(std::move(prelude.diagnostic));
        }
        auto captured = classify_merge_prepared_publication_prefix_v1(
            root_fd, absolute_root, manifest, expected_wave_root_identity, creator_process_id, {},
            prelude.prelude->recovered_workers, !prelude.prelude->cleanup_records.empty());
        if (!captured) {
            return fail_with(std::move(captured.diagnostic));
        }
        if (!captured.prefix_present() || !captured.witness.has_value() ||
            !captured.retained.has_value()) {
            return conflict();
        }
        const auto& aggregate = *captured.witness;
        const auto& retained = *captured.retained;
        if (aggregate.wave_merge_commit_records.size() != 1U ||
            !aggregate.wave_merge_commit_records.front().canonical_snapshot.has_value() ||
            aggregate.wave_merge_commit_records.front().pending_snapshot.has_value() ||
            !aggregate.chunk_terminal_failure_records.empty() ||
            !aggregate.prefix.canonical_terminal() || !retained.witness.canonical_terminal() ||
            aggregate.prefix != retained.witness ||
            aggregate.typed_prepared != retained.typed_prepared ||
            aggregate.start_record != retained.start_record) {
            return conflict();
        }
        auto aggregate_raw_inventory = aggregate.inventory;
        aggregate_raw_inventory.worker_attempt_records.clear();
        aggregate_raw_inventory.merge_started_records.clear();
        aggregate_raw_inventory.chunk_terminal_failure_records.clear();
        aggregate_raw_inventory.wave_merge_commit_records.clear();
        if (aggregate_raw_inventory != prelude.prelude->inventory ||
            aggregate.wave_merge_commit_records.front() != prelude.prelude->commit) {
            return conflict();
        }
        const auto& commit = aggregate.wave_merge_commit_records.front().record;
        const auto& prepared = aggregate.typed_prepared.prepared;
        if (commit.manifest_digest != manifest.self_digest ||
            commit.work_digest != manifest.work_sha256 ||
            commit.merge_prepared_digest != prepared.self_digest ||
            commit.merged_lease != prepared.merged_lease ||
            commit.merged_artifact != prepared.merged_artifact ||
            commit.chunks.size() != manifest.chunks.size()) {
            return conflict();
        }

        std::vector<std::optional<WorkerHandoffV1>> worker_handoffs(manifest.chunks.size());
        for (const auto& recovered : prelude.prelude->recovered_workers) {
            if (recovered.manifest_slot >= worker_handoffs.size() ||
                worker_handoffs[recovered.manifest_slot].has_value()) {
                return conflict();
            }
            worker_handoffs[recovered.manifest_slot] = recovered.typed_handoff.handoff;
        }
        std::vector<WorkerHandoffPublicationAggregateWitness> worker_prefixes;
        worker_prefixes.reserve(aggregate.retained_workers.entries.size());
        for (const auto& worker : aggregate.retained_workers.entries) {
            if (worker.manifest_slot >= manifest.chunks.size() ||
                worker_handoffs[worker.manifest_slot].has_value() ||
                !worker.witness.canonical_terminal() || worker.consumed || worker.adopting ||
                worker.reader != nullptr || !worker.permit.valid() || !worker.permit.held()) {
                return conflict();
            }
            worker_handoffs[worker.manifest_slot] = worker.typed_handoff.handoff;
            worker_prefixes.push_back(WorkerHandoffPublicationAggregateWitness{
                .attempt_index = worker.attempt_index,
                .names = worker.names,
                .coordinate = worker.coordinate,
                .prefix = worker.witness,
                .typed_handoff = worker.typed_handoff,
                .attempt_record = worker.attempt_record,
            });
        }
        std::size_t expected_worker_count = 0;
        for (std::size_t slot = 0; slot < manifest.chunks.size(); ++slot) {
            const auto disposition = commit.chunks[slot].input.disposition;
            if (disposition == ChunkDispositionV1::empty) {
                if (worker_handoffs[slot].has_value()) {
                    return conflict();
                }
                continue;
            }
            if (disposition != ChunkDispositionV1::handoff || !worker_handoffs[slot].has_value()) {
                return conflict();
            }
            ++expected_worker_count;
        }
        if (worker_prefixes.size() + prelude.prelude->recovered_workers.size() !=
            expected_worker_count) {
            return conflict();
        }

        std::vector<const WorkerHandoffV1*> handoff_pointers;
        handoff_pointers.reserve(worker_handoffs.size());
        for (const auto& handoff : worker_handoffs) {
            handoff_pointers.push_back(handoff.has_value() ? std::addressof(*handoff) : nullptr);
        }
        auto cleanup = classify_distributed_sieve_worker_cleanup_prefix_v1(
            manifest, commit, handoff_pointers, prelude.prelude->cleanup_records);
        if (!cleanup) {
            return fail_with(std::move(cleanup.diagnostic));
        }

        const auto recovered_for =
            [&](std::uint32_t manifest_slot) -> const CleanupRecoveredWorkerPublicationV1* {
            const auto position =
                std::find_if(prelude.prelude->recovered_workers.begin(),
                             prelude.prelude->recovered_workers.end(), [&](const auto& candidate) {
                                 return candidate.manifest_slot == manifest_slot;
                             });
            return position == prelude.prelude->recovered_workers.end() ? nullptr
                                                                        : std::addressof(*position);
        };
        const auto namespace_absent = [&](const ArtifactCleanupAuthorizedV1& authorization) {
            const auto* recovered = recovered_for(authorization.manifest_order_ordinal);
            if (recovered == nullptr) {
                return false;
            }
            const auto& base_leaf = recovered->provisional_lease.base_lock_leaf;
            const auto base_position = std::lower_bound(
                aggregate.inventory.private_lease_base_lock_leaves.begin(),
                aggregate.inventory.private_lease_base_lock_leaves.end(), base_leaf);
            if (base_position == aggregate.inventory.private_lease_base_lock_leaves.end() ||
                *base_position != base_leaf) {
                return false;
            }
            const auto base_index = static_cast<std::size_t>(std::distance(
                aggregate.inventory.private_lease_base_lock_leaves.begin(), base_position));
            if (base_index >= aggregate.base_lock_identities.size() ||
                aggregate.base_lock_identities[base_index] != authorization.base_lock_identity) {
                return false;
            }
            for (const auto& leaf : aggregate.inventory.private_lease_protocol_leaves) {
                const auto parsed =
                    parse_manifest_bound_private_lease_protocol_leaf(manifest, leaf);
                if (!parsed.has_value() || parsed->names.base_lock_leaf == base_leaf) {
                    return false;
                }
            }
            return true;
        };
        const auto retained_live_for = [&](std::uint32_t manifest_slot) {
            return std::ranges::count_if(aggregate.retained_workers.entries,
                                         [&](const auto& worker) {
                                             return worker.manifest_slot == manifest_slot;
                                         }) == 1;
        };
        for (const auto& coordinate : cleanup.prefix->coordinates) {
            const auto* recovered = recovered_for(coordinate.manifest_order_ordinal);
            switch (coordinate.state) {
            case DistributedSieveWorkerCleanupPrefixStateV1::authorization_pending_only:
                // A pending-only authorization grants no cleanup authority. Its
                // worker must therefore still be the exact canonical live handoff
                // retained by the ordinary publication-prefix classifier.
                if (recovered != nullptr || !retained_live_for(coordinate.manifest_order_ordinal)) {
                    return conflict();
                }
                break;
            case DistributedSieveWorkerCleanupPrefixStateV1::authorization_canonical_only:
            case DistributedSieveWorkerCleanupPrefixStateV1::authorization_identical_dual:
                // Canonical authorization is the sole authority-free source for
                // reconstructing the committed worker. The relation namespace may
                // be live, partial, or absent; exact T2b legality is not asserted
                // here and must be revalidated by the authorization-bound relation
                // executor before mutation.
                if (recovered == nullptr || retained_live_for(coordinate.manifest_order_ordinal)) {
                    return conflict();
                }
                break;
            case DistributedSieveWorkerCleanupPrefixStateV1::completion_pending_only:
            case DistributedSieveWorkerCleanupPrefixStateV1::completion_identical_dual:
            case DistributedSieveWorkerCleanupPrefixStateV1::completed:
                if (recovered == nullptr || retained_live_for(coordinate.manifest_order_ordinal) ||
                    !namespace_absent(coordinate.authorization)) {
                    return conflict();
                }
                break;
            }
        }

        // Every cleanup leaf is an immutable dependency of this observation.
        // Re-read it at the observation tail so same-name replacement cannot
        // survive one `valid()` call merely because the outer double observation
        // happened to straddle the mutation.
        std::vector<DistributedSieveWorkerCleanupRecordLeafWitnessV1> confirmed_cleanup_records;
        confirmed_cleanup_records.reserve(prelude.prelude->inventory.cleanup_record_leaves.size());
        for (const auto& leaf : prelude.prelude->inventory.cleanup_record_leaves) {
            auto loaded = load_distributed_sieve_worker_cleanup_record_leaf_v1(root_fd, leaf,
                                                                               creator_process_id);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            confirmed_cleanup_records.push_back(std::move(*loaded.witness));
        }
        if (confirmed_cleanup_records != prelude.prelude->cleanup_records) {
            return conflict();
        }
        auto final_inventory = inspect_namespace(root_fd);
        if (!final_inventory) {
            return fail_with(std::move(final_inventory.diagnostic));
        }
        if (*final_inventory.inventory != prelude.prelude->inventory) {
            return conflict();
        }

        return {
            WorkerCleanupRootCommittedObservationV1{
                .inventory = aggregate.inventory,
                .base_lock_identities = aggregate.base_lock_identities,
                .private_leases = aggregate.private_leases,
                .worker_attempt_records = aggregate.worker_attempt_records,
                .merge_started_records = aggregate.merge_started_records,
                .chunk_terminal_failure_records = aggregate.chunk_terminal_failure_records,
                .commit_record = aggregate.wave_merge_commit_records.front(),
                .merge_attempt_index = aggregate.attempt_index,
                .merge_names = aggregate.names,
                .merged_prefix = aggregate.prefix,
                .typed_prepared = aggregate.typed_prepared,
                .final_merge_start = aggregate.start_record,
                .worker_prefixes = std::move(worker_prefixes),
                .cleanup_recovered_workers = std::move(prelude.prelude->recovered_workers),
                .cleanup_prefix = std::move(*cleanup.prefix),
            },
            {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

} // namespace

struct DistributedSieveWorkerCleanupRootAdmissionV1::State final {
    // Reverse destruction closes the read-only merged corpus before WaveLock.
    std::unique_ptr<DistributedSieveWaveStore> store;
    std::unique_ptr<relation::OOCRelationReader> merged_reader;
    WorkerCleanupRootCommittedObservationV1 observation;
    std::uint64_t creator_process_id = 0;
};

DistributedSieveWorkerCleanupRootAdmissionV1::DistributedSieveWorkerCleanupRootAdmissionV1(
    std::unique_ptr<State> state) noexcept
    : state_(std::move(state)) {}

DistributedSieveWorkerCleanupRootAdmissionV1::DistributedSieveWorkerCleanupRootAdmissionV1(
    DistributedSieveWorkerCleanupRootAdmissionV1&&) noexcept = default;

DistributedSieveWorkerCleanupRootAdmissionV1&
DistributedSieveWorkerCleanupRootAdmissionV1::operator=(
    DistributedSieveWorkerCleanupRootAdmissionV1&&) noexcept = default;

DistributedSieveWorkerCleanupRootAdmissionV1::~DistributedSieveWorkerCleanupRootAdmissionV1() =
    default;

bool DistributedSieveWorkerCleanupRootAdmissionV1::valid() const noexcept {
    if (state_ == nullptr || state_->store == nullptr || state_->merged_reader == nullptr ||
        !process_matches(state_->creator_process_id) || !state_->merged_reader->valid()) {
        return false;
    }
    const auto authority_valid = [&] {
        return WorkerCleanupRootRevalidatorAuthorityV1::revalidate_authority(*state_->store)
                   .status == DistributedSieveWaveStoreStatus::ready;
    };
    const auto observe = [&] {
        return observe_cleanup_root_v1(
            WorkerCleanupRootRevalidatorAuthorityV1::root_fd(*state_->store),
            WorkerCleanupRootRevalidatorAuthorityV1::absolute_root(*state_->store),
            WorkerCleanupRootRevalidatorAuthorityV1::manifest(*state_->store),
            state_->store->wave_root_identity(), state_->creator_process_id);
    };
    if (!authority_valid()) {
        return false;
    }
    const auto first = observe();
    if (!first || *first.observation != state_->observation || !authority_valid()) {
        return false;
    }
    const auto second = observe();
    return second && *second.observation == state_->observation &&
           *second.observation == *first.observation && state_->merged_reader->valid() &&
           authority_valid();
}

const WaveMergeCommitV1& DistributedSieveWorkerCleanupRootAdmissionV1::commit() const {
    if (state_ == nullptr) {
        throw std::logic_error("worker cleanup root admission is moved-from");
    }
    return state_->observation.commit_record.record;
}

const DistributedSieveWorkerCleanupPrefixWitnessV1&
DistributedSieveWorkerCleanupRootAdmissionV1::cleanup_prefix() const {
    if (state_ == nullptr) {
        throw std::logic_error("worker cleanup root admission is moved-from");
    }
    return state_->observation.cleanup_prefix;
}

const relation::OOCRelationReader& DistributedSieveWorkerCleanupRootAdmissionV1::reader() const {
    if (state_ == nullptr || state_->merged_reader == nullptr || !state_->merged_reader->valid()) {
        throw std::logic_error("worker cleanup root admission reader is unavailable");
    }
    return *state_->merged_reader;
}

DistributedSieveWaveStoreOpenResult
DistributedSieveWaveStore::create(const std::filesystem::path& absolute_root,
                                  WaveManifestV1 manifest_draft,
                                  DistributedSieveWaveStoreTestHooks hooks) noexcept {
    try {
        auto frozen = freeze_absolute_root(absolute_root);
        if (!frozen.has_value() || !valid_manifest_draft(manifest_draft)) {
            return open_failure(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                           invalid_argument_error()));
        }
        const auto preflight = validate_manifest_draft_semantics(manifest_draft);
        if (!preflight) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            failure.protocol_status = preflight;
            return open_failure(std::move(failure));
        }
        const std::uint64_t creator_process_id = current_process_id();
        if (!process_matches(creator_process_id)) {
            return open_failure(process_mismatch());
        }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)hooks;
        return open_failure(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        auto root = open_root(*frozen, true, creator_process_id);
        if (!root) {
            return open_failure(std::move(root.diagnostic));
        }
        auto inventory = inspect_namespace(root.root.get());
        if (!inventory) {
            return open_failure(std::move(inventory.diagnostic));
        }
        const NamespaceInventory root_inventory = *inventory.inventory;
        const auto root_hook = invoke_store_hook(
            hooks, DistributedSieveWaveStoreFaultPoint::RootDurable, creator_process_id);
        if (root_hook.status == DistributedSieveWaveStoreStatus::invalid_request) {
            return open_failure(root_hook);
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        const auto after_root_hook = inspect_namespace(root.root.get());
        if (!after_root_hook) {
            return open_failure(after_root_hook.diagnostic);
        }
        if (*after_root_hook.inventory != root_inventory) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (root_hook.status == DistributedSieveWaveStoreStatus::interrupted) {
            return open_failure(root_hook);
        }

        auto lock = open_lock(root.root.get(), root_inventory, true, creator_process_id);
        if (!lock) {
            return open_failure(std::move(lock.diagnostic));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }
        inventory = inspect_namespace(root.root.get());
        if (!inventory) {
            return open_failure(std::move(inventory.diagnostic));
        }
        if (!inventory.inventory->lock) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const NamespaceInventory lock_inventory = *inventory.inventory;
        const auto lock_hook = invoke_store_hook(
            hooks, DistributedSieveWaveStoreFaultPoint::LockDurable, creator_process_id);
        if (lock_hook.status == DistributedSieveWaveStoreStatus::invalid_request) {
            return open_failure(lock_hook);
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }
        const auto after_lock_hook = inspect_namespace(root.root.get());
        if (!after_lock_hook) {
            return open_failure(after_lock_hook.diagnostic);
        }
        if (*after_lock_hook.inventory != lock_inventory) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (lock_hook.status == DistributedSieveWaveStoreStatus::interrupted) {
            return open_failure(lock_hook);
        }
        manifest_draft.wave_root_identity = root.root_identity;
        manifest_draft.permanent_lock_identity = lock.lock_identity;
        manifest_draft.lock_semantics_version = DISTRIBUTED_SIEVE_WAVE_LOCK_SEMANTICS_VERSION_V1;
        DistributedSieveProtocolRecordV1 record(std::move(manifest_draft));
        const auto sealed = seal_distributed_sieve_record(record);
        if (!sealed) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            failure.protocol_status = sealed;
            return open_failure(std::move(failure));
        }
        const auto& sealed_manifest = std::get<WaveManifestV1>(record);
        const util::Sha256Digest expected_manifest_digest = sealed_manifest.self_digest;
        auto encoded = encode_distributed_sieve_record(record);
        if (!encoded) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            failure.protocol_status = encoded.status;
            return open_failure(std::move(failure));
        }
        if (!process_matches(creator_process_id)) {
            return open_failure(process_mismatch());
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }
        const auto before_publish = inspect_namespace(root.root.get());
        if (!before_publish) {
            return open_failure(before_publish.diagnostic);
        }
        if (*before_publish.inventory != lock_inventory) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        PrivateLeaseBaseLockInventoryValidationResult before_publish_private_inventory;
        std::optional<WaveManifestV1> existing_inventory_manifest;
        if (before_publish.inventory->manifest || before_publish.inventory->pending ||
            !before_publish.inventory->private_lease_base_lock_leaves.empty()) {
            auto existing = read_existing_manifest(root.root.get(), *before_publish.inventory,
                                                   expected_manifest_digest, root.root_identity,
                                                   lock.lock_identity, creator_process_id);
            if (!existing) {
                return open_failure(std::move(existing.diagnostic));
            }
            existing_inventory_manifest = std::move(existing.manifest);
        }
        if (has_pre_manifest_private_lease_candidate(*before_publish.inventory)) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const WaveManifestV1& inventory_manifest = existing_inventory_manifest.has_value()
                                                       ? *existing_inventory_manifest
                                                       : sealed_manifest;
        before_publish_private_inventory = validate_private_lease_base_lock_inventory(
            root.root.get(), *before_publish.inventory, inventory_manifest, creator_process_id);
        if (!before_publish_private_inventory) {
            return open_failure(before_publish_private_inventory.diagnostic);
        }
        auto before_publish_private_leases = validate_private_lease_protocol_inventory(
            root.root.get(), frozen->absolute, *before_publish.inventory, inventory_manifest,
            *before_publish_private_inventory.identities, creator_process_id);
        if (!before_publish_private_leases) {
            return open_failure(std::move(before_publish_private_leases.diagnostic));
        }
        auto before_publish_worker_attempts = validate_worker_attempt_record_inventory(
            root.root.get(), *before_publish.inventory, inventory_manifest,
            *before_publish_private_inventory.identities, *before_publish_private_leases.witnesses,
            creator_process_id);
        if (!before_publish_worker_attempts) {
            return open_failure(std::move(before_publish_worker_attempts.diagnostic));
        }

        auto published =
            publish_manifest(root.root.get(), *encoded.bytes, hooks, creator_process_id);
        if (!published) {
            return open_failure(std::move(published.diagnostic));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }

        auto final_manifest = confirm_final_manifest(
            root.root.get(), *encoded.bytes, expected_manifest_digest, root.root_identity,
            lock.lock_identity, *published.snapshot, creator_process_id);
        if (!final_manifest) {
            return open_failure(std::move(final_manifest.diagnostic));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }
        const auto final_inventory = inspect_namespace(root.root.get());
        if (!final_inventory) {
            return open_failure(final_inventory.diagnostic);
        }
        const auto final_inventory_validated = validate_manifest_bound_inventory(
            root.root.get(), frozen->absolute, *final_inventory.inventory, sealed_manifest,
            creator_process_id);
        if (!final_inventory_validated) {
            return open_failure(final_inventory_validated.diagnostic);
        }
        if (final_inventory.inventory->private_lease_base_lock_leaves !=
                before_publish.inventory->private_lease_base_lock_leaves ||
            final_inventory.inventory->private_lease_protocol_leaves !=
                before_publish.inventory->private_lease_protocol_leaves ||
            final_inventory.inventory->worker_attempt_record_leaves !=
                before_publish.inventory->worker_attempt_record_leaves ||
            *final_inventory_validated.base_lock_identities !=
                *before_publish_private_inventory.identities ||
            *final_inventory_validated.private_lease_witnesses !=
                *before_publish_private_leases.witnesses ||
            *final_inventory_validated.worker_attempt_records !=
                *before_publish_worker_attempts.witnesses) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (!process_matches(creator_process_id)) {
            return open_failure(process_mismatch());
        }

        auto state = std::make_shared<State>();
        state->absolute_root = std::move(frozen->absolute);
        state->root_leaf = std::move(frozen->leaf);
        state->parent_components = std::move(frozen->parent_components);
        state->manifest = std::move(*final_manifest.manifest);
        state->manifest_snapshot = *final_manifest.snapshot;
        state->root_identity = root.root_identity;
        state->lock_identity = lock.lock_identity;
        state->manifest_bytes = std::move(*final_manifest.bytes);
        state->creator_process_id = creator_process_id;
        state->parent_fd = root.parent.release();
        state->root_fd = root.root.release();
        state->lock_fd = lock.lock.release();

        auto store = std::unique_ptr<DistributedSieveWaveStore>(
            new DistributedSieveWaveStore(std::move(state)));
        if (!process_matches(creator_process_id)) {
            store.reset();
            return open_failure(process_mismatch());
        }
        return {std::move(store), std::nullopt, std::move(published.diagnostic)};
#endif
    } catch (const std::bad_alloc&) {
        return open_failure(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                       std::make_error_code(std::errc::not_enough_memory)));
    } catch (const std::filesystem::filesystem_error& error) {
        return open_failure(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, error.code()));
    } catch (...) {
        return open_failure(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                       std::make_error_code(std::errc::io_error)));
    }
}

DistributedSieveWaveStoreOpenResult
DistributedSieveWaveStore::open(const std::filesystem::path& absolute_root,
                                const util::Sha256Digest& expected_manifest_digest,
                                DistributedSieveWaveStoreTestHooks hooks) noexcept {
    try {
        auto frozen = freeze_absolute_root(absolute_root);
        if (!frozen.has_value() || nil_digest(expected_manifest_digest)) {
            return open_failure(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                           invalid_argument_error()));
        }
        const std::uint64_t creator_process_id = current_process_id();
        if (!process_matches(creator_process_id)) {
            return open_failure(process_mismatch());
        }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)expected_manifest_digest;
        (void)hooks;
        return open_failure(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        auto root = open_root(*frozen, false, creator_process_id);
        if (!root) {
            return open_failure(std::move(root.diagnostic));
        }
        auto inventory = inspect_namespace(root.root.get());
        if (!inventory) {
            return open_failure(std::move(inventory.diagnostic));
        }

        const NamespaceInventory initial_inventory = *inventory.inventory;
        auto lock = open_lock(root.root.get(), initial_inventory, false, creator_process_id);
        if (!lock) {
            return open_failure(std::move(lock.diagnostic));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }

        inventory = inspect_namespace(root.root.get());
        if (!inventory) {
            return open_failure(std::move(inventory.diagnostic));
        }
        if (!inventory.inventory->lock) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (*inventory.inventory != initial_inventory) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        auto existing =
            read_existing_manifest(root.root.get(), *inventory.inventory, expected_manifest_digest,
                                   root.root_identity, lock.lock_identity, creator_process_id);
        if (!existing) {
            return open_failure(std::move(existing.diagnostic));
        }
        if (has_pre_manifest_private_lease_candidate(*inventory.inventory)) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        if (inventory.inventory->manifest && !inventory.inventory->pending) {
            auto resume_manifest =
                read_manifest_leaf(root.root.get(), MANIFEST_LEAF, creator_process_id);
            if (!resume_manifest) {
                return open_failure(std::move(resume_manifest.diagnostic));
            }
            if (*resume_manifest.bytes != *existing.bytes) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::manifest_conflict,
                                               protocol_error()));
            }
            if (const auto authority = validate_held_wave_store_manifest_authority(
                    root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                    root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                    *resume_manifest.snapshot, creator_process_id);
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return open_failure(authority);
            }

            constexpr std::size_t maximum_merge_prepared_resume_rounds = 1;
            std::size_t merge_prepared_resume_round = 0;
            while (true) {
                if (const auto authority = validate_held_wave_store_manifest_authority(
                        root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                        root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                        *resume_manifest.snapshot, creator_process_id);
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(authority);
                }
                auto merge_prepared_prefix = classify_merge_prepared_publication_prefix_v1(
                    root.root.get(), frozen->absolute, *existing.manifest, root.root_identity,
                    creator_process_id);
                if (!merge_prepared_prefix) {
                    return open_failure(std::move(merge_prepared_prefix.diagnostic));
                }
                if (!merge_prepared_prefix.prefix_present()) {
                    break;
                }
                if (!merge_prepared_prefix.witness.has_value() ||
                    !merge_prepared_prefix.retained.has_value()) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
                }
                auto& aggregate = *merge_prepared_prefix.witness;
                auto& retained = *merge_prepared_prefix.retained;
                if (retained.witness.canonical_terminal()) {
                    merge_prepared_prefix.retained.reset();
                    merge_prepared_prefix.witness.reset();
                    break;
                }
                if (merge_prepared_resume_round >= maximum_merge_prepared_resume_rounds) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                if (const auto projection = revalidate_merge_prepared_aggregate_projection(
                        root.root.get(), frozen->absolute, *existing.manifest, aggregate, retained,
                        creator_process_id);
                    projection.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(projection);
                }
                if (const auto exact = revalidate_exact_canonical_merge_started(
                        root.root.get(), retained.names, retained.start_record, creator_process_id);
                    exact.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(exact);
                }
                if (const auto authority = validate_held_wave_store_manifest_authority(
                        root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                        root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                        *resume_manifest.snapshot, creator_process_id);
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(authority);
                }

                const auto expected_prefix = retained.witness;
                const auto expected_disposition =
                    expected_prefix.pending_only() || expected_prefix.rollback_armed()
                        ? private_lease::PrivateHandoffPublicationResumeDispositionV1::
                              PendingRolledBack
                        : private_lease::PrivateHandoffPublicationResumeDispositionV1::
                              CanonicalConverged;
                MergePreparedResumeBridgeContext resume_bridge{
                    .user_hooks = hooks.merge_prepared_resume,
                    .parent_fd = root.parent.get(),
                    .parent_components = &frozen->parent_components,
                    .root_fd = root.root.get(),
                    .root_leaf = &frozen->leaf,
                    .root_identity = root.root_identity,
                    .lock_fd = lock.lock.get(),
                    .lock_identity = lock.lock_identity,
                    .manifest_bytes = &*existing.bytes,
                    .manifest_snapshot = *resume_manifest.snapshot,
                    .absolute_root = &frozen->absolute,
                    .manifest = &*existing.manifest,
                    .aggregate = &aggregate,
                    .retained = &retained,
                    .creator_process_id = creator_process_id,
                };
                auto converged = private_lease::reconcile_private_handoff_publication_for_resume_v1(
                    retained.permit, relation_merge_prepared_resume_hooks(resume_bridge));
                if (resume_bridge.revalidation_failed) {
                    return open_failure(std::move(resume_bridge.revalidation_diagnostic));
                }
                if (!converged.converged()) {
                    return open_failure(worker_handoff_inspection_failure(converged.result));
                }
                if (converged.disposition != expected_disposition ||
                    !converged.expected_prefix.has_value() ||
                    *converged.expected_prefix != expected_prefix) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                if (expected_disposition ==
                            private_lease::PrivateHandoffPublicationResumeDispositionV1::
                                PendingRolledBack
                        ? converged.terminal_prefix.has_value()
                        : !converged.terminal_prefix.has_value() ||
                              !converged.terminal_prefix->canonical_terminal()) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                if (!retained.permit.held() || retained.permit.valid()) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
                }
                retained.consumed = true;
                merge_prepared_prefix.retained.reset();
                merge_prepared_prefix.witness.reset();
                if (hooks.merge_prepared_resume.after_round_locks_released != nullptr) {
                    hooks.merge_prepared_resume.after_round_locks_released(
                        hooks.merge_prepared_resume.context);
                    if (!process_matches(creator_process_id)) {
                        return open_failure(process_mismatch());
                    }
                }
                ++merge_prepared_resume_round;

                if (const auto authority = validate_held_wave_store_manifest_authority(
                        root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                        root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                        *resume_manifest.snapshot, creator_process_id);
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(authority);
                }
                inventory = inspect_namespace(root.root.get());
                if (!inventory) {
                    return open_failure(std::move(inventory.diagnostic));
                }
            }

            const std::size_t maximum_resume_rounds =
                existing.manifest->chunks.size() *
                static_cast<std::size_t>(existing.manifest->max_worker_attempts);
            std::size_t resume_round = 0;
            while (true) {
                if (const auto authority = validate_held_wave_store_manifest_authority(
                        root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                        root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                        *resume_manifest.snapshot, creator_process_id);
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(authority);
                }
                // Any absorbing terminal or merge-start prefix freezes
                // handoff-prefix recovery. The manifest-bound loader below
                // validates those immutable records and their complete
                // dependency chains without mutating successful worker
                // artifacts.
                if (!inventory.inventory->chunk_terminal_failure_record_leaves.empty() ||
                    !inventory.inventory->merge_started_record_leaves.empty()) {
                    break;
                }
                auto recoverable = capture_recoverable_worker_handoff_inventory(
                    root.root.get(), frozen->absolute, *inventory.inventory, *existing.manifest,
                    root.root_identity, creator_process_id);
                if (!recoverable) {
                    return open_failure(std::move(recoverable.diagnostic));
                }
                const auto selected = std::find_if(
                    recoverable.retained.entries.rbegin(), recoverable.retained.entries.rend(),
                    [](const RetainedWorkerHandoffPublicationPrefix& prefix) {
                        return !prefix.witness.canonical_terminal();
                    });
                if (selected == recoverable.retained.entries.rend()) {
                    break;
                }
                if (resume_round >= maximum_resume_rounds) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                if (const auto projection = revalidate_worker_handoff_aggregate_projection(
                        root.root.get(), frozen->absolute, *existing.manifest, *recoverable.witness,
                        recoverable.retained, selected->attempt_index, creator_process_id);
                    projection.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(projection);
                }

                auto& prefix = *selected;
                if (const auto attempt = revalidate_exact_canonical_worker_attempt(
                        root.root.get(), prefix.names, prefix.attempt_record, creator_process_id);
                    attempt.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(attempt);
                }
                if (const auto authority = validate_held_wave_store_manifest_authority(
                        root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                        root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                        *resume_manifest.snapshot, creator_process_id);
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(authority);
                }

                const auto expected_prefix = prefix.witness;
                const auto expected_disposition =
                    expected_prefix.pending_only() || expected_prefix.rollback_armed()
                        ? private_lease::PrivateHandoffPublicationResumeDispositionV1::
                              PendingRolledBack
                        : private_lease::PrivateHandoffPublicationResumeDispositionV1::
                              CanonicalConverged;
                WorkerHandoffResumeBridgeContext resume_bridge{
                    .user_hooks = hooks.worker_handoff_resume,
                    .parent_fd = root.parent.get(),
                    .parent_components = &frozen->parent_components,
                    .root_fd = root.root.get(),
                    .root_leaf = &frozen->leaf,
                    .root_identity = root.root_identity,
                    .lock_fd = lock.lock.get(),
                    .lock_identity = lock.lock_identity,
                    .manifest_bytes = &*existing.bytes,
                    .manifest_snapshot = *resume_manifest.snapshot,
                    .absolute_root = &frozen->absolute,
                    .manifest = &*existing.manifest,
                    .aggregate = &*recoverable.witness,
                    .retained = &recoverable.retained,
                    .current_attempt_index = prefix.attempt_index,
                    .attempt_names = &prefix.names,
                    .attempt_record = &prefix.attempt_record,
                    .creator_process_id = creator_process_id,
                };
                auto converged = private_lease::reconcile_private_handoff_publication_for_resume_v1(
                    prefix.permit, relation_worker_handoff_resume_hooks(resume_bridge));
                if (resume_bridge.revalidation_failed) {
                    return open_failure(std::move(resume_bridge.revalidation_diagnostic));
                }
                if (!converged.converged()) {
                    return open_failure(worker_handoff_inspection_failure(converged.result));
                }
                if (converged.disposition != expected_disposition ||
                    !converged.expected_prefix.has_value() ||
                    *converged.expected_prefix != expected_prefix) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                if (!prefix.permit.held() || prefix.permit.valid()) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
                }
                prefix.consumed = true;
                while (!recoverable.retained.entries.empty()) {
                    recoverable.retained.entries.pop_back();
                }
                if (hooks.worker_handoff_resume.after_round_locks_released != nullptr) {
                    hooks.worker_handoff_resume.after_round_locks_released(
                        hooks.worker_handoff_resume.context);
                    if (!process_matches(creator_process_id)) {
                        return open_failure(process_mismatch());
                    }
                }
                ++resume_round;

                if (const auto authority = validate_held_wave_store_manifest_authority(
                        root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                        root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                        *resume_manifest.snapshot, creator_process_id);
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return open_failure(authority);
                }
                inventory = inspect_namespace(root.root.get());
                if (!inventory) {
                    return open_failure(std::move(inventory.diagnostic));
                }
            }
        }

        const auto inventory_validated = validate_manifest_bound_inventory(
            root.root.get(), frozen->absolute, *inventory.inventory, *existing.manifest,
            creator_process_id, {}, false);
        if (!inventory_validated) {
            return open_failure(inventory_validated.diagnostic);
        }

        const auto after_read = inspect_namespace(root.root.get());
        if (!after_read) {
            return open_failure(std::move(after_read.diagnostic));
        }
        if (*after_read.inventory != *inventory.inventory) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (has_pre_manifest_private_lease_candidate(*after_read.inventory)) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto after_read_inventory_validated = validate_manifest_bound_inventory(
            root.root.get(), frozen->absolute, *after_read.inventory, *existing.manifest,
            creator_process_id, {}, false);
        if (!after_read_inventory_validated) {
            return open_failure(after_read_inventory_validated.diagnostic);
        }
        if (*after_read_inventory_validated.base_lock_identities !=
                *inventory_validated.base_lock_identities ||
            *after_read_inventory_validated.private_lease_witnesses !=
                *inventory_validated.private_lease_witnesses ||
            *after_read_inventory_validated.worker_attempt_records !=
                *inventory_validated.worker_attempt_records ||
            *after_read_inventory_validated.merge_started_records !=
                *inventory_validated.merge_started_records ||
            *after_read_inventory_validated.chunk_terminal_failure_records !=
                *inventory_validated.chunk_terminal_failure_records ||
            *after_read_inventory_validated.wave_merge_commit_records !=
                *inventory_validated.wave_merge_commit_records) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }

        auto published =
            publish_manifest(root.root.get(), *existing.bytes, hooks, creator_process_id);
        if (!published) {
            return open_failure(std::move(published.diagnostic));
        }
        auto final_manifest = confirm_final_manifest(
            root.root.get(), *existing.bytes, expected_manifest_digest, root.root_identity,
            lock.lock_identity, *published.snapshot, creator_process_id);
        if (!final_manifest) {
            return open_failure(std::move(final_manifest.diagnostic));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(lock_validated);
        }
        const auto final_inventory = inspect_namespace(root.root.get());
        if (!final_inventory) {
            return open_failure(final_inventory.diagnostic);
        }
        const auto final_inventory_validated = validate_manifest_bound_inventory(
            root.root.get(), frozen->absolute, *final_inventory.inventory, *final_manifest.manifest,
            creator_process_id);
        if (!final_inventory_validated) {
            return open_failure(final_inventory_validated.diagnostic);
        }
        if (!final_inventory.inventory->lock || !final_inventory.inventory->manifest ||
            final_inventory.inventory->pending ||
            final_inventory.inventory->private_lease_base_lock_leaves !=
                after_read.inventory->private_lease_base_lock_leaves ||
            final_inventory.inventory->private_lease_protocol_leaves !=
                after_read.inventory->private_lease_protocol_leaves ||
            final_inventory.inventory->worker_attempt_record_leaves !=
                after_read.inventory->worker_attempt_record_leaves ||
            final_inventory.inventory->merge_started_record_leaves !=
                after_read.inventory->merge_started_record_leaves ||
            final_inventory.inventory->chunk_terminal_failure_record_leaves !=
                after_read.inventory->chunk_terminal_failure_record_leaves ||
            final_inventory.inventory->wave_merge_commit_record_leaves !=
                after_read.inventory->wave_merge_commit_record_leaves ||
            final_inventory.inventory->cleanup_record_leaves !=
                after_read.inventory->cleanup_record_leaves ||
            *final_inventory_validated.base_lock_identities !=
                *after_read_inventory_validated.base_lock_identities ||
            *final_inventory_validated.private_lease_witnesses !=
                *after_read_inventory_validated.private_lease_witnesses ||
            *final_inventory_validated.worker_attempt_records !=
                *after_read_inventory_validated.worker_attempt_records ||
            *final_inventory_validated.merge_started_records !=
                *after_read_inventory_validated.merge_started_records ||
            *final_inventory_validated.chunk_terminal_failure_records !=
                *after_read_inventory_validated.chunk_terminal_failure_records ||
            *final_inventory_validated.wave_merge_commit_records !=
                *after_read_inventory_validated.wave_merge_commit_records) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (!process_matches(creator_process_id)) {
            return open_failure(process_mismatch());
        }

        std::optional<std::size_t> merge_prepared_lease_index;
        std::optional<std::size_t> merge_raw_recovery_lease_index;
        const bool wave_merge_commit_prefix_present =
            !final_inventory_validated.wave_merge_commit_records->empty();
        std::size_t retained_worker_reader_count = 0;
        for (std::size_t index = 0;
             index < final_inventory_validated.private_lease_witnesses->size(); ++index) {
            const auto& lease = final_inventory_validated.private_lease_witnesses->at(index);
            const std::size_t typed_merge_states =
                static_cast<std::size_t>(lease.merge_prepared.has_value()) +
                static_cast<std::size_t>(lease.merge_raw_writer_recovery.has_value());
            if ((lease.worker_handoff.has_value() && typed_merge_states != 0U) ||
                typed_merge_states > 1U) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                               protocol_error()));
            }
            if (lease.worker_handoff.has_value()) {
                ++retained_worker_reader_count;
            }
            if (lease.merge_raw_writer_recovery.has_value()) {
                if (merge_raw_recovery_lease_index.has_value()) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                merge_raw_recovery_lease_index = index;
            }
            if (!lease.merge_prepared.has_value()) {
                continue;
            }
            if (merge_prepared_lease_index.has_value()) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                               protocol_error()));
            }
            merge_prepared_lease_index = index;
        }
        if (wave_merge_commit_prefix_present && !merge_prepared_lease_index.has_value()) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        std::optional<NamespaceInventory> final_witnessed_inventory;
        std::shared_ptr<RecoveredMergePreparedAdmissionStateV1> recovered_prepared_state;
        std::vector<HeldCanonicalPublicationInventoryEntryV1> held_publications;
        if (merge_prepared_lease_index.has_value()) {
            const auto& prepared_lease =
                final_inventory_validated.private_lease_witnesses->at(*merge_prepared_lease_index);
            if (!prepared_lease.merge_prepared.has_value()) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                               protocol_error()));
            }
            recovered_prepared_state = std::make_shared<RecoveredMergePreparedAdmissionStateV1>();
            recovered_prepared_state->worker_readers.entries.resize(retained_worker_reader_count);
            recovered_prepared_state->worker_handoffs.resize(existing.manifest->chunks.size());
            recovered_prepared_state->merge_started_chain.reserve(
                final_inventory_validated.merge_started_records->size());
            for (const auto& start : *final_inventory_validated.merge_started_records) {
                if (!start.canonical_snapshot.has_value() || start.pending_snapshot.has_value()) {
                    return open_failure(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
                }
                recovered_prepared_state->merge_started_chain.push_back(start.record);
            }
            recovered_prepared_state->prepared_record = prepared_lease.merge_prepared->prepared;
            recovered_prepared_state->creator_process_id = creator_process_id;
            held_publications.reserve(retained_worker_reader_count + 1U);
            final_witnessed_inventory.emplace(*final_inventory.inventory);
            final_witnessed_inventory->worker_attempt_records =
                *final_inventory_validated.worker_attempt_records;
            final_witnessed_inventory->merge_started_records =
                *final_inventory_validated.merge_started_records;
            final_witnessed_inventory->chunk_terminal_failure_records =
                *final_inventory_validated.chunk_terminal_failure_records;
            final_witnessed_inventory->wave_merge_commit_records =
                *final_inventory_validated.wave_merge_commit_records;
        }

        auto state = std::make_shared<State>();
        state->absolute_root = std::move(frozen->absolute);
        state->root_leaf = std::move(frozen->leaf);
        state->parent_components = std::move(frozen->parent_components);
        state->manifest = std::move(*final_manifest.manifest);
        state->manifest_snapshot = *final_manifest.snapshot;
        state->root_identity = root.root_identity;
        state->lock_identity = lock.lock_identity;
        state->manifest_bytes = std::move(*final_manifest.bytes);
        if (merge_raw_recovery_lease_index.has_value()) {
            NamespaceInventory anchored_inventory = *final_inventory.inventory;
            anchored_inventory.worker_attempt_records =
                *final_inventory_validated.worker_attempt_records;
            anchored_inventory.merge_started_records =
                *final_inventory_validated.merge_started_records;
            anchored_inventory.chunk_terminal_failure_records =
                *final_inventory_validated.chunk_terminal_failure_records;
            anchored_inventory.wave_merge_commit_records =
                *final_inventory_validated.wave_merge_commit_records;
            state->merge_raw_recovery_open_anchor.emplace(ManifestBoundInventoryWitnessResult{
                .inventory = std::move(anchored_inventory),
                .base_lock_identities = *final_inventory_validated.base_lock_identities,
                .private_lease_witnesses = *final_inventory_validated.private_lease_witnesses,
                .diagnostic = {},
            });
        }
        state->creator_process_id = creator_process_id;
        state->parent_fd = root.parent.release();
        state->root_fd = root.root.release();
        state->lock_fd = lock.lock.release();

        auto store = std::unique_ptr<DistributedSieveWaveStore>(
            new DistributedSieveWaveStore(std::move(state)));
        if (!process_matches(creator_process_id)) {
            store.reset();
            return open_failure(process_mismatch());
        }
        if (!merge_prepared_lease_index.has_value()) {
            return {std::move(store), std::nullopt, std::move(published.diagnostic)};
        }

        auto coordinator = store->claim_worker_coordinator_v1();
        if (!coordinator) {
            return open_failure(std::move(coordinator.diagnostic));
        }

        auto merge_prepared = classify_merge_prepared_publication_prefix_v1(
            MergePreparedAdmissionRevalidatorAuthorityV1::root_fd(*store),
            MergePreparedAdmissionRevalidatorAuthorityV1::absolute_root(*store),
            MergePreparedAdmissionRevalidatorAuthorityV1::manifest(*store),
            store->wave_root_identity(), creator_process_id);
        if (!merge_prepared) {
            return open_failure(std::move(merge_prepared.diagnostic));
        }
        if (!merge_prepared.prefix_present() || !merge_prepared.witness.has_value() ||
            !merge_prepared.retained.has_value()) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        auto& aggregate = *merge_prepared.witness;
        auto& retained_target = *merge_prepared.retained;
        const auto& expected_prepared_lease =
            final_inventory_validated.private_lease_witnesses->at(*merge_prepared_lease_index);
        if (!expected_prepared_lease.merge_prepared.has_value() ||
            !final_witnessed_inventory.has_value() ||
            aggregate.inventory != *final_witnessed_inventory ||
            aggregate.base_lock_identities != *final_inventory_validated.base_lock_identities ||
            aggregate.private_leases != *final_inventory_validated.private_lease_witnesses ||
            aggregate.worker_attempt_records != *final_inventory_validated.worker_attempt_records ||
            aggregate.merge_started_records != *final_inventory_validated.merge_started_records ||
            aggregate.chunk_terminal_failure_records !=
                *final_inventory_validated.chunk_terminal_failure_records ||
            aggregate.wave_merge_commit_records !=
                *final_inventory_validated.wave_merge_commit_records ||
            aggregate.attempt_index != *merge_prepared_lease_index ||
            aggregate.attempt_index >= aggregate.private_leases.size() ||
            aggregate.typed_prepared != *expected_prepared_lease.merge_prepared ||
            aggregate.private_leases[aggregate.attempt_index] != expected_prepared_lease ||
            aggregate.prefix != retained_target.witness ||
            aggregate.typed_prepared != retained_target.typed_prepared ||
            aggregate.start_record != retained_target.start_record ||
            aggregate.names != retained_target.names ||
            aggregate.attempt_index != retained_target.attempt_index ||
            retained_target.provisional_lease != expected_prepared_lease ||
            !retained_target.witness.canonical_terminal() ||
            aggregate.retained_workers.entries.size() != retained_worker_reader_count ||
            recovered_prepared_state == nullptr ||
            recovered_prepared_state->worker_readers.entries.size() !=
                retained_worker_reader_count) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        for (std::size_t index = 0; index < aggregate.retained_workers.entries.size(); ++index) {
            auto& worker = aggregate.retained_workers.entries[index];
            if (worker.attempt_index >= aggregate.private_leases.size() ||
                worker.attempt_index >= aggregate.base_lock_identities.size() ||
                worker.manifest_slot >= store->manifest().chunks.size() ||
                store->manifest().chunks[worker.manifest_slot].chunk_id !=
                    worker.coordinate.chunk_id) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                               protocol_error()));
            }
            const auto& expected_lease = aggregate.private_leases[worker.attempt_index];
            if (!expected_lease.worker_handoff.has_value() ||
                expected_lease.merge_prepared.has_value() ||
                expected_lease != worker.provisional_lease ||
                *expected_lease.worker_handoff != worker.typed_handoff ||
                !worker.witness.canonical_terminal() || worker.adopting || worker.consumed ||
                worker.reader != nullptr || !worker.permit.valid() || !worker.permit.held()) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                               protocol_error()));
            }
            recovered_prepared_state->worker_readers.entries[index].expected_relation_count =
                worker.typed_handoff.handoff.relation_count;
            recovered_prepared_state->worker_handoffs[worker.manifest_slot] =
                worker.typed_handoff.handoff;
            held_publications.push_back(HeldCanonicalPublicationInventoryEntryV1{
                .base_lock_leaf = worker.names.base_lock_leaf,
                .base_lock_identity = aggregate.base_lock_identities[worker.attempt_index],
                .expected_lease = std::addressof(expected_lease),
                .expected_prefix = std::addressof(worker.witness),
                .permit = std::addressof(worker.permit),
                .reader_slot = std::addressof(worker.reader),
                .adopting = std::addressof(worker.adopting),
            });
        }
        if (retained_target.attempt_index >= aggregate.base_lock_identities.size() ||
            retained_target.adopting || retained_target.consumed ||
            retained_target.reader != nullptr || !retained_target.permit.valid() ||
            !retained_target.permit.held()) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        held_publications.push_back(HeldCanonicalPublicationInventoryEntryV1{
            .base_lock_leaf = retained_target.names.base_lock_leaf,
            .base_lock_identity = aggregate.base_lock_identities[retained_target.attempt_index],
            .expected_lease =
                std::addressof(aggregate.private_leases[retained_target.attempt_index]),
            .expected_prefix = std::addressof(retained_target.witness),
            .permit = std::addressof(retained_target.permit),
            .reader_slot = std::addressof(retained_target.reader),
            .adopting = std::addressof(retained_target.adopting),
        });

        HeldCanonicalPublicationInventoryV1 held{
            .entries = held_publications,
        };
        if (const auto projection =
                revalidate_recovered_merge_prepared_projection(*store, aggregate, held);
            projection.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(projection);
        }

        const auto adopt_retained_publication =
            [&](auto& retained, HeldCanonicalPublicationInventoryEntryV1& held_entry,
                DistributedSieveRecoveredPreparedPublicationSubjectV1 subject,
                std::size_t manifest_slot) noexcept {
                retained.adopting = true;
                held.current = std::addressof(held_entry);
                held.current_reader = nullptr;

                auto converged = private_lease::reconcile_private_handoff_publication_for_resume_v1(
                    retained.permit);
                if (!converged.converged()) {
                    return worker_handoff_inspection_failure(converged.result);
                }
                if (converged.disposition !=
                        private_lease::PrivateHandoffPublicationResumeDispositionV1::
                            CanonicalTerminal ||
                    !converged.expected_prefix.has_value() ||
                    *converged.expected_prefix != retained.witness ||
                    !converged.terminal_prefix.has_value() ||
                    *converged.terminal_prefix != retained.witness || !retained.permit.held() ||
                    retained.permit.valid()) {
                    return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                      protocol_error());
                }

                MergePreparedAdmissionRevalidationContextV1 context{
                    .store = store.get(),
                    .expected = std::addressof(aggregate),
                    .hooks = std::addressof(hooks.merge_prepared_resume),
                    .held = held,
                    .subject = subject,
                    .manifest_slot = manifest_slot,
                };
                auto adopted = private_lease::adopt_consumed_canonical_private_handoff_reader_v1(
                    retained.permit,
                    MergePreparedAdmissionRevalidatorAuthorityV1::bind(
                        revalidate_merge_prepared_admission_for_relation, std::addressof(context)));
                if (!adopted.adopted() || adopted.reader == nullptr) {
                    if (context.diagnostic.status != DistributedSieveWaveStoreStatus::ready) {
                        return context.diagnostic;
                    }
                    if (!retained.permit.held()) {
                        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                          protocol_error());
                    }
                    return worker_handoff_inspection_failure(adopted.result);
                }
                if (retained.permit.held() || retained.permit.valid()) {
                    return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                      protocol_error());
                }
                if (context.aggregate_revalidation_count !=
                    static_cast<std::size_t>(
                        DistributedSieveRecoveredPreparedAggregatePhaseV1::Count)) {
                    return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                      protocol_error());
                }

                retained.reader = std::move(adopted.reader);
                retained.adopting = false;
                retained.consumed = true;
                held.current = nullptr;
                held.current_reader = nullptr;
                return revalidate_recovered_merge_prepared_projection(*store, aggregate, held);
            };

        if (auto adopted = adopt_retained_publication(
                retained_target, held_publications.back(),
                DistributedSieveRecoveredPreparedPublicationSubjectV1::Target,
                static_cast<std::size_t>(DISTRIBUTED_SIEVE_PROTOCOL_NO_INDEX));
            adopted.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(std::move(adopted));
        }
        for (std::size_t index = 0; index < aggregate.retained_workers.entries.size(); ++index) {
            auto& worker = aggregate.retained_workers.entries[index];
            if (auto adopted = adopt_retained_publication(
                    worker, held_publications[index],
                    DistributedSieveRecoveredPreparedPublicationSubjectV1::Worker,
                    worker.manifest_slot);
                adopted.status != DistributedSieveWaveStoreStatus::ready) {
                return open_failure(std::move(adopted));
            }
        }

        recovered_prepared_state->store = std::move(store);
        recovered_prepared_state->coordinator_claim = std::move(coordinator.claim);
        for (std::size_t index = 0; index < aggregate.retained_workers.entries.size(); ++index) {
            recovered_prepared_state->worker_readers.entries[index].reader =
                std::move(aggregate.retained_workers.entries[index].reader);
        }
        recovered_prepared_state->merged_reader = std::move(retained_target.reader);
        std::vector<const WorkerHandoffV1*> retained_handoffs;
        retained_handoffs.reserve(recovered_prepared_state->worker_handoffs.size());
        for (const auto& handoff : recovered_prepared_state->worker_handoffs) {
            retained_handoffs.push_back(handoff.has_value() ? std::addressof(*handoff) : nullptr);
        }
        const WaveMergeCommitV1* existing_commit =
            wave_merge_commit_prefix_present
                ? std::addressof(
                      final_inventory_validated.wave_merge_commit_records->front().record)
                : nullptr;
        MergeCommitPredecessorSnapshots predecessor_snapshots;
        const auto captured =
            recovered_prepared_state->store->capture_merge_commit_predecessor_snapshots_v1(
                recovered_prepared_state->prepared_record,
                recovered_prepared_state->merge_started_chain, retained_handoffs, existing_commit,
                predecessor_snapshots);
        if (captured.status != DistributedSieveWaveStoreStatus::ready) {
            return open_failure(captured);
        }
        recovered_prepared_state->predecessor_snapshots.emplace(std::move(predecessor_snapshots));
        if (!recovered_merge_prepared_admission_state_valid(
                recovered_prepared_state.get(),
                std::addressof(recovered_prepared_state->prepared_record), creator_process_id)) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
        }

        if (wave_merge_commit_prefix_present) {
            auto tail_predecessor_snapshots = *recovered_prepared_state->predecessor_snapshots;
            auto committed = recovered_prepared_state->store->publish_wave_merge_commit_v1(
                recovered_prepared_state->prepared_record,
                recovered_prepared_state->merge_started_chain, retained_handoffs,
                tail_predecessor_snapshots, hooks.wave_merge_commit);
            if (!committed || !committed.admission_spent) {
                return open_failure(std::move(committed.diagnostic));
            }

            using CommittedTail = distributed_sieve_merge_writer_authority_detail::
                DistributedSieveCommittedTailAdmissionV1;
            std::shared_ptr<distributed_sieve_merge_writer_authority_detail::
                                DistributedSieveMergePreparedOriginV1>
                committed_origin = recovered_prepared_state;
            CommittedTail tail(std::move(committed_origin),
                               std::addressof(recovered_prepared_state->prepared_record),
                               std::move(*committed.commit), std::move(tail_predecessor_snapshots),
                               *committed.canonical_snapshot, creator_process_id);
            if (!tail.valid()) {
                return open_failure(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                               protocol_error()));
            }
            std::optional<CommittedTail> committed_result;
            committed_result.emplace(std::move(tail));
            return {nullptr, std::nullopt, std::move(committed_result),
                    std::move(committed.diagnostic)};
        }

        using PreparedAdmission = distributed_sieve_merge_writer_authority_detail::
            DistributedSieveMergePreparedAdmissionV1;
        std::shared_ptr<
            distributed_sieve_merge_writer_authority_detail::DistributedSieveMergePreparedOriginV1>
            origin = recovered_prepared_state;
        PreparedAdmission prepared_admission(
            std::move(origin), std::addressof(recovered_prepared_state->prepared_record),
            creator_process_id);
        if (!prepared_admission.valid()) {
            return open_failure(
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error()));
        }
        std::optional<PreparedAdmission> prepared_result;
        prepared_result.emplace(std::move(prepared_admission));
        return {nullptr, std::move(prepared_result), std::nullopt, std::move(published.diagnostic)};
#endif
    } catch (const std::bad_alloc&) {
        return open_failure(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                       std::make_error_code(std::errc::not_enough_memory)));
    } catch (const std::filesystem::filesystem_error& error) {
        return open_failure(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, error.code()));
    } catch (...) {
        return open_failure(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                       std::make_error_code(std::errc::io_error)));
    }
}

DistributedSieveWorkerCleanupRootOpenResultV1
open_worker_cleanup_root_v1(const std::filesystem::path& absolute_root,
                            const util::Sha256Digest& expected_manifest_digest,
                            DistributedSieveWorkerCleanupRootOpenTestHooksV1 hooks) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        return DistributedSieveWorkerCleanupRootOpenResultV1{std::nullopt, std::move(failure)};
    };
    const auto conflict = [&] {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    };
    try {
        auto frozen = freeze_absolute_root(absolute_root);
        if (!frozen.has_value() || nil_digest(expected_manifest_digest)) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const std::uint64_t creator_process_id = current_process_id();
        if (!process_matches(creator_process_id)) {
            return fail_with(process_mismatch());
        }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)hooks;
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        auto root = open_root(*frozen, false, creator_process_id);
        if (!root) {
            return fail_with(std::move(root.diagnostic));
        }
        auto inspected = inspect_namespace(root.root.get());
        if (!inspected) {
            return fail_with(std::move(inspected.diagnostic));
        }
        const NamespaceInventory initial_inventory = *inspected.inventory;
        auto lock = open_lock(root.root.get(), initial_inventory, false, creator_process_id);
        if (!lock) {
            return fail_with(std::move(lock.diagnostic));
        }
        if (const auto root_validated =
                validate_root_binding(root.parent.get(), frozen->parent_components, root.root.get(),
                                      frozen->leaf, creator_process_id, root.root_identity);
            root_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(root_validated);
        }
        if (const auto lock_validated = validate_lock_binding(
                root.root.get(), lock.lock.get(), creator_process_id, lock.lock_identity);
            lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(lock_validated);
        }
        inspected = inspect_namespace(root.root.get());
        if (!inspected) {
            return fail_with(std::move(inspected.diagnostic));
        }
        if (*inspected.inventory != initial_inventory || !inspected.inventory->lock ||
            !inspected.inventory->manifest || inspected.inventory->pending) {
            return conflict();
        }
        auto existing =
            read_existing_manifest(root.root.get(), *inspected.inventory, expected_manifest_digest,
                                   root.root_identity, lock.lock_identity, creator_process_id);
        if (!existing) {
            return fail_with(std::move(existing.diagnostic));
        }
        auto canonical_manifest =
            read_manifest_leaf(root.root.get(), MANIFEST_LEAF, creator_process_id);
        if (!canonical_manifest) {
            return fail_with(std::move(canonical_manifest.diagnostic));
        }
        if (*canonical_manifest.bytes != *existing.bytes) {
            return conflict();
        }
        const auto validate_authority = [&]() noexcept {
            return validate_held_wave_store_manifest_authority(
                root.parent.get(), frozen->parent_components, root.root.get(), frozen->leaf,
                root.root_identity, lock.lock.get(), lock.lock_identity, *existing.bytes,
                *canonical_manifest.snapshot, creator_process_id);
        };
        if (const auto authority = validate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }

        auto first = observe_cleanup_root_v1(root.root.get(), frozen->absolute, *existing.manifest,
                                             root.root_identity, creator_process_id);
        if (!first) {
            return fail_with(std::move(first.diagnostic));
        }
        if (hooks.after_first_observation != nullptr) {
            hooks.after_first_observation(hooks.context);
        }
        if (!process_matches(creator_process_id)) {
            return fail_with(process_mismatch());
        }
        if (const auto authority = validate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }
        auto second = observe_cleanup_root_v1(root.root.get(), frozen->absolute, *existing.manifest,
                                              root.root_identity, creator_process_id);
        if (!second) {
            return fail_with(std::move(second.diagnostic));
        }
        if (*first.observation != *second.observation) {
            return conflict();
        }

        auto wave_state = std::make_shared<DistributedSieveWaveStore::State>();
        wave_state->absolute_root = frozen->absolute;
        wave_state->root_leaf = frozen->leaf;
        wave_state->parent_components = frozen->parent_components;
        wave_state->manifest = *existing.manifest;
        wave_state->manifest_snapshot = *canonical_manifest.snapshot;
        wave_state->root_identity = root.root_identity;
        wave_state->lock_identity = lock.lock_identity;
        wave_state->manifest_bytes = *existing.bytes;
        wave_state->creator_process_id = creator_process_id;
        wave_state->parent_fd = root.parent.release();
        wave_state->root_fd = root.root.release();
        wave_state->lock_fd = lock.lock.release();
        auto store = std::unique_ptr<DistributedSieveWaveStore>(
            new DistributedSieveWaveStore(std::move(wave_state)));
        if (WorkerCleanupRootRevalidatorAuthorityV1::revalidate_authority(*store).status !=
            DistributedSieveWaveStoreStatus::ready) {
            return conflict();
        }

        const auto merged_base = store->absolute_root() /
                                 second.observation->merge_names.private_directory_leaf / "corpus";
        auto adopted = relation::OOCCleanupTransaction::adopt_private_handoff(merged_base);
        if (!adopted.adopted() || !adopted.adoption.has_value()) {
            return fail_with(worker_handoff_inspection_failure(adopted.result));
        }
        auto adopted_reader =
            std::make_unique<relation::OOCPrivateHandoffReader>(std::move(*adopted.adoption));
        const auto& expected = *second.observation;
        if (!adopted_reader->valid() || adopted_reader->record() != expected.merged_prefix.record ||
            !expected.merged_prefix.canonical_snapshot.has_value() ||
            adopted_reader->handoff_snapshot() != *expected.merged_prefix.canonical_snapshot ||
            adopted_reader->index_snapshot() != expected.typed_prepared.index_snapshot ||
            adopted_reader->data_snapshot() != expected.typed_prepared.data_snapshot) {
            return conflict();
        }
        auto decoded = decode_distributed_sieve_record(adopted_reader->record().opaque_payload);
        const auto* prepared = decoded && decoded.value.has_value()
                                   ? std::get_if<MergePreparedV1>(std::addressof(*decoded.value))
                                   : nullptr;
        if (prepared == nullptr ||
            prepared->self_digest != expected.typed_prepared.prepared.self_digest) {
            return conflict();
        }

        auto read_only = private_lease::take_read_only_reader_and_release_adoption_authority(
            std::move(*adopted_reader));
        adopted_reader.reset();
        if (!read_only.valid()) {
            return conflict();
        }
        auto merged_reader = std::make_unique<relation::OOCRelationReader>(std::move(read_only));
        auto confirmed =
            observe_cleanup_root_v1(WorkerCleanupRootRevalidatorAuthorityV1::root_fd(*store),
                                    WorkerCleanupRootRevalidatorAuthorityV1::absolute_root(*store),
                                    WorkerCleanupRootRevalidatorAuthorityV1::manifest(*store),
                                    store->wave_root_identity(), creator_process_id);
        if (!confirmed || *confirmed.observation != expected ||
            WorkerCleanupRootRevalidatorAuthorityV1::revalidate_authority(*store).status !=
                DistributedSieveWaveStoreStatus::ready) {
            return confirmed ? conflict() : fail_with(std::move(confirmed.diagnostic));
        }

        auto admission_state =
            std::make_unique<DistributedSieveWorkerCleanupRootAdmissionV1::State>();
        admission_state->store = std::move(store);
        admission_state->merged_reader = std::move(merged_reader);
        admission_state->observation = std::move(*confirmed.observation);
        admission_state->creator_process_id = creator_process_id;
        std::optional<DistributedSieveWorkerCleanupRootAdmissionV1> admission;
        admission.emplace(DistributedSieveWorkerCleanupRootAdmissionV1(std::move(admission_state)));
        return {std::move(admission), {}};
#endif
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (const std::filesystem::filesystem_error& error) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, error.code()));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

const std::filesystem::path& DistributedSieveWaveStore::absolute_root() const noexcept {
    return state_->absolute_root;
}

const WaveManifestV1& DistributedSieveWaveStore::manifest() const noexcept {
    return state_->manifest;
}

const util::Sha256Digest& DistributedSieveWaveStore::manifest_digest() const noexcept {
    return state_->manifest.self_digest;
}

const NativeIdentityV1& DistributedSieveWaveStore::wave_root_identity() const noexcept {
    return state_->root_identity;
}

const NativeIdentityV1& DistributedSieveWaveStore::permanent_lock_identity() const noexcept {
    return state_->lock_identity;
}

const durable_record::RecordSnapshot&
DistributedSieveWaveStore::manifest_snapshot() const noexcept {
    return state_->manifest_snapshot;
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveWaveStore::revalidate_authority() const noexcept {
    if (state_ == nullptr) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    if (state_->creator_process_id == 0 || !process_matches(state_->creator_process_id)) {
        return process_mismatch();
    }
    if (const auto root_validated = validate_root_binding(
            state_->parent_fd, state_->parent_components, state_->root_fd, state_->root_leaf,
            state_->creator_process_id, state_->root_identity);
        root_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return root_validated;
    }
    if (const auto lock_validated = validate_lock_binding(
            state_->root_fd, state_->lock_fd, state_->creator_process_id, state_->lock_identity);
        lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return lock_validated;
    }
    if (const auto pending =
            require_manifest_pending_missing(state_->root_fd, state_->creator_process_id);
        pending.status != DistributedSieveWaveStoreStatus::ready) {
        return pending;
    }

    auto canonical = read_manifest_leaf(state_->root_fd, MANIFEST_LEAF, state_->creator_process_id);
    if (!canonical) {
        return canonical.diagnostic;
    }
    if (*canonical.bytes != state_->manifest_bytes ||
        *canonical.snapshot != state_->manifest_snapshot) {
        return diagnostic(DistributedSieveWaveStoreStatus::manifest_conflict, protocol_error());
    }
    auto decoded = decode_manifest(*canonical.bytes, state_->manifest.self_digest,
                                   state_->root_identity, state_->lock_identity);
    if (!decoded) {
        return decoded.diagnostic;
    }

    if (const auto root_validated = validate_root_binding(
            state_->parent_fd, state_->parent_components, state_->root_fd, state_->root_leaf,
            state_->creator_process_id, state_->root_identity);
        root_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return root_validated;
    }
    if (const auto lock_validated = validate_lock_binding(
            state_->root_fd, state_->lock_fd, state_->creator_process_id, state_->lock_identity);
        lock_validated.status != DistributedSieveWaveStoreStatus::ready) {
        return lock_validated;
    }
    if (const auto pending =
            require_manifest_pending_missing(state_->root_fd, state_->creator_process_id);
        pending.status != DistributedSieveWaveStoreStatus::ready) {
        return pending;
    }
    if (!process_matches(state_->creator_process_id)) {
        return process_mismatch();
    }
    return {};
#endif
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveWaveStore::capture_merge_commit_predecessor_snapshots_v1(
    const MergePreparedV1& prepared, std::span<const MergeStartedV1> merge_started_chain,
    std::span<const WorkerHandoffV1* const> worker_handoffs,
    const WaveMergeCommitV1* existing_commit,
    MergeCommitPredecessorSnapshots& snapshots) const noexcept {
    if (state_ == nullptr) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }
#if !defined(__APPLE__)
    (void)prepared;
    (void)merge_started_chain;
    (void)worker_handoffs;
    (void)existing_commit;
    (void)snapshots;
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    try {
        std::optional<std::vector<std::byte>> existing_commit_bytes;
        if (existing_commit != nullptr) {
            DistributedSieveProtocolRecordV1 typed_commit(*existing_commit);
            auto encoded = encode_distributed_sieve_record(typed_commit);
            if (!encoded || !encoded.bytes.has_value()) {
                auto failure = diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                          protocol_error());
                failure.protocol_status = encoded.status;
                return failure;
            }
            existing_commit_bytes.emplace(std::move(*encoded.bytes));
        }
        struct ExactPredecessorObservation final {
            NamespaceInventory inventory;
            MergeCommitPredecessorSnapshots snapshots;
        };
        const auto observe = [&]() -> std::pair<std::optional<ExactPredecessorObservation>,
                                                DistributedSieveWaveStoreDiagnostic> {
            auto observed = inspect_namespace(state_->root_fd);
            if (!observed) {
                return {std::nullopt, std::move(observed.diagnostic)};
            }
            if (!observed.inventory->cleanup_record_leaves.empty()) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            auto commit_inventory = validate_wave_merge_commit_record_inventory(
                state_->root_fd, *observed.inventory, state_->manifest, state_->creator_process_id);
            if (!commit_inventory) {
                return {std::nullopt, std::move(commit_inventory.diagnostic)};
            }
            const bool exact_commit_prefix =
                existing_commit == nullptr
                    ? commit_inventory.witnesses->empty()
                    : commit_inventory.witnesses->size() == 1U &&
                          existing_commit_bytes.has_value() &&
                          commit_inventory.witnesses->front().bytes == *existing_commit_bytes;
            if (!exact_commit_prefix) {
                return {std::nullopt,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())};
            }
            observed.inventory->wave_merge_commit_records = *commit_inventory.witnesses;
            auto dependency = validate_wave_merge_commit_dependency_snapshot(
                state_->root_fd, *observed.inventory, state_->manifest, prepared,
                merge_started_chain, worker_handoffs, existing_commit, state_->creator_process_id);
            if (!dependency) {
                return {std::nullopt, std::move(dependency.diagnostic)};
            }
            return {ExactPredecessorObservation{
                        .inventory = std::move(*observed.inventory),
                        .snapshots = std::move(*dependency.snapshots),
                    },
                    {}};
        };

        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        auto [first, first_failure] = observe();
        if (!first.has_value()) {
            return first_failure;
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        auto [confirmed, confirmed_failure] = observe();
        if (!confirmed.has_value()) {
            return confirmed_failure;
        }
        if (confirmed->inventory != first->inventory || confirmed->snapshots != first->snapshots) {
            return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                              protocol_error());
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        snapshots = std::move(confirmed->snapshots);
        return {};
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }
#endif
}

bool DistributedSieveWaveStore::revalidate_committed_tail_v1(
    const MergePreparedV1& prepared, std::span<const MergeStartedV1> merge_started_chain,
    std::span<const WorkerHandoffV1* const> worker_handoffs,
    const MergeCommitPredecessorSnapshots& predecessor_snapshots, const WaveMergeCommitV1& commit,
    const durable_record::RecordSnapshot& canonical_snapshot) const noexcept {
    if (state_ == nullptr) {
        return false;
    }
#if !defined(__APPLE__)
    (void)prepared;
    (void)merge_started_chain;
    (void)worker_handoffs;
    (void)predecessor_snapshots;
    (void)commit;
    (void)canonical_snapshot;
    return false;
#else
    try {
        DistributedSieveProtocolRecordV1 typed_commit(commit);
        if (const auto valid = validate_distributed_sieve_record(typed_commit, true); !valid) {
            return false;
        }
        const auto encoded = encode_distributed_sieve_record(typed_commit);
        if (!encoded || !encoded.bytes.has_value()) {
            return false;
        }

        const auto names = distributed_sieve_wave_merge_commit_names_v1();
        struct ExactCommitObservation final {
            NamespaceInventory inventory;
            DistributedSieveWaveMergeCommitRecordInventoryWitnessV1 witness;
            MergeCommitPredecessorSnapshots predecessor_snapshots;
        };
        const auto observe_exact_commit = [&]() -> std::optional<ExactCommitObservation> {
            auto observed = inspect_namespace(state_->root_fd);
            if (!observed || !observed.inventory->cleanup_record_leaves.empty() ||
                observed.inventory->wave_merge_commit_record_leaves !=
                    std::vector<std::string>{names.canonical_record_leaf}) {
                return std::nullopt;
            }
            auto loaded = validate_wave_merge_commit_record_inventory(
                state_->root_fd, *observed.inventory, state_->manifest, state_->creator_process_id);
            if (!loaded || loaded.witnesses->size() != 1U) {
                return std::nullopt;
            }
            auto witness = std::move(loaded.witnesses->front());
            if (witness.bytes != *encoded.bytes || !witness.canonical_snapshot.has_value() ||
                witness.pending_snapshot.has_value() ||
                *witness.canonical_snapshot != canonical_snapshot) {
                return std::nullopt;
            }
            observed.inventory->wave_merge_commit_records = {witness};
            auto dependency = validate_wave_merge_commit_dependency_snapshot(
                state_->root_fd, *observed.inventory, state_->manifest, prepared,
                merge_started_chain, worker_handoffs, std::addressof(witness.record),
                state_->creator_process_id, std::addressof(predecessor_snapshots));
            if (!dependency) {
                return std::nullopt;
            }
            return ExactCommitObservation{
                .inventory = std::move(*observed.inventory),
                .witness = std::move(witness),
                .predecessor_snapshots = std::move(*dependency.snapshots),
            };
        };

        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return false;
        }
        auto first = observe_exact_commit();
        if (!first.has_value()) {
            return false;
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return false;
        }
        auto confirmed = observe_exact_commit();
        if (!confirmed.has_value() || confirmed->inventory != first->inventory ||
            confirmed->witness != first->witness ||
            confirmed->predecessor_snapshots != first->predecessor_snapshots) {
            return false;
        }
        return revalidate_authority().status == DistributedSieveWaveStoreStatus::ready;
    } catch (...) {
        return false;
    }
#endif
}

DistributedSieveWaveMergeCommitPublicationResultV1
DistributedSieveWaveStore::publish_wave_merge_commit_v1(
    const MergePreparedV1& prepared, std::span<const MergeStartedV1> merge_started_chain,
    std::span<const WorkerHandoffV1* const> worker_handoffs,
    const MergeCommitPredecessorSnapshots& predecessor_snapshots,
    DistributedSieveWaveMergeCommitTestHooksV1 hooks) const noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure,
                              bool admission_spent = false) {
        return DistributedSieveWaveMergeCommitPublicationResultV1{
            .commit = std::nullopt,
            .canonical_snapshot = std::nullopt,
            .diagnostic = std::move(failure),
            .admission_spent = admission_spent,
        };
    };
    if (state_ == nullptr) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }

#if !defined(__APPLE__)
    (void)prepared;
    (void)merge_started_chain;
    (void)worker_handoffs;
    (void)predecessor_snapshots;
    (void)hooks;
    return fail_with(
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
    if (const auto authority = revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(authority);
    }
    bool admission_spent = false;
    try {
        if (prepared.manifest_digest != state_->manifest.self_digest ||
            prepared.work_digest != state_->manifest.work_sha256 ||
            prepared.merge_policy_version != state_->manifest.merge_policy_version ||
            prepared.ordered_inputs.size() != state_->manifest.chunks.size() ||
            prepared.per_chunk_retained_counts.size() != state_->manifest.chunks.size() ||
            worker_handoffs.size() != state_->manifest.chunks.size() ||
            merge_started_chain.empty()) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

        WaveMergeCommitV1 commit{
            .manifest_digest = prepared.manifest_digest,
            .work_digest = prepared.work_digest,
            .merge_policy_version = prepared.merge_policy_version,
            .input_relation_count = prepared.input_relation_count,
            .duplicate_relation_count = prepared.duplicate_relation_count,
            .output_relation_count = prepared.output_relation_count,
            .merge_prepared_digest = prepared.self_digest,
            .merged_lease = prepared.merged_lease,
            .merged_artifact = prepared.merged_artifact,
        };
        commit.chunks.reserve(prepared.ordered_inputs.size());
        for (std::size_t slot = 0; slot < prepared.ordered_inputs.size(); ++slot) {
            if (prepared.per_chunk_retained_counts[slot].chunk_id !=
                prepared.ordered_inputs[slot].chunk_id) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                            invalid_argument_error()));
            }
            commit.chunks.push_back(ChunkCommitSummaryV1{
                .input = prepared.ordered_inputs[slot],
                .retained_relation_count =
                    prepared.per_chunk_retained_counts[slot].retained_relation_count,
                .diagnostic =
                    NormalizedDiagnosticV1{
                        .kind = NormalizedDiagnosticKindV1::none,
                        .code = 0,
                    },
            });
        }
        DistributedSieveProtocolRecordV1 typed_commit(std::move(commit));
        if (const auto sealed = seal_distributed_sieve_record(typed_commit); !sealed) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            failure.protocol_status = sealed;
            return fail_with(std::move(failure));
        }
        auto encoded = encode_distributed_sieve_record(typed_commit);
        if (!encoded || !encoded.bytes.has_value()) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            failure.protocol_status = encoded.status;
            return fail_with(std::move(failure));
        }
        commit = std::get<WaveMergeCommitV1>(std::move(typed_commit));

        auto initial = inspect_namespace(state_->root_fd);
        if (!initial) {
            return fail_with(std::move(initial.diagnostic));
        }
        const bool existing_prefix = !initial.inventory->wave_merge_commit_record_leaves.empty();
        admission_spent = existing_prefix;
        if (!initial.inventory->cleanup_record_leaves.empty()) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
                existing_prefix);
        }
        auto initial_commit = validate_wave_merge_commit_record_inventory(
            state_->root_fd, *initial.inventory, state_->manifest, state_->creator_process_id);
        if (!initial_commit) {
            return fail_with(std::move(initial_commit.diagnostic), existing_prefix);
        }
        if (!initial_commit.witnesses->empty() &&
            initial_commit.witnesses->front().bytes != *encoded.bytes) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
                true);
        }
        auto initial_dependency = validate_wave_merge_commit_dependency_snapshot(
            state_->root_fd, *initial.inventory, state_->manifest, prepared, merge_started_chain,
            worker_handoffs, std::addressof(commit), state_->creator_process_id,
            std::addressof(predecessor_snapshots));
        if (!initial_dependency) {
            return fail_with(std::move(initial_dependency.diagnostic), existing_prefix);
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority, existing_prefix);
        }
        if (hooks.before_record_publication != nullptr) {
            hooks.before_record_publication(hooks.context);
            if (!process_matches(state_->creator_process_id)) {
                return fail_with(process_mismatch(), existing_prefix);
            }
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority, existing_prefix);
        }
        auto confirmed_predecessor = inspect_namespace(state_->root_fd);
        if (!confirmed_predecessor) {
            return fail_with(std::move(confirmed_predecessor.diagnostic), existing_prefix);
        }
        if (*confirmed_predecessor.inventory != *initial.inventory) {
            const bool commit_now_visible =
                !confirmed_predecessor.inventory->wave_merge_commit_record_leaves.empty();
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
                existing_prefix || commit_now_visible);
        }
        auto confirmed_commit = validate_wave_merge_commit_record_inventory(
            state_->root_fd, *confirmed_predecessor.inventory, state_->manifest,
            state_->creator_process_id);
        if (!confirmed_commit) {
            return fail_with(std::move(confirmed_commit.diagnostic), existing_prefix);
        }
        if (*confirmed_commit.witnesses != *initial_commit.witnesses) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
                existing_prefix);
        }
        auto confirmed_dependency = validate_wave_merge_commit_dependency_snapshot(
            state_->root_fd, *confirmed_predecessor.inventory, state_->manifest, prepared,
            merge_started_chain, worker_handoffs, std::addressof(commit),
            state_->creator_process_id, std::addressof(predecessor_snapshots));
        if (!confirmed_dependency) {
            return fail_with(std::move(confirmed_dependency.diagnostic), existing_prefix);
        }
        if (*confirmed_dependency.snapshots != *initial_dependency.snapshots) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()),
                existing_prefix);
        }

        const auto names = distributed_sieve_wave_merge_commit_names_v1();
        WaveMergeCommitHookBridgeV1 bridge{
            .hooks = hooks,
            .creator_process_id = state_->creator_process_id,
        };
        admission_spent = true;
        const auto published = durable_record::publish_at(
            static_cast<durable_record::NativeHandle>(state_->root_fd), names.pending_record_leaf,
            names.canonical_record_leaf, *encoded.bytes,
            durable_record::RecordTestHooks{
                .stop_after = bridge_wave_merge_commit_hook_v1,
                .context = &bridge,
            });
        auto publication = wave_merge_commit_publication_diagnostic_v1(published, bridge);
        const auto attach_publication = [&](DistributedSieveWaveStoreDiagnostic higher) {
            higher.publication_status = publication.publication_status;
            higher.publication_disposition = publication.publication_disposition;
            higher.last_wave_merge_commit_fault_point =
                publication.last_wave_merge_commit_fault_point;
            return higher;
        };
        if (published.status() != durable_record::RecordPublishStatus::durable ||
            !published.canonical_snapshot().has_value()) {
            return fail_with(std::move(publication), true);
        }

        struct ExactCommitObservation final {
            NamespaceInventory inventory;
            DistributedSieveWaveMergeCommitRecordInventoryWitnessV1 witness;
            MergeCommitPredecessorSnapshots predecessor_snapshots;
        };
        const auto observe_exact_successor =
            [&]() -> std::pair<std::optional<ExactCommitObservation>,
                               DistributedSieveWaveStoreDiagnostic> {
            auto observed = inspect_namespace(state_->root_fd);
            if (!observed) {
                return {std::nullopt, attach_publication(std::move(observed.diagnostic))};
            }
            auto loaded = validate_wave_merge_commit_record_inventory(
                state_->root_fd, *observed.inventory, state_->manifest, state_->creator_process_id);
            if (!loaded) {
                return {std::nullopt, attach_publication(std::move(loaded.diagnostic))};
            }
            if (!observed.inventory->cleanup_record_leaves.empty() ||
                observed.inventory->wave_merge_commit_record_leaves !=
                    std::vector<std::string>{names.canonical_record_leaf} ||
                loaded.witnesses->size() != 1U) {
                return {std::nullopt, attach_publication(diagnostic(
                                          DistributedSieveWaveStoreStatus::namespace_conflict,
                                          protocol_error()))};
            }
            auto witness = std::move(loaded.witnesses->front());
            if (witness.bytes != *encoded.bytes || !witness.canonical_snapshot.has_value() ||
                witness.pending_snapshot.has_value() ||
                witness.canonical_snapshot != published.canonical_snapshot()) {
                return {std::nullopt, attach_publication(diagnostic(
                                          DistributedSieveWaveStoreStatus::namespace_conflict,
                                          protocol_error()))};
            }
            observed.inventory->wave_merge_commit_records = {witness};
            auto dependency = validate_wave_merge_commit_dependency_snapshot(
                state_->root_fd, *observed.inventory, state_->manifest, prepared,
                merge_started_chain, worker_handoffs, std::addressof(witness.record),
                state_->creator_process_id, std::addressof(predecessor_snapshots));
            if (!dependency) {
                return {std::nullopt, attach_publication(std::move(dependency.diagnostic))};
            }
            return {ExactCommitObservation{
                        .inventory = std::move(*observed.inventory),
                        .witness = std::move(witness),
                        .predecessor_snapshots = std::move(*dependency.snapshots),
                    },
                    {}};
        };

        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority), true);
        }
        auto [first_successor, first_failure] = observe_exact_successor();
        if (!first_successor.has_value()) {
            return fail_with(std::move(first_failure), true);
        }
        if (hooks.after_first_successor_validation != nullptr) {
            hooks.after_first_successor_validation(hooks.context);
            if (!process_matches(state_->creator_process_id)) {
                return fail_with(attach_publication(process_mismatch()), true);
            }
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority), true);
        }
        auto [confirmed_successor, confirmed_failure] = observe_exact_successor();
        if (!confirmed_successor.has_value()) {
            return fail_with(std::move(confirmed_failure), true);
        }
        if (confirmed_successor->inventory != first_successor->inventory ||
            confirmed_successor->witness != first_successor->witness ||
            confirmed_successor->predecessor_snapshots != first_successor->predecessor_snapshots) {
            return fail_with(
                attach_publication(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())),
                true);
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority), true);
        }
        publication.status = DistributedSieveWaveStoreStatus::ready;
        return {
            .commit = std::move(confirmed_successor->witness.record),
            .canonical_snapshot = *confirmed_successor->witness.canonical_snapshot,
            .diagnostic = std::move(publication),
            .admission_spent = true,
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)),
                         admission_spent);
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)),
                         admission_spent);
    }
#endif
}

DistributedSieveWaveStoreDiagnostic DistributedSieveWaveStore::revalidate(
    DistributedSieveWaveStoreInventoryTestHooks hooks) const noexcept {
    if (const auto authority = revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    const auto inventory = inspect_namespace(state_->root_fd);
    if (!inventory) {
        return inventory.diagnostic;
    }
    if (inventory.inventory->chunk_terminal_failure_record_leaves.empty() &&
        inventory.inventory->merge_started_record_leaves.empty()) {
        auto recoverable = capture_recoverable_worker_handoff_inventory(
            state_->root_fd, state_->absolute_root, *inventory.inventory, state_->manifest,
            state_->root_identity, state_->creator_process_id);
        if (!recoverable) {
            return recoverable.diagnostic;
        }
        if (!recoverable.retained.entries.empty()) {
            auto first_witness = std::move(*recoverable.witness);
            const bool reconciliation_required =
                std::any_of(first_witness.prefixes.begin(), first_witness.prefixes.end(),
                            [](const WorkerHandoffPublicationAggregateWitness& prefix) {
                                return !prefix.prefix.canonical_terminal();
                            });
            const std::size_t retained_count = recoverable.retained.entries.size();
            if (hooks.observe_reservation_witnesses != nullptr) {
                hooks.observe_reservation_witnesses(first_witness.private_leases, hooks.context);
            }
            while (!recoverable.retained.entries.empty()) {
                recoverable.retained.entries.pop_back();
            }
            if (hooks.after_first_validation != nullptr) {
                hooks.after_first_validation(hooks.context);
            }
            if (const auto authority = revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            const auto final_inventory = inspect_namespace(state_->root_fd);
            if (!final_inventory) {
                return final_inventory.diagnostic;
            }
            auto final_recoverable = capture_recoverable_worker_handoff_inventory(
                state_->root_fd, state_->absolute_root, *final_inventory.inventory,
                state_->manifest, state_->root_identity, state_->creator_process_id);
            if (!final_recoverable) {
                return final_recoverable.diagnostic;
            }
            const bool stable = final_recoverable.witness.has_value() &&
                                *final_recoverable.witness == first_witness &&
                                final_recoverable.retained.entries.size() == retained_count;
            while (!final_recoverable.retained.entries.empty()) {
                final_recoverable.retained.entries.pop_back();
            }
            if (!stable) {
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            if (const auto authority = revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            return reconciliation_required
                       ? diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                    protocol_error())
                       : DistributedSieveWaveStoreDiagnostic{};
        }
    }
    const auto validated = validate_manifest_bound_inventory(state_->root_fd, state_->absolute_root,
                                                             *inventory.inventory, state_->manifest,
                                                             state_->creator_process_id);
    if (!validated) {
        return validated.diagnostic;
    }
    if (hooks.observe_reservation_witnesses != nullptr) {
        hooks.observe_reservation_witnesses(*validated.private_lease_witnesses, hooks.context);
    }
    if (hooks.after_first_validation != nullptr) {
        hooks.after_first_validation(hooks.context);
    }
    if (const auto authority = revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    const auto final_inventory = inspect_namespace(state_->root_fd);
    if (!final_inventory) {
        return final_inventory.diagnostic;
    }
    if (*final_inventory.inventory != *inventory.inventory) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    const auto final_validated = validate_manifest_bound_inventory(
        state_->root_fd, state_->absolute_root, *final_inventory.inventory, state_->manifest,
        state_->creator_process_id);
    if (!final_validated) {
        return final_validated.diagnostic;
    }
    if (*final_validated.base_lock_identities != *validated.base_lock_identities ||
        *final_validated.private_lease_witnesses != *validated.private_lease_witnesses ||
        *final_validated.worker_attempt_records != *validated.worker_attempt_records ||
        *final_validated.merge_started_records != *validated.merge_started_records ||
        *final_validated.chunk_terminal_failure_records !=
            *validated.chunk_terminal_failure_records ||
        *final_validated.wave_merge_commit_records != *validated.wave_merge_commit_records) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    }
    if (!process_matches(state_->creator_process_id)) {
        return process_mismatch();
    }
    return {};
#endif
}

DistributedSieveWorkerChunkInventoryResultV1
DistributedSieveWaveStore::observe_worker_chunks_v1() const noexcept {
    DistributedSieveWorkerChunkInventoryResultV1 result;
    const auto fail_with = [&](DistributedSieveWaveStoreDiagnostic failure) {
        result.chunks.clear();
        result.diagnostic = std::move(failure);
        return std::move(result);
    };

    if (const auto authority = revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(authority);
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return fail_with(
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
    try {
        auto first = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id);
        if (!first) {
            return fail_with(std::move(first.diagnostic));
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }
        auto second = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id);
        if (!second) {
            return fail_with(std::move(second.diagnostic));
        }
        if (*first.inventory != *second.inventory ||
            *first.base_lock_identities != *second.base_lock_identities ||
            *first.private_lease_witnesses != *second.private_lease_witnesses) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        auto attempts = parse_private_lease_attempt_inventory(*second.inventory, state_->manifest,
                                                              *second.base_lock_identities);
        if (!attempts) {
            return fail_with(std::move(attempts.diagnostic));
        }

        result.chunks.reserve(state_->manifest.chunks.size());
        for (const auto& chunk : state_->manifest.chunks) {
            const DistributedSieveWorkerAttemptRecordInventoryWitness* latest_attempt = nullptr;
            for (const auto& record : second.inventory->worker_attempt_records) {
                if (record.chunk_id == chunk.chunk_id &&
                    (latest_attempt == nullptr ||
                     record.attempt_ordinal > latest_attempt->attempt_ordinal)) {
                    latest_attempt = &record;
                }
            }

            const DistributedSieveWorkerHandoffInventoryWitnessV1* terminal_handoff = nullptr;
            for (const auto& lease : *second.private_lease_witnesses) {
                if (!lease.worker_handoff.has_value() ||
                    lease.worker_handoff->handoff.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (terminal_handoff != nullptr) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                terminal_handoff = &*lease.worker_handoff;
            }

            const DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1* terminal_failure =
                nullptr;
            for (const auto& failure : second.inventory->chunk_terminal_failure_records) {
                if (failure.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (terminal_failure != nullptr) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                terminal_failure = &failure;
            }

            bool attempt_namespace_present = false;
            for (const auto& attempt : *attempts.attempts) {
                if (attempt.worker_coordinate.has_value() &&
                    attempt.worker_coordinate->chunk_id == chunk.chunk_id) {
                    attempt_namespace_present = true;
                    break;
                }
            }

            DistributedSieveWorkerChunkInventoryV1 observed{.chunk = chunk};
            const bool empty = chunk.sq_begin == chunk.sq_end;
            if (empty) {
                if (latest_attempt != nullptr || terminal_failure != nullptr ||
                    terminal_handoff != nullptr || attempt_namespace_present) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                observed.state = DistributedSieveWorkerChunkDurableStateV1::empty;
            } else if (terminal_failure != nullptr) {
                if (terminal_handoff != nullptr || latest_attempt == nullptr ||
                    latest_attempt->record.self_digest !=
                        terminal_failure->record.last_attempt_digest ||
                    latest_attempt->attempt_ordinal + 1U != state_->manifest.max_worker_attempts) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                // Raw namespace shape is not publication authority. Even a
                // canonical-only prefix may have been observed immediately
                // after rename and before its parent-directory durability
                // barrier. The retained final-attempt transaction must
                // idempotently confirm it through the typed publisher.
                observed.state =
                    DistributedSieveWorkerChunkDurableStateV1::terminal_failure_pending;
                observed.latest_attempt = latest_attempt->record;
                observed.terminal_failure = terminal_failure->record;
            } else if (terminal_handoff != nullptr) {
                if (latest_attempt == nullptr ||
                    latest_attempt->record.self_digest !=
                        terminal_handoff->handoff.attempt_started_digest ||
                    latest_attempt->attempt_ordinal != terminal_handoff->handoff.attempt_ordinal) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                observed.state = DistributedSieveWorkerChunkDurableStateV1::handoff;
                observed.latest_attempt = latest_attempt->record;
                observed.handoff = terminal_handoff->handoff;
            } else if (latest_attempt != nullptr || attempt_namespace_present) {
                observed.state = DistributedSieveWorkerChunkDurableStateV1::incomplete_attempt;
                if (latest_attempt != nullptr) {
                    observed.latest_attempt = latest_attempt->record;
                }
            } else {
                observed.state = DistributedSieveWorkerChunkDurableStateV1::missing;
            }
            result.chunks.push_back(std::move(observed));
        }

        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }
        result.diagnostic = {};
        return result;
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
#endif
}

DistributedSieveWorkerHandoffAdoptionResultV1 DistributedSieveWaveStore::adopt_worker_handoff_v1(
    std::uint32_t chunk_id, DistributedSieveWorkerHandoffAdoptionTestHooksV1 hooks) const noexcept {
    return adopt_worker_handoff_impl_v1(chunk_id, nullptr, hooks);
}

DistributedSieveWorkerHandoffAdoptionResultV1
DistributedSieveWaveStore::adopt_expected_worker_handoff_v1(
    const DistributedSieveWorkerHandoffInventoryWitnessV1& expected,
    DistributedSieveWorkerHandoffAdoptionTestHooksV1 hooks) const noexcept {
    return adopt_worker_handoff_impl_v1(expected.handoff.chunk_id, &expected, hooks);
}

DistributedSieveWorkerHandoffAdoptionResultV1
DistributedSieveWaveStore::adopt_worker_handoff_impl_v1(
    std::uint32_t chunk_id, const DistributedSieveWorkerHandoffInventoryWitnessV1* expected,
    DistributedSieveWorkerHandoffAdoptionTestHooksV1 hooks) const noexcept {
    DistributedSieveWorkerHandoffAdoptionResultV1 result;
    const auto fail_with = [&](DistributedSieveWaveStoreDiagnostic failure) {
        result.adopted.reset();
        result.diagnostic = std::move(failure);
        return std::move(result);
    };

    if (state_ == nullptr || (expected != nullptr && expected->handoff.chunk_id != chunk_id) ||
        std::none_of(state_->manifest.chunks.begin(), state_->manifest.chunks.end(),
                     [&](const ChunkPlanV1& chunk) {
                         return chunk.chunk_id == chunk_id && chunk.sq_begin < chunk.sq_end;
                     })) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }
    if (const auto authority = revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(authority);
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return fail_with(
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
    try {
        auto first_inventory = inspect_namespace(state_->root_fd);
        if (!first_inventory) {
            return fail_with(std::move(first_inventory.diagnostic));
        }
        auto first_base_locks = validate_private_lease_base_lock_inventory(
            state_->root_fd, *first_inventory.inventory, state_->manifest,
            state_->creator_process_id);
        if (!first_base_locks) {
            return fail_with(std::move(first_base_locks.diagnostic));
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }
        auto inventory = inspect_namespace(state_->root_fd);
        if (!inventory) {
            return fail_with(std::move(inventory.diagnostic));
        }
        auto base_locks = validate_private_lease_base_lock_inventory(
            state_->root_fd, *inventory.inventory, state_->manifest, state_->creator_process_id);
        if (!base_locks) {
            return fail_with(std::move(base_locks.diagnostic));
        }
        if (*inventory.inventory != *first_inventory.inventory ||
            *base_locks.identities != *first_base_locks.identities) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (!inventory.inventory->chunk_terminal_failure_record_leaves.empty()) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        auto attempts = parse_private_lease_attempt_inventory(
            *inventory.inventory, state_->manifest, *base_locks.identities);
        if (!attempts) {
            return fail_with(std::move(attempts.diagnostic));
        }

        const PrivateLeaseAttemptInventory* attempt = nullptr;
        std::vector<DistributedSieveWorkerAttemptRecordInventoryWitness> attempt_witnesses;
        std::vector<AttemptStartedV1> attempt_chain;
        for (const auto& candidate : *attempts.attempts) {
            if (!candidate.worker_coordinate.has_value() ||
                candidate.worker_coordinate->chunk_id != chunk_id) {
                continue;
            }
            if (candidate.final_directory) {
                if (attempt != nullptr || candidate.reserved || candidate.reserved_pending ||
                    !candidate.owned || candidate.owned_pending || candidate.rollback_handoff ||
                    candidate.staging_directory_leaf.has_value()) {
                    return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error()));
                }
                attempt = &candidate;
            }
        }
        if (attempt == nullptr) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        attempt_witnesses.reserve(
            static_cast<std::size_t>(attempt->worker_coordinate->attempt_ordinal) + 1U);
        attempt_chain.reserve(
            static_cast<std::size_t>(attempt->worker_coordinate->attempt_ordinal) + 1U);
        for (std::uint32_t ordinal = 0; ordinal <= attempt->worker_coordinate->attempt_ordinal;
             ++ordinal) {
            const auto candidate =
                std::find_if(attempts.attempts->begin(), attempts.attempts->end(),
                             [&](const PrivateLeaseAttemptInventory& item) {
                                 return item.worker_coordinate.has_value() &&
                                        item.worker_coordinate->chunk_id == chunk_id &&
                                        item.worker_coordinate->attempt_ordinal == ordinal;
                             });
            if (candidate == attempts.attempts->end()) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
            auto loaded = load_exact_canonical_worker_attempt(
                state_->root_fd, *candidate, state_->manifest, state_->creator_process_id);
            if (!loaded) {
                return fail_with(std::move(loaded.diagnostic));
            }
            attempt_chain.push_back(loaded.witness->record);
            attempt_witnesses.push_back(std::move(*loaded.witness));
        }
        if (std::any_of(attempts.attempts->begin(), attempts.attempts->end(),
                        [&](const PrivateLeaseAttemptInventory& candidate) {
                            return candidate.worker_coordinate.has_value() &&
                                   candidate.worker_coordinate->chunk_id == chunk_id &&
                                   candidate.worker_coordinate->attempt_ordinal >
                                       attempt->worker_coordinate->attempt_ordinal;
                        })) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        auto directory = inspect_private_lease_directory_at(
            state_->root_fd, attempt->names.private_directory_leaf, state_->creator_process_id);
        if (!directory) {
            return fail_with(std::move(directory.diagnostic));
        }
        const auto& directory_entries = *directory.inventory;
        if (!directory_entries.owner || directory_entries.owner_pending ||
            directory_entries.work_package_residue || !directory_entries.corpus_index ||
            !directory_entries.corpus_data || !directory_entries.worker_handoff ||
            directory_entries.worker_handoff_pending) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const std::filesystem::path base_path =
            state_->absolute_root / attempt->names.private_directory_leaf / "corpus";
        DistributedSieveWaveStoreDiagnostic retained_lock_outcome;
        auto retained_base_lock = DistributedSievePrivateLeaseBaseLockAt::open_existing_locked(
            state_->root_fd, attempt->names.base_lock_leaf, attempt->base_lock_identity,
            state_->creator_process_id, retained_lock_outcome);
        if (retained_base_lock == nullptr ||
            retained_lock_outcome.status != DistributedSieveWaveStoreStatus::ready) {
            if (retained_lock_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                retained_lock_outcome =
                    diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                               std::make_error_code(std::errc::io_error));
            }
            return fail_with(std::move(retained_lock_outcome));
        }
        auto adoption = retained_base_lock->adopt_exact_private_handoff(base_path);
        if (!adoption.adopted() || !adoption.adoption.has_value()) {
            return fail_with(worker_handoff_inspection_failure(adoption.result));
        }

        const auto& receipt = *adoption.adoption;
        auto envelope = validate_worker_handoff_envelope(
            *attempt, state_->manifest, *directory.identity, receipt.record(),
            receipt.handoff_snapshot(), receipt.index_snapshot(), receipt.data_snapshot(),
            state_->creator_process_id);
        if (!envelope) {
            return fail_with(std::move(envelope.diagnostic));
        }
        if (expected != nullptr && *envelope.witness != *expected) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto& exact_handoff = envelope.witness->handoff;
        if (exact_handoff.chunk_id != chunk_id ||
            exact_handoff.attempt_ordinal != attempt->worker_coordinate->attempt_ordinal ||
            exact_handoff.attempt_started_digest != attempt_chain.back().self_digest ||
            exact_handoff.lease != attempt_chain.back().lease) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto chain_status = validate_worker_attempt_chain(
                state_->manifest, chunk_id, attempt_chain, &exact_handoff, nullptr);
            !chain_status) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
            failure.protocol_status = chain_status;
            return fail_with(std::move(failure));
        }

        const private_lease::PrivateLeaseRecord expected_reserved{
            .platform_id = private_lease::PLATFORM_ID,
            .phase = private_lease::PrivateLeasePhase::Reserved,
            .capability = private_lease::PrivateLeaseCapability::RollbackStagingOnly,
            .lease_id = receipt.record().lease_id,
            .base_path_digest = private_lease::frozen_path_digest(base_path),
            .parent_identity = relation_identity(state_->root_identity),
            .lock_identity = relation_identity(attempt->base_lock_identity),
        };
        const auto expected_owner = private_lease::make_private_lease_owner_record(
            expected_reserved, relation_identity(*directory.identity));
        const auto expected_owned = private_lease::make_private_lease_owned_record(
            expected_owner, relation_identity(exact_handoff.lease.owner_marker));
        const NativeIdentityV1 expected_owned_marker_identity =
            protocol_identity(receipt.record().owned_marker_identity);

        auto reader =
            std::make_unique<relation::OOCPrivateHandoffReader>(std::move(*adoption.adoption));
        if (!reader->valid() ||
            static_cast<std::uint64_t>(reader->reader().count()) != exact_handoff.relation_count ||
            exact_handoff.relation_count != exact_handoff.artifact.descriptor.relation_count) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        relation::RelationSequenceReceiptAccumulator sequence;
        relation::RelationCorpusSha256AccumulatorV1 corpus;
        for (std::size_t index = 0; index < reader->reader().count(); ++index) {
            const auto relation_row = reader->reader().read(index);
            sequence.append(relation_row);
            if (!corpus.append(relation_row)) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
        }
        const auto sequence_receipt = sequence.finish();
        const auto corpus_digest = corpus.finalize();
        if (!corpus_digest.has_value() ||
            sequence_receipt.relation_count !=
                exact_handoff.artifact.sequence_receipt.relation_count ||
            sequence_receipt.low != exact_handoff.artifact.sequence_receipt.low ||
            sequence_receipt.high != exact_handoff.artifact.sequence_receipt.high ||
            *corpus_digest != exact_handoff.artifact.corpus_sha256 ||
            sequence.count() != exact_handoff.relation_count ||
            corpus.count() != exact_handoff.relation_count) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        if (hooks.before_final_namespace_revalidation != nullptr) {
            hooks.before_final_namespace_revalidation(hooks.context);
        }

        auto final_inventory = inspect_namespace(state_->root_fd);
        if (!final_inventory) {
            return fail_with(std::move(final_inventory.diagnostic));
        }
        auto final_base_locks = validate_private_lease_base_lock_inventory(
            state_->root_fd, *final_inventory.inventory, state_->manifest,
            state_->creator_process_id);
        if (!final_base_locks || *final_inventory.inventory != *inventory.inventory ||
            *final_base_locks.identities != *base_locks.identities) {
            return fail_with(final_base_locks
                                 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(final_base_locks.diagnostic));
        }
        for (const auto& expected : attempt_witnesses) {
            const auto expected_attempt = std::find_if(
                attempts.attempts->begin(), attempts.attempts->end(),
                [&](const PrivateLeaseAttemptInventory& candidate) {
                    return candidate.worker_coordinate.has_value() &&
                           candidate.worker_coordinate->chunk_id == expected.chunk_id &&
                           candidate.worker_coordinate->attempt_ordinal == expected.attempt_ordinal;
                });
            if (expected_attempt == attempts.attempts->end()) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
            const auto exact = revalidate_exact_canonical_worker_attempt(
                state_->root_fd, *expected_attempt->worker_attempt_names, expected,
                state_->creator_process_id);
            if (exact.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(exact);
            }
        }
        if (const auto rebound = validate_private_lease_directory_binding(
                state_->root_fd, directory.directory.get(), attempt->names.private_directory_leaf,
                state_->creator_process_id, *directory.identity);
            rebound.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(rebound);
        }
        auto final_directory = inspect_private_lease_directory_at(
            state_->root_fd, attempt->names.private_directory_leaf, state_->creator_process_id);
        if (!final_directory || final_directory.identity != directory.identity ||
            !final_directory.inventory->owner || final_directory.inventory->owner_pending ||
            final_directory.inventory->work_package_residue ||
            !final_directory.inventory->corpus_index || !final_directory.inventory->corpus_data ||
            !final_directory.inventory->worker_handoff ||
            final_directory.inventory->worker_handoff_pending) {
            return fail_with(final_directory
                                 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(final_directory.diagnostic));
        }
        auto final_owner = read_private_lease_marker_at(
            final_directory.directory.get(),
            std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF), state_->creator_process_id);
        if (!final_owner || *final_owner.record != expected_owner ||
            *final_owner.identity != exact_handoff.lease.owner_marker) {
            return fail_with(final_owner
                                 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(final_owner.diagnostic));
        }
        if (const auto validated = validate_named_private_lease_marker_handle(
                final_directory.directory.get(),
                std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF).c_str(),
                final_owner.marker.get(), expected_owner, exact_handoff.lease.owner_marker,
                state_->creator_process_id);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto final_owned = read_private_lease_marker_at(state_->root_fd, attempt->names.owned_leaf,
                                                        state_->creator_process_id);
        if (!final_owned || *final_owned.record != expected_owned ||
            *final_owned.identity != expected_owned_marker_identity) {
            return fail_with(final_owned
                                 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(final_owned.diagnostic));
        }
        if (const auto validated = validate_named_private_lease_marker_handle(
                state_->root_fd, attempt->names.owned_leaf.c_str(), final_owned.marker.get(),
                expected_owned, expected_owned_marker_identity, state_->creator_process_id);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        const auto named_snapshot_matches =
            [&](std::string_view leaf, const durable_record::RecordSnapshot& snapshot) noexcept {
                const std::string owned_leaf(leaf);
                struct stat metadata{};
                return fstatat_retrying_eintr(final_directory.directory.get(), owned_leaf.c_str(),
                                              metadata) == 0 &&
                       S_ISREG(metadata.st_mode) && metadata.st_nlink == 1 &&
                       static_cast<std::uint64_t>(metadata.st_uid) ==
                           static_cast<std::uint64_t>(::geteuid()) &&
                       exact_mode(metadata, 0600) &&
                       protocol_identity(metadata) == protocol_identity(snapshot.identity) &&
                       static_cast<std::uint64_t>(metadata.st_size) == snapshot.size;
            };
        if (!named_snapshot_matches(WORKER_CORPUS_INDEX_LEAF, reader->index_snapshot()) ||
            !named_snapshot_matches(WORKER_CORPUS_DATA_LEAF, reader->data_snapshot()) ||
            !named_snapshot_matches(WORKER_HANDOFF_LEAF, reader->handoff_snapshot())) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto authority = revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }
        if (const auto validated = validate_named_private_lease_marker_handle(
                final_directory.directory.get(),
                std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF).c_str(),
                final_owner.marker.get(), expected_owner, exact_handoff.lease.owner_marker,
                state_->creator_process_id);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        if (const auto validated = validate_named_private_lease_marker_handle(
                state_->root_fd, attempt->names.owned_leaf.c_str(), final_owned.marker.get(),
                expected_owned, expected_owned_marker_identity, state_->creator_process_id);
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        DistributedSieveAdoptedWorkerChunkV1 adopted(std::shared_ptr<const void>(state_),
                                                     exact_handoff, std::move(retained_base_lock),
                                                     std::move(reader), state_->creator_process_id);
        result.adopted.emplace(std::move(adopted));
        result.diagnostic = {};
        return result;
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
#endif
}

DistributedSieveWorkerCoordinatorClaimResultV1
DistributedSieveWaveStore::claim_worker_coordinator_v1() const noexcept {
    if (state_ == nullptr) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }
    if (state_->creator_process_id == 0 || !process_matches(state_->creator_process_id)) {
        return {nullptr, process_mismatch()};
    }
    if (state_->worker_coordinator_claimed.test_and_set(std::memory_order_acq_rel)) {
        return {
            nullptr,
            diagnostic(DistributedSieveWaveStoreStatus::worker_coordinator_busy,
                       std::make_error_code(std::errc::device_or_resource_busy)),
        };
    }

    std::unique_ptr<DistributedSieveWorkerCoordinatorClaimV1> claim;
    try {
        claim.reset(new DistributedSieveWorkerCoordinatorClaimV1(
            std::shared_ptr<const void>(state_), &state_->worker_coordinator_claimed,
            state_->creator_process_id));
    } catch (const std::bad_alloc&) {
        state_->worker_coordinator_claimed.clear(std::memory_order_release);
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        state_->worker_coordinator_claimed.clear(std::memory_order_release);
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error))};
    }

    if (const auto authority = revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        claim.reset();
        return {nullptr, authority};
    }
    return {std::move(claim), {}};
}

DistributedSievePrivateLeaseRootClaimResult DistributedSieveWaveStore::claim_private_lease_root(
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs)
    const noexcept {
    return claim_private_lease_root_impl(held_worker_handoffs, false);
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::claim_private_lease_root_impl(
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs,
    bool allow_merge_raw_recovery_pending) const noexcept {
    if (state_ == nullptr) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }
    if (state_->creator_process_id == 0 || !process_matches(state_->creator_process_id)) {
        return {nullptr, process_mismatch()};
    }
    if (state_->merge_raw_recovery_open_anchor.has_value() &&
        !state_->merge_raw_recovery_completed.load(std::memory_order_acquire) &&
        !allow_merge_raw_recovery_pending) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                    protocol_error())};
    }

    std::vector<const DistributedSievePrivateLeaseBaseLockAt*> borrowed_worker_base_locks;
    try {
        if (!held_worker_handoffs.empty()) {
            const auto nonempty_chunks = static_cast<std::size_t>(std::count_if(
                state_->manifest.chunks.begin(), state_->manifest.chunks.end(),
                [](const ChunkPlanV1& chunk) { return chunk.sq_begin < chunk.sq_end; }));
            if (held_worker_handoffs.size() != nonempty_chunks) {
                return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                            invalid_argument_error())};
            }
            borrowed_worker_base_locks.reserve(held_worker_handoffs.size());
            std::size_t handoff_index = 0;
            for (const auto& chunk : state_->manifest.chunks) {
                if (chunk.sq_begin == chunk.sq_end) {
                    continue;
                }
                const auto* adopted = held_worker_handoffs[handoff_index++];
                if (adopted == nullptr || !adopted->valid() ||
                    adopted->wave_store_state_anchor_.get() !=
                        static_cast<const void*>(state_.get()) ||
                    adopted->retained_base_lock_ == nullptr || adopted->reader_ == nullptr) {
                    return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                                invalid_argument_error())};
                }
                const auto& handoff = adopted->handoff_;
                const auto names = distributed_sieve_worker_attempt_names_v1(
                    chunk.relative_artifact_stem, chunk.chunk_id, handoff.attempt_ordinal);
                const auto lock_identity =
                    protocol_identity(adopted->reader_->record().lock_identity);
                if (handoff.manifest_digest != state_->manifest.self_digest ||
                    handoff.work_digest != state_->manifest.work_sha256 ||
                    handoff.wave_id != state_->manifest.wave_id ||
                    handoff.chunk_id != chunk.chunk_id || handoff.sq_begin != chunk.sq_begin ||
                    handoff.sq_end != chunk.sq_end ||
                    handoff.attempt_ordinal >= state_->manifest.max_worker_attempts ||
                    !names.has_value() ||
                    handoff.lease.relative_stem != names->relative_lease_stem ||
                    !adopted->retained_base_lock_->matches_exact_binding(names->base_lock_leaf,
                                                                         lock_identity)) {
                    return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                                invalid_argument_error())};
                }
                borrowed_worker_base_locks.push_back(adopted->retained_base_lock_.get());
            }
        }
    } catch (const std::bad_alloc&) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error))};
    }

    if (state_->private_lease_root_action_claimed.test_and_set(std::memory_order_acq_rel)) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::private_lease_root_busy,
                                    std::make_error_code(std::errc::device_or_resource_busy))};
    }

    std::unique_ptr<DistributedSievePrivateLeaseRootClaim> claim;
    try {
        claim.reset(new DistributedSievePrivateLeaseRootClaim(
            state_, std::move(borrowed_worker_base_locks)));
    } catch (const std::bad_alloc&) {
        state_->private_lease_root_action_claimed.clear(std::memory_order_release);
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        state_->private_lease_root_action_claimed.clear(std::memory_order_release);
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error))};
    }

    const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
        std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
            claim->borrowed_worker_base_locks_.data(), claim->borrowed_worker_base_locks_.size())};
    auto merge_prepared_prefix = classify_merge_prepared_publication_prefix_v1(
        state_->root_fd, state_->absolute_root, state_->manifest, state_->root_identity,
        state_->creator_process_id, held_inventory);
    if (!merge_prepared_prefix) {
        return {nullptr, merge_prepared_prefix.diagnostic};
    }
    if (merge_prepared_prefix.prefix_present()) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                    protocol_error())};
    }

    auto validated = claim->revalidate();
    if (validated.status != DistributedSieveWaveStoreStatus::ready) {
        return {nullptr, std::move(validated)};
    }
    return {std::move(claim), {}};
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::create_worker_attempt_private_lease_root(
    std::uint32_t chunk_id, std::uint32_t attempt_ordinal,
    DistributedSievePrivateLeaseBaseLockTestHooks hooks) const noexcept {
    return claim_worker_attempt_private_lease_root(chunk_id, attempt_ordinal,
                                                   AttemptBaseLockExpectation::absent, hooks);
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::open_worker_attempt_private_lease_root(
    std::uint32_t chunk_id, std::uint32_t attempt_ordinal,
    DistributedSievePrivateLeaseBaseLockTestHooks hooks) const noexcept {
    return claim_worker_attempt_private_lease_root(chunk_id, attempt_ordinal,
                                                   AttemptBaseLockExpectation::present, hooks);
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::create_merge_generation_private_lease_root(
    std::uint32_t merge_attempt_ordinal,
    DistributedSievePrivateLeaseBaseLockTestHooks hooks) const noexcept {
    return claim_merge_generation_private_lease_root(merge_attempt_ordinal,
                                                     AttemptBaseLockExpectation::absent, hooks);
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::open_merge_generation_private_lease_root(
    std::uint32_t merge_attempt_ordinal,
    DistributedSievePrivateLeaseBaseLockTestHooks hooks) const noexcept {
    return claim_merge_generation_private_lease_root(merge_attempt_ordinal,
                                                     AttemptBaseLockExpectation::present, hooks);
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::claim_worker_attempt_private_lease_root(
    std::uint32_t chunk_id, std::uint32_t attempt_ordinal, AttemptBaseLockExpectation expectation,
    DistributedSievePrivateLeaseBaseLockTestHooks hooks) const noexcept {
    if (state_ == nullptr) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }
    if (state_->creator_process_id == 0 || !process_matches(state_->creator_process_id)) {
        return {nullptr, process_mismatch()};
    }

    try {
        const ChunkPlanV1* matched_chunk = nullptr;
        if (attempt_ordinal < state_->manifest.max_worker_attempts) {
            for (const auto& chunk : state_->manifest.chunks) {
                if (chunk.chunk_id == chunk_id) {
                    matched_chunk = &chunk;
                    break;
                }
            }
        }
        if (matched_chunk == nullptr || matched_chunk->sq_begin >= matched_chunk->sq_end) {
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error())};
        }
        auto names = distributed_sieve_worker_attempt_names_v1(
            matched_chunk->relative_artifact_stem, chunk_id, attempt_ordinal);
        if (!names.has_value()) {
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error())};
        }
        std::string target_leaf = names->base_lock_leaf;

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)expectation;
        (void)hooks;
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                                    unsupported_error())};
#else
        auto claimed = claim_private_lease_root();
        if (!claimed || claimed.claim == nullptr) {
            return claimed;
        }
        auto& claim = *claimed.claim;

        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }
        auto before = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id);
        if (!before) {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, authority};
            }
            return {nullptr, std::move(before.diagnostic)};
        }
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }

        if (!before.inventory->chunk_terminal_failure_records.empty()) {
            const auto& terminal = before.inventory->chunk_terminal_failure_records.front();
            const bool recoverable_prefix =
                terminal.pending_snapshot.has_value() || terminal.canonical_snapshot.has_value();
            const bool exact_final_recovery = expectation == AttemptBaseLockExpectation::present &&
                                              recoverable_prefix && terminal.chunk_id == chunk_id &&
                                              static_cast<std::uint64_t>(attempt_ordinal) + 1U ==
                                                  state_->manifest.max_worker_attempts;
            if (!exact_final_recovery) {
                return {
                    nullptr,
                    diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                               protocol_error()),
                };
            }
        }

        const auto target_position =
            std::lower_bound(before.inventory->private_lease_base_lock_leaves.begin(),
                             before.inventory->private_lease_base_lock_leaves.end(), target_leaf);
        const std::size_t target_index = static_cast<std::size_t>(std::distance(
            before.inventory->private_lease_base_lock_leaves.begin(), target_position));
        const bool target_present =
            target_position != before.inventory->private_lease_base_lock_leaves.end() &&
            *target_position == target_leaf;
        if ((expectation == AttemptBaseLockExpectation::absent && target_present) ||
            (expectation == AttemptBaseLockExpectation::present && !target_present)) {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, authority};
            }
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        protocol_error())};
        }
        if (expectation == AttemptBaseLockExpectation::absent) {
            for (const auto& lease : *before.private_lease_witnesses) {
                if (lease.worker_handoff.has_value() &&
                    lease.worker_handoff->handoff.chunk_id == chunk_id) {
                    return {
                        nullptr,
                        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error()),
                    };
                }
                if (!lease.work_package_residue.has_value()) {
                    continue;
                }
                if (!lease.base_lock_leaf.ends_with(
                        DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX)) {
                    return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                                protocol_error())};
                }
                const auto relative_stem =
                    std::string_view(lease.base_lock_leaf)
                        .substr(0, lease.base_lock_leaf.size() -
                                       DISTRIBUTED_SIEVE_PRIVATE_LEASE_BASE_LOCK_SUFFIX.size());
                const auto residue_names =
                    manifest_attempt_names_from_relative_stem(state_->manifest, relative_stem);
                const auto residue_coordinate =
                    residue_names.has_value() ? parse_distributed_sieve_worker_attempt_leaf_v1(
                                                    residue_names->canonical_record_leaf)
                                              : std::nullopt;
                if (!residue_names.has_value() || !residue_coordinate.has_value() ||
                    residue_coordinate->pending) {
                    return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                                protocol_error())};
                }
                if (residue_coordinate->chunk_id == chunk_id) {
                    return {
                        nullptr,
                        diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                   protocol_error()),
                    };
                }
            }
        }

        NamespaceInventory expected_successor_inventory = *before.inventory;
        std::vector<NativeIdentityV1> expected_successor_identities = *before.base_lock_identities;
        std::vector<PrivateLeaseReservationWitness> expected_successor_private_leases =
            *before.private_lease_witnesses;
        std::optional<NativeIdentityV1> expected_existing_identity;
        if (expectation == AttemptBaseLockExpectation::absent) {
            expected_successor_inventory.private_lease_base_lock_leaves.insert(
                expected_successor_inventory.private_lease_base_lock_leaves.begin() +
                    static_cast<std::ptrdiff_t>(target_index),
                target_leaf);
            expected_successor_identities.insert(expected_successor_identities.begin() +
                                                     static_cast<std::ptrdiff_t>(target_index),
                                                 NativeIdentityV1{});
            expected_successor_private_leases.insert(expected_successor_private_leases.begin() +
                                                         static_cast<std::ptrdiff_t>(target_index),
                                                     PrivateLeaseReservationWitness{
                                                         .base_lock_leaf = target_leaf,
                                                     });
        } else {
            expected_existing_identity = before.base_lock_identities->at(target_index);
        }
        claim.worker_attempt_names_.emplace(std::move(*names));
        const auto expected_successor_matches =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                return observed && *observed.inventory == expected_successor_inventory &&
                       *observed.base_lock_identities == expected_successor_identities &&
                       *observed.private_lease_witnesses == expected_successor_private_leases;
            };
        bool terminal_transition_refreshed = false;
        const auto refresh_exact_terminal_transition =
            [&](const ManifestBoundInventoryWitnessResult& observed) {
                if (terminal_transition_refreshed ||
                    expectation != AttemptBaseLockExpectation::present ||
                    !exact_worker_attempt_terminal_transition(
                        before, observed, state_->manifest, *claim.worker_attempt_names_, chunk_id,
                        attempt_ordinal, target_index)) {
                    return false;
                }
                expected_successor_inventory = *observed.inventory;
                expected_successor_identities = *observed.base_lock_identities;
                expected_successor_private_leases = *observed.private_lease_witnesses;
                expected_existing_identity = observed.base_lock_identities->at(target_index);
                terminal_transition_refreshed = true;
                return true;
            };

        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.after_initial_phase_validation, hooks.context, state_->creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, hooked};
        }
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }

        // A terminal worker handoff is published while the worker owns its
        // attempt BaseLock.  Creating attempt N must therefore join that same
        // serialization domain before its final inventory witness.  Always
        // acquire predecessor locks in increasing attempt order, and only with
        // nonblocking flock, so every creator follows the fixed order
        //
        //   same-State root claim -> attempts [0, N) -> attempt N.
        //
        // Holding the root claim while probing a busy predecessor cannot form
        // a wait cycle: the probe never waits, and a worker publishing a
        // handoff acquires neither the root claim nor another attempt lock.
        std::vector<std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>> predecessor_base_locks;
        if (expectation == AttemptBaseLockExpectation::absent) {
            predecessor_base_locks.reserve(attempt_ordinal);
            for (std::uint32_t predecessor_ordinal = 0; predecessor_ordinal < attempt_ordinal;
                 ++predecessor_ordinal) {
                const auto predecessor_names = distributed_sieve_worker_attempt_names_v1(
                    matched_chunk->relative_artifact_stem, chunk_id, predecessor_ordinal);
                if (!predecessor_names.has_value()) {
                    return {
                        nullptr,
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   protocol_error()),
                    };
                }
                const auto predecessor_position =
                    std::lower_bound(before.inventory->private_lease_base_lock_leaves.begin(),
                                     before.inventory->private_lease_base_lock_leaves.end(),
                                     predecessor_names->base_lock_leaf);
                if (predecessor_position ==
                        before.inventory->private_lease_base_lock_leaves.end() ||
                    *predecessor_position != predecessor_names->base_lock_leaf) {
                    continue;
                }
                const std::size_t predecessor_index = static_cast<std::size_t>(
                    std::distance(before.inventory->private_lease_base_lock_leaves.begin(),
                                  predecessor_position));
                DistributedSieveWaveStoreDiagnostic predecessor_outcome;
                auto predecessor = DistributedSievePrivateLeaseBaseLockAt::open_existing_locked(
                    state_->root_fd, predecessor_names->base_lock_leaf,
                    before.base_lock_identities->at(predecessor_index), state_->creator_process_id,
                    predecessor_outcome);
                if (predecessor == nullptr ||
                    predecessor_outcome.status != DistributedSieveWaveStoreStatus::ready) {
                    if (const auto authority = claim.revalidate_authority();
                        authority.status != DistributedSieveWaveStoreStatus::ready) {
                        return {nullptr, authority};
                    }
                    if (predecessor_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                        predecessor_outcome =
                            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                       std::make_error_code(std::errc::io_error));
                    }
                    return {nullptr, std::move(predecessor_outcome)};
                }
                predecessor_base_locks.emplace_back(std::move(predecessor));
            }
        }
        std::vector<const DistributedSievePrivateLeaseBaseLockAt*> held_base_locks;
        held_base_locks.reserve(claim.borrowed_worker_base_locks_.size() +
                                predecessor_base_locks.size() + 1U);
        held_base_locks.insert(held_base_locks.end(), claim.borrowed_worker_base_locks_.begin(),
                               claim.borrowed_worker_base_locks_.end());
        for (const auto& predecessor : predecessor_base_locks) {
            held_base_locks.push_back(predecessor.get());
        }
        const auto held_inventory = [&]() noexcept {
            return HeldPrivateLeaseBaseLockInventoryV1{
                std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                    held_base_locks.data(), held_base_locks.size()),
            };
        };

        auto immediately_before = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            held_inventory());
        if (!immediately_before) {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, authority};
            }
            return {nullptr, std::move(immediately_before.diagnostic)};
        }
        const bool immediately_matches_initial =
            *immediately_before.inventory == *before.inventory &&
            *immediately_before.base_lock_identities == *before.base_lock_identities &&
            *immediately_before.private_lease_witnesses == *before.private_lease_witnesses;
        if (!immediately_matches_initial &&
            !refresh_exact_terminal_transition(immediately_before)) {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, authority};
            }
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        protocol_error())};
        }
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }
        for (const auto& predecessor : predecessor_base_locks) {
            if (const auto predecessor_validated = predecessor->revalidate();
                predecessor_validated.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return {nullptr, authority};
                }
                return {nullptr, predecessor_validated};
            }
        }
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }

        DistributedSieveWaveStoreDiagnostic target_outcome;
        std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> target;
        if (expectation == AttemptBaseLockExpectation::absent) {
            target = DistributedSievePrivateLeaseBaseLockAt::create_new_locked(
                state_->root_fd, std::move(target_leaf), state_->creator_process_id,
                target_outcome);
        } else {
            target = DistributedSievePrivateLeaseBaseLockAt::open_existing_locked(
                state_->root_fd, target_leaf, *expected_existing_identity,
                state_->creator_process_id, target_outcome);
        }
        if (target == nullptr || target_outcome.status != DistributedSieveWaveStoreStatus::ready) {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, authority};
            }
            if (target_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                target_outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            std::make_error_code(std::errc::io_error));
            }
            return {nullptr, std::move(target_outcome)};
        }

        if (expectation == AttemptBaseLockExpectation::absent) {
            expected_successor_identities[target_index] = target->identity();
        }
        held_base_locks.push_back(target.get());

        bool target_revalidation_boundary_offered = false;
        const auto revalidate_higher_priority_bindings =
            [&](bool offer_target_boundary = true) noexcept -> DistributedSieveWaveStoreDiagnostic {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            for (const auto& predecessor : predecessor_base_locks) {
                if (const auto predecessor_validated = predecessor->revalidate();
                    predecessor_validated.status != DistributedSieveWaveStoreStatus::ready) {
                    if (const auto authority = claim.revalidate_authority();
                        authority.status != DistributedSieveWaveStoreStatus::ready) {
                        return authority;
                    }
                    return predecessor_validated;
                }
            }
            if (const auto target_validated = target->revalidate();
                target_validated.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return target_validated;
            }
            if (offer_target_boundary && !target_revalidation_boundary_offered) {
                target_revalidation_boundary_offered = true;
                if (const auto hooked = invoke_private_lease_base_lock_hook(
                        hooks.after_target_revalidation, hooks.context, state_->creator_process_id);
                    hooked.status != DistributedSieveWaveStoreStatus::ready) {
                    return hooked;
                }
            }
            return claim.revalidate_authority();
        };

        if (const auto bindings = revalidate_higher_priority_bindings(false);
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }
        auto held_target = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            held_inventory());
        if (!held_target) {
            if (const auto bindings = revalidate_higher_priority_bindings(false);
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, bindings};
            }
            return {nullptr, std::move(held_target.diagnostic)};
        }
        if (!expected_successor_matches(held_target) &&
            !refresh_exact_terminal_transition(held_target)) {
            if (const auto bindings = revalidate_higher_priority_bindings(false);
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, bindings};
            }
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        protocol_error())};
        }
        if (const auto bindings = revalidate_higher_priority_bindings(false);
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }

        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.after_target_lock_acquired, hooks.context, state_->creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, hooked};
        }
        if (const auto bindings = revalidate_higher_priority_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }
        auto held_target_confirmed = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            held_inventory());
        if (!held_target_confirmed || !expected_successor_matches(held_target_confirmed)) {
            if (const auto bindings = revalidate_higher_priority_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, bindings};
            }
            return {
                nullptr,
                held_target_confirmed
                    ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                 protocol_error())
                    : std::move(held_target_confirmed.diagnostic),
            };
        }
        if (const auto bindings = revalidate_higher_priority_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }

        const auto revalidate_closed_successor =
            [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
            if (const auto bindings = revalidate_higher_priority_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }

            auto first = capture_manifest_bound_inventory_witness(
                state_->root_fd, state_->manifest, state_->absolute_root,
                state_->creator_process_id, held_inventory());
            if (!first) {
                if (const auto bindings = revalidate_higher_priority_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return bindings;
                }
                return std::move(first.diagnostic);
            }
            if (*first.inventory != expected_successor_inventory ||
                *first.base_lock_identities != expected_successor_identities ||
                *first.private_lease_witnesses != expected_successor_private_leases) {
                if (const auto bindings = revalidate_higher_priority_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return bindings;
                }
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            if (const auto bindings = revalidate_higher_priority_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }

            auto confirmed = capture_manifest_bound_inventory_witness(
                state_->root_fd, state_->manifest, state_->absolute_root,
                state_->creator_process_id, held_inventory());
            if (!confirmed) {
                if (const auto bindings = revalidate_higher_priority_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return bindings;
                }
                return std::move(confirmed.diagnostic);
            }
            if (*confirmed.inventory != *first.inventory ||
                *confirmed.base_lock_identities != *first.base_lock_identities ||
                *confirmed.private_lease_witnesses != *first.private_lease_witnesses) {
                if (const auto bindings = revalidate_higher_priority_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return bindings;
                }
                return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                  protocol_error());
            }
            return revalidate_higher_priority_bindings();
        };

        if (const auto validated = revalidate_closed_successor();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, validated};
        }
        if (const auto synchronized = target->synchronize(hooks);
            synchronized.status != DistributedSieveWaveStoreStatus::ready) {
            if (const auto validated = revalidate_closed_successor();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, validated};
            }
            return {nullptr, synchronized};
        }
        if (const auto validated = revalidate_closed_successor();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, validated};
        }

        claim.expected_private_lease_base_lock_leaves_.emplace(
            std::move(expected_successor_inventory.private_lease_base_lock_leaves));
        claim.expected_private_lease_base_lock_identities_.emplace(
            std::move(expected_successor_identities));
        claim.expected_private_lease_reservation_witnesses_.emplace(
            std::move(expected_successor_private_leases));
        claim.expected_worker_attempt_record_witnesses_.emplace(
            std::move(expected_successor_inventory.worker_attempt_records));
        claim.expected_merge_started_record_witnesses_.emplace(
            std::move(expected_successor_inventory.merge_started_records));
        claim.expected_chunk_terminal_failure_record_witnesses_.emplace(
            std::move(expected_successor_inventory.chunk_terminal_failure_records));
        claim.base_lock_acquisition_ =
            expectation == AttemptBaseLockExpectation::absent
                ? DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::CreatedNew
                : DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting;
        claim.predecessor_base_locks_ = std::move(predecessor_base_locks);
        claim.base_lock_at_ = std::move(target);
        if (const auto fully_validated = claim.revalidate();
            fully_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, fully_validated};
        }
        return claimed;
#endif
    } catch (const std::bad_alloc&) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error))};
    }
}

DistributedSievePrivateLeaseRootClaimResult
DistributedSieveWaveStore::claim_merge_generation_private_lease_root(
    std::uint32_t merge_attempt_ordinal, AttemptBaseLockExpectation expectation,
    DistributedSievePrivateLeaseBaseLockTestHooks hooks,
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs,
    bool allow_merge_raw_recovery_pending) const noexcept {
    if (state_ == nullptr) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }
    if (state_->creator_process_id == 0 || !process_matches(state_->creator_process_id)) {
        return {nullptr, process_mismatch()};
    }
    if (merge_attempt_ordinal >= state_->manifest.max_merge_build_attempts) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }

    try {
        auto names = distributed_sieve_merge_generation_names_v1(merge_attempt_ordinal);
        if (!names.has_value()) {
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error())};
        }
        std::string target_leaf = names->base_lock_leaf;

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)expectation;
        (void)hooks;
        (void)held_worker_handoffs;
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported,
                                    unsupported_error())};
#else
        auto claimed =
            claim_private_lease_root_impl(held_worker_handoffs, allow_merge_raw_recovery_pending);
        if (!claimed || claimed.claim == nullptr) {
            return claimed;
        }
        auto& claim = *claimed.claim;
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }
        const HeldPrivateLeaseBaseLockInventoryV1 borrowed_inventory{
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                claim.borrowed_worker_base_locks_.data(),
                claim.borrowed_worker_base_locks_.size())};
        auto before = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            borrowed_inventory);
        if (!before) {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, authority};
            }
            return {nullptr, std::move(before.diagnostic)};
        }
        if (!before.inventory->chunk_terminal_failure_records.empty()) {
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        protocol_error())};
        }
        for (const auto& start : before.inventory->merge_started_records) {
            const bool selected_existing_start =
                expectation == AttemptBaseLockExpectation::present &&
                start.merge_attempt_ordinal == merge_attempt_ordinal;
            if (!selected_existing_start &&
                (!start.canonical_snapshot.has_value() || start.pending_snapshot.has_value())) {
                return {nullptr,
                        diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                   protocol_error())};
            }
        }
        const std::size_t started_count = before.inventory->merge_started_records.size();
        if ((expectation == AttemptBaseLockExpectation::absent &&
             merge_attempt_ordinal != started_count) ||
            (expectation == AttemptBaseLockExpectation::present &&
             !(merge_attempt_ordinal == started_count ||
               static_cast<std::size_t>(merge_attempt_ordinal) + 1U == started_count))) {
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        protocol_error())};
        }

        const auto target_position =
            std::lower_bound(before.inventory->private_lease_base_lock_leaves.begin(),
                             before.inventory->private_lease_base_lock_leaves.end(), target_leaf);
        const std::size_t target_index = static_cast<std::size_t>(std::distance(
            before.inventory->private_lease_base_lock_leaves.begin(), target_position));
        const bool target_present =
            target_position != before.inventory->private_lease_base_lock_leaves.end() &&
            *target_position == target_leaf;
        if ((expectation == AttemptBaseLockExpectation::absent && target_present) ||
            (expectation == AttemptBaseLockExpectation::present && !target_present)) {
            return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                        protocol_error())};
        }
        if (expectation == AttemptBaseLockExpectation::absent) {
            for (const auto& start : before.inventory->merge_started_records) {
                const auto predecessor_names =
                    distributed_sieve_merge_generation_names_v1(start.merge_attempt_ordinal);
                if (!predecessor_names.has_value()) {
                    return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error())};
                }
                const auto predecessor_position =
                    std::lower_bound(before.inventory->private_lease_base_lock_leaves.begin(),
                                     before.inventory->private_lease_base_lock_leaves.end(),
                                     predecessor_names->base_lock_leaf);
                if (predecessor_position ==
                        before.inventory->private_lease_base_lock_leaves.end() ||
                    *predecessor_position != predecessor_names->base_lock_leaf) {
                    return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error())};
                }
                const auto predecessor_index = static_cast<std::size_t>(
                    std::distance(before.inventory->private_lease_base_lock_leaves.begin(),
                                  predecessor_position));
                if (before.private_lease_witnesses->at(predecessor_index).boundary !=
                    DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
                    return {nullptr,
                            diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                       protocol_error())};
                }
            }
        }
        if (expectation == AttemptBaseLockExpectation::present &&
            static_cast<std::size_t>(merge_attempt_ordinal) + 1U == started_count &&
            started_count < state_->manifest.max_merge_build_attempts) {
            const auto successor_names = distributed_sieve_merge_generation_names_v1(
                static_cast<std::uint32_t>(started_count));
            if (!successor_names.has_value()) {
                return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error())};
            }
            const auto successor =
                std::lower_bound(before.inventory->private_lease_base_lock_leaves.begin(),
                                 before.inventory->private_lease_base_lock_leaves.end(),
                                 successor_names->base_lock_leaf);
            if (successor != before.inventory->private_lease_base_lock_leaves.end() &&
                *successor == successor_names->base_lock_leaf) {
                return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error())};
            }
        }

        NamespaceInventory expected_successor_inventory = *before.inventory;
        std::vector<NativeIdentityV1> expected_successor_identities = *before.base_lock_identities;
        std::vector<PrivateLeaseReservationWitness> expected_successor_private_leases =
            *before.private_lease_witnesses;
        std::optional<NativeIdentityV1> expected_existing_identity;
        if (expectation == AttemptBaseLockExpectation::absent) {
            expected_successor_inventory.private_lease_base_lock_leaves.insert(
                expected_successor_inventory.private_lease_base_lock_leaves.begin() +
                    static_cast<std::ptrdiff_t>(target_index),
                target_leaf);
            expected_successor_identities.insert(expected_successor_identities.begin() +
                                                     static_cast<std::ptrdiff_t>(target_index),
                                                 NativeIdentityV1{});
            expected_successor_private_leases.insert(
                expected_successor_private_leases.begin() +
                    static_cast<std::ptrdiff_t>(target_index),
                PrivateLeaseReservationWitness{.base_lock_leaf = target_leaf});
        } else {
            expected_existing_identity = before.base_lock_identities->at(target_index);
        }
        claim.merge_generation_names_.emplace(std::move(*names));
        const auto expected_successor_matches =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                return observed && *observed.inventory == expected_successor_inventory &&
                       *observed.base_lock_identities == expected_successor_identities &&
                       *observed.private_lease_witnesses == expected_successor_private_leases;
            };

        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.after_initial_phase_validation, hooks.context, state_->creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, hooked};
        }
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, authority};
        }

        std::vector<std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>> predecessor_base_locks;
        predecessor_base_locks.reserve(merge_attempt_ordinal);
        for (std::uint32_t predecessor_ordinal = 0; predecessor_ordinal < merge_attempt_ordinal;
             ++predecessor_ordinal) {
            const auto predecessor_names =
                distributed_sieve_merge_generation_names_v1(predecessor_ordinal);
            if (!predecessor_names.has_value()) {
                return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            protocol_error())};
            }
            const auto predecessor_position =
                std::lower_bound(before.inventory->private_lease_base_lock_leaves.begin(),
                                 before.inventory->private_lease_base_lock_leaves.end(),
                                 predecessor_names->base_lock_leaf);
            if (predecessor_position == before.inventory->private_lease_base_lock_leaves.end() ||
                *predecessor_position != predecessor_names->base_lock_leaf) {
                return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error())};
            }
            const std::size_t predecessor_index = static_cast<std::size_t>(std::distance(
                before.inventory->private_lease_base_lock_leaves.begin(), predecessor_position));
            DistributedSieveWaveStoreDiagnostic predecessor_outcome;
            auto predecessor = DistributedSievePrivateLeaseBaseLockAt::open_existing_locked(
                state_->root_fd, predecessor_names->base_lock_leaf,
                before.base_lock_identities->at(predecessor_index), state_->creator_process_id,
                predecessor_outcome);
            if (predecessor == nullptr ||
                predecessor_outcome.status != DistributedSieveWaveStoreStatus::ready) {
                if (predecessor_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                    predecessor_outcome =
                        diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                   std::make_error_code(std::errc::io_error));
                }
                return {nullptr, std::move(predecessor_outcome)};
            }
            predecessor_base_locks.emplace_back(std::move(predecessor));
        }

        std::vector<const DistributedSievePrivateLeaseBaseLockAt*> held_base_locks;
        held_base_locks.reserve(claim.borrowed_worker_base_locks_.size() +
                                predecessor_base_locks.size() + 1U);
        held_base_locks.insert(held_base_locks.end(), claim.borrowed_worker_base_locks_.begin(),
                               claim.borrowed_worker_base_locks_.end());
        for (const auto& predecessor : predecessor_base_locks) {
            held_base_locks.push_back(predecessor.get());
        }
        const auto held_inventory = [&]() noexcept {
            return HeldPrivateLeaseBaseLockInventoryV1{
                std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                    held_base_locks.data(), held_base_locks.size()),
            };
        };

        auto immediately_before = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            held_inventory());
        if (!immediately_before || *immediately_before.inventory != *before.inventory ||
            *immediately_before.base_lock_identities != *before.base_lock_identities ||
            *immediately_before.private_lease_witnesses != *before.private_lease_witnesses) {
            return {
                nullptr,
                immediately_before ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                                protocol_error())
                                   : std::move(immediately_before.diagnostic),
            };
        }
        for (const auto& predecessor : predecessor_base_locks) {
            if (const auto validated = predecessor->revalidate();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return {nullptr, validated};
            }
        }

        DistributedSieveWaveStoreDiagnostic target_outcome;
        std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> target;
        if (expectation == AttemptBaseLockExpectation::absent) {
            target = DistributedSievePrivateLeaseBaseLockAt::create_new_locked(
                state_->root_fd, std::move(target_leaf), state_->creator_process_id,
                target_outcome);
        } else {
            target = DistributedSievePrivateLeaseBaseLockAt::open_existing_locked(
                state_->root_fd, target_leaf, *expected_existing_identity,
                state_->creator_process_id, target_outcome);
        }
        if (target == nullptr || target_outcome.status != DistributedSieveWaveStoreStatus::ready) {
            if (target_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                target_outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            std::make_error_code(std::errc::io_error));
            }
            return {nullptr, std::move(target_outcome)};
        }
        if (expectation == AttemptBaseLockExpectation::absent) {
            expected_successor_identities[target_index] = target->identity();
        }
        held_base_locks.push_back(target.get());

        const auto revalidate_bindings = [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            for (const auto& predecessor : predecessor_base_locks) {
                if (const auto validated = predecessor->revalidate();
                    validated.status != DistributedSieveWaveStoreStatus::ready) {
                    return validated;
                }
            }
            return target->revalidate();
        };
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }
        auto first = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            held_inventory());
        if (!expected_successor_matches(first)) {
            return {
                nullptr,
                first ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                   protocol_error())
                      : std::move(first.diagnostic),
            };
        }
        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.after_target_lock_acquired, hooks.context, state_->creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, hooked};
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }
        auto confirmed = capture_manifest_bound_inventory_witness(
            state_->root_fd, state_->manifest, state_->absolute_root, state_->creator_process_id,
            held_inventory());
        if (!confirmed || *confirmed.inventory != *first.inventory ||
            *confirmed.base_lock_identities != *first.base_lock_identities ||
            *confirmed.private_lease_witnesses != *first.private_lease_witnesses) {
            return {
                nullptr,
                confirmed ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                       protocol_error())
                          : std::move(confirmed.diagnostic),
            };
        }
        if (const auto synchronized = target->synchronize(hooks);
            synchronized.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, synchronized};
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, bindings};
        }

        claim.expected_private_lease_base_lock_leaves_.emplace(
            std::move(expected_successor_inventory.private_lease_base_lock_leaves));
        claim.expected_private_lease_base_lock_identities_.emplace(
            std::move(expected_successor_identities));
        claim.expected_private_lease_reservation_witnesses_.emplace(
            std::move(expected_successor_private_leases));
        claim.expected_worker_attempt_record_witnesses_.emplace(
            std::move(expected_successor_inventory.worker_attempt_records));
        claim.expected_merge_started_record_witnesses_.emplace(
            std::move(expected_successor_inventory.merge_started_records));
        claim.expected_chunk_terminal_failure_record_witnesses_.emplace(
            std::move(expected_successor_inventory.chunk_terminal_failure_records));
        claim.base_lock_acquisition_ =
            expectation == AttemptBaseLockExpectation::absent
                ? DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::CreatedNew
                : DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting;
        claim.predecessor_base_locks_ = std::move(predecessor_base_locks);
        claim.base_lock_at_ = std::move(target);
        if (const auto fully_validated = claim.revalidate();
            fully_validated.status != DistributedSieveWaveStoreStatus::ready) {
            return {nullptr, fully_validated};
        }
        return claimed;
#endif
    } catch (const std::bad_alloc&) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory))};
    } catch (...) {
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error))};
    }
}

DistributedSievePrivateLeaseReservationReceipt::DistributedSievePrivateLeaseReservationReceipt(
    std::shared_ptr<const DistributedSieveWaveStore::State> wave_store_state,
    DistributedSieveWorkerAttemptNamesV1 worker_attempt_names, NativeIdentityV1 base_lock_identity,
    DistributedSievePrivateLeaseReservationInventoryWitness final_witness,
    std::uint64_t creator_process_id) noexcept
    : wave_store_state_(std::move(wave_store_state)),
      worker_attempt_names_(std::move(worker_attempt_names)),
      base_lock_identity_(base_lock_identity), final_witness_(std::move(final_witness)),
      creator_process_id_(creator_process_id) {}

bool DistributedSievePrivateLeaseReservationReceipt::owned_by_current_process() const noexcept {
    return wave_store_state_ != nullptr && creator_process_id_ != 0 &&
           process_matches(creator_process_id_);
}

DistributedSieveWaveStoreDiagnostic DistributedSievePrivateLeaseReservationReceipt::revalidate(
    DistributedSievePrivateLeaseReservationReceiptTestHooks hooks) const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    if (final_witness_.base_lock_leaf != worker_attempt_names_.base_lock_leaf ||
        final_witness_.boundary !=
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
        final_witness_.lease_id == std::array<std::uint64_t, 2>{} ||
        !final_witness_.reserved_marker_identity.has_value() ||
        !final_witness_.directory_identity.has_value() ||
        !final_witness_.owner_marker_identity.has_value() ||
        !final_witness_.owned_marker_identity.has_value() ||
        final_witness_.work_package_residue.has_value() ||
        final_witness_.worker_handoff.has_value() || final_witness_.merge_prepared.has_value() ||
        nil_identity(base_lock_identity_)) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    DistributedSieveWaveStore view(wave_store_state_);
    const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
        if (const auto authority = view.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        return lower;
    };
    const auto target_matches = [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
        if (!observed ||
            observed.inventory->private_lease_base_lock_leaves.size() !=
                observed.base_lock_identities->size() ||
            observed.inventory->private_lease_base_lock_leaves.size() !=
                observed.private_lease_witnesses->size()) {
            return false;
        }
        const auto position =
            std::lower_bound(observed.inventory->private_lease_base_lock_leaves.begin(),
                             observed.inventory->private_lease_base_lock_leaves.end(),
                             final_witness_.base_lock_leaf);
        if (position == observed.inventory->private_lease_base_lock_leaves.end() ||
            *position != final_witness_.base_lock_leaf) {
            return false;
        }
        const auto index = static_cast<std::size_t>(
            std::distance(observed.inventory->private_lease_base_lock_leaves.begin(), position));
        return observed.base_lock_identities->at(index) == base_lock_identity_ &&
               observed.private_lease_witnesses->at(index) == final_witness_;
    };

    if (const auto authority = view.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    auto first = capture_manifest_bound_inventory_witness(
        wave_store_state_->root_fd, wave_store_state_->manifest, wave_store_state_->absolute_root,
        creator_process_id_);
    if (!first) {
        if (const auto authority = view.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        return std::move(first.diagnostic);
    }
    if (!target_matches(first)) {
        return reject_lower_priority(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto authority = view.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    if (!process_matches(creator_process_id_)) {
        return process_mismatch();
    }
    if (hooks.after_first_target_validation != nullptr) {
        hooks.after_first_target_validation(hooks.context);
    }
    if (!process_matches(creator_process_id_)) {
        return process_mismatch();
    }
    if (const auto authority = view.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    auto confirmed = capture_manifest_bound_inventory_witness(
        wave_store_state_->root_fd, wave_store_state_->manifest, wave_store_state_->absolute_root,
        creator_process_id_);
    if (!confirmed) {
        if (const auto authority = view.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        return std::move(confirmed.diagnostic);
    }
    if (!target_matches(confirmed)) {
        return reject_lower_priority(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto authority = view.revalidate_authority();
        authority.status != DistributedSieveWaveStoreStatus::ready) {
        return authority;
    }
    return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{} : process_mismatch();
#endif
}

std::string_view
DistributedSievePrivateLeaseReservationReceipt::relative_lease_stem() const noexcept {
    return worker_attempt_names_.relative_lease_stem;
}

const std::array<std::uint64_t, 2>&
DistributedSievePrivateLeaseReservationReceipt::lease_id() const noexcept {
    return final_witness_.lease_id;
}

const NativeIdentityV1&
DistributedSievePrivateLeaseReservationReceipt::directory_identity() const noexcept {
    return *final_witness_.directory_identity;
}

const NativeIdentityV1&
DistributedSievePrivateLeaseReservationReceipt::owner_marker_identity() const noexcept {
    return *final_witness_.owner_marker_identity;
}

const NativeIdentityV1&
DistributedSievePrivateLeaseReservationReceipt::owned_marker_identity() const noexcept {
    return *final_witness_.owned_marker_identity;
}

DistributedSieveMergeLeaseReservationReceiptV1::DistributedSieveMergeLeaseReservationReceiptV1(
    std::shared_ptr<const DistributedSieveWaveStore::State> wave_store_state,
    DistributedSieveMergeGenerationNamesV1 merge_generation_names,
    NativeIdentityV1 base_lock_identity,
    DistributedSievePrivateLeaseReservationInventoryWitness final_witness,
    std::vector<TerminalChunkInputV1> terminal_inputs, std::uint32_t merge_policy_version,
    std::vector<std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>> retained_worker_base_locks,
    std::vector<const DistributedSievePrivateLeaseBaseLockAt*> retained_worker_base_lock_views,
    std::uint64_t creator_process_id) noexcept
    : wave_store_state_(std::move(wave_store_state)),
      merge_generation_names_(std::move(merge_generation_names)),
      base_lock_identity_(base_lock_identity), final_witness_(std::move(final_witness)),
      terminal_inputs_(std::move(terminal_inputs)), merge_policy_version_(merge_policy_version),
      retained_worker_base_locks_(std::move(retained_worker_base_locks)),
      retained_worker_base_lock_views_(std::move(retained_worker_base_lock_views)),
      creator_process_id_(creator_process_id) {}

bool DistributedSieveMergeLeaseReservationReceiptV1::owned_by_current_process() const noexcept {
    if (wave_store_state_ == nullptr || creator_process_id_ == 0 ||
        !process_matches(creator_process_id_) ||
        retained_worker_base_locks_.size() != retained_worker_base_lock_views_.size()) {
        return false;
    }
    for (std::size_t index = 0; index < retained_worker_base_locks_.size(); ++index) {
        const auto& retained = retained_worker_base_locks_[index];
        if (retained == nullptr || retained_worker_base_lock_views_[index] != retained.get() ||
            !retained->owned_by_current_process()) {
            return false;
        }
    }
    return true;
}

DistributedSieveWaveStoreDiagnostic DistributedSieveMergeLeaseReservationReceiptV1::revalidate(
    DistributedSievePrivateLeaseReservationReceiptTestHooks hooks) const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    const auto parsed = parse_distributed_sieve_merge_started_leaf_v1(
        merge_generation_names_.canonical_record_leaf);
    if (!parsed.has_value() || parsed->pending ||
        final_witness_.base_lock_leaf != merge_generation_names_.base_lock_leaf ||
        final_witness_.boundary !=
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
        final_witness_.lease_id == std::array<std::uint64_t, 2>{} ||
        !final_witness_.reserved_marker_identity.has_value() ||
        !final_witness_.directory_identity.has_value() ||
        !final_witness_.owner_marker_identity.has_value() ||
        !final_witness_.owned_marker_identity.has_value() ||
        final_witness_.work_package_residue.has_value() ||
        final_witness_.worker_handoff.has_value() || final_witness_.merge_prepared.has_value() ||
        nil_identity(base_lock_identity_) ||
        terminal_inputs_.size() != wave_store_state_->manifest.chunks.size() ||
        merge_policy_version_ != wave_store_state_->manifest.merge_policy_version) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    (void)hooks;
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    DistributedSieveWaveStore view(wave_store_state_);
    const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
        std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
            retained_worker_base_lock_views_.data(), retained_worker_base_lock_views_.size())};
    const auto revalidate_bindings = [&]() noexcept {
        if (const auto authority = view.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return authority;
        }
        for (const auto& retained : retained_worker_base_locks_) {
            if (retained == nullptr) {
                return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                  protocol_error());
            }
            if (const auto worker = retained->revalidate();
                worker.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return worker;
            }
        }
        return view.revalidate_authority();
    };
    const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        return lower;
    };
    const auto target_matches = [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
        if (!observed || !observed.inventory->chunk_terminal_failure_records.empty() ||
            observed.inventory->private_lease_base_lock_leaves.size() !=
                observed.base_lock_identities->size() ||
            observed.inventory->private_lease_base_lock_leaves.size() !=
                observed.private_lease_witnesses->size()) {
            return false;
        }
        const auto position =
            std::lower_bound(observed.inventory->private_lease_base_lock_leaves.begin(),
                             observed.inventory->private_lease_base_lock_leaves.end(),
                             final_witness_.base_lock_leaf);
        if (position == observed.inventory->private_lease_base_lock_leaves.end() ||
            *position != final_witness_.base_lock_leaf) {
            return false;
        }
        const auto index = static_cast<std::size_t>(
            std::distance(observed.inventory->private_lease_base_lock_leaves.begin(), position));
        const auto record = std::lower_bound(
            observed.inventory->merge_started_records.begin(),
            observed.inventory->merge_started_records.end(), parsed->merge_attempt_ordinal,
            [](const DistributedSieveMergeStartedRecordInventoryWitnessV1& candidate,
               std::uint32_t ordinal) { return candidate.merge_attempt_ordinal < ordinal; });
        return observed.base_lock_identities->at(index) == base_lock_identity_ &&
               observed.private_lease_witnesses->at(index) == final_witness_ &&
               (record == observed.inventory->merge_started_records.end() ||
                record->merge_attempt_ordinal != parsed->merge_attempt_ordinal);
    };

    if (const auto bindings = revalidate_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return bindings;
    }
    auto first = capture_manifest_bound_inventory_witness(
        wave_store_state_->root_fd, wave_store_state_->manifest, wave_store_state_->absolute_root,
        creator_process_id_, held_inventory);
    if (!first) {
        return reject_lower_priority(std::move(first.diagnostic));
    }
    if (!target_matches(first)) {
        return reject_lower_priority(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto bindings = revalidate_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return bindings;
    }
    if (const auto hooked = invoke_private_lease_base_lock_hook(hooks.after_first_target_validation,
                                                                hooks.context, creator_process_id_);
        hooked.status != DistributedSieveWaveStoreStatus::ready) {
        return hooked;
    }
    if (const auto bindings = revalidate_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return bindings;
    }
    auto confirmed = capture_manifest_bound_inventory_witness(
        wave_store_state_->root_fd, wave_store_state_->manifest, wave_store_state_->absolute_root,
        creator_process_id_, held_inventory);
    if (!confirmed) {
        return reject_lower_priority(std::move(confirmed.diagnostic));
    }
    if (!target_matches(confirmed) || *first.inventory != *confirmed.inventory ||
        *first.base_lock_identities != *confirmed.base_lock_identities ||
        *first.private_lease_witnesses != *confirmed.private_lease_witnesses) {
        return reject_lower_priority(
            diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
    }
    if (const auto bindings = revalidate_bindings();
        bindings.status != DistributedSieveWaveStoreStatus::ready) {
        return bindings;
    }
    return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{} : process_mismatch();
#endif
}

std::uint32_t
DistributedSieveMergeLeaseReservationReceiptV1::merge_attempt_ordinal() const noexcept {
    const auto parsed = parse_distributed_sieve_merge_started_leaf_v1(
        merge_generation_names_.canonical_record_leaf);
    return parsed.has_value() ? parsed->merge_attempt_ordinal
                              : std::numeric_limits<std::uint32_t>::max();
}

DistributedSieveWorkerAttemptStartReceipt::DistributedSieveWorkerAttemptStartReceipt(
    std::shared_ptr<const DistributedSieveWaveStore::State> wave_store_state,
    DistributedSieveWorkerAttemptNamesV1 worker_attempt_names, AttemptStartedV1 record,
    durable_record::RecordSnapshot canonical_snapshot,
    DistributedSievePrivateLeaseReservationInventoryWitness final_witness,
    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> base_lock_at,
    std::uint64_t creator_process_id) noexcept
    : wave_store_state_(std::move(wave_store_state)),
      worker_attempt_names_(std::move(worker_attempt_names)), record_(std::move(record)),
      canonical_snapshot_(canonical_snapshot), final_witness_(std::move(final_witness)),
      base_lock_at_(std::move(base_lock_at)), creator_process_id_(creator_process_id) {}

bool DistributedSieveWorkerAttemptStartReceipt::owned_by_current_process() const noexcept {
    return wave_store_state_ != nullptr && creator_process_id_ != 0 &&
           process_matches(creator_process_id_) && base_lock_at_ != nullptr &&
           base_lock_at_->owned_by_current_process();
}

namespace {

enum class WorkerAttemptRecordPrefixShape : std::uint8_t {
    absent,
    pending_only,
    canonical_only,
    identical_dual,
};

enum class MergeStartedRecordPrefixShapeV1 : std::uint8_t {
    absent,
    pending_only,
    canonical_only,
    identical_dual,
};

enum class ChunkTerminalFailureRecordPrefixShape : std::uint8_t {
    absent,
    pending_only,
    canonical_only,
    identical_dual,
};

[[nodiscard]] bool
worker_attempt_start_observations_equal(const ManifestBoundInventoryWitnessResult& left,
                                        const ManifestBoundInventoryWitnessResult& right) noexcept {
    return left && right && *left.inventory == *right.inventory &&
           *left.base_lock_identities == *right.base_lock_identities &&
           *left.private_lease_witnesses == *right.private_lease_witnesses;
}

[[nodiscard]] std::optional<ManifestBoundInventoryWitnessResult>
worker_attempt_recordless_projection(const ManifestBoundInventoryWitnessResult& observed,
                                     const DistributedSieveWorkerAttemptNamesV1& names,
                                     std::uint32_t chunk_id, std::uint32_t attempt_ordinal) {
    if (!observed) {
        return std::nullopt;
    }
    ManifestBoundInventoryWitnessResult projection = observed;
    auto& leaves = projection.inventory->worker_attempt_record_leaves;
    const auto erase_leaf = [&](const std::string& leaf) {
        const auto position = std::lower_bound(leaves.begin(), leaves.end(), leaf);
        if (position != leaves.end() && *position == leaf) {
            leaves.erase(position);
        }
    };
    erase_leaf(names.canonical_record_leaf);
    erase_leaf(names.pending_record_leaf);

    auto& records = projection.inventory->worker_attempt_records;
    const auto record = std::lower_bound(
        records.begin(), records.end(), std::pair{chunk_id, attempt_ordinal},
        [](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate,
           const std::pair<std::uint32_t, std::uint32_t>& coordinate) {
            return std::pair{candidate.chunk_id, candidate.attempt_ordinal} < coordinate;
        });
    if (record == records.end() || record->chunk_id != chunk_id ||
        record->attempt_ordinal != attempt_ordinal) {
        return std::nullopt;
    }
    records.erase(record);
    return projection;
}

[[nodiscard]] std::optional<ManifestBoundInventoryWitnessResult>
merge_started_recordless_projection_v1(const ManifestBoundInventoryWitnessResult& observed,
                                       const DistributedSieveMergeGenerationNamesV1& names,
                                       std::uint32_t merge_attempt_ordinal) {
    if (!observed) {
        return std::nullopt;
    }
    ManifestBoundInventoryWitnessResult projection = observed;
    auto& leaves = projection.inventory->merge_started_record_leaves;
    const auto erase_leaf = [&](const std::string& leaf) {
        const auto position = std::lower_bound(leaves.begin(), leaves.end(), leaf);
        if (position != leaves.end() && *position == leaf) {
            leaves.erase(position);
        }
    };
    erase_leaf(names.canonical_record_leaf);
    erase_leaf(names.pending_record_leaf);

    auto& records = projection.inventory->merge_started_records;
    const auto record = std::lower_bound(
        records.begin(), records.end(), merge_attempt_ordinal,
        [](const DistributedSieveMergeStartedRecordInventoryWitnessV1& candidate,
           std::uint32_t ordinal) { return candidate.merge_attempt_ordinal < ordinal; });
    if (record != records.end() && record->merge_attempt_ordinal == merge_attempt_ordinal) {
        records.erase(record);
    }
    return projection;
}

[[nodiscard]] std::optional<MergeStartedRecordPrefixShapeV1>
merge_started_record_prefix_shape_v1(const ManifestBoundInventoryWitnessResult& predecessor,
                                     const ManifestBoundInventoryWitnessResult& observed,
                                     const DistributedSieveMergeGenerationNamesV1& names,
                                     std::uint32_t merge_attempt_ordinal,
                                     std::span<const std::byte> expected_bytes) {
    auto predecessor_projection =
        merge_started_recordless_projection_v1(predecessor, names, merge_attempt_ordinal);
    auto observed_projection =
        merge_started_recordless_projection_v1(observed, names, merge_attempt_ordinal);
    if (!predecessor_projection.has_value() || !observed_projection.has_value() ||
        !worker_attempt_start_observations_equal(*predecessor_projection, *observed_projection)) {
        return std::nullopt;
    }

    const bool canonical_leaf_present = std::binary_search(
        observed.inventory->merge_started_record_leaves.begin(),
        observed.inventory->merge_started_record_leaves.end(), names.canonical_record_leaf);
    const bool pending_leaf_present = std::binary_search(
        observed.inventory->merge_started_record_leaves.begin(),
        observed.inventory->merge_started_record_leaves.end(), names.pending_record_leaf);
    const auto record = std::lower_bound(
        observed.inventory->merge_started_records.begin(),
        observed.inventory->merge_started_records.end(), merge_attempt_ordinal,
        [](const DistributedSieveMergeStartedRecordInventoryWitnessV1& candidate,
           std::uint32_t ordinal) { return candidate.merge_attempt_ordinal < ordinal; });
    if (record == observed.inventory->merge_started_records.end() ||
        record->merge_attempt_ordinal != merge_attempt_ordinal) {
        if (canonical_leaf_present || pending_leaf_present) {
            return std::nullopt;
        }
        return MergeStartedRecordPrefixShapeV1::absent;
    }
    if (record->bytes.size() != expected_bytes.size() ||
        !std::equal(record->bytes.begin(), record->bytes.end(), expected_bytes.begin(),
                    expected_bytes.end()) ||
        record->canonical_snapshot.has_value() != canonical_leaf_present ||
        record->pending_snapshot.has_value() != pending_leaf_present) {
        return std::nullopt;
    }
    if (canonical_leaf_present && pending_leaf_present) {
        return MergeStartedRecordPrefixShapeV1::identical_dual;
    }
    if (canonical_leaf_present) {
        return MergeStartedRecordPrefixShapeV1::canonical_only;
    }
    if (pending_leaf_present) {
        return MergeStartedRecordPrefixShapeV1::pending_only;
    }
    return std::nullopt;
}

[[nodiscard]] std::optional<ManifestBoundInventoryWitnessResult>
chunk_terminal_failure_recordless_projection(
    const ManifestBoundInventoryWitnessResult& observed,
    const DistributedSieveChunkTerminalFailureNamesV1& names, std::uint32_t chunk_id) {
    if (!observed) {
        return std::nullopt;
    }
    ManifestBoundInventoryWitnessResult projection = observed;
    auto& leaves = projection.inventory->chunk_terminal_failure_record_leaves;
    const auto erase_leaf = [&](const std::string& leaf) {
        const auto position = std::lower_bound(leaves.begin(), leaves.end(), leaf);
        if (position != leaves.end() && *position == leaf) {
            leaves.erase(position);
        }
    };
    erase_leaf(names.canonical_record_leaf);
    erase_leaf(names.pending_record_leaf);

    auto& records = projection.inventory->chunk_terminal_failure_records;
    const auto record = std::lower_bound(
        records.begin(), records.end(), chunk_id,
        [](const DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1& candidate,
           std::uint32_t coordinate) { return candidate.chunk_id < coordinate; });
    if (record != records.end() && record->chunk_id == chunk_id) {
        records.erase(record);
    }
    return projection;
}

[[nodiscard]] std::optional<ChunkTerminalFailureRecordPrefixShape>
chunk_terminal_failure_record_prefix_shape(const ManifestBoundInventoryWitnessResult& predecessor,
                                           const ManifestBoundInventoryWitnessResult& observed,
                                           const DistributedSieveChunkTerminalFailureNamesV1& names,
                                           std::uint32_t chunk_id,
                                           std::span<const std::byte> expected_bytes) {
    auto predecessor_projection =
        chunk_terminal_failure_recordless_projection(predecessor, names, chunk_id);
    auto observed_projection =
        chunk_terminal_failure_recordless_projection(observed, names, chunk_id);
    if (!predecessor_projection.has_value() || !observed_projection.has_value() ||
        !worker_attempt_start_observations_equal(*predecessor_projection, *observed_projection)) {
        return std::nullopt;
    }

    const bool canonical_leaf_present =
        std::binary_search(observed.inventory->chunk_terminal_failure_record_leaves.begin(),
                           observed.inventory->chunk_terminal_failure_record_leaves.end(),
                           names.canonical_record_leaf);
    const bool pending_leaf_present = std::binary_search(
        observed.inventory->chunk_terminal_failure_record_leaves.begin(),
        observed.inventory->chunk_terminal_failure_record_leaves.end(), names.pending_record_leaf);
    const auto record = std::lower_bound(
        observed.inventory->chunk_terminal_failure_records.begin(),
        observed.inventory->chunk_terminal_failure_records.end(), chunk_id,
        [](const DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1& candidate,
           std::uint32_t coordinate) { return candidate.chunk_id < coordinate; });
    if (record == observed.inventory->chunk_terminal_failure_records.end() ||
        record->chunk_id != chunk_id) {
        if (canonical_leaf_present || pending_leaf_present) {
            return std::nullopt;
        }
        return ChunkTerminalFailureRecordPrefixShape::absent;
    }
    if (record->bytes.size() != expected_bytes.size() ||
        !std::equal(record->bytes.begin(), record->bytes.end(), expected_bytes.begin(),
                    expected_bytes.end()) ||
        record->canonical_snapshot.has_value() != canonical_leaf_present ||
        record->pending_snapshot.has_value() != pending_leaf_present) {
        return std::nullopt;
    }
    if (canonical_leaf_present && pending_leaf_present) {
        return ChunkTerminalFailureRecordPrefixShape::identical_dual;
    }
    if (canonical_leaf_present) {
        return ChunkTerminalFailureRecordPrefixShape::canonical_only;
    }
    if (pending_leaf_present) {
        return ChunkTerminalFailureRecordPrefixShape::pending_only;
    }
    return std::nullopt;
}

[[nodiscard]] std::optional<WorkerAttemptRecordPrefixShape>
worker_attempt_record_prefix_shape(const ManifestBoundInventoryWitnessResult& predecessor,
                                   const ManifestBoundInventoryWitnessResult& observed,
                                   const DistributedSieveWorkerAttemptNamesV1& names,
                                   std::uint32_t chunk_id, std::uint32_t attempt_ordinal,
                                   std::span<const std::byte> expected_bytes) noexcept {
    if (!predecessor || !observed ||
        *observed.base_lock_identities != *predecessor.base_lock_identities ||
        *observed.private_lease_witnesses != *predecessor.private_lease_witnesses ||
        observed.inventory->lock != predecessor.inventory->lock ||
        observed.inventory->manifest != predecessor.inventory->manifest ||
        observed.inventory->pending != predecessor.inventory->pending ||
        observed.inventory->private_lease_base_lock_leaves !=
            predecessor.inventory->private_lease_base_lock_leaves ||
        observed.inventory->private_lease_protocol_leaves !=
            predecessor.inventory->private_lease_protocol_leaves) {
        return std::nullopt;
    }

    bool canonical_leaf_present = false;
    bool pending_leaf_present = false;
    std::size_t predecessor_leaf_index = 0;
    for (const auto& leaf : observed.inventory->worker_attempt_record_leaves) {
        if (leaf == names.canonical_record_leaf) {
            if (canonical_leaf_present) {
                return std::nullopt;
            }
            canonical_leaf_present = true;
            continue;
        }
        if (leaf == names.pending_record_leaf) {
            if (pending_leaf_present) {
                return std::nullopt;
            }
            pending_leaf_present = true;
            continue;
        }
        if (predecessor_leaf_index >= predecessor.inventory->worker_attempt_record_leaves.size() ||
            leaf != predecessor.inventory->worker_attempt_record_leaves[predecessor_leaf_index]) {
            return std::nullopt;
        }
        ++predecessor_leaf_index;
    }
    if (predecessor_leaf_index != predecessor.inventory->worker_attempt_record_leaves.size()) {
        return std::nullopt;
    }

    const DistributedSieveWorkerAttemptRecordInventoryWitness* target = nullptr;
    std::size_t predecessor_record_index = 0;
    for (const auto& witness : observed.inventory->worker_attempt_records) {
        if (witness.chunk_id == chunk_id && witness.attempt_ordinal == attempt_ordinal) {
            if (target != nullptr) {
                return std::nullopt;
            }
            target = &witness;
            continue;
        }
        if (predecessor_record_index >= predecessor.inventory->worker_attempt_records.size() ||
            !(witness == predecessor.inventory->worker_attempt_records[predecessor_record_index])) {
            return std::nullopt;
        }
        ++predecessor_record_index;
    }
    if (predecessor_record_index != predecessor.inventory->worker_attempt_records.size()) {
        return std::nullopt;
    }

    if (target == nullptr) {
        if (canonical_leaf_present || pending_leaf_present) {
            return std::nullopt;
        }
        return WorkerAttemptRecordPrefixShape::absent;
    }
    if (!std::equal(target->bytes.begin(), target->bytes.end(), expected_bytes.begin(),
                    expected_bytes.end()) ||
        target->bytes.size() != expected_bytes.size() ||
        target->canonical_snapshot.has_value() != canonical_leaf_present ||
        target->pending_snapshot.has_value() != pending_leaf_present) {
        return std::nullopt;
    }
    if (canonical_leaf_present && pending_leaf_present) {
        return WorkerAttemptRecordPrefixShape::identical_dual;
    }
    if (canonical_leaf_present) {
        return WorkerAttemptRecordPrefixShape::canonical_only;
    }
    if (pending_leaf_present) {
        return WorkerAttemptRecordPrefixShape::pending_only;
    }
    return std::nullopt;
}

[[nodiscard]] const DistributedSieveWorkerAttemptRecordInventoryWitness*
find_worker_attempt_record_witness(const ManifestBoundInventoryWitnessResult& observed,
                                   std::uint32_t chunk_id, std::uint32_t attempt_ordinal) noexcept {
    if (!observed) {
        return nullptr;
    }
    const auto position = std::lower_bound(
        observed.inventory->worker_attempt_records.begin(),
        observed.inventory->worker_attempt_records.end(), std::pair{chunk_id, attempt_ordinal},
        [](const DistributedSieveWorkerAttemptRecordInventoryWitness& candidate,
           const std::pair<std::uint32_t, std::uint32_t>& coordinate) {
            return std::pair{candidate.chunk_id, candidate.attempt_ordinal} < coordinate;
        });
    if (position == observed.inventory->worker_attempt_records.end() ||
        position->chunk_id != chunk_id || position->attempt_ordinal != attempt_ordinal) {
        return nullptr;
    }
    return &*position;
}

[[nodiscard]] const DistributedSieveMergeStartedRecordInventoryWitnessV1*
find_merge_started_record_witness_v1(const ManifestBoundInventoryWitnessResult& observed,
                                     std::uint32_t merge_attempt_ordinal) noexcept {
    if (!observed) {
        return nullptr;
    }
    const auto position = std::lower_bound(
        observed.inventory->merge_started_records.begin(),
        observed.inventory->merge_started_records.end(), merge_attempt_ordinal,
        [](const DistributedSieveMergeStartedRecordInventoryWitnessV1& candidate,
           std::uint32_t ordinal) { return candidate.merge_attempt_ordinal < ordinal; });
    if (position == observed.inventory->merge_started_records.end() ||
        position->merge_attempt_ordinal != merge_attempt_ordinal) {
        return nullptr;
    }
    return &*position;
}

[[nodiscard]] DistributedSieveProtocolStatus validate_merge_terminal_projection_v1(
    const WaveManifestV1& manifest,
    std::span<const DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempts,
    std::span<const DistributedSievePrivateLeaseReservationInventoryWitness> private_leases,
    std::span<const TerminalChunkInputV1> terminal_inputs) noexcept {
    if (terminal_inputs.size() != manifest.chunks.size()) {
        return {
            .error = DistributedSieveProtocolError::invalid_value,
        };
    }
    try {
        std::vector<std::vector<AttemptStartedV1>> attempt_chains(manifest.chunks.size());
        std::vector<const WorkerHandoffV1*> handoffs(manifest.chunks.size(), nullptr);
        for (std::size_t chunk_index = 0; chunk_index < manifest.chunks.size(); ++chunk_index) {
            const auto& chunk = manifest.chunks[chunk_index];
            auto& chain = attempt_chains[chunk_index];
            for (const auto& attempt : worker_attempts) {
                if (attempt.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (!attempt.canonical_snapshot.has_value() ||
                    attempt.pending_snapshot.has_value() ||
                    attempt.attempt_ordinal != chain.size()) {
                    return {
                        .error = DistributedSieveProtocolError::noncanonical_order,
                        .element_index = static_cast<std::uint32_t>(chunk_index),
                    };
                }
                chain.push_back(attempt.record);
            }
            for (const auto& lease : private_leases) {
                if (!lease.worker_handoff.has_value() ||
                    lease.worker_handoff->handoff.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (handoffs[chunk_index] != nullptr) {
                    return {
                        .error = DistributedSieveProtocolError::duplicate_entry,
                        .element_index = static_cast<std::uint32_t>(chunk_index),
                    };
                }
                handoffs[chunk_index] = &lease.worker_handoff->handoff;
            }
            if (const auto status = validate_terminal_chunk_projection(
                    manifest, chunk.chunk_id, chain, handoffs[chunk_index],
                    terminal_inputs[chunk_index]);
                !status) {
                return {
                    .error = status.error,
                    .byte_offset = status.byte_offset,
                    .element_index = static_cast<std::uint32_t>(chunk_index),
                };
            }
        }
    } catch (...) {
        return {
            .error = DistributedSieveProtocolError::invalid_value,
        };
    }
    return {};
}

[[nodiscard]] DistributedSieveProtocolStatus validate_merge_start_chain_v1(
    const WaveManifestV1& manifest,
    std::span<const DistributedSieveWorkerAttemptRecordInventoryWitness> worker_attempts,
    std::span<const DistributedSievePrivateLeaseReservationInventoryWitness> private_leases,
    std::span<const MergeStartedV1> starts) noexcept {
    try {
        std::vector<std::vector<AttemptStartedV1>> attempt_chains(manifest.chunks.size());
        std::vector<const WorkerHandoffV1*> handoffs(manifest.chunks.size(), nullptr);
        std::vector<ChunkTerminalEvidenceViewV1> evidence;
        evidence.reserve(manifest.chunks.size());
        for (std::size_t chunk_index = 0; chunk_index < manifest.chunks.size(); ++chunk_index) {
            const auto& chunk = manifest.chunks[chunk_index];
            auto& chain = attempt_chains[chunk_index];
            for (const auto& attempt : worker_attempts) {
                if (attempt.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (!attempt.canonical_snapshot.has_value() ||
                    attempt.pending_snapshot.has_value() ||
                    attempt.attempt_ordinal != chain.size()) {
                    return {
                        .error = DistributedSieveProtocolError::noncanonical_order,
                        .element_index = static_cast<std::uint32_t>(chunk_index),
                    };
                }
                chain.push_back(attempt.record);
            }
            for (const auto& lease : private_leases) {
                if (!lease.worker_handoff.has_value() ||
                    lease.worker_handoff->handoff.chunk_id != chunk.chunk_id) {
                    continue;
                }
                if (handoffs[chunk_index] != nullptr) {
                    return {
                        .error = DistributedSieveProtocolError::duplicate_entry,
                        .element_index = static_cast<std::uint32_t>(chunk_index),
                    };
                }
                handoffs[chunk_index] = &lease.worker_handoff->handoff;
            }
            evidence.push_back({
                .attempts = chain,
                .handoff = handoffs[chunk_index],
            });
        }
        return validate_merge_dependency_chain(manifest, evidence, starts, nullptr, nullptr);
    } catch (...) {
        return {
            .error = DistributedSieveProtocolError::invalid_value,
        };
    }
}

[[nodiscard]] const DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1*
find_chunk_terminal_failure_record_witness(const ManifestBoundInventoryWitnessResult& observed,
                                           std::uint32_t chunk_id) noexcept {
    if (!observed) {
        return nullptr;
    }
    const auto position = std::lower_bound(
        observed.inventory->chunk_terminal_failure_records.begin(),
        observed.inventory->chunk_terminal_failure_records.end(), chunk_id,
        [](const DistributedSieveChunkTerminalFailureRecordInventoryWitnessV1& candidate,
           std::uint32_t coordinate) { return candidate.chunk_id < coordinate; });
    if (position == observed.inventory->chunk_terminal_failure_records.end() ||
        position->chunk_id != chunk_id) {
        return nullptr;
    }
    return &*position;
}

[[nodiscard]] DistributedSieveWaveStoreDiagnostic
worker_attempt_protocol_conflict(DistributedSieveProtocolStatus status) noexcept {
    auto outcome =
        diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
    outcome.protocol_status = status;
    return outcome;
}

} // namespace

DistributedSieveWaveStoreDiagnostic DistributedSieveWorkerAttemptStartReceipt::revalidate(
    DistributedSieveWorkerAttemptStartReceiptTestHooks hooks) const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    const auto parsed =
        parse_distributed_sieve_worker_attempt_leaf_v1(worker_attempt_names_.canonical_record_leaf);
    if (!parsed.has_value() || parsed->pending ||
        final_witness_.base_lock_leaf != worker_attempt_names_.base_lock_leaf ||
        final_witness_.boundary !=
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
        final_witness_.lease_id == std::array<std::uint64_t, 2>{} ||
        !final_witness_.reserved_marker_identity.has_value() ||
        !final_witness_.directory_identity.has_value() ||
        !final_witness_.owner_marker_identity.has_value() ||
        !final_witness_.owned_marker_identity.has_value() ||
        final_witness_.work_package_residue.has_value() ||
        final_witness_.worker_handoff.has_value() || final_witness_.merge_prepared.has_value() ||
        record_.chunk_id != parsed->chunk_id ||
        record_.attempt_ordinal != parsed->attempt_ordinal ||
        record_.manifest_digest != wave_store_state_->manifest.self_digest ||
        record_.lease.lease_id.limbs != final_witness_.lease_id ||
        record_.lease.owner_marker != *final_witness_.owner_marker_identity ||
        record_.lease.directory != *final_witness_.directory_identity ||
        record_.lease.relative_stem != worker_attempt_names_.relative_lease_stem ||
        base_lock_at_->identity() == NativeIdentityV1{}) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    (void)hooks;
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    try {
        DistributedSieveProtocolRecordV1 sealed(record_);
        const auto encoded = encode_distributed_sieve_record(sealed);
        if (!encoded) {
            auto outcome =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            outcome.protocol_status = encoded.status;
            return outcome;
        }
        const auto regenerated = [&]() -> std::optional<DistributedSieveWorkerAttemptNamesV1> {
            for (const auto& chunk : wave_store_state_->manifest.chunks) {
                if (chunk.chunk_id == parsed->chunk_id) {
                    return distributed_sieve_worker_attempt_names_v1(
                        chunk.relative_artifact_stem, parsed->chunk_id, parsed->attempt_ordinal);
                }
            }
            return std::nullopt;
        }();
        if (!regenerated.has_value() || *regenerated != worker_attempt_names_) {
            return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                              invalid_argument_error());
        }

        DistributedSieveWaveStore view(wave_store_state_);
        const auto revalidate_bindings = [&]() noexcept {
            if (const auto authority = view.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            if (const auto target = base_lock_at_->revalidate();
                target.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return target;
            }
            return view.revalidate_authority();
        };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };
        const auto target_matches =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                if (!observed || !observed.inventory->chunk_terminal_failure_records.empty() ||
                    observed.inventory->private_lease_base_lock_leaves.size() !=
                        observed.base_lock_identities->size() ||
                    observed.inventory->private_lease_base_lock_leaves.size() !=
                        observed.private_lease_witnesses->size()) {
                    return false;
                }
                const auto lock_position =
                    std::lower_bound(observed.inventory->private_lease_base_lock_leaves.begin(),
                                     observed.inventory->private_lease_base_lock_leaves.end(),
                                     worker_attempt_names_.base_lock_leaf);
                if (lock_position == observed.inventory->private_lease_base_lock_leaves.end() ||
                    *lock_position != worker_attempt_names_.base_lock_leaf) {
                    return false;
                }
                const auto lock_index = static_cast<std::size_t>(std::distance(
                    observed.inventory->private_lease_base_lock_leaves.begin(), lock_position));
                const auto* witness = find_worker_attempt_record_witness(observed, parsed->chunk_id,
                                                                         parsed->attempt_ordinal);
                return observed.base_lock_identities->at(lock_index) == base_lock_at_->identity() &&
                       observed.private_lease_witnesses->at(lock_index) == final_witness_ &&
                       witness != nullptr && witness->bytes == *encoded.bytes &&
                       witness->canonical_snapshot == canonical_snapshot_ &&
                       !witness->pending_snapshot.has_value();
            };

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        auto first = capture_manifest_bound_inventory_witness(
            wave_store_state_->root_fd, wave_store_state_->manifest,
            wave_store_state_->absolute_root, creator_process_id_);
        if (!first) {
            return reject_lower_priority(std::move(first.diagnostic));
        }
        if (!target_matches(first)) {
            return reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.after_first_validation, hooks.context, creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return hooked;
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        auto confirmed = capture_manifest_bound_inventory_witness(
            wave_store_state_->root_fd, wave_store_state_->manifest,
            wave_store_state_->absolute_root, creator_process_id_);
        if (!confirmed) {
            return reject_lower_priority(std::move(confirmed.diagnostic));
        }
        if (!target_matches(confirmed)) {
            return reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{}
                                          : process_mismatch();
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }
#endif
}

const AttemptStartedV1& DistributedSieveWorkerAttemptStartReceipt::record() const noexcept {
    return record_;
}

const durable_record::RecordSnapshot&
DistributedSieveWorkerAttemptStartReceipt::canonical_snapshot() const noexcept {
    return canonical_snapshot_;
}

DistributedSieveMergeStartedReceiptV1::DistributedSieveMergeStartedReceiptV1(
    std::shared_ptr<const DistributedSieveWaveStore::State> wave_store_state,
    DistributedSieveMergeGenerationNamesV1 merge_generation_names, MergeStartedV1 record,
    std::vector<MergeStartedV1> merge_started_chain,
    durable_record::RecordSnapshot canonical_snapshot,
    DistributedSievePrivateLeaseReservationInventoryWitness final_witness,
    std::vector<std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>> retained_worker_base_locks,
    std::vector<const DistributedSievePrivateLeaseBaseLockAt*> retained_worker_base_lock_views,
    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> base_lock_at,
    std::uint64_t creator_process_id) noexcept
    : wave_store_state_(std::move(wave_store_state)),
      merge_generation_names_(std::move(merge_generation_names)), record_(std::move(record)),
      merge_started_chain_(std::move(merge_started_chain)), canonical_snapshot_(canonical_snapshot),
      final_witness_(std::move(final_witness)),
      retained_worker_base_locks_(std::move(retained_worker_base_locks)),
      retained_worker_base_lock_views_(std::move(retained_worker_base_lock_views)),
      base_lock_at_(std::move(base_lock_at)), creator_process_id_(creator_process_id) {}

bool DistributedSieveMergeStartedReceiptV1::owned_by_current_process() const noexcept {
    if (wave_store_state_ == nullptr || creator_process_id_ == 0 ||
        !process_matches(creator_process_id_) || base_lock_at_ == nullptr ||
        !base_lock_at_->owned_by_current_process() ||
        retained_worker_base_locks_.size() != retained_worker_base_lock_views_.size()) {
        return false;
    }
    for (std::size_t index = 0; index < retained_worker_base_locks_.size(); ++index) {
        const auto& retained = retained_worker_base_locks_[index];
        if (retained == nullptr || retained_worker_base_lock_views_[index] != retained.get() ||
            !retained->owned_by_current_process()) {
            return false;
        }
    }
    return true;
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveMergeStartedReceiptV1::revalidate() const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    const auto parsed = parse_distributed_sieve_merge_started_leaf_v1(
        merge_generation_names_.canonical_record_leaf);
    if (!parsed.has_value() || parsed->pending ||
        record_.merge_attempt_ordinal != parsed->merge_attempt_ordinal ||
        merge_started_chain_.empty() ||
        merge_started_chain_.size() !=
            static_cast<std::size_t>(record_.merge_attempt_ordinal) + 1U ||
        merge_started_chain_.back().self_digest != record_.self_digest ||
        record_.manifest_digest != wave_store_state_->manifest.self_digest ||
        record_.work_digest != wave_store_state_->manifest.work_sha256 ||
        record_.merge_policy_version != wave_store_state_->manifest.merge_policy_version ||
        record_.ordered_inputs.size() != wave_store_state_->manifest.chunks.size() ||
        !final_witness_.reserved_marker_identity.has_value() ||
        !final_witness_.directory_identity.has_value() ||
        !final_witness_.owner_marker_identity.has_value() ||
        !final_witness_.owned_marker_identity.has_value() ||
        record_.merged_lease.lease_id.limbs != final_witness_.lease_id ||
        record_.merged_lease.owner_marker != *final_witness_.owner_marker_identity ||
        record_.merged_lease.directory != *final_witness_.directory_identity ||
        record_.merged_lease.relative_stem != merge_generation_names_.relative_lease_stem ||
        final_witness_.base_lock_leaf != merge_generation_names_.base_lock_leaf ||
        final_witness_.boundary !=
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
        final_witness_.work_package_residue.has_value() ||
        final_witness_.worker_handoff.has_value() || final_witness_.merge_prepared.has_value() ||
        base_lock_at_->identity() == NativeIdentityV1{}) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    try {
        if (const auto chain = validate_merge_predecessor_chain(
                wave_store_state_->manifest, merge_started_chain_, nullptr, nullptr);
            !chain) {
            auto outcome =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            outcome.protocol_status = chain;
            return outcome;
        }
        DistributedSieveProtocolRecordV1 sealed(record_);
        const auto encoded = encode_distributed_sieve_record(sealed);
        if (!encoded) {
            auto outcome =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            outcome.protocol_status = encoded.status;
            return outcome;
        }
        const auto regenerated =
            distributed_sieve_merge_generation_names_v1(parsed->merge_attempt_ordinal);
        if (!regenerated.has_value() || *regenerated != merge_generation_names_) {
            return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                              invalid_argument_error());
        }

        DistributedSieveWaveStore view(wave_store_state_);
        const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                retained_worker_base_lock_views_.data(), retained_worker_base_lock_views_.size())};
        const auto revalidate_bindings = [&]() noexcept {
            if (const auto authority = view.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            for (const auto& retained : retained_worker_base_locks_) {
                if (retained == nullptr) {
                    return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                      protocol_error());
                }
                if (const auto worker = retained->revalidate();
                    worker.status != DistributedSieveWaveStoreStatus::ready) {
                    return worker;
                }
            }
            if (const auto target = base_lock_at_->revalidate();
                target.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = view.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return target;
            }
            return view.revalidate_authority();
        };
        const auto target_matches =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                if (!observed || !observed.inventory->chunk_terminal_failure_records.empty()) {
                    return false;
                }
                const auto lock_position =
                    std::lower_bound(observed.inventory->private_lease_base_lock_leaves.begin(),
                                     observed.inventory->private_lease_base_lock_leaves.end(),
                                     merge_generation_names_.base_lock_leaf);
                if (lock_position == observed.inventory->private_lease_base_lock_leaves.end() ||
                    *lock_position != merge_generation_names_.base_lock_leaf) {
                    return false;
                }
                const auto lock_index = static_cast<std::size_t>(std::distance(
                    observed.inventory->private_lease_base_lock_leaves.begin(), lock_position));
                const auto* witness =
                    find_merge_started_record_witness_v1(observed, parsed->merge_attempt_ordinal);
                return observed.base_lock_identities->at(lock_index) == base_lock_at_->identity() &&
                       observed.private_lease_witnesses->at(lock_index) == final_witness_ &&
                       witness != nullptr && witness->bytes == *encoded.bytes &&
                       witness->canonical_snapshot == canonical_snapshot_ &&
                       !witness->pending_snapshot.has_value();
            };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        auto first = capture_manifest_bound_inventory_witness(
            wave_store_state_->root_fd, wave_store_state_->manifest,
            wave_store_state_->absolute_root, creator_process_id_, held_inventory);
        if (!first) {
            return reject_lower_priority(std::move(first.diagnostic));
        }
        if (!target_matches(first)) {
            return reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        auto confirmed = capture_manifest_bound_inventory_witness(
            wave_store_state_->root_fd, wave_store_state_->manifest,
            wave_store_state_->absolute_root, creator_process_id_, held_inventory);
        if (!confirmed) {
            return reject_lower_priority(std::move(confirmed.diagnostic));
        }
        if (!target_matches(confirmed) ||
            !worker_attempt_start_observations_equal(first, confirmed)) {
            return reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{}
                                          : process_mismatch();
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }
#endif
}

const MergeStartedV1& DistributedSieveMergeStartedReceiptV1::record() const noexcept {
    return record_;
}

const durable_record::RecordSnapshot&
DistributedSieveMergeStartedReceiptV1::canonical_snapshot() const noexcept {
    return canonical_snapshot_;
}

DistributedSieveWaveStoreDiagnostic
DistributedSieveMergeStartedReceiptV1::revalidate_for_merge_writer_lifetime(
    int directory_fd) const noexcept {
    if (!owned_by_current_process()) {
        return process_mismatch();
    }
    const auto parsed = parse_distributed_sieve_merge_started_leaf_v1(
        merge_generation_names_.canonical_record_leaf);
    if (directory_fd < 0 || !parsed.has_value() || parsed->pending ||
        record_.merge_attempt_ordinal != parsed->merge_attempt_ordinal ||
        merge_started_chain_.empty() ||
        merge_started_chain_.size() !=
            static_cast<std::size_t>(record_.merge_attempt_ordinal) + 1U ||
        merge_started_chain_.back().self_digest != record_.self_digest ||
        record_.manifest_digest != wave_store_state_->manifest.self_digest ||
        record_.work_digest != wave_store_state_->manifest.work_sha256 ||
        record_.merge_policy_version != wave_store_state_->manifest.merge_policy_version ||
        final_witness_.base_lock_leaf != merge_generation_names_.base_lock_leaf ||
        final_witness_.boundary !=
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
        final_witness_.lease_id == std::array<std::uint64_t, 2>{} ||
        !final_witness_.reserved_marker_identity.has_value() ||
        !final_witness_.directory_identity.has_value() ||
        !final_witness_.owner_marker_identity.has_value() ||
        !final_witness_.owned_marker_identity.has_value() ||
        final_witness_.work_package_residue.has_value() ||
        final_witness_.worker_handoff.has_value() || final_witness_.merge_prepared.has_value() ||
        record_.merged_lease.lease_id.limbs != final_witness_.lease_id ||
        record_.merged_lease.owner_marker != *final_witness_.owner_marker_identity ||
        record_.merged_lease.directory != *final_witness_.directory_identity ||
        record_.merged_lease.relative_stem != merge_generation_names_.relative_lease_stem ||
        base_lock_at_->identity() == NativeIdentityV1{}) {
        return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                          invalid_argument_error());
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
#else
    try {
        if (const auto chain = validate_merge_predecessor_chain(
                wave_store_state_->manifest, merge_started_chain_, nullptr, nullptr);
            !chain) {
            auto outcome =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            outcome.protocol_status = chain;
            return outcome;
        }
        const auto regenerated =
            distributed_sieve_merge_generation_names_v1(parsed->merge_attempt_ordinal);
        if (!regenerated.has_value() || *regenerated != merge_generation_names_) {
            return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                              invalid_argument_error());
        }

        DistributedSieveProtocolRecordV1 sealed(record_);
        auto encoded = encode_distributed_sieve_record(sealed);
        if (!encoded) {
            auto outcome =
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error());
            outcome.protocol_status = encoded.status;
            return outcome;
        }
        DistributedSieveMergeStartedRecordInventoryWitnessV1 exact_started{
            .merge_attempt_ordinal = record_.merge_attempt_ordinal,
            .record = record_,
            .bytes = std::move(*encoded.bytes),
            .canonical_snapshot = canonical_snapshot_,
        };

        const std::filesystem::path exact_base_path =
            wave_store_state_->absolute_root / merge_generation_names_.private_directory_leaf /
            "corpus";
        const auto exact_paths = gnfs::relation::OOCCleanupTransaction::paths_for(exact_base_path);
        const std::filesystem::path expected_private_directory =
            wave_store_state_->absolute_root / merge_generation_names_.private_directory_leaf;
        const std::filesystem::path expected_lock_path =
            wave_store_state_->absolute_root / merge_generation_names_.base_lock_leaf;
        if (exact_paths.base_path != exact_base_path ||
            exact_paths.private_directory != expected_private_directory ||
            exact_paths.lock_path != expected_lock_path) {
            return diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                              invalid_argument_error());
        }

        const auto exact_reserved = private_lease::make_private_lease_reserved_record(
            exact_paths, final_witness_.lease_id,
            relation_identity(wave_store_state_->root_identity),
            relation_identity(base_lock_at_->identity()));
        const auto exact_owner = private_lease::make_private_lease_owner_record(
            exact_reserved, relation_identity(*final_witness_.directory_identity));
        const auto exact_owned = private_lease::make_private_lease_owned_record(
            exact_owner, relation_identity(*final_witness_.owner_marker_identity));

        DistributedSieveWaveStore view(wave_store_state_);
        const auto revalidate_bindings = [&]() noexcept {
            if (const auto authority = view.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            for (const auto& retained : retained_worker_base_locks_) {
                if (retained == nullptr) {
                    return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                      protocol_error());
                }
                if (const auto worker = retained->revalidate();
                    worker.status != DistributedSieveWaveStoreStatus::ready) {
                    return worker;
                }
            }
            if (const auto target = base_lock_at_->revalidate();
                target.status != DistributedSieveWaveStoreStatus::ready) {
                return target;
            }
            if (const auto directory = validate_private_lease_directory_binding(
                    wave_store_state_->root_fd, directory_fd,
                    merge_generation_names_.private_directory_leaf, creator_process_id_,
                    *final_witness_.directory_identity);
                directory.status != DistributedSieveWaveStoreStatus::ready) {
                return directory;
            }
            if (const auto canonical = revalidate_exact_canonical_merge_started(
                    wave_store_state_->root_fd, merge_generation_names_, exact_started,
                    creator_process_id_);
                canonical.status != DistributedSieveWaveStoreStatus::ready) {
                return canonical;
            }
            return view.revalidate_authority();
        };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };

        struct MergeWriterLifetimeObservation final {
            NamespaceInventory inventory;
            std::vector<NativeIdentityV1> base_lock_identities;
            std::vector<PrivateLeaseReservationWitness> private_leases;
            PrivateLeaseDirectoryInventory directory;
            private_lease::PrivateLeaseRecord reserved;
            private_lease::PrivateLeaseRecord owner;
            private_lease::PrivateLeaseRecord owned;
            NativeIdentityV1 reserved_identity;
            NativeIdentityV1 owner_identity;
            NativeIdentityV1 owned_identity;
        };
        struct MergeWriterLifetimeObservationResult final {
            std::optional<MergeWriterLifetimeObservation> observation;
            DistributedSieveWaveStoreDiagnostic diagnostic;
        };
        const auto observations_equal = [](const MergeWriterLifetimeObservation& left,
                                           const MergeWriterLifetimeObservation& right) noexcept {
            return left.inventory == right.inventory &&
                   left.base_lock_identities == right.base_lock_identities &&
                   left.private_leases == right.private_leases &&
                   left.directory == right.directory && left.reserved == right.reserved &&
                   left.owner == right.owner && left.owned == right.owned &&
                   left.reserved_identity == right.reserved_identity &&
                   left.owner_identity == right.owner_identity &&
                   left.owned_identity == right.owned_identity;
        };
        const auto capture = [&]() -> MergeWriterLifetimeObservationResult {
            const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
                return MergeWriterLifetimeObservationResult{
                    .observation = std::nullopt,
                    .diagnostic = std::move(failure),
                };
            };
            const auto conflict = [&] {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            };
            if (!process_matches(creator_process_id_)) {
                return fail_with(process_mismatch());
            }
            auto inventory = inspect_namespace(wave_store_state_->root_fd);
            if (!inventory) {
                return fail_with(std::move(inventory.diagnostic));
            }
            if (!inventory.inventory->lock || !inventory.inventory->manifest ||
                inventory.inventory->pending) {
                return conflict();
            }
            auto base_locks = validate_private_lease_base_lock_inventory(
                wave_store_state_->root_fd, *inventory.inventory, wave_store_state_->manifest,
                creator_process_id_);
            if (!base_locks) {
                return fail_with(std::move(base_locks.diagnostic));
            }
            auto attempts = parse_private_lease_attempt_inventory(
                *inventory.inventory, wave_store_state_->manifest, *base_locks.identities);
            if (!attempts) {
                return fail_with(std::move(attempts.diagnostic));
            }

            auto held_lock_for = [&](const PrivateLeaseAttemptInventory& attempt)
                -> const DistributedSievePrivateLeaseBaseLockAt* {
                if (attempt.names.base_lock_leaf == merge_generation_names_.base_lock_leaf) {
                    return base_lock_at_.get();
                }
                for (const auto* retained : retained_worker_base_lock_views_) {
                    if (retained != nullptr &&
                        retained->matches_exact_binding(attempt.names.base_lock_leaf,
                                                        attempt.base_lock_identity)) {
                        return retained;
                    }
                }
                return nullptr;
            };

            const PrivateLeaseAttemptInventory* target_attempt = nullptr;
            std::vector<PrivateLeaseReservationWitness> private_leases;
            private_leases.reserve(attempts.attempts->size());
            for (const auto& attempt : *attempts.attempts) {
                if (attempt.names.base_lock_leaf == merge_generation_names_.base_lock_leaf) {
                    if (target_attempt != nullptr || !attempt.merge_generation_names.has_value() ||
                        *attempt.merge_generation_names != merge_generation_names_ ||
                        attempt.merge_attempt_ordinal != record_.merge_attempt_ordinal ||
                        attempt.base_lock_identity != base_lock_at_->identity() ||
                        !attempt.reserved || attempt.reserved_pending || !attempt.owned ||
                        attempt.owned_pending || attempt.rollback_handoff ||
                        !attempt.final_directory || attempt.staging_directory_leaf.has_value()) {
                        return conflict();
                    }
                    target_attempt = &attempt;
                    private_leases.push_back(final_witness_);
                    continue;
                }
                auto validated = validate_private_lease_attempt_inventory(
                    wave_store_state_->root_fd, wave_store_state_->absolute_root, attempt,
                    wave_store_state_->manifest, creator_process_id_, held_lock_for(attempt));
                if (!validated) {
                    return fail_with(std::move(validated.diagnostic));
                }
                private_leases.push_back(std::move(*validated.witness));
            }
            if (target_attempt == nullptr) {
                return conflict();
            }

            auto directory =
                inspect_private_lease_directory_handle(directory_fd, creator_process_id_);
            if (!directory) {
                return fail_with(std::move(directory.diagnostic));
            }
            const auto& directory_entries = *directory.inventory;
            if (!directory_entries.owner || directory_entries.owner_pending ||
                directory_entries.work_package_residue || directory_entries.worker_handoff ||
                directory_entries.worker_handoff_pending) {
                return conflict();
            }
            if (const auto rebound = validate_private_lease_directory_binding(
                    wave_store_state_->root_fd, directory_fd,
                    merge_generation_names_.private_directory_leaf, creator_process_id_,
                    *final_witness_.directory_identity);
                rebound.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(rebound);
            }

            auto reserved = read_private_lease_marker_at(wave_store_state_->root_fd,
                                                         merge_generation_names_.reserved_leaf,
                                                         creator_process_id_);
            auto owner = read_private_lease_marker_at(
                directory_fd, std::string(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF),
                creator_process_id_);
            auto owned = read_private_lease_marker_at(wave_store_state_->root_fd,
                                                      merge_generation_names_.owned_leaf,
                                                      creator_process_id_);
            if (!reserved) {
                return fail_with(std::move(reserved.diagnostic));
            }
            if (!owner) {
                return fail_with(std::move(owner.diagnostic));
            }
            if (!owned) {
                return fail_with(std::move(owned.diagnostic));
            }
            if (*reserved.record != exact_reserved || *owner.record != exact_owner ||
                *owned.record != exact_owned ||
                *reserved.identity != *final_witness_.reserved_marker_identity ||
                *owner.identity != *final_witness_.owner_marker_identity ||
                *owned.identity != *final_witness_.owned_marker_identity) {
                return conflict();
            }
            if (const auto validated = validate_named_private_lease_marker_handle(
                    wave_store_state_->root_fd, merge_generation_names_.reserved_leaf.c_str(),
                    reserved.marker.get(), exact_reserved, *final_witness_.reserved_marker_identity,
                    creator_process_id_);
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }
            const std::string owner_leaf(DISTRIBUTED_SIEVE_PRIVATE_LEASE_OWNER_LEAF);
            if (const auto validated = validate_named_private_lease_marker_handle(
                    directory_fd, owner_leaf.c_str(), owner.marker.get(), exact_owner,
                    *final_witness_.owner_marker_identity, creator_process_id_);
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }
            if (const auto validated = validate_named_private_lease_marker_handle(
                    wave_store_state_->root_fd, merge_generation_names_.owned_leaf.c_str(),
                    owned.marker.get(), exact_owned, *final_witness_.owned_marker_identity,
                    creator_process_id_);
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }

            auto worker_attempts = validate_worker_attempt_record_inventory(
                wave_store_state_->root_fd, *inventory.inventory, wave_store_state_->manifest,
                *base_locks.identities, private_leases, creator_process_id_);
            if (!worker_attempts) {
                return fail_with(std::move(worker_attempts.diagnostic));
            }
            auto merge_starts = validate_merge_started_record_inventory(
                wave_store_state_->root_fd, *inventory.inventory, wave_store_state_->manifest,
                *base_locks.identities, private_leases, *worker_attempts.witnesses,
                creator_process_id_);
            if (!merge_starts) {
                return fail_with(std::move(merge_starts.diagnostic));
            }
            auto terminal_failures = validate_chunk_terminal_failure_record_inventory(
                wave_store_state_->root_fd, *inventory.inventory, wave_store_state_->manifest,
                private_leases, *worker_attempts.witnesses, creator_process_id_);
            if (!terminal_failures) {
                return fail_with(std::move(terminal_failures.diagnostic));
            }
            if (!terminal_failures.witnesses->empty() ||
                merge_starts.witnesses->size() != merge_started_chain_.size() ||
                merge_starts.witnesses->empty() ||
                merge_starts.witnesses->back() != exact_started) {
                return conflict();
            }
            for (std::size_t index = 0; index < merge_started_chain_.size(); ++index) {
                const auto& observed = merge_starts.witnesses->at(index);
                if (observed.merge_attempt_ordinal != index ||
                    !observed.canonical_snapshot.has_value() ||
                    observed.pending_snapshot.has_value() ||
                    observed.record.self_digest != merge_started_chain_[index].self_digest) {
                    return conflict();
                }
            }

            inventory.inventory->worker_attempt_records = std::move(*worker_attempts.witnesses);
            inventory.inventory->merge_started_records = std::move(*merge_starts.witnesses);
            inventory.inventory->chunk_terminal_failure_records =
                std::move(*terminal_failures.witnesses);
            return {
                .observation =
                    MergeWriterLifetimeObservation{
                        .inventory = std::move(*inventory.inventory),
                        .base_lock_identities = std::move(*base_locks.identities),
                        .private_leases = std::move(private_leases),
                        .directory = directory_entries,
                        .reserved = *reserved.record,
                        .owner = *owner.record,
                        .owned = *owned.record,
                        .reserved_identity = *reserved.identity,
                        .owner_identity = *owner.identity,
                        .owned_identity = *owned.identity,
                    },
                .diagnostic = {},
            };
        };

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        auto first = capture();
        if (!first.observation.has_value()) {
            return reject_lower_priority(std::move(first.diagnostic));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        auto confirmed = capture();
        if (!confirmed.observation.has_value()) {
            return reject_lower_priority(std::move(confirmed.diagnostic));
        }
        if (!observations_equal(*first.observation, *confirmed.observation)) {
            return reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return bindings;
        }
        return owned_by_current_process() ? DistributedSieveWaveStoreDiagnostic{}
                                          : process_mismatch();
    } catch (const std::bad_alloc&) {
        return diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                          std::make_error_code(std::errc::not_enough_memory));
    } catch (const private_lease::Failure& failure) {
        return diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                          failure.error ? failure.error : protocol_error());
    } catch (const std::system_error& failure) {
        return diagnostic(DistributedSieveWaveStoreStatus::io_failed, failure.code());
    } catch (...) {
        return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                          std::make_error_code(std::errc::io_error));
    }
#endif
}

DistributedSieveMergeStartedWriterMintV1::DistributedSieveMergeStartedWriterMintV1(
    DistributedSieveMergeStartedReceiptV1&& receipt,
    std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt> target_base_lock_duplicate, int root_fd,
    int directory_fd, std::filesystem::path base_path, std::filesystem::path private_directory,
    std::filesystem::path lock_path, std::array<std::uint64_t, 3> root_identity,
    std::array<std::uint64_t, 3> directory_identity, std::array<std::uint64_t, 2> lease_id,
    std::array<std::uint64_t, 3> owner_marker_identity,
    std::array<std::uint64_t, 3> owned_marker_identity, std::uint64_t creator_process_id) noexcept
    : receipt_(std::move(receipt)),
      target_base_lock_duplicate_(std::move(target_base_lock_duplicate)), root_fd_(root_fd),
      directory_fd_(directory_fd), base_path_(std::move(base_path)),
      private_directory_(std::move(private_directory)), lock_path_(std::move(lock_path)),
      root_identity_(root_identity), directory_identity_(directory_identity), lease_id_(lease_id),
      owner_marker_identity_(owner_marker_identity), owned_marker_identity_(owned_marker_identity),
      creator_process_id_(creator_process_id) {}

DistributedSieveMergeStartedWriterMintV1::DistributedSieveMergeStartedWriterMintV1(
    DistributedSieveMergeStartedWriterMintV1&& other) noexcept
    : receipt_(std::move(other.receipt_)),
      target_base_lock_duplicate_(std::move(other.target_base_lock_duplicate_)),
      root_fd_(std::exchange(other.root_fd_, -1)),
      directory_fd_(std::exchange(other.directory_fd_, -1)),
      base_path_(std::move(other.base_path_)),
      private_directory_(std::move(other.private_directory_)),
      lock_path_(std::move(other.lock_path_)), root_identity_(other.root_identity_),
      directory_identity_(other.directory_identity_), lease_id_(other.lease_id_),
      owner_marker_identity_(other.owner_marker_identity_),
      owned_marker_identity_(other.owned_marker_identity_),
      creator_process_id_(std::exchange(other.creator_process_id_, 0)),
      consumed_(std::exchange(other.consumed_, true)) {}

DistributedSieveMergeStartedWriterMintV1::~DistributedSieveMergeStartedWriterMintV1() noexcept {
    close_directory_noexcept();
}

void DistributedSieveMergeStartedWriterMintV1::close_directory_noexcept() noexcept {
#if !defined(_WIN32)
    if (directory_fd_ >= 0) {
        (void)::close(directory_fd_);
    }
#endif
    directory_fd_ = -1;
}

const WaveManifestV1& DistributedSieveMergeStartedWriterMintV1::manifest() const noexcept {
    return receipt_.wave_store_state_->manifest;
}

std::span<const MergeStartedV1>
DistributedSieveMergeStartedWriterMintV1::merge_started_chain() const noexcept {
    return receipt_.merge_started_chain_;
}

bool DistributedSieveMergeStartedWriterMintV1::writer_lifetime_stable() const noexcept {
    if (creator_process_id_ == 0 || !process_matches(creator_process_id_) || root_fd_ < 0 ||
        directory_fd_ < 0 || receipt_.wave_store_state_ == nullptr || base_path_.empty() ||
        private_directory_.empty() || lock_path_.empty()) {
        return false;
    }
    try {
        if (root_fd_ != receipt_.wave_store_state_->root_fd ||
            root_identity_ != relation_identity(receipt_.wave_store_state_->root_identity) ||
            !receipt_.final_witness_.directory_identity.has_value() ||
            !receipt_.final_witness_.owner_marker_identity.has_value() ||
            !receipt_.final_witness_.owned_marker_identity.has_value() ||
            directory_identity_ != relation_identity(*receipt_.final_witness_.directory_identity) ||
            lease_id_ != receipt_.final_witness_.lease_id ||
            owner_marker_identity_ !=
                relation_identity(*receipt_.final_witness_.owner_marker_identity) ||
            owned_marker_identity_ !=
                relation_identity(*receipt_.final_witness_.owned_marker_identity) ||
            base_path_ != receipt_.wave_store_state_->absolute_root /
                              receipt_.merge_generation_names_.private_directory_leaf / "corpus" ||
            private_directory_ != receipt_.wave_store_state_->absolute_root /
                                      receipt_.merge_generation_names_.private_directory_leaf ||
            lock_path_ != receipt_.wave_store_state_->absolute_root /
                              receipt_.merge_generation_names_.base_lock_leaf) {
            return false;
        }
        if (!consumed_) {
            if (target_base_lock_duplicate_ == nullptr ||
                !target_base_lock_duplicate_->owned_by_current_process() ||
                !target_base_lock_duplicate_->matches_exact_binding(
                    receipt_.merge_generation_names_.base_lock_leaf,
                    receipt_.base_lock_at_->identity())) {
                return false;
            }
            if (const auto duplicate = target_base_lock_duplicate_->revalidate();
                duplicate.status != DistributedSieveWaveStoreStatus::ready) {
                return false;
            }
        }
        if (const auto receipt = receipt_.revalidate_for_merge_writer_lifetime(directory_fd_);
            receipt.status != DistributedSieveWaveStoreStatus::ready) {
            return false;
        }
        if (!consumed_) {
            const auto duplicate = target_base_lock_duplicate_->revalidate();
            if (duplicate.status != DistributedSieveWaveStoreStatus::ready) {
                return false;
            }
        }
        return process_matches(creator_process_id_);
    } catch (...) {
        return false;
    }
}

DistributedSieveMergeStartedWriterMintResultV1 consume_distributed_sieve_merge_started_writer_v1(
    DistributedSieveMergeStartedReceiptV1&& receipt) noexcept {
    auto consumed = std::move(receipt);
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic failure) {
        if (failure.status == DistributedSieveWaveStoreStatus::ready) {
            failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        }
        return DistributedSieveMergeStartedWriterMintResultV1{
            .mint = std::nullopt,
            .diagnostic = std::move(failure),
        };
    };
    if (!consumed.owned_by_current_process()) {
        return fail_with(process_mismatch());
    }
    if (const auto initial = consumed.revalidate();
        initial.status != DistributedSieveWaveStoreStatus::ready) {
        return fail_with(initial);
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    return fail_with(
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
    try {
        auto directory = inspect_private_lease_directory_at(
            consumed.wave_store_state_->root_fd,
            consumed.merge_generation_names_.private_directory_leaf, consumed.creator_process_id_);
        if (!directory) {
            return fail_with(std::move(directory.diagnostic));
        }
        const auto owner_only = [](const PrivateLeaseDirectoryInventory& inventory) noexcept {
            return inventory.owner && !inventory.owner_pending && !inventory.work_package_residue &&
                   !inventory.corpus_index && !inventory.corpus_data && !inventory.worker_handoff &&
                   !inventory.worker_handoff_pending;
        };
        if (!consumed.final_witness_.directory_identity.has_value() ||
            *directory.identity != *consumed.final_witness_.directory_identity ||
            !owner_only(*directory.inventory)) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (const auto bound = validate_private_lease_directory_binding(
                consumed.wave_store_state_->root_fd, directory.directory.get(),
                consumed.merge_generation_names_.private_directory_leaf,
                consumed.creator_process_id_, *directory.identity);
            bound.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(bound);
        }
        if (const auto still_preactive = consumed.revalidate();
            still_preactive.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(still_preactive);
        }

        DistributedSieveWaveStoreDiagnostic duplicate_diagnostic;
        auto target_duplicate =
            consumed.base_lock_at_->duplicate_same_open_file_description(duplicate_diagnostic);
        if (target_duplicate == nullptr ||
            duplicate_diagnostic.status != DistributedSieveWaveStoreStatus::ready) {
            if (duplicate_diagnostic.status == DistributedSieveWaveStoreStatus::ready) {
                duplicate_diagnostic = diagnostic(
                    DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            }
            return fail_with(std::move(duplicate_diagnostic));
        }

        const std::filesystem::path exact_base_path =
            consumed.wave_store_state_->absolute_root /
            consumed.merge_generation_names_.private_directory_leaf / "corpus";
        const std::filesystem::path exact_private_directory =
            consumed.wave_store_state_->absolute_root /
            consumed.merge_generation_names_.private_directory_leaf;
        const std::filesystem::path exact_lock_path =
            consumed.wave_store_state_->absolute_root /
            consumed.merge_generation_names_.base_lock_leaf;
        const auto exact_paths = gnfs::relation::OOCCleanupTransaction::paths_for(exact_base_path);
        if (exact_paths.base_path != exact_base_path ||
            exact_paths.private_directory != exact_private_directory ||
            exact_paths.lock_path != exact_lock_path) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

        if (const auto lifetime =
                consumed.revalidate_for_merge_writer_lifetime(directory.directory.get());
            lifetime.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(lifetime);
        }
        auto final_directory = inspect_private_lease_directory_handle(directory.directory.get(),
                                                                      consumed.creator_process_id_);
        if (!final_directory || !owner_only(*final_directory.inventory)) {
            return fail_with(final_directory
                                 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(final_directory.diagnostic));
        }
        if (const auto final_preactive = consumed.revalidate();
            final_preactive.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(final_preactive);
        }
        if (const auto rebound = validate_private_lease_directory_binding(
                consumed.wave_store_state_->root_fd, directory.directory.get(),
                consumed.merge_generation_names_.private_directory_leaf,
                consumed.creator_process_id_, *directory.identity);
            rebound.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(rebound);
        }
        if (const auto duplicate = target_duplicate->revalidate();
            duplicate.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(duplicate);
        }

        const auto root_identity = relation_identity(consumed.wave_store_state_->root_identity);
        const auto directory_identity =
            relation_identity(*consumed.final_witness_.directory_identity);
        const auto lease_id = consumed.final_witness_.lease_id;
        const auto owner_marker_identity =
            relation_identity(*consumed.final_witness_.owner_marker_identity);
        const auto owned_marker_identity =
            relation_identity(*consumed.final_witness_.owned_marker_identity);
        const auto creator_process_id = consumed.creator_process_id_;
        const int root_fd = consumed.wave_store_state_->root_fd;
        // Complete every potentially throwing path copy while the exact
        // directory descriptor is still owned by the local RAII handle.
        auto frozen_base_path = exact_paths.base_path;
        auto frozen_private_directory = exact_paths.private_directory;
        auto frozen_lock_path = exact_paths.lock_path;
        const int transferred_directory_fd = directory.directory.release();

        DistributedSieveMergeStartedWriterMintV1 prepared(
            std::move(consumed), std::move(target_duplicate), root_fd, transferred_directory_fd,
            std::move(frozen_base_path), std::move(frozen_private_directory),
            std::move(frozen_lock_path), root_identity, directory_identity, lease_id,
            owner_marker_identity, owned_marker_identity, creator_process_id);
        std::optional<DistributedSieveMergeStartedWriterMintV1> mint;
        mint.emplace(std::move(prepared));
        return {
            .mint = std::move(mint),
            .diagnostic = {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (const private_lease::Failure& failure) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                    failure.error ? failure.error : protocol_error()));
    } catch (const std::system_error& failure) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::io_failed, failure.code()));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
#endif
}

DistributedSievePrivateLeaseRootClaimResult
recover_worker_attempt_private_lease(DistributedSievePrivateLeaseRootClaimResult&& claimed,
                                     DistributedSievePrivateLeaseRecoveryTestHooks hooks) noexcept {
    auto owned_claim = std::move(claimed);
    if (owned_claim.claim == nullptr ||
        owned_claim.diagnostic.status != DistributedSieveWaveStoreStatus::ready ||
        !owned_claim.claim->owned_by_current_process() ||
        owned_claim.claim->base_lock_acquisition_ !=
            DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting) {
        auto outcome = std::move(owned_claim.diagnostic);
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                 invalid_argument_error());
        }
        owned_claim.claim.reset();
        return {nullptr, std::move(outcome)};
    }

    if (const auto validated = owned_claim.claim->revalidate();
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        owned_claim.claim.reset();
        return {nullptr, validated};
    }
    if (!owned_claim.claim->worker_attempt_names_.has_value() ||
        !owned_claim.claim->expected_private_lease_base_lock_leaves_.has_value() ||
        !owned_claim.claim->expected_private_lease_reservation_witnesses_.has_value()) {
        owned_claim.claim.reset();
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }
    const auto recovery_target =
        std::lower_bound(owned_claim.claim->expected_private_lease_base_lock_leaves_->begin(),
                         owned_claim.claim->expected_private_lease_base_lock_leaves_->end(),
                         owned_claim.claim->worker_attempt_names_->base_lock_leaf);
    if (recovery_target == owned_claim.claim->expected_private_lease_base_lock_leaves_->end() ||
        *recovery_target != owned_claim.claim->worker_attempt_names_->base_lock_leaf) {
        owned_claim.claim.reset();
        return {nullptr,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    const auto recovery_target_index = static_cast<std::size_t>(std::distance(
        owned_claim.claim->expected_private_lease_base_lock_leaves_->begin(), recovery_target));
    if (recovery_target_index >=
        owned_claim.claim->expected_private_lease_reservation_witnesses_->size()) {
        owned_claim.claim.reset();
        return {nullptr,
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())};
    }
    const auto& recovery_witness =
        owned_claim.claim->expected_private_lease_reservation_witnesses_->at(recovery_target_index);
    if (recovery_witness.work_package_residue.has_value() ||
        recovery_witness.worker_handoff.has_value() ||
        recovery_witness.merge_prepared.has_value()) {
        owned_claim.claim.reset();
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                    protocol_error())};
    }

    bool completed = false;
    DistributedSieveWaveStoreDiagnostic outcome;
    try {
        {
            DistributedSieveFdPrivateLeaseRecoveryTarget target(*owned_claim.claim, hooks);
            const auto run = private_lease::run_private_lease_recovery_protocol(target);
            if (run == private_lease::PrivateLeaseRecoveryRunResult::Interrupted) {
                outcome = target.interruption_diagnostic();
            } else if (run == private_lease::PrivateLeaseRecoveryRunResult::Rejected) {
                outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                     invalid_argument_error());
            } else if (!target.completed()) {
                outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                     protocol_error());
            } else {
                completed = true;
            }
        }
    } catch (const DistributedSieveFdPrivateLeaseRecoveryTarget::Failure& failure) {
        outcome = failure.diagnostic;
    } catch (const std::bad_alloc&) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                             std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                             std::make_error_code(std::errc::io_error));
    }

    if (completed && outcome.status == DistributedSieveWaveStoreStatus::ready &&
        owned_claim.claim != nullptr && owned_claim.claim->owned_by_current_process()) {
        owned_claim.diagnostic = {};
        return owned_claim;
    }
    owned_claim.claim.reset();
    if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    return {nullptr, std::move(outcome)};
}

DistributedSievePrivateLeaseRootClaimResult recover_merge_generation_private_lease_v1(
    DistributedSievePrivateLeaseRootClaimResult&& claimed,
    DistributedSievePrivateLeaseRecoveryTestHooks hooks) noexcept {
    auto owned_claim = std::move(claimed);
    if (owned_claim.claim == nullptr ||
        owned_claim.diagnostic.status != DistributedSieveWaveStoreStatus::ready ||
        !owned_claim.claim->owned_by_current_process() ||
        owned_claim.claim->base_lock_acquisition_ !=
            DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting) {
        auto outcome = std::move(owned_claim.diagnostic);
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                 invalid_argument_error());
        }
        owned_claim.claim.reset();
        return {nullptr, std::move(outcome)};
    }

    if (const auto validated = owned_claim.claim->revalidate();
        validated.status != DistributedSieveWaveStoreStatus::ready) {
        owned_claim.claim.reset();
        return {nullptr, validated};
    }
    if (owned_claim.claim->worker_attempt_names_.has_value() ||
        !owned_claim.claim->merge_generation_names_.has_value() ||
        !owned_claim.claim->expected_private_lease_base_lock_leaves_.has_value() ||
        !owned_claim.claim->expected_private_lease_reservation_witnesses_.has_value()) {
        owned_claim.claim.reset();
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                    invalid_argument_error())};
    }
    const auto recovery_target =
        std::lower_bound(owned_claim.claim->expected_private_lease_base_lock_leaves_->begin(),
                         owned_claim.claim->expected_private_lease_base_lock_leaves_->end(),
                         owned_claim.claim->merge_generation_names_->base_lock_leaf);
    if (recovery_target == owned_claim.claim->expected_private_lease_base_lock_leaves_->end() ||
        *recovery_target != owned_claim.claim->merge_generation_names_->base_lock_leaf) {
        owned_claim.claim.reset();
        return {nullptr,
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())};
    }
    const auto recovery_target_index = static_cast<std::size_t>(std::distance(
        owned_claim.claim->expected_private_lease_base_lock_leaves_->begin(), recovery_target));
    if (recovery_target_index >=
        owned_claim.claim->expected_private_lease_reservation_witnesses_->size()) {
        owned_claim.claim.reset();
        return {nullptr,
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())};
    }
    const auto& witness =
        owned_claim.claim->expected_private_lease_reservation_witnesses_->at(recovery_target_index);
    if (witness.work_package_residue.has_value() || witness.worker_handoff.has_value() ||
        witness.merge_prepared.has_value()) {
        owned_claim.claim.reset();
        return {nullptr, diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                                    protocol_error())};
    }

    bool completed = false;
    DistributedSieveWaveStoreDiagnostic outcome;
    try {
        {
            DistributedSieveFdPrivateLeaseRecoveryTarget target(*owned_claim.claim, hooks);
            const auto run = private_lease::run_private_lease_recovery_protocol(target);
            if (run == private_lease::PrivateLeaseRecoveryRunResult::Interrupted) {
                outcome = target.interruption_diagnostic();
            } else if (run == private_lease::PrivateLeaseRecoveryRunResult::Rejected) {
                outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                     invalid_argument_error());
            } else if (!target.completed()) {
                outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                     protocol_error());
            } else {
                completed = true;
            }
        }
    } catch (const DistributedSieveFdPrivateLeaseRecoveryTarget::Failure& failure) {
        outcome = failure.diagnostic;
    } catch (const std::bad_alloc&) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                             std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                             std::make_error_code(std::errc::io_error));
    }

    if (completed && outcome.status == DistributedSieveWaveStoreStatus::ready &&
        owned_claim.claim != nullptr && owned_claim.claim->owned_by_current_process()) {
        owned_claim.diagnostic = {};
        return owned_claim;
    }
    owned_claim.claim.reset();
    if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    return {nullptr, std::move(outcome)};
}

DistributedSieveWorkerAttemptReconcileResult
reconcile_worker_attempt_started(DistributedSievePrivateLeaseRootClaimResult&& claimed,
                                 DistributedSieveWorkerAttemptReconcileTestHooks hooks) noexcept {
    auto owned_claim = std::move(claimed);
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome) {
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        }
        return DistributedSieveWorkerAttemptReconcileResult{
            std::nullopt,
            std::move(outcome),
            std::nullopt,
            std::nullopt,
        };
    };

    if (owned_claim.claim == nullptr ||
        owned_claim.diagnostic.status != DistributedSieveWaveStoreStatus::ready ||
        !owned_claim.claim->owned_by_current_process() ||
        owned_claim.claim->wave_store_state_ == nullptr ||
        owned_claim.claim->base_lock_at_ == nullptr ||
        !owned_claim.claim->worker_attempt_names_.has_value() ||
        !owned_claim.claim->expected_private_lease_base_lock_leaves_.has_value() ||
        !owned_claim.claim->expected_private_lease_base_lock_identities_.has_value() ||
        !owned_claim.claim->expected_private_lease_reservation_witnesses_.has_value() ||
        !owned_claim.claim->expected_worker_attempt_record_witnesses_.has_value() ||
        !owned_claim.claim->expected_merge_started_record_witnesses_.has_value() ||
        !owned_claim.claim->expected_chunk_terminal_failure_record_witnesses_.has_value() ||
        owned_claim.claim->base_lock_acquisition_ !=
            DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting) {
        auto outcome = std::move(owned_claim.diagnostic);
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                 invalid_argument_error());
        }
        return fail_with(std::move(outcome));
    }

#if defined(_WIN32)
    (void)hooks;
    return fail_with(
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
    try {
        auto& claim = *owned_claim.claim;
        const auto& state = *claim.wave_store_state_;
        const std::array<const DistributedSievePrivateLeaseBaseLockAt*, 1> held_base_locks{
            claim.base_lock_at_.get(),
        };
        const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{held_base_locks};
        const auto revalidate_bindings = [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
            return claim.revalidate_authority();
        };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };
        const auto parsed = parse_distributed_sieve_worker_attempt_leaf_v1(
            claim.worker_attempt_names_->canonical_record_leaf);
        if (!parsed.has_value() || parsed->pending ||
            parsed->attempt_ordinal >= state.manifest.max_worker_attempts) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

        const ChunkPlanV1* chunk = nullptr;
        for (const auto& candidate : state.manifest.chunks) {
            if (candidate.chunk_id == parsed->chunk_id) {
                chunk = &candidate;
                break;
            }
        }
        if (chunk == nullptr || chunk->sq_begin >= chunk->sq_end) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const auto names = distributed_sieve_worker_attempt_names_v1(
            chunk->relative_artifact_stem, parsed->chunk_id, parsed->attempt_ordinal);
        if (!names.has_value() || *names != *claim.worker_attempt_names_) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }

        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto initial = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory);
        if (!initial) {
            return fail_with(reject_lower_priority(std::move(initial.diagnostic)));
        }
        if (initial.inventory->private_lease_base_lock_leaves !=
                *claim.expected_private_lease_base_lock_leaves_ ||
            *initial.base_lock_identities != *claim.expected_private_lease_base_lock_identities_ ||
            *initial.private_lease_witnesses !=
                *claim.expected_private_lease_reservation_witnesses_ ||
            initial.inventory->worker_attempt_records !=
                *claim.expected_worker_attempt_record_witnesses_ ||
            initial.inventory->merge_started_records !=
                *claim.expected_merge_started_record_witnesses_ ||
            initial.inventory->chunk_terminal_failure_records !=
                *claim.expected_chunk_terminal_failure_record_witnesses_) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        const auto* initial_record =
            find_worker_attempt_record_witness(initial, parsed->chunk_id, parsed->attempt_ordinal);
        if (initial_record == nullptr) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        for (const auto& record : initial.inventory->worker_attempt_records) {
            if (record.chunk_id == parsed->chunk_id &&
                record.attempt_ordinal > parsed->attempt_ordinal) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
        }
        for (std::uint32_t ordinal = parsed->attempt_ordinal + 1U;
             ordinal < state.manifest.max_worker_attempts; ++ordinal) {
            const auto later_names = distributed_sieve_worker_attempt_names_v1(
                chunk->relative_artifact_stem, parsed->chunk_id, ordinal);
            if (!later_names.has_value()) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            protocol_error()));
            }
            if (std::binary_search(initial.inventory->private_lease_base_lock_leaves.begin(),
                                   initial.inventory->private_lease_base_lock_leaves.end(),
                                   later_names->base_lock_leaf)) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
        }

        const auto target_lock = std::lower_bound(
            initial.inventory->private_lease_base_lock_leaves.begin(),
            initial.inventory->private_lease_base_lock_leaves.end(), names->base_lock_leaf);
        if (target_lock == initial.inventory->private_lease_base_lock_leaves.end() ||
            *target_lock != names->base_lock_leaf) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_index = static_cast<std::size_t>(
            std::distance(initial.inventory->private_lease_base_lock_leaves.begin(), target_lock));
        if (target_index >= initial.private_lease_witnesses->size()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())));
        }
        auto recordless = worker_attempt_recordless_projection(initial, *names, parsed->chunk_id,
                                                               parsed->attempt_ordinal);
        if (!recordless.has_value()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto initial_shape =
            worker_attempt_record_prefix_shape(*recordless, initial, *names, parsed->chunk_id,
                                               parsed->attempt_ordinal, initial_record->bytes);
        if (!initial_shape.has_value() ||
            *initial_shape == WorkerAttemptRecordPrefixShape::absent) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        const auto initial_boundary = initial.private_lease_witnesses->at(target_index).boundary;
        if (*initial_shape == WorkerAttemptRecordPrefixShape::pending_only &&
            initial_boundary !=
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        const auto initial_record_bytes = initial_record->bytes;
        const auto initial_canonical_snapshot = initial_record->canonical_snapshot;
        const auto initial_pending_snapshot = initial_record->pending_snapshot;
        if ((*initial_shape == WorkerAttemptRecordPrefixShape::pending_only &&
             (!initial_pending_snapshot.has_value() || initial_canonical_snapshot.has_value())) ||
            ((*initial_shape == WorkerAttemptRecordPrefixShape::canonical_only ||
              *initial_shape == WorkerAttemptRecordPrefixShape::identical_dual) &&
             !initial_canonical_snapshot.has_value())) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }
        auto confirmed_initial = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory);
        if (!confirmed_initial) {
            return fail_with(reject_lower_priority(std::move(confirmed_initial.diagnostic)));
        }
        const auto confirmed_initial_shape = worker_attempt_record_prefix_shape(
            *recordless, confirmed_initial, *names, parsed->chunk_id, parsed->attempt_ordinal,
            initial_record_bytes);
        if (!confirmed_initial_shape.has_value() || *confirmed_initial_shape != *initial_shape ||
            !worker_attempt_start_observations_equal(initial, confirmed_initial)) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(authority);
        }

        const auto& initial_lease = initial.private_lease_witnesses->at(target_index);
        if (initial_lease.worker_handoff.has_value()) {
            const auto& terminal = *initial_lease.worker_handoff;
            if (*initial_shape != WorkerAttemptRecordPrefixShape::canonical_only ||
                initial_lease.boundary !=
                    DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
                terminal.handoff.chunk_id != parsed->chunk_id ||
                terminal.handoff.attempt_ordinal != parsed->attempt_ordinal ||
                terminal.handoff.attempt_started_digest != initial_record->record.self_digest ||
                terminal.handoff.lease != initial_record->record.lease) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            return {
                std::nullopt,
                diagnostic(DistributedSieveWaveStoreStatus::reconciliation_required,
                           protocol_error()),
                terminal,
                std::nullopt,
            };
        }

        if (initial_boundary ==
            DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) {
            if (!initial_lease.directory_identity.has_value() ||
                !initial_lease.reserved_marker_identity.has_value() ||
                !initial_lease.owner_marker_identity.has_value() ||
                !initial_lease.owned_marker_identity.has_value() ||
                initial_lease.lease_id == std::array<std::uint64_t, 2>{} ||
                initial.base_lock_identities->at(target_index) != claim.base_lock_at_->identity()) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            const NativeIdentityV1 cleanup_directory_identity = *initial_lease.directory_identity;
            const auto expected_cleanup_residue = initial_lease.work_package_residue;

            const bool pin_canonical = initial_canonical_snapshot.has_value();
            const auto& pinned_snapshot =
                pin_canonical ? *initial_canonical_snapshot : *initial_pending_snapshot;
            const std::string& pinned_leaf =
                pin_canonical ? names->canonical_record_leaf : names->pending_record_leaf;
            auto pinned_record = open_worker_attempt_canonical_record(
                state.root_fd, pinned_leaf, pinned_snapshot, initial_record_bytes,
                state.creator_process_id);
            if (!pinned_record) {
                return fail_with(reject_lower_priority(std::move(pinned_record.diagnostic)));
            }

            auto directory = inspect_private_lease_directory_at(
                state.root_fd, names->private_directory_leaf, state.creator_process_id);
            if (!directory || directory.identity != cleanup_directory_identity ||
                directory.inventory->work_package_residue != expected_cleanup_residue.has_value()) {
                return fail_with(reject_lower_priority(
                    directory ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                           protocol_error())
                              : std::move(directory.diagnostic)));
            }

            const auto validate_cleanup_bindings =
                [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
                if (const auto validated = claim.revalidate();
                    validated.status != DistributedSieveWaveStoreStatus::ready) {
                    return validated;
                }
                if (const auto pinned = validate_exact_worker_attempt_record_handle(
                        state.root_fd, pinned_record.canonical_record.get(), pinned_leaf,
                        pinned_snapshot, initial_record_bytes, state.creator_process_id);
                    pinned.status != DistributedSieveWaveStoreStatus::ready) {
                    return pinned;
                }
                if (const auto bound = validate_private_lease_directory_binding(
                        state.root_fd, directory.directory.get(), names->private_directory_leaf,
                        state.creator_process_id, cleanup_directory_identity);
                    bound.status != DistributedSieveWaveStoreStatus::ready) {
                    return bound;
                }
                return claim.revalidate_authority();
            };
            const auto validate_cleanup_successor_bindings =
                [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                if (const auto pinned = validate_exact_worker_attempt_record_handle(
                        state.root_fd, pinned_record.canonical_record.get(), pinned_leaf,
                        pinned_snapshot, initial_record_bytes, state.creator_process_id);
                    pinned.status != DistributedSieveWaveStoreStatus::ready) {
                    return pinned;
                }
                if (const auto bound = validate_private_lease_directory_binding(
                        state.root_fd, directory.directory.get(), names->private_directory_leaf,
                        state.creator_process_id, cleanup_directory_identity);
                    bound.status != DistributedSieveWaveStoreStatus::ready) {
                    return bound;
                }
                return claim.revalidate_authority();
            };
            const auto reject_after_cleanup_mutation =
                [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
                    if (const auto bindings = validate_cleanup_successor_bindings();
                        bindings.status != DistributedSieveWaveStoreStatus::ready) {
                        return bindings;
                    }
                    return lower;
                };

            if (const auto hooked = invoke_private_lease_base_lock_hook(
                    hooks.work_package_residue.before_reconciliation,
                    hooks.work_package_residue.context, state.creator_process_id);
                hooked.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(hooked);
            }
            bool inject_directory_sync_failure = false;
            if (hooks.work_package_residue.fail_before_directory_sync != nullptr) {
                if (!process_matches(state.creator_process_id)) {
                    return fail_with(process_mismatch());
                }
                inject_directory_sync_failure =
                    hooks.work_package_residue.fail_before_directory_sync(
                        hooks.work_package_residue.context);
                if (!process_matches(state.creator_process_id)) {
                    return fail_with(process_mismatch());
                }
            }
            if (const auto validated = validate_cleanup_bindings();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }

            std::optional<work_package_file::DistributedSieveWorkerWorkPackageResidueWitnessV1>
                full_residue;
            if (expected_cleanup_residue.has_value()) {
                auto inspected =
                    work_package_file::inspect_distributed_sieve_worker_work_package_residue_v1({
                        .borrowed_attempt_directory_handle = static_cast<
                            work_package_file::DistributedSieveWorkerWorkPackageNativeHandle>(
                            directory.directory.get()),
                        .expected_directory_identity = cleanup_directory_identity,
                        .observer_process_id = state.creator_process_id,
                    });
                if (!inspected) {
                    return fail_with(reject_lower_priority(
                        work_package_residue_reconciliation_inspection_failure(
                            inspected.diagnostic)));
                }
                const auto work_digest = distributed_sieve_work_digest(inspected.witness->identity);
                const auto manifest_status =
                    validate_manifest_work_identity(state.manifest, inspected.witness->identity);
                if (!work_digest || *work_digest.digest != state.manifest.work_sha256 ||
                    !manifest_status ||
                    inspected.witness->package.work_sha256 != state.manifest.work_sha256 ||
                    inspected.witness->package.total_bytes != inspected.witness->file_extent ||
                    compact_work_package_residue_witness(*inspected.witness) !=
                        *expected_cleanup_residue) {
                    const auto protocol_status =
                        !work_digest
                            ? work_digest.status
                            : (!manifest_status
                                   ? manifest_status
                                   : DistributedSieveProtocolStatus{
                                         .error = DistributedSieveProtocolError::digest_mismatch,
                                     });
                    const auto status =
                        protocol_status.error == DistributedSieveProtocolError::resource_exhausted
                            ? DistributedSieveWaveStoreStatus::resource_exhausted
                            : DistributedSieveWaveStoreStatus::namespace_conflict;
                    auto failure = diagnostic(status, protocol_error());
                    failure.protocol_status = protocol_status;
                    return fail_with(reject_lower_priority(std::move(failure)));
                }
                full_residue.emplace(std::move(*inspected.witness));
            }
            if (const auto validated = validate_cleanup_bindings();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }

            const auto carrier_stop_after = carrier_work_package_residue_reconciliation_fault_point(
                hooks.work_package_residue.stop_after);
            if (hooks.work_package_residue.stop_after.has_value() &&
                (!carrier_stop_after.has_value() ||
                 (!full_residue.has_value() &&
                  hooks.work_package_residue.stop_after ==
                      DistributedSieveWorkerWorkPackageResidueReconciliationFaultPoint::
                          AfterNameUnlinked))) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                            invalid_argument_error()));
            }
            const auto carrier_hooks = work_package_file::
                DistributedSieveWorkerWorkPackageResidueReconciliationTestHooksV1{
                    .before_unlink = nullptr,
                    .fail_before_directory_sync =
                        inject_directory_sync_failure
                            ? preselected_work_package_directory_sync_failure
                            : nullptr,
                    .after_directory_durable = nullptr,
                    .stop_after = carrier_stop_after,
                    .context = &inject_directory_sync_failure,
                };
            const auto residue_reconciled =
                work_package_file::reconcile_distributed_sieve_worker_work_package_residue_v1(
                    {
                        .borrowed_attempt_directory_handle = static_cast<
                            work_package_file::DistributedSieveWorkerWorkPackageNativeHandle>(
                            directory.directory.get()),
                        .expected_directory_identity = cleanup_directory_identity,
                        .reconciler_process_id = state.creator_process_id,
                        .expected_residue =
                            full_residue.has_value() ? std::addressof(*full_residue) : nullptr,
                    },
                    carrier_hooks);
            if (!residue_reconciled) {
                return fail_with(reject_after_cleanup_mutation(
                    work_package_residue_reconciliation_failure(residue_reconciled)));
            }
            const auto expected_disposition =
                full_residue.has_value()
                    ? work_package_file::
                          DistributedSieveWorkerWorkPackageResidueReconciliationDispositionV1::
                              removed
                    : work_package_file::
                          DistributedSieveWorkerWorkPackageResidueReconciliationDispositionV1::
                              confirmed_absent;
            if (residue_reconciled.disposition != expected_disposition ||
                residue_reconciled.fault_point.has_value()) {
                return fail_with(reject_after_cleanup_mutation(diagnostic(
                    DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())));
            }

            auto expected_cleanup_successor = std::move(confirmed_initial);
            expected_cleanup_successor.private_lease_witnesses->at(target_index)
                .work_package_residue.reset();
            const auto exact_cleanup_successor =
                [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                    return worker_attempt_start_observations_equal(expected_cleanup_successor,
                                                                   observed);
                };

            if (const auto bindings = validate_cleanup_successor_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(bindings);
            }
            auto first_cleanup_successor = capture_manifest_bound_inventory_witness(
                state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
                held_inventory);
            if (!first_cleanup_successor || !exact_cleanup_successor(first_cleanup_successor)) {
                return fail_with(reject_after_cleanup_mutation(
                    first_cleanup_successor
                        ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                     protocol_error())
                        : std::move(first_cleanup_successor.diagnostic)));
            }
            if (const auto bindings = validate_cleanup_successor_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(bindings);
            }
            if (const auto hooked = invoke_private_lease_base_lock_hook(
                    hooks.work_package_residue.after_first_successor_validation,
                    hooks.work_package_residue.context, state.creator_process_id);
                hooked.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(hooked);
            }
            if (const auto bindings = validate_cleanup_successor_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(bindings);
            }
            auto confirmed_cleanup_successor = capture_manifest_bound_inventory_witness(
                state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
                held_inventory);
            if (!confirmed_cleanup_successor ||
                !exact_cleanup_successor(confirmed_cleanup_successor) ||
                !worker_attempt_start_observations_equal(first_cleanup_successor,
                                                         confirmed_cleanup_successor)) {
                return fail_with(reject_after_cleanup_mutation(
                    confirmed_cleanup_successor
                        ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                     protocol_error())
                        : std::move(confirmed_cleanup_successor.diagnostic)));
            }
            if (const auto bindings = validate_cleanup_successor_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(bindings);
            }

            *claim.expected_private_lease_base_lock_leaves_ =
                confirmed_cleanup_successor.inventory->private_lease_base_lock_leaves;
            *claim.expected_private_lease_base_lock_identities_ =
                *confirmed_cleanup_successor.base_lock_identities;
            *claim.expected_private_lease_reservation_witnesses_ =
                *confirmed_cleanup_successor.private_lease_witnesses;
            *claim.expected_worker_attempt_record_witnesses_ =
                confirmed_cleanup_successor.inventory->worker_attempt_records;
            *claim.expected_merge_started_record_witnesses_ =
                confirmed_cleanup_successor.inventory->merge_started_records;
            *claim.expected_chunk_terminal_failure_record_witnesses_ =
                confirmed_cleanup_successor.inventory->chunk_terminal_failure_records;
            initial = std::move(confirmed_cleanup_successor);
            recordless = worker_attempt_recordless_projection(initial, *names, parsed->chunk_id,
                                                              parsed->attempt_ordinal);
            if (!recordless.has_value()) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            protocol_error()));
            }
            if (const auto validated = claim.revalidate();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }
            if (const auto pinned = validate_exact_worker_attempt_record_handle(
                    state.root_fd, pinned_record.canonical_record.get(), pinned_leaf,
                    pinned_snapshot, initial_record_bytes, state.creator_process_id);
                pinned.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(pinned);
            }
            if (const auto bound = validate_private_lease_directory_binding(
                    state.root_fd, directory.directory.get(), names->private_directory_leaf,
                    state.creator_process_id, cleanup_directory_identity);
                bound.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(bound);
            }
        }

        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.before_record_normalization, hooks.context, state.creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(hooked);
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        WorkerAttemptReconcileHookBridge bridge{
            .hooks = hooks,
            .creator_process_id = state.creator_process_id,
            .process_changed = false,
            .last_fault_point = std::nullopt,
        };
        const auto published = durable_record::publish_at(
            static_cast<durable_record::NativeHandle>(state.root_fd), names->pending_record_leaf,
            names->canonical_record_leaf, initial_record_bytes,
            durable_record::RecordTestHooks{
                .stop_after = bridge_worker_attempt_reconcile_hook,
                .context = &bridge,
            });
        auto publication_outcome =
            worker_attempt_reconcile_publication_diagnostic(published, bridge);
        const auto expected_disposition =
            *initial_shape == WorkerAttemptRecordPrefixShape::pending_only
                ? durable_record::RecordPublishDisposition::recovered_pending
                : durable_record::RecordPublishDisposition::confirmed_existing;
        const auto expected_source_snapshot =
            *initial_shape == WorkerAttemptRecordPrefixShape::pending_only
                ? initial_pending_snapshot
                : initial_canonical_snapshot;
        const auto attach_publication = [&](DistributedSieveWaveStoreDiagnostic higher) noexcept {
            higher.publication_status = publication_outcome.publication_status;
            higher.publication_disposition = publication_outcome.publication_disposition;
            higher.last_worker_attempt_reconcile_fault_point =
                publication_outcome.last_worker_attempt_reconcile_fault_point;
            return higher;
        };
        const auto exact_visible_prefix = [&](const ManifestBoundInventoryWitnessResult& observed,
                                              WorkerAttemptRecordPrefixShape shape) noexcept {
            const auto* witness = find_worker_attempt_record_witness(observed, parsed->chunk_id,
                                                                     parsed->attempt_ordinal);
            if (witness == nullptr) {
                return false;
            }
            const auto expected_canonical_snapshot = published.canonical_snapshot().has_value()
                                                         ? published.canonical_snapshot()
                                                         : expected_source_snapshot;
            if (shape == WorkerAttemptRecordPrefixShape::pending_only) {
                return !published.canonical_snapshot().has_value() &&
                       expected_source_snapshot.has_value() &&
                       witness->pending_snapshot == expected_source_snapshot;
            }
            if (shape != WorkerAttemptRecordPrefixShape::canonical_only &&
                shape != WorkerAttemptRecordPrefixShape::identical_dual) {
                return false;
            }
            if (!expected_canonical_snapshot.has_value() ||
                witness->canonical_snapshot != expected_canonical_snapshot) {
                return false;
            }
            return shape != WorkerAttemptRecordPrefixShape::identical_dual ||
                   (initial_pending_snapshot.has_value() &&
                    witness->pending_snapshot == initial_pending_snapshot);
        };
        const auto adjudicate_visible_prefix =
            [&](DistributedSieveWaveStoreDiagnostic lower,
                std::optional<WorkerAttemptRecordPrefixShape>& visible_shape) noexcept {
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(authority);
                }
                auto first = capture_manifest_bound_inventory_witness(
                    state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
                    held_inventory);
                if (!first) {
                    if (const auto authority = claim.revalidate_authority();
                        authority.status != DistributedSieveWaveStoreStatus::ready) {
                        return attach_publication(authority);
                    }
                    return attach_publication(std::move(first.diagnostic));
                }
                visible_shape = worker_attempt_record_prefix_shape(
                    *recordless, first, *names, parsed->chunk_id, parsed->attempt_ordinal,
                    initial_record_bytes);
                if (!visible_shape.has_value() || !exact_visible_prefix(first, *visible_shape)) {
                    return attach_publication(reject_lower_priority(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
                }
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(authority);
                }
                auto confirmed = capture_manifest_bound_inventory_witness(
                    state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
                    held_inventory);
                if (!confirmed) {
                    if (const auto authority = claim.revalidate_authority();
                        authority.status != DistributedSieveWaveStoreStatus::ready) {
                        return attach_publication(authority);
                    }
                    return attach_publication(std::move(confirmed.diagnostic));
                }
                const auto confirmed_shape = worker_attempt_record_prefix_shape(
                    *recordless, confirmed, *names, parsed->chunk_id, parsed->attempt_ordinal,
                    initial_record_bytes);
                if (!confirmed_shape.has_value() || *confirmed_shape != *visible_shape ||
                    !exact_visible_prefix(confirmed, *confirmed_shape) ||
                    !worker_attempt_start_observations_equal(first, confirmed)) {
                    return attach_publication(reject_lower_priority(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
                }
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(authority);
                }
                return lower;
            };

        const bool accepted_publication =
            published.status() == durable_record::RecordPublishStatus::durable &&
            published.disposition() == expected_disposition &&
            published.canonical_snapshot().has_value() &&
            expected_source_snapshot == published.canonical_snapshot();
        if (!accepted_publication) {
            std::optional<WorkerAttemptRecordPrefixShape> visible_shape;
            publication_outcome = adjudicate_visible_prefix(publication_outcome, visible_shape);
            if (published.status() == durable_record::RecordPublishStatus::durable &&
                published.disposition() == durable_record::RecordPublishDisposition::created &&
                publication_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                publication_outcome.status =
                    DistributedSieveWaveStoreStatus::reconciliation_required;
                publication_outcome.native_error = protocol_error();
            } else if (published.status() == durable_record::RecordPublishStatus::durable &&
                       publication_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                publication_outcome.status = DistributedSieveWaveStoreStatus::namespace_conflict;
                publication_outcome.native_error = protocol_error();
            }
            return fail_with(std::move(publication_outcome));
        }

        const auto exact_normalized =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                const auto shape = worker_attempt_record_prefix_shape(
                    *recordless, observed, *names, parsed->chunk_id, parsed->attempt_ordinal,
                    initial_record_bytes);
                if (!shape.has_value() ||
                    *shape != WorkerAttemptRecordPrefixShape::canonical_only) {
                    return false;
                }
                const auto* witness = find_worker_attempt_record_witness(observed, parsed->chunk_id,
                                                                         parsed->attempt_ordinal);
                return witness != nullptr &&
                       witness->canonical_snapshot == published.canonical_snapshot() &&
                       !witness->pending_snapshot.has_value();
            };

        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority));
        }
        auto first_normalized = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory);
        if (!first_normalized || !exact_normalized(first_normalized)) {
            return fail_with(attach_publication(reject_lower_priority(
                first_normalized ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(first_normalized.diagnostic))));
        }
        auto opened_record = open_worker_attempt_canonical_record(
            state.root_fd, names->canonical_record_leaf, *published.canonical_snapshot(),
            initial_record_bytes, state.creator_process_id);
        if (!opened_record) {
            return fail_with(
                attach_publication(reject_lower_priority(std::move(opened_record.diagnostic))));
        }
        const auto reject_after_pin = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (const auto authority = revalidate_bindings();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            if (const auto pinned = validate_exact_worker_attempt_record_handle(
                    state.root_fd, opened_record.canonical_record.get(),
                    names->canonical_record_leaf, *published.canonical_snapshot(),
                    initial_record_bytes, state.creator_process_id);
                pinned.status != DistributedSieveWaveStoreStatus::ready) {
                return pinned;
            }
            return lower;
        };
        if (const auto authority = claim.revalidate_authority();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority));
        }
        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.after_first_normalized_successor_validation, hooks.context,
                state.creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(hooked));
        }
        if (const auto authority = revalidate_bindings();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority));
        }
        if (const auto pinned = validate_exact_worker_attempt_record_handle(
                state.root_fd, opened_record.canonical_record.get(), names->canonical_record_leaf,
                *published.canonical_snapshot(), initial_record_bytes, state.creator_process_id);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(reject_after_pin(pinned)));
        }
        auto confirmed_normalized = capture_manifest_bound_inventory_witness(
            state.root_fd, state.manifest, state.absolute_root, state.creator_process_id,
            held_inventory);
        if (!confirmed_normalized || !exact_normalized(confirmed_normalized) ||
            !worker_attempt_start_observations_equal(first_normalized, confirmed_normalized)) {
            return fail_with(attach_publication(reject_after_pin(
                confirmed_normalized
                    ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                 protocol_error())
                    : std::move(confirmed_normalized.diagnostic))));
        }
        if (const auto pinned = validate_exact_worker_attempt_record_handle(
                state.root_fd, opened_record.canonical_record.get(), names->canonical_record_leaf,
                *published.canonical_snapshot(), initial_record_bytes, state.creator_process_id);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(reject_after_pin(pinned)));
        }
        if (const auto authority = revalidate_bindings();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority));
        }

        *claim.expected_private_lease_base_lock_leaves_ =
            confirmed_normalized.inventory->private_lease_base_lock_leaves;
        *claim.expected_private_lease_base_lock_identities_ =
            *confirmed_normalized.base_lock_identities;
        *claim.expected_private_lease_reservation_witnesses_ =
            *confirmed_normalized.private_lease_witnesses;
        *claim.expected_worker_attempt_record_witnesses_ =
            confirmed_normalized.inventory->worker_attempt_records;
        *claim.expected_merge_started_record_witnesses_ =
            confirmed_normalized.inventory->merge_started_records;
        *claim.expected_chunk_terminal_failure_record_witnesses_ =
            confirmed_normalized.inventory->chunk_terminal_failure_records;
        if (const auto authority = revalidate_bindings();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority));
        }
        if (const auto pinned = validate_exact_worker_attempt_record_handle(
                state.root_fd, opened_record.canonical_record.get(), names->canonical_record_leaf,
                *published.canonical_snapshot(), initial_record_bytes, state.creator_process_id);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(reject_after_pin(pinned)));
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(validated));
        }

        publication_outcome.last_worker_attempt_reconcile_fault_point =
            DistributedSieveWorkerAttemptReconcileFaultPoint::RecordNormalized;
        bool stop_after_normalized = false;
        if (!process_matches(state.creator_process_id)) {
            return fail_with(attach_publication(process_mismatch()));
        }
        if (hooks.stop_after != nullptr) {
            stop_after_normalized = hooks.stop_after(
                DistributedSieveWorkerAttemptReconcileFaultPoint::RecordNormalized, hooks.context);
        }
        if (!process_matches(state.creator_process_id)) {
            return fail_with(attach_publication(process_mismatch()));
        }
        if (const auto authority = revalidate_bindings();
            authority.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(authority));
        }
        if (const auto pinned = validate_exact_worker_attempt_record_handle(
                state.root_fd, opened_record.canonical_record.get(), names->canonical_record_leaf,
                *published.canonical_snapshot(), initial_record_bytes, state.creator_process_id);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(reject_after_pin(pinned)));
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(validated));
        }
        if (stop_after_normalized) {
            auto interrupted = diagnostic(DistributedSieveWaveStoreStatus::interrupted,
                                          std::make_error_code(std::errc::operation_canceled));
            return fail_with(attach_publication(reject_after_pin(std::move(interrupted))));
        }

        const auto* normalized_record = find_worker_attempt_record_witness(
            confirmed_normalized, parsed->chunk_id, parsed->attempt_ordinal);
        if (normalized_record == nullptr ||
            normalized_record->canonical_snapshot != published.canonical_snapshot() ||
            normalized_record->pending_snapshot.has_value()) {
            return fail_with(attach_publication(reject_after_pin(diagnostic(
                DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))));
        }
        DistributedSieveStartedAttemptCleanupAdmission admission(
            std::move(opened_record.canonical_record), *names, *normalized_record,
            state.creator_process_id);

        std::optional<DistributedSieveReconciledWorkerAttemptV1> reconciled;
        std::optional<DistributedSieveStartedAttemptCleanupAdmission> terminal_failure_pin;
        DistributedSieveWaveStoreDiagnostic recovery_outcome;
        try {
            {
                DistributedSieveFdPrivateLeaseRecoveryTarget target(claim, std::move(admission),
                                                                    hooks.recovery);
                const auto run = private_lease::run_private_lease_recovery_protocol(target);
                if (run == private_lease::PrivateLeaseRecoveryRunResult::Interrupted) {
                    recovery_outcome = target.interruption_diagnostic();
                } else if (run == private_lease::PrivateLeaseRecoveryRunResult::Rejected) {
                    recovery_outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                                  invalid_argument_error());
                } else if (!target.completed()) {
                    recovery_outcome = diagnostic(
                        DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
                } else {
                    reconciled = target.started_completion();
                    if (!reconciled.has_value()) {
                        recovery_outcome = diagnostic(
                            DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
                    } else if (!reconciled->next_attempt_ordinal.has_value()) {
                        auto retained = target.take_started_admission();
                        if (!retained.has_value()) {
                            recovery_outcome =
                                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                           protocol_error());
                        } else {
                            terminal_failure_pin.emplace(std::move(*retained));
                        }
                    }
                }
            }
        } catch (const DistributedSieveFdPrivateLeaseRecoveryTarget::Failure& failure) {
            recovery_outcome = failure.diagnostic;
        }
        if (!reconciled.has_value() ||
            recovery_outcome.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(std::move(recovery_outcome)));
        }

        publication_outcome.status = DistributedSieveWaveStoreStatus::ready;
        publication_outcome.native_error.clear();
        if (reconciled->next_attempt_ordinal.has_value()) {
            if (terminal_failure_pin.has_value()) {
                return fail_with(attach_publication(diagnostic(
                    DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())));
            }
            owned_claim.claim.reset();
            return {std::move(reconciled), std::move(publication_outcome), std::nullopt,
                    std::nullopt};
        }
        if (!terminal_failure_pin.has_value() ||
            parsed->attempt_ordinal + 1U != state.manifest.max_worker_attempts) {
            return fail_with(attach_publication(
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())));
        }

        std::vector<AttemptStartedV1> terminal_chain;
        terminal_chain.reserve(state.manifest.max_worker_attempts);
        for (const auto& witness : *claim.expected_worker_attempt_record_witnesses_) {
            if (witness.chunk_id != parsed->chunk_id) {
                continue;
            }
            if (!witness.canonical_snapshot.has_value() || witness.pending_snapshot.has_value() ||
                witness.attempt_ordinal != terminal_chain.size()) {
                return fail_with(attach_publication(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            terminal_chain.push_back(witness.record);
        }
        const auto terminal_names =
            distributed_sieve_chunk_terminal_failure_names_v1(parsed->chunk_id);
        const auto chain_status = validate_worker_attempt_chain(state.manifest, parsed->chunk_id,
                                                                terminal_chain, nullptr, nullptr);
        if (!terminal_names.has_value() ||
            terminal_chain.size() != state.manifest.max_worker_attempts ||
            terminal_chain.back().self_digest != reconciled->record.self_digest || !chain_status) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
            if (!chain_status) {
                failure.protocol_status = chain_status;
            }
            return fail_with(attach_publication(std::move(failure)));
        }
        if (const auto pinned = terminal_failure_pin->revalidate(state.root_fd);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(pinned));
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(validated));
        }

        std::optional<DistributedSieveChunkTerminalFailureAdmissionV1> terminal_admission;
        terminal_admission.emplace(DistributedSieveChunkTerminalFailureAdmissionV1(
            std::make_unique<DistributedSieveChunkTerminalFailureAdmissionV1::State>(
                std::move(owned_claim.claim), std::move(*terminal_failure_pin),
                std::move(terminal_chain), *chunk, *terminal_names, state.creator_process_id)));
        if (!terminal_admission->owned_by_current_process()) {
            return fail_with(attach_publication(
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error())));
        }
        return {std::move(reconciled), std::move(publication_outcome), std::nullopt,
                std::move(terminal_admission)};
    } catch (const DistributedSieveFdPrivateLeaseRecoveryTarget::Failure& failure) {
        return fail_with(failure.diagnostic);
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
#endif
}

DistributedSieveChunkTerminalFailurePublicationResultV1
publish_chunk_terminal_failure_v1(DistributedSieveChunkTerminalFailureAdmissionV1&& admission,
                                  DistributedSieveChunkTerminalFailureTestHooksV1 hooks) noexcept {
    auto consumed = std::move(admission.state_);
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome) {
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        }
        return DistributedSieveChunkTerminalFailurePublicationResultV1{
            std::nullopt,
            std::nullopt,
            std::move(outcome),
        };
    };

    if (consumed == nullptr || consumed->claim == nullptr || consumed->creator_process_id == 0 ||
        !process_matches(consumed->creator_process_id) ||
        !consumed->claim->owned_by_current_process()) {
        return fail_with(process_mismatch());
    }

    try {
#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)hooks;
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        auto& claim = *consumed->claim;
        if (claim.wave_store_state_ == nullptr ||
            claim.wave_store_state_->creator_process_id != consumed->creator_process_id ||
            claim.creator_process_id_ != consumed->creator_process_id ||
            claim.base_lock_acquisition_ !=
                DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting ||
            claim.base_lock_at_ == nullptr || !claim.worker_attempt_names_.has_value() ||
            !claim.expected_private_lease_base_lock_leaves_.has_value() ||
            !claim.expected_private_lease_base_lock_identities_.has_value() ||
            !claim.expected_private_lease_reservation_witnesses_.has_value() ||
            !claim.expected_worker_attempt_record_witnesses_.has_value() ||
            !claim.expected_merge_started_record_witnesses_.has_value() ||
            !claim.expected_chunk_terminal_failure_record_witnesses_.has_value()) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const auto& state = *claim.wave_store_state_;
        const auto parsed_attempt = parse_distributed_sieve_worker_attempt_leaf_v1(
            claim.worker_attempt_names_->canonical_record_leaf);
        const auto regenerated_attempt =
            parsed_attempt.has_value()
                ? distributed_sieve_worker_attempt_names_v1(consumed->chunk.relative_artifact_stem,
                                                            parsed_attempt->chunk_id,
                                                            parsed_attempt->attempt_ordinal)
                : std::nullopt;
        const auto regenerated_terminal =
            distributed_sieve_chunk_terminal_failure_names_v1(consumed->chunk.chunk_id);
        if (!parsed_attempt.has_value() || parsed_attempt->pending ||
            parsed_attempt->chunk_id != consumed->chunk.chunk_id ||
            parsed_attempt->attempt_ordinal + 1U != state.manifest.max_worker_attempts ||
            consumed->chunk.chunk_id >= state.manifest.chunks.size() ||
            state.manifest.chunks[consumed->chunk.chunk_id] != consumed->chunk ||
            consumed->chunk.sq_begin >= consumed->chunk.sq_end ||
            !regenerated_attempt.has_value() ||
            *regenerated_attempt != *claim.worker_attempt_names_ ||
            !regenerated_terminal.has_value() || *regenerated_terminal != consumed->names ||
            consumed->chain.size() != state.manifest.max_worker_attempts ||
            consumed->chain.empty() ||
            consumed->chain.back().self_digest !=
                consumed->pinned_attempt.record_witness_.record.self_digest ||
            consumed->pinned_attempt.worker_attempt_names_ != *claim.worker_attempt_names_) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

        const auto chain_digest =
            distributed_sieve_attempt_chain_digest(state.manifest.self_digest, consumed->chain);
        if (!chain_digest) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
            failure.protocol_status = chain_digest.status;
            return fail_with(std::move(failure));
        }
        ChunkTerminalFailureV1 terminal_failure{
            .manifest_digest = state.manifest.self_digest,
            .chunk_id = consumed->chunk.chunk_id,
            .sq_begin = consumed->chunk.sq_begin,
            .sq_end = consumed->chunk.sq_end,
            .exhausted_attempt_count = static_cast<std::uint32_t>(consumed->chain.size()),
            .last_attempt_digest = consumed->chain.back().self_digest,
            .predecessor_chain_digest = *chain_digest.digest,
            .reason = ChunkTerminalFailureReasonV1::attempt_budget_exhausted,
            .wait_facts =
                WorkerWaitFactsV1{
                    .kind = WorkerWaitFactKindV1::unavailable,
                    .exit_code = 0,
                    .signal = 0,
                    .native_error = 0,
                },
            .no_canonical_handoff_confirmed = true,
            .exact_attempt_lease_absent_confirmed = true,
            .next_sq_index = consumed->chunk.sq_begin,
            .processed_sq_count = 0,
            .statistics = {},
            .self_digest = {},
        };
        DistributedSieveProtocolRecordV1 protocol_record(std::move(terminal_failure));
        if (const auto sealed = seal_distributed_sieve_record(protocol_record); !sealed) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            failure.protocol_status = sealed;
            return fail_with(std::move(failure));
        }
        auto encoded = encode_distributed_sieve_record(protocol_record);
        if (!encoded) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            failure.protocol_status = encoded.status;
            return fail_with(std::move(failure));
        }
        terminal_failure = std::move(std::get<ChunkTerminalFailureV1>(protocol_record));
        if (const auto chain_status =
                validate_worker_attempt_chain(state.manifest, consumed->chunk.chunk_id,
                                              consumed->chain, nullptr, &terminal_failure);
            !chain_status) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error());
            failure.protocol_status = chain_status;
            return fail_with(std::move(failure));
        }

        const std::array<const DistributedSievePrivateLeaseBaseLockAt*, 1> held_base_locks{
            claim.base_lock_at_.get()};
        const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{held_base_locks};
        const auto revalidate_bindings = [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            if (const auto pinned = consumed->pinned_attempt.revalidate(state.root_fd);
                pinned.status != DistributedSieveWaveStoreStatus::ready) {
                if (const auto authority = claim.revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    return authority;
                }
                return pinned;
            }
            return claim.revalidate_authority();
        };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };
        const auto capture_inventory = [&]() noexcept {
            return capture_manifest_bound_inventory_witness(
                state.root_fd, state.manifest, state.absolute_root, consumed->creator_process_id,
                held_inventory);
        };

        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        if (const auto pinned = consumed->pinned_attempt.revalidate(state.root_fd);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(reject_lower_priority(pinned));
        }
        auto predecessor = capture_inventory();
        if (!predecessor) {
            return fail_with(reject_lower_priority(std::move(predecessor.diagnostic)));
        }
        if (predecessor.inventory->private_lease_base_lock_leaves !=
                *claim.expected_private_lease_base_lock_leaves_ ||
            *predecessor.base_lock_identities !=
                *claim.expected_private_lease_base_lock_identities_ ||
            *predecessor.private_lease_witnesses !=
                *claim.expected_private_lease_reservation_witnesses_ ||
            predecessor.inventory->worker_attempt_records !=
                *claim.expected_worker_attempt_record_witnesses_ ||
            predecessor.inventory->merge_started_records !=
                *claim.expected_merge_started_record_witnesses_ ||
            predecessor.inventory->chunk_terminal_failure_records !=
                *claim.expected_chunk_terminal_failure_record_witnesses_) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_position =
            std::lower_bound(predecessor.inventory->private_lease_base_lock_leaves.begin(),
                             predecessor.inventory->private_lease_base_lock_leaves.end(),
                             claim.worker_attempt_names_->base_lock_leaf);
        if (target_position == predecessor.inventory->private_lease_base_lock_leaves.end() ||
            *target_position != claim.worker_attempt_names_->base_lock_leaf) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_index = static_cast<std::size_t>(std::distance(
            predecessor.inventory->private_lease_base_lock_leaves.begin(), target_position));
        const auto& target_lease = predecessor.private_lease_witnesses->at(target_index);
        if (target_lease.boundary !=
                DistributedSievePrivateLeaseReservationBoundary::PermitAcquired ||
            target_lease.worker_handoff.has_value() ||
            target_lease.work_package_residue.has_value() ||
            target_lease.merge_prepared.has_value() ||
            predecessor.base_lock_identities->at(target_index) !=
                claim.expected_private_lease_base_lock_identities_->at(target_index)) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto initial_shape = chunk_terminal_failure_record_prefix_shape(
            predecessor, predecessor, consumed->names, consumed->chunk.chunk_id, *encoded.bytes);
        if (!initial_shape.has_value()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto* initial_terminal_witness =
            find_chunk_terminal_failure_record_witness(predecessor, consumed->chunk.chunk_id);

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(bindings);
        }
        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.before_record_publication, hooks.context, consumed->creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(hooked);
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        if (const auto pinned = consumed->pinned_attempt.revalidate(state.root_fd);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(reject_lower_priority(pinned));
        }

        ChunkTerminalFailureHookBridge bridge{
            .hooks = hooks,
            .creator_process_id = consumed->creator_process_id,
            .process_changed = false,
            .last_fault_point = std::nullopt,
        };
        const auto published =
            durable_record::publish_at(static_cast<durable_record::NativeHandle>(state.root_fd),
                                       consumed->names.pending_record_leaf,
                                       consumed->names.canonical_record_leaf, *encoded.bytes,
                                       durable_record::RecordTestHooks{
                                           .stop_after = bridge_chunk_terminal_failure_hook,
                                           .context = &bridge,
                                       });
        auto publication_outcome = chunk_terminal_failure_publication_diagnostic(published, bridge);
        const bool expected_publication_route = [&]() noexcept {
            if (!published.canonical_snapshot().has_value()) {
                return false;
            }
            switch (*initial_shape) {
            case ChunkTerminalFailureRecordPrefixShape::absent:
                return initial_terminal_witness == nullptr &&
                       published.disposition() == durable_record::RecordPublishDisposition::created;
            case ChunkTerminalFailureRecordPrefixShape::pending_only:
                return initial_terminal_witness != nullptr &&
                       !initial_terminal_witness->canonical_snapshot.has_value() &&
                       initial_terminal_witness->pending_snapshot ==
                           published.canonical_snapshot() &&
                       published.disposition() ==
                           durable_record::RecordPublishDisposition::recovered_pending;
            case ChunkTerminalFailureRecordPrefixShape::canonical_only:
            case ChunkTerminalFailureRecordPrefixShape::identical_dual:
                return initial_terminal_witness != nullptr &&
                       initial_terminal_witness->canonical_snapshot ==
                           published.canonical_snapshot() &&
                       published.disposition() ==
                           durable_record::RecordPublishDisposition::confirmed_existing;
            }
            return false;
        }();
        const auto attach_publication = [&](DistributedSieveWaveStoreDiagnostic higher) noexcept {
            higher.publication_status = publication_outcome.publication_status;
            higher.publication_disposition = publication_outcome.publication_disposition;
            higher.last_chunk_terminal_failure_fault_point =
                publication_outcome.last_chunk_terminal_failure_fault_point;
            return higher;
        };
        const auto published_snapshot_matches =
            [&](const ManifestBoundInventoryWitnessResult& observed,
                ChunkTerminalFailureRecordPrefixShape shape) noexcept {
                if (!published.canonical_snapshot().has_value()) {
                    return true;
                }
                if (shape != ChunkTerminalFailureRecordPrefixShape::canonical_only &&
                    shape != ChunkTerminalFailureRecordPrefixShape::identical_dual) {
                    return false;
                }
                const auto* witness =
                    find_chunk_terminal_failure_record_witness(observed, consumed->chunk.chunk_id);
                return witness != nullptr &&
                       witness->canonical_snapshot == published.canonical_snapshot();
            };
        const auto visible_prefix_preserves_source =
            [&](const ManifestBoundInventoryWitnessResult& observed,
                ChunkTerminalFailureRecordPrefixShape shape) noexcept {
                const auto* witness =
                    find_chunk_terminal_failure_record_witness(observed, consumed->chunk.chunk_id);
                switch (*initial_shape) {
                case ChunkTerminalFailureRecordPrefixShape::absent:
                    if (published.disposition() == durable_record::RecordPublishDisposition::none) {
                        return shape == ChunkTerminalFailureRecordPrefixShape::absent;
                    }
                    return published.disposition() ==
                               durable_record::RecordPublishDisposition::created &&
                           (shape == ChunkTerminalFailureRecordPrefixShape::absent ||
                            shape == ChunkTerminalFailureRecordPrefixShape::pending_only ||
                            shape == ChunkTerminalFailureRecordPrefixShape::canonical_only);
                case ChunkTerminalFailureRecordPrefixShape::pending_only:
                    if (initial_terminal_witness == nullptr ||
                        !initial_terminal_witness->pending_snapshot.has_value() ||
                        initial_terminal_witness->canonical_snapshot.has_value() ||
                        (published.disposition() !=
                             durable_record::RecordPublishDisposition::none &&
                         published.disposition() !=
                             durable_record::RecordPublishDisposition::recovered_pending)) {
                        return false;
                    }
                    if (shape == ChunkTerminalFailureRecordPrefixShape::pending_only) {
                        return witness != nullptr &&
                               witness->pending_snapshot ==
                                   initial_terminal_witness->pending_snapshot &&
                               !witness->canonical_snapshot.has_value();
                    }
                    if (shape == ChunkTerminalFailureRecordPrefixShape::canonical_only) {
                        return witness != nullptr &&
                               witness->canonical_snapshot ==
                                   initial_terminal_witness->pending_snapshot &&
                               !witness->pending_snapshot.has_value();
                    }
                    return false;
                case ChunkTerminalFailureRecordPrefixShape::canonical_only:
                    return initial_terminal_witness != nullptr &&
                           initial_terminal_witness->canonical_snapshot.has_value() &&
                           published.disposition() !=
                               durable_record::RecordPublishDisposition::created &&
                           published.disposition() !=
                               durable_record::RecordPublishDisposition::recovered_pending &&
                           shape == ChunkTerminalFailureRecordPrefixShape::canonical_only &&
                           witness != nullptr &&
                           witness->canonical_snapshot ==
                               initial_terminal_witness->canonical_snapshot &&
                           !witness->pending_snapshot.has_value();
                case ChunkTerminalFailureRecordPrefixShape::identical_dual:
                    if (initial_terminal_witness == nullptr ||
                        !initial_terminal_witness->canonical_snapshot.has_value() ||
                        !initial_terminal_witness->pending_snapshot.has_value() ||
                        published.disposition() ==
                            durable_record::RecordPublishDisposition::created ||
                        published.disposition() ==
                            durable_record::RecordPublishDisposition::recovered_pending ||
                        witness == nullptr) {
                        return false;
                    }
                    if (shape == ChunkTerminalFailureRecordPrefixShape::identical_dual) {
                        return witness->canonical_snapshot ==
                                   initial_terminal_witness->canonical_snapshot &&
                               witness->pending_snapshot ==
                                   initial_terminal_witness->pending_snapshot;
                    }
                    return shape == ChunkTerminalFailureRecordPrefixShape::canonical_only &&
                           witness->canonical_snapshot ==
                               initial_terminal_witness->canonical_snapshot &&
                           !witness->pending_snapshot.has_value();
                }
                return false;
            };
        const auto adjudicate_visible_prefix = [&](DistributedSieveWaveStoreDiagnostic lower) {
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return attach_publication(bindings);
            }
            auto first = capture_inventory();
            if (!first) {
                return attach_publication(reject_lower_priority(std::move(first.diagnostic)));
            }
            const auto shape = chunk_terminal_failure_record_prefix_shape(
                predecessor, first, consumed->names, consumed->chunk.chunk_id, *encoded.bytes);
            if (!shape.has_value() || !published_snapshot_matches(first, *shape) ||
                !visible_prefix_preserves_source(first, *shape)) {
                return attach_publication(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return attach_publication(bindings);
            }
            auto confirmed = capture_inventory();
            if (!confirmed) {
                return attach_publication(reject_lower_priority(std::move(confirmed.diagnostic)));
            }
            const auto confirmed_shape = chunk_terminal_failure_record_prefix_shape(
                predecessor, confirmed, consumed->names, consumed->chunk.chunk_id, *encoded.bytes);
            if (!confirmed_shape.has_value() || *confirmed_shape != *shape ||
                !published_snapshot_matches(confirmed, *confirmed_shape) ||
                !visible_prefix_preserves_source(confirmed, *confirmed_shape) ||
                !worker_attempt_start_observations_equal(first, confirmed)) {
                return attach_publication(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return attach_publication(bindings);
            }
            return lower;
        };

        if (published.status() != durable_record::RecordPublishStatus::durable ||
            publication_outcome.status != DistributedSieveWaveStoreStatus::ready ||
            !published.canonical_snapshot().has_value() || !expected_publication_route) {
            if (published.status() == durable_record::RecordPublishStatus::durable &&
                publication_outcome.status == DistributedSieveWaveStoreStatus::ready &&
                !expected_publication_route) {
                publication_outcome = attach_publication(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            return fail_with(adjudicate_visible_prefix(std::move(publication_outcome)));
        }

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings));
        }
        auto first_successor = capture_inventory();
        if (!first_successor) {
            return fail_with(
                attach_publication(reject_lower_priority(std::move(first_successor.diagnostic))));
        }
        const auto first_shape = chunk_terminal_failure_record_prefix_shape(
            predecessor, first_successor, consumed->names, consumed->chunk.chunk_id,
            *encoded.bytes);
        const auto* first_witness =
            find_chunk_terminal_failure_record_witness(first_successor, consumed->chunk.chunk_id);
        if (!first_shape.has_value() ||
            *first_shape != ChunkTerminalFailureRecordPrefixShape::canonical_only ||
            !visible_prefix_preserves_source(first_successor, *first_shape) ||
            first_witness == nullptr ||
            first_witness->canonical_snapshot != published.canonical_snapshot() ||
            first_witness->pending_snapshot.has_value()) {
            return fail_with(attach_publication(reject_lower_priority(diagnostic(
                DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings));
        }
        if (const auto hooked =
                invoke_private_lease_base_lock_hook(hooks.after_first_successor_validation,
                                                    hooks.context, consumed->creator_process_id);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(hooked));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings));
        }
        auto confirmed_successor = capture_inventory();
        if (!confirmed_successor) {
            return fail_with(attach_publication(
                reject_lower_priority(std::move(confirmed_successor.diagnostic))));
        }
        const auto confirmed_shape = chunk_terminal_failure_record_prefix_shape(
            predecessor, confirmed_successor, consumed->names, consumed->chunk.chunk_id,
            *encoded.bytes);
        const auto* confirmed_witness = find_chunk_terminal_failure_record_witness(
            confirmed_successor, consumed->chunk.chunk_id);
        if (!confirmed_shape.has_value() ||
            *confirmed_shape != ChunkTerminalFailureRecordPrefixShape::canonical_only ||
            !visible_prefix_preserves_source(confirmed_successor, *confirmed_shape) ||
            confirmed_witness == nullptr ||
            confirmed_witness->canonical_snapshot != published.canonical_snapshot() ||
            confirmed_witness->pending_snapshot.has_value() ||
            !worker_attempt_start_observations_equal(first_successor, confirmed_successor)) {
            return fail_with(attach_publication(reject_lower_priority(diagnostic(
                DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings));
        }

        *claim.expected_chunk_terminal_failure_record_witnesses_ =
            confirmed_successor.inventory->chunk_terminal_failure_records;
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(validated));
        }
        if (const auto pinned = consumed->pinned_attempt.revalidate(state.root_fd);
            pinned.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(reject_lower_priority(pinned)));
        }

        publication_outcome.status = DistributedSieveWaveStoreStatus::ready;
        publication_outcome.native_error.clear();
        return {
            std::move(terminal_failure),
            *published.canonical_snapshot(),
            std::move(publication_outcome),
        };
#endif
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

DistributedSieveMergeLeaseReservationResultV1 reserve_distributed_sieve_merge_generation_v1(
    DistributedSieveWaveStore& store, std::uint32_t merge_attempt_ordinal,
    std::span<const TerminalChunkInputV1> terminal_inputs, std::uint32_t merge_policy_version,
    DistributedSievePrivateLeaseProtocolTestHooks hooks,
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome) {
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        }
        return DistributedSieveMergeLeaseReservationResultV1{
            .receipt = std::nullopt,
            .diagnostic = std::move(outcome),
        };
    };
    if (store.state_ == nullptr || !process_matches(store.state_->creator_process_id)) {
        return fail_with(store.state_ == nullptr
                             ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                          invalid_argument_error())
                             : process_mismatch());
    }
    if (merge_attempt_ordinal >= store.state_->manifest.max_merge_build_attempts ||
        merge_policy_version != store.state_->manifest.merge_policy_version ||
        terminal_inputs.size() != store.state_->manifest.chunks.size()) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }

    try {
        std::vector<TerminalChunkInputV1> frozen_inputs(terminal_inputs.begin(),
                                                        terminal_inputs.end());
        auto claimed = store.claim_merge_generation_private_lease_root(
            merge_attempt_ordinal, DistributedSieveWaveStore::AttemptBaseLockExpectation::absent,
            {}, held_worker_handoffs);
        if (!claimed) {
            if (claimed.diagnostic.status != DistributedSieveWaveStoreStatus::namespace_conflict &&
                claimed.diagnostic.status !=
                    DistributedSieveWaveStoreStatus::reconciliation_required) {
                return fail_with(std::move(claimed.diagnostic));
            }
            auto opened = store.claim_merge_generation_private_lease_root(
                merge_attempt_ordinal,
                DistributedSieveWaveStore::AttemptBaseLockExpectation::present, {},
                held_worker_handoffs);
            if (!opened) {
                return fail_with(std::move(opened.diagnostic));
            }
            if (opened.claim == nullptr ||
                !opened.claim->expected_merge_started_record_witnesses_.has_value() ||
                opened.claim->expected_merge_started_record_witnesses_->size() !=
                    merge_attempt_ordinal) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                            protocol_error()));
            }
            claimed = recover_merge_generation_private_lease_v1(std::move(opened));
            if (!claimed) {
                return fail_with(std::move(claimed.diagnostic));
            }
        }
        if (claimed.claim == nullptr || !claimed.claim->owned_by_current_process() ||
            claimed.claim->worker_attempt_names_.has_value() ||
            !claimed.claim->merge_generation_names_.has_value() ||
            !claimed.claim->expected_private_lease_reservation_witnesses_.has_value() ||
            !claimed.claim->expected_worker_attempt_record_witnesses_.has_value() ||
            !claimed.claim->expected_merge_started_record_witnesses_.has_value() ||
            !claimed.claim->expected_chunk_terminal_failure_record_witnesses_.has_value() ||
            !claimed.claim->expected_chunk_terminal_failure_record_witnesses_->empty()) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const auto projection = validate_merge_terminal_projection_v1(
            store.state_->manifest, *claimed.claim->expected_worker_attempt_record_witnesses_,
            *claimed.claim->expected_private_lease_reservation_witnesses_, frozen_inputs);
        if (!projection) {
            return fail_with(worker_attempt_protocol_conflict(projection));
        }
        for (const auto& existing : *claimed.claim->expected_merge_started_record_witnesses_) {
            if (!existing.canonical_snapshot.has_value() || existing.pending_snapshot.has_value()) {
                return fail_with(diagnostic(
                    DistributedSieveWaveStoreStatus::reconciliation_required, protocol_error()));
            }
        }
        if (const auto validated = claimed.claim->revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        std::vector<std::unique_ptr<DistributedSievePrivateLeaseBaseLockAt>>
            retained_worker_base_locks;
        std::vector<const DistributedSievePrivateLeaseBaseLockAt*> retained_worker_base_lock_views;
        retained_worker_base_locks.reserve(claimed.claim->borrowed_worker_base_locks_.size());
        retained_worker_base_lock_views.reserve(claimed.claim->borrowed_worker_base_locks_.size());
        for (const auto* borrowed : claimed.claim->borrowed_worker_base_locks_) {
            if (borrowed == nullptr) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                            protocol_error()));
            }
            DistributedSieveWaveStoreDiagnostic duplicate_outcome;
            auto retained = borrowed->duplicate_same_open_file_description(duplicate_outcome);
            if (retained == nullptr ||
                duplicate_outcome.status != DistributedSieveWaveStoreStatus::ready) {
                if (duplicate_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                    duplicate_outcome = diagnostic(
                        DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
                }
                return fail_with(std::move(duplicate_outcome));
            }
            retained_worker_base_lock_views.push_back(retained.get());
            retained_worker_base_locks.push_back(std::move(retained));
        }
        if (const auto validated = claimed.claim->revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        std::optional<DistributedSieveMergeLeaseReservationReceiptV1> receipt;
        DistributedSieveWaveStoreDiagnostic outcome;
        {
            DistributedSieveFdPrivateLeaseReservationTarget target(*claimed.claim, hooks);
            const auto run = private_lease::run_private_lease_reservation_protocol(target);
            if (run == private_lease::PrivateLeaseReservationRunResult::Interrupted) {
                outcome = target.interruption_diagnostic();
            } else {
                auto completed = target.take_completion();
                DistributedSieveMergeLeaseReservationReceiptV1 prepared(
                    claimed.claim->wave_store_state_,
                    std::move(*claimed.claim->merge_generation_names_),
                    completed.base_lock_identity, std::move(completed.final_witness),
                    std::move(frozen_inputs), merge_policy_version,
                    std::move(retained_worker_base_locks),
                    std::move(retained_worker_base_lock_views), claimed.claim->creator_process_id_);
                receipt.emplace(std::move(prepared));
            }
        }
        claimed.claim.reset();
        if (receipt.has_value() && outcome.status == DistributedSieveWaveStoreStatus::ready) {
            return {
                .receipt = std::move(receipt),
                .diagnostic = std::move(outcome),
            };
        }
        receipt.reset();
        return fail_with(std::move(outcome));
    } catch (const DistributedSieveFdPrivateLeaseReservationTarget::Failure& failure) {
        return fail_with(failure.diagnostic);
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

DistributedSievePrivateLeaseReservationResult
reserve_worker_attempt_private_lease(DistributedSievePrivateLeaseRootClaimResult&& claimed,
                                     DistributedSievePrivateLeaseProtocolTestHooks hooks) noexcept {
    auto owned_claim = std::move(claimed);
    if (owned_claim.claim == nullptr ||
        owned_claim.diagnostic.status != DistributedSieveWaveStoreStatus::ready ||
        !owned_claim.claim->owned_by_current_process() ||
        !owned_claim.claim->worker_attempt_names_.has_value() ||
        owned_claim.claim->merge_generation_names_.has_value()) {
        auto outcome = std::move(owned_claim.diagnostic);
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                 invalid_argument_error());
        }
        owned_claim.claim.reset();
        return {std::nullopt, std::move(outcome)};
    }

    std::optional<DistributedSievePrivateLeaseReservationReceipt> receipt;
    DistributedSieveWaveStoreDiagnostic outcome;
    try {
        {
            DistributedSieveFdPrivateLeaseReservationTarget target(*owned_claim.claim, hooks);
            const auto run = private_lease::run_private_lease_reservation_protocol(target);
            if (run == private_lease::PrivateLeaseReservationRunResult::Interrupted) {
                outcome = target.interruption_diagnostic();
            } else {
                auto completed = target.take_completion();
                DistributedSievePrivateLeaseReservationReceipt prepared(
                    owned_claim.claim->wave_store_state_,
                    std::move(*owned_claim.claim->worker_attempt_names_),
                    completed.base_lock_identity, std::move(completed.final_witness),
                    owned_claim.claim->creator_process_id_);
                receipt.emplace(std::move(prepared));
            }
        }
    } catch (const DistributedSieveFdPrivateLeaseReservationTarget::Failure& failure) {
        outcome = failure.diagnostic;
    } catch (const std::bad_alloc&) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                             std::make_error_code(std::errc::not_enough_memory));
    } catch (...) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                             std::make_error_code(std::errc::io_error));
    }

    // The reservation result is deliberately not a live target capability.
    // Close the target flock, then clear the same-State root slot before the
    // caller can observe either success or interruption.
    owned_claim.claim.reset();
    if (receipt.has_value() && outcome.status == DistributedSieveWaveStoreStatus::ready) {
        return {std::move(receipt), std::move(outcome)};
    }
    receipt.reset();
    if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
        outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
    }
    return {std::nullopt, std::move(outcome)};
}

DistributedSieveWorkerAttemptStartResult
publish_worker_attempt_started(DistributedSievePrivateLeaseReservationReceipt&& reservation,
                               DistributedSieveWorkerAttemptStartTestHooks hooks) noexcept {
    auto consumed = std::move(reservation);
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome,
                              DistributedSieveWorkerAttemptStartDisposition disposition =
                                  DistributedSieveWorkerAttemptStartDisposition::failed)
        -> DistributedSieveWorkerAttemptStartResult {
        return {std::nullopt, std::move(outcome), disposition};
    };

    if (!consumed.owned_by_current_process() || consumed.wave_store_state_ == nullptr ||
        consumed.creator_process_id_ != consumed.wave_store_state_->creator_process_id) {
        return fail_with(process_mismatch());
    }

    try {
        const auto parsed = parse_distributed_sieve_worker_attempt_leaf_v1(
            consumed.worker_attempt_names_.canonical_record_leaf);
        if (!parsed.has_value() || parsed->pending) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const ChunkPlanV1* chunk = nullptr;
        for (const auto& candidate : consumed.wave_store_state_->manifest.chunks) {
            if (candidate.chunk_id == parsed->chunk_id) {
                chunk = &candidate;
                break;
            }
        }
        if (chunk == nullptr || chunk->sq_begin >= chunk->sq_end ||
            parsed->attempt_ordinal >= consumed.wave_store_state_->manifest.max_worker_attempts) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const auto regenerated = distributed_sieve_worker_attempt_names_v1(
            chunk->relative_artifact_stem, parsed->chunk_id, parsed->attempt_ordinal);
        if (!regenerated.has_value() || *regenerated != consumed.worker_attempt_names_ ||
            consumed.final_witness_.base_lock_leaf != regenerated->base_lock_leaf ||
            consumed.final_witness_.boundary !=
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
            consumed.final_witness_.lease_id == std::array<std::uint64_t, 2>{} ||
            !consumed.final_witness_.reserved_marker_identity.has_value() ||
            !consumed.final_witness_.directory_identity.has_value() ||
            !consumed.final_witness_.owner_marker_identity.has_value() ||
            !consumed.final_witness_.owned_marker_identity.has_value() ||
            consumed.final_witness_.work_package_residue.has_value() ||
            consumed.final_witness_.worker_handoff.has_value() ||
            consumed.final_witness_.merge_prepared.has_value() ||
            nil_identity(consumed.base_lock_identity_)) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)hooks;
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        DistributedSieveWaveStore view(consumed.wave_store_state_);
        auto claimed = view.open_worker_attempt_private_lease_root(
            parsed->chunk_id, parsed->attempt_ordinal, hooks.base_lock);
        if (!claimed || claimed.claim == nullptr) {
            return fail_with(std::move(claimed.diagnostic));
        }
        auto& claim = *claimed.claim;
        if (claim.wave_store_state_ != consumed.wave_store_state_ ||
            claim.creator_process_id_ != consumed.creator_process_id_ ||
            claim.worker_attempt_names_ != std::optional<DistributedSieveWorkerAttemptNamesV1>(
                                               consumed.worker_attempt_names_) ||
            claim.base_lock_acquisition_ !=
                DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting ||
            claim.base_lock_at_ == nullptr ||
            claim.base_lock_at_->identity() != consumed.base_lock_identity_ ||
            !claim.base_lock_at_->owned_by_current_process() ||
            !claim.expected_private_lease_base_lock_leaves_.has_value() ||
            !claim.expected_private_lease_base_lock_identities_.has_value() ||
            !claim.expected_private_lease_reservation_witnesses_.has_value() ||
            !claim.expected_worker_attempt_record_witnesses_.has_value() ||
            !claim.expected_merge_started_record_witnesses_.has_value() ||
            !claim.expected_chunk_terminal_failure_record_witnesses_.has_value()) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

        const auto revalidate_bindings = [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
            return claim.revalidate_authority();
        };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };

        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto predecessor = capture_manifest_bound_inventory_witness(
            consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
            consumed.wave_store_state_->absolute_root, consumed.creator_process_id_);
        if (!predecessor) {
            return fail_with(reject_lower_priority(std::move(predecessor.diagnostic)));
        }
        if (predecessor.inventory->private_lease_base_lock_leaves !=
                *claim.expected_private_lease_base_lock_leaves_ ||
            *predecessor.base_lock_identities !=
                *claim.expected_private_lease_base_lock_identities_ ||
            *predecessor.private_lease_witnesses !=
                *claim.expected_private_lease_reservation_witnesses_ ||
            predecessor.inventory->worker_attempt_records !=
                *claim.expected_worker_attempt_record_witnesses_ ||
            predecessor.inventory->merge_started_records !=
                *claim.expected_merge_started_record_witnesses_ ||
            predecessor.inventory->chunk_terminal_failure_records !=
                *claim.expected_chunk_terminal_failure_record_witnesses_ ||
            predecessor.inventory->private_lease_base_lock_leaves.size() !=
                predecessor.base_lock_identities->size() ||
            predecessor.inventory->private_lease_base_lock_leaves.size() !=
                predecessor.private_lease_witnesses->size()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_position =
            std::lower_bound(predecessor.inventory->private_lease_base_lock_leaves.begin(),
                             predecessor.inventory->private_lease_base_lock_leaves.end(),
                             regenerated->base_lock_leaf);
        if (target_position == predecessor.inventory->private_lease_base_lock_leaves.end() ||
            *target_position != regenerated->base_lock_leaf) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_index = static_cast<std::size_t>(std::distance(
            predecessor.inventory->private_lease_base_lock_leaves.begin(), target_position));
        const auto& predecessor_witness = predecessor.private_lease_witnesses->at(target_index);
        if (predecessor_witness.work_package_residue.has_value() ||
            predecessor_witness.worker_handoff.has_value() ||
            predecessor_witness.merge_prepared.has_value()) {
            return fail_with(reject_lower_priority(diagnostic(
                DistributedSieveWaveStoreStatus::reconciliation_required, protocol_error())));
        }
        if (predecessor.base_lock_identities->at(target_index) != consumed.base_lock_identity_ ||
            predecessor.private_lease_witnesses->at(target_index) != consumed.final_witness_ ||
            std::binary_search(predecessor.inventory->worker_attempt_record_leaves.begin(),
                               predecessor.inventory->worker_attempt_record_leaves.end(),
                               regenerated->canonical_record_leaf) ||
            std::binary_search(predecessor.inventory->worker_attempt_record_leaves.begin(),
                               predecessor.inventory->worker_attempt_record_leaves.end(),
                               regenerated->pending_record_leaf) ||
            find_worker_attempt_record_witness(predecessor, parsed->chunk_id,
                                               parsed->attempt_ordinal) != nullptr) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        std::vector<AttemptStartedV1> chain;
        chain.reserve(static_cast<std::size_t>(parsed->attempt_ordinal) + 1U);
        for (const auto& witness : predecessor.inventory->worker_attempt_records) {
            if (witness.chunk_id != parsed->chunk_id) {
                continue;
            }
            if (!witness.canonical_snapshot.has_value() || witness.pending_snapshot.has_value() ||
                witness.attempt_ordinal != chain.size() ||
                witness.attempt_ordinal >= parsed->attempt_ordinal) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            const auto historical_names = distributed_sieve_worker_attempt_names_v1(
                chunk->relative_artifact_stem, parsed->chunk_id, witness.attempt_ordinal);
            if (!historical_names.has_value()) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            const auto historical_lock =
                std::lower_bound(predecessor.inventory->private_lease_base_lock_leaves.begin(),
                                 predecessor.inventory->private_lease_base_lock_leaves.end(),
                                 historical_names->base_lock_leaf);
            if (historical_lock == predecessor.inventory->private_lease_base_lock_leaves.end() ||
                *historical_lock != historical_names->base_lock_leaf) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            const auto historical_index = static_cast<std::size_t>(std::distance(
                predecessor.inventory->private_lease_base_lock_leaves.begin(), historical_lock));
            if (predecessor.private_lease_witnesses->at(historical_index).boundary !=
                DistributedSievePrivateLeaseReservationBoundary::PermitAcquired) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            chain.push_back(witness.record);
        }
        if (chain.size() != parsed->attempt_ordinal) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        AttemptStartedV1 attempt{
            .manifest_digest = consumed.wave_store_state_->manifest.self_digest,
            .chunk_id = parsed->chunk_id,
            .sq_begin = chunk->sq_begin,
            .sq_end = chunk->sq_end,
            .attempt_ordinal = parsed->attempt_ordinal,
            .predecessor_digest = chain.empty() ? consumed.wave_store_state_->manifest.self_digest
                                                : chain.back().self_digest,
            .lease =
                LeaseIdentityV1{
                    .lease_id = LeaseIdV1{.limbs = consumed.final_witness_.lease_id},
                    .owner_marker = *consumed.final_witness_.owner_marker_identity,
                    .directory = *consumed.final_witness_.directory_identity,
                    .relative_stem = consumed.worker_attempt_names_.relative_lease_stem,
                },
            .retry_policy_version = consumed.wave_store_state_->manifest.retry_policy_version,
        };
        for (const auto& identity : *predecessor.base_lock_identities) {
            if (attempt.lease.owner_marker == identity || attempt.lease.directory == identity) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
        }
        for (const auto& existing : predecessor.inventory->worker_attempt_records) {
            if (attempt_lease_identities_conflict(attempt.lease, existing.record.lease)) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
        }

        DistributedSieveProtocolRecordV1 protocol_record(std::move(attempt));
        if (const auto sealed = seal_distributed_sieve_record(protocol_record); !sealed) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            failure.protocol_status = sealed;
            return fail_with(reject_lower_priority(std::move(failure)));
        }
        auto encoded = encode_distributed_sieve_record(protocol_record);
        if (!encoded) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            failure.protocol_status = encoded.status;
            return fail_with(reject_lower_priority(std::move(failure)));
        }
        attempt = std::move(std::get<AttemptStartedV1>(protocol_record));
        chain.push_back(attempt);
        if (const auto chain_status = validate_worker_attempt_chain(
                consumed.wave_store_state_->manifest, parsed->chunk_id, chain, nullptr, nullptr);
            !chain_status) {
            return fail_with(reject_lower_priority(worker_attempt_protocol_conflict(chain_status)));
        }

        if (const auto hooked =
                invoke_private_lease_base_lock_hook(hooks.after_locked_predecessor_validation,
                                                    hooks.context, consumed.creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(hooked);
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto confirmed_predecessor = capture_manifest_bound_inventory_witness(
            consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
            consumed.wave_store_state_->absolute_root, consumed.creator_process_id_);
        if (!confirmed_predecessor) {
            return fail_with(reject_lower_priority(std::move(confirmed_predecessor.diagnostic)));
        }
        if (!worker_attempt_start_observations_equal(predecessor, confirmed_predecessor)) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(bindings);
        }
        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.before_record_publication, hooks.context, consumed.creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(hooked);
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        WorkerAttemptStartHookBridge bridge{
            .hooks = hooks,
            .creator_process_id = consumed.creator_process_id_,
            .process_changed = false,
            .last_fault_point = std::nullopt,
        };
        const auto published = durable_record::publish_at(
            static_cast<durable_record::NativeHandle>(consumed.wave_store_state_->root_fd),
            regenerated->pending_record_leaf, regenerated->canonical_record_leaf, *encoded.bytes,
            durable_record::RecordTestHooks{
                .stop_after = bridge_worker_attempt_start_hook,
                .context = &bridge,
            });
        auto publication_outcome = worker_attempt_start_publication_diagnostic(published, bridge);
        const auto attach_publication = [&](DistributedSieveWaveStoreDiagnostic higher) noexcept {
            higher.publication_status = publication_outcome.publication_status;
            higher.publication_disposition = publication_outcome.publication_disposition;
            higher.last_worker_attempt_start_fault_point =
                publication_outcome.last_worker_attempt_start_fault_point;
            return higher;
        };
        const auto adjudicate_visible_prefix =
            [&](DistributedSieveWaveStoreDiagnostic lower,
                std::optional<WorkerAttemptRecordPrefixShape>& visible_shape) noexcept {
                const auto published_snapshot_matches =
                    [&](const ManifestBoundInventoryWitnessResult& observed,
                        WorkerAttemptRecordPrefixShape shape) noexcept {
                        if (!published.canonical_snapshot().has_value()) {
                            return true;
                        }
                        if (shape != WorkerAttemptRecordPrefixShape::canonical_only &&
                            shape != WorkerAttemptRecordPrefixShape::identical_dual) {
                            return false;
                        }
                        const auto* witness = find_worker_attempt_record_witness(
                            observed, parsed->chunk_id, parsed->attempt_ordinal);
                        return witness != nullptr &&
                               witness->canonical_snapshot == published.canonical_snapshot();
                    };
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(bindings);
                }
                auto first = capture_manifest_bound_inventory_witness(
                    consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
                    consumed.wave_store_state_->absolute_root, consumed.creator_process_id_);
                if (!first) {
                    if (const auto bindings = revalidate_bindings();
                        bindings.status != DistributedSieveWaveStoreStatus::ready) {
                        return attach_publication(bindings);
                    }
                    return attach_publication(std::move(first.diagnostic));
                }
                visible_shape = worker_attempt_record_prefix_shape(
                    predecessor, first, *regenerated, parsed->chunk_id, parsed->attempt_ordinal,
                    *encoded.bytes);
                if (!visible_shape.has_value() ||
                    !published_snapshot_matches(first, *visible_shape)) {
                    return attach_publication(reject_lower_priority(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
                }
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(bindings);
                }
                auto confirmed = capture_manifest_bound_inventory_witness(
                    consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
                    consumed.wave_store_state_->absolute_root, consumed.creator_process_id_);
                if (!confirmed) {
                    if (const auto bindings = revalidate_bindings();
                        bindings.status != DistributedSieveWaveStoreStatus::ready) {
                        return attach_publication(bindings);
                    }
                    return attach_publication(std::move(confirmed.diagnostic));
                }
                const auto confirmed_shape = worker_attempt_record_prefix_shape(
                    predecessor, confirmed, *regenerated, parsed->chunk_id, parsed->attempt_ordinal,
                    *encoded.bytes);
                if (!confirmed_shape.has_value() || *confirmed_shape != *visible_shape ||
                    !published_snapshot_matches(confirmed, *confirmed_shape) ||
                    !worker_attempt_start_observations_equal(first, confirmed)) {
                    return attach_publication(reject_lower_priority(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
                }
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(bindings);
                }
                return lower;
            };

        const bool fresh_canonical =
            published.status() == durable_record::RecordPublishStatus::durable &&
            published.disposition() == durable_record::RecordPublishDisposition::created &&
            published.canonical_snapshot().has_value();
        if (!fresh_canonical) {
            std::optional<WorkerAttemptRecordPrefixShape> visible_shape;
            publication_outcome =
                adjudicate_visible_prefix(std::move(publication_outcome), visible_shape);
            const bool reconciliation_needed =
                visible_shape.has_value() &&
                *visible_shape != WorkerAttemptRecordPrefixShape::absent;
            if (published.status() == durable_record::RecordPublishStatus::durable &&
                publication_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                publication_outcome.status =
                    DistributedSieveWaveStoreStatus::reconciliation_required;
            }
            return fail_with(std::move(publication_outcome),
                             reconciliation_needed ||
                                     published.disposition() !=
                                         durable_record::RecordPublishDisposition::none
                                 ? DistributedSieveWorkerAttemptStartDisposition::reconcile_required
                                 : DistributedSieveWorkerAttemptStartDisposition::failed);
        }

        const auto exact_fresh_successor =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                const auto shape = worker_attempt_record_prefix_shape(
                    predecessor, observed, *regenerated, parsed->chunk_id, parsed->attempt_ordinal,
                    *encoded.bytes);
                if (!shape.has_value() ||
                    *shape != WorkerAttemptRecordPrefixShape::canonical_only) {
                    return false;
                }
                const auto* witness = find_worker_attempt_record_witness(observed, parsed->chunk_id,
                                                                         parsed->attempt_ordinal);
                return witness != nullptr &&
                       witness->canonical_snapshot == published.canonical_snapshot() &&
                       !witness->pending_snapshot.has_value();
            };

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        auto first_successor = capture_manifest_bound_inventory_witness(
            consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
            consumed.wave_store_state_->absolute_root, consumed.creator_process_id_);
        if (!first_successor) {
            return fail_with(
                attach_publication(reject_lower_priority(std::move(first_successor.diagnostic))),
                DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        if (!exact_fresh_successor(first_successor)) {
            return fail_with(
                attach_publication(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))),
                DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        if (const auto hooked =
                invoke_private_lease_base_lock_hook(hooks.after_first_successor_validation,
                                                    hooks.context, consumed.creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(hooked),
                             DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        auto confirmed_successor = capture_manifest_bound_inventory_witness(
            consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
            consumed.wave_store_state_->absolute_root, consumed.creator_process_id_);
        if (!confirmed_successor) {
            return fail_with(attach_publication(
                                 reject_lower_priority(std::move(confirmed_successor.diagnostic))),
                             DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        if (!exact_fresh_successor(confirmed_successor) ||
            !worker_attempt_start_observations_equal(first_successor, confirmed_successor)) {
            return fail_with(
                attach_publication(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))),
                DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveWorkerAttemptStartDisposition::reconcile_required);
        }

        std::optional<DistributedSieveWorkerAttemptStartReceipt> start_receipt;
        DistributedSieveWorkerAttemptStartReceipt prepared(
            std::move(consumed.wave_store_state_), std::move(consumed.worker_attempt_names_),
            std::move(attempt), *published.canonical_snapshot(), std::move(consumed.final_witness_),
            std::move(claim.base_lock_at_), consumed.creator_process_id_);
        start_receipt.emplace(std::move(prepared));
        claimed.claim.reset();
        publication_outcome.status = DistributedSieveWaveStoreStatus::ready;
        return {std::move(start_receipt), std::move(publication_outcome),
                DistributedSieveWorkerAttemptStartDisposition::fresh_start};
#endif
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

DistributedSieveMergeStartResultV1 publish_merge_started_v1(
    DistributedSieveMergeLeaseReservationReceiptV1&& reservation,
    std::span<const TerminalChunkInputV1> terminal_inputs, std::uint32_t merge_policy_version,
    DistributedSieveMergeStartTestHooksV1 hooks,
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs) noexcept {
    auto consumed = std::move(reservation);
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome,
                              DistributedSieveMergeStartDispositionV1 disposition =
                                  DistributedSieveMergeStartDispositionV1::failed)
        -> DistributedSieveMergeStartResultV1 {
        return {
            .receipt = std::nullopt,
            .diagnostic = std::move(outcome),
            .disposition = disposition,
        };
    };
    if (!consumed.owned_by_current_process() || consumed.wave_store_state_ == nullptr ||
        consumed.creator_process_id_ != consumed.wave_store_state_->creator_process_id) {
        return fail_with(process_mismatch());
    }
    if (terminal_inputs.size() != consumed.terminal_inputs_.size() ||
        !std::equal(terminal_inputs.begin(), terminal_inputs.end(),
                    consumed.terminal_inputs_.begin(), consumed.terminal_inputs_.end()) ||
        merge_policy_version != consumed.merge_policy_version_ ||
        merge_policy_version != consumed.wave_store_state_->manifest.merge_policy_version) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }

    try {
        const auto parsed = parse_distributed_sieve_merge_started_leaf_v1(
            consumed.merge_generation_names_.canonical_record_leaf);
        const auto regenerated =
            parsed.has_value()
                ? distributed_sieve_merge_generation_names_v1(parsed->merge_attempt_ordinal)
                : std::nullopt;
        if (!parsed.has_value() || parsed->pending || !regenerated.has_value() ||
            *regenerated != consumed.merge_generation_names_ ||
            parsed->merge_attempt_ordinal >=
                consumed.wave_store_state_->manifest.max_merge_build_attempts ||
            consumed.final_witness_.base_lock_leaf != regenerated->base_lock_leaf ||
            consumed.final_witness_.boundary !=
                DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable ||
            consumed.final_witness_.lease_id == std::array<std::uint64_t, 2>{} ||
            !consumed.final_witness_.reserved_marker_identity.has_value() ||
            !consumed.final_witness_.directory_identity.has_value() ||
            !consumed.final_witness_.owner_marker_identity.has_value() ||
            !consumed.final_witness_.owned_marker_identity.has_value() ||
            consumed.final_witness_.work_package_residue.has_value() ||
            consumed.final_witness_.worker_handoff.has_value() ||
            consumed.final_witness_.merge_prepared.has_value() ||
            nil_identity(consumed.base_lock_identity_)) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        (void)hooks;
        (void)held_worker_handoffs;
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
        DistributedSieveWaveStore view(consumed.wave_store_state_);
        auto claimed = view.claim_merge_generation_private_lease_root(
            parsed->merge_attempt_ordinal,
            DistributedSieveWaveStore::AttemptBaseLockExpectation::present, hooks.base_lock,
            held_worker_handoffs);
        if (!claimed || claimed.claim == nullptr) {
            return fail_with(std::move(claimed.diagnostic));
        }
        auto& claim = *claimed.claim;
        if (claim.wave_store_state_ != consumed.wave_store_state_ ||
            claim.creator_process_id_ != consumed.creator_process_id_ ||
            claim.worker_attempt_names_.has_value() ||
            claim.merge_generation_names_ != std::optional<DistributedSieveMergeGenerationNamesV1>(
                                                 consumed.merge_generation_names_) ||
            claim.base_lock_acquisition_ !=
                DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting ||
            claim.base_lock_at_ == nullptr ||
            claim.base_lock_at_->identity() != consumed.base_lock_identity_ ||
            !claim.base_lock_at_->owned_by_current_process() ||
            claim.predecessor_base_locks_.size() != parsed->merge_attempt_ordinal ||
            !claim.expected_private_lease_base_lock_leaves_.has_value() ||
            !claim.expected_private_lease_base_lock_identities_.has_value() ||
            !claim.expected_private_lease_reservation_witnesses_.has_value() ||
            !claim.expected_worker_attempt_record_witnesses_.has_value() ||
            !claim.expected_merge_started_record_witnesses_.has_value() ||
            !claim.expected_chunk_terminal_failure_record_witnesses_.has_value()) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        if (consumed.retained_worker_base_locks_.size() !=
                consumed.retained_worker_base_lock_views_.size() ||
            consumed.retained_worker_base_locks_.size() !=
                claim.borrowed_worker_base_locks_.size()) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        for (std::size_t index = 0; index < claim.borrowed_worker_base_locks_.size(); ++index) {
            const auto& retained = consumed.retained_worker_base_locks_[index];
            const auto* retained_view = consumed.retained_worker_base_lock_views_[index];
            const auto* borrowed = claim.borrowed_worker_base_locks_[index];
            if (retained == nullptr || retained_view != retained.get() || borrowed == nullptr ||
                retained->root_fd_ != borrowed->root_fd_ ||
                retained->creator_process_id_ != borrowed->creator_process_id_ ||
                !retained->matches_exact_binding(borrowed->leaf_, borrowed->identity())) {
                return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                            invalid_argument_error()));
            }
        }

        std::vector<const DistributedSievePrivateLeaseBaseLockAt*> held_base_locks;
        held_base_locks.reserve(claim.borrowed_worker_base_locks_.size() +
                                claim.predecessor_base_locks_.size() + 1U);
        held_base_locks.insert(held_base_locks.end(), claim.borrowed_worker_base_locks_.begin(),
                               claim.borrowed_worker_base_locks_.end());
        for (const auto& predecessor_lock : claim.predecessor_base_locks_) {
            held_base_locks.push_back(predecessor_lock.get());
        }
        held_base_locks.push_back(claim.base_lock_at_.get());
        const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(held_base_locks.data(),
                                                                           held_base_locks.size())};
        const auto capture_merge_inventory = [&]() noexcept {
            return capture_manifest_bound_inventory_witness(
                consumed.wave_store_state_->root_fd, consumed.wave_store_state_->manifest,
                consumed.wave_store_state_->absolute_root, consumed.creator_process_id_,
                held_inventory);
        };
        const auto revalidate_bindings = [&]() noexcept -> DistributedSieveWaveStoreDiagnostic {
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return authority;
            }
            for (const auto& retained : consumed.retained_worker_base_locks_) {
                if (retained == nullptr) {
                    return diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                      protocol_error());
                }
                if (const auto worker = retained->revalidate();
                    worker.status != DistributedSieveWaveStoreStatus::ready) {
                    if (const auto authority = claim.revalidate_authority();
                        authority.status != DistributedSieveWaveStoreStatus::ready) {
                        return authority;
                    }
                    return worker;
                }
            }
            return claim.revalidate_authority();
        };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };

        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto predecessor = capture_merge_inventory();
        if (!predecessor) {
            return fail_with(reject_lower_priority(std::move(predecessor.diagnostic)));
        }
        if (predecessor.inventory->private_lease_base_lock_leaves !=
                *claim.expected_private_lease_base_lock_leaves_ ||
            *predecessor.base_lock_identities !=
                *claim.expected_private_lease_base_lock_identities_ ||
            *predecessor.private_lease_witnesses !=
                *claim.expected_private_lease_reservation_witnesses_ ||
            predecessor.inventory->worker_attempt_records !=
                *claim.expected_worker_attempt_record_witnesses_ ||
            predecessor.inventory->merge_started_records !=
                *claim.expected_merge_started_record_witnesses_ ||
            predecessor.inventory->chunk_terminal_failure_records !=
                *claim.expected_chunk_terminal_failure_record_witnesses_ ||
            !predecessor.inventory->chunk_terminal_failure_records.empty() ||
            predecessor.inventory->merge_started_records.size() != parsed->merge_attempt_ordinal) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_position =
            std::lower_bound(predecessor.inventory->private_lease_base_lock_leaves.begin(),
                             predecessor.inventory->private_lease_base_lock_leaves.end(),
                             regenerated->base_lock_leaf);
        if (target_position == predecessor.inventory->private_lease_base_lock_leaves.end() ||
            *target_position != regenerated->base_lock_leaf) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_index = static_cast<std::size_t>(std::distance(
            predecessor.inventory->private_lease_base_lock_leaves.begin(), target_position));
        if (predecessor.base_lock_identities->at(target_index) != consumed.base_lock_identity_ ||
            predecessor.private_lease_witnesses->at(target_index) != consumed.final_witness_ ||
            find_merge_started_record_witness_v1(predecessor, parsed->merge_attempt_ordinal) !=
                nullptr) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        std::vector<MergeStartedV1> chain;
        chain.reserve(static_cast<std::size_t>(parsed->merge_attempt_ordinal) + 1U);
        for (const auto& witness : predecessor.inventory->merge_started_records) {
            if (!witness.canonical_snapshot.has_value() || witness.pending_snapshot.has_value() ||
                witness.merge_attempt_ordinal != chain.size()) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
            chain.push_back(witness.record);
        }

        MergeStartedV1 start{
            .manifest_digest = consumed.wave_store_state_->manifest.self_digest,
            .work_digest = consumed.wave_store_state_->manifest.work_sha256,
            .ordered_inputs = consumed.terminal_inputs_,
            .merge_policy_version = merge_policy_version,
            .merged_lease =
                LeaseIdentityV1{
                    .lease_id = LeaseIdV1{.limbs = consumed.final_witness_.lease_id},
                    .owner_marker = *consumed.final_witness_.owner_marker_identity,
                    .directory = *consumed.final_witness_.directory_identity,
                    .relative_stem = consumed.merge_generation_names_.relative_lease_stem,
                },
            .merge_attempt_ordinal = parsed->merge_attempt_ordinal,
            .predecessor_digest = chain.empty() ? consumed.wave_store_state_->manifest.self_digest
                                                : chain.back().self_digest,
        };
        for (const auto& identity : *predecessor.base_lock_identities) {
            if (start.merged_lease.owner_marker == identity ||
                start.merged_lease.directory == identity) {
                return fail_with(reject_lower_priority(diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
            }
        }

        DistributedSieveProtocolRecordV1 protocol_record(std::move(start));
        if (const auto sealed = seal_distributed_sieve_record(protocol_record); !sealed) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            failure.protocol_status = sealed;
            return fail_with(reject_lower_priority(std::move(failure)));
        }
        auto encoded = encode_distributed_sieve_record(protocol_record);
        if (!encoded) {
            auto failure =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
            failure.protocol_status = encoded.status;
            return fail_with(reject_lower_priority(std::move(failure)));
        }
        start = std::move(std::get<MergeStartedV1>(protocol_record));
        chain.push_back(start);
        if (const auto dependency = validate_merge_start_chain_v1(
                consumed.wave_store_state_->manifest, predecessor.inventory->worker_attempt_records,
                *predecessor.private_lease_witnesses, chain);
            !dependency) {
            return fail_with(reject_lower_priority(worker_attempt_protocol_conflict(dependency)));
        }

        if (const auto hooked =
                invoke_private_lease_base_lock_hook(hooks.after_locked_predecessor_validation,
                                                    hooks.context, consumed.creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(hooked);
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto confirmed_predecessor = capture_merge_inventory();
        if (!confirmed_predecessor ||
            !worker_attempt_start_observations_equal(predecessor, confirmed_predecessor)) {
            return fail_with(
                confirmed_predecessor
                    ? reject_lower_priority(diagnostic(
                          DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))
                    : reject_lower_priority(std::move(confirmed_predecessor.diagnostic)));
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(bindings);
        }
        if (const auto hooked = invoke_private_lease_base_lock_hook(
                hooks.before_record_publication, hooks.context, consumed.creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(hooked);
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        MergeStartHookBridgeV1 bridge{
            .hooks = hooks,
            .creator_process_id = consumed.creator_process_id_,
        };
        const auto published = durable_record::publish_at(
            static_cast<durable_record::NativeHandle>(consumed.wave_store_state_->root_fd),
            regenerated->pending_record_leaf, regenerated->canonical_record_leaf, *encoded.bytes,
            durable_record::RecordTestHooks{
                .stop_after = bridge_merge_start_hook_v1,
                .context = &bridge,
            });
        auto publication_outcome = merge_start_publication_diagnostic_v1(published, bridge);
        const auto attach_publication = [&](DistributedSieveWaveStoreDiagnostic higher) noexcept {
            higher.publication_status = publication_outcome.publication_status;
            higher.publication_disposition = publication_outcome.publication_disposition;
            higher.last_merge_start_fault_point = publication_outcome.last_merge_start_fault_point;
            return higher;
        };
        const auto adjudicate_visible_prefix =
            [&](DistributedSieveWaveStoreDiagnostic lower,
                std::optional<MergeStartedRecordPrefixShapeV1>& visible_shape) noexcept {
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(bindings);
                }
                auto first = capture_merge_inventory();
                if (!first) {
                    return attach_publication(reject_lower_priority(std::move(first.diagnostic)));
                }
                visible_shape = merge_started_record_prefix_shape_v1(
                    predecessor, first, *regenerated, parsed->merge_attempt_ordinal,
                    *encoded.bytes);
                if (!visible_shape.has_value()) {
                    return attach_publication(reject_lower_priority(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
                }
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(bindings);
                }
                auto confirmed = capture_merge_inventory();
                if (!confirmed) {
                    return attach_publication(
                        reject_lower_priority(std::move(confirmed.diagnostic)));
                }
                const auto confirmed_shape = merge_started_record_prefix_shape_v1(
                    predecessor, confirmed, *regenerated, parsed->merge_attempt_ordinal,
                    *encoded.bytes);
                if (!confirmed_shape.has_value() || *confirmed_shape != *visible_shape ||
                    !worker_attempt_start_observations_equal(first, confirmed)) {
                    return attach_publication(reject_lower_priority(diagnostic(
                        DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
                }
                if (const auto bindings = revalidate_bindings();
                    bindings.status != DistributedSieveWaveStoreStatus::ready) {
                    return attach_publication(bindings);
                }
                return lower;
            };

        const bool fresh_canonical =
            published.status() == durable_record::RecordPublishStatus::durable &&
            published.disposition() == durable_record::RecordPublishDisposition::created &&
            published.canonical_snapshot().has_value();
        if (!fresh_canonical) {
            std::optional<MergeStartedRecordPrefixShapeV1> visible_shape;
            publication_outcome =
                adjudicate_visible_prefix(std::move(publication_outcome), visible_shape);
            const bool reconciliation_needed =
                visible_shape.has_value() &&
                *visible_shape != MergeStartedRecordPrefixShapeV1::absent;
            if (published.status() == durable_record::RecordPublishStatus::durable &&
                publication_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                publication_outcome.status =
                    DistributedSieveWaveStoreStatus::reconciliation_required;
            }
            return fail_with(std::move(publication_outcome),
                             reconciliation_needed ||
                                     published.disposition() !=
                                         durable_record::RecordPublishDisposition::none
                                 ? DistributedSieveMergeStartDispositionV1::reconcile_required
                                 : DistributedSieveMergeStartDispositionV1::failed);
        }

        const auto exact_fresh_successor =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                const auto shape = merge_started_record_prefix_shape_v1(
                    predecessor, observed, *regenerated, parsed->merge_attempt_ordinal,
                    *encoded.bytes);
                const auto* witness =
                    find_merge_started_record_witness_v1(observed, parsed->merge_attempt_ordinal);
                return shape.has_value() &&
                       *shape == MergeStartedRecordPrefixShapeV1::canonical_only &&
                       witness != nullptr &&
                       witness->canonical_snapshot == published.canonical_snapshot() &&
                       !witness->pending_snapshot.has_value();
            };

        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveMergeStartDispositionV1::reconcile_required);
        }
        auto first_successor = capture_merge_inventory();
        if (!first_successor || !exact_fresh_successor(first_successor)) {
            return fail_with(
                first_successor
                    ? attach_publication(reject_lower_priority(diagnostic(
                          DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())))
                    : attach_publication(
                          reject_lower_priority(std::move(first_successor.diagnostic))),
                DistributedSieveMergeStartDispositionV1::reconcile_required);
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveMergeStartDispositionV1::reconcile_required);
        }
        if (const auto hooked =
                invoke_private_lease_base_lock_hook(hooks.after_first_successor_validation,
                                                    hooks.context, consumed.creator_process_id_);
            hooked.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(hooked),
                             DistributedSieveMergeStartDispositionV1::reconcile_required);
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveMergeStartDispositionV1::reconcile_required);
        }
        auto confirmed_successor = capture_merge_inventory();
        if (!confirmed_successor || !exact_fresh_successor(confirmed_successor) ||
            !worker_attempt_start_observations_equal(first_successor, confirmed_successor)) {
            return fail_with(
                confirmed_successor
                    ? attach_publication(reject_lower_priority(diagnostic(
                          DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())))
                    : attach_publication(
                          reject_lower_priority(std::move(confirmed_successor.diagnostic))),
                DistributedSieveMergeStartDispositionV1::reconcile_required);
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(attach_publication(bindings),
                             DistributedSieveMergeStartDispositionV1::reconcile_required);
        }

        std::optional<DistributedSieveMergeStartedReceiptV1> start_receipt;
        DistributedSieveMergeStartedReceiptV1 prepared(
            std::move(consumed.wave_store_state_), std::move(consumed.merge_generation_names_),
            std::move(start), std::move(chain), *published.canonical_snapshot(),
            std::move(consumed.final_witness_), std::move(consumed.retained_worker_base_locks_),
            std::move(consumed.retained_worker_base_lock_views_), std::move(claim.base_lock_at_),
            consumed.creator_process_id_);
        start_receipt.emplace(std::move(prepared));
        claimed.claim.reset();
        publication_outcome.status = DistributedSieveWaveStoreStatus::ready;
        return {
            .receipt = std::move(start_receipt),
            .diagnostic = std::move(publication_outcome),
            .disposition = DistributedSieveMergeStartDispositionV1::fresh_start,
        };
#endif
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

DistributedSieveMergeStartedReconcileResultV1 reconcile_merge_started_generation_v1(
    DistributedSieveWaveStore& store, std::uint32_t merge_attempt_ordinal,
    DistributedSieveMergeStartedReconcileTestHooksV1 hooks,
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome) {
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        }
        return DistributedSieveMergeStartedReconcileResultV1{
            .reconciled = std::nullopt,
            .diagnostic = std::move(outcome),
        };
    };
    if (store.state_ == nullptr || !process_matches(store.state_->creator_process_id)) {
        return fail_with(store.state_ == nullptr
                             ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                          invalid_argument_error())
                             : process_mismatch());
    }
    if (merge_attempt_ordinal >= store.state_->manifest.max_merge_build_attempts) {
        return fail_with(
            diagnostic(DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error()));
    }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
    (void)hooks;
    (void)held_worker_handoffs;
    return fail_with(
        diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error()));
#else
    try {
        auto claimed = store.claim_merge_generation_private_lease_root(
            merge_attempt_ordinal, DistributedSieveWaveStore::AttemptBaseLockExpectation::present,
            hooks.base_lock, held_worker_handoffs, true);
        if (!claimed || claimed.claim == nullptr) {
            return fail_with(std::move(claimed.diagnostic));
        }
        auto& claim = *claimed.claim;
        if (claim.worker_attempt_names_.has_value() || !claim.merge_generation_names_.has_value() ||
            claim.base_lock_at_ == nullptr ||
            claim.base_lock_acquisition_ !=
                DistributedSievePrivateLeaseRootClaim::BaseLockAcquisition::OpenedExisting ||
            !claim.expected_merge_started_record_witnesses_.has_value() ||
            !claim.expected_private_lease_reservation_witnesses_.has_value() ||
            claim.expected_merge_started_record_witnesses_->size() !=
                static_cast<std::size_t>(merge_attempt_ordinal) + 1U) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        const auto names = distributed_sieve_merge_generation_names_v1(merge_attempt_ordinal);
        if (!names.has_value() || *names != *claim.merge_generation_names_) {
            return fail_with(diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                        invalid_argument_error()));
        }
        std::vector<const DistributedSievePrivateLeaseBaseLockAt*> held_base_locks;
        held_base_locks.reserve(claim.borrowed_worker_base_locks_.size() +
                                claim.predecessor_base_locks_.size() + 1U);
        held_base_locks.insert(held_base_locks.end(), claim.borrowed_worker_base_locks_.begin(),
                               claim.borrowed_worker_base_locks_.end());
        for (const auto& predecessor_lock : claim.predecessor_base_locks_) {
            held_base_locks.push_back(predecessor_lock.get());
        }
        held_base_locks.push_back(claim.base_lock_at_.get());
        const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(held_base_locks.data(),
                                                                           held_base_locks.size())};
        const auto capture_merge_inventory = [&]() noexcept {
            return capture_manifest_bound_inventory_witness(
                store.state_->root_fd, store.state_->manifest, store.state_->absolute_root,
                store.state_->creator_process_id, held_inventory);
        };
        const auto revalidate_bindings = [&]() noexcept { return claim.revalidate_authority(); };
        const auto reject_lower_priority = [&](DistributedSieveWaveStoreDiagnostic lower) noexcept {
            if (const auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                return bindings;
            }
            return lower;
        };

        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto initial = capture_merge_inventory();
        if (!initial) {
            return fail_with(reject_lower_priority(std::move(initial.diagnostic)));
        }
        const bool cold_open_raw_recovery_pending =
            store.state_->merge_raw_recovery_open_anchor.has_value() &&
            !store.state_->merge_raw_recovery_completed.load(std::memory_order_acquire);
        if (cold_open_raw_recovery_pending &&
            !worker_attempt_start_observations_equal(*store.state_->merge_raw_recovery_open_anchor,
                                                     initial)) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        if (initial.inventory->private_lease_base_lock_leaves !=
                *claim.expected_private_lease_base_lock_leaves_ ||
            *initial.base_lock_identities != *claim.expected_private_lease_base_lock_identities_ ||
            *initial.private_lease_witnesses !=
                *claim.expected_private_lease_reservation_witnesses_ ||
            initial.inventory->worker_attempt_records !=
                *claim.expected_worker_attempt_record_witnesses_ ||
            initial.inventory->merge_started_records !=
                *claim.expected_merge_started_record_witnesses_ ||
            initial.inventory->chunk_terminal_failure_records !=
                *claim.expected_chunk_terminal_failure_record_witnesses_) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto* initial_record =
            find_merge_started_record_witness_v1(initial, merge_attempt_ordinal);
        if (initial_record == nullptr || (!initial_record->canonical_snapshot.has_value() &&
                                          !initial_record->pending_snapshot.has_value())) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_position = std::lower_bound(
            initial.inventory->private_lease_base_lock_leaves.begin(),
            initial.inventory->private_lease_base_lock_leaves.end(), names->base_lock_leaf);
        if (target_position == initial.inventory->private_lease_base_lock_leaves.end() ||
            *target_position != names->base_lock_leaf) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto target_index = static_cast<std::size_t>(std::distance(
            initial.inventory->private_lease_base_lock_leaves.begin(), target_position));
        const auto initial_boundary = initial.private_lease_witnesses->at(target_index).boundary;
        const bool pending_present = initial_record->pending_snapshot.has_value();
        const bool canonical_present = initial_record->canonical_snapshot.has_value();
        if ((pending_present &&
             initial_boundary !=
                 DistributedSievePrivateLeaseReservationBoundary::FinalDirectoryDurable) ||
            initial.private_lease_witnesses->at(target_index).work_package_residue.has_value() ||
            initial.private_lease_witnesses->at(target_index).worker_handoff.has_value() ||
            initial.private_lease_witnesses->at(target_index).merge_prepared.has_value()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        const auto initial_record_bytes = initial_record->bytes;
        const auto initial_canonical_snapshot = initial_record->canonical_snapshot;
        const auto initial_pending_snapshot = initial_record->pending_snapshot;
        const auto expected_disposition =
            canonical_present ? durable_record::RecordPublishDisposition::confirmed_existing
                              : durable_record::RecordPublishDisposition::recovered_pending;
        const auto expected_source_snapshot =
            canonical_present ? initial_canonical_snapshot : initial_pending_snapshot;
        if (!expected_source_snapshot.has_value()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        if (hooks.before_record_normalization != nullptr) {
            if (!process_matches(store.state_->creator_process_id)) {
                return fail_with(process_mismatch());
            }
            hooks.before_record_normalization(hooks.context);
            if (!process_matches(store.state_->creator_process_id)) {
                return fail_with(process_mismatch());
            }
        }
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }

        MergeStartHookBridgeV1 bridge{
            .hooks =
                DistributedSieveMergeStartTestHooksV1{
                    .stop_after = hooks.stop_after,
                    .context = hooks.context,
                },
            .creator_process_id = store.state_->creator_process_id,
        };
        const auto published = durable_record::publish_at(
            static_cast<durable_record::NativeHandle>(store.state_->root_fd),
            names->pending_record_leaf, names->canonical_record_leaf, initial_record_bytes,
            durable_record::RecordTestHooks{
                .stop_after = bridge_merge_start_hook_v1,
                .context = &bridge,
            });
        auto publication_outcome = merge_start_publication_diagnostic_v1(published, bridge);
        const bool accepted_publication =
            published.status() == durable_record::RecordPublishStatus::durable &&
            published.disposition() == expected_disposition &&
            published.canonical_snapshot().has_value() &&
            published.canonical_snapshot() == expected_source_snapshot;
        if (!accepted_publication) {
            if (auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                bindings.publication_status = publication_outcome.publication_status;
                bindings.publication_disposition = publication_outcome.publication_disposition;
                bindings.last_merge_start_fault_point =
                    publication_outcome.last_merge_start_fault_point;
                return fail_with(bindings);
            }
            auto first_visible = capture_merge_inventory();
            if (!first_visible) {
                return fail_with(reject_lower_priority(std::move(first_visible.diagnostic)));
            }
            if (auto bindings = revalidate_bindings();
                bindings.status != DistributedSieveWaveStoreStatus::ready) {
                bindings.publication_status = publication_outcome.publication_status;
                bindings.publication_disposition = publication_outcome.publication_disposition;
                bindings.last_merge_start_fault_point =
                    publication_outcome.last_merge_start_fault_point;
                return fail_with(bindings);
            }
            auto confirmed_visible = capture_merge_inventory();
            if (!confirmed_visible ||
                !worker_attempt_start_observations_equal(first_visible, confirmed_visible)) {
                return fail_with(
                    confirmed_visible
                        ? reject_lower_priority(
                              diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                         protocol_error()))
                        : reject_lower_priority(std::move(confirmed_visible.diagnostic)));
            }
            if (published.status() == durable_record::RecordPublishStatus::durable &&
                published.disposition() == durable_record::RecordPublishDisposition::created) {
                publication_outcome.status =
                    DistributedSieveWaveStoreStatus::reconciliation_required;
                publication_outcome.native_error = protocol_error();
            } else if (published.status() == durable_record::RecordPublishStatus::durable) {
                publication_outcome.status = DistributedSieveWaveStoreStatus::namespace_conflict;
                publication_outcome.native_error = protocol_error();
            } else if (publication_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                publication_outcome.status =
                    DistributedSieveWaveStoreStatus::reconciliation_required;
                publication_outcome.native_error = protocol_error();
            }
            return fail_with(std::move(publication_outcome));
        }

        const auto exact_normalized =
            [&](const ManifestBoundInventoryWitnessResult& observed) noexcept {
                const auto* witness =
                    find_merge_started_record_witness_v1(observed, merge_attempt_ordinal);
                return witness != nullptr && witness->bytes == initial_record_bytes &&
                       witness->canonical_snapshot == published.canonical_snapshot() &&
                       !witness->pending_snapshot.has_value();
            };
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(bindings);
        }
        auto first_normalized = capture_merge_inventory();
        if (!first_normalized || !exact_normalized(first_normalized)) {
            return fail_with(
                first_normalized
                    ? reject_lower_priority(diagnostic(
                          DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))
                    : reject_lower_priority(std::move(first_normalized.diagnostic)));
        }
        if (hooks.after_first_normalized_successor_validation != nullptr) {
            if (!process_matches(store.state_->creator_process_id)) {
                return fail_with(process_mismatch());
            }
            hooks.after_first_normalized_successor_validation(hooks.context);
            if (!process_matches(store.state_->creator_process_id)) {
                return fail_with(process_mismatch());
            }
        }
        if (const auto bindings = revalidate_bindings();
            bindings.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(bindings);
        }
        auto confirmed_normalized = capture_merge_inventory();
        if (!confirmed_normalized || !exact_normalized(confirmed_normalized) ||
            !worker_attempt_start_observations_equal(first_normalized, confirmed_normalized)) {
            return fail_with(
                confirmed_normalized
                    ? reject_lower_priority(diagnostic(
                          DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()))
                    : reject_lower_priority(std::move(confirmed_normalized.diagnostic)));
        }
        const auto* normalized_record =
            find_merge_started_record_witness_v1(confirmed_normalized, merge_attempt_ordinal);
        if (normalized_record == nullptr || !normalized_record->canonical_snapshot.has_value()) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }
        if (cold_open_raw_recovery_pending &&
            !worker_attempt_start_observations_equal(*store.state_->merge_raw_recovery_open_anchor,
                                                     confirmed_normalized)) {
            return fail_with(reject_lower_priority(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error())));
        }

        *claim.expected_merge_started_record_witnesses_ =
            confirmed_normalized.inventory->merge_started_records;
        if (const auto validated = claim.revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        auto pinned = open_worker_attempt_canonical_record(
            store.state_->root_fd, names->canonical_record_leaf,
            *normalized_record->canonical_snapshot, normalized_record->bytes,
            store.state_->creator_process_id);
        if (!pinned) {
            return fail_with(reject_lower_priority(std::move(pinned.diagnostic)));
        }
        DistributedSieveStartedMergeCleanupAdmissionV1 admission(std::move(pinned.canonical_record),
                                                                 *names, *normalized_record,
                                                                 store.state_->creator_process_id);

        bool recovered = false;
        DistributedSieveWaveStoreDiagnostic recovery_outcome;
        const auto raw_recovery = confirmed_normalized.private_lease_witnesses->at(target_index)
                                      .merge_raw_writer_recovery;
        if (raw_recovery.has_value()) {
            struct RawRecoveryBridgeContext final {
                const DistributedSieveWaveStore::State* state = nullptr;
                DistributedSievePrivateLeaseRootClaim* claim = nullptr;
                const DistributedSieveStartedMergeCleanupAdmissionV1* admission = nullptr;
                const ManifestBoundInventoryWitnessResult* expected = nullptr;
                HeldPrivateLeaseBaseLockInventoryV1 held;
                DistributedSieveMergeStartedReconcileTestHooksV1 hooks;
                DistributedSieveWaveStoreDiagnostic failure;
                std::optional<DistributedSieveMergeRawWriterRecoveryFaultPointV1> last_point;
                bool permit_checked = false;
            } raw_context{
                .state = store.state_.get(),
                .claim = std::addressof(claim),
                .admission = std::addressof(admission),
                .expected = std::addressof(confirmed_normalized),
                .held = held_inventory,
                .hooks = hooks,
            };
            const auto raw_stop_after = [](gnfs::relation::OOCPrivateLeaseFaultPoint point,
                                           void* opaque) noexcept {
                auto* context = static_cast<RawRecoveryBridgeContext*>(opaque);
                if (context == nullptr || context->state == nullptr || context->claim == nullptr ||
                    context->admission == nullptr || context->expected == nullptr) {
                    return true;
                }
                const auto mapped = wave_merge_raw_recovery_fault_point(point);
                if (!mapped.has_value()) {
                    return false;
                }
                context->last_point = *mapped;
                if (const auto authority = context->claim->revalidate_authority();
                    authority.status != DistributedSieveWaveStoreStatus::ready) {
                    context->failure = authority;
                    context->failure.last_merge_raw_writer_recovery_fault_point = mapped;
                    return true;
                }
                if (const auto pinned = context->admission->revalidate(context->state->root_fd);
                    pinned.status != DistributedSieveWaveStoreStatus::ready) {
                    context->failure = pinned;
                    context->failure.last_merge_raw_writer_recovery_fault_point = mapped;
                    return true;
                }
                if (point == gnfs::relation::OOCPrivateLeaseFaultPoint::RecoveryPermitAcquired) {
                    auto observed = capture_manifest_bound_inventory_witness(
                        context->state->root_fd, context->state->manifest,
                        context->state->absolute_root, context->state->creator_process_id,
                        context->held);
                    if (!observed ||
                        !worker_attempt_start_observations_equal(*context->expected, observed)) {
                        context->failure =
                            observed
                                ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                             protocol_error())
                                : std::move(observed.diagnostic);
                        context->failure.last_merge_raw_writer_recovery_fault_point = mapped;
                        return true;
                    }
                    context->permit_checked = true;
                }
                if (context->hooks.raw_recovery_stop_after != nullptr &&
                    context->hooks.raw_recovery_stop_after(*mapped, context->hooks.context)) {
                    context->failure =
                        diagnostic(DistributedSieveWaveStoreStatus::interrupted,
                                   std::make_error_code(std::errc::operation_canceled));
                    context->failure.last_merge_raw_writer_recovery_fault_point = mapped;
                    return true;
                }
                return false;
            };
            const std::filesystem::path base_path =
                store.state_->absolute_root / names->private_directory_leaf / "corpus";
            const auto lower = claim.base_lock_at_->recover_exact_merge_raw_writer_private_lease(
                base_path, *raw_recovery,
                gnfs::relation::OOCPrivateLeaseTestHooks{
                    .stop_after = raw_stop_after,
                    .context = &raw_context,
                });
            if (raw_context.failure.status != DistributedSieveWaveStoreStatus::ready) {
                recovery_outcome = std::move(raw_context.failure);
            } else if (!raw_context.permit_checked) {
                recovery_outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                              protocol_error());
                recovery_outcome.last_merge_raw_writer_recovery_fault_point =
                    raw_context.last_point;
            } else if (!lower.completed()) {
                recovery_outcome = merge_raw_recovery_failure(lower, raw_context.last_point);
            } else {
                recovered = true;
                publication_outcome.last_merge_raw_writer_recovery_fault_point =
                    raw_context.last_point;
            }
        } else {
            DistributedSieveFdPrivateLeaseRecoveryTarget target(claim, std::move(admission),
                                                                hooks.recovery);
            const auto run = private_lease::run_private_lease_recovery_protocol(target);
            if (run == private_lease::PrivateLeaseRecoveryRunResult::Interrupted) {
                recovery_outcome = target.interruption_diagnostic();
            } else if (run == private_lease::PrivateLeaseRecoveryRunResult::Rejected) {
                recovery_outcome = diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                              invalid_argument_error());
            } else if (!target.completed()) {
                recovery_outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                              protocol_error());
            } else {
                recovered = true;
            }
        }
        if (!recovered) {
            if (recovery_outcome.status == DistributedSieveWaveStoreStatus::ready) {
                recovery_outcome = diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                              protocol_error());
            }
            return fail_with(std::move(recovery_outcome));
        }

        if (raw_recovery.has_value()) {
            ManifestBoundInventoryWitnessResult expected_p0 = confirmed_normalized;
            auto& protocol_leaves = expected_p0.inventory->private_lease_protocol_leaves;
            std::erase_if(protocol_leaves, [&](const std::string& leaf) {
                const auto parsed =
                    parse_manifest_bound_private_lease_protocol_leaf(store.state_->manifest, leaf);
                return parsed.has_value() && parsed->names.base_lock_leaf == names->base_lock_leaf;
            });
            expected_p0.private_lease_witnesses->at(target_index) =
                PrivateLeaseReservationWitness{.base_lock_leaf = names->base_lock_leaf};

            const auto exact_p0 = [&](const ManifestBoundInventoryWitnessResult& observed) {
                return worker_attempt_start_observations_equal(expected_p0, observed);
            };
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(authority);
            }
            if (const auto pinned_start = admission.revalidate(store.state_->root_fd);
                pinned_start.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(pinned_start);
            }
            auto first_p0 = capture_merge_inventory();
            if (!first_p0 || !exact_p0(first_p0)) {
                return fail_with(
                    first_p0 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                          protocol_error())
                             : std::move(first_p0.diagnostic));
            }
            if (hooks.after_first_raw_recovery_successor_validation != nullptr) {
                if (!process_matches(store.state_->creator_process_id)) {
                    return fail_with(process_mismatch());
                }
                hooks.after_first_raw_recovery_successor_validation(hooks.context);
                if (!process_matches(store.state_->creator_process_id)) {
                    return fail_with(process_mismatch());
                }
            }
            if (const auto authority = claim.revalidate_authority();
                authority.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(authority);
            }
            if (const auto pinned_start = admission.revalidate(store.state_->root_fd);
                pinned_start.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(pinned_start);
            }
            auto confirmed_p0 = capture_merge_inventory();
            if (!confirmed_p0 || !exact_p0(confirmed_p0) ||
                !worker_attempt_start_observations_equal(first_p0, confirmed_p0)) {
                return fail_with(
                    confirmed_p0 ? diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict,
                                              protocol_error())
                                 : std::move(confirmed_p0.diagnostic));
            }
            *claim.expected_private_lease_reservation_witnesses_ =
                *confirmed_p0.private_lease_witnesses;
            if (const auto validated = claim.revalidate();
                validated.status != DistributedSieveWaveStoreStatus::ready) {
                return fail_with(validated);
            }
            if (cold_open_raw_recovery_pending) {
                store.state_->merge_raw_recovery_completed.store(true, std::memory_order_release);
            }
        }

        std::optional<std::uint32_t> next_ordinal;
        if (merge_attempt_ordinal + 1U < store.state_->manifest.max_merge_build_attempts) {
            next_ordinal = merge_attempt_ordinal + 1U;
        }
        DistributedSieveReconciledMergeStartedV1 reconciled{
            .record = normalized_record->record,
            .canonical_snapshot = *normalized_record->canonical_snapshot,
            .next_merge_attempt_ordinal = next_ordinal,
        };
        publication_outcome.status = DistributedSieveWaveStoreStatus::ready;
        return {
            .reconciled = std::move(reconciled),
            .diagnostic = std::move(publication_outcome),
        };
    } catch (const DistributedSieveFdPrivateLeaseRecoveryTarget::Failure& failure) {
        return fail_with(failure.diagnostic);
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
#endif
}

DistributedSieveMergeGenerationCursorResultV1 prepare_distributed_sieve_merge_generation_v1(
    DistributedSieveWaveStore& store,
    std::span<const DistributedSieveAdoptedWorkerChunkV1* const> held_worker_handoffs) noexcept {
    const auto fail_with = [](DistributedSieveWaveStoreDiagnostic outcome) {
        if (outcome.status == DistributedSieveWaveStoreStatus::ready) {
            outcome =
                diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
        }
        return DistributedSieveMergeGenerationCursorResultV1{
            .merge_attempt_ordinal = std::nullopt,
            .diagnostic = std::move(outcome),
        };
    };
    if (store.state_ == nullptr || !process_matches(store.state_->creator_process_id)) {
        return fail_with(store.state_ == nullptr
                             ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                          invalid_argument_error())
                             : process_mismatch());
    }

    try {
        auto claimed = store.claim_private_lease_root_impl(held_worker_handoffs, true);
        if (!claimed || claimed.claim == nullptr) {
            return fail_with(std::move(claimed.diagnostic));
        }
        if (const auto validated = claimed.claim->revalidate();
            validated.status != DistributedSieveWaveStoreStatus::ready) {
            return fail_with(validated);
        }
        const HeldPrivateLeaseBaseLockInventoryV1 held_inventory{
            std::span<const DistributedSievePrivateLeaseBaseLockAt* const>(
                claimed.claim->borrowed_worker_base_locks_.data(),
                claimed.claim->borrowed_worker_base_locks_.size())};
        auto observed = capture_manifest_bound_inventory_witness(
            store.state_->root_fd, store.state_->manifest, store.state_->absolute_root,
            store.state_->creator_process_id, held_inventory);
        if (!observed) {
            return fail_with(std::move(observed.diagnostic));
        }
        if (store.state_->merge_raw_recovery_open_anchor.has_value() &&
            !store.state_->merge_raw_recovery_completed.load(std::memory_order_acquire) &&
            !worker_attempt_start_observations_equal(*store.state_->merge_raw_recovery_open_anchor,
                                                     observed)) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        if (!observed.inventory->chunk_terminal_failure_records.empty()) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const std::size_t start_count = observed.inventory->merge_started_records.size();
        if (start_count == 0U) {
            claimed.claim.reset();
            return {
                .merge_attempt_ordinal = 0U,
                .diagnostic = {},
            };
        }
        const auto& latest = observed.inventory->merge_started_records.back();
        if (latest.merge_attempt_ordinal + 1U != start_count) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto latest_names =
            distributed_sieve_merge_generation_names_v1(latest.merge_attempt_ordinal);
        if (!latest_names.has_value()) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto lease_position = std::lower_bound(
            observed.inventory->private_lease_base_lock_leaves.begin(),
            observed.inventory->private_lease_base_lock_leaves.end(), latest_names->base_lock_leaf);
        if (lease_position == observed.inventory->private_lease_base_lock_leaves.end() ||
            *lease_position != latest_names->base_lock_leaf) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::namespace_conflict, protocol_error()));
        }
        const auto lease_index = static_cast<std::size_t>(std::distance(
            observed.inventory->private_lease_base_lock_leaves.begin(), lease_position));
        const bool canonical_only =
            latest.canonical_snapshot.has_value() && !latest.pending_snapshot.has_value();
        const bool lease_closed = observed.private_lease_witnesses->at(lease_index).boundary ==
                                  DistributedSievePrivateLeaseReservationBoundary::PermitAcquired;
        const std::uint32_t latest_ordinal = latest.merge_attempt_ordinal;
        claimed.claim.reset();

        if (!canonical_only || !lease_closed) {
            auto reconciled = reconcile_merge_started_generation_v1(store, latest_ordinal, {},
                                                                    held_worker_handoffs);
            if (!reconciled) {
                return fail_with(std::move(reconciled.diagnostic));
            }
            if (!reconciled.reconciled->next_merge_attempt_ordinal.has_value()) {
                return fail_with(
                    diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error()));
            }
            return {
                .merge_attempt_ordinal = reconciled.reconciled->next_merge_attempt_ordinal,
                .diagnostic = std::move(reconciled.diagnostic),
            };
        }
        if (start_count >= store.state_->manifest.max_merge_build_attempts) {
            return fail_with(
                diagnostic(DistributedSieveWaveStoreStatus::invalid_request, protocol_error()));
        }
        return {
            .merge_attempt_ordinal = static_cast<std::uint32_t>(start_count),
            .diagnostic = {},
        };
    } catch (const std::bad_alloc&) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                                    std::make_error_code(std::errc::not_enough_memory)));
    } catch (...) {
        return fail_with(diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                                    std::make_error_code(std::errc::io_error)));
    }
}

} // namespace gnfs::sieve::distributed_sieve_resume_detail

namespace gnfs::sieve::distributed_sieve_worker_launcher_detail {

DistributedSieveWorkerLaunchSlotV1::DistributedSieveWorkerLaunchSlotV1(
    distributed_sieve_resume_detail::DistributedSieveWorkerAttemptStartReceipt&& receipt,
    std::vector<std::string> arguments) noexcept
    : receipt_(std::move(receipt)), arguments_(std::move(arguments)) {}

DistributedSieveWorkerLaunchRequestV1::DistributedSieveWorkerLaunchRequestV1(
    std::string executable_path, std::vector<DistributedSieveWorkerLaunchSlotV1> slots,
    DistributedSieveWorkerLauncherTestHooksV1 hooks) noexcept
    : executable_path_(std::move(executable_path)), slots_(std::move(slots)), hooks_(hooks) {}

DistributedSieveLaunchedWorkerAttemptV1::DistributedSieveLaunchedWorkerAttemptV1(
    std::unique_ptr<distributed_sieve_resume_detail::DistributedSieveWorkerAttemptStartReceipt>&&
        receipt,
    distributed_sieve_worker_process_detail::DistributedSieveWorkerProcess&& process) noexcept
    : receipt_(std::move(receipt)), process_(std::move(process)) {}

DistributedSieveLaunchedWorkerAttemptV1::~DistributedSieveLaunchedWorkerAttemptV1() noexcept {
    if (!terminal_reaped_ && receipt_ != nullptr) {
        // No terminal proof means the child may still hold or use fd 5. Leak
        // this receipt intentionally until process exit instead of releasing
        // its BaseLock early. The process member still closes its report
        // endpoint during ordinary member destruction.
        (void)receipt_.release();
    }
}

const AttemptStartedV1& DistributedSieveLaunchedWorkerAttemptV1::record() const noexcept {
    return receipt_->record();
}

distributed_sieve_resume_detail::DistributedSieveWaveStoreDiagnostic
DistributedSieveLaunchedWorkerAttemptV1::revalidate() const noexcept {
    return receipt_->revalidate();
}

distributed_sieve_worker_process_detail::DistributedSieveWorkerProcessId
DistributedSieveLaunchedWorkerAttemptV1::process_id() const noexcept {
    return process_.process_id();
}

int DistributedSieveLaunchedWorkerAttemptV1::report_descriptor() const noexcept {
    return process_.report_descriptor();
}

distributed_sieve_worker_process_detail::DistributedSieveWorkerProcessWaitResult
DistributedSieveLaunchedWorkerAttemptV1::wait_terminal(
    distributed_sieve_worker_process_detail::DistributedSieveWorkerProcessWaitTestHooks
        hooks) noexcept {
    const auto observed = process_.wait_terminal(hooks);
    terminal_reaped_ = terminal_reaped_ || observed.reaped;
    return observed;
}

int DistributedSieveLaunchedWorkerAttemptV1::release_report_descriptor() noexcept {
    return process_.release_report_descriptor();
}

} // namespace gnfs::sieve::distributed_sieve_worker_launcher_detail

namespace gnfs::sieve::distributed_sieve_resume_detail {

distributed_sieve_worker_launcher_detail::DistributedSieveWorkerLaunchBatchResultV1
DistributedSieveWaveStore::launch_worker_process_batch_v1(
    distributed_sieve_worker_launcher_detail::DistributedSieveWorkerLaunchRequestV1&& request,
    const DistributedSieveWorkIdentityV1& identity,
    const distributed_sieve_execution_policy_detail::DistributedSieveFrozenExecutionPolicyV1&
        frozen_policy,
    const core::PolynomialContext& polynomial,
    const factor_base::FactorBase& factor_base) const noexcept {
    namespace launcher = distributed_sieve_worker_launcher_detail;
    namespace process = distributed_sieve_worker_process_detail;
    namespace carrier = distributed_sieve_worker_work_package_file_detail;
    namespace execution = distributed_sieve_execution_policy_detail;

    auto consumed = std::move(request);
    launcher::DistributedSieveWorkerLaunchBatchResultV1 result;
    bool armed = false;
    launcher::DistributedSieveWorkerLaunchPhaseV1 active_phase =
        launcher::DistributedSieveWorkerLaunchPhaseV1::request_validation;
    std::size_t active_slot = launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT;
    bool batch_absence_unproven = false;

    // Once any carrier transaction succeeds, every failure remains
    // reconciliation-required until the complete batch has repeated its
    // directory/absence checks and revalidated every receipt.
    const auto set_failure = [&](launcher::DistributedSieveWorkerLaunchPhaseV1 phase,
                                 std::size_t slot) noexcept {
        result.diagnostic.phase = phase;
        result.diagnostic.slot = slot;
        result.diagnostic.reconciliation_required =
            result.diagnostic.reconciliation_required || batch_absence_unproven;
        result.disposition =
            armed ? launcher::DistributedSieveWorkerLaunchDispositionV1::armed_no_child
                  : launcher::DistributedSieveWorkerLaunchDispositionV1::failed_before_gate;
    };

    try {
        const std::size_t slot_count = consumed.slots_.size();
        if (state_ == nullptr || !process_matches(state_->creator_process_id) || slot_count == 0U) {
            set_failure(launcher::DistributedSieveWorkerLaunchPhaseV1::request_validation,
                        launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT);
            result.diagnostic.wave_store =
                process_matches(state_ != nullptr ? state_->creator_process_id : 0U)
                    ? diagnostic(DistributedSieveWaveStoreStatus::invalid_request,
                                 invalid_argument_error())
                    : process_mismatch();
            return result;
        }

        result.children.resize(slot_count);
        std::vector<std::unique_ptr<DistributedSieveWorkerAttemptStartReceipt>> receipt_anchors(
            slot_count);
        for (std::size_t index = 0; index < slot_count; ++index) {
            receipt_anchors[index] = std::make_unique<DistributedSieveWorkerAttemptStartReceipt>(
                std::move(consumed.slots_[index].receipt_));
        }
        std::vector<std::vector<std::byte>> bootstrap_frames(slot_count);
        std::vector<std::vector<std::string_view>> argument_views(slot_count);
        std::vector<process::DistributedSieveWorkerProcessSpawnSpec> spawn_specs;
        spawn_specs.reserve(slot_count);

        std::optional<std::uint32_t> prior_chunk_id;
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            auto& slot = consumed.slots_[index];
            auto& receipt = *receipt_anchors[index];
            const auto& record = receipt.record_;
            auto& child = result.children[index];
            child.chunk_id = record.chunk_id;
            child.attempt_ordinal = record.attempt_ordinal;
            child.attempt_started_digest = record.self_digest;

            const ChunkPlanV1* manifest_chunk = nullptr;
            for (const auto& candidate : state_->manifest.chunks) {
                if (candidate.chunk_id == record.chunk_id) {
                    manifest_chunk = &candidate;
                    break;
                }
            }
            const bool receipt_shape_valid =
                receipt.wave_store_state_.get() == state_.get() &&
                receipt.owned_by_current_process() &&
                receipt.creator_process_id_ == state_->creator_process_id &&
                receipt.base_lock_at_ != nullptr &&
                receipt.final_witness_.directory_identity.has_value() &&
                (!prior_chunk_id.has_value() || *prior_chunk_id < record.chunk_id) &&
                manifest_chunk != nullptr && manifest_chunk->sq_begin == record.sq_begin &&
                manifest_chunk->sq_end == record.sq_end &&
                record.attempt_ordinal < state_->manifest.max_worker_attempts &&
                record.manifest_digest == state_->manifest.self_digest &&
                record.retry_policy_version == state_->manifest.retry_policy_version;
            if (!receipt_shape_valid) {
                set_failure(launcher::DistributedSieveWorkerLaunchPhaseV1::request_validation,
                            index);
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error());
                return result;
            }
            prior_chunk_id = record.chunk_id;

            DistributedSieveProtocolRecordV1 sealed(record);
            auto encoded = encode_distributed_sieve_record(sealed);
            if (!encoded) {
                set_failure(launcher::DistributedSieveWorkerLaunchPhaseV1::request_validation,
                            index);
                result.diagnostic.protocol = encoded.status;
                return result;
            }
            bootstrap_frames[index] = std::move(*encoded.bytes);

            auto& views = argument_views[index];
            views.reserve(slot.arguments_.size());
            for (const auto& argument : slot.arguments_) {
                views.emplace_back(argument);
            }
        }

        for (std::size_t index = 0; index < slot_count; ++index) {
            spawn_specs.push_back({
                .executable_path = consumed.executable_path_,
                .arguments = argument_views[index],
                .bootstrap_frame = bootstrap_frames[index],
            });
        }

        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::process_preparation;
        active_slot = launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT;
        if (consumed.hooks_.force_fixed_capability_close_all_unavailable ||
            !process::distributed_sieve_worker_process_fixed_capability_close_all_supported()) {
            set_failure(active_phase, active_slot);
            result.diagnostic.transport = {
                .error = process::DistributedSieveWorkerProcessTransportError::platform_unavailable,
                .native_error = ENOTSUP,
            };
            return result;
        }
        auto prepared_processes =
            process::prepare_distributed_sieve_worker_process_batch(spawn_specs);
        if (!prepared_processes) {
            set_failure(active_phase, active_slot);
            result.diagnostic.transport = prepared_processes.diagnostic;
            return result;
        }

        std::vector<std::optional<carrier::DistributedSieveWorkerWorkPackageFileV1>> packages(
            slot_count);
        std::vector<process::DistributedSieveWorkerProcessFixedCapabilitySourcesV1> capabilities(
            slot_count);
#if !defined(_WIN32)
        std::vector<UniqueFd> attempt_directories(slot_count);
#endif

        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::work_binding;
        if (const auto status = validate_manifest_work_identity(state_->manifest, identity);
            !status) {
            set_failure(active_phase, active_slot);
            result.diagnostic.protocol = status;
            return result;
        }
        auto bound = execution::bind_distributed_sieve_work_v1(identity, frozen_policy, polynomial,
                                                               factor_base);
        if (!bound) {
            set_failure(active_phase, active_slot);
            result.diagnostic.protocol = bound.status;
            return result;
        }
        if (bound.work->work_digest != state_->manifest.work_sha256) {
            set_failure(active_phase, active_slot);
            result.diagnostic.protocol = {
                .error = DistributedSieveProtocolError::invalid_value,
            };
            return result;
        }

        // No failure before this point has crossed the one-shot receipt gate.
        // From here onward every receipt remains consumed on every outcome.
        armed = true;
        result.disposition = launcher::DistributedSieveWorkerLaunchDispositionV1::armed_no_child;

        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::initial_receipt_revalidation;
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            const auto checked = receipt_anchors[index]->revalidate();
            if (checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }
        }
        if (consumed.hooks_.after_initial_receipts != nullptr) {
            consumed.hooks_.after_initial_receipts(consumed.hooks_.context);
        }
        // The hook represents arbitrary concurrent namespace activity. Pin
        // every receipt again before the first carrier is allowed to create
        // even a transient named package.
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            const auto checked = receipt_anchors[index]->revalidate();
            if (checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }
        }

#if defined(_WIN32) || (!defined(__APPLE__) && !defined(__linux__))
        set_failure(launcher::DistributedSieveWorkerLaunchPhaseV1::attempt_directory_binding,
                    launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT);
        result.diagnostic.wave_store =
            diagnostic(DistributedSieveWaveStoreStatus::platform_unsupported, unsupported_error());
        return result;
#else
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            auto& receipt = *receipt_anchors[index];
            const auto& directory_leaf = receipt.worker_attempt_names_.private_directory_leaf;
            const NativeIdentityV1 expected_directory_identity =
                *receipt.final_witness_.directory_identity;

            active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::attempt_directory_binding;
            const int directory_fd =
                openat_retrying_eintr(state_->root_fd, directory_leaf.c_str(),
                                      O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
            if (directory_fd < 0) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
                return result;
            }
            attempt_directories[index].reset(directory_fd);
            if (const auto checked = validate_private_lease_directory_binding(
                    state_->root_fd, attempt_directories[index].get(), directory_leaf,
                    state_->creator_process_id, expected_directory_identity);
                checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }

            if (consumed.hooks_.before_carrier != nullptr) {
                consumed.hooks_.before_carrier(index, consumed.hooks_.context);
            }
            // The hook stands in for activity immediately before the named
            // carrier window. Re-pin both the receipt and its exact
            // root-relative directory binding while no package name exists.
            active_phase =
                launcher::DistributedSieveWorkerLaunchPhaseV1::initial_receipt_revalidation;
            if (const auto checked = receipt.revalidate();
                checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }
            active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::attempt_directory_binding;
            if (const auto checked = validate_private_lease_directory_binding(
                    state_->root_fd, attempt_directories[index].get(), directory_leaf,
                    state_->creator_process_id, expected_directory_identity);
                checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }

            active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::work_package_creation;
            auto created = carrier::create_distributed_sieve_worker_work_package_file_v1(
                {
                    .borrowed_attempt_directory_handle =
                        static_cast<carrier::DistributedSieveWorkerWorkPackageNativeHandle>(
                            attempt_directories[index].get()),
                    .expected_directory_identity = expected_directory_identity,
                    .creator_process_id = state_->creator_process_id,
                },
                identity);
            if (!created) {
                set_failure(active_phase, index);
                result.diagnostic.carrier = created.diagnostic;
                if (created.diagnostic.named_may_remain) {
                    result.diagnostic.reconciliation_required = true;
                    result.diagnostic.wave_store = diagnostic(
                        DistributedSieveWaveStoreStatus::reconciliation_required, protocol_error());
                }
                return result;
            }

            batch_absence_unproven = true;
            packages[index].emplace(std::move(*created.file));

            if (consumed.hooks_.after_carrier != nullptr) {
                consumed.hooks_.after_carrier(index, consumed.hooks_.context);
            }

            active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::attempt_directory_binding;
            if (const auto checked = validate_private_lease_directory_binding(
                    state_->root_fd, attempt_directories[index].get(), directory_leaf,
                    state_->creator_process_id, expected_directory_identity);
                checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }

            struct stat fixed_leaf_metadata{};
            if (fstatat_retrying_eintr(
                    attempt_directories[index].get(),
                    carrier::DISTRIBUTED_SIEVE_WORKER_WORK_PACKAGE_FILE_LEAF_V1.data(),
                    fixed_leaf_metadata) == 0) {
                set_failure(active_phase, index);
                result.diagnostic.reconciliation_required = true;
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::reconciliation_required, protocol_error());
                return result;
            }
            if (errno != ENOENT) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
                return result;
            }
        }

        // A later-slot hook may invalidate an earlier slot after its immediate
        // post-carrier check. Keep every directory descriptor pinned and close
        // the batch as one absence proof before assembling any capability.
        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::attempt_directory_binding;
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            auto& receipt = *receipt_anchors[index];
            const auto& directory_leaf = receipt.worker_attempt_names_.private_directory_leaf;
            const NativeIdentityV1 expected_directory_identity =
                *receipt.final_witness_.directory_identity;
            if (const auto checked = validate_private_lease_directory_binding(
                    state_->root_fd, attempt_directories[index].get(), directory_leaf,
                    state_->creator_process_id, expected_directory_identity);
                checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }

            struct stat fixed_leaf_metadata{};
            if (fstatat_retrying_eintr(
                    attempt_directories[index].get(),
                    carrier::DISTRIBUTED_SIEVE_WORKER_WORK_PACKAGE_FILE_LEAF_V1.data(),
                    fixed_leaf_metadata) == 0) {
                set_failure(active_phase, index);
                result.diagnostic.reconciliation_required = true;
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::reconciliation_required, protocol_error());
                return result;
            }
            if (errno != ENOENT) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::namespace_conflict, posix_error(errno));
                return result;
            }
        }

        // Receipt revalidation follows the complete absence sweep so a
        // directory or record change between those observations also keeps
        // the whole batch in reconciliation-required state.
        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::final_receipt_revalidation;
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            const auto checked = receipt_anchors[index]->revalidate();
            if (checked.status != DistributedSieveWaveStoreStatus::ready) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = checked;
                return result;
            }
        }
        batch_absence_unproven = false;
        for (auto& attempt_directory : attempt_directories) {
            attempt_directory.reset();
        }

        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::work_package_revalidation;
        for (std::size_t index = 0; index < slot_count; ++index) {
            active_slot = index;
            if (!packages[index].has_value()) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::unexpected_failure, protocol_error());
                return result;
            }
            const auto checked = packages[index]->revalidate();
            if (!checked ||
                packages[index]->witness().package.work_sha256 != bound.work->work_digest ||
                packages[index]->witness().creator_process_id != state_->creator_process_id) {
                set_failure(active_phase, index);
                result.diagnostic.carrier = checked;
                if (checked.status == carrier::DistributedSieveWorkerWorkPackageFileStatus::ready) {
                    result.diagnostic.protocol = {
                        .error = DistributedSieveProtocolError::digest_mismatch,
                    };
                }
                return result;
            }
            if (packages[index]->retained_reader_ < 0 ||
                packages[index]->retained_reader_ >
                    static_cast<carrier::DistributedSieveWorkerWorkPackageNativeHandle>(
                        std::numeric_limits<int>::max())) {
                set_failure(active_phase, index);
                result.diagnostic.wave_store = diagnostic(
                    DistributedSieveWaveStoreStatus::invalid_request, invalid_argument_error());
                return result;
            }
            capabilities[index] = {
                .wave_root_directory_descriptor = state_->root_fd,
                .permanent_wave_store_lock_descriptor = state_->lock_fd,
                .attempt_base_lock_descriptor = receipt_anchors[index]->base_lock_at_->lock_fd_,
                .work_package_reader_descriptor =
                    static_cast<int>(packages[index]->retained_reader_),
            };
        }

        active_phase = launcher::DistributedSieveWorkerLaunchPhaseV1::process_spawn;
        active_slot = launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT;
        const process::DistributedSieveWorkerProcessSpawnTestHooks spawn_hooks{
            .before_spawn = consumed.hooks_.before_spawn,
            .context = consumed.hooks_.context,
            .force_fixed_capability_close_all_unavailable =
                consumed.hooks_.force_fixed_capability_close_all_unavailable,
        };
        // Every launcher-owned container and receipt anchor is fixed before
        // this call. The post-spawn path performs only no-throw ownership
        // moves into already-sized result slots.
        auto launched = process::spawn_distributed_sieve_worker_process_batch_with_capabilities(
            std::move(*prepared_processes.batch), capabilities, {}, spawn_hooks);
        result.diagnostic.transport = launched.diagnostic;

        const bool closed_global_pre_spawn_failure =
            !launched.spawn_loop_entered && !launched.child_set_complete &&
            launched.children.empty() &&
            launched.diagnostic.error != process::DistributedSieveWorkerProcessTransportError::none;
        if (closed_global_pre_spawn_failure) {
            set_failure(active_phase, active_slot);
            return result;
        }

        bool complete_child_set =
            launched.child_set_complete && launched.children.size() == slot_count &&
            launched.diagnostic.error == process::DistributedSieveWorkerProcessTransportError::none;
        if (complete_child_set) {
            for (const auto& child : launched.children) {
                const bool has_process = child.process.has_value();
                const bool slot_succeeded =
                    child.diagnostic.error ==
                    process::DistributedSieveWorkerProcessTransportError::none;
                if (has_process != slot_succeeded ||
                    (!launched.spawn_loop_entered && has_process)) {
                    complete_child_set = false;
                    break;
                }
            }
        }
        if (!complete_child_set) {
            result.diagnostic.phase = active_phase;
            result.diagnostic.slot = launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT;
            result.disposition = launcher::DistributedSieveWorkerLaunchDispositionV1::indeterminate;
            if (result.diagnostic.transport.error ==
                process::DistributedSieveWorkerProcessTransportError::none) {
                result.diagnostic.transport = {
                    .error = process::DistributedSieveWorkerProcessTransportError::spawn_failed,
                    .native_error = EPROTO,
                };
            }
            // An incomplete or internally inconsistent child set cannot prove
            // which slots have live children. Quarantine every receipt rather
            // than release any possibly active BaseLock.
            for (auto& receipt_anchor : receipt_anchors) {
                (void)receipt_anchor.release();
            }
            return result;
        }

        std::size_t launched_count = 0;
        for (std::size_t index = 0; index < slot_count; ++index) {
            auto& source = launched.children[index];
            auto& destination = result.children[index];
            destination.transport = source.diagnostic;
            if (!source.process.has_value()) {
                if (result.diagnostic.slot == launcher::DISTRIBUTED_SIEVE_WORKER_LAUNCH_NO_SLOT) {
                    result.diagnostic.slot = index;
                    result.diagnostic.transport = source.diagnostic;
                }
                continue;
            }
            launcher::DistributedSieveLaunchedWorkerAttemptV1 combined(
                std::move(receipt_anchors[index]), std::move(*source.process));
            destination.worker.emplace(std::move(combined));
            ++launched_count;
        }

        if (launched_count == slot_count) {
            result.disposition = launcher::DistributedSieveWorkerLaunchDispositionV1::all;
            result.diagnostic = {};
        } else if (launched_count == 0U) {
            result.disposition =
                launcher::DistributedSieveWorkerLaunchDispositionV1::armed_no_child;
            result.diagnostic.phase = active_phase;
        } else {
            result.disposition = launcher::DistributedSieveWorkerLaunchDispositionV1::partial;
            result.diagnostic.phase = active_phase;
        }
        return result;
#endif
    } catch (const std::bad_alloc&) {
        set_failure(active_phase, active_slot);
        result.diagnostic.wave_store =
            diagnostic(DistributedSieveWaveStoreStatus::resource_exhausted,
                       std::make_error_code(std::errc::not_enough_memory));
        return result;
    } catch (...) {
        set_failure(active_phase, active_slot);
        result.diagnostic.wave_store =
            diagnostic(DistributedSieveWaveStoreStatus::unexpected_failure,
                       std::make_error_code(std::errc::io_error));
        return result;
    }
}

} // namespace gnfs::sieve::distributed_sieve_resume_detail
