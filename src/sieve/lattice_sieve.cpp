#include "gnfs/sieve/lattice_sieve.hpp"

// Implementation for LatticeSieve is entirely inline in the header.
// This file provides the compiled unit but no additional definitions are needed
// since all methods are defined in lattice_sieve.hpp.

namespace gnfs::sieve {

// All LatticeSieve implementation is in the header file (inline).
// This translation unit is kept for future non-inline implementations.

} // namespace gnfs::sieve
