name: Script Checks

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
    paths:
      - '.agents/**'
      - '.claude/**'
      - '.github/workflows/**'
      - '.gitattributes'
      - '.gitignore'
      - 'AGENTS.md'
      - 'CMakeLists.txt'
      - 'CLAUDE.md'
      - 'docs/harness-engineering.md'
      - 'include/**'
      - 'scripts/**'
      - 'src/**'
      - 'tests/*.sh'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: scripts-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  syntax:
    name: zsh/bash syntax
    runs-on: ubuntu-24.04
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install shells
        run: |
          sudo apt-get update
          sudo apt-get install -y zsh

      - name: Check script syntax
        shell: bash
        run: |
          set -euo pipefail

          while IFS= read -r file; do
            zsh -n "${file}"
          done < <(git ls-files 'scripts/*.sh' 'scripts/perf/*.sh' 'tests/*.sh' \
            | xargs grep -l '^#!/usr/bin/env zsh')

          while IFS= read -r file; do
            bash -n "${file}"
          done < <(git ls-files '.claude/hooks/*.sh' 'scripts/*.sh' 'scripts/perf/*.sh' 'tests/*.sh' \
            | xargs grep -l '^#!/usr/bin/env bash')

          python3 -m py_compile \
            .claude/hooks/project-guard.py \
            scripts/check_distributed_sieve_policy.py \
            scripts/check_harness.py \
            scripts/check_stdout_contract.py \
            scripts/release_binary_contract.py \
            scripts/release_contract.py \
            scripts/reproducible_archive.py \
            scripts/windows_release_runtime.py \
            scripts/windows_runtime_contract.py

      - name: Check release automation contracts
        run: |
          python3 scripts/release_contract.py self-test
          python3 scripts/release_contract.py check-workflows
          python3 scripts/release_binary_contract.py self-test
          python3 scripts/reproducible_archive.py self-test
          python3 scripts/windows_release_runtime.py self-test
          python3 scripts/windows_runtime_contract.py self-test

      - name: Check test catalog
        run: zsh ./scripts/test.sh list >/tmp/gnfs-test-list.txt

      - name: Check agent Harness
        run: |
          python3 scripts/check_distributed_sieve_policy.py --self-test
          python3 scripts/check_harness.py
          bash tests/test_harness_hooks.sh

      - name: Check stdout ownership
        run: |
          python3 scripts/check_stdout_contract.py --self-test
          python3 scripts/check_stdout_contract.py
