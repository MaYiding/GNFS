# GNFS Claude Code Entry Point

@AGENTS.md

Claude Code project integration lives in `.claude/`. Keep shared behavior in `AGENTS.md`; do not duplicate it here. Store personal permissions and machine-specific preferences only in the ignored `.claude/settings.local.json` or `CLAUDE.local.md`.
